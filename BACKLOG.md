# .dev-knowledge BACKLOG

## Big picture

`.dev-knowledge` is the ecosystem's methodology brain: it absorbs lessons from every repo, universalizes them into enforced conventions, and audits the ecosystem against them — the LLM-development "scrum master" for all `Dev/` projects (VISION: Knowledge Guardian · Methodology Author · Auditor · Disseminator). The backlog advances that mission across seven themes.

**Themes (backbone):** Handoff continuity · Enforced governance · Lessons feedback loop · Decision management · Canonical-file integrity · Cross-repo universalization · Tooling & evaluation

---

## Handoff continuity
> As a session inheriting this repo, I want to pick up with full state and lose nothing.

### Give every handoff a fresh-eyes review, not just the big ones
So that routine handoffs stop inheriting the ~25% insider blind spot.
- [#1] [P2][L] Extend triangulation from the promotion gate to every routine handoff (sub-prompt / periodic audit / agent-framework) · Done when: every Phase-2 handoff runs a fresh-eyes pass · refs protocols/AGENT_FRAMEWORK.md (Council) · Evidence: 2026-06-03 bundle review caught (A1) an unverified session-state claim bypassing the cross-check table and (A2) a "Tier" terminology collision across three axes — both defects that an adversarial fresh-eyes pass would catch (v4.3.2 §D) · serialize-group: handoff

### Let an inheritor act on the operator's defaults instead of re-asking
So that a fresh chat states-and-acts rather than over-asking on things it should assume.
- [#26] [P3][S] Add an operator-invariants section to the handoff (clean-tree, immediate-merge, three-domain) · Done when: the section ships in the handoff (now the v5 boot/residual — the v4 Phase-1 interview/bundle target is superseded; the goal persists) · refs HANDOFF_PROCESS.md (v5) · serialize-group: handoff

### Match the handoff payload to the work mode
So that an architecture session inherits the big picture + vision while an execution session inherits lean task-state — one process, two payloads.
- [#159] [P2][S] Operator-context beat for architect mode — a lightweight in-session step: after orienting (§13c) and before design begins, the browser makes ONE targeted ask for **off-repo** context (intent / priorities for this planning session / findings not in the repo / changed decisions). CC's handoff is repo-derived and structurally cannot carry operator intent or off-repo findings (local proof: this session's `profile ↔ repo` overlap finding was browser-side, nearly lost for want of a channel). **Architect mode ONLY** (execution unchanged — the strategic-vs-tactical split, human-in-the-loop steering at the planning layer); **off-repo only** (does NOT duplicate/re-narrate the CC-generated residual); a **posture step** in §13 + HANDOFF_BOOT.md, **not a new bundle file**, and **not** the heavy v4 eight-file interview (the residual fills the gaps now — this is the interview's operator-injection function without the gap-filling). 2026-06-11: the beat landed on the v5-**beta** track (HANDOFF_PROCESS.md §13(d), HANDOFF_BOOT architect posture, tests/test_handoff_modes.py); stays open pending real-architect-session use. **2026-06-12: the #149 v5→canonical carry is DONE** — v5 is canonical and the beat lives in the canonical spec §13(d); the only remaining clause is exercising it in a real architect session. **2026-06-16:** v5.1 made the off-repo context a first-class advisory supplement (the architect interview's Q6) and reconciled §13(d) to the lighter "changed since the supplement?" check (HANDOFF_PROCESS v5.1; ADR-82 amendment 2026-06-16; `92fa88e`/`929c91d`/`5191ce8`) — exercising the beat **and** the supplement interview in a real architect session is now this clause. **2026-06-17:** v5.2 made the supplement an **always-generated fillable file** and **defined the cold-handoff disposition** (a cold/`/clear`ed handoff commits an empty-ANSWERS supplement, folds nothing, and the incoming §13(d) beat fires full) — addressing the RESIDUAL §2 cold-handoff finding (HANDOFF_PROCESS v5.2; ADR-82 amendment 2026-06-17; `0e13038`). The remaining clause is unchanged: a real architect run with a *filled* supplement (the mechanism is now defined + hardened, not yet exercised). · Done when: the operator-context beat is exercised in a real architect session (the #149 v5→canonical carry clause is met) · refs #150, #149, protocols/HANDOFF_PROCESS.md §13, protocols/HANDOFF_BOOT.md, docs/audits/2026-06-11-surface-responsibility-audit.md (profile↔repo-overlap noted-limitation) · serialize-group: handoff
- [#161] [P3][S] **Capture only — do NOT codify**: define a stable teeth probe-core (the ~5 shared forced-read probes) plus the architect-only orientation probe, so bundles stop hand-assembling probe sets per handoff (today the architect bundle lacks execution's §5-labels probe; probe sets are assembled by hand). Thin signal — scoping is a future architect-session decision, not pre-committed here. · Done when: scoped by a future architect session (a stable probe-core + the architect orientation probe defined, or each explicitly deferred) · refs protocols/HANDOFF_PROCESS.md §13, protocols/HANDOFF_BOOT.md, #150, #159 · serialize-group: handoff
- [#162] [P2][M] Vocab decision (architect-session): disambiguate "architect" as the Layer-1 **actor** (ARCHITECTURE.md / ADR-28 — the browser-chat role) vs the handoff **mode** (HANDOFF_PROCESS.md §13 / HANDOFF_BOOT — architect|execution). Enumerate every surface where each sense appears and decide the disambiguation atomically (rename one sense, or formally scope both — do not leave the collision live). The boot-ack narrow slice shipped 2026-06-11 (HANDOFF_BOOT.md ack → "Layer-1 browser", no on-load mode claim); the model decision remains. · Done when: an ADR or operator ruling lands the disambiguation across all enumerated surfaces · refs ADR-28, protocols/HANDOFF_BOOT.md, protocols/HANDOFF_PROCESS.md §13, ARCHITECTURE.md Ch1 · serialize-group: handoff

### Finish the v5 handoff machinery deferred at the #149 flip
So that the canonical v5 handoff is fully implemented (generator) and mechanically verified (teeth), not just specified.
- [#164] [P2][L] Implement the v5 `/handoff` generator — at the #149 flip the `.claude/commands/handoff.md` body still carries the v4 8-file two-phase generator (marked SUPERSEDED) and SESSION_SETUP's two-phase flow likewise; the v5 emissions (residual + probe manifest + thin-boot pointer + lean task-state) are specified but not yet the wired generator. Replace the v4 Phase-1/2 mechanics with the v5 generator per `HANDOFF_PROCESS.md` §2–§10 (+ the architect|execution mode param, §13). · Done when: `/handoff` create/complete emit the v5 **four-file bundle** (`HANDOFF_BOOT.md` + `RESIDUAL.md` + `PROBES.md` + `PASTE_THIS.md`, **no per-bundle README**) with the session-header (slug · purpose · mode) in the bundle `HANDOFF_BOOT.md`, **idempotently seed/update each repo's `docs/handoffs/README.md` canonical runbook** from one source (`templates/handoff/v5/README.md.tmpl`, write-if-absent-or-version-changed), and the superseded v4 generator prose is removed from the command + SESSION_SETUP · refs protocols/HANDOFF_PROCESS.md §2–§10/§13, .claude/commands/handoff.md, protocols/SESSION_SETUP.md, templates/handoff/v5/README.md.tmpl, #150, #163 · 2026-06-12 (operator-first README dependency): the generator MUST emit the bundle README in the operator-first shape — `templates/handoff/v5/README.md.tmpl` — walkthrough-first (who-each-file-is-for · the role-file vs bundle-pointer same-name collision · PROBES-handed-once · the run loop with its why), rationale demoted; NOT the rationale-first shape the hand-assembled bundles carried. Operator-first redesign + a rendered reference for session-2 landed 2026-06-12 (`docs/handoffs/_references/2026-06-12-dev-knowledge-session-2-README-reference.md`); the generator wiring of this shape is this item's work. · 2026-06-12 (canonical runbook supersedes the per-bundle README): the per-bundle README is dropped entirely — the stable operator boilerplate now lives once in the canonical per-repo runbook `docs/handoffs/README.md` (HANDOFF_PROCESS §13). The generator therefore emits NO per-bundle README; it seeds/updates each repo's runbook idempotently from the single source `templates/handoff/v5/README.md.tmpl` (reduced to a deferred stub until this lands) and puts the session-header in the bundle `HANDOFF_BOOT.md`. The session-2 rendered reference under `docs/handoffs/_references/` is superseded by the canonical runbook and removed. · 2026-06-12: ADR-83 inventory correction — the v4 `templates/handoff/` bundle templates are **live for v4 repos** (corp-monorepo + any v4 repo run the v4 Phase-1/2 cross-repo path), NOT dead; they were archived prematurely by the #149 flip and have been **restored** to `templates/handoff/` (branch `fix/restore-v4-handoff-templates`), with the v4 README stamp pinned to literal v4.4/live. Re-archival of these templates (and removal of that stamp pin) is **gated to this item's close** — when the repo-parameterized v5 cross-repo generator lands and corp-monorepo migrates off v4. Also surfaced for this item: the v5 *routing* ambiguity — a v4 target repo has no deterministic route to the v4 path (an executing agent infers it because v5 emissions can't bind a target lacking `scripts/audit.py`); the repo-parameterized generator is the durable fix. · 2026-06-15 (audit F4 — concrete instance for this item): the operator-paste slice shipped early (`scripts/assemble_paste.py` + `PASTE_THIS.md`, four-file bundle), but `PASTE_THIS.md` has **no freshness gate vs its sources** — `handoff_probes` validates `PROBES.md`, not `PASTE_THIS`'s embedded copy, so the assembled snapshot can silently drift if a source changes without re-running the assembler. Live drift example: the architect bundle's embedded P8 "three files" hint is already stale (that bundle now has five files). The generator (this item) should regenerate `PASTE_THIS.md` as part of emit; a read-only freshness check (`PASTE_THIS` vs its sources, like `validate_doc_claims`) is the durable fix. · 2026-06-16 (v5.1 dependency): the architect strategic supplement landed (HANDOFF_PROCESS v5.1) — `templates/handoff/v5/SUPPLEMENT.md.tmpl` (the 6-question interview schema) + `scripts/assemble_paste.py` architect-mode warn-if-absent. The generator must, in architect mode, emit the interview block from that template and write the browser's answers to `<bundle>/SUPPLEMENT.md`; the schema is kept generic/portable so this repo-parameterized generator carries it. · 2026-06-17 (v5.2): the supplement is now an **always-generated fillable file** — the generator must, in architect mode, **write `<bundle>/SUPPLEMENT.md` unconditionally** (QUESTIONS + an empty ANSWERS section) and commit it on the handoff branch, NOT emit an ephemeral interview block; the assembler folds the **ANSWERS region only, only when non-empty** (HANDOFF_PROCESS v5.2). · serialize-group: handoff

---

## Enforced governance
> As the operator, I want load-bearing conventions enforced by tools, not memory, so they can't silently drift.

### Turn advisory guards into enforced gates
So that a convention can't be skipped under load (the failure mode behind real aborts).
- [#146] [P3][S] De-hardcode-first doctrine + sweep — the `amendment_coherence` gate (#11, shipped) is a BACKSTOP; the superior fix for a coupled version surface is to de-hardcode it so it interpolates the anchor (the v3.4 handoff surfaces already do — `handoff.md`/templates read `{{VERSION}}`). #11's honest limit names de-hardcoding as the actual v3.4 fix, so per ADR-81 (d) that deferral must be tracked, not left as a disclaimer: (a) record de-hardcode-first as doctrine in PLAYBOOK "Multi-surface amendment coherence"; (b) sweep remaining hand-maintained version surfaces for de-hardcoding candidates · Done when: de-hardcode-first is doctrine AND a sweep is run (each candidate de-hardcoded, or explicitly kept-as-manifest with a reason) · refs #11, ADR-81 (d), LESSONS 2026-05-29 (v3.4 abort) · serialize-group: playbook
- [#15] [P2][M] Add hyphen-only-separator enforcement (pre-commit + Action) with a scoped path set + exceptions · Done when: a non-conforming new filename is blocked · refs ADR-34 · serialize-group: pre-commit-config
- [#105] [P2][S] PreToolUse immutability guard — block Edit on `docs/decisions/*`, transcripts, handoffs, audits (new-file creation allowed); extends the block-onedrive pattern; immutable paths become a second zone class under the ADR-75 register (amending ADR) · Done when: an in-place edit attempt on an existing ADR is blocked fail-closed and a new-ADR creation passes · 2026-06-06 PARTIAL (stays open): transcripts portion shipped — `scripts/hooks/block_immutable_edits.py` live + wired + witnessed, ADR-77 amends ADR-75; handoffs/audits REMOVED from scope (sanctioned append / dated-addenda flows); ADR portion re-routed to #112 (Option A script-enforced amend path, per the operator ruling — Option B held until #23)
- [#112] [P2][M] adr_amend helper + ADR immutable-zone extension — `scripts/hooks/adr_amend.py` becomes the ONLY sanctioned writer of an Amendment/status line on an existing ADR (canonical format from ADR-68's formal Amendment + HANDOFF v4.3 Amendment A); extend `block_immutable_edits.py` to deny every OTHER in-place ADR edit (fail-closed); resolve the CLAUDE.md §5 self-contradiction ("supersede with a new file or in-file marker; never edit in place" → "append-only via the helper"); includes the CLAUDE.md end-to-end re-read + `last_reviewed` restamp · Done when: an in-place ADR edit NOT routed through the helper is blocked, a helper-written Amendment passes, and CLAUDE.md §5 is de-contradicted + restamped · refs ADR-77, #105, #23, ADR-68 · changelog 2.1.148-167 finding — native deny rules strengthened (v2.1.166/2.1.163): glob in tool-name position, `$HOME`-referencing Bash commands now matched by path deny rules; evaluate a native deny on the transcripts zone as a complement for the documented shell-write residual (C2) — not a replacement (deny rules cannot express exists-deny/new-allow) · audit-trio finding (docs/audits/2026-06-07-platform-max-audit.md UN-7/KILL-4/KILL-1) — `permissions.deny` path-globs as a defense-in-depth layer for BOTH zones (transcripts AND OneDrive); KILL-4: deny-glob on arbitrary Bash is historically bypassable → defense-in-depth only, never a replacement for the PreToolUse guards; KILL-1: native OS sandbox is macOS/Linux/WSL2 only — NOT available on native Windows, so the guards stay · depends-on: #23 · serialize-group: claude-md
- [#153] [P2][M] Enforcement-completeness pass — move browser-re-stated constraints to CC-side mechanical enforcement (the browser repeating a rule each session is the symptom of a missing hook). Priority: a `--no-ff`-merge ff-guard — core-invariant #5 is prose-only today, so a fast-forward into `main` can slip through; then evaluate mechanizing minimal-diffs / append-only (LESSONS/TOKEN-LOG/JOURNAL) / no-CHANGELOG-recreation, or explicitly accept each as prose-only. (FF-guard shipped 2026-06-11 as the `no_ff_merges` audit check — scripts/validate_no_ff.py: hub-only, detect-and-surface WARN, pre-2026-06-11 history grandfathered, ADR-80 automation allowlisted; true FF *prevention* via a pre-push hook is noted as deferred machinery, not built. The remaining prose-only constraints — minimal-diffs / append-only / no-CHANGELOG-recreation — are not yet mechanized-or-accepted, so this item stays open.) · Done when: the --no-ff ff-guard ships (or is explicitly deferred) AND each remaining prose-only constraint is mechanized or recorded as accepted-prose-only with a reason · refs docs/audits/2026-06-11-architecture-coherence-audit.md (scope E), LESSONS 2026-06-11 (constraints belong CC-side), ~/.claude/rules/core-invariants.md #5 · serialize-group: audit-py

### Extend structural validation to more governance artifacts
So that drift in transcripts, ADRs, and folders is caught cheaply, not by reviewer luck.
- [#7] [P2][M] Apply the audit.py #8 structural-validator pattern to Council transcripts + ADRs (frontmatter/supersession/template) · Done when: registered checks + tests folded into health · refs scripts/audit.py #8/#9 · serialize-group: audit-py
- [#36] [P3][M] Add read-only check_folder_semantics asserting each docs/ subfolder carries one ADR-60 role · Done when: the check ships with fixtures + tests · refs ADR-60 · serialize-group: audit-py
- [#42] [P3][S] Decide whether mermaid-theme check #7 expands beyond ARCHITECTURE.md (extend paths or accept) · Done when: scope decision recorded · refs durability-audit J4
- [#95] [P3][M] Template↔copy orchestration drift watcher (G5) — per ADR-73, per-repo orchestration copies propagate at rollout moments but no organ detects a copy diverging from the hub canonical template between rollouts; candidates: version/hash header in copies + deterministic check, or a rollout-checklist item · Done when: a deterministic, read-only check (or ratified checklist step) detects template↔copy divergence and surfaces it · refs ADR-72, ADR-73, ADR-74 · serialize-group: audit-py
- [#77] [P3][L] protocols/ content consolidation — operator-gated pass to resolve the rot the 2026-06-03 audit surfaced: extract PLAYBOOK §18 (hub-only audit.py/ecosystem tooling) from the universal protocol; reconcile the three divergent lesson→rule statements (PLAYBOOK L1687 vs L2318 vs ESSENTIALS L393) into one canonical statement; trim ESSENTIALS to its "under 1 page" contract; strip the 10+ PLAYBOOK Section-history blocks (ADR-49-retired changelogs) · Done when: rot end-state is gone — §18 extracted from the universal protocol, the three lesson→rule variants reconciled into one canonical statement, ESSENTIALS within its 1-page contract, and the PLAYBOOK Section-history blocks stripped — each operator-approved before landing · refs ADR-49, docs/audits/2026-06-03-protocols-rot-audit.md · [CLOSURE-VOIDED 2026-06-09: never removed from BACKLOG; 77e5d7d closes [#77] was a misattribution — shipped CONTRIBUTING→v4, not this work; doc-rot checker split into #140] · serialize-group: playbook
- [#140] [P3][L] doc-rot checker — build read-only WARN-only checks in audit.py (and/or the ADR-80 Routine path) covering: intra-file duplication, file bloat/size, per-section "Section history" accumulation, and cross-file summary-fidelity drift · Dependency: reconcile scope with #89 (prose-vs-state checker) BEFORE building — cross-file fidelity drift appears in both; decide the seam to avoid two overlapping validators · Done when: each of the four rot categories (intra-file duplication, file bloat, per-section changelog accumulation, cross-file fidelity drift) is either shipped read-only + WARN-only meeting ADR-81 DoD, or explicitly deferred to a named successor item; and the #89 scope seam is documented · refs ADR-80, ADR-81, ADR-49, #89, docs/audits/2026-06-03-protocols-rot-audit.md · [split from #77 2026-06-09] · serialize-group: audit-py
- [#139] [P2][L] merged-arc→record verifier (a.k.a. **#90b** — direction (b), deferred from #90 which shipped direction (a) as `scripts/validate_git_backlog.py` + the `git_backlog_drift` audit check) — every merged arc on `main` maps to a backlog item, a closure, or a documented no-item class (cross-repo arc per ADR-41, doc/chore, routine baseline), by inspecting the arc's **content** (`<merge>^1..<merge>^2` work commits + the merge body), **NOT** the merge subject: the #90 field finding showed /ship merge subjects are never Conventional-Commits-prefixed (`Merge <branch> — …`), so a `^(feat|fix)` subject predicate matches nothing = a vacuous pass · Done when: the verifier surfaces a seeded merged-arc lacking an item/closure/no-item-class, runs read-only (Layer-2), and is demonstrably NOT vacuous against real /ship merge subjects · refs ADR-65, ADR-41, #90 (direction (a) shipped), this session's merge-subject finding · serialize-group: audit-py
- [#99] [P3][S] FLEET-HEALTH digest names the failing check per red repo — today the digest shows `corp-monorepo !! 1 fail` with no check name; the operator needs a second command (`audit.py repo <name>`) to learn WHAT is red · Done when: a red repo's digest line carries the failing check name(s) · refs ADR-76, #85, ADR-36
- [#132] [P2][M] Organ-index generator — `scripts/generate_organ_index.py` (read-only, codemap/toc pattern — Layer-2-safe) walks `.claude/{agents,commands,skills,workflows}`, the plugin manifest, `settings.json` hooks, `.pre-commit-config.yaml`, and user-level `~/.claude` → emits `docs/ORGAN-INDEX.md` (name / class / trigger / source / distribution / status) + a freshness gate (the codemap/toc-freshness hook pattern); kills "operator-as-registry" · Done when: the generator emits `docs/ORGAN-INDEX.md` covering all organ classes and a freshness hook flags a stale index · refs ADR-71, ADR-74, docs/audits/2026-06-07-copilot-collections-peer-audit-v2.md §3.6 roster table (seed shape), #95 · serialize-group: pre-commit-config
- [#166] [P3][M] doctrine_enforcement_coherence check — a read-only `audit.py` check flagging any ADR/amendment whose status still contains `PROPOSED` / `NOT RATIFIED` while its implementing task is closed (or its enforcement is already live in a gate) — i.e. live enforcement running ahead of its doctrine (the #156 gap this session reconciled by hand) · Done when: the check ships read-only with fixtures + tests, folds into `audit.py health`, and flags a seeded enforcement-ahead-of-doctrine case · refs #11 (amendment_coherence — version-straggler sibling), #153 (enforcement-completeness), ADR-65 (done-items-leave), this #156 gap · serialize-group: audit-py
- [#167] [P3][S] Extend the #156 task-graph schema to capture multi-surface collisions — `validate_backlog._parse_serialize_group` uses `.search()`, so only the FIRST `serialize-group` clause per task is read; a task that collides on TWO shared surfaces cannot be fully encoded (single-group-only limit). Evaluate allowing multiple `serialize-group` clauses per task (or complementing the edge with `depends-on`). Evidence: the 2026-06-14 collision-encoding pass dropped two real edges — #105↔#112 (both edit `scripts/hooks/block_immutable_edits.py`) and #5↔#77 (both edit `protocols/ESSENTIALS.md`) — because each task's second shared surface had nowhere to go · Parser-hardening sub-item (cite: 2026-06-14 incident): the clause parser (`_parse_serialize_group`, `.search()`) substring-matches the bare token, so this very task's prose naming the clause self-tripped it (phantom group + cp1252 print-crash, worked around by de-tokenizing the wording) — anchor the parser to the clause position/delimiter so free-text mentions of the keyword don't register as clauses · Done when: a go/no-go on multi-`serialize-group` support is recorded, and if yes the parser + `serialize_groups()` summary + tests handle ≥2 groups per task, AND the clause parser is position/delimiter-anchored so prose-tokens cannot trip it (the 2026-06-14 self-trip cannot recur) · refs ADR-66, scripts/validate_backlog.py, #156, the 2026-06-14 collision-encoding pass

### Wire up the lifecycle hooks the workflow relies on
So that session-start/close automation actually runs instead of being wired-but-vacuous.
- [#116] [P3][S] Hooks hygiene — `shell:"powershell"` + `args:[]` exec-form migration: replace wrapper-string invocations on our PowerShell hooks with the array exec-form to kill the Windows quoting failure class; note the `.cmd` shim caveat (per the hooks doc); also adopt `if:` conditional filters on PreToolUse guards to scope spawn overhead · Done when: our PS hooks use exec-form `args:[]` and at least one PreToolUse guard carries an `if:` scope filter · refs docs/audits/2026-06-07-platform-max-audit.md UN-5/UN-6 · serialize-group: settings-json
- [#117] [P3][S] Evaluate prompt/agent-based hooks (`type:"prompt"` Haiku-eval / `type:"agent"` multi-turn, experimental) — candidate: run the Tier-1 closure/triage eval in-session via a `type:"prompt"` hook instead of a Python shell-out; EXPERIMENTAL flag; GATED on the VF-1 auth check (add a trivial `type:"prompt"` PreToolUse returning approve, run any tool call, confirm NO auth error under the empty-key billing route) · Done when: VF-1 passes and a go/no-go on a prompt-hook Tier-1 eval is recorded · refs docs/audits/2026-06-07-platform-max-audit.md AD-2, VF-1, #8 · serialize-group: settings-json
- [#168] [P3][S] Harden the ADR-85 BACKLOG leg from advisory to a hard gate — replace the interim structural-marker check in `check_backlog_marker()` with the airtight issue-ID↔commit anchor once the traceability-spine ADR lands (R1 co-design); blocked on #170 (the spine ADR). · Done when: the BACKLOG leg blocks turn-end on a missing issue-ID↔commit link (not just a marker present) and the advisory-only interim is retired · refs docs/decisions/ADR-85-session-lifecycle-enforcement.md (R1), scripts/session_end_backpressure.py, #8 · depends-on: #170
- [#170] [P3][M] Design + land the traceability-spine ADR (issue-ID↔commit anchor) that #168 depends on — the airtight, always-warranted link that promotes ADR-85's BACKLOG leg from advisory to a hard gate. The spine is the un-gameable AND always-warranted anchor the structural-marker check lacks. · Done when: an ADR defines the issue-ID↔commit linkage and #168 has a ratified anchor to depends-on · refs docs/decisions/ADR-85-session-lifecycle-enforcement.md (R1), #168, #8
---

## Lessons feedback loop
> As the methodology author, I want lessons to flow back into enforced rules, not sit in an archive.

### Make lessons an active feedback loop, not a passive archive
So that captured lessons reach runtime rules and stay enforceable.
- [#4] [P2][M] Build lessons-index.json + SessionStart retrieval + CLI query · Done when: lessons are queryable + surfaced at session start · refs ADR-35, ADR-70 (Tier-1 lesson-promotion)
- [#130] [P3][S] Memory-hygiene review — periodic pass over the CC auto-memory + the `gotchas` skill: flag duplicate/overlapping entries, stale or RETIRED cleanup candidates, and MEMORY.md index cap-proximity; surface candidates as a digest into the morning funnel for ratification (operator-gated — never auto-delete, per the no-delete invariant) · Done when: one hygiene pass runs and emits a ratify-only candidates digest (duplicates / stale-RETIRED / cap-proximity) · refs MEMORY.md, ~/.claude/skills/gotchas, #4
- [#134] [P2][S] Backlog-grooming review — periodic operator-invoked pass over BACKLOG: deterministic signals (item age, dead refs, dependency closed/withdrawn) + LLM obsolescence proposals → ratify-only digest → one removal commit + grooming-log append (operator-gated — never auto-delete, per the no-delete invariant) · Done when: one grooming pass runs and emits a ratify-only candidates digest (age / dead-ref / closed-dependency) · refs BACKLOG.md, #130 (family pattern), ADR-65 · 2026-06-07 n=1 field-run design input: deterministic pre-filter = 3 signals AS FLAGS (never auto-kills) — (1) retired-token grep (`/boot`, `/evolve`, `AGENTS.md`, night-agent, withdrawn-ADR nums, repo-tier), (2) ref-resolver (resolve each `refs ADR-NN`/path; an unresolved cross-repo ref = "verify elsewhere", not a kill), (3) orphan-story check (a task removal leaving a 0-task story); anti-signals (NEVER auto-kill) — dependency-closed usually means UNBLOCKED → emit an "unblocked?" nudge, item-age is never standalone. The substantive removals (done-undetected, decided-by-arc) need git-history + cross-ADR judgment — not mechanizable, invisible to propose_closures. Flow: deterministic flags → LLM judgment (done-undetected / merge / stale) → operator ratify-gate → one removal commit + grooming-log. Cadence: POST-ARC (obsolescence is event-driven — the n=1 calendar-style pass on a 2026-06-05-groomed backlog found 0 deterministic kills) + quarterly backstop (next 2026-07-01). n=1 evidence: 78→67 tasks, 0 hard-kills-from-signals, all substance via judgment

### Codify recurring patterns into the methodology
So that observed failure-modes become written guidance instead of recurring.
- [#67] [P3][S] PLAYBOOK §7 polish: skill/command naming-convention note + orchestrator/worker how-to · Done when: both notes land in PLAYBOOK §7 · refs methodology-audit C10/C12 · serialize-group: playbook
- [#143] [P3][M] Methodology internalizes parallel-work as a first-class mode — evaluate whether the routing rule / sequencing doctrine / prompt-format must treat parallel worktrees as a first-class option, not just a how-to a session reaches for by hand: should the architect's prompt-format or routing step ASK "is this parallelizable?" and emit a two-stream plan when the decide-first checks pass? · Done when: a go/no-go is recorded on whether parallel-work becomes a first-class routing/sequencing/prompt-format element (vs staying a PLAYBOOK how-to) · refs PLAYBOOK "Parallel sessions & worktree discipline", #111, ADR-81 · origin: 2026-06-10 worktree-codification deferral
- [#144] [P3][M] Feature DoD = end-to-end / user-flow test — sharpen ADR-81 (d) "deployed": a feature is not done until an automated test exercises the WHOLE sequence as a user would, not only unit tests. VERIFY-FIRST with operator: does "in the cloud" mean CI / GitHub Action or cloud CC sessions? — the answer sets where the E2E test runs · Done when: ADR-81 (d) carries an E2E/user-flow clause (or an explicit deferral) AND the "in the cloud" target is resolved · refs ADR-81, PLAYBOOK "Definition of done", #143 · origin: 2026-06-10 worktree-codification deferral
- [#145] [P3][M] Codification-completeness pass — audit what else a fresh session CANNOT do from PLAYBOOK + the handoff bundle alone (the fresh-session-can't-do-X test, per the sharpened ADR-81 (a)); pattern alarm: 3 transmission gaps surfaced this session (child-floor deploy, rollout ambiguity, worktree parallelization) · Done when: a read-only pass enumerates fresh-session transmission gaps across PLAYBOOK/bundle and each is filed or closed · refs ADR-81 (a), #124, #131, LESSONS 2026-06-10 worktree transmission gap · origin: 2026-06-10 worktree-codification deferral

---

## Decision management
> As a reader of 65+ ADRs, I want decisions navigable and free of silent contradiction.

### Keep the decision corpus navigable and contradiction-aware
So that growth past 65 ADRs doesn't bury or quietly contradict prior decisions.
- [#2] [P2][L] Add contradiction-detection across decisions + an ownership model (amend vs new ADR vs clarification) · Done when: a detection mechanism + ownership rule exist · refs ADR-64 (consolidated-index sub-item done)
- [#23] [P3][M] Build a navigable supersedes/related/amends graph from ADR frontmatter (or an index doc) · Done when: the graph renders + is freshness-checked · refs posture-audit I1

### Codify the meta-decision rules
So that future sessions route decisions consistently (convene-vs-Path-A; relax-vs-gate).
- [#18] [P2][M] Codify the convene-vs-Path-A criterion in AI_COUNCIL_PROCESS/PLAYBOOK · Done when: the criterion is written · refs ADR-62/63/65 · serialize-group: playbook
- [#27] [P3][M] Codify the cost/value-asymmetric relax-vs-gate criterion for drifted guards · Done when: a LESSON/ADR records the criterion · refs ADR-62/63, coherence-audit ML-2

### Operationalize the Council decision loop
So that Council questions are gated and their verdicts return deterministically instead of being shuttled by hand.
- [#70] [P3][M] Operationalize the AI-Council process — implement the gated question→verdict→ADR loop per ADR-67 (templated question + self-gate + deterministic `council.return_dir` return; downstream pieces in ai-council + ~/.claude). Gate lifted 2026-06-02 — Phase-2 universalization is complete (all 5 repos unified), so this is now actionable. · Done when: the gated loop runs end-to-end (/council-question generates + self-gates → council runs → operator pastes verdict → ADR returns via council.return_dir) · refs ADR-67
- [#96] [P2][M] ai-council VERDICT marker (producer-side) — synthesizer emits a machine-readable VERDICT block at the top of every transcript (verdict per question, one-line rationale, panel/cost metadata); consumers (distillation prompts) read only the block · Done when: a council run produces the block and a distillation completes without reading the debate body · refs ADR-74 (marker doctrine), PLAYBOOK context-budget rule; note: implementation lands in the ai-council repo via its dedicated chat (ADR-41) — this is the ecosystem queue entry; refs round-2 isolation audit (#110)
- [#110] [P3][S] ai-council round-2 isolation audit — verify whether round-2 debaters see round-1 peer arguments; 2026 evidence: 58% sycophantic convergence in debate, 23.9% unanimous-wrong by round 3; confirm blind-vote isolation properties hold for our 2-round design · execute in the ai-council dedicated chat · Done when: isolation properties confirmed or a remediation is recorded · refs #96, ADR-03, research transcript 2026-06-06 · 2026-06-07 groom: ADR-03 is an ai-council-repo ADR (no such ADR in dev-knowledge/docs/decisions) — verify the ref in the ai-council chat
- [#128] [P3][S] ai-council prompt caching — apply `cache_control` to the repeated brief/persona blocks reused across providers and debate rounds, cutting input-token cost on multi-round debates · **execute in the ai-council dedicated chat** (ADR-41 — queue-only here) · Done when: a council run reuses cached brief/persona blocks and the input-token saving is recorded · refs Anthropic API prompt-caching docs, #96, #110

### Close the small ADR cross-reference + registry amendments
So that the ADR web is internally consistent.
- [#19] [P3][M] Complete the ADR-39 register — (a) add a BACKLOG.md lifecycle entry (formerly bundled with #20); (b) decide register/exempt/hybrid for the non-handoff templates/ class (merged from #21, 2026-06-07 groom) · Done when: ADR-39 registry includes BACKLOG.md AND the templates/ class decision is recorded · refs ADR-39/41
- [#165] [P3][S] Ratify the diagram-format selection rule (ASCII vs mermaid) as an **ADR-51-family amendment** — operator-set 2026-06-12; **propose for ratification, do NOT unilaterally mint a new ADR**. Rule: **ASCII sequence / numbered list** for small linear diagrams (≤~6 steps, single actor, no branch or loop to draw — especially terminal / plain-text-facing); **mermaid** for branching, ≥2 actors/lanes, a loop that must be drawn, >~6–8 nodes, OR any persistent rendered repo doc (tie-break to mermaid even if borderline-small). First application: the operator-first v5 handoff README (`templates/handoff/v5/README.md.tmpl`) chose mermaid under this rule (durable rendered doc + a loop + a non-linear split). · Done when: the selection rule is ratified (operator ruling or ADR-51-family amendment) and recorded where the diagram convention lives (ARCHITECTURE.md §Output formatting / ADR-51) · refs ADR-51, ADR-59, ARCHITECTURE.md §Output formatting, templates/handoff/v5/README.md.tmpl, #164 · serialize-group: architecture

---

## Canonical-file integrity
> As any agent reading this repo, I want the canonical files accurate and current.

### Keep canonical files accurate
So that stale ground truth stops silently misleading sessions (the repo's own VISION is "drift detected proactively").
- [#10] [P2][M] Align TOKEN-LOG doc references to HANDOFF_PROCESS §14 — the token-log cadence now lives in HANDOFF_PROCESS §14 (relocated from PLAYBOOK §8 by #152). Placement is settled by root-hygiene: ADR-59's 2026-06-01 amendment puts usage/telemetry logs under `logs/` (a subdir, NOT the canonical root), and the file already sits correctly at `logs/TOKEN-LOG.md` — so the original "move-to-root vs `logs/`" question lands on `logs/` per the existing convention, no file move. Remaining scope is doc-alignment: (a) repoint any cadence reference to HANDOFF_PROCESS §14; (b) confirm CLAUDE/ARCHITECTURE/PLAYBOOK name the file as `logs/TOKEN-LOG.md`, not bare. · Done when: cadence references point to HANDOFF_PROCESS §14 and the path is named `logs/TOKEN-LOG.md` consistently across CLAUDE/ARCHITECTURE/PLAYBOOK · refs coherence-audit 2026-06-01, ADR-59 amendment 2026-06-01, HANDOFF_PROCESS §14 (#152) · serialize-group: handoff
- [#35] [P3][S] Fix the self-owned low-severity cleanups (ARCHITECTURE diagram attribution + SBAR label; VISION adoption-signal + stale last_reviewed) · Done when: WF-3 + GO-1/2 resolved · refs coherence-audit · serialize-group: architecture
- [#71] [P3][S] Reconcile ENVIRONMENT.md's `~/.claude/` directory tree with live contents (commands = codex-review + session-summary only — boot/evolve archived 2026-06-05 Phase-C3, target re-scoped 2026-06-07 groom; skills = gotchas + verify; agents/hooks already match) · Done when: the ENVIRONMENT `~/.claude/` tree matches `ls ~/.claude/{commands,skills}` · refs G6 process-hardening sweep · serialize-group: environment
- [#157] [P3][S] Collapse the intra-CLAUDE.md file-lifecycle restatement — §4 Conventions "File lifecycle" bullet (line 54) AND §5 Critical rules #1–3 (lines 69–71) both authoritatively define the append-only/immutable disposition in full: the resident-copy-drift class inside a single file. Keep one location as the authority and make the other a pointer (do NOT edit the rule's content, per source→gate→agent). Coordinate with #112 (also edits §5 — the supersede-vs-never-edit contradiction) to avoid a collision; includes the CLAUDE.md end-to-end re-read + last_reviewed restamp. · Done when: CLAUDE.md states the file-lifecycle disposition once (authority) with the other location reduced to a pointer, §4/§5 no longer both define it, and last_reviewed is restamped after a genuine re-read · refs docs/audits/2026-06-11-surface-responsibility-audit.md (FLAG-1), #112, #140, ADR-29, ADR-39 · serialize-group: claude-md
- [#169] [P3][M] Ungated-doc staleness detection (ADR-85 R2) — surface ARCHITECTURE/VISION/LESSONS/CONTRIBUTING staleness (e.g. untouched >N sessions while code changed) as a deterministic signal in the daily digest / conformance dashboard, NOT a per-session gate and NOT human memory; rides with the conformance-dashboard work. · Done when: a deterministic staleness signal for the four ADR-85-ungated docs lands in the digest/dashboard · refs docs/decisions/ADR-85-session-lifecycle-enforcement.md (R2), scripts/fleet_health.py, #166 · depends-on: #171
- [#171] [P3][M] Build the conformance dashboard at `ecosystem/conformance.md` (ADR-86 / ADR-85 R2) — a read-only validator generates it and commits its own output (ADR-80 committed-generated-zone writer policy); this is the deterministic surface #169's ungated-doc staleness signal lands in, and ARCHITECTURE Ch2 points to it. · Done when: `ecosystem/conformance.md` is generated + committed by a read-only validator (Layer-2-safe) and ARCHITECTURE Ch2 carries the pointer · refs docs/decisions/ADR-86-conformance-dashboard-location.md, ADR-80, ARCHITECTURE.md Ch2, #169

### Keep the day-to-day docs right-sized and current
So that the cheat-sheet and architecture stay scannable as conventions accrue.
- [#5] [P2][M] Add high-leverage ESSENTIALS cheats for ADRs 35-76 under the one-page constraint · Done when: the chosen cheats land + ESSENTIALS stays ~1 page · refs posture-audit C2
- [#41] [P3][S] Split ARCHITECTURE §Processes into PROCESS.md if it grows past the comfort threshold · Done when: re-evaluated at the next process addition · refs ADR-51

---

## Cross-repo universalization
> As the disseminator, I want every child repo to converge on the universal baseline.

### Converge every child repo on the universal baseline
So that "open any repo, same layout/governance" actually holds.
- [#47] [P3][S] Confirm status of corp-knowledge-extractor / corp-by-os / corp-rfp-agent · Done when: each is classified (renamed/archived/uncloned/dropped) · refs 2026-05-11 audit
- [#9] [P2][M] Inventory skills across repos, classify repo-specific vs universal, propose a canonical home · Done when: a skills inventory + universalization proposal exists · refs VISION cross-repo consistency · changelog 2.1.148-167 finding — distribution simplification (v2.1.157): plugins in `.claude/skills` auto-load without a marketplace; `claude plugin init <name>` scaffolds; factor into the canonical-home proposal and fleet rollout of verify/guard organs
- [#82] [P3][M] Define per-repository agentic-review profiles — what agentic review each repo runs (dev-knowledge = methodology conformance #81; child repos = their domain needs, e.g. corp-monorepo deep-audit #75, ai-council's flow) and on what cadence, so agentic-ness is deliberate per repo, not one-size-fits-all · Done when: each repo's agentic-review profile is recorded · refs ADR-70, #75, #9, #70; note: iteration-2 verifier extensions (e.g. a folder/naming guardian for corp-monorepo) belong to this per-repo profile design, not to #81's hub conformance workflow; evaluation set grows: `/code-review` plugin (parallel multi-angle subagents, confidence-scored findings), `code-simplifier` post-diff agent, GitHub-triggered routines for PR review; doctrine note: heterogeneous second reader (different model architecture) is the field-validated shape — same-model self-review fails to sycophantic convergence · changelog 2.1.148-167 finding — native-first evaluation order (v2.1.152/2.1.154): `/code-review --fix` (applies findings; reuse/simplification/efficiency) and `/simplify` (cleanup-only review) are built into the CLI; evaluate natives before any plugin or custom simplifier agent · audit-trio finding (docs/audits/2026-06-07-platform-max-audit.md AD-4/KILL-3 + docs/audits/2026-06-07-codex-max-audit.md) — fold `/code-review ultra` into the evaluation (Claude-side: a COMPLEMENT, not a substitute for Codex cross-vendor heterogeneity — KILL-3); review-profile design inputs: `model_reasoning_effort` per-call/profile, `--ephemeral`, output-hygiene keys (`hide_agent_reasoning`, `model_reasoning_summary`, `web_search="disabled"`); xhigh-engages VERIFY recorded (`codex exec --sandbox read-only -c model_reasoning_effort=xhigh` on a throwaway diff)
- [#100] [P3][S] corp-monorepo CLAUDE.md freshness restamp — genuine end-to-end re-read + `last_reviewed` bump (edited 2026-06-06, stamp 2026-06-02; witnessed by fleet audit 2026-06-06) · **execute in the corp dedicated chat** (ADR-41 routing — this entry is queue-only) · Done when: corp audit `canonical_freshness` passes · refs ADR-41
- [#120] [P3][S] Child backlog schema-conformance probe — verify each child repo uses the ADR-66 story-map (Big Picture → Theme → Story → Task) before `validate-backlog` can travel; prerequisite for distributing the hook, not the distribution itself · Done when: each child's BACKLOG schema-conformance is recorded (conformant / needs-migration) · refs docs/audits/2026-06-07-methodology-transfer-audit.md G3, ADR-66
- [#131] [P2][M] Repo-onboarding runbook — `docs/runbooks/repo-onboarding.md` documenting the 6-layer install sequence to bring a repo onto the universal baseline (floor → carriers → lifecycle command → review profile → ecosystem registration → re-anchor mechanic), with a pilot-then-n=2 adoption gate · Done when: the runbook exists, lists all 6 layers in order, and has been piloted on one repo (n=1) with the n=2 gate recorded · refs ADR-71, ADR-72, ADR-78, #121, #82, docs/audits/2026-06-07-copilot-collections-peer-audit-v2.md §3.5 + §4 adoption-path row · re-homed from #121 (2026-06-07, T2 ruling, #8 precedent): #131 OWNS (a) fleet-wide child `CLAUDE.md` `@.claude/CLAUDE-FLOOR.md` references and (b) the 3-week re-explain measurement as the rollout success metric — #121's hub components + single-child pilot ship independently of fleet rollout · 2026-06-08 pilot finding (corp-sca-time-automation): the runbook MUST list `pre-commit install` as a **MANDATORY per-clone rollout step** — the child floor hash-verify hook is installed-as-config-but-INERT (wired-but-inert class) until armed, and `pre-commit install` writes untracked `.git/hooks/` so it does not travel with the repo; every fleet clone runs it itself (refs docs/audits/2026-06-08-floor-pilot-corp-sca-validation.md) · 2026-06-08 re-pilot findings (floor now under `.claude/`, refs docs/audits/2026-06-08-floor-repilot-corp-sca-validation.md): the runbook MUST (c) add per-child `.gitignore` **negations** (`!.claude/CLAUDE-FLOOR.md`, `!.claude/CLAUDE-FLOOR.md.sha256`, `!.claude/check_floor_hash.py`) where `.claude/` is gitignored, so the floor is tracked without `git add -f` (see [#138]); and (d) **delete any orphaned ROOT floor copy** (`CLAUDE-FLOOR.md` + `.sha256`) on migration to `.claude/` — `audit.py floor_integrity` now reads `.claude/` ONLY, so a stray root floor is an invisible vacuous-skip-PASS · Subsumes #115 residual: distribute `/ship` (tier1-lifecycle plugin) to child repos — hub half done (plugin-provided), child-distribution travels via the carriers + lifecycle-command onboarding layers
- [#138] [P2][S] Generator install-note adds child `.gitignore` negations — the baked install note (`scripts/generate_floor.py` INSTALL_NOTE) must instruct the child to add `!.claude/CLAUDE-FLOOR.md`, `!.claude/CLAUDE-FLOOR.md.sha256`, `!.claude/check_floor_hash.py` to its `.gitignore` so the floor is tracked WITHOUT `git add -f`; `.claude/` is commonly gitignored, so without negations the floor files are swept up → force-add friction + variable fleet behavior · Done when: a generated install note carries the three negation lines and a child with `.claude/` gitignored tracks the floor without `-f` · refs docs/audits/2026-06-08-floor-repilot-corp-sca-validation.md, #131, ADR-78

### Make new-repo scaffolding correct-by-default
So that a new repo inherits the full baseline in one step, not by re-derivation.
- [#16] [P2][M] Collapse workspace-{S,M,L}.code-workspace into one maximal scale-adaptive template; element-selection guidance encodes the small=single-root / large=multi-root rule (merged from #38, 2026-06-07 groom) · Done when: one workspace template + element-selection guidance (incl. the single/multi-root rule) exist · refs operator Q5, ADR-59
- [#17] [P2][M] Encode ADR-54–63 high-leverage guidance into templates/CLAUDE-md-template.md (structural lock + frontmatter already done at v2.2; the stale "§11 last-5 = 61-65" sub-spec is dropped — the template uses generic `ADR-NN` placeholders by design) · Done when: the template's guidance reflects ADR-54–63 · refs durability-audit J1, template v2.2; scope extended: CLAUDE-md-template gains compaction-preservation instructions (what auto-compact must preserve: modified-file list, test commands, active branch, open task ids) · refs PLAYBOOK context-budget rule; evidence-backed compaction guidance (field: ~67% token reduction from threshold compaction; too-low thresholds cause destructive summary loops); add instruction-priority tiers (CRITICAL/STANDARD/PREFERENCES) to the template; pointer to subdir-CLAUDE.md item (#109) · audit-trio finding (docs/audits/2026-06-07-platform-max-audit.md UN-8) — native `/context` diagnostic (v2.1.74) as a companion to the compaction-preservation guidance · serialize-group: claude-md-template
- [#43] [P3][L] Decide + (if yes) author a one-step new-repo scaffold (ADR + templates/new-repo-skeleton/, no scripts) · Done when: decision recorded + scaffold authored if approved · refs durability-audit J6
- [#109] [P3][S] Subdirectory CLAUDE.md convention — per-module CLAUDE.md layer for large repos; add to the template standard; pilot in corp-monorepo via its dedicated chat · Done when: the convention lands in `templates/CLAUDE-md-template.md` and corp pilot is queued · refs #17, ADR-55–63 cluster · serialize-group: claude-md-template
- [#129] [P3][S] Skill-authoring scaffold — `templates/SKILL-scaffold.md` (the lone untracked peer-audit residual, C11) modeled on TSH `tsh-creating-skills`; **MIT attribution required** if their markdown is adapted verbatim (© The Software House). Encodes the 3-tier token-budget discipline their skills carry but ours lack — discovery (~100-token name+description) / activation (<5k body) / `resources/` (loaded on demand) + a 500-line body cap — as a frontmatter/structure row; mirrors how `templates/prompt-template.md` anchors prompt authoring · Done when: `templates/SKILL-scaffold.md` exists carrying the 3-tier budget + 500-line cap guidance and the MIT-attribution note · refs docs/audits/2026-06-07-copilot-collections-peer-audit-v2.md C11/§2/§3.6 S1, #9

---

## Tooling & evaluation
> As the operator, I want low-friction tooling and timely tech adoption.

### Cut session friction with better tooling
So that cognitive overhead per session drops.
- [#37] [P3][S] Amend ADR-59 with a workspace-sort-change verification rule — the visual-pattern (workspace sort settings) is now pointered from PLAYBOOK to ADR-59 (#158), so the rule lands as an ADR-59 amendment, not in PLAYBOOK/ESSENTIALS. Rule: on a workspace-sort-setting change, read the merged PR + do a visual editor check before relying on it (the `sortOrderReverse` saga is the cautionary precedent). · Done when: the verification rule lands as an ADR-59 amendment · refs ADR-59 amendment, #158
- [#39] [P3][S] Codify entry-scripts → scripts/ in PLAYBOOK root-hygiene — blocked on Move 2: the root-hygiene convention is retained PLAYBOOK-local (not pointered, per #158), so full dissolution needs the Move-2 structural split to give the rule a stable home. · Done when: the convention is written (after Move 2 lands the root-hygiene home) · refs 2026-05-27 retrofit, #158, Move-2 structural split · serialize-group: playbook
- [#102] [P2][M] Machine-readable repo index for agent consumption — field reports ~50k-token exploratory reads replaced by ~250-token precompiled indexes; evaluate for hub + corp · verify-first: confirm claimed tools (`stacklit`, `scip-search`) actually exist before any design · Done when: a pilot index on one repo demonstrably replaces exploratory reads in a CC session · refs #89, #91, ADR-51, research transcript 2026-06-06 · context-engineering field study (InsForge) — pilot shape: a single-call repo-topology spec (~500 tokens — structure, conventions, gate names, entry points, plus a `hints` field) consumed at UNDERSTAND time; makes the ~50k→~250-token replacement target concrete
- [#118] [P3][S] Run the native `fewer-permission-prompts` skill — one-shot allowlist tightening (scans transcripts → proposes a prioritized allow-list) under the deny-rule posture; low effort · Done when: the skill has run and its proposed allow-list is reviewed + applied (or explicitly declined) · refs docs/audits/2026-06-07-platform-max-audit.md AD-3
- [#119] [P3][S] Codex currency — update 0.136.0 → 0.137.0 (Windows SQLite stability fix #25490 + exec approval-policy preservation) via the sanctioned `codex update`; add/refresh the ENVIRONMENT.md Codex version row · Done when: `codex --version` reports 0.137.0 and the ENVIRONMENT row matches · refs docs/audits/2026-06-07-codex-max-audit.md · serialize-group: environment
- [#122] [P3][S] Retire the PATH shim (`claude.cmd`) — the native settings.json `env` route (empty `ANTHROPIC_API_KEY`) is primary since 2026-06-06 and covers all launch paths; the shim is now redundant. Sentinel stays as a tripwire; removal needs an explicit operator ask per the no-delete invariant · Done when: the operator approves and the shim is removed (or the item is closed as keep-for-defence-in-depth) · refs docs/audits/2026-06-07-platform-max-audit.md UN-1
- [#127] [P3][S] verify skill failure-output contract — on failure the `verify` skill emits an **actionable** block (file, expected, received, directive) per the backpressure spec, giving an iterate-until-green loop a deterministic feedback signal; success output stays the 3-line compact form · Done when: a seeded failure produces the file/expected/received/directive block and success stays 3-line · refs #104 (verify-as-skill lineage), #126 · enrichment — failure outputs also carry a semantic exit code + expected-vs-got context (not just a directive) so an iterate-until-green loop gets a machine-distinguishable signal (anti-retry-loop)
- [#133] [P3][S] Peer-watch copilot-collections — add `github.com/TheSoftwareHouse/copilot-collections` to `ecosystem/tool-versions.yaml` as a watched source for `/changelog-review` (their Keep-a-Changelog is dated, so diffable); review cadence = on sentinel nudge, not nightly · Done when: the source is in the watch list and a `/changelog-review` run classifies its latest changelog delta · refs #113 (/changelog-review), docs/audits/2026-06-07-copilot-collections-peer-audit-v2.md

### Decide the undecided artifact/tool models
So that cadence-less artifacts and unevaluated tools don't rot or get adopted blind.
- [#106] [P3][S] Auto Mode evaluation — classified permission middle-ground between bypass and babysitting (3 tiers, injection probe, audit trail) · verify-first: current availability/state on the Max plan · Done when: a go/no-go posture decision is recorded · refs unattended-run posture, ADR-68 autonomy boundary · changelog 2.1.148-167 finding — evaluation unblocked: Auto Mode no longer requires opt-in consent (v2.1.152), classifier improved (exfiltration detection, fewer false "could not evaluate" blocks, v2.1.154/160); evaluate against current state, not the research-preview caveats · audit-trio finding (docs/audits/2026-06-07-platform-max-audit.md VF-3) — exact availability check: `/config` → permission-mode picker, confirm "auto" appears WITHOUT setting `CLAUDE_CODE_ENABLE_AUTO_MODE` (that env var is Bedrock/Vertex/Foundry-only per 2.1.158; first-party "no longer requires opt-in" per 2.1.152)
- [#126] [P2][M] Backpressure-loop pattern evaluation — a sanctioned in-session *iterate-until-green* primitive for unknown-iteration tasks (lint/type-debt burndown; red→green after a rebase or dependency bump). **VERIFY FIRST:** current `/loop` command semantics in CC docs/help — the ADR-74 REJECTED row covers `/loop` as a *persistence host* ONLY, not in-session repair (ADR-74 Amendment 2026-06-07). Doctrine bounds: operator-invoked only, never on automated paths, **deterministic checks as the feedback source** (pytest/ruff/type-check, not model judgment), a **mandatory iteration budget**, and "do-not-change-tests"-class constraints (fix code to satisfy fixed acceptance criteria, never edit the criteria). Pilot target: corp-monorepo (**execute in the corp dedicated chat**, ADR-41 — queue-only here) · Done when: a go/no-go is recorded with the doctrine bounds + a corp pilot result (or an explicit operator drop) · refs ADR-74 (REJECTED row, now persistence-scoped), ADR-76, #8 (Stop `additionalContext` = native backpressure surface), #127, generativeprogrammer backpressure article 2026-06

- [#123] [P2][S] Routine observability convention + value review — (1) all automation commits carry a standard marker (scope `chore(routine/<name>)` or trailer `Routine: <name>`) so routine output is git-indexable; (2) periodic value review in the morning funnel: findings-acted-on vs noise per routine (first datapoint: 2026-06-07 nightly digest 0/0/0 vs local baseline 1 FAIL) · Done when: the marker convention is recorded + adopted by the fleet/routine jobs AND one morning-funnel value review records per-routine findings-acted-on vs noise · refs #85, #14, #113, audit C matrix R7 · origin: operator proposal 2026-06-07
- [#155] [P3][S] "Loops architecture" — explore (scope TBD; operator-flagged 2026-06-11, not yet specced): the architecture of in-session / cross-session loops (iterate-until-green backpressure #126, `/loop` persistence ADR-74, the night/Routine cadence, the verification-mesh outcome loop). VERIFY-FIRST with the operator what "loops architecture" scopes before any design. · Done when: the operator confirms scope and a go/no-go on a loops-architecture design pass is recorded · refs operator flag 2026-06-11, #126, ADR-74, ARCHITECTURE.md Ch6 (verification mesh)

---

**About this file** — open `.dev-knowledge` work as a story map (ADR-66): Big Picture → Theme → User Story → Task. Stories are human (goal + `So that`); tasks carry `[#id] [P][size] · Done when · refs`. Done tasks **leave** (ADR-65); git is the implementation record (`git log --grep 'closes \[#'`). Child-repo execution items live in `docs/audits/2026-06-01-child-repo-relocation-proposal.md`. Schema: PLAYBOOK §10; machine-checked by `scripts/validate_backlog.py`.

**Grooming log:** 2026-05-09 · 2026-05-23 · 2026-05-24 · 2026-05-31 (marathon-arc) · 2026-06-01 (ADR-64/65 migration + readability + ADR-66 story-map) · 2026-06-02 (git-verified retroactive closure: retired #29/#31/#40/#44/#45/#46; re-scoped #17/#13; updated #70/#10; ADR-69 records the #44 reach model) · 2026-06-02 (ADR-70 incorporation: annotated #13/#8/#12/#72/#4 with `refs ADR-70` as the three-tier build units; added #73 Tier-1 plugin bundle + cross-repo install, #74 Workflow-escalation rule, #75 first scoped corp-monorepo Tier-3 Workflow) · 2026-06-02 (closes [#13]: ruff pre-commit gate wired + proven; #13 retired) · 2026-06-02 (closes [#72]: fleet_health.py Tier-2 daily cross-repo audit + no_sibling_orphans now runs all 5 repos; #72 retired) · 2026-06-03 (agentic-arc prep — added #80 Dynamic Workflows research P1, #81 methodology-conformance workflow P2, #82 per-repo agentic profiles P3; next-session focus is the Tier-3 agentic conformance/review arc composing ADR-68 night-agent + ADR-70 Tier-3 with #74/#75/#77/#70 + #80→#81→#82, grounded in the shipped Dynamic Workflows feature; #70 AI-Council loop remains the heavy-decision path composing with the heavy-execution workflows) · 2026-06-05 (pre-handoff sweep — git↔BACKLOG reconciliation: withdrew [#87] (routing bug-report premise refuted by the Phase-C3 re-probe — non-functional routing was our own `CLAUDE_CODE_SUBAGENT_MODEL=haiku` override, not a platform bug), corrected #83's stale "Haiku-only de-facto" note, added [#89] deterministic prose-vs-state checker + [#90] V4 git↔backlog two-direction verifier; all other arcs since 2026-06-04 reconciled consistent). Next quarterly: 2026-07-01. · 2026-06-06 (closes [#75]: corp scoped audit Workflow ran end-to-end 2026-06-06, 29 targets / ~588k tok / ~4 min, fit confirmed; annotated #89 with check-source + #84 with n=1-of-2 evidence trail). · 2026-06-06 (nightly-triage N1 ratified ACCEPTED-AS-CLARIFICATION — #74 closure clarification: the Done-when's "alongside #18/#27" clause was *consciously* satisfied via the PLAYBOOK forward-pointer, not literal co-location; the clause reconciliation is recorded in the Track B close (commits 11ef2d8 / 4c62f63). No literal co-location work owed; #18/#27 remain open siblings; no reopen). · 2026-06-06 (closes [#86]: sub-decisions 1–2 → ADR-72; sub-decision 3 → ADR-73; ruled 2026-06-06, source corp JOURNAL 2026-06-06). · 2026-06-06 (doctrine-pass codification: amended #85 to record ratified direction — Tier-2 cross-repo host, Desktop scheduler, n=1 gate, riders R1/R2, ADR-74; added #95 template↔copy drift watcher (G5), refs ADR-72/73/74). · 2026-06-06 (Council verdict per ADR-76: amended #85 to replace Desktop-scheduler mechanism with Task Scheduler → python directly; added ADR-76 to refs; rider R1 removed — n=1 gate still pending). · 2026-06-06 (funnel-integrity defects: added #98 PROPOSALS persistence, #99 FLEET-HEALTH check-name detail, #100 corp-monorepo CLAUDE.md restamp queue; annotated #5 scope ADRs 35-63→35-76, #34 added pre-emit prompt checklist codification candidate). · 2026-06-06 (billing-trap capture: added #101 SessionStart billing-leak sentinel — fail-soft WARN when ANTHROPIC_API_KEY visible in CC session). · 2026-06-06 (SOTA gap-analysis capture: added #102 machine-readable repo index, #103 /ship git-finish command, #104 verify-as-skill, #105 PreToolUse immutability guard, #106 Auto Mode eval, #107 worktree convention, #108 Projects-as-context-layer eval, #109 subdir CLAUDE.md convention, #110 ai-council round-2 isolation audit, #111 PLAYBOOK prompt-format enrichment pack; refs research+article review 2026-06-06). · 2026-06-06 (immutability-guard implementation wave: shipped the transcripts immutable-zone — `block_immutable_edits.py` PreToolUse guard live + wired + witnessed, ADR-77 amends ADR-75; annotated #105 PARTIAL + scope-split (handoffs/audits dropped — sanctioned append flows); added #112 adr_amend helper + ADR-zone extension (Option A; Option B held until #23)). · 2026-06-06 (closes [#101]: SessionStart billing-leak sentinel `scripts/billing_leak_sentinel.ps1` wired + witnessed both branches (leaked→WARN, clean→silent); `-NoProfile` required so the hook reads CC's env not the re-sourced profile — new gotcha recorded). · 2026-06-06 (closes [#104]: verify skill shipped — `.claude/skills/verify/{SKILL.md,verify.py}` + `templates/prompt-template.md` v1.2 drops inline check sequence in favour of skill invocation). · 2026-06-06 (closes [#97]: artifact-reader subagent shipped — `.claude/agents/artifact-reader.md` (Read/Grep/Glob, model pinned); witnessed on 96,897-byte transcript → 1.3k summary (~98.7% context reduction)). · 2026-06-07 (closes [#84]: two-tier automation doctrine codified — PLAYBOOK "Two-tier automation doctrine" section + the two ADR-authorship-paths note + Model/Effort platform-doctrine refresh + Routine/night operational-standard extension; ADR-80 adoption (writer policy Option b + 3 riders; channel verdict compliant-by-design per PR #17/221c63e; fallbackModel doctrine — interactive MAY, pinned stages MUST NOT). n=2 evidence gate cleared (06-06 red → 06-07 clean conformance digests). Added #125 local-writer-commit wiring follow-up (capture-only, riders 1-3); the `ship-around-fleet-health-dirty-tree` CC memory note is now OBSOLETE per ADR-80). · 2026-06-07 (backpressure capture: added #126 backpressure-loop pattern eval (P2/M, corp pilot, VERIFY-FIRST /loop semantics), #127 verify-skill failure-output contract (P3/S), #128 ai-council prompt caching (P3/S, ai-council chat); ADR-74 amended in-file — `/loop` REJECTED row + KILL-7 ref scoped to *persistence host*, in-session repair-loop split out to #126; LESSONS "scoped-rejection leakage" appended). · 2026-06-07 (peer-audit-v2 capture + operator pushback rounds: added #129 skill-authoring scaffold (C11 lone untracked residual), #130 memory-hygiene review, #131 repo-onboarding runbook, #132 organ-index generator, #133 peer-watch copilot-collections; annotated #121 (per-action floor re-anchor §3.5 + InsForge amplify-cost), #111 (explore-mode readiness §3.6 S2), #102 (single-call topology spec), #127 (semantic exit codes), #91 (ORGAN-INDEX consumer + agents-distribution doctrine); bumped #107 P3→P1 on the 2nd witnessed same-checkout collision; refs docs/audits/2026-06-07-copilot-collections-peer-audit-v2.md + addendum). · 2026-06-07 (#134 n=1 grooming field run — ratified removals; deterministic signals gave 0 hard kills on this freshly-groomed backlog, so judgment-driven; 11 tasks removed, 78→67). DONE-UNDETECTED: closes [#81] (methodology-conformance Dynamic Workflow — `conformance-hub.js` shipped + ran nightly; 06-06 digest surfaced real drift 1H/1M ratified via triage N1/N2; 06-07 clean; Done-when met; per-rule expansion NOT re-captured — re-enters via funnel if needed, #77 owns doc-rot); closes [#14] (ecosystem/ = committed continuous-audit substrate; decided by ADR-76 + ADR-80 + #125 arc — audit.py sole writer, scheduled cadence). MERGE-INTO: [#21]→#19 (templates/ class clause transferred); [#38]→#16 (single/multi-root rule transferred). STALE-INTEREST (operator item-by-item): removed [#6] [#22] [#28] [#30] [#32] [#33] [#94]; kept #47. Removed 2 orphaned story headers with their sole tasks (advisory-framing story = #6; methodology-conformance story = #81) for 0 warnings. ALIVE flag-edits (next commit): #71 target re-scoped to live command set (codex-review + session-summary; boot/evolve archived 2026-06-05), #77 vehicle ADR-68 night-agent → ADR-80 Routine/Workflow, #110 ADR-03-ref verify note; #1/#2 P1→P2 — #107 stays the only P1.) · 2026-06-08 (closes [#121]: ADR-78 child methodology floor — Step-5 pilot ran in corp-sca-time-automation; three close-gate witnesses recorded in docs/audits/2026-06-08-floor-pilot-corp-sca-validation.md — (a) floor auto-loaded + re-anchor rule quoted verbatim, (b) tamper caught BOTH sides (child pre-commit exit 1 AND hub floor_integrity FAIL→restore→PASS, corroborated on disk by the two 2026-06-08 corp-sca audit snapshots), (c) re-anchor quoted before structural work; Done-when met. Pilot install-note findings (i) missing `pre-commit install` → wired-but-inert hook + (ii) per-clone untracked-hook rollout step annotated onto #131; added #137 LF-pin templates/*.sha256 .gitattributes (recurring CRLF phantom diff). Out-of-scope: corp-sca canonical_freshness FAIL → that repo's own session per ADR-41.) · 2026-06-09 (reconcile: #125 removed — git-closed, Done-when met; #115 surfaced for ruling — easy-metric closure; 66→65) · 2026-06-09 (closes [#89]: prose-vs-state checker shipped — `scripts/validate_doc_claims.py` + `doc_claims` audit check; home/path/cadence/deployed per ADR-81; #140 seam documented in-module; first full run surfaced real drift — ARCHITECTURE "372 collected" → 396, left for operator ruling per Layer-2; 65→64) · 2026-06-09 (closes [#141]: 3 Codex findings resolved — (1) `_derive_pytest_collected` now read-only (`-p no:cacheprovider` + `PYTHONDONTWRITEBYTECODE=1`); (2) claim-3 test re-mocked to prove a NON-skipped eval (teeth: `!= skipped` rejects vacuous skip-pass) + a separate `None→skipped` test; (3) `cmd_health` `_GATE_MODE` set/reset proven by two click-runner tests (teeth-verified: both go red if the `finally` reset is removed). The #89 drift it caught is now FIXED — re-derived AFTER this pass's +4 tests landed: ARCHITECTURE count 372→**400** (not 396), `validate_doc_claims` claim-3 MATCH (doc 400 / actual 400) — catch→fix loop closed. Filed #142 (backpressure-hook same-day blind-spot); 64 net (−#141 +#142)) · 2026-06-10 (worktree-codification deferrals: added #143 parallel-work-as-first-class-mode, #144 feature-DoD=E2E-test (VERIFY "in the cloud" target with operator), #145 codification-completeness pass; refs the 2026-06-10 worktree transmission-gap LESSON + sharpened ADR-81 (a); +3, validate_backlog reports 65 tasks) · 2026-06-10 (added #147 pre-ship verification-organ gate — makes "Definition of shipped" point (6) enforceable; origin: the 2026-06-10 "deployment is half the success" LESSON; distinct from #144 (E2E=point 3) and #145 (codification-completeness); +1, 67 tasks) · 2026-06-10 (consolidation audit — JOURNAL<->BACKLOG reconcile over the 06-04..06-10 arc; all 5 Group-A organs E2E-witnessed live (#11 gate fires+blocks exit 1; #89/#90a/floor teeth-confirmed; /ship source-verified): closes [#11] (amendment_coherence gate shipped + E2E-witnessed, ADR-81 DoD met; de-hardcode residual = #146), closes [#111] (prompt-format pack (a)+(b)+readiness valve in PLAYBOOK, Done-when met); #77 stays open — genuine CLOSURE-VOIDED (77e5d7d misattribution, content-consolidation unshipped); bumped #139 P3->P2 (this audit justifies it — #90a direction-(a) structurally can't distinguish voided vs real closure). close 2, correct 0; 67->65 tasks) · 2026-06-10 (added #148 HANDOFF_PROCESS v5 redesign — Tier-1 candidate, AI-Council-routed; origin: the 2026-06-10 handoff primary/secondary-source LESSON; slotted under the "deferred handoff-template refinements" story, sibling to #25/#124; +1, validate_backlog reports 65 tasks) · 2026-06-11 (architecture-coherence audit capture: added #150 two-mode handoff, #151 #149 flip doc-sync, #152 pointerize duplicated governing-doc sections, #153 enforcement-completeness pass, #154 DRIFT-1 ARCHITECTURE test-count restamp, #155 loops-architecture exploration; refs docs/audits/2026-06-11-architecture-coherence-audit.md; +6) · 2026-06-12 (#149 v5→canonical flip, atomic — Council gate on ADR-82 waived by operator authority: closes [#149] (v5 promoted Version 5.0/stable, v4.4 archived per ADR-83, 4 coupled surfaces moved atomically, audit-health green), closes [#151] (full prose sweep — PLAYBOOK/ESSENTIALS/SESSION_SETUP/ARCHITECTURE/CONTRIBUTING/HANDOFF_BOOT/CLAUDE repointed v4→v5; no living doc names v4 canonical), closes [#160] (v4.4 archived with conforming ADR-83 tombstone), closes [#148] (v5 shipped with teeth + lean task-state + self-updating /handoff — Done-when met), closes [#124] (superseded-by-v5 — the bundle it would consolidate is gone; 05_NOW init-ack intent delivered via the boot-ack; operator-ruled), closes [#25] (superseded-by-v5 — v4 .tmpl refinements moot, templates archived; portable idea "fresh-eyes cadence" already owned by #1, other 6 were v4-.tmpl-specific). KEPT OPEN reframed: #152 (§8 v4→v5 repointed but pointerization unmet), #159 (flip-carry done, real-session exercise remains), #26/#150/#161/#162 (now canonical; dangling _v5.md refs repointed). ADDED #163 (teeth validator verify_handoff_probes.py) + #164 (v5 /handoff generator) — deferred machinery. Retired the parallel-ship test fence; kept the straggler guard generalized. 72 tasks)
