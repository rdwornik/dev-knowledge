# .dev-knowledge BACKLOG

## Big picture

`.dev-knowledge` is the ecosystem's methodology brain: it absorbs lessons from every repo, universalizes them into enforced conventions, and audits the ecosystem against them — the LLM-development "scrum master" for all `Dev/` projects (VISION: Knowledge Guardian · Methodology Author · Auditor · Disseminator). The backlog advances that mission across seven themes.

**Themes (backbone):** Handoff continuity · Enforced governance · Lessons feedback loop · Decision management · Canonical-file integrity · Cross-repo universalization · Tooling & evaluation

---

## Handoff continuity
> As a session inheriting this repo, I want to pick up with full state and lose nothing.

### Give every handoff a fresh-eyes review, not just the big ones
So that routine handoffs stop inheriting the ~25% insider blind spot.
- [#1] [P1][L] Extend triangulation from the promotion gate to every routine handoff (sub-prompt / periodic audit / agent-framework) · Done when: every Phase-2 handoff runs a fresh-eyes pass · refs protocols/AGENT_FRAMEWORK.md (Council) · Evidence: 2026-06-03 bundle review caught (A1) an unverified session-state claim bypassing the cross-check table and (A2) a "Tier" terminology collision across three axes — both defects that an adversarial fresh-eyes pass would catch (v4.3.2 §D)

### Stop advisory framing from pressuring the receiver
So that "REST recommended"-style framing stops driving unprompted receiver behavior.
- [#6] [P2][M] Classify the root cause of advisory-framing leakage and propose a mitigation · Done when: root cause documented + a mitigation rule lands in HANDOFF_PROCESS/ESSENTIALS · refs ADR-37, LESSONS 2026-05-11

### Let an inheritor act on the operator's defaults instead of re-asking
So that a fresh chat states-and-acts rather than over-asking on things it should assume.
- [#26] [P3][S] Add an operator-invariants section to the Phase-1 interview/bundle (clean-tree, immediate-merge, three-domain) · Done when: the section ships in the handoff template/skill · refs HANDOFF_PROCESS v4

### Close the deferred handoff-template refinements
So that the v4 templates stop carrying known minor gaps.
- [#25] [P3][M] Apply the 7 deferred v4.3.1 refinements (tag-lint, fresh-eyes cadence, CLI example, ML namespace, wording, auto-routing, passive-storage) at the .tmpl level · Done when: all 7 resolved in templates/handoff/*.tmpl · refs HANDOFF_PROCESS v4.3.1 · 02_METHODOLOGY embeds VERBATIM (never paraphrased): the non-negotiable Model/Mode/Effort table spec incl. the Mode enumeration (bypass permissions / plan), from PLAYBOOK §"Rob's prompt format is non-negotiable" (~line 1705)
- [#124] [P2][S] Consolidate handoff bundle to single-file BUNDLE.md + add 05_NOW init-acknowledgment clause — HANDOFF_PROCESS edit + generation-script change per ADR-79 ergonomics mandate: (1) handoff generation outputs ONE BUNDLE.md (5 uploads → 1); (2) 05_NOW gains "Loaded methodology vX.X. Ready." acknowledgment so a missing/partial upload is visible · Done when: handoff generation produces BUNDLE.md + 05_NOW carries the acknowledgment clause · refs ADR-79, HANDOFF_PROCESS, #25

---

## Enforced governance
> As the operator, I want load-bearing conventions enforced by tools, not memory, so they can't silently drift.

### Turn advisory guards into enforced gates
So that a convention can't be skipped under load (the failure mode behind real aborts).
- [#11] [P2][M] Convert LESSON-#9's advisory cross-case guard into an amendment checklist/gate (Layer-2 checklist, not orchestration) · Done when: a multi-surface amendment must pass the gate · refs coherence-audit ML-2
- [#15] [P2][M] Add hyphen-only-separator enforcement (pre-commit + Action) with a scoped path set + exceptions · Done when: a non-conforming new filename is blocked · refs ADR-34
- [#105] [P2][S] PreToolUse immutability guard — block Edit on `docs/decisions/*`, transcripts, handoffs, audits (new-file creation allowed); extends the block-onedrive pattern; immutable paths become a second zone class under the ADR-75 register (amending ADR) · Done when: an in-place edit attempt on an existing ADR is blocked fail-closed and a new-ADR creation passes · 2026-06-06 PARTIAL (stays open): transcripts portion shipped — `scripts/hooks/block_immutable_edits.py` live + wired + witnessed, ADR-77 amends ADR-75; handoffs/audits REMOVED from scope (sanctioned append / dated-addenda flows); ADR portion re-routed to #112 (Option A script-enforced amend path, per the operator ruling — Option B held until #23)
- [#112] [P2][M] adr_amend helper + ADR immutable-zone extension — `scripts/hooks/adr_amend.py` becomes the ONLY sanctioned writer of an Amendment/status line on an existing ADR (canonical format from ADR-68's formal Amendment + HANDOFF v4.3 Amendment A); extend `block_immutable_edits.py` to deny every OTHER in-place ADR edit (fail-closed); resolve the CLAUDE.md §5 self-contradiction ("supersede with a new file or in-file marker; never edit in place" → "append-only via the helper"); includes the CLAUDE.md end-to-end re-read + `last_reviewed` restamp · Done when: an in-place ADR edit NOT routed through the helper is blocked, a helper-written Amendment passes, and CLAUDE.md §5 is de-contradicted + restamped · refs ADR-77, #105, #23, ADR-68 · changelog 2.1.148-167 finding — native deny rules strengthened (v2.1.166/2.1.163): glob in tool-name position, `$HOME`-referencing Bash commands now matched by path deny rules; evaluate a native deny on the transcripts zone as a complement for the documented shell-write residual (C2) — not a replacement (deny rules cannot express exists-deny/new-allow) · audit-trio finding (docs/audits/2026-06-07-platform-max-audit.md UN-7/KILL-4/KILL-1) — `permissions.deny` path-globs as a defense-in-depth layer for BOTH zones (transcripts AND OneDrive); KILL-4: deny-glob on arbitrary Bash is historically bypassable → defense-in-depth only, never a replacement for the PreToolUse guards; KILL-1: native OS sandbox is macOS/Linux/WSL2 only — NOT available on native Windows, so the guards stay
- [#107] [P3][S] Worktree convention for parallel sessions — PLAYBOOK rule: 2+ concurrent CC sessions on one repo = separate git worktrees; canonical fix for the witnessed parallel-session tree-contamination gotcha · Done when: the rule lands in PLAYBOOK · refs ADR-61 · changelog 2.1.148-167 finding — base the convention on Claude-managed worktrees (v2.1.157/154): `EnterWorktree` switches mid-session, managed worktrees auto-unlock on finish, subagent worktree-isolation bypass fixed — not on manual `git worktree` · audit-trio finding (docs/audits/2026-06-07-platform-max-audit.md UN-4/KILL-2) — native `isolation:"worktree"` auto-cleans changeless worktrees (directly targets the rule-9 orphan class); convention = native for Claude-spawned worktrees, manual discipline residual for cross-repo/interactive sessions (KILL-2: native manages only Claude-spawned → a complement, not a full replacement of the ADR-61 manual flow)

### Extend structural validation to more governance artifacts
So that drift in transcripts, ADRs, and folders is caught cheaply, not by reviewer luck.
- [#7] [P2][M] Apply the audit.py #8 structural-validator pattern to Council transcripts + ADRs (frontmatter/supersession/template) · Done when: registered checks + tests folded into health · refs scripts/audit.py #8/#9
- [#36] [P3][M] Add read-only check_folder_semantics asserting each docs/ subfolder carries one ADR-60 role · Done when: the check ships with fixtures + tests · refs ADR-60
- [#42] [P3][S] Decide whether mermaid-theme check #7 expands beyond ARCHITECTURE.md (extend paths or accept) · Done when: scope decision recorded · refs durability-audit J4
- [#95] [P3][M] Template↔copy orchestration drift watcher (G5) — per ADR-73, per-repo orchestration copies propagate at rollout moments but no organ detects a copy diverging from the hub canonical template between rollouts; candidates: version/hash header in copies + deterministic check, or a rollout-checklist item · Done when: a deterministic, read-only check (or ratified checklist step) detects template↔copy divergence and surfaces it · refs ADR-72, ADR-73, ADR-74
- [#77] [P3][L] Extend the review machinery (audit.py + ADR-68 night-agent) to catch doc-rot — intra-file duplication, file bloat/size, per-section "Section history" accumulation, and cross-file summary-fidelity drift — not just freshness/structure; first concrete deliverable is the PLAYBOOK/protocols consolidation surfaced by the 2026-06-03 audit · Done when: a doc-rot check/review surfaces at least the audit's HIGH findings AND the protocols consolidation lands · refs ADR-68, ADR-49, docs/audits/2026-06-03-protocols-rot-audit.md
- [#89] [P3][M] Build a deterministic prose-vs-state checker — assert living-doc claims match repo reality at the three loci where drift is mechanically checkable: counts (test/hook/check totals), version stamps (HANDOFF_PROCESS version, ADR `last_reviewed`), and enumerated lists (pre-commit hooks, commands/skills) — flagging edited-but-not-reconciled prose the freshness gate can't catch · Done when: the checker flags a seeded count/stamp/list mismatch and runs read-only (Layer-2) · refs #77, #83, ADR-65, the Phase-D codification handoff — disposition 2026-06-06: corp #75 audit (29 CLAUDE.md targets, 2026-06-06) — ~6 of 29 checks are binary existence checks, mechanizable deterministically; reserve Workflows for judgment-laden conformance. Concrete check-source for #89.
- [#90] [P3][M] Build a V4 git↔backlog two-direction reconciliation verifier (the inverse of V3's living-doc check): (a) every `closes [#id]`/`advances #id` commit since a baseline maps to a BACKLOG item in the expected state (closed→absent, advanced→present + current); (b) every merged arc has a backlog item, a closure, or a documented no-item class · Done when: the verifier surfaces a seeded record-gap in both directions and runs read-only · refs ADR-65, #81 (can host it as a verifier), this sweep's manual reconciliation as the reference spec
- [#99] [P3][S] FLEET-HEALTH digest names the failing check per red repo — today the digest shows `corp-monorepo !! 1 fail` with no check name; the operator needs a second command (`audit.py repo <name>`) to learn WHAT is red · Done when: a red repo's digest line carries the failing check name(s) · refs ADR-76, #85, ADR-36

### Run methodology conformance via agents, continuously
So that "is the methodology actually followed and current" is checked by isolated verifiers, not manual review under load.
- [#81] [P2][L] Build the methodology-conformance Dynamic Workflow for dev-knowledge — verifier-per-rule (fan-out) + skeptic persona (reviews the rules to suppress false positives), checking every methodology file/rule for drift + needed updates and proposing updates; the "Memory and rule adherence" pattern, run via /loop+/goal, read-only proposals only (no auto-write — Layer-2); Opus authors the harness + judges, Sonnet/Haiku fan-out workers; #77 doc-rot becomes one verifier within it · Done when: the workflow runs end-to-end, surfaces real methodology drift, and proposes updates for operator ratification · refs ADR-68, ADR-70 (Tier-3), #77, #80

### Wire up the lifecycle hooks the workflow relies on
So that session-start/close automation actually runs instead of being wired-but-vacuous.
- [#8] [P2][M] Implement the analysed hooks (session-end clean-tree/staleness + SessionStart lessons-retrieval) and resolve the review-hook overlap — the **closure loop is whole**: Stop→propose (`propose_closures.py`), SessionStart→surface + `/review-closures` review/approve/close (`review_closures.py`), ADR-70 Tier-1; still remaining → session-end clean-tree/staleness hook, SessionStart **lessons-retrieval** (#4 territory), and the review-hook overlap · Done when: the remaining hooks run + are documented · refs 2026-05-29 hooks audit, ADR-70 (Tier-1 lifecycle hooks) · changelog 2.1.148-167 finding — mechanism found (v2.1.163): Stop/SubagentStop hooks can return `hookSpecificOutput.additionalContext`, feeding Claude feedback and continuing the turn without a hook error; the session-end clean-tree/staleness enforcement is now implementable natively · audit-trio finding (docs/audits/2026-06-07-platform-max-audit.md UN-3) — Stop/SubagentStop `additionalContext` can ALSO surface closure proposals INLINE at session end, collapsing the write-then-next-session-nudge latency; scope = "emit the field"; mechanism confirmed shipped + docs-current
- [#98] [P2][S] PROPOSALS persistence — un-reviewed proposals must survive to operator review · Witnessed 2026-06-06: SessionStart banner `[closures] 1 proposed` while `PROPOSALS-2026-06-06.md` read "No closures detected" (`window_commits: 0`) — a later Stop-run had overwritten the file; the proposed item is unrecoverable (gitignored, no history). Fix candidates: per-run timestamped files + surface newest non-empty; carry-forward of un-reviewed items into regenerated files; refuse-to-overwrite while a file contains unchecked `- [ ]` lines · Done when: a proposal generated at Stop is still reviewable at the next SessionStart regardless of intervening sessions · refs ADR-70 (Tier-1), tier1-lifecycle plugin, `surface-closures.ps1`
- [#116] [P3][S] Hooks hygiene — `shell:"powershell"` + `args:[]` exec-form migration: replace wrapper-string invocations on our PowerShell hooks with the array exec-form to kill the Windows quoting failure class; note the `.cmd` shim caveat (per the hooks doc); also adopt `if:` conditional filters on PreToolUse guards to scope spawn overhead · Done when: our PS hooks use exec-form `args:[]` and at least one PreToolUse guard carries an `if:` scope filter · refs docs/audits/2026-06-07-platform-max-audit.md UN-5/UN-6
- [#117] [P3][S] Evaluate prompt/agent-based hooks (`type:"prompt"` Haiku-eval / `type:"agent"` multi-turn, experimental) — candidate: run the Tier-1 closure/triage eval in-session via a `type:"prompt"` hook instead of a Python shell-out; EXPERIMENTAL flag; GATED on the VF-1 auth check (add a trivial `type:"prompt"` PreToolUse returning approve, run any tool call, confirm NO auth error under the empty-key billing route) · Done when: VF-1 passes and a go/no-go on a prompt-hook Tier-1 eval is recorded · refs docs/audits/2026-06-07-platform-max-audit.md AD-2, VF-1, #8
---

## Lessons feedback loop
> As the methodology author, I want lessons to flow back into enforced rules, not sit in an archive.

### Make lessons an active feedback loop, not a passive archive
So that captured lessons reach runtime rules and stay enforceable.
- [#4] [P2][M] Build lessons-index.json + SessionStart retrieval + CLI query · Done when: lessons are queryable + surfaced at session start · refs ADR-35, ADR-70 (Tier-1 lesson-promotion)
- [#22] [P3][S] Broaden _LESSONS_H3_RE for an optional parenthetical (or reformat the 8 entries with sign-off) · Done when: the 8 parenthetical entries are ordering-checked · refs ADR-29

### Codify recurring patterns into the methodology
So that observed failure-modes become written guidance instead of recurring.
- [#34] [P3][S] Add the 4 posture-audit codifications to PLAYBOOK (stable-end-state, verify-destination, no-delete-canonical-dup, ADR-with-N=1) · Done when: the 4 land in PLAYBOOK · refs posture-audit H3/H4/T1/T2 · candidate codification: pre-emit prompt checklist — before any formal CC prompt: (1) header verbatim from PLAYBOOK non-negotiable spec, (2) Codex-applicability check (3+ code files / safety-critical), (3) context-budget pass on every read instruction, (4) JOURNAL-read needed? (observed 2026-06-06: three knowledge-present/application-skipped misses in one session)
- [#28] [P3][S] Codify the sage→apprentice metaphor pattern for teaching design intent · Done when: a short methodology section lands in PLAYBOOK/ESSENTIALS · refs HANDOFF_PROCESS v4 §3.1
- [#67] [P3][S] PLAYBOOK §7 polish: skill/command naming-convention note + orchestrator/worker how-to · Done when: both notes land in PLAYBOOK §7 · refs methodology-audit C10/C12
- [#111] [P3][M] PLAYBOOK prompt-format enrichment pack — (a) checkable-rules style for CLAUDE.md/prompt rules (models follow "max 50 lines" and break "clean code", even when traces show they know the rule); (b) circular-testing guard: acceptance criteria are authored in the prompt by the architect, CC derives tests FROM the criteria, never from its own implementation · Done when: both (a) and (b) land in PLAYBOOK · refs research transcript 2026-06-06

---

## Decision management
> As a reader of 65+ ADRs, I want decisions navigable and free of silent contradiction.

### Keep the decision corpus navigable and contradiction-aware
So that growth past 65 ADRs doesn't bury or quietly contradict prior decisions.
- [#2] [P1][L] Add contradiction-detection across decisions + an ownership model (amend vs new ADR vs clarification) · Done when: a detection mechanism + ownership rule exist · refs ADR-64 (consolidated-index sub-item done)
- [#23] [P3][M] Build a navigable supersedes/related/amends graph from ADR frontmatter (or an index doc) · Done when: the graph renders + is freshness-checked · refs posture-audit I1

### Codify the meta-decision rules
So that future sessions route decisions consistently (convene-vs-Path-A; relax-vs-gate).
- [#18] [P2][M] Codify the convene-vs-Path-A criterion in AI_COUNCIL_PROCESS/PLAYBOOK · Done when: the criterion is written · refs ADR-62/63/65
- [#27] [P3][M] Codify the cost/value-asymmetric relax-vs-gate criterion for drifted guards · Done when: a LESSON/ADR records the criterion · refs ADR-62/63, coherence-audit ML-2

### Operationalize the Council decision loop
So that Council questions are gated and their verdicts return deterministically instead of being shuttled by hand.
- [#70] [P3][M] Operationalize the AI-Council process — implement the gated question→verdict→ADR loop per ADR-67 (templated question + self-gate + deterministic `council.return_dir` return; downstream pieces in ai-council + ~/.claude). Gate lifted 2026-06-02 — Phase-2 universalization is complete (all 5 repos unified), so this is now actionable. · Done when: the gated loop runs end-to-end (/council-question generates + self-gates → council runs → operator pastes verdict → ADR returns via council.return_dir) · refs ADR-67
- [#96] [P2][M] ai-council VERDICT marker (producer-side) — synthesizer emits a machine-readable VERDICT block at the top of every transcript (verdict per question, one-line rationale, panel/cost metadata); consumers (distillation prompts) read only the block · Done when: a council run produces the block and a distillation completes without reading the debate body · refs ADR-74 (marker doctrine), PLAYBOOK context-budget rule; note: implementation lands in the ai-council repo via its dedicated chat (ADR-41) — this is the ecosystem queue entry; refs round-2 isolation audit (#110)
- [#110] [P3][S] ai-council round-2 isolation audit — verify whether round-2 debaters see round-1 peer arguments; 2026 evidence: 58% sycophantic convergence in debate, 23.9% unanimous-wrong by round 3; confirm blind-vote isolation properties hold for our 2-round design · execute in the ai-council dedicated chat · Done when: isolation properties confirmed or a remediation is recorded · refs #96, ADR-03, research transcript 2026-06-06

### Close the small ADR cross-reference + registry amendments
So that the ADR web is internally consistent.
- [#19] [P3][S] Add a BACKLOG.md lifecycle entry to ADR-39 (formerly bundled with #20) · Done when: ADR-39 registry includes BACKLOG.md · refs ADR-39/41
- [#21] [P3][M] Decide register/exempt/hybrid for the non-handoff templates/ class · Done when: the class decision is recorded in ADR-39 · refs ADR-39

---

## Canonical-file integrity
> As any agent reading this repo, I want the canonical files accurate and current.

### Keep canonical files accurate
So that stale ground truth stops silently misleading sessions (the repo's own VISION is "drift detected proactively").
- [#10] [P2][M] Decide TOKEN-LOG placement + align the docs — the file is at `logs/TOKEN-LOG.md` but CLAUDE/ARCHITECTURE/PLAYBOOK reference it bare (implying root) vs the ADR-59 ALL-CAPS-at-root rule; move-to-root vs docs-say-`logs/` is the operator's call (the broader doc-truth set was already closed by the 2026-06-01 `chore/doc-coherence-audit` — git history + JOURNAL hold that record) · Done when: TOKEN-LOG placement decided + docs aligned · refs coherence-audit 2026-06-01
- [#35] [P3][S] Fix the self-owned low-severity cleanups (ARCHITECTURE diagram attribution + SBAR label; VISION adoption-signal + stale last_reviewed) · Done when: WF-3 + GO-1/2 resolved · refs coherence-audit
- [#71] [P3][S] Reconcile ENVIRONMENT.md's `~/.claude/` directory tree with live contents (commands = boot/codex-review/evolve/session-summary, not handoff; skills = gotchas + verify; agents/hooks already match) · Done when: the ENVIRONMENT `~/.claude/` tree matches `ls ~/.claude/{commands,skills}` · refs G6 process-hardening sweep
- [#91] [P2][L] Rewrite ARCHITECTURE.md to reflect the shipped night/cloud architecture — the GitHub remote/push reality, the cloud-night pipeline + nightly Routine, the spec-orchestration doctrine, the in-repo conformance-hub workflow, the GitHub-Action/nightly-triage outcome loop, t-shirt model routing, C3 machinery retirements, and the adoption process — PLUS a supersession note on the ADR-68 night-agent section (it describes a never-registered mechanism, superseded in reality by the cloud Routine) · Done when: its row in the 2026-06-05-living-doc-staleness audit flips to CURRENT · refs docs/audits/2026-06-05-living-doc-staleness.md, ADR-68, ADR-51
- [#92] [P2][S] Add the spec-orchestration fallback rationale to CONTRIBUTING.md — why `.claude/workflows/conformance-hub.js` exists and when it runs (native-attempt-first, cloud-disabled launcher, nightly re-probe); the PARTIAL row's one missing piece · Done when: its row in the 2026-06-05-living-doc-staleness audit flips to CURRENT · refs docs/audits/2026-06-05-living-doc-staleness.md
- [#93] [P2][S] Add CLAUDE.md pointers to the new cloud/nightly/spec-orchestration/routing sections (STALE-by-deferral → pointer-current under the ≤200-line constraint) — SEQUENCED AFTER #91 (depends on ARCHITECTURE gaining the sections to point at) · Done when: its row in the 2026-06-05-living-doc-staleness audit flips to CURRENT · refs docs/audits/2026-06-05-living-doc-staleness.md, #91

### Keep the day-to-day docs right-sized and current
So that the cheat-sheet and architecture stay scannable as conventions accrue.
- [#5] [P2][M] Add high-leverage ESSENTIALS cheats for ADRs 35-76 under the one-page constraint · Done when: the chosen cheats land + ESSENTIALS stays ~1 page · refs posture-audit C2
- [#41] [P3][S] Split ARCHITECTURE §Processes into PROCESS.md if it grows past the comfort threshold · Done when: re-evaluated at the next process addition · refs ADR-51

---

## Cross-repo universalization
> As the disseminator, I want every child repo to converge on the universal baseline.

### Converge every child repo on the universal baseline
So that "open any repo, same layout/governance" actually holds.
- [#47] [P3][S] Confirm status of corp-knowledge-extractor / corp-by-os / corp-rfp-agent · Done when: each is classified (renamed/archived/uncloned/dropped) · refs 2026-05-11 audit
- [#9] [P2][M] Inventory skills across repos, classify repo-specific vs universal, propose a canonical home · Done when: a skills inventory + universalization proposal exists · refs VISION cross-repo consistency · changelog 2.1.148-167 finding — distribution simplification (v2.1.157): plugins in `.claude/skills` auto-load without a marketplace; `claude plugin init <name>` scaffolds; factor into the canonical-home proposal and fleet rollout of verify/guard organs
- [#82] [P3][M] Define per-repository agentic-review profiles — what agentic review each repo runs (dev-knowledge = methodology conformance #81; child repos = their domain needs, e.g. corp-monorepo deep-audit #75, ai-council's flow) and on what cadence, so agentic-ness is deliberate per repo, not one-size-fits-all · Done when: each repo's agentic-review profile is recorded · refs ADR-70, #75, #9, #70; note: iteration-2 verifier extensions (e.g. a folder/naming guardian for corp-monorepo) belong to this per-repo profile design, not to #81's hub conformance workflow; evaluation set grows: `/code-review` plugin (parallel multi-angle subagents, confidence-scored findings), `code-simplifier` post-diff agent, GitHub-triggered routines for PR review; doctrine note: heterogeneous second reader (different model architecture) is the field-validated shape — same-model self-review fails to sycophantic convergence · changelog 2.1.148-167 finding — native-first evaluation order (v2.1.152/2.1.154): `/code-review --fix` (applies findings; reuse/simplification/efficiency) and `/simplify` (cleanup-only review) are built into the CLI; evaluate natives before any plugin or custom simplifier agent · audit-trio finding (docs/audits/2026-06-07-platform-max-audit.md AD-4/KILL-3 + docs/audits/2026-06-07-codex-max-audit.md) — fold `/code-review ultra` into the evaluation (Claude-side: a COMPLEMENT, not a substitute for Codex cross-vendor heterogeneity — KILL-3); review-profile design inputs: `model_reasoning_effort` per-call/profile, `--ephemeral`, output-hygiene keys (`hide_agent_reasoning`, `model_reasoning_summary`, `web_search="disabled"`); xhigh-engages VERIFY recorded (`codex exec --sandbox read-only -c model_reasoning_effort=xhigh` on a throwaway diff)
- [#100] [P3][S] corp-monorepo CLAUDE.md freshness restamp — genuine end-to-end re-read + `last_reviewed` bump (edited 2026-06-06, stamp 2026-06-02; witnessed by fleet audit 2026-06-06) · **execute in the corp dedicated chat** (ADR-41 routing — this entry is queue-only) · Done when: corp audit `canonical_freshness` passes · refs ADR-41
- [#114] [P2][S] Publish `backlog-id-on-close` in the hub `.pre-commit-hooks.yaml` — expose the commit-msg hook (require `[#id]` when a commit removes a backlog task) via the ADR-71 source-repo carrier corp-monorepo already consumes for TOC; schema-agnostic (fires only when a task line is removed), children add one stanza; closes the silent-close half of child backlog drift · Done when: at least one child repo consumes the published hook and a backlog-removing commit without `[#id]` is blocked there · refs ADR-71, docs/audits/2026-06-07-methodology-transfer-audit.md Q1
- [#115] [P2][S] Distribute `/ship` via the tier1-lifecycle plugin — move `ship.md` (verified repo-agnostic) into the plugin whose carrier already ships `/review-closures` fleet-wide, carrying the branch→merge `--no-ff` discipline into child repos · Done when: a child repo invokes `/ship` from the plugin and the no-ff merge discipline travels with it · refs docs/audits/2026-06-07-methodology-transfer-audit.md Q2, #103
- [#120] [P3][S] Child backlog schema-conformance probe — verify each child repo uses the ADR-66 story-map (Big Picture → Theme → Story → Task) before `validate-backlog` can travel; prerequisite for distributing the hook, not the distribution itself · Done when: each child's BACKLOG schema-conformance is recorded (conformant / needs-migration) · refs docs/audits/2026-06-07-methodology-transfer-audit.md G3, ADR-66
- [#121] [P2][M] Child methodology floor — build the O2 Bounded Hybrid floor per ADR-78: (1) extend ADR-75 register with `methodology_surface` zone class (done in ADR-78); (2) `templates/child-methodology-floor.md.tmpl` — floor template (whitelist: prompt-header/valve-discipline/cadence/ship-rule/context-budget; F5 blacklist enforced at generation); (3) operator-invoked generator (rollout-moment, ADR-73 pattern — NOT scheduled, NOT autonomous); (4) update each child CLAUDE.md to reference `CLAUDE-FLOOR.md`; (5) conformance wiring: child pre-commit hash verify (into #114's hook family), hub CI F5-grep, hub `/ship` stale-hash warning, link-checker for pointer targets; (6) 3-week re-explain measurement post-rollout. **VERIFY FIRST:** check whether `@`-include in a child CLAUDE.md natively auto-resolves at CC session start — implementation is conditional on a positive result (council open-VERIFY (a)); if unresolved, log finding and gate remaining work · Done when: at least one child repo runs a CC session with the floor auto-loaded AND the conformance wiring passes · refs ADR-78, docs/audits/2026-06-07-methodology-transfer-audit.md G1/F2/F5, #114

### Make new-repo scaffolding correct-by-default
So that a new repo inherits the full baseline in one step, not by re-derivation.
- [#16] [P2][M] Collapse workspace-{S,M,L}.code-workspace into one maximal scale-adaptive template · Done when: one workspace template + element-selection guidance exist · refs operator Q5
- [#17] [P2][M] Encode ADR-54–63 high-leverage guidance into templates/CLAUDE-md-template.md (structural lock + frontmatter already done at v2.2; the stale "§11 last-5 = 61-65" sub-spec is dropped — the template uses generic `ADR-NN` placeholders by design) · Done when: the template's guidance reflects ADR-54–63 · refs durability-audit J1, template v2.2; scope extended: CLAUDE-md-template gains compaction-preservation instructions (what auto-compact must preserve: modified-file list, test commands, active branch, open task ids) · refs PLAYBOOK context-budget rule; evidence-backed compaction guidance (field: ~67% token reduction from threshold compaction; too-low thresholds cause destructive summary loops); add instruction-priority tiers (CRITICAL/STANDARD/PREFERENCES) to the template; pointer to subdir-CLAUDE.md item (#109) · audit-trio finding (docs/audits/2026-06-07-platform-max-audit.md UN-8) — native `/context` diagnostic (v2.1.74) as a companion to the compaction-preservation guidance
- [#43] [P3][L] Decide + (if yes) author a one-step new-repo scaffold (ADR + templates/new-repo-skeleton/, no scripts) · Done when: decision recorded + scaffold authored if approved · refs durability-audit J6
- [#109] [P3][S] Subdirectory CLAUDE.md convention — per-module CLAUDE.md layer for large repos; add to the template standard; pilot in corp-monorepo via its dedicated chat · Done when: the convention lands in `templates/CLAUDE-md-template.md` and corp pilot is queued · refs #17, ADR-55–63 cluster

---

## Tooling & evaluation
> As the operator, I want low-friction tooling and timely tech adoption.

### Cut session friction with better tooling
So that cognitive overhead per session drops.
- [#32] [P3][M] Audit VS Code extensions + workflow templates + tooling integration; produce a recommendation · Done when: a recommendation lands · refs VISION (tooling support)
- [#33] [P3][M] Build the 'Pinned Files' VS Code extension (TreeView of canonical files, local) · Done when: it loads + single-click opens + operator confirms ergonomic gain · refs trigger=keybindings-friction
- [#37] [P3][S] Add a workspace-sort-change verification rule (read the merged PR + a visual editor check) · Done when: the rule lands in PLAYBOOK/ESSENTIALS · refs ADR-59 amendment
- [#38] [P3][S] Codify small=single-root / large=multi-root workspace decision · Done when: an ADR-59 amendment or PLAYBOOK note lands · refs ADR-59
- [#39] [P3][S] Codify entry-scripts → scripts/ in PLAYBOOK root-hygiene · Done when: the convention is written · refs 2026-05-27 retrofit
- [#102] [P2][M] Machine-readable repo index for agent consumption — field reports ~50k-token exploratory reads replaced by ~250-token precompiled indexes; evaluate for hub + corp · verify-first: confirm claimed tools (`stacklit`, `scip-search`) actually exist before any design · Done when: a pilot index on one repo demonstrably replaces exploratory reads in a CC session · refs #89, #91, ADR-51, research transcript 2026-06-06
- [#118] [P3][S] Run the native `fewer-permission-prompts` skill — one-shot allowlist tightening (scans transcripts → proposes a prioritized allow-list) under the deny-rule posture; low effort · Done when: the skill has run and its proposed allow-list is reviewed + applied (or explicitly declined) · refs docs/audits/2026-06-07-platform-max-audit.md AD-3
- [#119] [P3][S] Codex currency — update 0.136.0 → 0.137.0 (Windows SQLite stability fix #25490 + exec approval-policy preservation) via the sanctioned `codex update`; add/refresh the ENVIRONMENT.md Codex version row · Done when: `codex --version` reports 0.137.0 and the ENVIRONMENT row matches · refs docs/audits/2026-06-07-codex-max-audit.md
- [#122] [P3][S] Retire the PATH shim (`claude.cmd`) — the native settings.json `env` route (empty `ANTHROPIC_API_KEY`) is primary since 2026-06-06 and covers all launch paths; the shim is now redundant. Sentinel stays as a tripwire; removal needs an explicit operator ask per the no-delete invariant · Done when: the operator approves and the shim is removed (or the item is closed as keep-for-defence-in-depth) · refs docs/audits/2026-06-07-platform-max-audit.md UN-1

### Decide the undecided artifact/tool models
So that cadence-less artifacts and unevaluated tools don't rot or get adopted blind.
- [#14] [P2][L] Decide the ecosystem/ folder operating model (regenerated snapshot / continuous-audit substrate / retire) · Done when: the model + cadence + ownership are recorded · refs Council-scope
- [#30] [P3][M] Evaluate Kimi K2 (capability / cost / integration); Council decision on adoption level · Done when: a Council verdict recommends an adoption level · refs VISION velocity
- [#94] [P3][M] /grok review — second-opinion reviewer experiment (EXPLORATORY: go/no-go pending, not yet committed work) — analogous to /codex review, calling the xAI `grok-build-0.1` API on the diff (≈$1/M in · $2/M out · 256K ctx · public beta since 2026-05-29; key lives in the central `.secrets/.env`, never per-repo). If greenlit, EXPERIMENT FIRST: run parallel to /codex review on ~10 real diffs, measure unique-catch rate vs noise rate, and report only deltas-vs-codex to protect operator triage bandwidth. NOTE: no published benchmark for the model *variant* itself — the 70.8% SWE-bench figure is the Grok Build CLI system, not this model; verify-before-encode · Done when: a go/no-go decision is recorded with calibration numbers (or an explicit operator drop without an experiment) · refs /codex-review, VISION velocity
- [#106] [P3][S] Auto Mode evaluation — classified permission middle-ground between bypass and babysitting (3 tiers, injection probe, audit trail) · verify-first: current availability/state on the Max plan · Done when: a go/no-go posture decision is recorded · refs unattended-run posture, ADR-68 autonomy boundary · changelog 2.1.148-167 finding — evaluation unblocked: Auto Mode no longer requires opt-in consent (v2.1.152), classifier improved (exfiltration detection, fewer false "could not evaluate" blocks, v2.1.154/160); evaluate against current state, not the research-preview caveats · audit-trio finding (docs/audits/2026-06-07-platform-max-audit.md VF-3) — exact availability check: `/config` → permission-mode picker, confirm "auto" appears WITHOUT setting `CLAUDE_CODE_ENABLE_AUTO_MODE` (that env var is Bedrock/Vertex/Foundry-only per 2.1.158; first-party "no longer requires opt-in" per 2.1.152)

- [#113] [P2][M] changelog-watch routine — scheduled routine fetches the Claude Code CHANGELOG diff since its last run, classifies entries against the stack rubric (hooks/guards, scheduling, context, review, permissions, distribution, worktrees, commands, ops), and emits a READ-ONLY candidates digest into the operator's morning funnel for ratification — never auto-implements · design refs: nightly-conformance shape (generate → filter → ratify), ADR-72 (if cloud-hosted: verify the routine environment can fetch the changelog URL — network-policy check is the gate); alternative host if cloud network denies the fetch: the local Tier-2 track (ADR-76) · Done when: one scheduled run lands a candidates digest and the operator ratifies/rejects from it · origin: operator proposal 2026-06-06 ("changelog na Cloud Code'a i wdrażanie nowości jako routine") · refs ADR-72, ADR-76, #85, #84 · audit-trio finding (docs/audits/2026-06-07-platform-max-audit.md + -codex-max-audit.md + -methodology-transfer-audit.md) — the audit trio is the manual v0 of this changelog-watch routine; the rubric + bucket format (UNDERSTAND/ADOPT/VERIFY/KILL/NOISE) from these prompts is the routine's spec seed
- [#123] [P2][S] Routine observability convention + value review — (1) all automation commits carry a standard marker (scope `chore(routine/<name>)` or trailer `Routine: <name>`) so routine output is git-indexable; (2) periodic value review in the morning funnel: findings-acted-on vs noise per routine (first datapoint: 2026-06-07 nightly digest 0/0/0 vs local baseline 1 FAIL) · Done when: the marker convention is recorded + adopted by the fleet/routine jobs AND one morning-funnel value review records per-routine findings-acted-on vs noise · refs #85, #14, #113, audit C matrix R7 · origin: operator proposal 2026-06-07
### Adopt Dynamic Workflows as the Tier-3 agentic harness
So that long-running, parallel, adversarial conformance/review work runs in isolated agent contexts instead of one drifting window.
- [#84] [P2][M] Workflow-doctrine codification package — author AFTER the corp-monorepo baseline lands (n=2 rule: two real Tier-3 runs before codifying a pattern — note: part (d) is UNGATED, purely editorial from existing records, and precedes Track C per operator ruling 2026-06-05): (a) a PLAYBOOK section capturing the two-tier pattern (Haiku-swarm fan-out + main-session-Opus verification of every load-bearing finding) + the read-only safety envelope (session Write/Edit deny + post-run fleet tripwire) + the Workflow-vs-subagent escalation rule (this section closes [#74] — RECONCILED 2026-06-05: #74 closed 2026-06-05 by the standalone escalation rule in PLAYBOOK "When to escalate to a Dynamic Workflow" (commit 11ef2d8); (a)'s fuller section — two-tier pattern + safety envelope — remains n=2-gated — evidence trail: run 1-of-2 recorded (corp scoped audit 2026-06-06, 29 targets / ~588k tok / ~4 min)) — forward-note: include the self-measuring-tool + degenerate-metric-guard line (external note §3); (b) an adoption ADR distilled from the Downloads Dynamic-Workflows research note (its landing commit closes [#80] — RECONCILED 2026-06-05: #80 closed 2026-06-05 (note landed + #74 informed); the adoption ADR remains (b)'s deliverable, no longer #80's closure condition); (c) a PLAYBOOK clarification of the two ADR-authorship paths (chat-drafted vs Council-convened) (operator-requested); (d) a PLAYBOOK Routine/night deployment-standard section — the cloud-night safety envelope (allow-only platform guards, no committed Write/Edit deny), the spec-orchestration doctrine (native-attempt-first + nightly re-probe), the outcome loop (Action diff-guard + nightly-triage Issues + SessionStart surfacing), t-shirt model-routing pins, cloud-session closeout (check stranded `claude/*` branches), and the shallow-clone finding class · Done when: the PLAYBOOK section + adoption ADR + ADR-authorship-paths note + the Routine/night deployment-standard section all land (the last flips the PLAYBOOK row toward CURRENT in docs/audits/2026-06-05-living-doc-staleness.md) · refs #74, #80, #81, ADR-70, ADR-67, docs/audits/2026-06-05-living-doc-staleness.md · changelog 2.1.148-167 finding — three codification inputs: (a) the dynamic-workflow trigger keyword is now `ultracode`, the word "workflow" no longer triggers (v2.1.160) — audit PLAYBOOK/escalation-ladder wording at codification; (b) Opus 4.8 defaults high, `/effort xhigh` exists, fast mode = 2× cost for 2.5× speed (v2.1.154) — refresh the Model/Effort table; (c) `fallbackModel` (up to 3, applies interactively, v2.1.166) needs a doctrine ruling — interactive sessions: allowed; pinned workflow/routine stages: document the policy explicitly · audit-trio finding (docs/audits/2026-06-07-platform-max-audit.md VF-2) — fallback ruling input: run the `fallbackModel` schema-accept test on subscription (add `"fallbackModel":"claude-sonnet-4-6"` to settings.json, start claude, confirm no schema reject) before codifying the interactive-vs-pinned-stage policy
- [#85] [P3][L] Night-run architecture — LOCAL track (direction ratified 2026-06-06: local = Tier-2 cross-repo host, structural advantage: only local sessions see sibling repos, ADR-74; mechanism ratified 2026-06-06 per ADR-76 Council verdict: Windows Task Scheduler → `python scripts/fleet_health.py` directly, no `claude -p` on scheduled path — LLM deferred to next interactive SessionStart; missed-run = Scheduler built-in catch-up "run as soon as possible after missed start", no wake-from-sleep, no alerting; rider R2: missed nights tolerated by design — fail-soft + catch-up + SessionStart surfacing is the accepted degradation path; config version-controlled in `scripts/setup-fleet-scheduler.ps1`; n=1 gate still pending — ADR-76 records the mechanism, not the gate run; natural n=1 workload: scheduled fleet-baseline run as a scheduled local task; fire-and-complete stages, Workflow tool pre-allowed in settings, session Write/Edit deny + post-run fleet `git status --porcelain` tripwire, digest-only output) · Done when: a scheduled local night-run executes a read-only conformance/review workflow end-to-end and lands a morning digest · refs #82 (profiles), #84 (codification), ADR-68, ADR-74, ADR-76, docs/audits/2026-06-03-phase0-workflow-gates-findings.md (gates 4/5), docs/archive/2026-06-05-agent-automation-external-research-note.md

---

**About this file** — open `.dev-knowledge` work as a story map (ADR-66): Big Picture → Theme → User Story → Task. Stories are human (goal + `So that`); tasks carry `[#id] [P][size] · Done when · refs`. Done tasks **leave** (ADR-65); git is the implementation record (`git log --grep 'closes \[#'`). Child-repo execution items live in `docs/audits/2026-06-01-child-repo-relocation-proposal.md`. Schema: PLAYBOOK §10; machine-checked by `scripts/validate_backlog.py`.

**Grooming log:** 2026-05-09 · 2026-05-23 · 2026-05-24 · 2026-05-31 (marathon-arc) · 2026-06-01 (ADR-64/65 migration + readability + ADR-66 story-map) · 2026-06-02 (git-verified retroactive closure: retired #29/#31/#40/#44/#45/#46; re-scoped #17/#13; updated #70/#10; ADR-69 records the #44 reach model) · 2026-06-02 (ADR-70 incorporation: annotated #13/#8/#12/#72/#4 with `refs ADR-70` as the three-tier build units; added #73 Tier-1 plugin bundle + cross-repo install, #74 Workflow-escalation rule, #75 first scoped corp-monorepo Tier-3 Workflow) · 2026-06-02 (closes [#13]: ruff pre-commit gate wired + proven; #13 retired) · 2026-06-02 (closes [#72]: fleet_health.py Tier-2 daily cross-repo audit + no_sibling_orphans now runs all 5 repos; #72 retired) · 2026-06-03 (agentic-arc prep — added #80 Dynamic Workflows research P1, #81 methodology-conformance workflow P2, #82 per-repo agentic profiles P3; next-session focus is the Tier-3 agentic conformance/review arc composing ADR-68 night-agent + ADR-70 Tier-3 with #74/#75/#77/#70 + #80→#81→#82, grounded in the shipped Dynamic Workflows feature; #70 AI-Council loop remains the heavy-decision path composing with the heavy-execution workflows) · 2026-06-05 (pre-handoff sweep — git↔BACKLOG reconciliation: withdrew [#87] (routing bug-report premise refuted by the Phase-C3 re-probe — non-functional routing was our own `CLAUDE_CODE_SUBAGENT_MODEL=haiku` override, not a platform bug), corrected #83's stale "Haiku-only de-facto" note, added [#89] deterministic prose-vs-state checker + [#90] V4 git↔backlog two-direction verifier; all other arcs since 2026-06-04 reconciled consistent). Next quarterly: 2026-07-01. · 2026-06-06 (closes [#75]: corp scoped audit Workflow ran end-to-end 2026-06-06, 29 targets / ~588k tok / ~4 min, fit confirmed; annotated #89 with check-source + #84 with n=1-of-2 evidence trail). · 2026-06-06 (nightly-triage N1 ratified ACCEPTED-AS-CLARIFICATION — #74 closure clarification: the Done-when's "alongside #18/#27" clause was *consciously* satisfied via the PLAYBOOK forward-pointer, not literal co-location; the clause reconciliation is recorded in the Track B close (commits 11ef2d8 / 4c62f63). No literal co-location work owed; #18/#27 remain open siblings; no reopen). · 2026-06-06 (closes [#86]: sub-decisions 1–2 → ADR-72; sub-decision 3 → ADR-73; ruled 2026-06-06, source corp JOURNAL 2026-06-06). · 2026-06-06 (doctrine-pass codification: amended #85 to record ratified direction — Tier-2 cross-repo host, Desktop scheduler, n=1 gate, riders R1/R2, ADR-74; added #95 template↔copy drift watcher (G5), refs ADR-72/73/74). · 2026-06-06 (Council verdict per ADR-76: amended #85 to replace Desktop-scheduler mechanism with Task Scheduler → python directly; added ADR-76 to refs; rider R1 removed — n=1 gate still pending). · 2026-06-06 (funnel-integrity defects: added #98 PROPOSALS persistence, #99 FLEET-HEALTH check-name detail, #100 corp-monorepo CLAUDE.md restamp queue; annotated #5 scope ADRs 35-63→35-76, #34 added pre-emit prompt checklist codification candidate). · 2026-06-06 (billing-trap capture: added #101 SessionStart billing-leak sentinel — fail-soft WARN when ANTHROPIC_API_KEY visible in CC session). · 2026-06-06 (SOTA gap-analysis capture: added #102 machine-readable repo index, #103 /ship git-finish command, #104 verify-as-skill, #105 PreToolUse immutability guard, #106 Auto Mode eval, #107 worktree convention, #108 Projects-as-context-layer eval, #109 subdir CLAUDE.md convention, #110 ai-council round-2 isolation audit, #111 PLAYBOOK prompt-format enrichment pack; refs research+article review 2026-06-06). · 2026-06-06 (immutability-guard implementation wave: shipped the transcripts immutable-zone — `block_immutable_edits.py` PreToolUse guard live + wired + witnessed, ADR-77 amends ADR-75; annotated #105 PARTIAL + scope-split (handoffs/audits dropped — sanctioned append flows); added #112 adr_amend helper + ADR-zone extension (Option A; Option B held until #23)). · 2026-06-06 (closes [#101]: SessionStart billing-leak sentinel `scripts/billing_leak_sentinel.ps1` wired + witnessed both branches (leaked→WARN, clean→silent); `-NoProfile` required so the hook reads CC's env not the re-sourced profile — new gotcha recorded). · 2026-06-06 (closes [#104]: verify skill shipped — `.claude/skills/verify/{SKILL.md,verify.py}` + `templates/prompt-template.md` v1.2 drops inline check sequence in favour of skill invocation). · 2026-06-06 (closes [#97]: artifact-reader subagent shipped — `.claude/agents/artifact-reader.md` (Read/Grep/Glob, model pinned); witnessed on 96,897-byte transcript → 1.3k summary (~98.7% context reduction)).
