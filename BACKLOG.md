# .dev-knowledge BACKLOG

## Big picture

`.dev-knowledge` is the ecosystem's methodology brain: it absorbs lessons from every repo, universalizes them into enforced conventions, and audits the ecosystem against them — the LLM-development "scrum master" for all `Dev/` projects (VISION: Knowledge Guardian · Methodology Author · Auditor · Disseminator). The backlog advances that mission across seven themes.

**Themes (backbone):** Handoff continuity · Enforced governance · Lessons feedback loop · Decision management · Canonical-file integrity · Cross-repo universalization · Tooling & evaluation

---

## Handoff continuity
> As a session inheriting this repo, I want to pick up with full state and lose nothing.

### Give every handoff a fresh-eyes review, not just the big ones
So that routine handoffs stop inheriting the ~25% insider blind spot.
- [#1] [P2][L] Extend triangulation from the promotion gate to every routine handoff (sub-prompt / periodic audit / agent-framework) · Done when: every Phase-2 handoff runs a fresh-eyes pass · refs protocols/AGENT_FRAMEWORK.md (Council) · Evidence: 2026-06-03 bundle review caught (A1) an unverified session-state claim bypassing the cross-check table and (A2) a "Tier" terminology collision across three axes — both defects that an adversarial fresh-eyes pass would catch (v4.3.2 §D) · serialize-group: handoff

### Let an inheritor act on the operator's defaults instead of re-asking
So that a fresh chat states-and-acts rather than over-asking on things it should assume.
- [#26] [P3][S] Add an operator-invariants section to the handoff (clean-tree, immediate-merge, three-domain) · Done when: the section ships in the handoff (now the v5 boot/residual — the v4 Phase-1 interview/bundle target is superseded; the goal persists) · refs HANDOFF_PROCESS.md (v5) · serialize-group: handoff

### Match the handoff payload to the work mode
So that an architecture session inherits the big picture + vision while an execution session inherits lean task-state — one process, two payloads.
- [#159] [P2][S] Operator-context beat for architect mode — after orienting (§13c) and before design, the browser makes ONE targeted ask for **off-repo** context (intent / priorities / off-repo findings / changed decisions). **Architect mode ONLY** (execution unchanged); **off-repo only** (does not re-narrate the CC residual); a **§13(d) posture step** + HANDOFF_BOOT, not a new bundle file. Mechanism shipped + hardened through v5.2 (always-generated fillable SUPPLEMENT; cold-handoff disposition defined); the fill→fold half is dogfooded. · Done when: the incoming §13(d) beat is exercised in a real architect session with a *filled* supplement · refs protocols/HANDOFF_PROCESS.md §13, protocols/HANDOFF_BOOT.md, #164 · serialize-group: handoff
- [#161] [P3][S] **Capture only — do NOT codify**: define a stable teeth probe-core (the ~5 shared forced-read probes) plus the architect-only orientation probe, so bundles stop hand-assembling probe sets per handoff (today the architect bundle lacks execution's §5-labels probe; probe sets are assembled by hand). Thin signal — scoping is a future architect-session decision, not pre-committed here. · Done when: scoped by a future architect session (a stable probe-core + the architect orientation probe defined, or each explicitly deferred) · 2026-06-25 (C2): the §5 shared probe-core was consolidated to 5 probes (drift-flag + freshness → ship-gate read-back, v5.3) — the shared-core leg is now defined; remaining scope = the architect-orientation-probe split + the architect/execution hand-assembly reconciliation, tied to the #164 generator · refs protocols/HANDOFF_PROCESS.md §13, protocols/HANDOFF_BOOT.md, #150, #159, #164 · serialize-group: handoff
- [#162] [P2][M] Vocab decision (architect-session): disambiguate "architect" as the Layer-1 **actor** (ARCHITECTURE.md / ADR-28 — the browser-chat role) vs the handoff **mode** (HANDOFF_PROCESS.md §13 / HANDOFF_BOOT — architect|execution). Enumerate every surface where each sense appears and decide the disambiguation atomically (rename one sense, or formally scope both — do not leave the collision live). The boot-ack narrow slice shipped 2026-06-11 (HANDOFF_BOOT.md ack → "Layer-1 browser", no on-load mode claim); the model decision remains. · Done when: an ADR or operator ruling lands the disambiguation across all enumerated surfaces · refs ADR-28, protocols/HANDOFF_BOOT.md, protocols/HANDOFF_PROCESS.md §13, ARCHITECTURE.md Ch1 · serialize-group: handoff

### Finish the v5 handoff machinery deferred at the #149 flip
So that the canonical v5 handoff is fully implemented (generator) and mechanically verified (teeth), not just specified.
- [#164] [P2][L] Implement the v5 `/handoff` generator — replace the SUPERSEDED v4 8-file two-phase mechanics with the v5 generator per `HANDOFF_PROCESS.md` §2–§10/§13 (architect|execution mode param). · Done when: `/handoff` create/complete emit the four-file bundle (HANDOFF_BOOT + RESIDUAL + PROBES + PASTE_THIS, no per-bundle README) with the session-header in HANDOFF_BOOT, idempotently seed each repo's `docs/handoffs/README.md` runbook from one source (`templates/handoff/v5/README.md.tmpl`), regenerate PASTE_THIS on emit + freshness check, write `<bundle>/SUPPLEMENT.md` unconditionally in architect mode, and remove the v4 generator prose from command + SESSION_SETUP · **ADR-83 LIVE constraint (must survive):** the v4 `templates/handoff/` bundle templates are LIVE for v4 repos (corp-monorepo's v4 cross-repo path) — re-archival + v4.4/live stamp-pin removal is gated to this item's close (v5 cross-repo generator lands + corp migrates off v4); removal arc 2026-06-24 verified-refuted (targets live/gated) · refs protocols/HANDOFF_PROCESS.md §2–§10/§13, .claude/commands/handoff.md, protocols/SESSION_SETUP.md, templates/handoff/v5/, #163 · serialize-group: handoff

---

## Enforced governance
> As the operator, I want load-bearing conventions enforced by tools, not memory, so they can't silently drift.

### Turn advisory guards into enforced gates
So that a convention can't be skipped under load (the failure mode behind real aborts).
- [#146] [P3][S] De-hardcode-first doctrine + sweep — the `amendment_coherence` gate (#11, shipped) is a BACKSTOP; the superior fix for a coupled version surface is to de-hardcode it so it interpolates the anchor (the v3.4 handoff surfaces already do — `handoff.md`/templates read `{{VERSION}}`). #11's honest limit names de-hardcoding as the actual v3.4 fix, so per ADR-81 (d) that deferral must be tracked, not left as a disclaimer: (a) record de-hardcode-first as doctrine in PLAYBOOK "Multi-surface amendment coherence"; (b) sweep remaining hand-maintained version surfaces for de-hardcoding candidates · Done when: de-hardcode-first is doctrine AND a sweep is run (each candidate de-hardcoded, or explicitly kept-as-manifest with a reason) · refs #11, ADR-81 (d), LESSONS 2026-05-29 (v3.4 abort) · serialize-group: playbook
- [#15] [P2][M] Add hyphen-only-separator enforcement (pre-commit + Action) with a scoped path set + exceptions · Done when: a non-conforming new filename is blocked · refs ADR-34 · serialize-group: pre-commit-config
- [#105] [P2][S] PreToolUse immutability guard — block Edit on `docs/decisions/*`, transcripts, handoffs, audits (new-file creation allowed); extends the block-onedrive pattern; immutable paths become a second zone class under the ADR-75 register (amending ADR) · Done when: an in-place edit attempt on an existing ADR is blocked fail-closed and a new-ADR creation passes · 2026-06-06 PARTIAL (stays open): transcripts portion shipped — `scripts/hooks/block_immutable_edits.py` live + wired + witnessed, ADR-77 amends ADR-75; handoffs/audits REMOVED from scope (sanctioned append / dated-addenda flows); ADR portion re-routed to #112 (Option A script-enforced amend path, per the operator ruling — Option B held until #23)
- [#112] [P2][M] adr_amend helper + ADR immutable-zone extension — `scripts/hooks/adr_amend.py` becomes the ONLY sanctioned writer of an Amendment/status line on an existing ADR (canonical format from ADR-68 + HANDOFF v4.3 Amendment A); extend `block_immutable_edits.py` to deny every OTHER in-place ADR edit (fail-closed); resolve the CLAUDE.md §5 self-contradiction ("supersede with a new file or in-file marker; never edit in place" → "append-only via the helper"); includes the CLAUDE.md end-to-end re-read + `last_reviewed` restamp · Done when: an in-place ADR edit NOT routed through the helper is blocked, a helper-written Amendment passes, and CLAUDE.md §5 is de-contradicted + restamped · Design caveat (live): a native `permissions.deny` path-glob on the transcripts/ADR zones is **defense-in-depth ONLY** — never a replacement for the PreToolUse guards (deny-globs are bypassable and cannot express exists-deny/new-allow; native OS sandbox is not available on native Windows, so the guards stay) · refs ADR-77, #105, #23, ADR-68 · depends-on: #23 · serialize-group: claude-md
- [#153] [P2][M] Enforcement-completeness pass — move browser-re-stated constraints to CC-side mechanical enforcement (a rule the browser repeats each session is the symptom of a missing hook). The `--no-ff` FF-guard shipped (`no_ff_merges` WARN, hub-only) and the true-FF *prevention* pre-push gate too (`block-ff-push`, 2026-06-20, hub-only + bypassable). Remaining: server-side prevention teeth (the local hook is bypassable); mechanize-or-accept each of minimal-diffs / append-only (LESSONS/TOKEN-LOG/JOURNAL) / no-CHANGELOG-recreation; **define core-invariant #5's `--no-ff` scope boundary** (hub vs `~/.claude` vs child repos — which trees the rule binds); and decide the **methodology-reach question** — should ecosystem commit-discipline enforcement reach `~/.claude` at all, or is that the runtime-config repo's own concern (filed separately as the `~/.claude`-executed #189). · Done when: each remaining prose-only constraint is mechanized or recorded as accepted-prose-only with a reason, the #5 `--no-ff` scope boundary is defined, and the ~/.claude-reach question is decided · refs ~/.claude/rules/core-invariants.md #5, #189 · serialize-group: audit-py
- [#185] [P2][M] GAP-2 deterministic gotcha-injection guard — a read-only PreToolUse guard that injects the matching gotcha when a commit-message / Edit-deletion pattern is about to fire (the execution-time micro-decision class that recurred n=2/n=3 even with the standing "check gotchas" line; prose can't fix it, a deterministic backstop can). Orthogonal to the ADR-87 prompt contract (ADR-87 closes the governance/read-only load gaps GAP-1/GAP-3, not GAP-2). Sibling of the #105/#112 PreToolUse guards. · Done when: a seeded commit-message / Edit-deletion pattern triggers injection of the matching gotcha (read-only, fail-soft), with tests · refs ADR-87, #105, #112, gotchas skill
- [#188] [P3][M] Deny-rule + hook completeness audit — a read-only pass enumerating every PreToolUse guard / `permissions.deny` glob / pre-commit hook against the zones they should cover (ADR-75 register), surfacing uncovered zones + bypassable globs (a coverage matrix). Distinct from #132's organ *inventory* — this is a coverage *gap* audit. · Done when: a read-only pass emits a coverage matrix flagging any zone/path lacking a guard · refs #112, #185, #132, ADR-75, ADR-77
- [#191] [P2][S] file_path PreToolUse guard for the P0 OneDrive exclusion zone — `block-onedrive.ps1` inspects only the Bash/PowerShell command string, so an Edit/Write/NotebookEdit whose `file_path` resolves under an `OneDrive - Blue Yonder` path is NOT caught (the P0 zone's file_path write-vector is unguarded — surfaced by the #188 hook-completeness audit, MEDIUM). Add a PreToolUse guard inspecting the Edit/Write `file_path` against the OneDrive exclusion pattern, fail-closed (sibling of the #105/#112/#185 path guards). · Done when: an Edit/Write targeting a `file_path` under an `OneDrive - Blue Yonder` path is blocked fail-closed, a normal-path edit passes, with a test · refs #188, ADR-75, #105, #112, #185, global CLAUDE.md §P0

### Extend structural validation to more governance artifacts
So that drift in transcripts, ADRs, and folders is caught cheaply, not by reviewer luck.
- [#7] [P2][M] Apply the audit.py #8 structural-validator pattern to Council transcripts + ADRs (frontmatter/supersession/template) · Done when: registered checks + tests folded into health · refs scripts/audit.py #8/#9 · serialize-group: audit-py
- [#36] [P3][M] Add read-only check_folder_semantics asserting each docs/ subfolder carries one ADR-60 role · Done when: the check ships with fixtures + tests · refs ADR-60 · serialize-group: audit-py
- [#42] [P3][S] Decide whether mermaid-theme check #7 expands beyond ARCHITECTURE.md (extend paths or accept) · Done when: scope decision recorded · refs durability-audit J4
- [#95] [P3][M] Template↔copy orchestration drift watcher (G5) — per ADR-73, per-repo orchestration copies propagate at rollout moments but no organ detects a copy diverging from the hub canonical template between rollouts; candidates: version/hash header in copies + deterministic check, or a rollout-checklist item · Done when: a deterministic, read-only check (or ratified checklist step) detects template↔copy divergence and surfaces it · refs ADR-72, ADR-73, ADR-74 · serialize-group: audit-py
- [#77] [P3][L] protocols/ content consolidation — operator-gated pass to resolve the rot the protocols-rot audit surfaced, 4 legs (each operator-approved before landing): (1) extract PLAYBOOK §18 (hub-only audit.py/ecosystem tooling) from the universal protocol; (2) reconcile the three divergent lesson→rule statements into one canonical statement; (3) trim ESSENTIALS to its "under 1 page" contract; (4) strip the PLAYBOOK Section-history blocks (ADR-49-retired changelogs) · Done when: all 4 legs land · refs ADR-49, docs/audits/2026-06-03-protocols-rot-audit.md · [CLOSURE-VOIDED 2026-06-09: 77e5d7d closes [#77] was a misattribution — shipped CONTRIBUTING→v4, not this work; doc-rot checker split into #140] · serialize-group: playbook
- [#139] [P2][L] merged-arc→record verifier (a.k.a. **#90b** — direction (b), deferred from #90 which shipped direction (a) as `scripts/validate_git_backlog.py` + the `git_backlog_drift` audit check) — every merged arc on `main` maps to a backlog item, a closure, or a documented no-item class (cross-repo arc per ADR-41, doc/chore, routine baseline), by inspecting the arc's **content** (`<merge>^1..<merge>^2` work commits + the merge body), **NOT** the merge subject: the #90 field finding showed /ship merge subjects are never Conventional-Commits-prefixed (`Merge <branch> — …`), so a `^(feat|fix)` subject predicate matches nothing = a vacuous pass · Done when: the verifier surfaces a seeded merged-arc lacking an item/closure/no-item-class, runs read-only (Layer-2), and is demonstrably NOT vacuous against real /ship merge subjects · refs ADR-65, ADR-41, #90 (direction (a) shipped), this session's merge-subject finding · serialize-group: audit-py
- [#99] [P3][S] FLEET-HEALTH digest names the failing check per red repo — today the digest shows `corp-monorepo !! 1 fail` with no check name; the operator needs a second command (`audit.py repo <name>`) to learn WHAT is red · Done when: a red repo's digest line carries the failing check name(s) · refs ADR-76, #85, ADR-36
- [#132] [P2][M] Organ-index generator — `scripts/generate_organ_index.py` (read-only, codemap/toc pattern — Layer-2-safe) walks `.claude/{agents,commands,skills,workflows}`, the plugin manifest, `settings.json` hooks, `.pre-commit-config.yaml`, and user-level `~/.claude` → emits `docs/ORGAN-INDEX.md` (name / class / trigger / source / distribution / status) + a freshness gate (the codemap/toc-freshness hook pattern); kills "operator-as-registry" · Done when: the generator emits `docs/ORGAN-INDEX.md` covering all organ classes and a freshness hook flags a stale index · refs ADR-71, ADR-74, docs/audits/2026-06-07-copilot-collections-peer-audit-v2.md §3.6 roster table (seed shape), #95 · serialize-group: pre-commit-config
- [#166] [P3][M] doctrine_enforcement_coherence check — a read-only `audit.py` check flagging any ADR/amendment whose status still contains `PROPOSED` / `NOT RATIFIED` while its implementing task is closed (or its enforcement is already live in a gate) — i.e. live enforcement running ahead of its doctrine (the #156 gap this session reconciled by hand) · Done when: the check ships read-only with fixtures + tests, folds into `audit.py health`, and flags a seeded enforcement-ahead-of-doctrine case · refs #11 (amendment_coherence — version-straggler sibling), #153 (enforcement-completeness), ADR-65 (done-items-leave), this #156 gap · serialize-group: audit-py
- [#190] [P3][M] General intra-file duplication detector — a read-only WARN-only `audit.py` check for the "resident-copy drift WITHIN a file" class (a rule/disposition stated authoritatively in two places in one doc — the failure class this repo is named to kill). Deferred from #140: the `doc_rot` checker shipped the history-accretion / file-bloat / grooming-cadence categories and explicitly did NOT build intra-file duplication. #157 is the concrete CLAUDE.md §4/§5 file-lifecycle instance; this is the general detector (deterministic near-duplicate-block first; semantic/LLM dedup stays out per ADR-88 Principle 5 do-not-build). · Done when: a read-only check flags a seeded intra-file duplicated block (WARN, with tests), or it is explicitly closed as not-mechanizable · refs #140, #157, ADR-49, ADR-88 · serialize-group: audit-py
- [#210] [P3][S] Convert journal-wrap no-ff WARNs from per-instance disposition to a standing rule — 3 instances of one class (journal-wrap direct-to-main) are now ratified one-by-one in `ecosystem/disposition-register.yaml`, so it is a recurring structural gap, not one-offs. Likely shape: a path-scoped EXEMPT in `no_ff_merges` for a direct-to-main non-merge commit whose diff is CONFINED to `JOURNAL.md` + `docs/.../transcripts/` (any OTHER path in the diff still WARNs — must NOT weaken core-invariant #5 for real code). Alternative: enforce branch-then-merge for the session-end JOURNAL wrap (eliminate the class, not exempt it). Decide the shape when built. · Done when: the shape is decided AND either (a) the exemption ships with a test proving a JOURNAL-only direct commit passes while a same-commit code-path edit still WARNs, or (b) the wrap moves behind a --no-ff arc; the 3 per-instance dispositions then retire · refs ecosystem/disposition-register.yaml, scripts/validate_no_ff.py, core-invariants #5, ADR-75 · serialize-group: audit-py
- [#211] [P2][M] propose/review closure-script hub<->plugin parity — `scripts/{propose_closures,review_closures}.py` are the tested-canonical source but their `plugins/tier1-lifecycle/scripts/` carriers (which actually run, via the plugin Stop-hook + `/review-closures`) have diverged with NO parity test — the hand-mirror drift `test_validate_backlog_twin_parity.py` (#206/GAP-2) pins for `validate_backlog`, on a sibling pair it doesn't cover. Divergence is mixed: the carriers legitimately add `_host_root()` (CLAUDE_PROJECT_DIR resolution — must stay) while the hub gained a `git_log_commits(first_parent=)` param the carriers lack (the real drift), so a parity test must compare MODULO the known root-resolution delta (as #206 does for the bundled path). · Done when: a twin-parity test covers both propose/review hub<->plugin pairs (extend the #206 pattern, allowing the _host_root delta) OR the carriers are single-sourced from canonical · refs docs/audits/2026-06-26-corpus-graph-justify-or-retire.md (obs A), #206, plugins/tier1-lifecycle/scripts/

### Catch spec/dependent drift mechanically, not by memory
So that "spec bumped, dependent partially updated, caught only because a human remembered" stops being the failure mode — machine-checkable coherence between a dependent doc and its spec.
- [#180] [P3][L] Transclusion engine — for highest-duplication content, remove the drift surface (single-source + transclude) rather than detect it; the removal arm of the coherence doctrine; GATED on DEC-04 (lead-with-removal vs detection — unresolved) · Done when: DEC-04 resolved in favour of removal AND a transclusion mechanism single-sources one real high-duplication block, with tests · refs #172, DEC-04 · serialize-group: coherence
- [#181] [P2][S] Coherence v2 nudge-response — decide escape-hatch vs deferred-hash vs promote-nudge-to-gate for the forgotten-version-bump nudge; data-gated on logs/coherence-nudge.log (noisy → escape-hatch/deferred-hash; accurate+rare → promote to gate) · Done when: the nudge log shows real firing signal AND the response is decided + implemented, with tests · refs #172, coherence_nudge.py, SUPPLEMENT-B · serialize-group: coherence
- [#182] [P3][M] Folder-level dependency contracts (_index.md) — DEC-07: declare folder-as-unit reconciliation (rollup-over-files; folder-as-unit for high-stakes only) · Done when: DEC-07's file-vs-element + high-stakes threshold decided AND _index.md folder contracts checked by the enumerator, with tests · refs #172, DEC-07 · serialize-group: coherence
- [#183] [P3][S] Reparent #169/#171/#166 under the coherence-spine doctrine — a deliberate evidence-gate, not an oversight · Done when: ≥2 real coherence drifts observed AND a #169-fit decision made, then #169/#171/#166 reparented (or explicitly kept independent) · refs #172, #169, #171, #166
- [#220] [P2][M] MODIFY / semantic-drift dependency axis — the coherence spine (#172, ADR-88) and the ADR-89 edges catch a dependent drifting from its source by VERSION (`reconciled_with` mismatch) and by EXISTENCE (undeclared / broken / removed edge), but not by MEANING: a source whose text or behaviour materially changes while its version pin and symbol signature stay put leaves dependents silently stale (the MODIFY / semantic-drift edge referenced across the coherence arc but never filed). VERIFY-FIRST: does the ADR-89 Pyright oracle (`references()`) or any existing check gate a MODIFY edge, or only add/remove/exist? Establish a demonstrated-catch bar before any design. · Done when: a verify-first pass records whether any existing organ detects a semantic/MODIFY change AND either a read-only detector flags a seeded meaning-change-without-version-bump on a fixture, or the axis is explicitly deferred with a recorded reason · refs #172, #180, #183, ADR-88, ADR-89 · serialize-group: coherence

### Know what depends on code before removing it (ADR-89 computed edges)
So that a symbol deletion can't silently break a dependent — the code analog of catching doc/spec drift.
- [#218] [P3][M] Safe-removal gate M2+M3 boundary (the deferred phases of #195) — extend the code→code (M1) safe-removal gate (`scripts/safe_remove.py`, audit check #24 — refuses removing a `scripts/` module while a live external **code** referrer survives) to the rest of the removal's heterogeneous direct-boundary union: **M2** (computed path / dotted-module string refs) + **M3** (declared-prose reverse-deps), each with its own compute-vs-declare posture — a direct cross-kind boundary, not a transitive walk; fail-closed on partial/oracle-unavailable. #195 delivered M1 only (operator-approved close 2026-06-26). · Done when: the gate refuses a removal with an M2 (path/dotted-module) referrer AND with an M3 (declared-prose) referrer on a fixture, extending #195's M1 catch to the full union · refs #195 (M1 delivered), ADR-89, #196 (closure-spike findings), docs/audits/2026-06-20-removal-closure-spike-findings.md · serialize-group: code-edge

### Wire up the lifecycle hooks the workflow relies on
So that session-start/close automation actually runs instead of being wired-but-vacuous.
- [#116] [P3][S] Hooks hygiene — `shell:"powershell"` + `args:[]` exec-form migration: replace wrapper-string invocations on our PowerShell hooks with the array exec-form to kill the Windows quoting failure class; note the `.cmd` shim caveat (per the hooks doc); also adopt `if:` conditional filters on PreToolUse guards to scope spawn overhead · Done when: our PS hooks use exec-form `args:[]` and at least one PreToolUse guard carries an `if:` scope filter · refs docs/audits/2026-06-07-platform-max-audit.md UN-5/UN-6 · serialize-group: settings-json
- [#117] [P3][S] Evaluate prompt/agent-based hooks (`type:"prompt"` Haiku-eval / `type:"agent"` multi-turn, experimental) — candidate: run the Tier-1 closure/triage eval in-session via a `type:"prompt"` hook instead of a Python shell-out; EXPERIMENTAL flag; GATED on the VF-1 auth check (add a trivial `type:"prompt"` PreToolUse returning approve, run any tool call, confirm NO auth error under the empty-key billing route) · Done when: VF-1 passes and a go/no-go on a prompt-hook Tier-1 eval is recorded · refs docs/audits/2026-06-07-platform-max-audit.md AD-2, VF-1, #8 · serialize-group: settings-json
- [#168] [P3][S] Harden the ADR-85 BACKLOG leg from advisory to a hard gate — replace the interim structural-marker check in `check_backlog_marker()` with the airtight issue-ID↔commit anchor once the traceability-spine ADR lands (R1 co-design); blocked on #170 (the spine ADR). · Done when: the BACKLOG leg blocks turn-end on a missing issue-ID↔commit link (not just a marker present) and the advisory-only interim is retired · refs docs/decisions/ADR-85-session-lifecycle-enforcement.md (R1), scripts/session_end_backpressure.py, #8 · depends-on: #170
- [#170] [P3][M] Design + land the traceability-spine ADR (issue-ID↔commit anchor) that #168 depends on — the airtight, always-warranted link that promotes ADR-85's BACKLOG leg from advisory to a hard gate. The spine is the un-gameable AND always-warranted anchor the structural-marker check lacks. · Done when: an ADR defines the issue-ID↔commit linkage and #168 has a ratified anchor to depends-on · refs docs/decisions/ADR-85-session-lifecycle-enforcement.md (R1), #168, #8
- [#189] [P3][S] **Execute in ~/.claude** (runtime-config repo; queue-only here, per the #100 execute-elsewhere precedent) — a session-end `~/.claude` commit-check (hook or routine) that flags uncommitted config/safety drift (chronic uncommitted `~/.claude` changes are the recurring failure). The methodology-reach question (should ecosystem enforcement own this?) is decided in #153. · Done when: a session-end check surfaces uncommitted `~/.claude` config/safety drift · refs #153, #100 (execute-elsewhere precedent), ~/.claude/rules
---

## Lessons feedback loop
> As the methodology author, I want lessons to flow back into enforced rules, not sit in an archive.

### Make lessons an active feedback loop, not a passive archive
So that captured lessons reach runtime rules and stay enforceable.
- [#4] [P2][M] Build lessons-index.json + SessionStart retrieval + CLI query · Done when: lessons are queryable + surfaced at session start · refs ADR-35, ADR-70 (Tier-1 lesson-promotion)
- [#130] [P3][S] Memory-hygiene review — periodic pass over the CC auto-memory + the `gotchas` skill: flag duplicate/overlapping entries, stale or RETIRED cleanup candidates, and MEMORY.md index cap-proximity; surface candidates as a digest into the morning funnel for ratification (operator-gated — never auto-delete, per the no-delete invariant). Plus a **capture-time scrub arm**: scrub a gotcha/memory at the moment of write (dedupe-against-existing before append), not only via the periodic pass — the cheaper intervention point. · Done when: one hygiene pass runs and emits a ratify-only candidates digest (duplicates / stale-RETIRED / cap-proximity) AND a capture-time scrub step exists in the gotcha/memory write path · refs MEMORY.md, ~/.claude/skills/gotchas, #4
- [#134] [P2][S] Backlog-grooming review — periodic operator-invoked pass over BACKLOG: deterministic signals + LLM obsolescence proposals → ratify-only digest → one removal commit + grooming-log (operator-gated, never auto-delete, per the no-delete invariant) · Done when: one grooming pass runs and emits a ratify-only candidates digest (age / dead-ref / closed-dependency) · refs BACKLOG.md, #130 (family pattern), ADR-65 · Design (full detail in git): 3 deterministic signals are FLAGS never auto-kills (retired-token grep · ref-resolver · orphan-story); anti-signals never kill (dependency-closed → "unblocked?" nudge; item-age never standalone); substantive removals (done-undetected, decided-by-arc) need git+cross-ADR judgment, invisible to propose_closures; flow = flags → judgment → operator ratify-gate → one removal commit + grooming-log; cadence = POST-ARC (obsolescence is event-driven) + quarterly backstop · Runs: n=1 2026-06-07 (78→67; 0 signal-kills, all substance via judgment); n=2 2026-06-25 (84→81; 0 deterministic kills; #198 done / #119 obsolete / #143 effectively-recorded)

### Codify recurring patterns into the methodology
So that observed failure-modes become written guidance instead of recurring.
- [#67] [P3][S] PLAYBOOK §7 polish: skill/command naming-convention note + orchestrator/worker how-to · Done when: both notes land in PLAYBOOK §7 · refs methodology-audit C10/C12 · serialize-group: playbook
- [#144] [P3][M] Feature DoD = end-to-end / user-flow test — sharpen ADR-81 (d) "deployed": a feature is not done until an automated test exercises the WHOLE sequence as a user would, not only unit tests. The general acceptance-contract requirement landed (ADR-81 amendment 2026-06-24 + PLAYBOOK Ch12.1, A2); REMAINING: the E2E/user-flow-specific clause + VERIFY-FIRST — does "in the cloud" mean CI / GitHub Action or cloud CC sessions? (sets where the E2E test runs) · Done when: ADR-81 (d) carries an explicit E2E/user-flow clause (or deferral) AND the "in the cloud" target is resolved · refs ADR-81 (2026-06-24 amendment), PLAYBOOK Ch12.1, #143 · origin: 2026-06-10 worktree-codification deferral
- [#145] [P3][M] Codification-completeness pass — audit what else a fresh session CANNOT do from PLAYBOOK + the handoff bundle alone (the fresh-session-can't-do-X test, per the sharpened ADR-81 (a)); pattern alarm: 3 transmission gaps surfaced this session (child-floor deploy, rollout ambiguity, worktree parallelization); A4 advance — the architect-contract essence-completeness gap (the resident loop had stages but no transition gates) is closed: gate-map + delivery-lifecycle + educate-on-value + premises self-check + closure→A2 pointer now resident in HANDOFF_BOOT; the broader fresh-session enumeration pass remains · Done when: a read-only pass enumerates fresh-session transmission gaps across PLAYBOOK/bundle and each is filed or closed · refs ADR-81 (a), #124, #131, LESSONS 2026-06-10 worktree transmission gap · origin: 2026-06-10 worktree-codification deferral

---

## Decision management
> As a reader of 65+ ADRs, I want decisions navigable and free of silent contradiction.

### Keep the decision corpus navigable and contradiction-aware
So that growth past 65 ADRs doesn't bury or quietly contradict prior decisions.
- [#2] [P2][L] Add contradiction-detection across decisions + an ownership model (amend vs new ADR vs clarification) · Done when: a detection mechanism + ownership rule exist · refs ADR-64 (consolidated-index sub-item done)
- [#23] [P3][M] Build a navigable supersedes/related/amends graph from ADR frontmatter (or an index doc) · Done when: the graph renders + is freshness-checked · refs posture-audit I1

### Codify the meta-decision rules
So that future sessions route decisions consistently (convene-vs-Path-A; relax-vs-gate).
- [#18] [P2][M] Codify the convene-vs-Path-A criterion in AI_COUNCIL_PROCESS/PLAYBOOK · Done when: the criterion is written · refs ADR-62/63/65 · serialize-group: playbook
- [#27] [P3][M] Codify the cost/value-asymmetric relax-vs-gate criterion for drifted guards · Done when: a LESSON/ADR records the criterion · refs ADR-62/63, coherence-audit ML-2

### Operationalize the Council decision loop
So that Council questions are gated and their verdicts return deterministically instead of being shuttled by hand.
- [#70] [P3][M] Operationalize the AI-Council process — implement the gated question→verdict→ADR loop per ADR-67 (templated question + self-gate + deterministic `council.return_dir` return; downstream pieces in ai-council + ~/.claude). Gate lifted 2026-06-02 — Phase-2 universalization is complete (all 5 repos unified), so this is now actionable. · Done when: the gated loop runs end-to-end (/council-question generates + self-gates → council runs → operator pastes verdict → ADR returns via council.return_dir) · refs ADR-67
- [#96] [P2][M] ai-council VERDICT marker (producer-side) — synthesizer emits a machine-readable VERDICT block at the top of every transcript (verdict per question, one-line rationale, panel/cost metadata); consumers (distillation prompts) read only the block · Done when: a council run produces the block and a distillation completes without reading the debate body · refs ADR-74 (marker doctrine), PLAYBOOK context-budget rule; note: implementation lands in the ai-council repo via its dedicated chat (ADR-41) — this is the ecosystem queue entry; refs round-2 isolation audit (#110)
- [#110] [P3][S] ai-council round-2 isolation audit — verify whether round-2 debaters see round-1 peer arguments; 2026 evidence: 58% sycophantic convergence in debate, 23.9% unanimous-wrong by round 3; confirm blind-vote isolation properties hold for our 2-round design · execute in the ai-council dedicated chat · Done when: isolation properties confirmed or a remediation is recorded · refs #96, ADR-03, research transcript 2026-06-06 · 2026-06-07 groom: ADR-03 is an ai-council-repo ADR (no such ADR in dev-knowledge/docs/decisions) — verify the ref in the ai-council chat
- [#128] [P3][S] ai-council prompt caching — apply `cache_control` to the repeated brief/persona blocks reused across providers and debate rounds, cutting input-token cost on multi-round debates · **execute in the ai-council dedicated chat** (ADR-41 — queue-only here) · Done when: a council run reuses cached brief/persona blocks and the input-token saving is recorded · refs Anthropic API prompt-caching docs, #96, #110

### Close the small ADR cross-reference + registry amendments
So that the ADR web is internally consistent.
- [#19] [P3][M] Complete the ADR-39 register — (a) add a BACKLOG.md lifecycle entry (formerly bundled with #20); (b) decide register/exempt/hybrid for the non-handoff templates/ class (merged from #21, 2026-06-07 groom) · Done when: ADR-39 registry includes BACKLOG.md AND the templates/ class decision is recorded · refs ADR-39/41
- [#165] [P3][S] Ratify the diagram-format selection rule (ASCII vs mermaid) as an **ADR-51-family amendment** — operator-set 2026-06-12; **propose for ratification, do NOT unilaterally mint a new ADR**. Rule: **ASCII sequence / numbered list** for small linear diagrams (≤~6 steps, single actor, no branch or loop to draw — especially terminal / plain-text-facing); **mermaid** for branching, ≥2 actors/lanes, a loop that must be drawn, >~6–8 nodes, OR any persistent rendered repo doc (tie-break to mermaid even if borderline-small). First application: the operator-first v5 handoff README (`templates/handoff/v5/README.md.tmpl`) chose mermaid under this rule (durable rendered doc + a loop + a non-linear split). · Done when: the selection rule is ratified (operator ruling or ADR-51-family amendment) and recorded where the diagram convention lives (ARCHITECTURE.md §Output formatting / ADR-51) · refs ADR-51, ADR-59, ARCHITECTURE.md §Output formatting, templates/handoff/v5/README.md.tmpl, #164 · serialize-group: architecture
- [#228] [P3][S] Fix ADR-90/91 ordering in the ADR index — `docs/decisions/README.md` lists the rows out of numeric order (…88, 89, 91, 90, 92); reorder 90 before 91. · Done when: the ADR index rows are in numeric order · refs docs/decisions/README.md

---

## Canonical-file integrity
> As any agent reading this repo, I want the canonical files accurate and current.

### Keep canonical files accurate
So that stale ground truth stops silently misleading sessions (the repo's own VISION is "drift detected proactively").
- [#35] [P3][S] Fix the self-owned low-severity cleanups (ARCHITECTURE diagram attribution + SBAR label; VISION adoption-signal + stale last_reviewed) · Done when: WF-3 + GO-1/2 resolved · refs coherence-audit · serialize-group: architecture
- [#71] [P3][S] Reconcile ENVIRONMENT.md's `~/.claude/` directory tree with live contents (commands = codex-review + session-summary only — boot/evolve archived 2026-06-05 Phase-C3, target re-scoped 2026-06-07 groom; skills = gotchas + verify; agents/hooks already match); **+ folded from #119:** ENVIRONMENT.md's "No Codex CLI / Rejected" lines (243/255) contradict the live `/codex-review` command + `codex-review` skill (Codex installed 0.141.0) — reconcile in the same pass · Done when: the ENVIRONMENT `~/.claude/` tree matches `ls ~/.claude/{commands,skills}` and the Codex/Rejected lines are reconciled to live state · refs G6 process-hardening sweep · serialize-group: environment
- [#157] [P3][S] Collapse the intra-CLAUDE.md file-lifecycle restatement — §4 Conventions "File lifecycle" bullet (line 54) AND §5 Critical rules #1–3 (lines 69–71) both authoritatively define the append-only/immutable disposition in full: the resident-copy-drift class inside a single file. Keep one location as the authority and make the other a pointer (do NOT edit the rule's content, per source→gate→agent). Coordinate with #112 (also edits §5 — the supersede-vs-never-edit contradiction) to avoid a collision; includes the CLAUDE.md end-to-end re-read + last_reviewed restamp. · Done when: CLAUDE.md states the file-lifecycle disposition once (authority) with the other location reduced to a pointer, §4/§5 no longer both define it, and last_reviewed is restamped after a genuine re-read · refs docs/audits/2026-06-11-surface-responsibility-audit.md (FLAG-1), #112, #140, ADR-29, ADR-39 · serialize-group: claude-md
- [#169] [P3][M] Ungated-doc staleness detection (ADR-85 R2) — surface ARCHITECTURE/VISION/LESSONS/CONTRIBUTING staleness (e.g. untouched >N sessions while code changed) as a deterministic signal in the daily digest / conformance dashboard, NOT a per-session gate and NOT human memory; rides with the conformance-dashboard work. · Done when: a deterministic staleness signal for the four ADR-85-ungated docs lands in the digest/dashboard · refs docs/decisions/ADR-85-session-lifecycle-enforcement.md (R2), scripts/fleet_health.py, #166 · depends-on: #171
- [#171] [P3][M] Build the conformance dashboard at `ecosystem/conformance.md` (ADR-86 / ADR-85 R2) — a read-only validator generates it and commits its own output (ADR-80 committed-generated-zone writer policy); this is the deterministic surface #169's ungated-doc staleness signal lands in, and ARCHITECTURE Ch2 points to it. · Done when: `ecosystem/conformance.md` is generated + committed by a read-only validator (Layer-2-safe) and ARCHITECTURE Ch2 carries the pointer · refs docs/decisions/ADR-86-conformance-dashboard-location.md, ADR-80, ARCHITECTURE.md Ch2, #169
- [#217] [P3][S] Add effort to the canonical "two lifelines" § Lifeline 1 division table — the architect↔CC table (PLAYBOOK "## The two lifelines", L229–236) lists "choose MODE (plan vs auto)" for the architect and "pick the model" for CC but is silent on **effort**, while §2 now reconciles effort to architect-owned (by token budget, #216) and ESSENTIALS states it explicitly (the Lifeline-1 prose at L238 already says "effort is set by token budget"; only the table omits it). Add effort (the architect's, by token budget) to the table's architect column for full three-way consistency (table ↔ §2 ↔ ESSENTIALS). Table/wording groom only — no rule change. · Done when: the Lifeline 1 table names effort as the architect's (by token budget) and table ↔ §2 ↔ ESSENTIALS agree on effort · refs protocols/PLAYBOOK.md "## The two lifelines" Lifeline 1 table, protocols/ESSENTIALS.md, #216, ADR-87 · serialize-group: playbook
- [#219] [P3][S] Cross-reference the two loop framings — the corpus describes the loop two ways with no cross-pointer: PLAYBOOK "## The two lifelines" (Workflow + Coherence — the execution/consistency view) and ARCHITECTURE Ch6 "the methodology engine" (Lessons → Decision → Conventions → Enforcement → Dissemination — the knowledge-evolution view). They are complementary, not competing, but a fresh reader has no signal of that. Add a one-line pointer in each naming them as complementary views, so neither reads as a competing "the loop". · Done when: each framing points to the other as a complementary view (execution/consistency vs knowledge-evolution) · refs protocols/PLAYBOOK.md "## The two lifelines", ARCHITECTURE.md Ch6 · serialize-group: playbook
- [#222] [P2][M] Decouple ARCHITECTURE's pytest-count claim from the freshness gate — ARCHITECTURE.md carries the gated `doc_claims` counts (`N registered checks`, `N collected`); a test-count change forces an ARCHITECTURE edit → its commit date advances → `canonical_freshness` (#10, commit-based) then demands a same-day `last_reviewed` re-stamp with no genuine re-read, corrupting the freshness signal (the 2026-07-01 stamp was forced this way across the deploy arc — 947→965→977→979→980). Fix: move the volatile count(s) out of the freshness-stamped file (e.g. a generated fragment), OR exempt count-only bumps from `canonical_freshness`. Migrated from the `architecture-claims-couple-to-freshness` MEMORY note (its only prior home). · Done when: a pure test-count bump lands with no forced `last_reviewed` re-stamp, with a test · refs scripts/audit.py (canonical_freshness, doc_claims), ARCHITECTURE.md · serialize-group: audit-py
- [#223] [P2][M] Document the deploy subsystem in ARCHITECTURE.md — ARCHITECTURE omits the ADR-91/92 `deploy/` subsystem entirely (the only 'deploy' hit is the unrelated nightly Routine). Add it via a genuine end-to-end re-read (same visit as #222): Ch2 organ map (the deploy tool + 4 carriers + their trigger/layer/posture), Ch4 distribution (a carrier/orchestrator row), Ch6 verification mesh (the `deployed_methodology_version` record + per-carrier verify-gate), and the Validators list; honest `last_reviewed` re-stamp after the real read. Note: the 2026-07-01 arming build (#226/ADR-93) grew the deploy surface (carrier_floor arming, the precommit floor-hash-verify hook, `deploy/floor_conformance.py`) — more for this visit to cover, and it reinforces #222's decouple-count-from-freshness (a claim-count bump forces a canonical_freshness re-stamp with no genuine re-read). · Done when: ARCHITECTURE's organ/carrier/mesh maps + Validators cover the deploy tool, the 4 carriers, the deployed-version check, and the ADR-93 floor arming/conformance, with an honest re-stamp · refs ARCHITECTURE.md, ADR-91, ADR-92, ADR-93, deploy/, #222 · serialize-group: architecture
- [#224] [P3][S] Rotate the Governing-ADR lists to include ADR-90/91/92 — ARCHITECTURE.md "Governing ADRs" and CLAUDE.md §11 "last 5" both stop at ADR-89, though 90/91/92 are Accepted and in the ADR index. Add ADR-90 (resolver-allows-N), ADR-91 (corpus versioning), ADR-92 (deploy runbook) to both surfaces; carry the CLAUDE.md §11 re-stamp. · Done when: ARCHITECTURE Governing-ADRs and CLAUDE §11 both reflect ADR-90/91/92 · refs ARCHITECTURE.md, CLAUDE.md, docs/decisions/README.md · serialize-group: architecture

### Keep the day-to-day docs right-sized and current
So that the cheat-sheet and architecture stay scannable as conventions accrue.
- [#5] [P2][M] Add high-leverage ESSENTIALS cheats for ADRs 35-76 under the one-page constraint · Done when: the chosen cheats land + ESSENTIALS stays ~1 page · refs posture-audit C2
- [#41] [P3][S] Split ARCHITECTURE §Processes into PROCESS.md if it grows past the comfort threshold · Done when: re-evaluated at the next process addition · refs ADR-51
- [#212] [P3][M] `docs/handoffs/**` retention/rollup policy — 416 immutable handoff bundles are the corpus's single largest growth vector: immutable-by-design (ADR-82) but unbounded. Open question (likely ADR-class): should bundles older than N be compressed / rolled-up the way ADR-49/65 condenses doc section-history, and if so by what mechanism + retention window? The one sanctioned uncontrolled-growth vector should get a deliberate call, not silent drift. Size is a placeholder (M) pending scoping. · Done when: a retention/rollup policy for `docs/handoffs/**` is decided (ADR or operator ruling) — either a compaction/rollup mechanism + window, or an explicit "keep all; unbounded accepted" with a recorded reason · refs docs/audits/2026-06-26-corpus-graph-justify-or-retire.md (latent archive-class question, lines 63-67), ADR-82, ADR-49, ADR-65, #134
- [#213] [P2][L] PLAYBOOK rule/history condensation — separate rule from rationale/history across protocols/PLAYBOOK.md (3410 lines): rules stay crisp in place, incidents relocate to LESSONS (verify-captured-or-append + pointer), re-argued rationale collapses to existing-ADR pointers; organized around the two-lifelines spine (a chapter→lifeline map). Preserve all Ch/§ numbers + ##/### titles (no renumber — inbound pointers survive); condense bodies only. Size is a byproduct (honest landing ~2600–2950), never a rule cut to hit it. · Done when: a rule-inventory diff shows zero rules lost AND every inbound pointer still resolves · refs protocols/PLAYBOOK.md, LESSONS.md, ADR-49, the two-lifelines spine · serialize-group: playbook
- [#214] [P3][S] PLAYBOOK surface cleanup — (a) rename /changelog-review (it reviews upstream TOOL changelogs, not the retired repo CHANGELOG.md — the name collides) to e.g. /tool-updates-review, OR retire it if dormant; (b) move the Ch8 worktree raw-git-worktree fallback recipe out of the main body into an appendix, keeping claude --worktree foregrounded. · Done when: the /changelog-review naming is resolved (renamed or retired) AND the raw-worktree fallback lives in an appendix · refs protocols/PLAYBOOK.md Ch8, .claude/commands/changelog-review.md, #213 · serialize-group: playbook
- [#227] [P3][S] Relocate AGENT_FRAMEWORK.md out of protocols/ — `protocols/AGENT_FRAMEWORK.md` is a self-labelled v0.1 unimplemented stub with no readers (no living doc references it) and no gate; it is not an authoritative loaded protocol, so it does not belong in the authoritative `protocols/` dir. Move it to `docs/` (idea / design-note home) and fix the one inbound ref (#1's `refs protocols/AGENT_FRAMEWORK.md`). · Done when: AGENT_FRAMEWORK.md lives under docs/ and every inbound ref resolves to the new path · refs protocols/AGENT_FRAMEWORK.md, #1, ADR-60

---

## Cross-repo universalization
> As the disseminator, I want every child repo to converge on the universal baseline.

### Converge every child repo on the universal baseline
So that "open any repo, same layout/governance" actually holds.
- [#9] [P2][M] Inventory skills across repos, classify repo-specific vs universal, propose a canonical home · Done when: a skills inventory + universalization proposal exists · refs VISION cross-repo consistency · changelog 2.1.148-167 finding — distribution simplification (v2.1.157): plugins in `.claude/skills` auto-load without a marketplace; `claude plugin init <name>` scaffolds; factor into the canonical-home proposal and fleet rollout of verify/guard organs
- [#82] [P3][M] Define per-repository agentic-review profiles — what agentic review each repo runs (dev-knowledge = methodology conformance; child repos = their domain needs, e.g. corp-monorepo deep-audit, ai-council's flow) and on what cadence, so agentic-ness is deliberate per repo. · Done when: each repo's agentic-review profile is recorded · refs ADR-70, #9, #70. Design inputs (live): **heterogeneous second reader** (different model architecture) is the field-validated shape — same-model self-review fails to sycophantic convergence; **evaluate natives first** (`/code-review --fix`, `/simplify`, `/code-review ultra` — the last a COMPLEMENT to Codex cross-vendor heterogeneity, not a substitute) before any plugin/custom simplifier; per-profile `model_reasoning_effort` + output-hygiene keys (`hide_agent_reasoning`, `web_search`)
- [#100] [P3][S] corp-monorepo CLAUDE.md freshness restamp — genuine end-to-end re-read + `last_reviewed` bump (edited 2026-06-06, stamp 2026-06-02; witnessed by fleet audit 2026-06-06) · **execute in the corp dedicated chat** (ADR-41 routing — this entry is queue-only) · Done when: corp audit `canonical_freshness` passes · refs ADR-41
- [#131] [P2][M] Repo-onboarding runbook — `docs/runbooks/repo-onboarding.md` documenting the 6-layer install sequence to bring a repo onto the universal baseline (floor → carriers → lifecycle command → review profile → ecosystem registration → re-anchor mechanic). Owns (a) fleet-wide child `CLAUDE.md` `@.claude/CLAUDE-FLOOR.md` references and (b) the 3-week re-explain rollout metric. Live runbook steps the pilots surfaced (must appear): **`pre-commit install` as a MANDATORY per-clone step** (floor hash-verify hook inert until armed; `.git/hooks/` doesn't travel); per-child `.gitignore` **negations** for the floor files (generator side closed as #138); **delete any orphaned ROOT floor copy** on migration to `.claude/` (`floor_integrity` reads `.claude/` only; a stray root floor = invisible vacuous PASS). Subsumes #115 residual: distribute `/ship` (tier1-lifecycle plugin) to child repos via the carriers + lifecycle-command layers. **#131↔#215 reconcile = SPLIT (see #215); deploy arc advanced this; rollout = #221.** · Done when: the runbook exists, lists all 6 layers in order, and has been piloted on one repo (n=1) with the n=2 gate recorded · refs ADR-71, ADR-72, ADR-78, #82
- [#215] [P2][M] Onboard + verify methodology in a new repo — a consolidated runbook to deploy the methodology into a fresh repo AND verify it conformed (the verify-it-conformed half ties #171 conformance.md, unbuilt). Distinct from #131 (the 6-layer install runbook) by its conformance-verification emphasis; reconcile scope with #131 when built. **Generalization probe:** the first onboarding pilot (ai-council) doubles as the first real test of whether the two lifelines (Workflow + Coherence) transfer to a structurally-different repo-shape (CLI / provider-heavy / fewer governance-docs) — surface any hub-specific convention that does NOT transfer before building distribution on the assumption that the methodology is universal. **#131↔#215 reconcile (2026-07-01) = SPLIT: #131 = the 6-layer runbook doc, #215 = the conformance-verify half (#171); a merge that drops an id stays an operator call. Deploy arc advanced both (ai-council n=1 v1.0.0); rollout = #221.** · Done when: one onboard runbook + a conformance verification exist (or the item is explicitly merged into #131 with a recorded reason) · refs #131, #171, #230 (the functional self-test), ADR-78
- [#221] [P2][M] Deploy the methodology corpus across the fleet + close the deploy-arc residuals — the ADR-91/92 deploy subsystem (orchestrator `deploy/tool.py` + the 4 carriers + the `deployed_methodology_version` record) was built 2026-06-29→07-01 and proven on run #1 (ai-council → v1.0.0), but shipped with no backlog item and no closure tag — recorded here retroactively. Remaining: run the tool on the other consumers (corp-monorepo, corp-ops, corp-sca-time-automation) to reach the n=2+ generalization gate and populate the registry fleet-wide; hardening/docs residuals tracked as #223/#225/#226. Sequencing: #221 MUST follow #226 (the arming carrier fix) so fleet repos don't replicate ai-council's configured-not-armed gap; each repo's deploy uses the #230 conformance self-test as its acceptance gate. · Done when: the deploy tool has run on ≥1 further repo (n=2 recorded) AND the deploy-arc residuals #223/#225/#226 are closed AND each such deploy passed the #230 self-test · refs ADR-91, ADR-92, PLAYBOOK §20, deploy/, #131, #215, #171, #230 (dep #226 satisfied — arming closed 2026-07-02)
- [#225] [P2][M] precommit carrier — comment-preserving surgical edit — `deploy/carrier_precommit.py` round-trips the consumer's whole `.pre-commit-config.yaml` (YAML load/dump), which strips comments and reindents the entire file, producing noisy deploy diffs. Replace the round-trip with a surgical, comment-preserving insert/update of only the methodology-owned hooks, matching the version-record writer's byte-faithful principle (deploy-arc residual). · Done when: deploying the precommit carrier into a commented consumer config changes only the methodology-owned hook block and leaves surrounding comments/indentation byte-identical, with a test · refs deploy/carrier_precommit.py, ADR-92, #221
- [#231] [P3][M] Consumer → hub feedback report — when a consumer session detects a methodology gap, ambiguity, or broken piece (including a failing #230 conformance self-test), it emits a STRUCTURED report destined for the hub so the hub can answer/fix, instead of the consumer guessing. Encodes the operating philosophy: when unsure, ask the hub and double-check; verification results flow back upstream. Sub-note (flag, do NOT do here): the philosophy line itself may belong in ESSENTIALS/PLAYBOOK — scope that when built, do not codify it in this item. · Done when: a consumer that hits a methodology gap/ambiguity/broken-piece emits a structured hub-destined report (schema + destination defined) rather than guessing · refs #230, #215, #221, ADR-41, protocols/ESSENTIALS.md, protocols/PLAYBOOK.md
- [#233] [P3][S] Isolate the deploy-record-index test's tempdir per-run — `tests/test_deploy_tool_execute.py::test_no_temp_index_leftovers` snapshots `listdir(tempfile.gettempdir())` before/after `execute`, so ANY concurrent process writing a `deploy-record-index-*` file (e.g. a second pytest run) trips its set-diff → false failure (witnessed 2026-07-01 running two suites concurrently; passes serially + isolated; `deploy/tool.py`'s writer + this test are the only touchers of that path). Fix: give the record writer + the test a per-run isolated tempdir (the `PRE_COMMIT_HOME` pattern) or pid-scope the assertion, so it is concurrency-robust. Pre-existing test-hygiene debt, out of #226 scope; adjacent to but distinct from #232 (ship-gate right-sizing). · Done when: the test asserts only its own run's temp artifacts and stays green under concurrent suite runs · refs tests/test_deploy_tool_execute.py, deploy/tool.py, #232

### Make new-repo scaffolding correct-by-default
So that a new repo inherits the full baseline in one step, not by re-derivation.
- [#16] [P2][M] Collapse workspace-{S,M,L}.code-workspace into one maximal scale-adaptive template; element-selection guidance encodes the small=single-root / large=multi-root rule (merged from #38, 2026-06-07 groom) · Done when: one workspace template + element-selection guidance (incl. the single/multi-root rule) exist · refs operator Q5, ADR-59
- [#17] [P2][M] Encode ADR-54–63 high-leverage guidance into `templates/CLAUDE-md-template.md` (structural lock + frontmatter already at v2.2; template uses generic `ADR-NN` placeholders by design). Extended scope (live): compaction-preservation instructions (what auto-compact must preserve — modified-file list, test commands, active branch, open task ids); instruction-priority tiers (CRITICAL/STANDARD/PREFERENCES); a pointer to the subdir-CLAUDE.md item (#109); native `/context` diagnostic as a companion. · Done when: the template's guidance reflects ADR-54–63 + the extended scope · refs PLAYBOOK context-budget rule, #109 · serialize-group: claude-md-template
- [#43] [P3][L] Decide + (if yes) author a one-step new-repo scaffold (ADR + templates/new-repo-skeleton/, no scripts) · Done when: decision recorded + scaffold authored if approved · refs durability-audit J6
- [#109] [P3][S] Subdirectory CLAUDE.md convention — per-module CLAUDE.md layer for large repos; add to the template standard; pilot in corp-monorepo via its dedicated chat · Done when: the convention lands in `templates/CLAUDE-md-template.md` and corp pilot is queued · refs #17, ADR-55–63 cluster · serialize-group: claude-md-template
- [#129] [P3][S] Skill-authoring scaffold — `templates/SKILL-scaffold.md` (the lone untracked peer-audit residual, C11) modeled on TSH `tsh-creating-skills`; **MIT attribution required** if their markdown is adapted verbatim (© The Software House). Encodes the 3-tier token-budget discipline their skills carry but ours lack — discovery (~100-token name+description) / activation (<5k body) / `resources/` (loaded on demand) + a 500-line body cap — as a frontmatter/structure row; mirrors how `templates/prompt-template.md` anchors prompt authoring · Done when: `templates/SKILL-scaffold.md` exists carrying the 3-tier budget + 500-line cap guidance and the MIT-attribution note · refs docs/audits/2026-06-07-copilot-collections-peer-audit-v2.md C11/§2/§3.6 S1, #9

---

## Tooling & evaluation
> As the operator, I want low-friction tooling and timely tech adoption.

### Cut session friction with better tooling
So that cognitive overhead per session drops.
- [#37] [P3][S] Amend ADR-59 with a workspace-sort-change verification rule — the visual-pattern (workspace sort settings) is now pointered from PLAYBOOK to ADR-59 (#158), so the rule lands as an ADR-59 amendment, not in PLAYBOOK/ESSENTIALS. Rule: on a workspace-sort-setting change, read the merged PR + do a visual editor check before relying on it (the `sortOrderReverse` saga is the cautionary precedent). · Done when: the verification rule lands as an ADR-59 amendment · refs ADR-59 amendment, #158
- [#39] [P3][S] Codify entry-scripts → scripts/ in PLAYBOOK root-hygiene — blocked on Move 2: the root-hygiene convention is retained PLAYBOOK-local (not pointered, per #158), so full dissolution needs the Move-2 structural split to give the rule a stable home. · Done when: the convention is written (after Move 2 lands the root-hygiene home) · refs 2026-05-27 retrofit, #158, Move-2 structural split · serialize-group: playbook
- [#102] [P2][M] Machine-readable repo index for agent consumption — field reports ~50k-token exploratory reads replaced by ~250-token precompiled indexes; evaluate for hub + corp · verify-first: confirm claimed tools (`stacklit`, `scip-search`) actually exist before any design · Done when: a pilot index on one repo demonstrably replaces exploratory reads in a CC session · refs #89, #91, ADR-51, research transcript 2026-06-06 · context-engineering field study (InsForge) — pilot shape: a single-call repo-topology spec (~500 tokens — structure, conventions, gate names, entry points, plus a `hints` field) consumed at UNDERSTAND time; makes the ~50k→~250-token replacement target concrete
- [#118] [P3][S] Run the native `fewer-permission-prompts` skill — one-shot allowlist tightening (scans transcripts → proposes a prioritized allow-list) under the deny-rule posture; low effort · Done when: the skill has run and its proposed allow-list is reviewed + applied (or explicitly declined) · refs docs/audits/2026-06-07-platform-max-audit.md AD-3
- [#122] [P3][S] Retire the PATH shim (`claude.cmd`) — the native settings.json `env` route (empty `ANTHROPIC_API_KEY`) is primary since 2026-06-06 and covers all launch paths; the shim is now redundant. Sentinel stays as a tripwire; removal needs an explicit operator ask per the no-delete invariant · Done when: the operator approves and the shim is removed (or the item is closed as keep-for-defence-in-depth) · refs docs/audits/2026-06-07-platform-max-audit.md UN-1
- [#127] [P3][S] verify skill failure-output contract — on failure the `verify` skill emits an **actionable** block (file, expected, received, directive) per the backpressure spec, giving an iterate-until-green loop a deterministic feedback signal; success output stays the 3-line compact form · Done when: a seeded failure produces the file/expected/received/directive block and success stays 3-line · refs #104 (verify-as-skill lineage), #126 · enrichment — failure outputs also carry a semantic exit code + expected-vs-got context (not just a directive) so an iterate-until-green loop gets a machine-distinguishable signal (anti-retry-loop)
- [#133] [P3][S] Peer-watch copilot-collections — add `github.com/TheSoftwareHouse/copilot-collections` to `ecosystem/tool-versions.yaml` as a watched source for `/changelog-review` (their Keep-a-Changelog is dated, so diffable); review cadence = on sentinel nudge, not nightly · Done when: the source is in the watch list and a `/changelog-review` run classifies its latest changelog delta · refs #113 (/changelog-review), docs/audits/2026-06-07-copilot-collections-peer-audit-v2.md
- [#229] [P3][S] Clear the temp/ scratch (operator-gated) — `temp/` holds ~700 MB of personal/client scratch (passport scans, a client `.pptx`, round2/round3 handoff leftovers). It is gitignored (not a git leak) but violates Three-Homes (client/personal intel does not live in the methodology repo working tree). Operator relocates/clears it (no-delete invariant → operator-gated, not an agent cleanup). · Done when: temp/ no longer holds personal/client material (operator-confirmed) · refs .gitignore, protocols/ESSENTIALS.md "Three Homes for Knowledge"

### Decide the undecided artifact/tool models
So that cadence-less artifacts and unevaluated tools don't rot or get adopted blind.
- [#106] [P3][S] Auto Mode evaluation — classified permission middle-ground between bypass and babysitting (3 tiers, injection probe, audit trail). **Current state (verify, don't re-research):** Auto Mode no longer requires opt-in consent since v2.1.152; availability check = `/config` → permission-mode picker, confirm "auto" appears WITHOUT setting `CLAUDE_CODE_ENABLE_AUTO_MODE` (that env var is Bedrock/Vertex/Foundry-only). The go/no-go must also **disambiguate the permission modes (auto/plan/bypass) from the handoff modes (architect/execution)** — the "auto mode" naming collision (cross-ref #162's architect actor-vs-mode vocab). · Done when: a go/no-go posture decision is recorded AND the permission-vs-handoff mode naming is disambiguated · refs unattended-run posture, ADR-68 autonomy boundary, #162
- [#126] [P2][M] Backpressure-loop pattern evaluation — a sanctioned in-session *iterate-until-green* primitive for unknown-iteration tasks (lint/type-debt burndown; red→green after a rebase or dependency bump). **VERIFY FIRST:** current `/loop` command semantics in CC docs/help — the ADR-74 REJECTED row covers `/loop` as a *persistence host* ONLY, not in-session repair (ADR-74 Amendment 2026-06-07). Doctrine bounds: operator-invoked only, never on automated paths, **deterministic checks as the feedback source** (pytest/ruff/type-check, not model judgment), a **mandatory iteration budget**, and "do-not-change-tests"-class constraints (fix code to satisfy fixed acceptance criteria, never edit the criteria). Pilot target: corp-monorepo (**execute in the corp dedicated chat**, ADR-41 — queue-only here) · Done when: a go/no-go is recorded with the doctrine bounds + a corp pilot result (or an explicit operator drop) · refs ADR-74 (REJECTED row, now persistence-scoped), ADR-76, #8 (Stop `additionalContext` = native backpressure surface), #127, generativeprogrammer backpressure article 2026-06

- [#123] [P2][S] Routine observability convention + value review — (1) all automation commits carry a standard marker (scope `chore(routine/<name>)` or trailer `Routine: <name>`) so routine output is git-indexable; (2) periodic value review in the morning funnel: findings-acted-on vs noise per routine (first datapoint: 2026-06-07 nightly digest 0/0/0 vs local baseline 1 FAIL) · Done when: the marker convention is recorded + adopted by the fleet/routine jobs AND one morning-funnel value review records per-routine findings-acted-on vs noise · refs #85, #14, #113, audit C matrix R7 · origin: operator proposal 2026-06-07
- [#155] [P3][S] "Loops architecture" — explore (scope TBD; operator-flagged 2026-06-11, not yet specced): the architecture of in-session / cross-session loops (iterate-until-green backpressure #126, `/loop` persistence ADR-74, the night/Routine cadence, the verification-mesh outcome loop). VERIFY-FIRST with the operator what "loops architecture" scopes before any design. · Done when: the operator confirms scope and a go/no-go on a loops-architecture design pass is recorded · refs operator flag 2026-06-11, #126, ADR-74, ARCHITECTURE.md Ch6 (verification mesh)

---

**About this file** — open `.dev-knowledge` work as a story map (ADR-66): Big Picture → Theme → User Story → Task. Stories are human (goal + `So that`); tasks carry `[#id] [P][size] · Done when · refs`. Done tasks **leave** (ADR-65); git is the implementation record (`git log --grep 'closes \[#'`). Child-repo execution items live in `docs/audits/2026-06-01-child-repo-relocation-proposal.md`. Schema: PLAYBOOK §10; machine-checked by `scripts/validate_backlog.py`.

**ai-council audit residuals (pointer, not filed here)** — the ai-council audit surfaced ai-council-owned cleanups (stale force-added `settings.local.json` pointing at dead paths; an empty `worktrees/fix/` dir; codemap/backlog-hook universalization gaps). Per ADR-41 routing these are filed in a dedicated ai-council session, not duplicated in the hub backlog.

**Grooming log:** git history is the record (`git log --grep 'groom'` + per-session JOURNAL). Recent: 2026-06-12 (#149 flip) · 2026-06-18 (currency pass) · 2026-06-25 (groom pass, n=2: 84→81). Next quarterly: 2026-07-01.
