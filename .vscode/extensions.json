{
  "//": "SHARED FLEET CONFIGURATION -- hub-owned, carried to consumers (deploy manifest component `vscode-boundary-decoration`).",
  "//boundary": "vscode-highlight renders the owner=hub / owner=repo background bands configured in settings.json. VS Code core has no mechanism to paint arbitrary line ranges from settings alone, so the decoration needs this extension; without it the file degrades to plain markdown (the generated headers stay readable).",
  "recommendations": [
    "fabiospampinato.vscode-highlight"
  ]
}
