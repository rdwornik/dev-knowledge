carried-by: OPEN
from: an era predating the `lands-via:` convention

## Fixture

An `OPEN` carrier with no anchored `lands-via:` key at all -- some of the live transport's
2026-09-08/09-17/09-18 files predate the convention. `carrier_landed_check.py` must count
it as measured, describe it honestly ("no anchored lands-via key"), and never flag it.
