# Sample protocol doc (doc-code-edge spike fixture, #194)

This minimal doc carries one rule-ID edge marker so the spike can prove the doc side
resolves. The marker below is the doc-side identity; its code counterpart lives in
`sample_module.py`.

A sample rule that some code enforces. <!-- rule: TEST-01 -->
