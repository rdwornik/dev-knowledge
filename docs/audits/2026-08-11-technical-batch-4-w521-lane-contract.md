# W-521 — lane-f-521-syspath-substrate: execute [#521] (the §4A substrate rollout)

## Dispatch
```
claude --worktree lane-f-521-syspath-substrate --bg --model opus --effort high --permission-mode bypassPermissions "[dk · #521 · syspath-substrate] Read and execute the frozen contract at $env:CLAUDE_PROMPTS_DIR\W521-LANE-SYSPATH-SUBSTRATE.md — its step 0 commits the contract of record; run the /lane-boot sequence from step 3 (seed) onward, commit-and-STOP."
```
Run the line above verbatim (full line, no alias). Authority: batch-4 GO `3b711e87` (birth `[#521]`, §4A ruling) + manifest `70a9ce2a` + the pairwise-parallel re-ruling. `[#521]` sits alone in the disjointness cut's L2 slot — still: verify zero shared files with LIVE lanes (W1, W2, W5) in your preamble; overlap = STOP-and-report.

| Model | Mode | Effort |
|---|---|---|
| opus | execute — this contract IS the plan | high |

## Purpose · row · bucket
`[#521]` — the sys.path substrate rollout born at the GO from the §4A ruling (the Shape-B owner decision). **THE ROW IS THE SPEC:** scope and Done-when come verbatim from `tasks/521-*.md` (born with a mechanically testable Done-when, quoted in the GO packet — hold it to that). This contract frames execution only. Bucket: hub-introspection is FULL (W1 holds the cap slot) — if the row's own frontmatter buckets it as hub-process, STOP after step 0 and report the cap conflict instead of proceeding; if it reads as substrate/enabling (finish-line-serving), state that reading with the frontmatter quote and proceed.

## Read first
`tasks/521-*.md` frontmatter + Done-when verbatim · the GO recording packet's birth section (its quoted Done-when is the reference copy) · `STANDING_RULINGS` §4A/I-D lines · PLAYBOOK Ch8 · the batch-4 manifest (this lane is OUTSIDE the W1–W6 roster: record yourself via an AMENDMENT MARKER adding a lane row — batch-3 precedent, never an in-place edit).

## UNDERSTAND (state back before work, quoting sources)
Packet preamble: the row's Done-when quoted · planned footprint · the zero-shared-files verification vs W1/W2/W5 with how you checked · the bucket reading per above.

## Lane requirements (PLAYBOOK Ch8)
Frozen contract from the tree · V-2 budget stated back in one line · `uv run --locked` on every test invocation · `worktree_import_probe` once · commit per step · JOURNAL entry ON THIS BRANCH ahead of the merge (letters race with three live lanes — claim the next free one, note the claim) · commit-and-STOP, `git stash list` empty.

## Steps
0. **Contract of record (I-D3):** commit THIS file byte-identical from the prompts dir to the home beside the batch-4 manifest as the FIRST commit on this branch; add the manifest lane row via AMENDMENT MARKER. **COMMIT**
1. **Scope statement** per UNDERSTAND (packet preamble; includes the bucket reading).
2..n. **Execute `[#521]` per its own Done-when** — one commit per coherent step; the Done-when is mechanical by birthright, so tests come FIRST and the final state must make them pass; an unmeetable clause = STOP on it with evidence, continue the rest.
n+1. **Close the row** with the closing-commit metric (SHA named in the row). **COMMIT**
n+2. **JOURNAL on the branch**, then final: `uv run --locked pytest -q` — expect only the known pre-existing REDs (`[#457]`-documented · sed-absent ×2 · linked-worktree context); anything new is yours until revert-and-rerun proves otherwise. Packet per Ch8; **sys.path/substrate work is code-impact by definition ⇒ terra review request is mandatory.** Commit-and-STOP.

## Decision budget (V-2)
Escalate only: curated-baseline touches · rule-vs-ruling conflicts (incl. the bucket/cap conflict above) · unmeetable/ambiguous Done-when clauses · any file overlapping a LIVE lane's footprint. Else decide per contract, report in the packet.

## What NOT to do
No births · no self-merge · no scope beyond the row · no `SKIP=`/`--no-verify` · no touching `CLAUDE.md` or `ARCHITECTURE.md` (claimed by live/queued lanes — STOP on contact) · no import-shim proliferation beyond what the row's Done-when licenses.

Library-first: the row governs; if any new machinery is written, record the stdlib→dependency→pattern search in the packet before writing it.
