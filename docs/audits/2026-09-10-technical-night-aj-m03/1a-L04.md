# 1a - AJ M03 L04 (Horizontal Integration Layer)

**Reader:** agy - **ORDERED**, invocation CORRECTED once. Not a substitution.
**Model:** gemini-3.8-flash-low.
**First attempt FAILED SILENTLY:** the file-write variant printed DONE and exited 0 **having written no file**. Nothing downstream could detect this from the exit code or stdout; it was caught only by listing the output directory. The most dangerous reader failure seen tonight - see NIGHT-LOG's reader-reliability table.
**Exact invocation (the one that answered):** agy --model gemini-3.8-flash-low --print-timeout=15m --dangerously-skip-permissions --add-dir . --print=<per-lesson table prompt>, run from the split-transcript directory, plus 'Print ONLY the table to stdout. Do not write any file.'
**Wall time:** ~6 min.
**Delivery:** stdout, single render.
**Locator caveat:** line numbers in L04.md.
**R2 status:** locators in this table are UNVERIFIED unless a row is marked otherwise, or unless this file is named in `R2-VERIFICATION.md`. Per R1/R2 nothing here is dropped; unverified content is carried and labelled.

---

| CLAIM | LOCATOR | PRACTICE-TAUGHT | MECHANISM-IMPLIED | TOPIC |
| :--- | :--- | :--- | :--- | :--- |
| Horizontal Integration Layers (HIL) serve as intermediary "crumple zones" preventing direct point-to-point interactions between enterprise layers of differing change velocity and component stability. | L04.md:5 | Decouple high-velocity digital/AI channels from low-velocity core systems via intermediate horizontal layers rather than direct point-to-point connections. | enterprise layer mismatch -> Horizontal Integration Layer -> decoupled system boundary | architecture |
| The 2025 MACH Reference Architecture Agentic update integrates AI orchestration, agent context, and governance as core architecture, choreographing data traffic, models, and controls. | L04.md:7 | Orchestrate AI models, agent context, and governance controls centrally within enterprise architecture. | agentic orchestration request -> MACH choreographing layer -> fast, secure, contextual experience | architecture |
| Rapidly changing AI interfaces/agents and digital channels (daily changes) sit on top, while slow-changing core systems like ERP, CRM, BI (yearly changes) sit on the bottom. | L04.md:9 | Categorize enterprise components by rate of change (daily vs. yearly) and avoid direct integration between extremes. | interface invocation -> layered enterprise hierarchy -> rate-of-change tiering | architecture |
| In rapidly changing microservice systems, maintain backward compatibility without explicit versioning (using 'latest'), while monolothic/slow-changing systems require rigorous API versioning. | L04.md:13 | Use unversioned 'latest' backward-compatible APIs for fast-moving microservices, and rigorously versioned APIs for slow-release monoliths. | release cycle trigger -> API contract policy -> versioned/unversioned API contract | architecture |
| High-velocity microservices should expose unversioned backward-compatible APIs internally, while exposing explicitly versioned APIs to monoliths to enable release-train switching. | L04.md:15 | Provide versioned facade endpoints for monolithic systems to insulate them while running microservices on continuous release. | monolithic release train -> versioned facade layer -> release-switched microservice API | architecture |
| Major cloud vendors independently isolate layers: Microsoft defined Gen AI Gateway and AWS defined Multi Provider Gateway to separate data feeding models from model control layers. | L04.md:17 | Separate the control layer that interacts with LLM models from the enterprise data layer feeding those models. | AI model interaction -> Gen AI Gateway / Multi Provider Gateway -> separated control and data streams | architecture |
| Point-to-point integrations create a mesh/star graph where every frontend/agent connects to every backend, multiplying coordination overhead and losing centralized security control. | L04.md:19 | Discontinue point-to-point integrations in favor of a centralized mediation layer. | point-to-point request -> distributed mesh -> uncoordinated change and security exposure | architecture |
| Routing all systems through HIL eliminates combinatorial connection complexity, accommodates divergent change velocities, and allows adding new clients scalably and independently. | L04.md:21 | Connect all frontends, agent apps, and backends exclusively to HIL rather than directly to each other. | client onboarding / request -> HIL mediation layer -> decoupled routing topology | architecture |
| HIL acts as a central security boundary, preventing sensitive data leakage and governing what information is exposed externally. | L04.md:23 | Enforce outward data filtering and sensitive data protection at the integration layer. | outbound response -> HIL security inspection -> sanitized external response | security |
| HIL enables composing user-facing digital frontend experiences dynamically without being slowed down by the slow change rate of the enterprise core. | L04.md:23 | Compose frontend user experiences in the HIL layer independently of core backend release cadences. | frontend experience composition -> HIL aggregation layer -> isolated digital experience | architecture |
| HIL capability 1 (API Orchestration) encompasses flow sequencing, routing, data mapping, dynamic CDN/caching, and load balancing. | L04.md:25 | Implement API flow sequencing, routing, data transformation, caching, and load balancing inside the HIL routing engine. | client API call -> routing engine / cache / load balancer -> sequenced/mapped backend request | task-intake-flow |
| HIL capability 2 (Content and Data Delivery) provides data federation, contextual agent memory, governance, and data processing constraint enforcement via RAG pipelines and data enrichment. | L04.md:27 | Centralize enterprise data federation, RAG enrichment, and agent contextual memory governance within HIL. | context / data retrieval -> RAG pipeline / enrichment engine -> governed contextual payload | task-intake-flow |
| HIL capability 3 (AI/Experience Orchestration) manages model orchestration, rule-based decision-making, context handoff between agents and UI, model routing, and business logic governance via MCP or A2A. | L04.md:29 | Manage agent-to-agent (A2A) / MCP interactions, model routing, and agent-to-UI context passing centrally in HIL. | user/agent action -> AI orchestration engine (MCP/A2A) -> routed model decision and context handoff | model-switching |
| HIL capability 4 (Zero-Trust Barrier) enforces segmentation and isolation via personal data (PII) protection, threat mitigation, policy engines, and data redaction. | L04.md:31 | Intercept all flows with policy and redaction engines to enforce Zero Trust and PII sanitization. | data traversal -> policy engine / PII redaction filter -> sanitized/isolated boundary artifact | security |
| HIL is not an application feature but core enterprise platform infrastructure that enables rapid UI/AI development and iteration. | L04.md:33 | Deploy HIL as reusable organizational platform infrastructure rather than per-app code. | app feature requirement -> platform infrastructure layer -> standardized deployment capability | architecture |
| AI-ready API orchestration requires token-based throttling and capacity orchestration rather than traditional requests-per-minute (RPM) rate limits. | L04.md:35 | Implement token-based rate limiting, quotas, and capacity management at the gateway layer (e.g., via LiteLLM). | prompt / completion stream -> token-based throttle engine -> rate-limited token allocation | telemetry |
| In AI, content delivery means semantic retrieval via RAG pipelines, embeddings, and vector databases rather than static content delivery. | L04.md:37 | Implement semantic retrieval and embedding vector stores as the content delivery mechanism for AI queries. | user query -> semantic retrieval pipeline / vector DB -> context-enriched prompt | task-intake-flow |
| AI/Experience Orchestration requires a multi-provider abstraction layer, canonical data models to normalize requests, SSE streaming support to avoid buffering large payloads, and semantic caching. | L04.md:39 | Standardize multi-model access through canonical request schemas, SSE streaming, and semantic cache rather than string-matching cache. | incoming AI prompt -> canonical data model / SSE handler / semantic cache -> normalized streaming response or cached semantic hit | task-intake-flow |
| Semantic caching must match queries based on semantic intent rather than literal strings (e.g., matching 'top-selling product' with 'most popular product in sales'). | L04.md:39 | Deploy vector/embedding-based semantic caching to avoid redundant model calls for semantically identical prompts. | varied semantic prompt -> semantic cache lookup -> cached completion response | cost |
| Zero-Trust Barrier for AI includes automated PII redaction, prompt injection detection, and compliance mechanisms such as human-in-the-loop audit logging. | L04.md:41 | Implement prompt injection filters, automated PII scrubbing, and audit logging for human-in-the-loop decisions at the AI gateway. | prompt/response intake -> security barrier / audit logger -> sanitized prompt and compliance audit log | compliance |
| Compliance in AI systems requires logging human-in-the-loop interactions, which can be handled centrally at the gateway layer like LiteLLM. | L04.md:41 | Centralize human-in-the-loop decision and review audit logging at the gateway level. | human intervention event -> gateway logging engine -> compliance audit trail | telemetry |
| Core enterprise systems (ERP, CRM) evolve yearly, whereas AI models and APIs change every few weeks, and MCP application specs evolve continuously (10x faster). | L04.md:43 | Insulate enterprise applications from bi-weekly model/API changes by placing HIL between them. | upstream AI model update -> HIL abstraction layer -> stable enterprise application interface | model-switching |
| Without HIL, changes in AI models require redeploying numerous business applications; HIL acts as a crumple zone allowing AI to develop at a two-speed organizational velocity. | L04.md:45 | Decouple release schedules so AI features evolve independently without forcing business application redeployments. | AI capability evolution -> HIL crumple zone -> zero-downtime decoupled application state | architecture |
| When applications integrate directly with AI, teams duplicate RAG pipelines (Legal, HR), CRM data fetching (Support, Finance), prompts, and PII handling. | L04.md:47 | Eliminate siloed, redundant implementations of RAG, CRM data ingestion, and PII redaction across departments by centralizing in HIL. | departmental AI requirement -> siloed application implementation -> duplicated RAG and PII logic | architecture |
| Fragmented embeddings and siloed AI logic create tight coupling where changes in AI implementations force widespread code modifications across business applications. | L04.md:49 | Standardize embedding pipelines and AI data fetching centrally to prevent application-layer lock-in. | embedding / model format change -> coupled business application -> widespread application refactoring | architecture |
| With HIL, all business applications send requests to HIL, which enriches prompts with customer, product, and invocation context before dispatching to specific AI providers. | L04.md:51 | Direct all AI requests to HIL to centrally append contextual corporate data (customer/product/call metadata) before calling AI models. | business app request -> HIL context enrichment pipeline -> context-augmented prompt to LLM provider | task-intake-flow |
| HIL allows switching underlying LLM models (e.g., swapping a support assistant's GPT version) while simultaneously enriching context from corporate knowledge graphs. | L04.md:53 | Abstract underlying model versions from business apps so models can be swapped without losing corporate knowledge enrichment. | model switch trigger -> HIL model router & enrichment engine -> switched model response with unified corporate context | model-switching |
| Applications should not call LiteLLM directly; instead, applications call HIL's chat completions endpoint, passing contextual metadata (e.g., user, channel, chat mode), and HIL delegates to LiteLLM. | L04.md:55 | Front LiteLLM with HIL to intercept chat completion calls and inject contextual parameters into the prompt structure. | chat completions API call with context -> HIL intake layer -> context-expanded prompt forwarded to LiteLLM | task-intake-flow |
| HIL determines how to expand prompts, route to appropriate downstream endpoints, and log transactions across the enterprise infrastructure. | L04.md:57 | Implement prompt expansion, intelligent routing, and enterprise logging inside HIL before requests reach execution backends. | augmented request payload -> HIL routing & logging engine -> logged transaction and dispatched model payload | telemetry |
| Organizations should introduce an intermediate HIL proxy in front of LiteLLM early—even if initially a simple data pass-through—to anchor future data flows and extensibility. | L04.md:57 | Introduce an early, lightweight pass-through HIL layer before LiteLLM to establish architectural placement for future routing and context logic. | architectural initialization -> pass-through HIL proxy -> extensible enterprise data flow anchor | architecture |


---

## Rows this file is evidence for

- **[#627]** - *the agy route is INERT; no row authorizes its analysis-role admission.* This file IS agy output produced in an analysis role, with its invocation, model and wall time recorded. It is a measurement against that row, not an authorization of it.
- **[#676]** - *no check verifies a provider CLI's non-interactive invocation shape.* The R3 header above records the shape that worked, and where a first attempt failed, the shape that did not.

