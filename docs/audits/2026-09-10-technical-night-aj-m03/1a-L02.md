# 1a - AJ M03 L02 (LLM Gateway)

**Reader:** agy - **ORDERED**, no substitution.
**Model:** gemini-3.8-flash-low.
**Exact invocation:** agy --model gemini-3.8-flash-low --print-timeout=15m --dangerously-skip-permissions --add-dir . --print=<per-lesson table prompt>, run from the split-transcript directory, with the table written by agy's own file-write tool.
**Wall time:** ~11 min (launched 2026-09-09T22:32, landed 22:43 +02:00).
**Delivery:** agy file-write.
**Why this lesson matters:** it is the MODEL-AGNOSTIC source - the gateway pattern is the model-switching mechanism M03 teaches.
**Locator caveat:** line numbers in L02.md; the source has no pages or timestamps.
**R2 status:** locators in this table are UNVERIFIED unless a row is marked otherwise, or unless this file is named in `R2-VERIFICATION.md`. Per R1/R2 nothing here is dropped; unverified content is carried and labelled.

---

| CLAIM | LOCATOR | PRACTICE-TAUGHT | MECHANISM-IMPLIED | TOPIC |
|---|---|---|---|---|
| If choosing a single tool or method to deploy secure AI in an enterprise organization, an LLM Gateway (acting as a man-in-the-middle between internal systems and AI systems) is the most critical component | L02.md:5 | Deploy an LLM Gateway as an intermediary control plane between applications and external AI APIs rather than connecting application code directly | AI integration requirement -> gateway deployment -> centralized control plane proxy | architecture |
| Direct integration of application code with multiple LLM provider APIs causes fragmentation, requiring each team to repeatedly write disparate retry logic, fallbacks, exception handling, and error formatting | L02.md:7 | Centralize provider integrations behind a common gateway layer to eliminate duplicated retry, fallback, and error handling logic across teams | provider fragmentation -> gateway abstraction -> unified resilience and error handling layer | architecture |
| More than half of engineering teams exceed their allocated AI tooling budget by approximately 50% due to an absence of centralized cost controls | L02.md:9 | Enforce centralized cost governance and token/spend tracking to prevent uncontrolled AI budget overruns | budget threshold definition -> spend tracking monitor -> cost ceiling enforcement | cost |
| Shadow AI and unmanaged API keys circulating in communication channels (e.g. Slack) cause security breaches due to lack of access control and data flow governance between operators and AI systems | L02.md:11 | Eliminate unmanaged API key distribution by routing all AI requests through centralized access-controlled gateways | developer key request -> credential management gate -> restricted gateway virtual key | security |
| Major LLM providers experience significant outages, meaning that systems lacking automated fallback mechanisms lose 100% of AI functionality for users during provider downtime | L02.md:13 | Implement automated provider fallbacks so that traffic seamlessly shifts to operational models when a primary provider suffers an outage | provider API downtime -> health probe / error interceptor -> failover model dispatch | model-switching |
| Enterprise prompts leak sensitive data to external public systems without filtration, with enterprise prompt analyses showing one-third contain legal text, nearly one-third contain source code, and 13% contain API keys | L02.md:15 | Inspect and sanitize outbound prompts before forwarding them to public model providers to prevent sensitive data leaks | outbound prompt submission -> prompt inspection filter -> sanitized outbound prompt | security |
| Traditional API gateways (e.g. Nginx, Envoy) designed for HTTP/REST evaluate throughput in Requests Per Minute (RPM), which is useless for LLM traffic where individual requests vary from 10 to 100,000 tokens | L02.md:19 | Measure and govern LLM traffic by effective load such as Tokens Per Minute (TPM) rather than traditional HTTP Requests Per Minute (RPM) | incoming request stream -> token rate counter -> TPM-based traffic throttling | telemetry |
| Traditional gateways inspect only inbound requests, whereas LLM governance requires scanning response payloads to ensure safety, policy compliance, and data protection | L02.md:21 | Scan and evaluate LLM responses in addition to input requests at the gateway boundary | model response generation -> response payload scanner -> validated safe response payload | security |
| Traditional API gateways cannot evaluate prompt complexity to route simple prompts to cheap models and complex prompts to capable models | L02.md:23 | Implement context-aware semantic routing that selects appropriate models based on prompt complexity | prompt submission -> semantic complexity evaluator -> routed model destination | model-switching |
| Traditional HTTP gateways handle large Server-Sent Events (SSE) streaming traffic poorly, which is the standard transmission mechanism for LLM responses | L02.md:25 | Deploy specialized gateway infrastructure capable of handling high-volume persistent Server-Sent Events (SSE) streaming connections | streaming response request -> SSE stream manager -> persistent client stream channel | architecture |
| Multi-team LLM governance requires tracking detailed cost structures by attributing exact token consumption and expenditure per team and model | L02.md:25 | Log granular token consumption per team and model to attribute cost accurately across the enterprise | request completion -> usage attribution logger -> team cost accounting ledger | cost |
| An AI Gateway is not merely a reverse proxy but a specialized control plane encompassing security, load balancing, circuit breakers, routing, governance, and backend abstraction | L02.md:27-29 | Adopt a dedicated AI Gateway control plane integrating authentication, sanitization, load balancing, routing, and governance | application AI invocation -> AI Gateway control plane -> orchestrated backend execution | architecture |
| Reference gateway architectures intercept traffic from applications, autonomous AI agents, human users, and developer tooling (e.g. Claude Code) before it reaches models | L02.md:29 | Route all organizational AI traffic sources—including interactive developer CLI tools and autonomous agents—through the centralized gateway layer | tool or agent invocation -> gateway interceptor -> governed API request | task-intake-flow |
| Reference gateway security layers issue scoped tokens and allocate virtual API keys according to caller identity, calling team, and application | L02.md:29 | Issue scoped virtual API keys per team and application to enforce granular access control without exposing master provider keys | client access request -> gateway identity provider -> scoped virtual API key | security |
| Reference gateway security layers enforce PII redaction, auditability, and centralized logging | L02.md:29 | Intercept and redact personal identifiers while maintaining an immutable audit log across all model traffic | prompt ingestion -> PII scrubbing filter -> redacted prompt and audit log record | security |
| Reference gateway load balancing incorporates circuit breakers to protect availability and handle fluctuating provider traffic | L02.md:29 | Implement circuit breakers on provider integrations to trip and isolate failing backends immediately | backend request failure -> circuit breaker evaluator -> tripped route and fallback redirection | architecture |
| Reference gateway routing modules dynamically select models based on latency, cost, prompt complexity, and specific provider constraints | L02.md:29 | Configure multi-criteria routing rules prioritizing models by latency SLAs, monetary cost, complexity, or provider traits | incoming request -> multi-factor routing engine -> selected optimal model target | model-switching |
| Reference gateway governance layers enforce organizational budgets, access policies, and compliance rules before forwarding prompts to cloud providers or local backends (e.g. Ollama) | L02.md:29 | Enforce hierarchical budget caps and operational policies across both external cloud providers and internal self-hosted backends | prompt dispatch request -> governance policy engine -> policy approval or budget rejection | cost |
| A centralized single gateway architecture for the entire organization maximizes prompt cache hit rates because high volumes of repetitive enterprise prompts are reused across teams | L02.md:31 | Centralize the AI gateway across the organization to maximize caching efficiency and response reuse on repeated prompts | prompt arrival -> gateway prompt cache -> cached response artifact | architecture |
| A single centralized gateway consolidates provider API keys in one location, avoiding key sprawl across dozens of independent services | L02.md:31 | Store provider master credentials exclusively within the centralized gateway rather than distributing them across service repositories | service deployment -> gateway credential vault -> secure upstream model call | security |
| A single centralized gateway provides a single control point for security policies, rate limits, and audit logs required to prove regulatory compliance (e.g. EU AI Act) | L02.md:31 | Consolidate compliance policy enforcement, rate limiting, and regulatory audit logging into a single verifiable gateway checkpoint | audit inspection trigger -> central audit logger -> EU AI Act compliance evidence | compliance |
| Organizations with strict domain isolation requirements (such as Chinese walls in banking) must sandbox and partition cache and routing domains rather than sharing cache across isolated units | L02.md:33 | Partition caching and routing domains into sandboxed instances when regulatory or organizational separation rules prohibit shared data access | isolated team request -> tenant domain sandbox -> isolated partition cache | security |
| Open-source Python gateways like LiteLLM integrate seamlessly with existing enterprise infrastructure (such as Kubernetes, Redis, and SQL databases) without exporting telemetry externally | L02.md:35 | Deploy self-hosted open-source gateways (e.g. LiteLLM) backed by Redis and SQL within Kubernetes infrastructure to maintain full data custody | gateway container start -> Kubernetes/Redis/SQL orchestrator -> on-premise gateway cluster | architecture |
| LiteLLM provides a unified API standardizing around the OpenAI API schema, enabling applications to switch underlying providers and models without modifying application code | L02.md:37 | Standardize internal application code on a unified API contract (e.g. OpenAI format) pointing to the gateway to prevent provider lock-in | application API call -> unified protocol translation -> provider-native API request | model-switching |
| Logical model aliases (e.g. `prod-model`) decouple application code from physical models, allowing provider swaps and model upgrades via gateway configuration changes without code refactoring | L02.md:37 | Route requests using logical model identifiers (`prod-model`) mapped to physical model targets within gateway routing tables | logical alias invocation -> alias mapping table -> physical provider model target | naming |
| Enterprise AI governance comprises a three-pillar stack: pre-deployment evals, real-time runtime guardrails, and post-production observability | L02.md:39-47 | Implement AI governance using a three-stage lifecycle: development evals, runtime guardrails, and post-production observability | SDLC phase transition -> governance tier evaluator -> lifecycle compliance artifact | architecture |
| Pre-deployment evals serve as unit tests for prompts and model integrations during development using specialized tools (e.g. Ragas, DeepEval, Promptfoo) to assess jailbreak resistance and faithfulness | L02.md:41 | Run automated unit evaluation suites on prompts and model integrations during development to verify quality and safety before release | test pipeline trigger -> eval framework (Ragas/DeepEval/Promptfoo) -> evaluation scorecard | testing |
| Pre-deployment evals are costly, complex, and contested, often requiring production-grade LLM judges whose maintenance mirrors the trade-offs between performance testing and production APM | L02.md:43 | Balance heavy pre-deployment eval investments against live production monitoring, using runtime telemetry to rapidly detect issues when eval suites are cost-prohibitive | deployment readiness check -> LLM-as-a-judge runner -> pre-flight evaluation report | testing |
| Runtime guardrails act as non-negotiable seatbelts and real-time middleware that intercept prompts to mask content or block unsafe requests during active execution | L02.md:45 | Deploy synchronous runtime guardrail middleware to validate, sanitize, or block prompt traffic on the live path | live request arrival -> guardrail middleware -> masked payload or blocked request | security |
| Production observability functions as a post-production review mechanism, surfacing real costs, hallucination rates, and model errors to feed back into development and eval suites | L02.md:47 | Use production observability telemetry to identify hallucinations and cost spikes, feeding defect traces back into evaluation test suites | production trace capture -> observability engine -> test case refinement artifact | telemetry |
| Microsoft Presidio can be integrated into the gateway as a pre-call guardrail that uses regex and pattern analyzers to detect PII entities such as national identification numbers (PESEL) and personal names | L02.md:51 | Configure automated PII detection engines (e.g. Presidio Analyzer) as pre-call gateway hooks to identify sensitive personal entities | inbound prompt -> Presidio Analyzer -> detected PII entity list | security |
| Presidio Anonymizer replaces detected sensitive entities with structured token placeholders (e.g. `{PESEL}`, `Person 1`) before prompts are dispatched to external model providers | L02.md:53 | Anonymize sensitive entity text with indexed structural placeholders prior to transmitting prompts to external models | detected PII list -> Presidio Anonymizer -> anonymized prompt payload | security |
| Post-call de-anonymization can restore original personal identifiers into the model's response before returning it to the client application | L02.md:55 | Re-map placeholder tokens back to original user entities on the response path so the application receives complete business data | model response reception -> de-anonymization replacer -> reconstituted user response | security |
| Guardrail YAML configurations in LiteLLM define pre-call execution modes, entity-specific rules, and distinct actions such as hard blocking for credit cards versus masking for identification numbers and phone numbers | L02.md:57 | Declare granular guardrail policies in configuration, defining blocking rules for financial secrets and masking rules for personal identifiers | prompt inspection -> declarative policy evaluator -> blocked request or masked payload | security |
| Default language-specific PII filters (such as Presidio's official PESEL rule requiring Polish request metadata) can silently fail on unflagged requests, requiring custom language-agnostic detection regex filters | L02.md:59 | Deploy language-agnostic regex filters for region-specific identifiers (e.g. PESEL) to prevent silent inspection bypasses | prompt ingestion -> language-agnostic regex filter -> sanitized entity tokens | security |
| Overly aggressive PII masking in Polish can redact normal sentence fragments, necessitating observability review to tune filter sensitivity and prevent prompt degradation | L02.md:59 | Inspect telemetry logs of redacted content to tune PII masking sensitivity against over-redaction that breaks semantic intent | redaction execution -> observability audit log -> tuned guardrail sensitivity profile | telemetry |
| Gateway guardrail ecosystems support pluggable integrations across multiple security providers, including Presidio, AWS Guardrails, OpenAI Moderation, and Google Cloud Model Armor | L02.md:61 | Integrate pluggable multi-vendor guardrails at the gateway layer to address diverse threat vectors | prompt ingress -> multi-provider guardrail chain -> filtered execution payload | security |
| Token-based budget enforcement at the gateway establishes hard spending caps across customizable time windows (e.g. $100 per 30 days) | L02.md:63 | Configure hard budget caps over rolling calendar windows at the gateway layer to prevent cost overruns | request token calculation -> budget quota tracker -> hard spend cap enforcement | cost |
| Hierarchical organizational budget models divide global budgets across teams and individual users using dedicated virtual API keys without exposing master production credentials | L02.md:63 | Provision hierarchical virtual keys with team- and user-level spending quotas to isolate costs and prevent key exposure | user request -> virtual key quota validator -> scoped request authorization | cost |
| Automated fallback management dynamically routes requests to secondary models when a primary model fails, tracking recovery timeouts to reinstate healthy backends | L02.md:65 | Configure dynamic fallback chains and health recovery probes to maintain service resilience across model outages | primary model timeout/error -> fallback routing manager -> secondary model request | model-switching |
| Resilient routing and automated fallback architectures enable high-volume systems (handling tens of thousands of requests per minute) to achieve 99.9% to 99.99% operational uptime | L02.md:65 | Combine intelligent routing with automatic retries and circuit breakers to achieve three to four nines of availability for LLM services | high-volume request load -> resilient gateway router -> high-availability service response | architecture |
| Advanced gateway routing strategies include usage-based routing (load distribution across token quotas) and latency-based routing (dispatching to the fastest responding model) | L02.md:67 | Implement usage-based and latency-based routing algorithms to optimize model throughput, turnaround speed, and quota utilization | inbound request -> telemetry-driven routing engine -> lowest-latency / capacity-available model | model-switching |
| Gateway processing overhead adds only a negligible few milliseconds to overall request latency, which is statistically insignificant compared to multi-second model inference times | L02.md:69 | Deploy gateway middleware without latency concerns, as proxy execution overhead represents a fraction of a percent of model response time | request interception -> lightweight proxy pipeline -> minimal latency overhead | architecture |
| Well-configured gateways often achieve net latency reductions by serving repeated requests directly from connection pools and semantic/exact response caches | L02.md:71 | Utilize gateway connection pooling and response caching to accelerate repeated prompt execution and reduce round-trip times | repeated prompt request -> gateway cache / connection pool -> accelerated cached response | architecture |
| Gateway circuit breakers detect failing or sluggish providers in real time, immediately diverting subsequent traffic to alternate models without waiting for repeated timeouts | L02.md:71 | Deploy circuit breakers that monitor backend error rates and latency, triggering instant failover away from degraded providers | provider latency spike/failure -> circuit breaker trip -> instant alternative provider routing | model-switching |
| Infrastructure maturity progresses across distinct stages: starting from unmanaged Shadow AI, progressing to single-team pilots, expanding to standardized cross-team adoption, and culminating in full organization-wide gateway routing | L02.md:73 | Drive organizational AI maturity iteratively from ad-hoc usage toward comprehensive, centralized gateway adoption | organizational maturity assessment -> staged gateway rollout roadmap -> enterprise-wide gateway standard | architecture |
| Advanced organizational maturity mandates routing all developer CLI and coding assistant traffic (such as Claude Code) through enterprise gateways to capture comprehensive telemetry | L02.md:75 | Funnel developer agent and coding CLI traffic through the enterprise gateway to gain visibility into developer prompts, generated code, and tooling spend | developer CLI command -> enterprise gateway capture -> centralized developer telemetry | telemetry |
| Developer tool telemetry captured via gateways enables monitoring the correlation between LLM-generated code and the actual committed codebase | L02.md:75 | Analyze gateway telemetry to measure the parity between AI code generation outputs and final repository commits | code generation completion -> commit comparison analyzer -> code parity and impact report | telemetry |
| Implementation of an intermediary gateway layer is a mandatory architectural prerequisite for satisfying formal legal compliance obligations under the EU AI Act and ISO AI standards | L02.md:77 | Deploy an intermediary AI gateway to establish the logging, security, and governance boundaries required by the EU AI Act and ISO standards | compliance audit review -> gateway governance enforcement -> verified regulatory compliance certification | compliance |


---

## Rows this file is evidence for

- **[#627]** - *the agy route is INERT; no row authorizes its analysis-role admission.* This file IS agy output produced in an analysis role, with its invocation, model and wall time recorded. It is a measurement against that row, not an authorization of it.
- **[#676]** - *no check verifies a provider CLI's non-interactive invocation shape.* The R3 header above records the shape that worked, and where a first attempt failed, the shape that did not.

