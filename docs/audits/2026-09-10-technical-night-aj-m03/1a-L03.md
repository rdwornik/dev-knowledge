# 1a - AJ M03 L03 (BAML)

**Reader:** agy - **ORDERED**, invocation CORRECTED once. Not a substitution.
**Model:** gemini-3.8-flash-low.
**First attempt FAILED:** the file-write variant, run as one of six concurrent agy processes, died with: error: the connection to the agent was interrupted before the response finished: subscriber fell behind updates, stalled for 5s. A CLIENT-SIDE streaming stall under concurrency, not a model or network failure.
**Exact invocation (the one that answered):** agy --model gemini-3.8-flash-low --print-timeout=15m --dangerously-skip-permissions --add-dir . --print=<per-lesson table prompt>, run from the split-transcript directory, plus 'Print ONLY the table to stdout. Do not write any file.', run with at most two concurrent agy processes.
**Wall time:** ~9 min.
**Delivery:** stdout; two duplicate renders removed on capture.
**Locator caveat:** line numbers in L03.md.
**R2 status:** locators in this table are UNVERIFIED unless a row is marked otherwise, or unless this file is named in `R2-VERIFICATION.md`. Per R1/R2 nothing here is dropped; unverified content is carried and labelled.

---

| CLAIM | LOCATOR | PRACTICE-TAUGHT | MECHANISM-IMPLIED | TOPIC |
| :--- | :--- | :--- | :--- | :--- |
| Treating prompts as raw strings in application code lacks types, schema contracts, and isolation testing guarantees | L03.md:5 | Replace raw string prompts with typed interface contracts | prompt string -> application code -> untyped runtime failure | architecture |
| Architecturally enforcing predictability and type safety changes how applications interact with LLMs | L03.md:7 | Architecturally enforce predictability and type contracts around LLMs | architecture rule -> interaction layer -> deterministic response | architecture |
| Direct JSON parsing of raw LLM output is brittle due to conversational filler and markdown wrapping | L03.md:9 | Avoid direct `JSON.parse` on raw unvalidated LLM output | raw model response -> JSON parser -> runtime crash | task-intake-flow |
| Provider-native structured outputs exhibit inter-provider differences and do not guarantee consistent behavior in production | L03.md:11 | Do not rely solely on provider-native structured output for mission-critical production guarantees | provider-specific constraint -> LLM gateway -> vendor-dependent failure | model-switching |
| Raw string parsing relies on fragile regex heuristics to extract data | L03.md:13 | Avoid regex-based extraction from unstructured model output | raw string response -> regex extractor -> malformed data | task-intake-flow |
| API-native structured outputs enforce JSON schemas only at runtime and cause vendor lock-in | L03.md:15 | Avoid proprietary runtime schema constraints that lock the codebase into a single vendor | API-native JSON schema -> provider decoding -> vendor lock-in | model-switching |
| BAML provides compile-time safety and self-repairing parsing that consumes fewer tokens than JSON Schema | L03.md:17 | Use BAML compile-time safety and parsing to fix broken outputs while optimizing token usage | BAML schema -> parsing engine -> sanitized typed output | architecture |
| Constrained decoding in API-native outputs fails hard on schema mismatches, whereas Schema-Aligned Parsing repairs malformed outputs | L03.md:21 | Use Schema-Aligned Parsing to automatically repair malformed model outputs instead of throwing hard decoding errors | malformed LLM response -> Schema-Aligned Parsing engine -> repaired structured output | task-intake-flow |
| Raw string and API-native output approaches lack built-in prompt unit testing facilities | L03.md:23 | Use testable prompt definitions rather than uninstrumented string calls | test execution -> prompt testing harness -> test report | testing |
| Prompts should be defined as strongly typed functions with named inputs, typed outputs, and unit tests in a dedicated DSL | L03.md:25 | Define prompts as strongly typed functions with explicit input/output contracts using BAML DSL | DSL function definition -> BAML compiler -> typed testable unit | naming |
| A Rust compiler generates strongly typed client interfaces from BAML definition files | L03.md:27 | Generate strongly typed client SDKs from BAML specifications during compilation | BAML file -> Rust compiler -> typed client SDK | architecture |
| BAML acts as a type boundary layer between the application and LLM providers to enforce structured responses | L03.md:27 | Insert a dedicated type layer between application logic and LLM APIs | model response -> BAML type layer -> structured entity | architecture |
| Field descriptions annotated with `@description` are injected directly into the LLM prompt as semantic instructions | L03.md:29 | Annotate schema fields with `@description` to guide LLM extraction behavior | `@description` annotation -> schema injector -> enriched prompt context | naming |
| Enum values and their semantic descriptions are injected into prompt context to constrain classification | L03.md:31 | Define enums with explicit value descriptions for precise LLM classification | enum definition -> prompt renderer -> constrained model choice | naming |
| BAML type schema representation consumes up to 80% fewer tokens than equivalent JSON Schema | L03.md:33 | Use compact BAML type schemas instead of verbose JSON Schemas to reduce token consumption and prompt confusion | compact BAML schema -> token tokenizer -> reduced prompt token count | cost |
| BAML prompt functions encapsulate client reference, prompt template, and output formatting under the Prompt-as-a-Function pattern | L03.md:35 | Implement the Prompt-as-a-Function pattern encapsulating client target and schema injection | prompt function invocation -> function engine -> typed result object | architecture |
| The `ctx.output_format` macro automatically injects the expected target schema into the prompt without manual JSON formatting | L03.md:35 | Use `ctx.output_format` macro to inject output schema specifications into prompt templates | `ctx.output_format` macro -> prompt builder -> schema instruction block | task-intake-flow |
| The `ctx.output_format` macro automatically expands classes, enum values, and primitives into schema instructions | L03.md:39 | Leverage macro-driven schema generation rather than hand-crafting serialization instructions | type definition -> `ctx.output_format` macro -> injected structural instructions | task-intake-flow |
| Schema-Aligned Parsing (SAP) corrects malformed JSON outputs in milliseconds | L03.md:45 | Use SAP to autocorrect malformed JSON output instead of paying latency and token costs on retries | malformed raw output -> SAP Rust engine -> corrected typed object | cost |
| Schema-Aligned Parsing repairs missing brackets, extra keys, case errors, and scalar-array mismatches | L03.md:47 | Rely on SAP to resolve structural edge cases while validating structure rather than semantic content | structural discrepancy -> SAP normalizer -> valid schema instance | task-intake-flow |
| Traditional LLM integration requires brittle regex matching and manual retry logic on schema drift | L03.md:49 | Eliminate manual regex extraction and brittle retry logic in application code | raw API completion -> regex extractor -> parse exception | task-intake-flow |
| Calling BAML generated functions produces guaranteed strongly typed results within application code | L03.md:51 | Call generated typed client functions directly from application code | client method invocation -> generated typed client -> validated domain object | architecture |
| Compiler-driven validation and SAP type correction eliminate manual schema prompting and fragile glue code | L03.md:53 | Replace fragile runtime parsing glue code with compiler-verified BAML clients | BAML compilation -> Rust compiler -> type-safe client binding | architecture |
| Tests defined alongside BAML functions support hard assertions using `@@assert` that fail the test run | L03.md:55 | Define prompt unit tests alongside functions with `@@assert` for strict validation | test case evaluation -> `@@assert` evaluator -> test failure status | testing |
| Soft assertions using `@@check` report quality discrepancies without breaking the evaluation pipeline | L03.md:55 | Use `@@check` soft assertions to track quality metrics without blocking CI pipelines | test case evaluation -> `@@check` evaluator -> quality warning report | telemetry |
| BAML provides a multi-second iterative test cycle via CLI and IDE playgrounds tracking output and token consumption | L03.md:57 | Use BAML CLI and VSCode Playground for fast prompt testing and token observability | test runner execution -> testing harness -> test telemetry output | testing |
| Compiling BAML produces a `baml_client` directory or can serve REST APIs as fallback for unsupported languages | L03.md:59 | Generate `baml_client` code for native languages or deploy REST API fallbacks for other runtimes | BAML source file -> code generator -> `baml_client` package | architecture |
| A single BAML file serves as the Single Source of Truth regenerating all downstream client contracts on modification | L03.md:59 | Maintain a single BAML schema file as the authoritative contract source for all clients | schema modification -> compiler regeneration -> synced client code | naming |
| Centralized schema definitions enable cross-language client reuse across microservices and plugins | L03.md:63 | Share compiled BAML client interfaces across polyglot microservices and plugins | centralized BAML spec -> compiler generation -> polyglot service clients | architecture |
| Decouple the type layer from model routing by combining BAML for output typing and LiteLLM for provider management | L03.md:65 | Separate schema enforcement from model routing by fronting providers with LiteLLM | client invocation -> BAML type layer -> LiteLLM proxy | architecture |
| BAML clients route through LiteLLM via logical model identifiers and base URL redirection | L03.md:65 | Target LiteLLM endpoints with logical model names in BAML client definitions | logical model target -> LiteLLM proxy -> upstream provider dispatch | model-switching |
| Pairing BAML with LiteLLM enables model switching via YAML configuration without application refactoring | L03.md:67 | Combine BAML and LiteLLM to achieve provider independence and zero-code model swapping | YAML configuration edit -> LiteLLM router -> switched model target | model-switching |


---

## Rows this file is evidence for

- **[#627]** - *the agy route is INERT; no row authorizes its analysis-role admission.* This file IS agy output produced in an analysis role, with its invocation, model and wall time recorded. It is a measurement against that row, not an authorization of it.
- **[#676]** - *no check verifies a provider CLI's non-interactive invocation shape.* The R3 header above records the shape that worked, and where a first attempt failed, the shape that did not.

