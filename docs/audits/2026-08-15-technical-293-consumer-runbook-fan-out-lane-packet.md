# Lane q — [#293] consumer runbook fan-out — STOP-and-report packet

T_start: 2026-08-15T16:41:29+02:00 (step-0 contract-of-record commit `8fd64cb8`; no earlier
in-tree clock reading is available for the actual dispatch instant).

## OWNED-FILES manifest, as executed

Per the contract's own line 3 ("if the row's Done-when requires touching another repo … STOP-and-report
with the derived plan instead"), the row-read below establishes that clause fires. Executed
footprint is therefore narrower than the contract's nominal step 1:

1. `docs/audits/2026-08-15-technical-293-consumer-runbook-fan-out-lane-contract.md` — contract of
   record (I-D3), step 0.
2. `docs/audits/README.md` — regenerated by `gen_audit_index.py` (excluded from the manifest
   restriction per contract line 16).
3. This file — the STOP-and-report packet.

No `docs/handoffs/README.md` was written in any consumer repo. No file outside this list was
touched.

## Step 0 — COMMIT

`8fd64cb8` — docs(audits): #293 satellite-runbooks contract of record (I-D3), step 0 — lane
letter q confirmed live.

## Row read (row-is-the-spec)

`tasks/293-consumer-runbook-fan-out.md`:

> seed each consumer repo's `docs/handoffs/README.md` from the hub canonical source via
> `python scripts/seed_runbook.py --target-root <repo>` … Executes per-repo in the Wave-1/Wave-2
> onboarding arcs (ADR-41 — consumer-side, queue-only here) … Done when: each onboarded consumer
> carries the seeded runbook (per-repo tracked, n≥1 recorded) … UN-DEFERRED 2026-08-09 (ARC-2):
> … work NOT done (0 of 6 consumers seeded), so un-defer not close.

The row's own Done-when is a write into each consumer repo's tree. That is a cross-repo write.

## Why STOP fires (quoted, not assumed)

- Contract line 3: *"If the row's Done-when requires touching another repo (satellite checkouts),
  STOP-and-report with the derived plan instead — cross-repo writes are not authorized by this
  contract."*
- `scripts/seed_runbook.py` module docstring (the exact tool the row names): *"Single-target by
  construction … the cross-repo consumer FAN-OUT is deliberately deferred to the per-repo
  Wave-1/Wave-2 onboarding arcs (ADR-41), so a hub session never writes into a consumer's tree
  *unmediated* — the sanctioned path is a consumer-local worktree/branch → report (RULING-W,
  ADR-36/41 amend. 2026-07-18); this tool is not that path. This session ships the MECHANISM + a
  hermetic test; it seeds no consumer."* and *"Run it FROM (or FOR) the repo being seeded; do not
  point it at a consumer from the hub."*
- `docs/handoffs/README.md`'s own header comment: *"the cross-repo consumer fan-out runs per-repo
  in the Wave-1/Wave-2 onboarding arcs, ADR-41, so the hub never writes a consumer's tree."*

Three independent sources — the contract, the tool's own docstring, and the canonical source
file's header — agree on the same boundary. No invented path was needed to reach this
conclusion; it was already stated at all three sites.

## What was NOT invented

The OWNED-FILES clause requires every home to be quote-justified before writing. There is no
hub-side "runbook file" the row names other than the canonical source (`docs/handoffs/README.md`,
already committed and unchanged — it is the *source* `seed_runbook.py` reads, not a deliverable
of this row) and the seeded copies inside each consumer's own tree. Writing anything else in the
hub to stand in for consumer seeding would be an invented path with no quoted home — exactly what
the contract's zero-invented-paths clause forbids.

## Derived plan (for the operator / a consumer-local seat)

The sanctioned mechanism already exists and needs no new code (#164 leg b shipped it):

1. From inside (or targeting) each consumer repo's own worktree/branch, run:
   `python <hub>/scripts/seed_runbook.py --target-root .` (or `--target-root <path>` from a
   session rooted in the hub but writing only through a consumer-local worktree, per RULING-W).
2. Commit the seeded `docs/handoffs/README.md` on that consumer's own branch, report back
   (per-repo tracked, n≥1 recorded — the row's own acceptance shape).
3. Repeat per consumer until the row's "n of 6" count reaches 6/6, then close the row.

**Open question this lane could not resolve without touching another repo:** which 6 consumers
are in scope. The ADR-104 fleet declaration names 8 non-hub members (`ai-council`,
`corp-monorepo`, `corp-ops`, `corp-sca-time-automation`, `demo-prep`, `life-architect`,
`terminal-setup`, `win-tooling`); the row says 6. No hub-side artifact found in this lane's search
(`ecosystem/satellite-onboarding-rulings.yaml`, `protocols/REPO_ONBOARDING.md`,
`ecosystem/index.yaml`, ADR-41) enumerates the specific 6 for *this* row — #282's "4 consumers"
list is for a different check (`.gitattributes` parity) and does not transfer. Resolving this is
itself a hub-side, in-scope act (reading/searching the hub tree), but this lane stops short of
guessing rather than asserting an unverified list.

## Decision budget (V-2), reported

- Escalation class used: (a)-adjacent — not a curated-baseline touch, but a row whose Done-when
  collides with a standing cross-repo guardrail. Decided per contract default (STOP-and-report is
  the contract's own stated instruction for this exact fork), not batched as a question, because
  the contract already names the outcome — no operator judgment call was required to reach it.
- No other decision points arose.

## Targeted checks

Doc hooks touching `docs/audits/*`: `audit-index-freshness` (fired and passed at the step-0
commit `8fd64cb8`, index regenerated via `gen_audit_index.py --write`); `audit-health` (fired and
passed at `8fd64cb8`). No code was touched; no suite run beyond these gates (tiered-suite law —
targeted only).

## Done-when, clause by clause vs delivered

Row's Done-when: "each onboarded consumer carries the seeded runbook (per-repo tracked, n≥1
recorded)."

- Delivered: 0/6 → **0/6** (unchanged). This lane performed no consumer-repo writes (none
  authorized) and therefore advances the count by zero. The row stays UN-DEFERRED / open; it is
  not closed and no BACKLOG edit was made.

## Deviations self-reported

- None from the contract's own instructions. The one substantive choice — STOP rather than
  attempt step 1 — is the contract's own line 3 applied to what the row-read found, not a
  deviation from it.

STOPPED.
