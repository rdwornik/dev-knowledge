# LANE batch-e-m-13-templates-disposition (HY-3) - act on tier (A)'s two censuses - universalize, relocate or retire. NO deletion of a live source.

| Model | Mode | Effort |
|---|---|---|
| opus | execute | high |

## Dispatch

**Shape:** `local`

```
Dispatch-Lane lane-m-13-templates-disposition LANE-m-13-templates-disposition.md -Effort high
```

**SUBSTRATE IS `local` BY MEASUREMENT, NOT PREFERENCE.** Batch E's codespace admission probe ran
2026-08-31 and came back RED - `RemoteExitCode=1`, the container's agent returning *"Not logged
in - Please run /login"* in 47 ms
(`docs/audits/2026-08-31-verification-codespace-admission-probe.md`). Z-G3's entry condition is
NOT met, so section 1(C)'s named fallback binds every committing lane in this batch.

## Worktree pairing

slug `lane-m-13-templates-disposition` -> branch `worktree-lane-m-13-templates-disposition` -> contract `LANE-m-13-templates-disposition.md`

## Write-scope (frozen)

- `templates/`
- `docs/audits/2026-08-31-technical-hy3-disposition.md`

## Blocked by

**Tier (A)'s A1 and A2 harvests.** This lane consumes censuses; it does not conduct them.

## THE `conflict/` HALF HAS NO SUBJECT - refuted at freeze

The brief pairs `templates/` with `conflict/`. **`conflict/` does not exist** - zero tracked
files in the hub, absent from all six sibling fleet repos, and cited as a path by no tracked
`.md`, `.py` or `.yaml`. A1's subject was re-pointed to Z-G5's actual discipline, *"No
single-file folders, ever"*, measured at **44 tracked single-file directories** at freeze.
**Do not invent a `conflict/` subject.** If A1's harvest disagrees, report the disagreement.

## Done-contract (immutable)

1. **Each censused item gets exactly one disposition** - universalize, relocate, or retire -
   with the evidence from the census attached.
2. **NO DELETION OF A LIVE SOURCE.** The brief says it and it is not softened here. A retirement
   is a relocation plus a retention rule, never an `rm`.
3. **`templates/archive/` is treated SEPARATELY.** An already-archived template is not dead
   stock, and conflating the two proposes deleting the record.
4. **Z-G5 exceptions the repo has granted IMPLICITLY are named.** A folder that is single-file
   today and passes every gate is an exception nobody recorded - those are the finding.

## Steps

1. Harvest and read A1 and A2. Do not re-measure what they measured.
2. Disposition each item with its evidence. Act only on `templates/`.
3. Run the TARGETED tests for this diff. Commit and STOP.

## Decision budget

**V-2 - escalate on three classes only:** (a) curated-baseline touches, (b) genuine
rule-vs-ruling conflicts, (c) fork classes with no standing ruling. Everything else is decided
per contract defaults and reported in the end packet. A refuted premise PAUSEs with the fact
(Q10) - deviation-with-disclosure discharges the reporting duty, it does not authorise the
deviation.

## What NOT to do

- No merges, no pushes to `main` - commit-and-STOP; integration is the integrator's act.
- No JOURNAL entry (`protocols/STANDING_RULINGS.md` P-1), no index regeneration (Q1).
- No row births beyond what the done-contract names - reconcile-before-birth binds this batch.
- No edits outside the declared footprint. Prose in English; hyphen-only names.
