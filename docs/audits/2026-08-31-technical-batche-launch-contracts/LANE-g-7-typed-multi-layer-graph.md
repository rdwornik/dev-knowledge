# LANE batch-e-g-7-typed-multi-layer-graph (DM-3) - DRAFTING lane - typed layers plus architectural attributes as node metadata. Intake #40, which is DRAFT and carries NO row.

| Model | Mode | Effort |
|---|---|---|
| opus | execute | high |

## Dispatch

**Shape:** `local`

```
Dispatch-Lane lane-g-7-typed-multi-layer-graph LANE-g-7-typed-multi-layer-graph.md -Effort high
```

**SUBSTRATE IS `local` BY MEASUREMENT, NOT PREFERENCE.** Batch E's codespace admission probe ran
2026-08-31 and came back RED - `RemoteExitCode=1`, the container's agent returning *"Not logged
in - Please run /login"* in 47 ms
(`docs/audits/2026-08-31-verification-codespace-admission-probe.md`). Z-G3's entry condition is
NOT met, so section 1(C)'s named fallback binds every committing lane in this batch.

## Worktree pairing

slug `lane-g-7-typed-multi-layer-graph` -> branch `worktree-lane-g-7-typed-multi-layer-graph` -> contract `LANE-g-7-typed-multi-layer-graph.md`

## Write-scope (frozen)

- `docs/intake/2026-08-22-tech-document-dependency-graph-organ.md`

## READ THIS FIRST - the brief's row citation was REFUTED at freeze

The batch brief routes this lane to `[#615]`. **`[#615]` is MODEL ATTRIBUTION** - a
model+version commit trailer, from intake #50. It is not the graph. **No `tasks/` row mentions
FPG, typed layers or architectural attributes at all**; the graph has no backlog row in any form.

Its real home is **intake #40**, `status: DRAFT`, extended by operator direction on 2026-08-29
from a *document* dependency graph to a **typed, multi-layer** one (layers: code, docs, backlog,
intake/ADR, and **L0**), explicitly cross-referencing intake #62 - the `.CLAUDE` GOVERNANCE
MODEL umbrella amended 2026-08-31.

## Done-contract (immutable)

1. **THIS IS A DRAFTING LANE, NOT A BUILD LANE.** Intake #40 is DRAFT and deliberately carries
   no carrier row: ADR-111 section 2 forbids a lane birthing one. **Build nothing.**
2. **Amend intake #40** with the typed-layer node-metadata design - the layers, the attributes,
   and the cross-layer edges to intake #62's L0 layer.
3. **ZERO rows born.** If the design feels ready to build, that readiness is REPORTED, not acted
   on; ratification is the operator's act.

## Steps

1. Read intake #40, its 2026-08-29 note, and intake #62's 2026-08-31 amendment.
2. Draft the design as an amendment. Commit and STOP.

## Decision budget

**V-2 - escalate on three classes only:** (a) curated-baseline touches, (b) genuine
rule-vs-ruling conflicts, (c) fork classes with no standing ruling. Everything else is decided
per contract defaults and reported in the end packet. A refuted premise PAUSEs with the fact
(Q10) - deviation-with-disclosure discharges the reporting duty, it does not authorise the
deviation.

## What NOT to do

- No merges, no pushes to `main` - commit-and-STOP; integration is the integrator's act.
- No JOURNAL entry (`protocols/STANDING_RULINGS.md` P-1), no index regeneration (Q1).
- No row births beyond what the done-contract names - reconcile-before-birth binds this batch.
- No edits outside the declared footprint. Prose in English; hyphen-only names.
