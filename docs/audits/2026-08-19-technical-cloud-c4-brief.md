# CLOUD C4 — RULING PRE-WORK PACK: [#397] + [#488] · read-only + research

Cloud lane. FIRST COMMIT = dispatch-stamp (this prompt as
`docs/audits/2026-08-19-technical-c4-ruling-prework-contract.md`). **No index regeneration, no
JOURNAL writes.** Read-only + one artifact.

Two AWAITING-RULING rows name their pre-work (N4 items 13–14); you deliver it so the architect
can rule tomorrow without a build step.

ITEM 1 — **[#397] scripts/ grouping map refresh (50→66 files):** enumerate every file under
scripts/ (incl. audit_checks/), classify each into the row's mapped grouping (read the row's
own proposed taxonomy first), flag files that fit no group, produce the refreshed map table +
a delta vs the row's original 50-file map. No moves, no proposals beyond the row's own frame.

ITEM 2 — **[#488] priority axis research leg:** the backlog has no ranking axis. Survey how
comparable systems rank (read the row for its named candidates; add at most 3 from established
practice — e.g. WSJF-class cost-of-delay, simple P-enum + age, dependency-unblocking count —
one paragraph each: what it needs as inputs, what it costs to maintain, whether our generator
could derive it mechanically). End with a 2×[options] table: axis | derivable-mechanically? |
maintenance cost | what it would have ranked #533 vs #486 this week (worked example). NO
recommendation ranked as ruled — one line of architect-facing lean is allowed, labelled LEAN.

OUTPUT: `docs/audits/2026-08-19-technical-c4-ruling-prework.md`. Commit, push, STOP packet
(both items CLEAR/BLOCKED + the LEAN line).
NOT: no file moves, no axis implementation, no BACKLOG writes.
