# Hygiene pre-handoff inventory — full-tree dead/live classification + scripts/ caller-impact map

- **Class:** technical (ADR-101 enum) · **Date:** 2026-07-22 · **Slug:** hygiene-pre-handoff-inventory
- **Lane:** operator-dictated hygiene lane (plan-then-auto, ADR-87); Phase-1 evidence record backing tickets [#397]/[#398]/[#399] and the executed archival/rename commits on `chore/hygiene-pre-handoff`.
- **Method:** 4-agent read-only fan-out (logs+ecosystem · docs statuses · templates+fixtures fleet-grep · scripts callers), every verdict re-verified against file:line evidence before action.

## 1. Verdict roll-up per folder

- **logs/** — ZERO orphans. Every file has a living producer (write-site verified per file); `TOKEN-LOG.md` is the sole git-tracked, hand-authored append-only canon (no code producer BY DESIGN). Executed: [#395] rename + the logs/ naming convention (CLAUDE.md §9 v2.45) — `COHERENCE-NUDGE.log`, `PARITY-EVENTS.jsonl`; `.session-override-token` exempt (dot-token).
- **docs/intake/** — 18 docs; 3 terminal (`status: CONSUMED` ×3, zero REJECTED) archived to `docs/intake/archive/` (first application of the archive-inside-each-folder ruling); 15 live stay. Six off-enum statuses NOT migrated here — decided-not-deployed enum divergence, ticketed as [#398] (see §3).
- **docs/decisions/** — 78 ADR files. Archived to `docs/decisions/archive/`: **ADR-52** (`~~Accepted~~ Superseded by ADR-53`) + **ADR-40** (`Deprecated (was: Accepted)`, zero inbound refs in scripts/tests/protocols/.claude/deploy/ecosystem/plugins). **ADR-45 STAYS** — fails the zero-refs bar (PLAYBOOK.md:3098/:3107/:3134 name-mention prose refs); flagged for a future re-rule. ADR-46/47 stay (partially superseded, retained as convention). Archive-path proof: `scan_undeclared_edges._IMMUTABLE_PREFIXES` covers `docs/decisions/` by prefix (:84); `gen_claude_rosters --check` byte-unchanged (last-5 = 99–103); README ledger rows annotated.
- **docs/runbooks/** — KEPT per ADR-101 §4 d.i (Accepted: KEEP + sanction; "Move docs/runbooks/" an explicitly rejected alternative). The brief's relocate+delete ask conflicts with the Accepted ADR; operator ruled KEEP at the plan gate. Conflict recorded here.
- **ecosystem/** — no dead files; all 10 top-level files consumed by named organs (audit.py, fleet_parity.py, fleet_health.py, changelog_sentinel.py, validate_doc_claims.py, validate_onboarding_rulings.py, probe_child_backlogs.py). Executed: `REGISTRY.md` → `registry.md` (casing defect vs generated `doc-counts.md`; no code reader — fleet_analytics handles index files by structural rule, not name). Report-only: `win-tooling/` contributes zero tracked content (gitignored state.yaml + empty history/); per-repo `history/` series stop at 2026-06-14 (durable records, kept).
- **templates/** — archive-list EMPTY; `templates/archive/` already holds the 4 retired files. `gen_handoff.py` reads v5/epic/functional `.tmpl` from disk (gen_handoff.py:44-46, :360-483) — mechanically LIVE; `claude-regions/` ×8 byte-match-guarded (test_boundary_headers.py:279); floor pair + ruff block + intake template + scrum-master letter all live. Borderlines kept with reasons: v4 handoff set retained-live per ADR-83 (.claude/commands/handoff.md:155-156) + scanned by the residual drift-guard; `JOURNAL/LESSONS-md-template.md` cited by live onboarding intake; `audit-template.md` author-facing. `handoff/v5/README.md.tmpl` → ticketed [#399] (§3).
- **tests/fixtures/** — ALL LIVE (55 files; every mini-repo dir referenced by a named test; `manifest-v1.1.0-pre-essence.yaml` is a deliberate frozen snapshot read by test_essence_spec.py:32; `fixtures/README.md` intentional human doc). Orphan delete-list EMPTY.
- **scripts/** — ~50 files, ZERO orphans; NO moves this lane; structure + caller map filed as [#397] (§2).

## 2. scripts/ descriptive grouping + caller-impact map ([#397] evidence)

Groupings (counts): audit-core/orchestrator 1 (`audit.py`) · standalone pre-commit/commit-msg/pre-push gates 7 (validate_backlog, validate_hermetization, block_ff_push, check_backlog_commit_msg, check_backlog_filing, normalize_headers, coherence_nudge) · audit `ALL_CHECKS` members 14 (canonical_freshness_gate, validate_doc_claims, validate_no_ff, validate_reconciliation, validate_doc_rot, validate_doc_structure, validate_git_backlog, validate_doc_code_edge, safe_remove, scan_undeclared_edges, verify_handoff_probes, validate_residual_completeness, fleet_parity, enforcement_coverage) · freshness-hook-wired generators 4 (gen_methodology_roster, gen_claude_rosters, gen_audit_index, gen_intake_index) · manual-CLI generators 5 (gen_doc_counts, generate_floor, gen_handoff, assemble_paste, seed_runbook) · doc-tooling packages 12 (`codemap/` 6 + codemap_hook, `toc/` 4 + toc_hook) · read-only reporters 4 (boundary_report, boundary_headers, fleet_analytics, fleet_health) · session hooks 5 py + 2 ps1 (session_end_backpressure, hooks/block_immutable_edits, fleet_health, changelog_sentinel, arm_hooks; billing_leak_sentinel.ps1, surface_triage.ps1) · Tier-1 loop 2 (propose_closures, review_closures — runtime copies are the plugin twins, drift-pinned by test_validate_backlog_twin_parity) · scheduler 2 (setup-fleet-scheduler.ps1, fleet-baseline.task.xml).

Highest move-impact (caller sites that break on a move): `audit.py` ~8 (pre-commit audit-health + imports by boundary_report:47, fleet_analytics:111, enforcement_coverage, gen_doc_counts, gen_handoff:187 + manifest) · `validate_reconciliation.py` ~5 (audit + coherence_nudge + coherence_enumerator + scan_undeclared_edges + check-against-spec skill) · `validate_backlog.py` ~5 (pre-commit + 3 path-loaders + plugin twin) · `gen_handoff.py` ~8 (all in .claude/commands/handoff.md). Lowest-impact (0 inbound wiring): fleet_analytics.py (owned by #391), validate_onboarding_rulings.py (test-only, advisory-by-design), gen_doc_counts.py, seed_runbook.py, setup-fleet-scheduler.ps1. Deployed-component constraint: canonical_freshness_gate, session_end_backpressure, block_ff_push/validate_no_ff, check_backlog_commit_msg, codemap_hook/toc_hook are manifest-declared (`deploy/manifest-v1.4.0.yaml`) — a path change ripples into the deploy contract.

## 3. Findings ticketed (filed this lane)

- **[#397]** scripts/ target structure — the §2 grouping + caller map; operator rules adopt/reject; no moves until then.
- **[#398]** intake status-enum reconcile THEN migrate — operator ruling 2026-07-19 (recorded `docs/handoffs/2026-07-20-dev-knowledge-architect/SUPPLEMENT.md:68-70`): ratifying the six off-canon statuses REJECTED; one enforced pattern `SEED→DRAFT→READY→{ACCEPTED|CONSUMED|SUPERSEDED|REJECTED}` + companion fields. The LIVE enum (`docs/intake/README.md` §5: `SEED→DRAFT→READY-FOR-TECHNICAL→CONSUMED|REJECTED`) diverges from the ratified pattern — decided-not-deployed. Migrating the six onto a divergent target would be wrong, hence reconcile-first. The six (status quoted, all L3): `2026-07-11-tech-c4-visualization-memo.md` `input-for-deferred-work` · `2026-07-11-tech-fleet-divergence-register.md` `superseded-pending` · `2026-07-11-tech-ownership-manifest.md` `settled` · `2026-07-11-tech-plan-of-record-fleet-hygiene.md` `plan-of-record-active` · `2026-07-12-siem-requirements-ruled-pack.md` `RULED` · `2026-07-16-satellite-onboarding-prompts.md` `READY-TO-FIRE`.
- **[#399]** `templates/handoff/v5/README.md.tmpl` doc-truth drift — `protocols/HANDOFF_PROCESS.md:481` declares it the source `docs/handoffs/README.md` is idempotently rendered from; NO script reads the `.tmpl` (`seed_runbook.py:85/:106` generalizes from the already-rendered hub README; grep of scripts/ for the tmpl = null). The [#359] phantom-mechanism class: a spec claiming machinery that does not exist.

## 4. Report-only findings (recorded, deliberately not actioned)

- ADR-45 archive candidacy blocked by PLAYBOOK prose refs (§1) — re-rule when convenient.
- `docs/handoffs/README.md` ↔ v5 README.tmpl relation is [#399]'s subject; HANDOFF_PROCESS version-stamp coherence is [#367]'s.
- v4 handoff template set: superseded-but-retained is an ADR-83 decision, not rot; retiring it is a separate ruling.
- `ecosystem/win-tooling/` empty-history anomaly; per-repo history series dormant since 2026-06-14 (append_history still live in audit.py — the series resumes when repo-runs write).
- Off-enum intake statuses stay AS-IS until [#398] step 1 rules the enum; `gen_intake_index`'s OTHER bucket keeps them loud.
