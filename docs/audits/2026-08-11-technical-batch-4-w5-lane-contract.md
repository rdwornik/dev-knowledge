# W5 — lane-e-132-feature: execute [#132] (feature bucket)

## Dispatch
```
claude --worktree lane-e-132-feature --bg --model opus --effort high --permission-mode bypassPermissions "[dk · #132 · feature] Read and execute the frozen contract at $env:CLAUDE_PROMPTS_DIR\W5-LANE-132-FEATURE.md — its step 0 commits the contract of record; run the /lane-boot sequence from step 3 (seed) onward, commit-and-STOP."
```
Operator runs `dispatch W5-LANE-132-FEATURE.md` (new Ctrl+` terminal post-fix). Authority: batch-4 GO `3b711e87` + manifest `70a9ce2a` (merge `1b4abd21`) + the operator's pairwise-parallel re-ruling — [#132] appears in no shared-file pair of the disjointness matrix; any overlap you MEASURE with live lanes W1/W2 is a STOP-and-report, not a workaround.

| Model | Mode | Effort |
|---|---|---|
| opus | execute — this contract IS the plan | high |

## Purpose · row · bucket
`[#132]` — top-3 feature/`[#490]`-class candidate (batch4-prep verdict). **THE ROW IS THE SPEC:** scope comes verbatim from `tasks/132-*.md` + its evidence in the batch4-prep report; this contract only frames execution. Bucket: feature (quota three-way split). Satellites: read-only, local, recorded — never mutated from this lane.

## Read first
`tasks/132-*.md` frontmatter + Done-when verbatim · the batch4-prep report's `[#132]` entry (feature-candidate section) · PLAYBOOK Ch8 · the batch-4 manifest (your W5 row — it reads PENDING-CONTRACT; your step 0 resolves it via the batch-3 amendment-marker precedent, never an in-place edit of the immutable artifact).

## UNDERSTAND (state back before work, quoting sources)
Packet preamble: the row's Done-when quoted · planned footprint vs any predicted footprint on record · one line naming which live lanes (W1, W2) you verified you share zero files with, and how.

## Lane requirements (PLAYBOOK Ch8)
Frozen contract from the tree · V-2 budget stated back in one line · `uv run --locked` on every test invocation · `worktree_import_probe` once · commit per step · JOURNAL entry ON THIS BRANCH ahead of the merge (letters race with live lanes — claim the next free one and note the claim for the integrator) · commit-and-STOP, `git stash list` empty.

## Steps
0. **Contract of record (I-D3):** commit THIS file byte-identical from the prompts dir to the home beside the batch-4 manifest as the FIRST commit on this branch; record the manifest W5 row resolution as an AMENDMENT MARKER (batch-3 precedent), not an edit of the committed manifest. **COMMIT**
1. **Scope statement** per UNDERSTAND (packet preamble).
2..n. **Execute `[#132]` per its own Done-when** — one commit per coherent step; mechanical clauses get tests first; an unmeetable clause = STOP on that clause with evidence, continue the meetable rest (budget item c).
n+1. **Close the row** with the closing-commit metric (the SHA satisfying the Done-when named in the row). **COMMIT**
n+2. **JOURNAL on the branch**, then final: `uv run --locked pytest -q` — expect only the known pre-existing REDs (`[#457]`-documented · sed-absent ×2 · linked-worktree context); anything new is yours until revert-and-rerun proves otherwise. Packet per Ch8; terra review request if any code/script/hook touched (docs-only ⇒ say so, reviewer waived with that line). Commit-and-STOP.

## Decision budget (V-2)
Escalate only: curated-baseline touches · rule-vs-ruling conflicts · unmeetable/ambiguous Done-when clauses · any file overlapping a LIVE lane's footprint. Else decide per contract, report in the packet.

## What NOT to do
No satellite mutations · no births · no self-merge · no scope beyond the row · no `SKIP=`/`--no-verify` · no touching `CLAUDE.md` or `ARCHITECTURE.md` (both are claimed by live/queued lanes — hitting either is a STOP).

Library-first: governed by the row's clauses; any machinery build records the stdlib→dependency→pattern search in the packet first.
