# Birth-row bodies `[#579]`–`[#586]`, verbatim before the D-2 trim

**Class:** technical · **Date:** 2026-08-26 · **Landed by:** the endgame governance session, act 6

> **Why this file exists.** Close-packet debt **D-2**: all eight rows born by lane G on 2026-08-25
> exceeded the declared `backlog-row-length` ceiling of **1320 chars**, producing 8 of the 29
> undispositioned `doc_rot` WARNs. The brief's instruction is that content stays in the row body
> up to the ceiling and *"anything beyond moves to the cited artifact"*. The artifacts those rows
> already cite are **immutable audits** (ADR-101 / CLAUDE.md §5 rule 3) and cannot absorb it, and
> the birth lane's own artifact `2026-08-25-technical-lane-g-governance-spine.md` records the rows
> but does **not** carry their bodies. So the overflow lands here, verbatim and complete, and each
> trimmed row now cites **this** file. **Nothing is deleted — it is relocated.** A reader who wants
> the full reasoning a row was born with reads it here; the row itself keeps the claim, the
> Done-when, and the pointers.

Recorded lengths before the trim, measured with the repo's own scanner
(`validate_doc_rot.scan`, category `backlog-row-length`):

| row | chars before | ceiling |
|---|---:|---:|
| `[#579]` | 2759 | 1320 |
| `[#580]` | 2780 | 1320 |
| `[#581]` | 2532 | 1320 |
| `[#582]` | 3923 | 1320 |
| `[#583]` | 3292 | 1320 |
| `[#584]` | 3292 | 1320 |
| `[#585]` | 3421 | 1320 |
| `[#586]` | 2891 | 1320 |

---

## `[#579]` — full body as born (source: `tasks\579-code-doctrine-fdd-one-adr-merging-intakes-31-and.md`)

- [#579] [P1][L] **Code doctrine & FDD — one ADR merging intakes #31 and #34 (packet ARC-A)** — **Direct birth under the ADR-111 amendment of 2026-08-25; packet row `C22` + `C23`** (`docs/audits/2026-08-25-technical-register-ruling-packet.md` §3 ARC-A: *"C22 ADOPT + C23 ADOPT, merged into one arc"*), pointed at by `protocols/STANDING_RULINGS.md` section U. **This row is the ADOPT's carrier, not its re-trial** — section U is binding and neither C22 nor C23 is reopened here. **The arc is the FDD vehicle:** execute intake **#31** (`ACCEPTED`, code-style doctrine) and ratify intake **#34** (`DRAFT`, architecture enforcement) into **ONE** code-doctrine ADR carrying functional-first style, ruff rule selection, complexity ceilings, an import-linter contract and the mypy decision — then mechanized by the gates that ADR names, never by a second doctrine file. **Two corrections the packet already made, carried so this arc starts from them rather than re-deriving them:** CONTRA-12 fixes the carriers (code-style = intake **31 ACCEPTED**, architecture-enforcement = intake **34 DRAFT**; the earlier `intake-4`/`intake-5` shorthand was the architect's own error E15), and E13/CONTRA-1 correct the #34 artifact's in-repo date to the git-measured **2026-08-13**, so it is unblocked. **Intake #31's own binding constraint carries into this ADR and is not renegotiated here:** its `decided-by` leaves the ruff rule-family list **OPEN** and rules §D binding — *no refactor row is born before a hotspot measurement* — so a family list settled before that measurement is the exact ordering error §D exists to prevent. **The measured evidence the packet ruled on:** `extend-select = []`, no mypy table, and no architecture contract beyond a fixture. · Done when: one ADR reads `Accepted` and carries all five decisions (style, ruff selection, complexity ceiling, import-linter contract, mypy), intake #31 and intake #34 each carry a terminal `status:` with `decided-by`, the §D hotspot measurement is recorded before any refactor row is filed, and every rule the ADR states either names its enforcing gate or is explicitly recorded as judgment-only · refs docs/audits/2026-08-25-technical-register-ruling-packet.md §3 ARC-A, protocols/STANDING_RULINGS.md section U, docs/intake/2026-08-09-func-code-style-doctrine.md, docs/intake/2026-08-16-code-architecture-enforcement.md, ADR-108 §B, ADR-111 · source: packet row C22+C23 (ARC-A), via `protocols/STANDING_RULINGS.md` section U · kill-candidates: none — no open row owns code-level style or architecture-enforcement doctrine; `[#146]` (de-hardcode-first sweep) and `[#153]` (enforcement-completeness pass) CONSUME such an ADR rather than produce it, so killing either would drop a downstream check, not a duplicate

## `[#580]` — full body as born (source: `tasks\580-state-as-data-atomic-id-allocation-and-tasks-as.md`)

- [#580] [P1][M] **State-as-data: atomic id allocation, and `tasks/` as the SOLE source (packet ARC-B)** — **Direct birth under the ADR-111 amendment of 2026-08-25; packet row `C08` + `C09`** (`docs/audits/2026-08-25-technical-register-ruling-packet.md` §3 ARC-B: *"C08 ADOPT (the missing 'SOLE') + C09 ADOPT, one carrier"*), via `protocols/STANDING_RULINGS.md` section U. **Two deliverables, one carrier.** (1) **An id-allocation mechanism** closing the hole ADR-107 named rather than claimed away — `next_free = max(tasks/ id) + 1` is not atomic (`ADR-107:463`, verified live: *"two branches can still each read the same maximum id and write differently-slugged files that git merges cleanly"*) — and the fork between *making `max+1` atomic* and *content-hash ids* (C09's option) is **decided in this row's spec, not here**. (2) **The manifest stops being hand-edited for membership** — the word missing from ADR-107's schema statement is `SOLE`, and filing a row today still means hand-editing `tasks/manifest.json`, which is exactly the second write path a sole-source rule forbids. **Measured, not asserted:** the 777-vs-577 double-bite is recorded in `JOURNAL.md` under *"Two traps this window walked into and out of"* — **the packet's `JOURNAL.md:1719` locator no longer resolves** (that line now carries a `[#578]` sentence; the entry has drifted), which is itself the cite-by-anchor lesson — and the synthetic `[#777]` that defeats the history scan is live in the corpus today. **Why it is P1 despite being invisible to a reader:** it protects every future parallel batch, and duplicate-id detection is post-hoc enforcement, never prevention. **Nothing live owns the hole:** ADR-107 names `[#429]` as its owner, that row is CLOSED and its Done-when is worktree provisioning portability, so this is a birth rather than an OWNED attachment — the packet's own A-6 reasoning, applied. · Done when: allocating an id and filing a row require no hand edit of `tasks/manifest.json`, two concurrent allocations cannot yield the same id (shown by a test that fails against today's `max+1`), the C09 fork is recorded as decided with its reason, and ADR-107's schema statement either carries `SOLE` or is amended to say what it actually means · refs docs/audits/2026-08-25-technical-register-ruling-packet.md §3 ARC-B, protocols/STANDING_RULINGS.md section U, ADR-107, ADR-109, scripts/gen_task_tree.py, tasks/manifest.json, JOURNAL.md, #429, #433 · source: packet row C08+C09 (ARC-B), via `protocols/STANDING_RULINGS.md` section U · kill-candidates: none — no open row carries id allocation; `[#523]` renders an executive index over the GENERATED `BACKLOG.md` and touches no allocation path, so killing it would drop a view, not a duplicate mechanism · serialize-group: architecture

## `[#581]` — full body as born (source: `tasks\581-backlog-vitals-three-flow-instruments-a-committe.md`)

- [#581] [P1][L] **Backlog vitals — three flow instruments, a committed digest, and what-is-unblocked-now (packet ARC-C)** — **Direct birth under the ADR-111 amendment of 2026-08-25; packet row `C10` + `C11` + `C13`** (`docs/audits/2026-08-25-technical-register-ruling-packet.md` §3 ARC-C: *"C10 ADOPT + C11 ADOPT + C13 ADOPT, one carrier ('backlog vitals')"*), via `protocols/STANDING_RULINGS.md` section U. **Three parts, one carrier.** (1) **Three instruments** — age-of-open, aging outliers, and throughput-per-window measured against **the R2-ruled denominator**, not a re-invented one. (2) **A committed digest** carrying the ledger line, the next five, the flow vitals and what changed since the last window. (3) **What-is-unblocked-now**, derived from the `depends-on` edges that already exist rather than from a new schema field. **The packet's scoping, carried so it is not re-argued:** C10's downgrade from `independent` to `practitioner` evidence changes the EVIDENCE LABEL, not the priority — the practitioner here is the operator, and this is his most-repeated ask; and **the delivery-telemetry research (R-J: DORA/SPACE plus AI code-attribution) feeds v2 of this arc — v1 does not wait for it.** **A named consumer rather than a hypothetical one:** the packet records `docs/archive/2026-08-09-research-multi-provider-portability-wf-d68b2f7f.md` as already carrying a per-tool matrix that was unremembered when R-L was commissioned — the window's third owned-unconsumed incident, and an unconsumed-findings count is the instrument that would have surfaced it. · Done when: the three instruments are COMPUTED from `tasks/` by a generator and never typed, the digest is a committed artifact regenerated per window behind a freshness gate, what-is-unblocked-now is derived from live `depends-on` edges with no new schema field, and the R2 denominator is cited by locator in the code that uses it · refs docs/audits/2026-08-25-technical-register-ruling-packet.md §3 ARC-C, protocols/STANDING_RULINGS.md section U, docs/audits/2026-08-25-technical-research-delivery-telemetry-attribution.md, docs/archive/2026-08-09-research-multi-provider-portability-wf-d68b2f7f.md, ADR-105, ADR-111 · source: packet row C10+C11+C13 (ARC-C), via `protocols/STANDING_RULINGS.md` section U · kill-candidates: none — `[#523]` renders an executive index over the generated `BACKLOG.md` and computes no flow metric, and `[#540]` fetches batch packets while measuring no age or throughput; neither absorbs the vitals · serialize-group: audit-py

## `[#582]` — full body as born (source: `tasks\582-substrate-router-one-gated-enum-a-capability-key.md`)

- [#582] [P1][L] **Substrate router — one gated enum, a capability-keyed table, and the generator that reads it (packet ARC-D)** — **Direct birth under the ADR-111 amendment of 2026-08-25; packet row `C30` + `C31` + `C32`** (`docs/audits/2026-08-25-technical-register-ruling-packet.md` §3 ARC-D: *"C30 ADOPT via intake 45 (READY) + C32 ADOPT (doctrine line) + C31 ADOPT (generator)"*), via `protocols/STANDING_RULINGS.md` section U. **CONTRA-6 rules the vocabulary and this row inherits it rather than re-deciding it:** the **gated enum wins as the surface** — `SHAPE_ENUM = ("local", "cloud", "interactive")` at `scripts/gen_lane_contract.py:104` is live and hook-enforced (verified 2026-08-25) — and the three-substrate vocabulary stays DESCRIPTIVE until intake **#45**'s ADR extends the enum **in ONE act**: generator, hook and table together. **No parallel vocabulary lands.** **The operator mandate is binding input, not a proposal:** batches shrink to 4–6 lanes; GitHub compute is the DEFAULT substrate; gate-dependent lanes route to the Codespaces devcontainer; read-only fan-out routes to CC-cloud containers where gates are NOT armed and gate-dependent work therefore never goes; local is reserved for operator-gated acts and vendor-CLI-on-disk work; **sequential-local as a default is retired.** **Second ruled criterion — provider-agnosticism:** GitHub compute is the only substrate where non-Anthropic producers can execute against the same armed gates, so routing toward it is a universalization act and not only a speed one. **The open learning the ADR must carry, stated as owed rather than assumed:** the cost/time/loop column per substrate — time is MEASURED (commit ~7× cheaper, suite ~4× faster on Codespace), **cost and feedback-loop latency are NOT** (Codespaces billing/quota to be verified, never assumed) — and the ADR routes on measured numbers only. C32's two-leg reviewer reason (binary absent AND auth absent) moves out of immutable audit prose into the table, closing the "someone adds an install line" hole; C31's capability file is generated from **three** sources (module exports, PATH scripts, `.claude/commands|skills`) and carried into the bundle. **D-vis rides this arc as an operator-priority sub-deliverable:** a GENERATED per-provider status block in the landed Tier-1 section — CLI present, auth alive, root path as a clickable link, role verdict, last-verified date with the probing command — machine-derived from `ecosystem/provider-registry.yaml` plus live probes, freshness-gated, covering all six admitted providers. **Symlinks are ruled OUT for it on measurement:** on Windows checkouts git materializes them as plain text files (`core.symlinks=false` default) and creating real ones needs elevated rights, so the visibility would break silently per clone. · Done when: one ADR extends `SHAPE_ENUM` with generator, hook and routing table moving in a single commit, `gen_lane_contract` derives substrate and command from the capability-keyed table rather than from prose, the four capability keys are populated from the three declared sources, the per-provider status block is generated and freshness-gated across all six providers, and every routing number the ADR cites is measured with its command recorded · refs docs/audits/2026-08-25-technical-register-ruling-packet.md §3 ARC-D and its ARC-D addendum, protocols/STANDING_RULINGS.md section U, docs/intake/2026-08-24-tech-substrate-router.md, scripts/gen_lane_contract.py, ecosystem/provider-registry.yaml, ARCHITECTURE.md Ch3, #554, #568 · source: packet row C30+C31+C32 plus the D-vis addendum (ARC-D), via `protocols/STANDING_RULINGS.md` section U · kill-candidates: none — `[#554]` built the devcontainer this arc ROUTES to and `[#568]` owns the provider config-as-code that D-vis READS; both are inputs to the router, so killing either would remove a dependency rather than a duplicate · serialize-group: architecture

## `[#583]` — full body as born (source: `tasks\583-green-by-skip-sweep-a-check-that-cannot-obtain-g.md`)

- [#583] [P2][M] **Green-by-skip sweep — a check that cannot obtain ground truth must not report OK (packet ARC-E, C18)** — **Direct birth under the ADR-111 amendment of 2026-08-25; packet row `C18`** (`docs/audits/2026-08-25-technical-register-ruling-packet.md` §3 ARC-E: *"C18 ADOPT (extend `fire_test` to the 21 local pre-commit gates + the substrate-inertness class)"*), via `protocols/STANDING_RULINGS.md` section U — the ADOPT's **skip half**: C18 asks whether a gate FIRES, and a check that silently declines to evaluate is the same defect measured from the other side. **The seed instance is section U's own defect note, re-measured in this lane on 2026-08-25:** `uv run --locked python scripts/validate_doc_claims.py` prints `OK — 4 claim(s) checked, no prose drift` while its first leg reads `skipped audit_check_count (doc - / actual <ground truth unavailable>)`. **The mechanism is recorded precisely, because a defect note that misstates its own mechanism is worse than none:** the `skipped` status is NOT caused by the `UnicodeEncodeError` that `cmd_checks` raises under a cp1252 console (a `→` through `click.echo`; `PYTHONUTF8=1` is the measured workaround) — it is the GAP-1 cycle-break design, where the standalone CLI passes `None` for the injected `len(ALL_CHECKS)` and only `audit health` / `audit run` supply it. The two compound rather than cause one another, and the net is identical: **no surface fails when that number drifts.** Latent rather than active — `ecosystem/doc-counts.md` and live `len(audit.ALL_CHECKS)` both read 46 — and `audit.py health` is unaffected, so the pre-commit gate is intact. **Scope is the CLASS, not the instance:** sweep every check, validator and gate leg that can report a non-failing "could not evaluate" status, and rule for each whether it fails loud, is removed, or becomes a declared and justified skip. **The sweep's artifact is contract-frozen in two lanes as `docs/audits/2026-08-25-green-by-skip-sweep.md`** and this row consumes it rather than re-authoring it — **but measured 2026-08-25, that exact path is REFUSED by the live `validate-hermetization` Rule B gate** (`classify()`: *"has no CLOSED-enum <class> token after the date"*), so it can only land as `docs/audits/2026-08-25-technical-green-by-skip-sweep.md`; whichever name the integrator rules, it is the same artifact and this row consumes it. · Done when: the frozen sweep artifact is consumed row by row, `validate_doc_claims` no longer prints OK while a leg reports `<ground truth unavailable>`, the `cmd_checks` cp1252 crash is fixed fail-loud rather than worked around in prose, and every remaining silent-skip site either fails loud or carries a declared reason · refs docs/audits/2026-08-25-technical-register-ruling-packet.md §3 ARC-E, protocols/STANDING_RULINGS.md section U ("Defect noted (2026-08-25 …)"), docs/audits/2026-08-25-technical-green-by-skip-sweep.md, scripts/validate_doc_claims.py, scripts/audit.py, scripts/validate_hermetization.py, ecosystem/doc-counts.md · source: packet row C18 (ARC-E), via `protocols/STANDING_RULINGS.md` section U · kill-candidates: none — `[#153]` asks which RULES lack an organ, while this row asks whether a LIVE organ silently declines to evaluate; no open row poses the second question · serialize-group: gates

## `[#584]` — full body as born (source: `tasks\584-claude-md-10-in-lockstep-with-its-form-a-templat.md`)

- [#584] [P2][M] **`CLAUDE.md` §10 in lockstep with its Form-A template, plus the §6/§9 procedure extraction (packet ARC-G, C02)** — **Direct birth under the ADR-111 amendment of 2026-08-25; packet row `C02`** (`docs/audits/2026-08-25-technical-register-ruling-packet.md` §3 ARC-G: *"C02 ADOPT-scoped (headroom is 5, not 2-3 — keep extracting procedure; §6/§9 named)"*), via `protocols/STANDING_RULINGS.md` section U. **Two halves, and the row is not discharged by either alone.** **(a) The lockstep act.** `CLAUDE.md` §10's first anti-pattern — *"Narrating or managing AGENTS.md — AGENTS.md is retired (ADR-53)"* — is false doctrine after register ruling R-1 admitted the file, and it has now been knowingly left standing across **three** consecutive versions (v2.64, v2.65, v2.66), each recording the same reason: §10 is a HUB-single-sourced Form-A region whose body must stay **byte-identical** to `templates/claude-regions/antipatterns-universal.md`, so correcting §10 alone breaks fleet parity while correcting the template was outside every prior lane's contract. **This row owns BOTH sites moving in ONE commit** — the act neither `[#577]` nor the v2.65/v2.66 lanes were scoped to take, and the reason intake #48 records `[#577]`'s Done-when as unexecutable as written. **(b) The C02 substance.** Keep extracting procedure out of `CLAUDE.md`, with **§6 (session-start protocol) and §9 (hooks roster) named by the packet as the targets**, on the corrected measurement that headroom is **5**, not the 2–3 the source register row assumed. **Two constraints the extraction must respect, both already recorded and neither re-argued here:** §9's pre-commit roster IS the surface `validate_doc_claims`'s `precommit_hook_roster` leg checks against `.pre-commit-config.yaml`, one line per hook by construction, so collapsing it DISARMS a gate — the v2.66 lane refused to convert it for exactly that reason; and §6 is itself a hub-single-sourced Form-A region (`session-start-protocol`), so anything extracted from it moves in template lockstep too, which is the discipline half (a) exists to establish, while §9 is repo-owned and moves alone. · Done when: `CLAUDE.md` §10 and `templates/claude-regions/antipatterns-universal.md` land the AGENTS.md correction in a single commit with fleet parity intact, procedure is extracted from §6 and §9 into a named durable home, `validate_doc_claims`'s `precommit_hook_roster` leg still resolves against a live surface afterwards, and the file's budget is reported before and after using `validate_doc_rot.scan_file_budget` rather than a typed number · refs docs/audits/2026-08-25-technical-register-ruling-packet.md §3 ARC-G, protocols/STANDING_RULINGS.md section U and R-1, CLAUDE.md §6 §9 §10, templates/claude-regions/antipatterns-universal.md, docs/audits/2026-08-23-technical-lane-docs-actual-state.md, docs/intake/2026-08-24-tech-agents-md-admission-vs-adr53.md, #577, #427 · source: packet row C02 (ARC-G), via `protocols/STANDING_RULINGS.md` section U · kill-candidates: none — `[#577]` owns the portable-instruction FILE and its own Done-when names the §10 correction it cannot execute, and `[#427]` owns the region templates' position-dependent PATH rather than their content; this row owns the two-site act itself · serialize-group: claude-md

## `[#585]` — full body as born (source: `tasks\585-suite-red-test-anchor-gate-probe-distinguishes-i.md`)

- [#585] [P2][S] **Suite RED — `test_anchor_gate_probe_distinguishes_installed_from_absent` does not discriminate: repair it, retire it, or fix the organ** — **Direct birth under the ADR-111 amendment of 2026-08-25; packet row `C18`** (`docs/audits/2026-08-25-technical-register-ruling-packet.md` §3 ARC-E: *"C18 ADOPT (extend `fire_test` to the 21 local pre-commit gates + the substrate-inertness class)"*), via `protocols/STANDING_RULINGS.md` section U — the ADOPT's **constant-refusal half**, the sibling of `[#583]`'s skip half. **The failure is INHERITED and is re-measured here rather than carried:** run 2026-08-25 against `origin/main` at `436e7375` in a clean lane worktree, `tests/test_enforcement_coverage.py:221` fails `assert 'absent' == 'enforcing-local'` with *"pre-push organ REFUSED an anchored push too (exit 1) — it does not discriminate; a constant refusal enforces nothing"*. **Standing at least since 2026-08-20**, where `docs/audits/2026-08-20-technical-codespaces-audit.md` already records it FAIL on both platforms and *pre-existing* — so the widely-copied "pre-existing since 2026-08-22" is itself carried rather than measured. **Why a probe that always refuses is worse than no probe:** it is green-looking enforcement evidence for an organ whose discrimination was never demonstrated. That is why this is a disposition-INVESTIGATION and not a fix-it — the honest outcomes are (a) the probe is repaired so it distinguishes installed from absent, (b) the probe is retired with the reason recorded and `block_unanchored_push`'s enforcement claim narrowed to what is actually demonstrated, or (c) the ORGAN is the defect and the hook is what changes. **The row assigns an owner to that choice**, which is what has been missing: the failure has been labelled *pre-existing* in lane report after lane report since 2026-08-20 without anyone being obliged to decide. **This row NARROWS `[#569]` rather than duplicating it:** `[#569]` item (A) filed both standing REDs grouped, and its sibling `test_routine_consumers_live_backlog_governs_exactly_one_row` was discharged 2026-08-22 at `353149ab` (verified live), leaving this half as the group's only live suite item while `[#569]`'s remaining weight is its 19-finding PLAYBOOK census. · Done when: `test_anchor_gate_probe_distinguishes_installed_from_absent` is green, or it is retired with the reason recorded and `block_unanchored_push`'s demonstrated enforcement scope restated wherever it is claimed, and `[#569]` is re-scoped to the PLAYBOOK census with this row named as item (A)'s carrier · refs tests/test_enforcement_coverage.py, scripts/block_unanchored_push.py, docs/audits/2026-08-25-technical-register-ruling-packet.md §3 ARC-E, protocols/STANDING_RULINGS.md section U, docs/audits/2026-08-20-technical-codespaces-audit.md, docs/audits/2026-08-22-technical-cloud-wave-close-funnel.md, ADR-85, #583, #569 · source: packet row C18 (ARC-E), via `protocols/STANDING_RULINGS.md` section U · kill-candidates: none — `[#569]` was this row's kill-candidate and was **CONSUMED 2026-08-26**: the operator accepted this row's narrowing and CLOSED `[#569]` (register `protocols/STANDING_RULINGS.md` section W, ruling W5). A spent id may not sit in a kill-candidates VALUE, which asserts the named row is open, so it now sits in refs. The closure did NOT discharge `[#569]`'s 19-finding PLAYBOOK census and no open row owns it · serialize-group: gates

## `[#586]` — full body as born (source: `tasks\586-suite-red-test-no-gate-hook-or-script-reads-the.md`)

- [#586] [P3][S] **Suite RED — `test_no_gate_hook_or_script_reads_the_export` has no `ecosystem/` naming-vs-reading carve-out (packet ARC-G, C04)** — **Direct birth under the ADR-111 amendment of 2026-08-25; packet row `C04`** (`docs/audits/2026-08-25-technical-register-ruling-packet.md` §3 ARC-G: *"C04 COVERED-BY the normative-home definition + one doctrine sentence: prose a gate READS is a fixture and stays; prose a gate merely ENFORCES is deletable"*), via `protocols/STANDING_RULINGS.md` section U — this row is that same naming-versus-reading distinction applied to a TEST PREDICATE instead of to prose. **Re-measured in this lane 2026-08-25** against `origin/main` at `436e7375`: `tests/test_export_backlog_view.py:521` fails with *"the export is read by governance: ecosystem/conformance.html references 'export_backlog_view'; ecosystem/conformance.md references 'export_backlog_view'"*. **The assertion is right and the finding is false.** The view-layer row's binding condition 3 — *governance stays bespoke; no gate, hook or script is re-pointed at the export* — is not breached: the two `ecosystem/` files NAME the exporter inside a rendered row's text, they do not READ its output. The test's `docs/` scan already carries exactly this carve-out and `ecosystem/` does not, so regenerating the conformance dashboard REDs the suite by construction. **The view-layer row is CLOSED and cannot carry the fix** — the packet applies the identical reasoning to C17 and `[#530]` (A-6) — so the ADR-111 OWNED outcome was tested first and does not apply, which is why this is a birth. **L4 residual, and it is filed as one:** the generalizable half is *a surface that NAMES an organ is not a surface that CONSUMES it*, and `protocols/STANDING_RULINGS.md` N-2 already records the same collision from the other direction, where a conforming `site:` declaration cannot even be written because the predicate file would then contain the module's own name. · Done when: `test_no_gate_hook_or_script_reads_the_export` is green with an `ecosystem/` carve-out distinguishing naming from reading (or with a recorded reason for refusing one), regenerating `ecosystem/conformance.html` and `ecosystem/conformance.md` no longer REDs the suite, and the naming-vs-reading rule is stated once in a durable home rather than duplicated per scan · refs tests/test_export_backlog_view.py, scripts/export_backlog_view.py, ecosystem/conformance.md, docs/audits/2026-08-23-technical-lane-dashboard-commit-path.md, docs/audits/2026-08-25-technical-register-ruling-packet.md §3 ARC-G, protocols/STANDING_RULINGS.md section U and N-2, ADR-113, #563 · source: packet row C04 (ARC-G), via `protocols/STANDING_RULINGS.md` section U · kill-candidates: none — the closed view-layer row cannot carry a fix, and `[#171]` builds the conformance dashboard whose regeneration triggers the RED while owning no test predicate

