# ARCH-ORGAN — ARCHITECTURE Ch2/Ch6 organ rows (spare-capacity lane)

## Dispatch
```
claude --bg --model sonnet --effort medium --worktree lane-<LETTER>-<ID>-arch-organ-rows --permission-mode bypassPermissions "[dk · #<ID> · arch-organ-rows] Read and execute the frozen contract at $env:CLAUDE_PROMPTS_DIR\ARCH-ORGAN-rows.md — step 0 commits the contract of record; run the /lane-boot sequence from step 3 onward, commit-and-STOP."
```
**Before pasting:** fill `<ID>` and `<LETTER>` from the cockpit pre-flight (one paste into the PRIMARY session):
```
Print: (1) the BACKLOG row id(s) for the ARCHITECTURE Ch2/Ch6 organ rows named in the 2026-08-12 supplement spare-capacity list, quoting each row's title line; (2) the next free lane letter per the manifest. Read-only — change nothing.
```
If the cockpit finds **no such rows**, this lane does not dispatch — report back instead; nothing is birthed.

## Repo + Purpose
`.dev-knowledge`. Purpose: execute the owed ARCHITECTURE Ch2/Ch6 organ rows — the organ-documentation debt recorded when the organ-index relocated to `ecosystem/` — exactly to those rows' Done-when. Row-is-the-spec; this contract adds no scope.

## Why this lane is legal beside W3 AND CORPUS-492 (stated, not assumed)
W3's NOT-list explicitly bars `ARCHITECTURE.md`; CORPUS-492 touches only the corpus artifact. This lane touches ONLY `ARCHITECTURE.md` Ch2/Ch6 + its own row bodies. Three-way intersection: empty. Hard stop: if either governing row's Done-when requires ANY file named in W3's or CORPUS-492's contract, STOP and batch the question — no entry into overlap.

## Steps
1. **Step 0 self-serve:** commit this contract (I-D3), manifest amendment marker, confirm letter. → COMMIT
2. Execute the row(s) per their Done-when: Ch2/Ch6 organ sections written **declaratively** (ratchet ≤ 441 verified after every prose landing), claims sourced from the live tree only — every organ statement carries a locator; nothing asserted from memory. → COMMIT per row
3. ARCHITECTURE frontmatter `last_reviewed` stamped per P5 convention. → COMMIT
4. Row flips + regenerate. → COMMIT

## Lane discipline
V-2 decision budget stated back in one line · JOURNAL on the lane branch · commit-and-STOP, never self-merge · questions batched · no births · no edits outside `ARCHITECTURE.md` + own row bodies + manifest marker · no `CLAUDE.md` edit · expected-REDs: none (docs lane) — any suite change is a defect, report it.

## Review
**Terra: waived — docs-only lane, no code impact** (one-line waiver; the overlap STOP above fires before any `scripts/` touch could occur).

## Done-when (frozen)
(1) Each governing row's Done-when satisfied as written; (2) every new organ claim carries a live locator; (3) ratchet ≤ 441; (4) stamp current; (5) lane packet: shas · rows closed/advanced · decision-budget report · confirmation no W3/CORPUS-492-named file touched.
