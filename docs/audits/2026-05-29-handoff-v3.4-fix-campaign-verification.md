# Handoff v3.4 Fix Campaign — Verification + Hard-Metric Simulation (2026-05-29)

<!-- scope: meta -->

| Field | Value |
|---|---|
| Date | 2026-05-29 |
| Campaign | Path C — all 13 findings from the 2026-05-29 handoff v3.4 process audit |
| Source audit | `docs/audits/2026-05-29-handoff-v3.4-process-audit.md` |
| Branch | `fix/handoff-v3.4-complete-campaign-2026-05-29` |
| Commits | 8 phase commits (`b4afff3` … `4e3cc3e`) off `main` tip `2327e27` |
| Verdict | **13/13 closed; hard-metric simulation PASS; handoff process retry-ready** |

---

## 1. Context

The 2026-05-29 process audit found 13 distinct bugs (2 critical, 4 high, 6 medium,
1 low) in the v3.4 handoff plumbing after the first real v3.4 run was aborted at
Stage 3 readiness. The browser-side default was a minimum-viable retry (fix only
B1+B2); the operator chose **Path C** — fix all 13 for stable-v3.4. This document
verifies that campaign and runs the closure check the audit demanded: a hard-metric
simulation that a fresh Stage 1 generation would now request claims + scope +
gate-probe review correctly.

ADR-39 immutability was respected throughout: ADR-42, ADR-45, ADR-55/56/57/58 were
corrected by **appended amendments only**, never in-place body edits. The spec, the
skill, and both templates are not immutable and were edited directly.

## 2. Per-finding closure table

| Finding | Sev | Dim | Closing commit | Files touched | Verification |
|---|---|---|---|---|---|
| **B1** | critical | B | `b4afff3` | `templates/HANDOFF_QUESTION_TEMPLATE.md` | §4 simulation PASS — claims/scope/probe now requested in Section B |
| **B2** | critical | C | `b4afff3` | `templates/HANDOFF_QUESTION_TEMPLATE.md` | §4 simulation PASS — audience-routing invariant added; requests in paste block, not Section A |
| **E3** | medium | E | `b4afff3` | `templates/HANDOFF_QUESTION_TEMPLATE.md` | "twice amended" → "amended four times" (matches ADR-42:5) |
| **A1** | high | A | `e2f85f4` | `.claude/commands/handoff.md` | version → v3.4; refs to 10/11/12, scope, Prompt Gen Card, ratification added |
| **A2** | medium | A | `e2f85f4` | `.claude/commands/handoff.md` | content-aware state detection + M-5/ancestry/claims/UNVERIFIED gates |
| **F1** | high | F | `581c3cb` | `protocols/HANDOFF_PROCESS.md` | folder-structure + file-responsibilities sections now include 10/11/12 |
| **F2** | medium | F | `581c3cb` | `protocols/HANDOFF_PROCESS.md` | count reconciled to 13/14; json sidecar clarified (resolves off-by-one) |
| **D2** | medium | D | `581c3cb` | `docs/decisions/ADR-42-handoff-format-v3.md` | append-only Q5 file-count addendum (supersedes body "11 files") |
| **D1** | high | D/A | `b696474` | `templates/HANDOFF_FOLDER_TEMPLATE.md` | header + body version strings normalized to v3.4 / ADR-42-Q5 |
| **E1** | high | E | `256e26b` + `a86c18d` | `HANDOFF_PROCESS.md` + ADR-55/56/57/58 | spec direct-edit to `docs/archive/`; 4 ADR append-only amendments |
| **F3** | medium | F | `60df5b6` | `protocols/HANDOFF_PROCESS.md` | synthesis prompt: `00_first-message.md` canonical, `01_MANIFEST.md` echoes |
| **F4** | low | F | `4e3cc3e` | `protocols/HANDOFF_PROCESS.md` | `{date}-{slug}` → `{slug}` (date double-count removed) |
| **E2** | medium | E | `4e3cc3e` | `docs/decisions/ADR-45-handoff-architecture-v4.md` | append-only current-authority pointer (v3.4 / ADR-42-Q5) |

**13 of 13 findings closed.**

## 3. Cross-check results (8a — grep-verified)

- **v3.3.3 stragglers in live docs:** none. All `v3.3.3` occurrences are in the
  spec's `Supersedes:` chain (`HANDOFF_PROCESS.md:8`), the version-history "What
  changed" comparison tables (:683/:690/:705/:708/:832), the immutable ADR-45 body,
  or the ADR-45 amendment that explicitly labels those strings historical. The spec
  header is `# HANDOFF_PROCESS v3.4`.
- **Canonical file count:** **13 fixed content files (self) / 14 (cross-repo)** is
  stated consistently across all live surfaces — `HANDOFF_PROCESS.md` :380, :445,
  :657, :830 and `.claude/commands/handoff.md:60`. ADR-42's body counts ("11/12
  files", "~11 files") are immutable v3.0 historical content and are explicitly
  superseded by the appended 2026-05-29 addendum.
- **Evidence-file path:** **zero live stale references** remain in `protocols/`. The
  only `docs/research/…` / `docs/council-questions/…` matches are (a) the immutable
  ADR-55/56/57/58 body citations, preserved for historical accuracy per ADR-39, and
  (b) the four 2026-05-29 amendment notes, which quote the old path while pointing to
  the canonical `docs/archive/2026-05-25-handoff-failures-evidence.md`. The 5 Council
  transcript refs were left unchanged by policy (immutable point-in-time records).

## 4. Hard-metric simulation (8b — the closure check)

**Method:** read the updated `templates/HANDOFF_QUESTION_TEMPLATE.md` as if I were
the architect receiving ONLY the paste block (Section B), and confirm every Stage 3
requirement is requested there.

The paste block runs from `PASTE_BOUNDARY` (line 71) to "End of paste block" (line
432). Within it, after the 5 pipeline sections (OBJECTIVE → BOUNDARIES), a new
**"Additional required Stage 2 outputs (v3.4)"** section (line 362) requests:

| Required Stage 3 input | Requested in paste block? | Location | Audience |
|---|---|---|---|
| 5 pipeline sections | yes (pre-existing) | `### 1.`–`### 5.` | Section B ✓ |
| `next_session_scope` (ADR-57) | **yes** | §A, line 368 — full controlled vocabulary listed inline | Section B ✓ |
| `11_CLAIMS.md` content (ADR-58) | **yes** | §B, line 384 — claims/assumptions/expected-articulation table schema inline | Section B ✓ |
| Gate-probe accuracy review (ADR-55) | **yes** | §C, line 419 — review readiness explained | Section B ✓ |

All four are inside Section B. None is operator-only. The closing divider (line 437)
restates the three v3.4 outputs. The generation rules (line 456) add an
audience-routing invariant forbidding any required architect artifact from living
only in Section A, and a rule to pre-create the `stage2-claims.md` save target.

**Result: PASS.** A fresh Stage 1 generation from this template would request
scope + claims (with schema) + gate-probe review from the architect, in the
architect-facing block. The exact B1+B2 failure that aborted the first v3.4 run is
closed.

## 5. Deferred items

**None.** All 13 findings were closed in this campaign (Path C). No finding was
deferred, partially fixed, or carried to BACKLOG as open.

The 5 Council transcript evidence-path references were deliberately **left
unchanged** (not deferred): per the audit's E1 policy and ADR-39, annotating
immutable point-in-time transcripts would undermine their immutability semantics.
This is a closed decision, not an open item.

## 6. Retry-readiness statement

**The handoff process is retry-ready.**

- The audit's retry gate (items 1–2: B1+B2) is closed and simulation-verified (§4).
- The "before declaring v3.4 stable" gate (items 3–5: F1+F2+D2, D1, E1) is closed (§2).
- The remaining medium/low findings (F3, F4, E2) are closed.
- Baseline health held throughout: `pytest` 90 passed, `ruff` clean, `audit.py
  health` 7/7 after every phase commit.

A v3.4 retry can be initiated in a new session after this branch merges. Stage 1
generation will now produce a complete architect-facing question.
