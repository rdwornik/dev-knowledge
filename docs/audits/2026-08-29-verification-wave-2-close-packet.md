# NIGHT-MISSION 2026-08-28/29 — WAVE-2 CLOSE PACKET

**Authorization.** The operator's **WAVE-2 GO** (2026-08-29), the **five-item addendum**, the
**reconcile-before-birth correction**, and the **six W2-C integration adjudications**. Wave 1
closed under its own packet (`docs/audits/2026-08-29-verification-night-mission-close-packet.md`);
this file reports wave 2 and everything ruled after it.

---

## 1 · FM-WAVE2's OWN Ex-ante lines, verbatim, then measured

| lane | Ex-ante, **verbatim from the frozen bundle** | measured | verdict |
|---|---|---|---|
| **FM-C** (wave 1) | *100 % classified, every claim locatored; a census routing most objects to ORPHAN without evidence has not censused* | 951 objects, **100 % classified, UNCLASSIFIED 0**; ORPHAN 11 = **1.2 %**, not the plurality. It named the honest inversion itself: PROTECTED is 94.1 % | **MET** |
| **FM-1** (H) | *two seats could run one funnel pass from this text alone and produce identical transitions* | `protocols/FUNNEL_LIFECYCLE.md`, **458 lines**, five terra rounds, terminal states given an explicit no-outgoing-edge rule. **Ratchet 443 → 443** (scope files 61 → 62) | **MET** |
| **FM-2** (I) | *RED-first against live main reproducing FM-C's findings … plus a seeded-violation test that FAILs then passes* | **both REDs delivered** — live main on **2 real subjects**, plus a seeded neutering of leg a1. Legs a1/a2/b/c MET; **leg d NOT-MET in-lane** | **MET** |
| **FM-3** (J) | *docs/intake and docs/decisions counts DROP … every relocation md5-proven; skipped items listed* | `docs/intake` **56 → 54**, archive **8 → 10**, md5-proven byte-identical. `docs/decisions` **89 → 89 by retention authority**, stated rather than left looking like a miss | **MET** |
| **FM-4** (K) | *the next assembled bundle carries the block; a golden test pins its shape* | block emitted in `architect`/`execution`/`epic`, **not** `functional`; golden test pins delimiters, heading and six fields; anti-bluff test refuses verdicts and SHAs; regenerated-not-carried has a seeded witness | **5 MET / 2 PARTIAL** |
| **FM-5** (L) | *the command runs on merged main and its numbers equal FM-4's block byte-for-byte for the shared fields* | **NOT-MET, and still NOT-MET post-merge** — §4. The lane's own title: *"the Ex-ante ships RED, and why that is the proof"* | **NOT-MET, by construction** |
| **FPG-1** (M) | *`why` answers correctly on 3 named governed files (witnessed transcripts) and FAILs on a planted unknown* | four witnessed transcripts pasted, three governed files of different kinds, **`why` FAILs on the planted unknown**. `uv.lock` pins `rustworkx-0.18.1-cp310-abi3-win_amd64.whl` by sha256 | **MET** |
| **DB-1** (N) | *DIRECTION visible in one glance; zero colored-table markdown; regenerable from the store with one command* | all three **MET** — per-panel direction verdict, no markdown table and no HTML `<table>`, two consecutive real runs **byte-identical** | **MET** |
| **A4-AGY** (O) | *a computed-gates cell (agy, analysis) exists with every SDA-1 discipline token present; the whole-repo-scan item's ground truth cross-checks at least one FM-C finding* | 8 gates computed with each failing leg named; 6 SDA-1 tokens each with its discharging site. **Cross-check: NOT MET by that item, MET by the pack** — the lane refused to round it up | **MET / the item NOT-MET** |

## 2 · The mission's W2-1..W2-6

| id | ex-ante, verbatim | measured | verdict |
|---|---|---|---|
| **W2-1** | *check_funnel_lifecycle: RED on a seeded violation, GREEN on main after FM-3* | seeded RED **delivered**; after FM-3 merged, the two live FAILs **cleared themselves** — the check now returns **one WARN, no FAIL** | **MET** |
| **W2-2** | *docs/intake + docs/decisions file counts DROP; exact numbers in the packet* | intake **56 → 54**; decisions **89 → 89**, unmovable by ADR-100 + ADR immutability, which FM-C had already proven | **MET on intake; decisions structurally exempt** |
| **W2-3** | *the next bundle carries the generated FUNNEL HEALTH block* | emitted and golden-tested; **end-to-end assembly MEASUREMENT-OWED** — `gen_handoff` refuses while worktrees are live | **MET at the testable level** |
| **W2-4** | *`why <path>` answers purpose+consumers+edges for governed surfaces, FAILs on unknown* | witnessed on 3 governed files; FAILs on a planted unknown | **MET** |
| **W2-5** | *A4: agy gate computation exists against the named incumbent baseline … no vibes verdict* | computed-gates cell exists; **no vibes verdict anywhere** — every gate names its failing leg | **MET** |
| **W2-6** | *operator process questions consumed tonight+morning: 0* | **0** questions asked. Two forks were handed over as one-line decisions rather than queries (§5) | **MET** |

---

## 3 · THE FIVE RULED ITEMS — item → funnel home (id) → act taken

Executed under the **reconcile-before-birth correction**: map first, amend on the object that
exists, birth only where the ledger shows a true gap. **The mapping changed two of the five.**

| # | ruling | funnel home | act | gap named |
|---|---|---|---|---|
| 1 | verify my own cp fix, attended | **none — an act, not a finding** | **DONE**, §4 | — |
| 2 | VISION superseded by a recreated root README | **ADR-114** (was PARKED) | **RULED Accepted**; status line in place (ADR-94 permits that and only that), decision by in-file AMENDMENT 1 | + **[#614]** — no row and no intake carried the *execution* arc; the only object was an **off-repo phase-0 CANDIDATE**, and ADR-111's path from CANDIDATE is not "stay there" |
| 3a | *"no single-file folders, ever"* | **`STANDING_RULINGS.md` Z-G5** | **AMENDED, verbatim.** In `EXCLUDED_RELPATHS`, so the ruling cost the ratchet **ZERO: 443 → 443** | none |
| 3b | codex/'s fate, both options costed | **C4's census + `[#579]`** | **AMENDED.** C4 measured **168 artifacts / 630,460 B, 0 removal-ready**, `codex/AGENTS.md` = PRECEDENCE-TRAP | none |
| 4 | **THE ROOT-CONTRACT**, verbatim | **intake #38** | **AMENDED — and this is the finding** | none |
| 5a | telemetry scope extension | **intake #50** | **AMENDED**, with DB-1's measured store enumeration attached | none |
| 5b | MODEL ATTRIBUTION | **nothing** | — | + **[#615]** — `grep` of `tasks/` for *model attribution · signature trailer · authoring model · model-authored* returned **ZERO rows** |

**banked_D = 2** — `[#577]` and `[#584]`, closed **BEFORE** any filing (D3 before D5, because a cap
not yet known cannot be spent against). **Births = 2. Spent exactly, nothing queued.**

**Item 4 is the one to read twice.** The root-contract maps to intake **#38** — *"Multi-repo
config standardization and fleet conformance at N=8"* — whose `consumers:` line **already records
R18, the conformance scorecard, as NOT-BORN at §6.4 with its un-parking conditions.** The
root-contract *is* R18's subject matter. Filing a fresh intake would have duplicated an **ACCEPTED**
one, which is precisely the outcome the correction exists to prevent; instead R18's un-parking
conditions become the thing to test. It **feeds** intake #61 q1 rather than duplicating it: q1 asks
where the *handoff engine's* boundary falls, the root-contract asks what a *repo root* must
contain.

**Item 3's identification is flagged, not asserted.** No row is titled *"the doctrine-consolidation
arc"*. `[#579]` (*Code doctrine & FDD — one ADR merging intakes #31 and #34*) is the ledger's only
doctrine **consolidation** arc and is CC's pick; `[#146]` and `[#166]` are the alternatives. **One
word settles it.** And the evidence points the same way as the new rule: `codex/` holds exactly one
file, which Z-G5 forbids, while deletion costs a Tier-1 doc's precedence section plus 166-of-168
baseline entries four live scripts read — so **universalisation, not deletion**, is the lawful
discharge. Both options are costed for the operator's cut.

**Standing constraint now live, owned by no lane:** *no further consumer deploys beyond the landed
floor until the root-contract is ruled.* win-tooling's v1.4.0 floor **is** landed
(`floor_integrity` PASS, `deployed_methodology_version` PASS), so it bites the **next** deploy.
Its parity role stays **pre-deploy** until the divergence arc closes — filed as a candidate.

---

## 4 · ADD-1 — the fix I was told to verify myself. THREE ATTEMPTS, and the defect was misnamed.

**Standing rule recorded as instructed: the operator does not test CC's fixes.**

```
1  deploy lane B's -O fix, re-fire      REFUTED. A NEW failure: "gh said: -O: No such file
                                        or directory". gh 2.93.0 does not forward `-- -O`
                                        to `cp` at all.
2  re-diagnose on a live codespace      home-relative dest  -> exit 0, NO file on disk (hollow)
   three arms, measured                 absolute, no flag   -> the packet's captured error
                                        --expand + absolute -> SUCCEEDS, ls confirms 809 B
3  fix with gh's own --expand           END-TO-END, the first this transport has produced:
                                        Ok=True, both cp legs, ssh ran, receipt.json 1176 chars
```

**The defect was never quoting.** `gh codespace cp --help`: a `remote:` path *"is resolved relative
to the home directory of the remote user"* and remote names are *"interpreted literally"*. The
single quotes in the old error were **gh quoting a literal path correctly**. The real defect is an
**absolute workspace path handed to a command that resolves against `$HOME`** — so the name
*"cp-quote defect"*, carried since the 2026-08-26 W4 measurement, described the **error text**
rather than the cause, and a fix built against that name could not have worked.

**W4 defect 1 is CLOSED.** Landed win-tooling `1a73dfb`, deployed via the ruled `Apply` verb, and
the deployed copy re-verified. `Dispatch-After` and `Harvest-Cloud` both resolve in a fresh shell.

**Z-G3 is still NOT met, and the next blocker is now named exactly.** `RemoteExitCode=1`, receipt:
*"Not logged in · Please run /login"*, and `gh secret list --app codespaces` returns **empty** —
`CLAUDE_CODE_OAUTH_TOKEN` is unset. That is an **operator secret act** the module deliberately
refuses to perform. Defects **2** (container `uv`) and **3** (silently stale clone) remain
**unmeasured**, because the probe's own commands never executed. All three codespaces stopped.

---

## 5 · THE SIX INTEGRATION ADJUDICATIONS

| # | adjudication | outcome |
|---|---|---|
| **1** | merge N and I as stopped; expect and **announce** the transient ship-gate RED | **Confirmed and announced.** The 2 `funnel_lifecycle` FAILs cleared themselves when J's relocations landed one merge later. **They never touched a commit gate** — lane I registered the check at **SHIP tier**, so `audit-health` never runs it. The gate discriminated on real state without obstructing the queue |
| **2** | `conformance.html` is NOT archived — two live consumers | **Recorded as a correction input, and it generalises** — see below |
| **3** | terra round-6 on N and I, post-queue | **MEASUREMENT-OWED.** `codex exec` returned *"You've hit your usage limit … try again at 4:20 PM"*. The provider limit is the recorded outcome your adjudication pre-authorised, not a skipped step. Both lanes carried terra **in-lane** already (N its own pass, I three commits including terra corrections); what is owed is the **post-merge** round |
| **4** | F9 accepted, no force-push | **Noted. No history was rewritten anywhere in this mission** |
| **5** | doc-counts sixth pin via the single regeneration pass | **Landed** in that pass, as lane I left it |
| **6** | MEMORY.md compaction deferred to operator/architect (`[#130]`) | **Noted, not acted on** |

**ADJ-2 generalises, and that is worth more than the correction itself.** FM-C classified
`ecosystem/conformance.html` as CONSUMED-BY `[#586]` + `ARCHITECTURE.md` and explicitly declined to
call it PROTECTED — *"a regenerable artifact earns no retention ruling, and I decline to invent one
for it."* Sound reasoning, incomplete input: lane N measured **two further live consumers** —
`generated_artifact_freshness` **outputs it in a tuple**, and **intake #42 carries an open operator
question** about it. **The generalisable gap: a consumption graph built from CITATIONS cannot see a
consumer that names its input IN CODE.** That edge type belongs in FPG-1's joined graph, so the
finding routes to **lane M's next slice**, not to a new organ. Filed as a candidate against FPG-1.

---

## 6 · THE PATTERN THIS BATCH KEPT PRODUCING — five instances, and it is the finding

**Two artefacts, each correct in isolation, disagreeing about a form neither could observe.**

1. The architect's `N1..N8` lane ids vs `validate_branch_naming`'s single-letter grammar — caught
   at freeze; would have cost every lane its ADR-110 exemption.
2. The launch-contracts home's **implicit schema** — every `*.md` there is parsed as a lane
   contract; caught by a REFUSED commit that would otherwise have wedged all seven wave-1 lanes.
3. *"the cp-quote defect"* — a name describing an error string, not a cause; three days old, and
   the fix built against the name could not work.
4. **FM-1 ruled `N = 30 days.`; FM-2 read for `READY threshold: <N> days`.** Leg d shipped NOT
   ARMED with 19 READY intakes unexamined, because the check **refused to invent** the constant.
   Repaired at integration; leg d's first live finding is real — intake **#15**, READY **44 days**,
   no `review-date:`.
5. **FM-5 resolves FM-4's emitter by CAPABILITY; FM-4 exposes THREE matching callables**
   (`funnel_health_block`, `funnel_health_numbers`, `write_funnel_health`). Lane L refuses two
   matches by design — *"picking one would be a guess wearing a resolution's clothes"* — so it
   renders `unavailable` naming all three. **OPEN: this needs a resolution rule, and it is the
   architect's, not mine.** Lane L's own comment says so: *"a resolution rule, not a guess,
   decides this."*

**Parallel lanes cannot see each other; only integration can.** That is an argument for what
integration is *for* — not against parallelism, and not for serialising the batch.

**And three of the eight wave-2 deliverables are REFUSALS**, each worth more than the green it
declined to claim: FM-2's leg d, FM-5's Ex-ante, A4's cross-check. **A batch that cannot produce a
refusal is not being measured.**

---

## 7 · THE ASSET BALANCE — your categories, across the whole mission

### SMALLER
| measure | batch start | now | delta |
|---|---|---|---|
| `validate_doc_rot` loci | **77** | **60** | **−17** |
| backlog row-body chars relocated | — | — | **−14,849 (−34.6 %)** over 20 rows, byte-identical |
| `docs/intake/*.md` | 56 | **54** | −2, md5-proven, archive 8 → 10 |
| `docs/decisions/*.md` | 89 | **89** | 0 — **structurally exempt**, ADR-100 + immutability |
| open backlog rows | 208 | **208** | 2 closed (`[#577]`, `[#584]`), 2 born (`[#614]`, `[#615]`) — **net zero, funded exactly** |
| `docs/intake/README.md` | `READY (13)` + an off-enum `OTHER (6)` rendering `[MISSING-ID]` | **SEED 10 · DRAFT 7 · READY 19 · ACCEPTED 19**, zero `MISSING-ID` | six quoted strings |

### VISIBLE
- **Funnel:** 951 objects, **100 % classified**, UNCLASSIFIED 0 · PROTECTED 895 · CONSUMED-BY 45 ·
  **CONSUMED-AND-ARCHIVABLE 0** · ORPHAN 11 (1.2 %), each re-tested by a second method.
- **Orphans both directions:** forward 11; backward **61 → 24** after identifier-aware resolution.
- **Codex:** 168 artifacts / 630,460 B, **0 removal-ready, 0 bytes freeable**.
- **README/VISION:** ~5,881 references, ~5,700 unmovable, **mergeable surface 5 lines in 1 file**.
- **Trends, live for the first time:** backlog velocity **+16 rows/week, IMPROVING**; three series
  render **ABSENT with their reasons**, including `logs/TELEMETRY.db` which **is gitignored and
  does not exist**.

### UNBLOCKED
| question | answer |
|---|---|
| funnel gate RED-capable? | **YES** — `check_funnel_lifecycle` at SHIP tier, RED-first twice, leg d armed at integration |
| FUNNEL HEALTH in bundle? | **YES** in `architect`/`execution`/`epic`; **not** `functional`; end-to-end MEASUREMENT-OWED |
| `why <path>` answers + refuses? | **YES**, witnessed on 3 governed files, FAILs on a planted unknown |
| trend surface regenerable in one command? | **YES**, byte-identical across two consecutive runs |
| freeze-time contract predicates? | **YES** — and they found a defect in the architect's own frozen text (`register R3` does not resolve; in this repo `R3` means `[#483] R3`) |
| codespace transport? | **W4 defect 1 CLOSED**; blocked on an operator secret act; defects 2 and 3 unmeasured |
| win-tooling role=consumer? | **NO — held at pre-deploy, measured**, with the divergence arc filed as a candidate |

### THE TRUST LINE
Every number above is measured on the merged tree or quoted from a lane packet that carries its
own witness. Where a number could not be obtained it says **MEASUREMENT-OWED** and names what is
owed. **Nothing here is estimated.**

---

## 8 · WHAT IS OPEN, AND WHOSE IT IS

1. **The `funnel_health` emitter resolution rule** — three candidates, lane L refuses to guess.
   *Architect.*
2. **`test_vi_batch1_reproduces_the_wrong_id_citation`** — my closures of `[#577]`/`[#584]` made
   lane G's **precision** assertion stale (`['[#577]','[#584]','[#587]']` vs `['[#587]']`). The
   predicate is right; the fixture is behind. Rewriting a stopped lane's assertion to green is what
   this repo warns against, so it is reported, not repaired. *Architect.*
3. **Terra round-6 on N and I** — provider limit, retry after 16:20. *CC, when the window opens.*
4. **`[#579]` identification** for item 3b. *One operator word.*
5. **Four pre-existing suite REDs**, none caused by this mission: `export_backlog_view`,
   `enforcement_coverage`, the doc-rot accretion arm, `desired_state_report` (lane A's literal
   fleet pin, one-commit fix already named in the wave-1 packet).
6. **`CLAUDE.md` §2.68's byte/line figures are still false** — 5,539 B / 107 lines, not
   *"103 lines, 5,270 B"*. Correcting them is a `CLAUDE.md` write and that file sits at 196/200.

## 9 · THE CLOCK CORRECTION, recorded because a wrong premise is the class this batch exists to catch
The night mission's **S5 hard stop (07:30)** was reasoned against a **stale clock**: the session
read 01:5x while the wall clock had moved to 11:0x. Wave 2 therefore ran on the operator's
explicit GO, not on the mission's timetable, and S5 was never the binding constraint it appeared
to be. Recorded rather than quietly dropped — a schedule derived from a wrong clock is a premise
error of exactly the kind the freeze predicates exist to refuse.
