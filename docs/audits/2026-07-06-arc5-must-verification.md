# Arc 5 — MUST install-verification sweep (intake doc #2 AC1)

**Date:** 2026-07-06 · **Install:** claude-code 2.1.202 (Windows), codex 0.141.0 · **Lane:** arc5-platform-triage worktree
**Contract:** intake doc #2 §3 MUST items, each verified against official docs AND on our install; adopted (recorded where) or rejected with a one-line why. Overnight citations split = SKIPPED (architect ruling D1). Immutable audit record.

## Verification method

- **Docs witness:** official-docs sweep via a read-only research subagent over code.claude.com/docs + the official CHANGELOG (citations per item below).
- **Install witness:** CLI existence checks (`claude agents --help`, `claude agents --json` live output) + string-table inspection of the installed 2.1.202 binary (`~/.local/bin/claude.exe`) for command registrations — chosen after headless `-p "/cmd"` probes proved unusable (a slash string becomes a full prompt turn; probe killed).
- **Honest limit (standing):** TUI-interactive surfaces (`/usage`, `/recap`, `/rewind`) were witnessed by binary registration + docs, not exercised end-to-end — an autonomous lane has no TUI. Where that limits the verdict it is stated per-item.

## MUST verdicts

| # | MUST | Docs witness | Install witness | Verdict | Recorded where |
|---|---|---|---|---|---|
| 1 | `claude agents` root cockpit | EXISTS — agent-view doc: sessions grouped Needs input / Working / Completed (code.claude.com/docs/en/agent-view.md) | `claude agents --help` full option set; `claude agents --json` returned the live fleet (5 sessions incl. this lane, statuses idle/busy); "needs input" grouping strings present in binary | **ADOPT** as the standard root cockpit for ADR-97 lane supervision | This doc + pointer line in the buy-vs-build verdicts doc (Routines/orchestration section); no doctrine rewrite (§5) |
| 2 | `/usage` per-skill/subagent/plugin/MCP breakdown | EXISTS — costs doc: attributes usage to skills, subagents, plugins, MCP servers; 24h/7d toggle (code.claude.com/docs/en/costs.md) | `/usage` registered in binary (45 hits); TUI-only — not exercised headless (honest limit) | **ADOPT** as cost-awareness practice, complements per-lane attribution (#123 family) | This doc; operator exercises it interactively |
| 3 | Background-subagent permission prompts surface in main session | EXISTS — v2.1.186+: prompts surface in the main-session dialog with agent attribution; pre-2.1.186 they auto-denied silently | 2.1.202 ≥ 2.1.186 ✓; live probe: a background subagent ran a benign network command **auto-allowed by read-only validation** — no ask raised, so the prompt path itself went unexercised (inconclusive probe, recorded as such); "needs input" grouping present | **ADOPT (confirmed in range)** with the load-bearing scope finding: the change is **interactive-session-scoped**. Our headless sandbox children (`deploy/lived_sandbox`, `claude -p`) have no main session → **NO G-batch workaround retires**. Retirement candidates list: **empty** (verified, not assumed) | This doc; G-batch workaround retirement explicitly declined with scope reason |
| 4 | `/less-permission-prompts` → native `fewer-permission-prompts` skill | EXISTS — scans recent sessions for repeat-prompting read-only commands, proposes `permissions.allow` additions (permissions doc) | **Executed live this arc** on 50 most-recent transcripts: top calls are auto-allowed (`grep`/`ls`/`git status/log/show`…), interpreters (`python`/`py` — allowlist-banned as arbitrary-code-exec), or mutating (`git add/checkout/merge/push`). **Zero eligible patterns** → zero additions applied | **ADOPT the tool** as periodic allowlist-maintenance tooling; **this run's proposal = empty, explicitly declined-with-reason** (interactive usage already covered by native auto-allow). Diff vs `ARC_ALLOW_RULES` (`deploy/lived_sandbox/arc.py:214`): disjoint by design — ours is a containment allowlist (exact-form grants incl. pinned `git commit -m:*` to keep `--no-verify` at the wall), the skill is a convenience allowlist; the skill can never maintain the containment class. Keep both, separate niches | This doc; satisfies #118 Done-when (run + reviewed + declined) → `closes [#118]` at integration |
| 5 | Sonnet-5 pin refresh (A1) | Sonnet 5 = CC default since 2.1.197, 1M context, promo $2/$10 → Aug 31 | **Amendment A executed:** `claude --model claude-sonnet-5 -p` → accepted, replies, zero deprecation/auto-update warns on 2.1.202; V1 pre-resolved 2026-07-06 (4-6 ACTIVE → refresh discretionary, ratified GO) | **ADOPTED** — commits `608eed6` (artifact-reader + conformance-hub V1/V2/V3 pins), `399c843` (PLAYBOOK doctrine); settings.json:73 prose verified generation-neutral → no edit needed | The pins; PLAYBOOK "Model / effort platform doctrine"; Aug-31 economics in the verdicts doc |
| 6 | Fable-5 t-shirt routing DECIDE | — | — | **RESOLVED BY RULING, not by this lane:** ADR-70 amendment 2026-07-07 adds XL = Fable 5, browser-architect layer only; NOT a fourth fan-out size; ADR-80 §5 anti-conflation mandatory | PLAYBOOK XL row (`399c843`); doctrine home = the amendment file |
| 7 | `/recap` + `/rewind`-past-`/clear` | Both EXIST — away-summary + manual `/recap`; `/rewind` offers previous-session restore past a `/clear` (v2.1.191+, checkpointing doc) | `recap` (54) / "Away summary" (2) / `rewind` (104) registered in binary; TUI-only — not exercised headless (honest limit) | **ADOPT** as free session hygiene; no config, no doctrine edit | This doc one-liner |

## SHOULD-feature availability (pre-pilot findings, feeds WI-3)

- **Outcomes:** NOT a Claude Code CLI feature — it is an Agent Platform (Managed Agents) outcome-grader; `outcome-grader` absent from the 2.1.202 binary. The P2 pilot proceeds per plan's absence-branch (reject-with-why + WATCH) with the ruled heterogeneous-grader substance where honestly executable.
- **/batch:** absent from official docs BUT registered in the installed binary (26 string hits) — an undocumented/pre-doc surface. The P3 sandbox pilot proceeds against the live surface; its verdict must carry the no-official-doc caveat (schema may change).
- **/goal:** EXISTS (docs + binary, v2.1.139+ floor ≤ 2.1.202). **Ultraplan:** EXISTS (v2.1.91+; needs CC-on-web account + GitHub repo — the hub HAS `origin https://github.com/rdwornik/dev-knowledge.git`). **Routines/`/schedule`:** EXISTS incl. CLI creation path. **Artifacts:** EXISTS (Artifact tool present in-session).

## Disposition summary

7/7 MUSTs dispositioned: 6 ADOPT (1,2,3,4,5,7 — with #3's retirement-candidates list explicitly EMPTY and #4's proposal explicitly EMPTY), 1 resolved-by-ruling (6). Zero rejections. No doctrine edited beyond the two A1 commits already ruled GO.
