# v7 BOOT-INVERSION — HISTORY DELTA and EQUILIBRIUM MAP, first cut

- **Class:** technical · **Date:** 2026-09-01 · **Arc:** `[#614]` · **Consumed by:** the batch-G seed (close packet §8), intake #66, `[#611]`
- **Author:** CC (Opus 5, batch-F integrator seat)

> **HAND-DERIVED ON PURPOSE. These two sections are specified to be GENERATED** — that generator
> is batch-G's build. This cut exists so G specifies against a **worked example** rather than a
> description, and so the operator gets the value one window early. Every number below cites the
> committed artifact it came from; nothing is from memory.
>
> **Flat by design** — no pipe tables, so this copies into browser chat unpainted.

## 1 · HISTORY DELTA — this bundle against the previous one

`docs/handoffs/` is an immutable time series; this reads two committed `FUNNEL_HEALTH.md`
files, both generated by `scripts/funnel_lifecycle.py::measure` (FM-2). **Direction is the
point, not the level.**

```
source A: docs/handoffs/2026-08-31-dev-knowledge-architect/FUNNEL_HEALTH.md
source B: docs/handoffs/2026-09-01-dev-knowledge-architect-v7/FUNNEL_HEALTH.md

metric                                  08-31    09-01    delta   direction
intakes live                               55       58       +3    RISING
intakes archived                           10       11       +1    RISING
intakes READY                              19       20       +1    RISING
ADRs live                                  89       89        0    FLAT
rows                                      355      362       +7    RISING
rows post-cutoff                           19       26       +7    RISING
leg a1  intakes terminal-unarchived         0        0        0    FLAT at zero
leg a2  ACCEPTED, rows terminal             0        0        0    FLAT at zero
leg b   ADRs terminal-unarchived            0        0        0    FLAT at zero
leg c   post-cutoff provenance unresolved    0        0        0    FLAT at zero
leg d   READY intakes past threshold         1        1        0    FLAT at one
```

**READING IT — the direction, not the levels, is the health check.**

- **All four rot legs are FLAT AT ZERO across the window.** That is the load-bearing line: a
  batch that merged eight lanes and closed zero rows did **not** manufacture rot.
- **`rows` +7 while closures were ZERO.** The queue grew and nothing left it. One window does not
  make a trend, but this is exactly the series that would show a filing/closing imbalance, and
  the direction this window is unambiguous.
- **`rows post-cutoff` +7 — the SAME +7.** Every new row is post-cutoff, i.e. all growth is
  recent-provenance rather than backfill.
- **`leg d` FLAT AT ONE, not zero.** A READY intake has been past threshold across both windows.
  Flat is not the same as clean; it is a thing that has not moved in two bundles.
- **`ADRs live` FLAT at 89** — the window produced no governance decision, which is consistent
  with a batch that executed rather than ruled.

**HONEST LIMIT: n=2.** This is the first delta the series can produce, because `FUNNEL_HEALTH.md`
is itself a v7-era artifact and only two bundles carry one. "Direction" over two points is a
line, not a trend. The value compounds from the third bundle.

## 2 · EQUILIBRIUM MAP — per dimension, WHO DRIVES

Three drivers: **REPO** = a mechanism decides (a gate, a generator, a validator — it refuses or
it passes, with no human in the loop). **BROWSER** = the architect seat's judgment. **OPERATOR**
= a decision only Rob can make. Derived from the organ inventory (`ecosystem/organ-index.md`),
the live gate roster (`CLAUDE.md` §9) and this window's own record of who actually decided what.

```
dimension          DRIVER    evidence from THIS window
execution          REPO      the gates ran the batch. journal_spine_anchor WEDGED six lanes
                             until fixed; block-unanchored-push refused; audit-health blocked
                             four separate commits until each cause was fixed. No human
                             adjudicated any of it.
funnel             REPO      funnel_lifecycle measures all four legs; 0 rot / 0 orphan is a
                             computed verdict. Filing/closing pressure is gated by
                             kill-candidates and backlog-id-on-close.
contracts          BROWSER   the freeze produced 7 contracts with 0 deviations -- authored, not
                             computed. [#630] is the row trying to move a PIECE of this to REPO
                             (manifest/contract slug agreement) and it landed UNWIRED.
routing            OPERATOR  and it is the sharpest gap on this map. Start-DispatchLane still
                             defaults to opus (line 96); the contract's routing table decided
                             nothing; -Model had to be passed by hand. [#631] fired THREE times
                             in one window. Nothing re-validates a model choice at dispatch.
review             REPO+     terra ran on all nine merged branches and refused in eight. The
                   BROWSER   RUNNING is mechanical (amendment 2 made it mandatory); the
                             DISPOSITION of each finding was the integrator's judgment -- which
                             is why five rows are open rather than closed.
eval               ABSENT    no eval loop exists. [#617] was RE-DECIDED as Tier L this window
                             precisely because building one touches gates. Recorded as absent
                             rather than assigned a driver it does not have.
observability      REPO      SessionStart surfaces [asks]/[funnel]/[load]/[fleet] every session.
                             GAP: credits per lane per model are UNDERIVABLE ([#615]) -- so the
                             cost dimension of observability has no driver at all.
graph              REPO      depends-on is validated and acyclic; boot_frontier computes the
                             unblocked frontier live (160 unblocked, 5 proposed at width<=6).
memory/distiller   BROWSER   no mechanism. LESSONS.md and the memory corpus are hand-authored.
                             F2 (the distiller) was DEFERRED to G, so this stays BROWSER.
planning           OPERATOR  boot_frontier PROPOSES and refuses to dispatch ("requires an
                             explicit operator GO -- adjudication stays a human act", AUT-R1
                             Phase 4). The batch THEME was cut by the operator, not scored.
doctrine           OPERATOR  every doctrine move this window was an operator ruling: the 6/12
                             concurrency amendment, doctrine option 4 (dispatch local), the
                             terra-every-lane amendment, the window-close precondition.
```

## 3 · WHAT THE MAP IS FOR — and the metric set it should carry

**The seat reads this map to know where it is still needed.** A dimension marked REPO is one the
seat can stop watching; a dimension marked OPERATOR is one where the seat must stop and ask.
**The DIRECTION of movement is the equilibrium's own health check** — an equilibrium that never
moves BROWSER→REPO is not maturing, and one that moves OPERATOR→REPO without a ruling is
drifting.

The operator's specified metric set, with what this window can and cannot answer:

```
metric                              this window          note
operator process questions          0 asked by CC        target is 0; met, but only because
  (target 0)                                             the seat resolved from the tree
budget-decided vs escalated         all budget-decided   zero escalations; 4 operator messages
                                                         arrived unprompted, none answered a
                                                         CC question
architect rulings per batch         4 amendments + 3     amendments 1-4 pre-dispatch, then the
                                    in-window rulings    concurrency, window-close and
                                                         batch-G-seed rulings
OPERATOR ASKS re-asked              5 rows, max re-ask 3 4 of 5 now carry a landed visible-fix;
                                                         3 marked PARTIAL
validator-caught vs human-caught    VALIDATOR 4 /        validator: the 3 missed v7 dependents,
  premise errors                    HUMAN 2              the duplicate day-letter, the DoD
                                                         freshness, the audits index. human: the
                                                         AGENTS.md drift, the lane-f near-teardown
review coverage                     9 of 9 merged        100% -- amendment 2 satisfied, incl. the
                                    branches reviewed    branch that changed no code
orphans / rot                       0 / 0                flat at zero across both bundles
off-laptop lane share               1 of 9 (11%)         only the [#632] proof lane ran off-laptop
credits per lane per model          UNDERIVABLE          [#615] is the row that fixes it; reported
                                                         ABSENT rather than proxied
```

**THE SINGLE MOST ACTIONABLE CELL ON THIS MAP IS `routing = OPERATOR`.** It is the only
dimension where a mechanism EXISTS (a routing table in every contract), the mechanism is
IGNORED (the dispatcher defaults to opus and nothing re-validates), and the failure has now
fired three times in one window. Moving that one cell from OPERATOR to REPO is a small, bounded
change with a measured justification already in hand.

## 4 · THE `[#611]` PASTE MEASUREMENT — from a real cut, and it MISSES both legs

`[#611]`'s Done-when requires *"an assembled paste from a **real cut** measuring **<=20 KB** at
**>=70% window-specific** content."* This is that cut — `docs/handoffs/2026-09-01-dev-knowledge-architect-v7`,
assembled by `scripts/assemble_paste.py` after the v7.0.0 bump, ROLE PIN emitting at v7.0.0.

```
PASTE_THIS.md total                       27,365 B   (26.7 KB)
  bundle HANDOFF_BOOT.md (session header) 11,401 B   42%   mostly generic: the file-roles table
                                                            and the operator-forms card
  PROBES.md                               14,511 B   53%   generated, structurally generic --
                                                            questions + commands, no answers
  RESIDUAL.md                              7,562 B          window-specific (the hand-written half)
  ROLE PIN                                  ~250 B          3 lines, replacing a 17,364 B inline

BAR 1  size            <= 20,480 B   ACTUAL 27,365 B   MISS by 6,885 B (+34%)
BAR 2  window-specific >= 70%        ACTUAL ~31%       MISS by ~39 points
       (window-specific = RESIDUAL 7,562 + the filled header fields ~800 = ~8,362 of 27,365)
```

**BOTH LEGS MISS, AND THE CAUSE IS NOT THE RESIDUAL.** The hand-written, window-specific half is
7.5 KB — under a fifth of the paste. **95% of the payload is the bundle's own session header plus
PROBES**, and both are largely generic across cuts. Making the residual terser would move the
size bar the wrong way *and* worsen the window-specific ratio; the only levers that reach the bar
are the 11.4 KB header and the 14.5 KB probe file.

**THIS IS WHY `[#611]` STAYS OPEN.** v7.0.0 IS cut, every dependent IS reconciled, and
`reconciled_versions` is green — but the row's Done-when is a *measured size target*, and the
measurement now exists and fails. The v6.3.0 residency flip already removed the single biggest
item (a 17,364 B inline role file became a 3-line pin); the remaining bulk is a different
problem, and it is the one G inherits.

**Honest limit:** this is an **architect-mode, cold-supplement** cut. An execution-mode bundle
drops the supplement and part of the walkthrough, so its ratio differs — one cut is one cut, and
the row should not be closed OR re-scoped on a single measurement.
