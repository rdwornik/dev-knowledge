# Handoff boot — session header + paste-pointer (architect mode)
<!-- scope: meta -->

| Field | Value |
|---|---|
| **Slug** | `2026-07-02-dev-knowledge-architect` |
| **Mode** | **architect** (v5.3 §13 — planning / way-of-working scope) |
| **Purpose** | **The operator's FILLED supplement sets the priority — and it is NOT fleet rollout. The three goals, in order: (a) run the time-boxed FABLE whole-system review + merge its rulings; (b) wire ai-council as a CC-GOVERNED query mechanism (browser → CC → ai-council); (c) CLOSE the two open methodology tracks — ADR methodology + ai-council methodology.** This **re-scopes** CC's repo-derived frontier: the deploy machinery is now BUILT + ARMED + PROVEN (n=1) + DOCUMENTED, so the backlog dependency graph reads **fleet rollout (#221)** as next — but the operator ruled the supplement **authoritative on priority** (§6/§A), and fleet is sequenced **AFTER** the three goals (the Fable review may reshape the methodology, so scaling to n=3 before merging its rulings would propagate a soon-to-change methodology; #234/#231 hardening pair with the ai-council work). **What shipped this window (the sealed backdrop — do NOT redo):** the 2026-07-01 arm-first brief **executed** — `b986711` armed the floor carrier + built the #230 conformance self-test + armed real ai-council (**ADR-93** ratified [#226][#230]); `a95e3b0` closed **#222/#223/#224** (documented the deploy subsystem + **resolved the forced-freshness-stamp anti-pattern** via the `ecosystem/doc-counts.md` decouple); plus a **comprehensive external-review system audit** (`4467159`) and the **first-ever cross-repo architect handoff** (ai-council, `fef026e`, filed **#234**). The incoming architect's job: **run Fable first (perishable window), then wire+seal the two coupled methodology tracks; treat fleet #221 as sequenced-after, not the immediate work.** A **decision + design** session — the floor model (A), the ARCHITECTURE currency, and the arm-first arc are settled; the OPEN forks are the Fable review scope, the ai-council-integration shape (skill vs hook vs other), and what "closed" means for the ADR methodology (the #170/#168 traceability spine is the sticking point). |
| **Generated at** | Bundle cut at HEAD `a95e3b0`, **working tree clean**, `main` **in sync with `origin/main`** (the whole window is pushed). This bundle's own commit + the later `/ship` `--no-ff` merge move HEAD and push `main` **ahead of `origin` until pushed**. **Re-derive HEAD / sync at read-time** (`PROBES.md` P3) — do not trust this line. |

> **Generated LIVE, then FILLED by the outgoing architect.** This bundle was generated by **CC at the end of a CC-execution window** (arm-first chain #226/#230 + ARCHITECTURE currency #222/#223/#224 + the external-review audit + the ai-council cross-repo handoff); the operator then **FILLED `SUPPLEMENT.md`** from the outgoing architect chat, so its ANSWERS are folded into `PASTE_THIS.md` and carry this window's **off-repo strategic "why."** The load-bearing thing the supplement supplies: **the priority is the three goals (Fable → ai-council-as-governed-query → close ADR + ai-council methodologies), NOT fleet #221** — and the operator ruled the supplement **authoritative on priority** where it diverges from CC's repo-derived frontier (§6/§A; the same supplement-vs-repo precedent set by the ai-council `0b61b6e` re-scope this session). BOOT + RESIDUAL have been **re-scoped to lead with the three goals** (fleet demoted to secondary, kept not deleted). Because the supplement carries answers, the incoming **§13(d) operator-context beat NARROWS** to *"anything changed since the supplement was written?"* — **not** a FULL re-ask. The CC-derived repo-side facts are **four-tag** scoped in `RESIDUAL.md`; the **off-repo** *why* lives in the folded SUPPLEMENT.

> **Operator runbook is not copied here.** The stable who-each-file-is-for / run-loop / rationale lives **once** in the canonical per-repo runbook **`docs/handoffs/README.md`** — read it for the walkthrough. This file carries only the **session header** (above) + the **paste-pointer** (below). v5 bundles carry **no per-bundle README** (the 2026-06-12 canonical-runbook collapse — `HANDOFF_PROCESS.md` §13).

## What the operator does (paste-pointer)

1. Open a fresh Claude.ai chat.
2. Paste **`PASTE_THIS.md`** (in this bundle directory) — the browser role file + residual + probes + the (empty) supplement, assembled in order by `scripts/assemble_paste.py`. One paste; never hand-feed individual files to the file-less browser.
3. The browser replies with its on-load acknowledgment line (it names the Layer-1 actor); a partial or missing paste is then visible.
4. **Architect mode** — the browser uses the **generative / decompositional** posture (`protocols/HANDOFF_BOOT.md` §"Architect mode"): orient first, ask the operator for off-repo context, drive decomposition, hold the whole-system view, surface design tensions — **not** the reactive-filter default.
5. **Orient before any mechanism** — hand the browser `PROBES.md` **P1** (orientation) first; it replies `run <command>`, CC reads live `VISION.md` / `ARCHITECTURE.md` Ch1 and substring-checks. The architect may not begin design until it holds both orienting lines. (P1's grep is a *tool* that confirms the frame — the **backlog navigates**, v5.3 §13c.)
6. **Then the operator-context beat (§13d) NARROWS.** The supplement is **FILLED** (its ANSWERS are in the paste — the three-goal priority, the Fable-first rationale, the ai-council-integration open shape, the "what NOT to redo" list), so the browser asks only *"anything changed since the supplement was written?"* — **not** a FULL re-ask. (The load-bearing off-repo call is already answered: **Fable-first, methodologies-next, fleet-after** — the priority the repo's dependency graph does not encode.)
7. Hand `RESIDUAL.md` — **drift-flags first** (the headline: `ship-gate` is **GREEN** and **clean** — 6 WARN dispositioned, **NO `[stale]`**, and the 2026-07-01 forced-freshness debt is **RESOLVED** by #222) — then the **next-frontier decisions** (§4, now led by the **operator's three goals** — Fable review → ai-council-as-governed-query → close ADR + ai-council methodologies — with **fleet #221 demoted to sequenced-after**) + the **shipped-this-window** map (§2, the arm-first-chain-executed arc) + run the rest of `PROBES.md` (P2–P9). Any probe FAIL blocks onboarding (the escalation ladder).

## Why a pointer, not a copy

The methodology, the browser role, and the operator runbook all live in the repo and are referenced by **pointer**; CC (which holds the repo) serves any part the file-less browser needs just-in-time. `PASTE_THIS.md` is the assembled boot source (generated by `scripts/assemble_paste.py`, never hand-edited); `docs/handoffs/README.md` is the operator runbook. If a pointer and its source disagree, the source wins — fix the pointer or regenerate `PASTE_THIS.md`.
