# Handoff boot — session header + paste-pointer (architect mode)
<!-- scope: meta -->

| Field | Value |
|---|---|
| **Slug** | `2026-07-02-dev-knowledge-architect` |
| **Mode** | **architect** (v5.3 §13 — planning / way-of-working scope) |
| **Purpose** | **The deploy subsystem is now ARMED + conformance-proven on n=1 (ai-council) — the arm-first chain executed. The next frontier is FLEET ROLLOUT (#221), the leaf that was gated on arming and is now UNBLOCKED.** The window since the 2026-07-01 architect handoff **executed that handoff's arm-first brief**: `b986711` **armed the floor carrier + built the #230 conformance self-test + armed real ai-council** ("configured" → "armed"; Layer-2 9/9 green) — **ADR-93 ratified** (floor provisioning model A, armed + conformance-proven [#226][#230]); then `a95e3b0` closed **#222/#223/#224** — decoupled the three volatile count-claims out of `ARCHITECTURE.md` into a loose-generated `ecosystem/doc-counts.md` (**resolving the forced-freshness-stamp anti-pattern the 2026-07-01 handoff flagged**), documented the deploy subsystem in `ARCHITECTURE.md` via a genuine end-to-end re-read, and rotated the Governing-ADR / CLAUDE §11 lists to 89→93. Two adjacent doc arcs also landed: a **comprehensive external-review system audit** (`4467159` + `a157dda` §4 completion) and the **first-ever cross-repo architect handoff** (ai-council, `fef026e`), which tripped + fixed the #163 probe-validator's cross-repo path and filed **#234**. So the deploy machinery is BUILT, ARMED, PROVEN (n=1), and DOCUMENTED. What remains of the chain: **#225 (surgical comment-preserving precommit carrier — clean diffs at scale) → #221 (fleet rollout to n=2+)**. The incoming architect's job: **sequence + drive the fleet rollout** (roll the armed methodology to corp-monorepo / corp-ops / corp-sca-time-automation using #230 as each repo's acceptance gate), and settle the adjacent open decisions (#231 feedback-loop shape, #234 cross-repo teeth, #220 MODIFY axis, #210 no-ff standing rule). This is a **decision + sequencing** session, not a re-decide — the floor model (A), the fleet-last ordering, and the #131↔#215 SPLIT are settled. |
| **Generated at** | Bundle cut at HEAD `a95e3b0`, **working tree clean**, `main` **in sync with `origin/main`** (the whole window is pushed). This bundle's own commit + the later `/ship` `--no-ff` merge move HEAD and push `main` **ahead of `origin` until pushed**. **Re-derive HEAD / sync at read-time** (`PROBES.md` P3) — do not trust this line. |

> **Generated LIVE at the end of a CC-execution-driven window.** This bundle was generated by **CC at the end of a CC-execution window** — the arm-first chain execution (#226/#230), the ARCHITECTURE currency arc (#222/#223/#224), the external-review audit, and the ai-council cross-repo handoff were all CC-driven work with the strategic "why" **largely already in the repo** (ADR-93, `LESSONS.md`, PLAYBOOK §20, the JOURNAL arc, the two 2026-07-02 audit artifacts). There was **no single outgoing browser architect chat** holding un-committed deliberation, so `SUPPLEMENT.md` is generated **empty (the defined cold-handoff disposition)** unless the operator fills it from a chat they still hold. Because the supplement is **empty**, the incoming **§13(d) operator-context beat fires FULL** (`HANDOFF_PROCESS.md` §13: the empty supplement = the cold disposition, nothing to narrow against). The CC-derived repo-side facts are **four-tag** scoped in `RESIDUAL.md` (`recall`/`inferred` for the reconstructed window, `witnessed` for live re-derivation — re-check via `PROBES.md`).

> **Operator runbook is not copied here.** The stable who-each-file-is-for / run-loop / rationale lives **once** in the canonical per-repo runbook **`docs/handoffs/README.md`** — read it for the walkthrough. This file carries only the **session header** (above) + the **paste-pointer** (below). v5 bundles carry **no per-bundle README** (the 2026-06-12 canonical-runbook collapse — `HANDOFF_PROCESS.md` §13).

## What the operator does (paste-pointer)

1. Open a fresh Claude.ai chat.
2. Paste **`PASTE_THIS.md`** (in this bundle directory) — the browser role file + residual + probes + the (empty) supplement, assembled in order by `scripts/assemble_paste.py`. One paste; never hand-feed individual files to the file-less browser.
3. The browser replies with its on-load acknowledgment line (it names the Layer-1 actor); a partial or missing paste is then visible.
4. **Architect mode** — the browser uses the **generative / decompositional** posture (`protocols/HANDOFF_BOOT.md` §"Architect mode"): orient first, ask the operator for off-repo context, drive decomposition, hold the whole-system view, surface design tensions — **not** the reactive-filter default.
5. **Orient before any mechanism** — hand the browser `PROBES.md` **P1** (orientation) first; it replies `run <command>`, CC reads live `VISION.md` / `ARCHITECTURE.md` Ch1 and substring-checks. The architect may not begin design until it holds both orienting lines. (P1's grep is a *tool* that confirms the frame — the **backlog navigates**, v5.3 §13c.)
6. **Then the operator-context beat (§13d) fires FULL.** The supplement is **EMPTY** (cold disposition — no outgoing chat held this window's deliberation), so the browser asks the operator the **full** off-repo question: *what intent / priorities / changed decisions / off-repo findings should steer this fleet-rollout planning session?* (not a narrowed "anything changed since?").
7. Hand `RESIDUAL.md` — **drift-flags first** (the headline: `ship-gate` is **GREEN** and **clean** — 6 WARN dispositioned, **NO `[stale]`**, and the 2026-07-01 forced-freshness debt is **RESOLVED** by #222) — then the **next-frontier decisions** (§4, led by **fleet rollout #221** now unblocked + the **#225 → #221** remaining chain) + the **shipped-this-window** map (§2, the arm-first-chain-executed arc) + run the rest of `PROBES.md` (P2–P9). Any probe FAIL blocks onboarding (the escalation ladder).

## Why a pointer, not a copy

The methodology, the browser role, and the operator runbook all live in the repo and are referenced by **pointer**; CC (which holds the repo) serves any part the file-less browser needs just-in-time. `PASTE_THIS.md` is the assembled boot source (generated by `scripts/assemble_paste.py`, never hand-edited); `docs/handoffs/README.md` is the operator runbook. If a pointer and its source disagree, the source wins — fix the pointer or regenerate `PASTE_THIS.md`.
