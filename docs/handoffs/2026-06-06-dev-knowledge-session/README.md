# Handoff — 2026-06-06-dev-knowledge-session

**Generated by HANDOFF_PROCESS v4.4 (status: beta)**

**Repo:** .dev-knowledge  ·  **Type:** session  ·  **Generated:** 2026-06-06
**Repo state at generation:** branch `docs/handoff-2026-06-06`, HEAD `608f26b`

This bundle onboards a **new chat** to continue work on `.dev-knowledge`. It is a
teaching sequence, not a file dump: the new chat learns its role, our methodology,
the project, what just happened, and what to do now — then proves it understood
before touching anything.

Files `01`–`07` are the teaching sequence (paste them to the new chat). This
`README.md` is for you only — do not paste it.

## Paste sequence + escalation ladder

```
Step A: Paste 01_ROLE + 02_METHODOLOGY + 03_PROJECT + 04_RECENT + 05_NOW
        as ONE message into the new chat. KEEP the ===== FILE: NN — start/end =====
        separators around each file (they make the boundaries salient — v4.4 §E).
        Wait for acknowledgment.
Step B: Paste 06_QUESTIONS. Read the chat's answers — and CHECK each answer names
        the bundle file + section it draws from (v4.4 §C). An answer with no source
        reference fails, however well it reads; treat it as a comprehension FAIL.

IF comprehension FAILS:
  Tier 1 — Re-paste the specific file(s) the chat got wrong + "read again carefully".
  IF still fails:
  Tier 2 — Ask Claude Code to verify the specific facts against repo state;
           paste CC's findings to the new chat.
  IF still fails:
  Tier 3 — ABORT onboarding. Reactivate the sender chat, or do a manual context dump.

IF comprehension PASSES:
  Step C: Paste 07_ASK_BACK. Answer the chat's questions from memory or the sender chat.
  Step D: The chat begins work.
```

## Drift cross-check

**No drift detected** — all sender claims verified against repo state at Phase 2.
The six load-bearing claims (ADR-72 existence, #84 n=1-of-2 gate, #86 sub-decision-3
open, the `surface_triage.ps1` backports, clean hub tree, #89 check-source) each match
repo state — full per-claim table with verification commands in `04_RECENT.md`.

One scope note (not drift): the sender's **corp-monorepo** claims (corp nightly LIVE +
real catch, corp code-owned renderer, the 2026-06-07 scheduled night) are cross-repo and
were **not** independently re-verified here, per the ADR-36/41 read-only boundary. They
are sourced to corp's JOURNAL 2026-06-06; confirm in that repo if a decision turns on them.

No graceful-degradation gaps — all source files (VISION, CLAUDE, PLAYBOOK, ESSENTIALS,
BACKLOG, JOURNAL, templates) were present at generation.

## Bundle contents

| File | Purpose | Budget |
|---|---|---|
| `01_ROLE.md` | Who the new chat is; who Rob is | ≤100 |
| `02_METHODOLOGY.md` | How we work (PLAYBOOK pointers + key extracts) | ≤200 |
| `03_PROJECT.md` | Vision + sacred files | ≤150 |
| `04_RECENT.md` | Narrative of recent work | ≤250 |
| `05_NOW.md` | Top P1s + in-progress branches | ≤100 |
| `06_QUESTIONS.md` | Comprehension check (7 Q + fixed first-move Q8) | ≤80 |
| `07_ASK_BACK.md` | New chat's question slot (max 3) | ≤50 |
