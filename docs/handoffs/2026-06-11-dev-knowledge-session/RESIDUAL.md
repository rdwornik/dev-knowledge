# Residual — 2026-06-11 (v5 beta)
<!-- scope: meta -->

> The **residual**: what the repo does not already encode. Drift-flags are the headline,
> not a footnote. Pointers are paths, not copies. Task-state is a pointer to `BACKLOG.md`,
> not re-narrated IDs. Generated from inside the repo at HEAD = the 2026-06-11
> fleet-baseline chore commit (working tree clean at generation).

---

## 1. Drift-flags — the headline

Two flags from the read-only drift-checks (`validate_doc_claims` #89, `validate_git_backlog`
#90) plus CC's state read. **A flag is a question, not a verdict** — one is a genuine
cleanup, the other is a correctly-open item the validator can't tell apart from a real one.

### DRIFT-1 — `doc_claims` #89: ARCHITECTURE.md test-count claim is STALE *(genuine, actionable)*

`validate_doc_claims` flags ARCHITECTURE.md's `**N collected**` pytest-count claim as
drifted: new tests landed (the v5 parallel-ship regression fence) and the doc's hardcoded
number no longer matches the live `pytest --collect-only` count.

- **Why it slipped:** the `pytest_collected` claim is the **expensive / off-gate** claim —
  the per-commit `audit.py health` gate **skips it** (only the full standalone run /
  `audit.py run` / SessionStart evaluates it). So the v5 ship commits ran green on
  `health` while this claim quietly drifted. This is exactly the coverage a full-state v5
  read adds over the commit path.
- **Exact delta:** `python scripts/validate_doc_claims.py` (prints doc-claimed vs actual).
- **Fix (NOT done here, on purpose):** re-stamp ARCHITECTURE.md's `**N collected**` to the
  live count. Left un-fixed deliberately — surfacing it is the deliverable, and fixing it
  would erase the live drift this dogfood demonstrates. Worth a one-line follow-up.

### DRIFT-2 — `git_backlog_drift` #90a: a `closes [#77]` still open in BACKLOG *(known-benign, NOT actionable)*

`validate_git_backlog` flags a `closes [#77]` merge whose `[#77]` is still present (open)
in `BACKLOG.md`. **This is a dispositioned voided closure — do not "fix" it.**

- **Disposition:** `ecosystem/disposition-register.yaml` → `warn-77-voided-closure`. The
  merge carried `closes [#77]` but shipped *different* work (CONTRIBUTING→v4, not the
  protocols-consolidation #77 names) — #77 was **re-scoped, not done**, so it legitimately
  stays open (corrected 2026-06-09). Direction-(a) #90a cannot tell a misattributed
  closure from a real one; only arc-content inspection (**#139** / #90b) can — which is
  why the register auto-clears this entry once #139 ships.
- **Why it doesn't block:** the `audit.py ship-gate` reads `Finding.status` and suppresses
  this one WARN via the register; `health` reports it as `[~~]` (WARN, informational) and
  still exits OK.
- **The lesson the validator line can't carry:** the "this is fine" lives in the register +
  the git arc (e979730 / 9639597 / 0ee6c3d), **not** in the one-line validator output a
  handoff surfaces. A v5 receiver that treats every flag as an action item would "close" a
  correctly-open ticket. **Consult the register before acting on any #90 flag.**
- **Confirm:** `python scripts/validate_git_backlog.py` + read `ecosystem/disposition-register.yaml`.

---

## 2. Un-committed session reasoning — the lived "why"

Not in git, JOURNAL, or BACKLOG (which record that v5 *shipped*, not this dogfood run):

1. **Why this artifact exists now.** It is a deliberate early exercise of v5 mode — the
   #149 **gate-3 empirical teeth dogfood** — run *ahead* of Council ratification of ADR-82,
   to prove the v5 residual format end-to-end and stress the teeth against live state. The
   ratify/fresh-eyes gates (#149 (1)(2)) are still pending; this only discharges gate (3)'s
   *mechanics* as a rehearsal, not the formal flip.
2. **What the dogfood already taught (live proof points, nowhere in git):**
   - **v5's "drift-flags as headline" earned its keep on the first run.** It surfaced a
     *real* stale claim (DRIFT-1) that the per-commit `health` gate **structurally cannot
     see** (the count claim is off-gate by cost). The strongest argument *for* model C was
     found empirically here, not in review: the full-state read catches what the commit
     path misses.
   - **DRIFT-2 is the inverse lesson** — a flag is a *question*. The nuance that #77 is
     correctly open lives in the disposition register + the merge arc, not the validator
     line. The residual's job is to carry "consult the register," which a raw validator dump
     would not.
3. **Design decision recorded for the #149 flip.** This bundle deliberately avoids the
   `Generated by HANDOFF_PROCESS vN.N (status: …)` stamp so `audit.py` check #8 skips it —
   the same parallel-ship invisibility the v5 *spec file* relies on (the regression fence in
   `tests/test_audit.py`). At the flip, check #8 must be retargeted to historical v4 bundles
   (spec §11) **and** a v5-shape validator built, or the stamp convention reconciled — else
   v5 bundles are validated by a v4-shaped check.
4. **The teeth discipline (see §4).** Every probe's exact answer is kept *out* of this
   residual — drift-flags are stated **qualitatively + the command**, never the raw
   integers/shas/dates. That is what makes the probes un-bluffable from a summary.
5. **Minor state notes.** `main` is **ahead of `origin/main` by 1** — the unpushed
   2026-06-11 fleet-baseline chore; benign, not pushed by design. Working tree clean at
   generation.

---

## 3. Pointers (paths — read these; do not copy them into the handoff)

- **Process (v5, beta, governing this handoff):** `protocols/HANDOFF_PROCESS_v5.md`;
  thin boot `protocols/HANDOFF_BOOT.md`.
- **Process (canonical, still authoritative for real handoffs):** `protocols/HANDOFF_PROCESS.md` (v4.4).
- **Methodology (referenced, never copied):** `protocols/PLAYBOOK.md`, `protocols/ESSENTIALS.md`, `CLAUDE.md`.
- **Decision record:** `docs/decisions/ADR-82-*.md` (Proposed — Council-pending, ADR-41 routing).
- **Drift-check sources:** `scripts/validate_doc_claims.py` (#89), `scripts/validate_git_backlog.py` (#90),
  `ecosystem/disposition-register.yaml`.
- **Task spec:** `BACKLOG.md` — successor **#149**, origin **#148**.

---

## 4. Why the probes have teeth (the design rule — see `PROBES.md`)

A probe is teeth-bearing only when its answer **cannot be read off a compaction summary** —
it exists only in the live primary file/state at answer-time. Three conditions (§5):
**live-only answer**, **generator-excluded** (ship the question + locator + command, never
the answer), **CC-checkable** (CC re-derives read-only and compares). The operational rule
this residual follows: **no probe's exact value appears anywhere in this artifact.** The
drift headline above names *that* a claim is stale and *which* id is flagged, but not the
collected-count integers, not the closing-merge sha, not the freshness dates — so the only
way to answer a probe is to open the live source. That is the whole point.

---

## 5. Lean task-state (pointer, not narration)

The **BACKLOG is the spec; items are tickets** — `BACKLOG.md` is the source; do not
re-narrate IDs here. State at generation:

- **Live successor:** **#149** — promote HANDOFF_PROCESS v5 beta→canonical (gates:
  Council ratifies ADR-82 · one fresh-eyes beta→stable review · the empirical teeth
  dogfood this run rehearses). Origin **#148** shipped its (a)(b)(c) clauses as beta.
- **Live branches:** none in flight — this handoff's own branch is the only non-`main`
  and merges on completion (`git branch -v` to confirm at read-time).
- **Task-state drift-flag:** #77 — dispositioned voided closure (DRIFT-2 above); not work.
