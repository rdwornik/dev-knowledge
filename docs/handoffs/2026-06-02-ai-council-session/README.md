# Handoff — 2026-06-02-ai-council-session

**Generated by HANDOFF_PROCESS v4.3.1 (status: stable)**

**Repo:** ai-council  ·  **Type:** session  ·  **Generated:** 2026-06-02
**Target-repo state at generation:** `ai-council` branch `main`, HEAD `b4135e3`, clean
**Bundle host:** `.dev-knowledge` branch `chore/ai-council-handoff` (read-only on ai-council, ADR-36)

This bundle onboards a **new chat** to continue work on `ai-council` — the ecosystem's
multi-model debate/research CLI. It is a teaching sequence, not a file dump: the new chat
learns its role, our methodology, the project, what just happened, and what to do now —
then proves it understood before touching anything.

Files `01`–`07` are the teaching sequence (paste them to the new chat). This
`README.md` is for you only — do not paste it.

> **Cross-repo handoff.** The bundle lives in `.dev-knowledge/docs/handoffs/` but covers
> `ai-council` only (ADR-41). It was generated read-only against ai-council state (ADR-36);
> nothing was written to the ai-council tree.

## Paste sequence + escalation ladder

```
Step A: Paste 01_ROLE + 02_METHODOLOGY + 03_PROJECT + 04_RECENT + 05_NOW
        as ONE message into the new chat. Wait for acknowledgment.
Step B: Paste 06_QUESTIONS. Read the chat's answers.

IF comprehension FAILS:
  Tier 1 — Re-paste the specific file(s) the chat got wrong + "read again carefully".
  IF still fails:
  Tier 2 — Ask Claude Code to verify the specific facts against repo state;
           paste CC's findings to the new chat.
  IF still fails:
  Tier 3 — ABORT onboarding. Reactivate the sender chat, or do a manual context dump.

IF comprehension PASSES:
  Step C: Paste 07_ASK_BACK. Answer the chat's questions from memory or the sender chat.
  Step D: The chat begins work.
```

## Drift cross-check

**No drift detected on the four load-bearing claims** — unified-to-7-file-canonical,
Stage-1 merged, ADR-67 obligation tracked, and 17 ruff errors were each verified against
ai-council repo state at generation time. Full per-claim table with verification commands
is in `04_RECENT.md`. One **state-currency note** (not a contradiction): the 7-file unify
merge (`e91ba24` → `b4135e3`) landed in git **after** the JOURNAL's newest entry (the
2026-06-02 G1 audit), so the unify is not yet journaled — see `05_NOW` boundaries.

No source file was missing; no graceful-degradation omissions. `03_PROJECT` was built from
ai-council's own `VISION.md` + `CLAUDE.md` (present), not degraded.

## Bundle contents

| File | Purpose | Budget |
|---|---|---|
| `01_ROLE.md` | Who the new chat is; who Rob is | ≤100 |
| `02_METHODOLOGY.md` | How we work (PLAYBOOK pointers + ai-council enforcement) | ≤200 |
| `03_PROJECT.md` | Vision + sacred files (from ai-council VISION/CLAUDE) | ≤150 |
| `04_RECENT.md` | Narrative of recent work + four-tag + claims cross-check | ≤250 |
| `05_NOW.md` | Pending ADR-67 work + ruff debt + repo state | ≤100 |
| `06_QUESTIONS.md` | Comprehension check (5–7 Q) | ≤80 |
| `07_ASK_BACK.md` | New chat's question slot (max 3) | ≤50 |
