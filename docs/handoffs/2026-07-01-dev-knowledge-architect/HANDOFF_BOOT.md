# Handoff boot — session header + paste-pointer (architect mode)
<!-- scope: meta -->

| Field | Value |
|---|---|
| **Slug** | `2026-07-01-dev-knowledge-architect` |
| **Mode** | **architect** (v5.3 §13 — planning / way-of-working scope) |
| **Purpose** | **The deploy subsystem is BUILT + proven on n=1 (ai-council → v1.0.0) but NOT armed or disseminated — decide the arming + rollout sequencing.** The window since the 2026-06-27 architect handoff was a heavy **build + reconciliation** window: the entire ADR-91/92 `deploy/` subsystem was built end-to-end (2 ADRs ratified, orchestrator ASSESS+EXECUTE, 4 carriers) and **proven on the first real deploy** (ai-council ratified at methodology **v1.0.0**), surviving a CRLF blocker + a second encoding bug → a **full Windows-text-mode-git-I/O class sweep**; the **C5 loop close** landed PLAYBOOK §20 + the run-#1 retrospective (4 LESSONS). Then the corpus was **reconciled to that reality** (doc-only): the hub currency+coherence audit filed **#220–#229** + the **#131↔#215 SPLIT**, and this session's **pre-handoff capture DECIDED the floor-provisioning model (#226a = model A)** and filed the two missing gates — **#230** (end-to-end conformance self-test) + **#231** (consumer→hub feedback loop) — and **sequenced #221** (`depends-on #226`, #230 as the per-repo acceptance gate). So the next architect's center of gravity is **dissemination of a built-but-un-armed subsystem** — and the outgoing architect (folded `SUPPLEMENT.md`) has **resolved the top-level sequencing: arm-first**. The chain is **#226(b) (arm the floor carrier + arm ai-council) → #230 (the conformance self-test = #226's acceptance, incl. hermetic isolation + tamper-detection) → #222/#223 (document the *armed* system + fix the forced-freshness stamp) → #225 (surgical precommit) → #221 (fleet = leaf, LAST)** — ARCHITECTURE currency comes **second** (a map problem; function before map). So the incoming session is a **fix-pass with architect oversight**: execute the arm-first chain and settle the still-open *build* decisions (the hash-guard robustness bar; #230's tamper-test), **not** re-decide the priority. |
| **Generated at** | Bundle cut at HEAD `7cc4b6a`, **working tree clean**, `main` **in sync with `origin/main`** (the pre-handoff window is pushed). This bundle's own commit + the later `/ship` `--no-ff` merge move HEAD and push `main` **ahead of `origin` until pushed**. **Re-derive HEAD / sync at read-time** (`PROBES.md` P3) — do not trust this line. |

> **Generated LIVE, then FILLED by the outgoing architect.** This bundle was generated by **CC at the end of a CC-execution-driven window** (deploy build + audit reconciliation + pre-handoff capture); the operator then **FILLED `SUPPLEMENT.md`** from the outgoing architect chat, so its ANSWERS are folded into `PASTE_THIS.md` and carry this window's **off-repo strategic "why"** — the part the repo structurally cannot encode. The headline the supplement supplies: **"configured, not armed" is the load-bearing frame** — the registry says ai-council is deployed at v1.0.0 but **no gate fires and the floor does not auto-load**, so the deploy system is currently a **facade**; the architect's resolved priority is **arm-first** (`#226 → #230 → #222/#223 → #225 → #221`-fleet-last), with model **A endorsed but contingent on a robust, hard-to-bypass hash-guard** (see the folded SUPPLEMENT §A/§B). Because the supplement carries answers, the incoming **§13(d) operator-context beat NARROWS** to *"anything changed since the supplement was written?"* — **not** a FULL re-ask (`HANDOFF_PROCESS.md` §13: "refined, not duplicated"). The CC-derived repo-side facts are **four-tag** scoped in `RESIDUAL.md` (`recall`/`inferred` for the reconstructed window, `witnessed` for live re-derivation — re-check via `PROBES.md`); the **off-repo** *why* lives in the folded SUPPLEMENT.

> **Operator runbook is not copied here.** The stable who-each-file-is-for / run-loop / rationale lives **once** in the canonical per-repo runbook **`docs/handoffs/README.md`** — read it for the walkthrough. This file carries only the **session header** (above) + the **paste-pointer** (below). v5 bundles carry **no per-bundle README** (the 2026-06-12 canonical-runbook collapse — `HANDOFF_PROCESS.md` §13).

## What the operator does (paste-pointer)

1. Open a fresh Claude.ai chat.
2. Paste **`PASTE_THIS.md`** (in this bundle directory) — the browser role file + residual + probes + the (empty) supplement, assembled in order by `scripts/assemble_paste.py`. One paste; never hand-feed individual files to the file-less browser.
3. The browser replies with its on-load acknowledgment line (it names the Layer-1 actor); a partial or missing paste is then visible.
4. **Architect mode** — the browser uses the **generative / decompositional** posture (`protocols/HANDOFF_BOOT.md` §"Architect mode"): orient first, ask the operator for off-repo context, drive decomposition, hold the whole-system view, surface design tensions — **not** the reactive-filter default.
5. **Orient before any mechanism** — hand the browser `PROBES.md` **P1** (orientation) first; it replies `run <command>`, CC reads live `VISION.md` / `ARCHITECTURE.md` Ch1 and substring-checks. The architect may not begin design until it holds both orienting lines. (P1's grep is a *tool* that confirms the frame — the **backlog navigates**, v5.3 §13c.)
6. **Then the operator-context beat (§13d) NARROWS.** The supplement is **FILLED** (its ANSWERS are in the paste — north-star + Q1–Q6 + addenda A/B carry the arm-first priority, the floor-A hash-guard reservation, and the `#226→#230→#222/223→#225→#221` decomposition chain), so the browser asks only *"anything changed since the supplement was written?"* — **not** a FULL re-ask. (The load-bearing off-repo call is already answered: **arm-first, fleet-last** — the priority the repo's dependency graph does not encode.)
7. Hand `RESIDUAL.md` — **drift-flags first** (the headline this time: `ship-gate` is **GREEN** and **clean** — zero new WARN, no `[stale]`; the one real gap is the **TRACKED** ARCHITECTURE↔deploy currency debt #222/#223/#224) — then the **next-frontier decisions** (§4, led by the resolved **arm-first** chain **#226(b) → #230 → #222/223 → #225 → #221**-fleet-last + the still-open build decisions) + the **shipped-this-window** map (§2, the deploy build arc) + run the rest of `PROBES.md` (P2–P9). Any probe FAIL blocks onboarding (the escalation ladder).

## Why a pointer, not a copy

The methodology, the browser role, and the operator runbook all live in the repo and are referenced by **pointer**; CC (which holds the repo) serves any part the file-less browser needs just-in-time. `PASTE_THIS.md` is the assembled boot source (generated by `scripts/assemble_paste.py`, never hand-edited); `docs/handoffs/README.md` is the operator runbook. If a pointer and its source disagree, the source wins — fix the pointer or regenerate `PASTE_THIS.md`.
