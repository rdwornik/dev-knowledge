# Handoff boot — session header + paste-pointer (architect mode)
<!-- scope: meta -->

| Field | Value |
|---|---|
| **Slug** | `2026-06-21-dev-knowledge-architect` |
| **Mode** | **architect** (v5.2 §13 — planning / way-of-working scope) |
| **Purpose** | **Disposition + decide.** The window since the 2026-06-20 handoff was mostly **deliberation-and-build done, now landing on the operator's desk for decisions**: the **overnight 3-layer documentation audit** ran (PROCESS · TECHNICAL · OPS/TASK) and found every layer **substantially current** but parked a **stack of held judgment proposals** awaiting operator disposition (PROCESS B1–B5 + C1–C4 · TECHNICAL P1–P6 · OPS prune-set + LESSONS appends + a §12-condensation proposal). Builds also closed: **#199** (refscan precision) and **#196** (removal-closure spike — verdict **GO for D-2b, scoped**) are **done**; **#153** advanced (the `block-ff-push` pre-push gate shipped + activated — stays open for scope-boundary + reach). **§12 was condensed** (CLAUDE.md back under 200 lines). And the **keystone**: **ADR-89 OQ1 is now DECIDED** (doc→code edge granularity) — the 4-model Council recommended heading-level and the architect **ruled** on it (recorded in JOURNAL `1984812`): **uniform heading-level** edges with **explicit stable `{#id}` anchors → module/file targets** (**no clause/callable in v1** — the council's escape hatch was dropped), **de-hardcode the residue**, two validation classes (`broken_edge` hard-FAIL / `staleness_signal` advisory), **advisory-first**. The ADR file still reads **OQ1 open / Proposed** — the ruling is *recorded*, not yet *ratified*. So the next architect's keystone shifts from *decide* to **ratify the OQ1 ruling into ADR-89 + build #194**, alongside **disposing the audit queue** and **sequencing the remaining builds** — it is not itself a build session. |
| **Generated at** | Bundle first cut at HEAD `5b584ff`; **reconciled 2026-06-21** at HEAD `30a142e` after `main` advanced 4 JOURNAL-only commits (the state-hygiene wrap that recorded the OQ1 ruling) — those are now merged into this branch, so a later `/ship` is conflict-free. This bundle's own update commit + the `--no-ff` merge move HEAD; `main` runs ahead of `origin` until pushed. **Re-derive HEAD / sync at read-time** (`PROBES.md` P3). |

> **This bundle is COLD.** Generated by **CC at a cleared session start** (no outgoing browser architect in the loop), capturing the window since the 2026-06-20 handoff from JOURNAL + git + live state. `SUPPLEMENT.md` is committed with **empty ANSWERS** — the defined cold-handoff disposition (`HANDOFF_PROCESS.md` §13), **not** a missing deliverable. The §13(d) operator-context beat therefore fires **FULL** (nothing to narrow against). The strategic *why* of this window is **CC-derived and lives in `RESIDUAL.md`** (the audit-disposition queue + the open decisions + key rulings), since there was no browser-side deliberation to capture. Facts are **four-tag** scoped in `RESIDUAL.md`: recent-window facts reconstructed from JOURNAL/git are **`recall`/`inferred`**; everything CC re-derived live this generation is **`witnessed`** — re-derive the load-bearing ones via `PROBES.md`.

> **Operator runbook is not copied here.** The stable who-each-file-is-for / run-loop / rationale lives **once** in the canonical per-repo runbook **`docs/handoffs/README.md`** — read it for the walkthrough. This file carries only the **session header** (above) + the **paste-pointer** (below). v5 bundles carry **no per-bundle README** (the 2026-06-12 canonical-runbook collapse — `HANDOFF_PROCESS.md` §13).

## What the operator does (paste-pointer)

1. Open a fresh Claude.ai chat.
2. Paste **`PASTE_THIS.md`** (in this bundle directory) — the browser role file + residual + probes, assembled in order by `scripts/assemble_paste.py`. One paste; never hand-feed individual files to the file-less browser.
3. The browser replies with its on-load acknowledgment line (it names the Layer-1 actor); a partial or missing paste is then visible.
4. **Architect mode** — the browser uses the **generative / decompositional** posture (`protocols/HANDOFF_BOOT.md` §"Architect mode"): orient first, ask the operator for off-repo context, drive decomposition, hold the whole-system view, surface design tensions — **not** the reactive-filter default.
5. **Orient before any mechanism** — hand the browser `PROBES.md` **P1** (orientation) first; it replies `run <command>`, CC reads live `VISION.md` / `ARCHITECTURE.md` Ch1 and substring-checks. The architect may not begin design until it holds both orienting lines.
6. **Then the operator-context beat (§13d).** This bundle is COLD (empty ANSWERS), so the beat fires **FULL** — one off-repo ask: intent, priorities for this planning session, findings not in the repo, changed decisions.
7. Hand `RESIDUAL.md` — **drift-flags first** (the headline this time: `ship-gate` stays **GREEN** + **one new `[stale]` disposition to remove** — the §12-condensation cleared the WARN it covered) — then the **audit-disposition queue** (§3) + the **open decisions** (§4, led by the ADR-89 OQ1 heading-level ratification) + run the rest of `PROBES.md` (P2–P9). Any probe FAIL blocks onboarding (the escalation ladder).

## Why a pointer, not a copy

The methodology, the browser role, and the operator runbook all live in the repo and are referenced by **pointer**; CC (which holds the repo) serves any part the file-less browser needs just-in-time. `PASTE_THIS.md` is the assembled boot source (generated by `scripts/assemble_paste.py`, never hand-edited); `docs/handoffs/README.md` is the operator runbook. If a pointer and its source disagree, the source wins — fix the pointer or regenerate `PASTE_THIS.md`.
