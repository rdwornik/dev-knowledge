# Residual — 2026-06-21 architect handoff (the part the repo does not already encode)

<!-- scope: meta -->

> **What this is (v5 §2).** CC's handoff is **only the residual** — the un-committed "why," the
> pointers, and the **drift-flags as the headline**. It does **not** re-transmit methodology
> (pointer + mechanical enforcement, §3) or re-narrate task-state (pointer to `BACKLOG.md`, §6).
> This is **architect mode** (§13): the residual is scoped to the *planning* "why," task-state
> points at the whole BACKLOG / relevant themes, and the open architecture questions travel as
> residual so the next session resumes the design rather than rediscovering it.
>
> **COLD handoff** — generated by CC at a **cleared session start** (no outgoing browser
> architect). The strategic *why* of this window is CC-derived (from JOURNAL + git + live state)
> and lives in §3–§5 below; `SUPPLEMENT.md` is committed **empty** (the §13 cold disposition), so
> the §13(d) operator-context beat fires **FULL**.
>
> **Four-tag discipline.** `witnessed` = CC re-derived it live this generation (the §1/§2 gate
> outputs); `recall`/`inferred` = reconstructed from the JOURNAL/git window, may have moved —
> the load-bearing ones are re-checkable via `PROBES.md`. `unknown` = stated as such.

---

## §1 — Drift-flags (THE HEADLINE — surfaced first, not buried)

Produced by the live read-only drift-checks (`audit.py health` / `ship-gate`,
`validate_git_backlog`, `validate_doc_claims`, `validate_backlog`) at generation (**witnessed**).
**Re-derive each at read-time** — the teeth are in `PROBES.md`, not in trusting these lines.

### ✅ HEADLINE — `ship-gate` stays GREEN; the window introduced **one new stale disposition**

`python scripts/audit.py ship-gate` → **GREEN** (**7 WARN dispositioned**) — verification organs
green against this arc. **New this window** (the only state change at the gate boundary):

- **`[stale] disposition warn-doc-rot-claude-section-history matched no live WARN — review/remove`.**
  The §12 condensation (`29b6ac6`/`f2bbb64`) cleared the CLAUDE.md section-history `doc_rot` WARN
  this disposition was covering, so the register entry is now **decoration** (ADR-75 decoration
  rule → remove it). **Benign** (ship-gate still GREEN; a stale-disposition is a notice, not a
  block), but it is the one register-hygiene item this window created. **Fileable micro-cleanup**
  — surfaced, not done here.

### Other flags (benign / dispositioned — unchanged from the 2026-06-20 bundle)

- `git_backlog_drift`: **#77** `[~~]` — the known voided-closure false positive (`77e5d7df9`
  closes #77 but #77 is still in BACKLOG by design). Dispositioned (`warn-77-voided-closure`).
  **Do not touch** (standing false positive).
- `no_ff_merges`: the two **2026-06-19** wrap commits (`3a894ee` journal-wrap, `d0f9ead`
  transcript-archive) still print `[~~]` under `health` (health does not apply the disposition
  register — it gates each *commit*, FAIL-only) but are `[disp]` under `ship-gate`. **Expected
  seam, not a regression** — `ship-gate` is the arc gate and it is GREEN.
- `doc_rot`: history-accretion `[~~]` on BACKLOG **#164 / #77 / #134 / #10**. All dispositioned
  (`warn-doc-rot-*`). Grooming candidates (#140 family), not blockers.

### Surfaced this window — non-blocking, for the architect (NOT gated, NOT fixed here)

These are the **held audit proposals** — the bulk of the next session's actionable surface. They
are documented in three findings docs (pointers in §6); the disposition queue is §3-C.

- **PROCESS-layer audit** (`docs/audits/2026-06-21-audit-process-findings.md`): 1 auto-fix landed
  (stale "ruff not wired" claim corrected — ruff shipped #13); **B1–B5** held (review-command
  graduated-rule reconcile · SESSION_SETUP v4-mechanics caveat now false · PLAYBOOK "8 sections"
  predates ADR-87 · a `max` effort tier exists but is absent from the effort tables · §7b
  command-examples incomplete) + **C1–C4** cross-domain flags (ENVIRONMENT.md broadly stale —
  dead `/boot`/`/evolve` refs, model list omits Fable 5, CC version stale · Fable 5 absent from
  the t-shirt routing doctrine · `verify`-skill description vs live cadence skill · `~/.claude/`
  evolution hooks).
- **TECHNICAL-layer audit** (`docs/audits/2026-06-21-audit-technical-findings.md`): 1 auto-fix
  landed (§Validators gate list 9→10, added `block-ff-push` + restamp); **P1–P6** held — **P1 is
  the sharp one:** ADR-82's header is frozen `Proposed` (its own immutability note) yet
  HANDOFF_PROCESS v5.2 has been **canonical since 2026-06-11** (Council gate **waived** #149), so
  the README "Council-pending" label is **flatly false** and the frozen-header reading is
  inconsistent with the in-place markers on ADR-52/45/46. P2 README missing ADR-73–80 rows · P3
  ADR-89 OQ3 cross-ref · P4 §Validators omits `validate_reconciliation.py` · P5 ratification
  readiness · P6 organ-map (no restructure).
- **OPS/TASK-layer audit** (`docs/audits/2026-06-21-audit-ops-findings.md`): 2 auto-fixes landed
  (CLAUDE §11 ADR-list rotation 76–80→85–89 + §7 command currency; CONTRIBUTING Validators table
  8→10); the **centerpiece is a decision-ready BACKLOG prune proposal** (no item removed — the
  no-remove invariant held): 4 `doc_rot` condense candidates (#164/#134/#77/#10), a verify-and-
  close set (**#10** substantially done; **#47** absent from the ecosystem registry; **#35**
  VISION-part moot), aging decision-items (#42/#37/#39/#67), an overlap call (#169↔#171), + **3
  proposed LESSONS** + the CLAUDE §12 condensation (now **done** this window) + cross-domain flags
  (`protocols/ENVIRONMENT.md` badly stale — confirms #71/#119 open).

- **CLAUDE.md §11/§12 currency is now CURRENT** (the twice-deferred groom landed this window): §11
  rotated to ADR-85–89, §12 condensed, `last_reviewed` re-stamped. The 2026-06-20 bundle's
  "CLAUDE §11 stale" surfaced-item is **resolved** — do not re-raise it.

---

## §2 — Shipped / closed this window (verified live where it is live state)

The window since the 2026-06-20 handoff (`dd98524` → `5b584ff`). **`recall` from JOURNAL/git
unless marked witnessed.**

| Item | What landed | Status | Evidence |
|---|---|---|---|
| **#199** | Refscan precision — prune immutable zones + gitignored scratch from `scan_undeclared_edges.py` (245/104 → 12/4 candidates) | **CLOSED** (`f201185`) | gone from BACKLOG (**witnessed**: `validate_git_backlog` clean of it) |
| **#196** | Removal-closure computation spike — design proposal + feasibility verdict | **CLOSED** (`468ab1a`); verdict **GO for D-2b, scoped** (direct cross-kind reverse-dep *boundary*, not a transitive walk; 3 mechanisms M1/M2/M3, no M4; fail-closed; computed-leg advisory-first) | `docs/audits/2026-06-20-removal-closure-spike-findings.md` |
| **#153** | `block-ff-push` pre-push gate — the PREVENT half of core-invariant #5 (refuses a non-merge commit on `main`'s first-parent spine; HUB-only; fail-soft; `--no-verify` bypass) | **ADVANCED, NOT closed** — scope-boundary + #189 methodology-reach + server-side teeth remain | `scripts/block_ff_push.py`; +21 tests; gate roster now **10/10** (**witnessed**) |
| **CLAUDE.md §12** | Section-history condensed (v1.0–v2.17 → git-log pointer) → file back under 200 lines | **DONE** | `29b6ac6` (left the `[stale]` disposition in §1) |
| **ADR-89 OQ1** | 4-model Council debate (doc→code edge granularity) ran; transcript archived | **DEBATED, NOT ratified** — see §4.1 (the keystone) | `docs/decisions/transcripts/council-out-20260621_003655-...-adr89-oq1-doc-code-granularity.md` |
| **3-layer audit** | PROCESS + TECHNICAL + OPS/TASK doc layers audited; each substantially current; proposals held | **PROPOSALS HELD** (see §1 / §3-C) | three `docs/audits/2026-06-21-audit-*-findings.md` |

**Green at generation (witnessed):** `pytest` **772 collected** · `ruff` clean · `audit.py
health` **OK** · **`ship-gate` GREEN** (7 WARN dispositioned + 1 stale) · `validate_backlog` OK
(**7 themes, 22 stories, 84 tasks**) · `doc_claims` **4/4** (audit checks **22/22**, pre-commit
hooks **10/10** + roster, `pytest_collected` **772/772**) · `validate_git_backlog` → only #77.
(All are `PROBES.md` teeth — re-derive at read-time.)

**ADR statuses — witnessed live:**
- **ADR-88** (file-oriented dependency management) — **Proposed**, OQ1–OQ4 all open.
- **ADR-89** (computed code-dependency edges) — **Proposed**. **OQ2 RESOLVED (#193)** in-file; **OQ1
  + OQ3 remain open → stays Proposed** (the ADR file still reads "OQ1 and OQ3 remain open").

---

## §3 — Next-ship action plan (the decision queue dominates this session)

This is **not** a build session. The new center of gravity is **disposition** (§3-C). The build
sequencing (§3-A/B) carries forward from the 2026-06-20 plan, updated for what closed.

### 3-A — What the 2026-06-20 plan said, reconciled

- **#199 → DONE.** **#196 → DONE** (spike; GO for D-2b). Remove both from the live plan.
- **#195-2b is now UNBLOCKED** — its gate (#196 closure spike) landed. The spike's framing
  refined #196's "traverse uniformly" → **"direct-boundary union"** (carried into #195-2b text;
  verify `da2723f`). Buildable as a **computed boundary-check, advisory-first**.
- **#194 is now DECISION-READY** — the council recommended its gating granularity (§4.1). Ratify
  ADR-89 OQ1, then #194 builds.
- **#153 advanced** (gate shipped + activated). Stays open for the scope-boundary + reach work.

### 3-B — Updated build/decision table

Status legend: **READY** = buildable now · **DECISION** = an architect/Council call gates it ·
**GATED** = blocked on another item. Parallel-safety is **derived** from the #156 durable
task-graph (no shared `serialize-group` + no `depends-on` path) — **witnessed groups**:
`code-edge` = **#194/#195**, `coherence` = **#180/#181/#182** (#199 left the group when it closed).

| # | What | Status | Gate / blocker | Notes |
|---|---|---|---|---|
| **ADR-89 OQ1** | Ratify doc→code edge granularity | **DECISION — the keystone** | Council ran; recommends **heading-level** (§4.1). Architect ratifies into the ADR | unblocks #194 |
| **#195-2a** | Safe-removal gate **phase 2a** (deletion ⇒ reverse-dep check + log entry) | **READY — top build** | none: the oracle (#193) shipped | `code-edge` group |
| **#194** | Track B — doc→code declared edge (rule-IDs + structural integrity) | **DECISION→READY** | ADR-89 OQ1 ratification (above); then build **heading-level** | `code-edge` group |
| **#195-2b** | Safe-removal gate **phase 2b** (full closure-gate) | **READY (newly unblocked)** | was gated on #196 — now done; build the direct-boundary check advisory-first | `code-edge` group |
| **#153** | FF-prevention scope-boundary + methodology-reach + server-side teeth | **READY — assess priority** | none (the local gate shipped) | `audit-py` group |
| **ADR-89 ratification** | Proposed → Accepted | **GATED (decision)** | OQ1 (ratify §4.1) + OQ3 (advisory→gate, data-gated). OQ2 resolved | — |
| **ADR-88 ratification** | Proposed → Accepted | **GATED (decision)** | OQ1–OQ4. At ratification add the ADR-88→ADR-89 back-link (deferred per immutability; the forward link exists) | ratify *with* ADR-89 so the back-link lands clean |

### 3-C — Audit-disposition queue (the headline work — operator-gated approvals)

The 3-layer audit returned **decision-ready** proposals. None were applied (judgment-shaped). The
next session's main job is to **approve/decline each, then CC applies the approved ones**:

1. **PROCESS B1–B5 + C1–C4** — `2026-06-21-audit-process-findings.md`. (C1 ENVIRONMENT.md is the
   big cross-domain one; B-items are mechanical once approved.)
2. **TECHNICAL P1–P6** — `2026-06-21-audit-technical-findings.md`. **P1 (ADR-82 status legibility
   / README "Council-pending" false) is the load-bearing one** — it is a *correctness* fix, not a
   groom.
3. **OPS prune-set + 3 LESSONS + cross-domain** — `2026-06-21-audit-ops-findings.md`. The prune
   proposal is a **removal** set → strictly operator-gated (no-remove invariant). Verify-and-close
   candidates (**#10**, **#47**, **#35**) and the #169↔#171 overlap call live here.
4. **Register hygiene** — remove the stale `warn-doc-rot-claude-section-history` disposition (§1).

---

## §4 — Open decisions the next session must make

1. **ADR-89 OQ1 — doc→code edge granularity (THE KEYSTONE).** The 4-model Council
   (claude-opus-4-7 · deepseek-v4-pro · gemini-3.1-pro · grok-4.3; openai synthesizer) **converged
   on HEADING-level** as the uniform default, with a **narrow, three-gate escape hatch to
   clause-level** (1:1 to a single named callable AND low-churn module AND actionably more precise),
   linter-enforced + soft-capped at ~10–15% of edges; **plus de-hardden first** — exclude any
   coupling the code can read directly, declare only the residue. **This REVERSES the prior
   architect lean** (the 2026-06-20 bundle recorded "clause-level, UNCONFIRMED"). Several panelists
   would drop the escape hatch entirely; the synthesizer kept it gated. **The architect's call:**
   ratify heading-level (with or without the gated clause-level hatch) into ADR-89, which then sets
   #194's build granularity and unblocks it. The transcript also flags the **weakest assumption**
   (the escape hatch metastasizes under self-policing) — decide whether to ship the hatch or start
   hatch-less.
2. **ADR-89 ratification readiness** — needs OQ1 (decide above) + OQ3 (advisory→gate; **data-gated**
   — the #196 spike ruled computed legs *eligible* to gate hard once data justifies, declared legs
   stay advisory). OQ2 resolved (#193).
3. **ADR-88 ratification readiness** — OQ1–OQ4. Both ADRs' **cores are empirically proven** (shipped
   organs: oracle #193, scan #179, linter #192); blockers are forward/pilot/data-gated OQs (technical
   audit P5). Ratify ADR-88 **with** ADR-89 so the deferred ADR-88→ADR-89 back-link lands cleanly.
4. **ADR-82 status representation (technical audit P1)** — the README "Council-pending" is false
   (gate waived #149; v5.2 canonical since 2026-06-11). Decide the fix: in-place status marker vs
   header-amendment vs README-label correction — and reconcile with ADR-82's own frozen-header note.
5. **#153 priority + scope** — the local pre-push gate shipped, but the `--no-ff` **scope boundary**
   (hub vs `~/.claude` vs child repos), the **#189 methodology-reach**, and **server-side teeth**
   are unsettled. Decide whether to invest now or hold.
6. **The audit prune-set (ops findings §3)** — operator owns every removal. Decide #10/#47/#35
   verify-and-close, the #164/#134/#77/#10 doc_rot condensations, and the #169↔#171 overlap.
7. **Conformance dashboard (#171)** — still deferred. The #169 ungated-doc staleness signal rides
   it; the ops audit flagged the #169↔#171 overlap. Decide if this window changes its priority.

---

## §5 — Key facts from THIS window (so they aren't re-litigated)

1. **ADR-89 OQ1 was debated, NOT decided.** The transcript is a *recommendation* (heading-level);
   the ADR still reads OQ1 open / Proposed. Do not treat the council output as ratified — the
   architect ratifies it (§4.1).
2. **The prior "clause-level" lean is superseded by the council's heading-level recommendation.**
   Read the 2026-06-20 RESIDUAL's "clause-level UNCONFIRMED" as **generation-time history**.
3. **#196 verdict is GO-for-D-2b, scoped** — a removal gate needs a **direct cross-kind reverse-dep
   boundary**, not a transitive walk; 3 mechanisms (M1 computed code→code / M2 computed path+dotted
   refs / M3 declared prose, human-confirmed); **no M4**; fail-closed on partial/unavailable/dirty;
   computed legs advisory-first. `_SPEC_REGISTRY`-row removal is the oracle-blind worst case → human.
4. **The 3-layer audit verdict is "substantially current."** The held proposals are *grooming +
   correctness*, not a structural problem. The one *correctness* item is technical-audit **P1**
   (ADR-82/README status), not a groom.
5. **CLAUDE.md is current** (§11 rotated, §12 condensed, restamped). ENVIRONMENT.md is **not** —
   confirmed badly stale by two audits (dead `/boot`/`/evolve` refs, model list, CC version);
   confirms #71/#119 open. It is a **cross-domain runtime snapshot** — half-freshening it is worse
   than flagging; it is owned by the runtime layer, not patched mid-audit.
6. **Organ registration is by design** (carried, don't re-derive): `reverse_dep_oracle.py` (query
   tool) + `scan_undeclared_edges.py` (awareness scan, writes nothing / exits 0) are **deliberately
   NOT** in `ALL_CHECKS`; only `validate_doc_structure.py` is a check (#22). PLAYBOOK/ESSENTIALS
   **correctly omit** the hub edge-model organ (#77 doctrine — confirmed by the process audit).

---

## §6 — Pointers (read the source, never a copy)

- **Audit findings (the disposition queue):** `docs/audits/2026-06-21-audit-process-findings.md`,
  `…-audit-technical-findings.md`, `…-audit-ops-findings.md`.
- **The keystone transcript:**
  `docs/decisions/transcripts/council-out-20260621_003655-pick-council-decision-adr89-oq1-doc-code-granularity.md`
  (read the **Recommendation** + **Decision Criteria** sections; OpenAI synthesizer at the tail).
- **Plan / task-graph:** `BACKLOG.md` (story-map; `code-edge` = #194/#195, `coherence` =
  #180/#181/#182). Schema machine-checked by `scripts/validate_backlog.py`.
- **Doctrine:** `docs/decisions/ADR-88-file-oriented-dependency-management.md` (declared edge),
  `docs/decisions/ADR-89-computed-code-dependency-edges.md` (computed edge + the OQ list).
- **#196 spike:** `docs/audits/2026-06-20-removal-closure-spike-findings.md`.
- **Organ map / inventory:** `ARCHITECTURE.md` Ch2 (organ map) + §Validators (the `scripts/`
  inventory; gate roster now 10).
- **This window's commits:** `git log --oneline dd98524..HEAD` (audit merges, #199/#196 closes,
  #153 gate, §12 condense, ADR-89 OQ1 transcript archive).
- **Prior context:** `docs/handoffs/2026-06-20-dev-knowledge-architect/` (the session-wrap audit
  bundle whose next-ship plan this window executed).
