# Handoff — 2026-05-31-dev-knowledge-session-2

**Generated by HANDOFF_PROCESS v4.3.1 (status: stable)**

**Repo:** .dev-knowledge  ·  **Type:** session  ·  **Generated:** 2026-05-31
**Repo state at generation:** branch `docs/handoff-session-2-2026-05-31`, HEAD `a21e355`

This bundle onboards a **new chat** to continue work on `.dev-knowledge`. It is a
teaching sequence, not a file dump: the new chat learns its role, our methodology,
the project, what just happened, and what to do now — then proves it understood
before touching anything.

Files `01`–`07` are the teaching sequence (paste them to the new chat). This
`README.md` is for you only — do not paste it.

> **Slug note:** this is `-session-2` because the morning bundle already holds
> `2026-05-31-dev-knowledge-session`. The skill's Case-4 matrix auto-picked the
> counter-suffix — it's differentiation, not a retry.

## Paste sequence + escalation ladder

```
Step A: Paste 01_ROLE + 02_METHODOLOGY + 03_PROJECT + 04_RECENT + 05_NOW
        as ONE message into the new chat. Wait for acknowledgment.
Step B: Paste 06_QUESTIONS. Read the chat's answers.

IF comprehension FAILS:
  Tier 1 — Re-paste the specific file(s) the chat got wrong + "read again carefully".
  IF still fails:
  Tier 2 — Ask Claude Code to verify the specific facts against repo state;
           paste CC's findings to the new chat.
  IF still fails:
  Tier 3 — ABORT onboarding. Reactivate the sender chat, or do a manual context dump.

IF comprehension PASSES:
  Step C: Paste 07_ASK_BACK. Answer the chat's questions from memory or the sender chat.
  Step D: The chat begins work.
```

## Drift cross-check

**Minor drift detected — both benign, neither blocks next-session work.** Full
per-claim table with verification commands is in `04_RECENT.md`.

1. **HEAD**: sender captured `f7de267`; actual at Phase 2 is `a21e355`. Expected —
   the Phase-1 interview commit advanced HEAD by one. No semantic drift.
2. **"63 ADRs"**: actual is **36 ADR files** numbered up to **ADR-63** (number gaps
   from supersessions/archival). The sender's "63" is the highest ADR *number*, not
   the file count. Cite ADR *numbers*, not a "63 decisions" total.
3. **LESSONS "50 entries"**: not independently verified — LESSONS uses pipe-delimited
   single-line entries, so a header-count probe is inconclusive. Treat as `inferred`.

No source file was missing; no graceful-degradation omissions.

## Bundle contents

| File | Purpose | Budget |
|---|---|---|
| `01_ROLE.md` | Who the new chat is; who Rob is | ≤100 |
| `02_METHODOLOGY.md` | How we work (PLAYBOOK pointers + key extracts) | ≤200 |
| `03_PROJECT.md` | Vision + sacred files + ecosystem-folder state | ≤150 |
| `04_RECENT.md` | Narrative of recent work + four-tag + claims cross-check | ≤250 |
| `05_NOW.md` | Top P1s + in-progress branches + universalization roadmap | ≤100 |
| `06_QUESTIONS.md` | Comprehension check (5–7 Q) | ≤80 |
| `07_ASK_BACK.md` | New chat's question slot (max 3) | ≤50 |
