# Handoff boot — session header + paste-pointer (architect mode)
<!-- scope: meta -->

| Field | Value |
|---|---|
| **Slug** | `2026-06-25-dev-knowledge-architect` |
| **Mode** | **architect** (v5.3 §13 — planning / way-of-working scope) |
| **Purpose** | **Dispose two fresh audits + sequence the next builds.** The window since the 2026-06-21 architect handoff was a heavy **execution** window that **drained that bundle's queue**: ADR-88/89 ratified **Accepted** (`911b561`/`38cb663`, in-place markers); the keystone — **ADR-89 OQ1 ratified + #194 doc→code edge BUILT** (now a live `doc_code_edge` check, 5 edges resolved, advisory-first); the **handoff-rework agenda landed** (A1–A4 resident Architect Operating Contract + gate-map + role→vision→backlog reorder; C1–C6 verified-refuted-removal → condense-only; **HANDOFF_PROCESS v5.2→v5.3** §5 probe consolidation); **PLAYBOOK restructured** to the two-part heading scheme (Part I Reference Ch1–14 / Part II Workflows 1–19); **#200** (merge-serialization), **#10**, **#184** (architect/CC equilibrium contract — empirically closed) closed + **#134 n=2 groom** (84→81; removed #198/#143/#119). The window **closed with two fresh read-only audits that REPLACE the old disposition queue**: **Audit-A** (dependency-architecture + test-coverage) and **Audit-B** (process/trigger/usage), both report-only under `docs/audits/2026-06-25-*`. So the next architect's center of gravity shifts: the old keystone is **done**; the new work is **(1) dispose Audit-A + Audit-B into a sequenced build plan**, and **(2) decide the new keystone — the computed/discovery-edge frontier.** Audit-A's verdict: the **declared-edge half is production-grade** (`reconciled_with` @5.3 + doc→code rule-IDs, both genuinely tested); the **computed/discovery half is unenforced** — the code↔code import graph among the flat `scripts/*.py` is policed by an ~empty codemap, and the **Pyright reverse-dep oracle is PROVEN (122 deps live) but consumed by NO gate** (GAP-1 / #195). This is a **disposition + sequencing** session, not itself a build. |
| **Generated at** | Bundle cut at HEAD `0f1fe07`, **working tree clean**, `main` **ahead of `origin` by 3** (the unpushed window). This bundle's own commit + the later `/ship` `--no-ff` merge move HEAD and push `main` further ahead of `origin` until pushed. **Re-derive HEAD / sync at read-time** (`PROBES.md` P3) — do not trust this line. |

> **This bundle is COLD.** Generated by **CC at a fresh session start** (no outgoing browser architect in the loop), capturing the window since the 2026-06-21 handoff from JOURNAL + git + live state. `SUPPLEMENT.md` is committed with **empty ANSWERS** — the defined cold-handoff disposition (`HANDOFF_PROCESS.md` §13), **not** a missing deliverable. The §13(d) operator-context beat therefore fires **FULL** (nothing to narrow against). The strategic *why* of this window is **CC-derived and lives in `RESIDUAL.md`** (the audit-disposition queue + the open decisions + key rulings), since there was no browser-side deliberation to capture. Facts are **four-tag** scoped in `RESIDUAL.md`: recent-window facts reconstructed from JOURNAL/git are **`recall`/`inferred`**; everything CC re-derived live this generation is **`witnessed`** — re-derive the load-bearing ones via `PROBES.md`.

> **Operator runbook is not copied here.** The stable who-each-file-is-for / run-loop / rationale lives **once** in the canonical per-repo runbook **`docs/handoffs/README.md`** — read it for the walkthrough. This file carries only the **session header** (above) + the **paste-pointer** (below). v5 bundles carry **no per-bundle README** (the 2026-06-12 canonical-runbook collapse — `HANDOFF_PROCESS.md` §13).

## What the operator does (paste-pointer)

1. Open a fresh Claude.ai chat.
2. Paste **`PASTE_THIS.md`** (in this bundle directory) — the browser role file + residual + probes, assembled in order by `scripts/assemble_paste.py`. One paste; never hand-feed individual files to the file-less browser.
3. The browser replies with its on-load acknowledgment line (it names the Layer-1 actor); a partial or missing paste is then visible.
4. **Architect mode** — the browser uses the **generative / decompositional** posture (`protocols/HANDOFF_BOOT.md` §"Architect mode"): orient first, ask the operator for off-repo context, drive decomposition, hold the whole-system view, surface design tensions — **not** the reactive-filter default.
5. **Orient before any mechanism** — hand the browser `PROBES.md` **P1** (orientation) first; it replies `run <command>`, CC reads live `VISION.md` / `ARCHITECTURE.md` Ch1 and substring-checks. The architect may not begin design until it holds both orienting lines. (P1's grep is a *tool* that confirms the frame — the **backlog navigates**, v5.3 §13c.)
6. **Then the operator-context beat (§13d).** This bundle is COLD (empty ANSWERS), so the beat fires **FULL** — one off-repo ask: intent, priorities for this planning session, findings not in the repo, changed decisions.
7. Hand `RESIDUAL.md` — **drift-flags first** (the headline this time: `ship-gate` stays **GREEN** + **clean** — the window added **zero** new WARN and the prior bundle's `[stale]` disposition was already cleared by the #134 groom; the one open drift is **#204**, filed-not-fixed) — then the **audit-disposition queue** (§3, led by Audit-A's computed-edge frontier) + the **open decisions** (§4) + run the rest of `PROBES.md` (P2–P9). Any probe FAIL blocks onboarding (the escalation ladder).

## Why a pointer, not a copy

The methodology, the browser role, and the operator runbook all live in the repo and are referenced by **pointer**; CC (which holds the repo) serves any part the file-less browser needs just-in-time. `PASTE_THIS.md` is the assembled boot source (generated by `scripts/assemble_paste.py`, never hand-edited); `docs/handoffs/README.md` is the operator runbook. If a pointer and its source disagree, the source wins — fix the pointer or regenerate `PASTE_THIS.md`.
