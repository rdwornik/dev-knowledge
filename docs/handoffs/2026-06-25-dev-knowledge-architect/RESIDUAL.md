# Residual — 2026-06-25 architect handoff (the part the repo does not already encode)

<!-- scope: meta -->

> **What this is (v5 §2).** CC's handoff is **only the residual** — the un-committed "why," the
> pointers, and the **drift-flags as the headline**. It does **not** re-transmit methodology
> (pointer + mechanical enforcement, §3) or re-narrate task-state (pointer to `BACKLOG.md`, §6).
> This is **architect mode** (§13): the residual is scoped to the *planning* "why," task-state
> points at the whole BACKLOG / relevant themes, and the open architecture questions travel as
> residual so the next session resumes the design rather than rediscovering it.
>
> **Generated COLD, then FILLED** — generated by CC at a **fresh session start** (no outgoing
> architect at generation); the operator then **FILLED `SUPPLEMENT.md`** from the outgoing chat(s).
> The CC-derived **repo-side** *why* of this window lives in §3–§5 below; the **off-repo** *why*
> (intent, priorities, tensions, decomposition rationale — what the repo structurally cannot encode)
> is in the folded `SUPPLEMENT.md` ANSWERS. Because the supplement carries answers, the §13(d)
> operator-context beat **narrows** to *"anything changed since the supplement was written?"* — not
> a FULL re-ask (§13(d): "refined, not duplicated").
>
> **Four-tag discipline.** `witnessed` = CC re-derived it live this generation (the §1/§2 gate
> outputs); `recall`/`inferred` = reconstructed from the JOURNAL/git window, may have moved —
> the load-bearing ones are re-checkable via `PROBES.md`. `unknown` = stated as such.

---

## §1 — Drift-flags (THE HEADLINE — surfaced first, not buried)

Produced by the live read-only drift-checks (`audit.py health` / `ship-gate`,
`validate_git_backlog`, `validate_doc_claims`, `validate_backlog`) at generation (**witnessed**).
**Re-derive each at read-time** — the teeth are in `PROBES.md`, not in trusting these lines.

### ✅ HEADLINE — `ship-gate` is GREEN and **CLEAN**; the window added **zero** new drift

`python scripts/audit.py ship-gate` → **GREEN** (**3 WARN dispositioned**) — verification organs
green against this arc. **Unlike the 2026-06-21 bundle, there is NO new stale disposition and NO
new WARN this window.** The previous bundle's `[stale] warn-doc-rot-claude-section-history` was
**cleared** (the #134 groom removed it + 2 other now-empty `warn-doc-rot-backlog-*` grandfather
dispositions, ADR-75). This was a build/cleanup window that *drained* drift, it did not create it.

### One open drift — **filed, not fixed** (the only actionable doc-drift)

- **`#204` — `CONTRIBUTING.md` "Nightly outcome management" describes the superseded model.** It
  still says nightly outcomes **squash-merge to `main`**; ADR-84 + `ARCHITECTURE.md` Ch3/Ch6 say
  the live model **diverts to `automation/conformance-digest` and closes** (no merge to `main`).
  Surfaced + **filed during the v5.3 bump's freshness re-read**, deliberately **not fixed** in
  that arc. Awaits a canonical-file-integrity groom (`Canonical-file integrity` theme).

### Standing flags (benign / dispositioned — unchanged, do NOT touch)

- `git_backlog_drift`: **#77** `[~~]` — the known voided-closure false positive (`77e5d7df9`
  closes #77 but #77 is still in BACKLOG by design). Dispositioned (`warn-77-voided-closure`).
  **Standing false positive — do not touch.**
- `no_ff_merges`: the two **2026-06-19** wrap commits (`3a894ee` journal-wrap, `d0f9ead`
  transcript-archive) print `[~~]` under `health` (health does not apply the register, FAIL-only)
  but are `[disp]` under `ship-gate`. **Expected seam, not a regression** — `ship-gate` is GREEN.

### Known doc-legibility item (NOT new — carried; surfaced for the disposition queue)

- **ADR-88 / ADR-89 status legibility — the ADR-82 class.** Both are **Accepted** (ratified
  2026-06-21, `911b561`, via an in-place `## Amendment — 2026-06-21: Accepted` marker + the README
  ADR-index dropping the `Proposed —` prefix per the status-prefix convention). But each ADR's
  **frozen header `**Status:** Proposed` (line 5) still reads Proposed** — the same
  header-vs-marker split the 2026-06-21 technical audit flagged for **ADR-82 P1**. Not gate-failing
  (no check reads the header), but it is a real legibility item for the corpus-coherence groom.
  **Do not "fix" by editing the frozen header** without deciding the convention first (§4.4).

### Surfaced this window — the disposition queue (NOT gated, NOT fixed here)

The bulk of the next session's actionable surface is **two fresh read-only audits** delivered at
the close of this window. They REPLACE the 2026-06-21 three-layer audit queue (which was disposed
+ executed this window). Pointers in §6; the queue itself is §3-C.

- **Audit-A — dependency-architecture + test-coverage** (`docs/audits/2026-06-25-dependency-architecture-coverage-audit.md`):
  full MATRIX across the 4 edge-types, per-mechanism live-run verification, a test-coverage table,
  and a ranked test-gap backlog. **Verdict: the declared-edge half is production-grade; the
  computed/discovery half is the frontier.** Report-only — nothing implemented.
- **Audit-B — process / trigger / usage** (`docs/audits/2026-06-25-process-trigger-usage-audit.md`):
  the process/automation organs vs their triggers + actual invocation. Report-only.

---

## §2 — Shipped / closed this window (verified live where it is live state)

The window since the **2026-06-21** architect handoff (`ad6d17e` add-SHA → HEAD `0f1fe07`). A heavy
execution window that drained the prior bundle's queue. **`recall` from JOURNAL/git unless marked
witnessed.**

| Item | What landed | Status | Evidence |
|---|---|---|---|
| **ADR-88 / ADR-89** | Both ratified **Proposed → Accepted** (operator ratification) | **ACCEPTED** (in-place markers; README index prefix dropped) | `911b561` / `38cb663`; `## Amendment — 2026-06-21: Accepted` in each ADR (witnessed) |
| **ADR-89 OQ1 + #194** | The keystone — OQ1 ratified (heading-level, `{#id}`→module/file, advisory-first) **and #194 doc→code declared-edge BUILT** across ~10 arcs (Phase-0 → Arc-1 coverage gate → structural-integrity L1 + rebuildable index) | **#194 BUILT** — now a live `audit.py` check (`doc_code_edge`, #23) | `cf2d858` (L1) + the `feat/194-*` arc; **witnessed**: `ship-gate` shows `doc_code_edge: 5 edges resolved` |
| **Handoff-rework** | A1–A4 (resident **Architect Operating Contract** in `HANDOFF_BOOT.md`: operating loop + gate-map + delivery-lifecycle + premises self-check; A2 test-first acceptance contract → PLAYBOOK Ch12.1 + ADR-81; A3/C3 role→vision→backlog reorder) + C1–C6 (**removal premises verified-REFUTED** → condense-only) + **HANDOFF_PROCESS v5.2 → v5.3** (§5 probe 6→5 consolidation) | **LANDED** (agenda items advanced; **#161/#164/#144/#145 advanced, NOT closed**) | the `docs/a*` + `docs/c*` + `docs/handoff-*` merges; v5.3 = `c5dd4f5`/`abe5793` |
| **PLAYBOOK restructure** | Two-part heading scheme — Part I Reference **Ch1–14** / Part II Workflows **1–19** (Council Option 2); + `scan_heading_scheme` detector + §14 canonical note | **LANDED** | `fe5b790` (restructure) + `fe3fa03` (heading-convention enforcement) |
| **#200** | Merge-serialization gate (worktree→`main` git-structurally prevented; integrate serially from primary) | **CLOSED** | `4a0e6d1`; gone from BACKLOG |
| **#10** | Doc-currency reconciliation (B1/B2/B3/B5) | **CLOSED** | `8a5ae7f` |
| **#184** | Architect/CC equilibrium contract (ADR-87) — **empirically demonstrated** across A1–C2 | **CLOSED** | `5c21933` (today); done-when met |
| **#134 n=2 groom** | Periodic backlog groom — **84 → 81 tasks**; removed **#198** (`claude -w` lifecycle) + **#143** (parallel-work) + **#119** (Codex obsolete); condensed #134/#164/#77 | **DONE** | `7a0e4c5` (today); cleared 3 grandfather `doc_rot` dispositions |
| **Audit-A + Audit-B** | Two fresh read-only audits (dependency-architecture + coverage; process/trigger/usage) | **DELIVERED, report-only** — the new disposition queue (§3-C) | `56cd7c5` / `6e92222` (today) |
| **#204** | CONTRIBUTING nightly-outcome drift | **FILED, not fixed** (§1) | `4e00c81` |

**Green at generation (witnessed):** `pytest` **818 collected** (817 passed / 1 skipped) · `ruff`
clean · `audit.py health` **OK** · **`ship-gate` GREEN** (3 WARN dispositioned, **no `[stale]`**) ·
`validate_backlog` OK (**7 themes, 21 stories, 81 tasks**) · `doc_claims` **4/4** (audit checks
**23/23**, pre-commit hooks **10/10** + roster, `pytest_collected` **818/818**) · `validate_git_backlog`
→ only #77. (All are `PROBES.md` teeth — re-derive at read-time.)

**ADR statuses — witnessed live:**
- **ADR-88** (file-oriented dependency management) — **Accepted** (in-place marker; header still
  reads `Proposed` — §1 legibility item). OQ1–OQ4 carried for the *paradigm* (the cores are proven).
- **ADR-89** (computed code-dependency edges) — **Accepted** (in-place marker; header still reads
  `Proposed`). OQ2 resolved (#193); **OQ1 ratified + #194 built** this window; **OQ3 (advisory→gate,
  data-gated) carried** — and OQ3 is exactly the GAP-1/#195 computed-edge-enforcement frontier (§4.1).

---

## §3 — Next-ship action plan (the audit-disposition queue dominates this session)

This is **not** a build session. The center of gravity is **disposing Audit-A + Audit-B** (§3-C)
into a sequenced plan, and **deciding the new keystone** (the computed/discovery-edge frontier, §4.1).

### 3-A — What changed vs the 2026-06-21 plan

- **The 2026-06-21 keystone is DONE.** ADR-89 OQ1 ratified + **#194 built**; ADR-88/89 ratified
  Accepted. Remove them from the live plan.
- **The center of gravity moved from "declared edge" to "computed edge."** The declared-edge half
  is now production-grade (Audit-A). The new frontier is the **computed/discovery half** — wiring
  the **proven Pyright reverse-dep oracle** into an actual gate. That is ADR-89 **OQ3** + the
  `code-edge` group (**#195/#201/#202/#203**) + Audit-A's **GAP-1**.
- **#153** (FF-prevention) still open for scope-boundary + #189 methodology-reach + server-side teeth.

### 3-B — Build/decision table (status legend below)

**READY** = buildable now · **DECISION** = an architect/Council call gates it · **GATED** = blocked
on another item. Parallel-safety is **derived** from the #156 durable task-graph (no shared
`serialize-group` + no `depends-on` path) — **witnessed groups (P9):** `code-edge` =
**#195/#201/#202/#203**, `coherence` = **#180/#181/#182**, `audit-py` = **#153/#7/#36/#95/#139/#166/#190**,
`handoff` = **#1/#26/#159/#161/#162/#164**.

| # | What | Status | Gate / blocker | Notes |
|---|---|---|---|---|
| **GAP-1 / #195** | Wire the **Pyright reverse-dep oracle** (PROVEN, 122 deps live) into a **gate** — a code↔code import-graph cycle/boundary check over the flat `scripts/*.py` | **DECISION → then READY (the new keystone)** | ADR-89 OQ3 (advisory→gate, data-gated): decide *advisory-first* vs *gate-now* | `code-edge` group; Audit-A's headline frontier |
| **Undeclared `reconciled_with` edges** | Promote the **8+ discovery-only** edges Audit-A found (spot-check CLAUDE/PLAYBOOK/VISION vs spec **@5.3 FIRST**) | **DECISION** (which to declare/gate) | none — but verify-first each edge | declared-edge hardening |
| **GAP-2** | `validate_backlog` **hub↔plugin twin-parity** test | **READY** | none | Audit-A test-gap backlog |
| **GAP-3 / #203** | `coverage_scope` **auto-enumeration drift-guard** | **READY** | none | `code-edge` group |
| **#201 / #202** | (code-edge group — see BACKLOG for current text) | **READY/GATED — read BACKLOG** | per item | `code-edge` group |
| **#204** | CONTRIBUTING nightly-outcome correction | **READY** (mechanical once approved) | none | §1 drift |
| **#153** | FF-prevention scope-boundary + #189 reach + server-side teeth | **READY — assess priority** | none (local gate shipped) | `audit-py` group |
| **ADR-88 / ADR-89 header legibility** | Resolve the frozen-`Proposed`-header vs Accepted-marker split (the ADR-82 P1 class) | **DECISION** | decide the convention first | §1 / §4.4 |

### 3-C — Audit-disposition queue (the headline work — operator-gated approvals)

The two fresh audits returned **report-only** findings. None applied (read-only contract). The next
session's main job is to **approve/decline each, then CC applies the approved ones** test-first:

1. **Audit-A test-gap backlog** — `2026-06-25-dependency-architecture-coverage-audit.md` §4. The
   ranked gaps (**GAP-1** code↔code import-graph gate is the load-bearing one; GAP-2 twin-parity;
   GAP-3 coverage_scope drift-guard) + the **8+ undeclared `reconciled_with`** promotion call. Each
   is **gated / test-first / review-first** — these become real build tickets, not auto-applied.
2. **Audit-B dispositions** — `2026-06-25-process-trigger-usage-audit.md`. The standouts (from the
   wrap): the **`check-against-spec` decision** (built-but-not-invoked-at-trigger → **exercise on
   the next spec bump or retire**); the **`../dev-knowledge-138` empty sibling-orphan** cleanup
   (needs operator OK — a removal); the **CLAUDE §8 stale user-level `verify`-skill claim** (a
   canonical-file groom).
3. **Grooms carried from #134's wrap** — **#71** now also carries the Codex/ENVIRONMENT
   reconciliation; **#77** wants a focused-session trim of legs 1+2 (contradictory evidence, do NOT
   trim on murky evidence); **#70** is an unblocked-nudge (self-states actionable since 2026-06-02).

---

## §4 — Open decisions the next session must make

1. **The new keystone — wire the computed code↔code edge into a gate (GAP-1 / #195 / ADR-89 OQ3).**
   The declared-edge half is production-grade; the **computed half is unenforced** — the Pyright
   reverse-dep oracle is **PROVEN** (122 deps live) but **consumed by no gate**, and the codemap
   polices an ~empty 2-package/0-edge graph among the flat `scripts/*.py`. ADR-89 OQ3 framed the
   call as **advisory→gate, data-gated** (the #196 spike: computed legs eligible to gate hard once
   data justifies; declared legs stay advisory). **Decide: build the code↔code import-graph
   cycle/boundary check now, advisory-first?** This is the natural successor to the #194
   declared-edge build — same paradigm (ADR-88/89), the other edge-kind.
2. **The 8+ undeclared `reconciled_with` edges (Audit-A).** Discovery-only today (never gated).
   **Decide which to promote to declared+gated** — but **spot-check each against the live spec
   @5.3 FIRST** (a stale declared edge is worse than an undeclared one). Verify-then-declare.
3. **Audit-B artifact/tool decisions.** (a) **`check-against-spec`** — built but not invoked at its
   trigger; **exercise it on the next spec bump, or retire it** (the "decide the undecided
   artifact/tool models" theme). (b) **`../dev-knowledge-138` empty sibling-orphan** — operator-gated
   removal. (c) **CLAUDE §8** stale user-level `verify`-skill claim — groom.
4. **ADR-88/89 (and ADR-82) status legibility (the ADR-82 P1 class).** Frozen header `Proposed` vs
   the Accepted in-place marker + README. **Decide the convention** — in-place status marker (status
   quo) vs a header-amendment vs a README-label-only norm — and apply it uniformly. Do **not** edit a
   frozen ADR header ad-hoc before the convention is set.
5. **#153 priority + scope** — the local pre-push gate shipped; the `--no-ff` **scope boundary**
   (hub vs `~/.claude` vs child repos), the **#189 methodology-reach**, and **server-side teeth**
   are unsettled. Decide whether to invest now or hold.
6. **#204 + the canonical-file-integrity groom.** #204 (CONTRIBUTING nightly-outcome) is a
   correctness fix, ready once approved. Decide whether to fold it into a broader canonical-file
   currency pass (the theme also holds the day-to-day-docs-right-sized work).
7. **Process hazard — shared-checkout race (carried, decision pending).** Parallel Claude sessions on
   **one shared checkout** raced on branch state twice this window (a report commit landed directly
   on `main`, repaired non-destructively; LESSONS witness appended). The doctrine exists (worktrees);
   the **gap was application**. Decide whether to harden the workflow (give each session its own
   `git worktree`/clone) now or leave it lessoned.

---

## §5 — Key facts from THIS window (so they aren't re-litigated)

1. **The 2026-06-21 keystone is DONE — do not re-open it.** ADR-89 OQ1 was ratified heading-level
   (`{#id}`→module/file, advisory-first) **and #194 was built** to that spec (now the live
   `doc_code_edge` check). ADR-88/89 are **Accepted**. The next keystone is the **computed** edge,
   not the declared one.
2. **ADR-88/89 are Accepted despite the `Proposed` header.** The header is **frozen** (immutability
   convention); the live status is the **in-place Amendment marker + the README index**. Read line-5
   `Proposed` as the legibility item (§1/§4.4), **not** the live status.
3. **The handoff-rework agenda LANDED but closed no parent ticket.** A1–A4 + C1–C6 + v5.3 all
   advanced **#161/#164/#144/#145** — none met its verbatim done-when (the v5 generator #164 is
   still unbuilt; #144's "in the cloud" leg needs an operator verify-first). **Advanced, not closed
   — do not mistake the heavy activity for closure.**
4. **C1/C4/C6 removal premises were verified-REFUTED.** The "delete the dead v4 corpus / v5 paste
   assembly" premise was **wrong** — every named target is live, gated, or load-bearing
   (`templates/handoff/*.tmpl` is live for corp-monorepo's v4 path; `templates/handoff/v5/` are
   #164's own seed sources; PASTE_THIS is built at runtime, not stored). The arc **removed nothing**;
   it condensed narration only. **Do not re-propose those removals** — they are gated to #164's close.
5. **Audit-A's verdict is the strategic signal: "declared-edge half production-grade; computed/
   discovery half is the frontier."** The code↔code import graph is unenforced and the Pyright
   oracle is proven-but-ungated. This is the single most load-bearing finding for sequencing.
6. **`audit.py checks` crashes mid-listing on a cp1252 Windows console** (U+2192 `→` in check #23's
   docstring). Run with `PYTHONUTF8=1` (or git-bash). Incidental — not a gate failure, but it will
   bite anyone reading the check registry on a bare PowerShell console.
7. **The shared-checkout race is real and recurred** (twice this window). The fix is application of
   existing doctrine (worktrees), not new doctrine — see §4.7. PowerShell also throws a **false-RED
   on `handoff_probes`**; verify the gate in **git-bash** (the JOURNAL records this each session).

---

## §6 — Pointers (read the source, never a copy)

- **The disposition queue (the headline work):**
  `docs/audits/2026-06-25-dependency-architecture-coverage-audit.md` (Audit-A — read its **verdict**
  + **§4 test-gap backlog**) and `docs/audits/2026-06-25-process-trigger-usage-audit.md` (Audit-B).
- **Plan / task-graph:** `BACKLOG.md` (story-map; **witnessed groups (P9):** `code-edge` =
  #195/#201/#202/#203, `coherence` = #180/#181/#182, `audit-py` = #153/#7/#36/#95/#139/#166/#190,
  `handoff` = #1/#26/#159/#161/#162/#164). Schema machine-checked by `scripts/validate_backlog.py`.
- **Doctrine (both now Accepted):** `docs/decisions/ADR-88-file-oriented-dependency-management.md`
  (declared edge), `docs/decisions/ADR-89-computed-code-dependency-edges.md` (computed edge + the OQ
  list — **OQ3 is the open computed-edge-enforcement question**).
- **The #194 build (the keystone that landed):** `scripts/validate_doc_structure.py` + the
  `doc_code_edge` check (#23 in `ALL_CHECKS`); the `feat/194-*` arc in `git log`.
- **Equilibrium contract (architect↔CC):** `docs/decisions/ADR-87-*` + `protocols/HANDOFF_BOOT.md`
  (the resident Architect Operating Contract — read the loop + gate-map + architect-mode posture).
- **Handoff process (current):** `protocols/HANDOFF_PROCESS.md` **v5.3** (§5 5-probe manifest, §13
  architect mode + the always-generated `SUPPLEMENT.md`).
- **Organ map / inventory:** `ARCHITECTURE.md` Ch2 (organ map) + §Validators (the `scripts/`
  inventory; **23 checks**, gate roster **10**, **818** collected).
- **This window's commits:** `git log --oneline --first-parent ad6d17e..HEAD` (ADR ratification,
  the #194 arc, the handoff-rework A/C arcs, the PLAYBOOK restructure, v5.3, #200/#10/#184 closes,
  the #134 groom, the two audits).
- **Prior context:** `docs/handoffs/2026-06-21-dev-knowledge-architect/` (the bundle whose
  disposition queue + keystone this window executed) and `…/2026-06-25-dev-knowledge-session-close/`
  (the lean session-close bundle whose §3 pending-queue this residual carries forward).
