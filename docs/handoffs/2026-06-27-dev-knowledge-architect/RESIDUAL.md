# Residual — 2026-06-27 architect handoff (the part the repo does not already encode)

<!-- scope: meta -->

> **What this is (v5 §2).** CC's handoff is **only the residual** — the un-committed "why," the
> pointers, and the **drift-flags as the headline**. It does **not** re-transmit methodology
> (pointer + mechanical enforcement, §3) or re-narrate task-state (pointer to `BACKLOG.md`, §6).
> This is **architect mode** (§13): the residual is scoped to the *planning* "why," task-state
> points at the whole BACKLOG / relevant themes, and the open architecture questions travel as
> residual so the next session resumes the design rather than rediscovering it.
>
> **Generated COLD, then FILLED** — generated by CC at a **fresh `/clear`ed session start** (no
> outgoing architect at generation); the operator then **FILLED `SUPPLEMENT.md`** from the outgoing
> architect chat. The CC-derived **repo-side** *why* of this window lives in §3–§5 below; the
> **off-repo** *why* (intent, tensions, decomposition rationale, the new-keystone steer — what the
> repo structurally cannot encode) is in the folded `SUPPLEMENT.md` ANSWERS. Because the supplement
> carries answers, the incoming **§13(d) operator-context beat narrows** to *"anything changed since
> the supplement was written?"* — not a FULL re-ask (`HANDOFF_PROCESS.md` §13: "refined, not duplicated").
>
> **Four-tag discipline.** `witnessed` = CC re-derived it live this generation (the §1/§2 gate
> outputs); `recall`/`inferred` = reconstructed from the JOURNAL/git window, may have moved —
> the load-bearing ones are re-checkable via `PROBES.md`. `unknown` = stated as such.

---

## §1 — Drift-flags (THE HEADLINE — surfaced first, not buried)

Produced by the live read-only drift-checks (`audit.py ship-gate`, `validate_git_backlog`,
`validate_doc_claims`, `validate_backlog`) at generation (**witnessed**).
**Re-derive each at read-time** — the teeth are in `PROBES.md`, not in trusting these lines.

### ✅ HEADLINE — `ship-gate` is GREEN and **CLEAN**; the window added **zero** new drift

`python scripts/audit.py ship-gate` → **GREEN** (**4 WARN dispositioned**) — verification organs
green against this arc. **No new WARN and NO `[stale]` disposition this window.** This was a
heavy build/disposition window that *drained* drift, not one that created it: it built two new
FAIL-class gates (#195 → check #24 `safe_removal`; #203 → check #25 `doc_code_coverage_drift`),
closed the one open doc-drift from the prior bundle (**#204**), and hardened four validators —
all landing GREEN.

### No open actionable doc-drift this window

The prior bundle's one open drift (**#204** — `CONTRIBUTING.md` "Nightly outcome management"
describing the superseded squash-merge-to-`main` model) was **fixed + closed** this window:
reconciled to the ADR-84 **divert-and-close** model (digest diverted to
`automation/conformance-digest`, PR closed, never merged to `main`) after a source-verified
re-read; `CONTRIBUTING.md` `last_reviewed` re-stamped. **No successor doc-drift is open.**

### Standing flags (benign / dispositioned — unchanged, do NOT touch)

- `git_backlog_drift`: **#77** — the known voided-closure false positive (`77e5d7df9` closes #77
  but #77 stays in BACKLOG by design — operator ruled keep-open 2026-06-26). Dispositioned
  (`warn-77-voided-closure`). **Standing false positive — do not touch.**
- `no_ff_merges`: **3** journal-wrap / transcript-archive direct-to-`main` commits
  (`533109f20` 2026-06-26 journal-wrap, `3a894eeb5` + `d0f9ead67` 2026-06-19) print `[~~]` under
  `health` but are `[disp]` under `ship-gate`. **Expected seam, not a regression** — `ship-gate`
  GREEN. **#210** (open) proposes converting this 3-instance class from per-instance disposition
  to a standing rule (path-scoped EXEMPT or branch-then-merge the JOURNAL wrap) — a §4 decision.

### Carried legibility item (NOT new — for the corpus-coherence groom)

- **ADR status-header legibility (the ADR-82 P1 class).** Ratified ADRs whose **frozen header
  `**Status:** Proposed` (line 5) still reads Proposed** while the live status is the in-place
  Amendment marker + the README index (the immutability convention freezes the header). Not
  gate-failing (no check reads the header); a real legibility item. **#162** (architect actor-vs-mode
  vocab) and the `doctrine_enforcement_coherence` check (**#166**, open) are the adjacent fixes.
  **Do not "fix" by editing a frozen ADR header** without deciding the convention first (§4).

---

## §2 — Shipped / closed this window (verified live where it is live state)

The window since the **2026-06-25** architect handoff (`9b4230c` add-SHA → HEAD `0e2af58`). A heavy
execution + audit-disposition window that **drained the prior bundle's queue and closed its
keystone.** **`recall` from JOURNAL/git unless marked witnessed.**

| Item | What landed | Status | Evidence |
|---|---|---|---|
| **#195 (the prior keystone)** | **Code↔code safe-removal gate BUILT** — `scripts/safe_remove.py` (the #193 Pyright reverse-dep oracle's consumer): BLOCKS removing a `scripts/` module while a live EXTERNAL code referrer survives, names it. M1 (computed code→code) only | **CLOSED** (M1); demonstrated catch (A deterministic + B real-Pyright end-to-end) | `05111bc` merge; live `audit.py` check **#24 `safe_removal`** (witnessed) |
| **#201 / #202 / #203 + ADR-90** | Doc→code declared-edge family **completed through Tier-3**: #201 resolver-allows-N (N-organ rule resolution) + **ADR-90**; #202 four Tier-3 declarations; #203 coverage drift-guard | **ALL CLOSED** | `12e500d`/`2e5a52c` merges; **witnessed**: `doc_code_edge` **12 edges**, check **#25 `doc_code_coverage_drift`** all 25 covered |
| **2026-06-25 Audit-A/B disposed** | Wave-1 hardening: GAP-1 cycle fix; **#206** twin-parity test; **#207** earned-by-value probe discriminator; **#208** data-driven doc_claims + doc_rot live-budget; **#205** check-against-spec wired to the re-stamp flow; **#209** declared `reconciled_with @5.3` | **#205–#209 CLOSED** (operator-approved Tier-1) | `01c6c77` merge; **witnessed**: `reconciled_versions` **5 edges @5.3** |
| **#204** | `CONTRIBUTING.md` nightly-outcome reconciled to ADR-84 divert-and-close (the prior bundle's one open drift) | **CLOSED** | `d6a2461`; §1 |
| **#216** | PLAYBOOK §2 effort ownership → **architect-owned** (by token budget); model stays CC's pick | **CLOSED** | `05974be` merge |
| **#77** | Canonical **"two lifelines"** section + architect↔CC consolidation (PLAYBOOK + ESSENTIALS) | **ADVANCED, NOT closed** (legs 1/2/4 PLAYBOOK work remain — operator: keep open) | `cc6d15f`/`2a3cf3d` merges |
| **SEAL-1a #213** | PLAYBOOK rule/history condensation (two-part heading spine; **zero rules lost**; −24 lines) | **CLOSED** (#214/#215 stay filed) | `9ab191f`/`d35e61f` merges |
| **Corpus cleanup** | Retired `migrate_links.py`; archived the 3 flat v3/v4 handoff templates; HANDOFF_PROCESS §11 false-archival fix; filed **#211/#212** | **DONE** | `4848ca4` merge + `323c85e` corpus-graph audit |
| **Pre-handoff hygiene** | Filed **#219** (loop cross-ref) + #131/#215 generalization-probe notes; closed #204 | **DONE** | `0e2af58` merge (HEAD) |

**Green at generation (witnessed):** `pytest` **884 collected** · `ruff` clean · `audit.py health` OK ·
**`ship-gate` GREEN** (4 WARN dispositioned, **no `[stale]`**) · `validate_backlog` OK (**7 themes,
21 stories, 85 tasks**) · `doc_claims` match · `audit.py checks` **25** (last `doc_code_coverage_drift`) ·
`doc_code_edge` **12 edges** · `validate_git_backlog` → only #77. (All are `PROBES.md` teeth —
re-derive at read-time.)

---

## §3 — The strategic shift (what changed for the next session)

### 3-A — The dependency-management spine is BUILT — the prior keystone is DONE

The 2026-06-25 bundle's strategic signal was *"declared-edge half production-grade; computed/
discovery half is the frontier."* **That frontier is now built:**
- **Computed code↔code edge is ENFORCED** — #195's safe-removal gate (check #24) consumes the
  proven Pyright reverse-dep oracle. The oracle is no longer "proven-but-ungated."
- **Declared doc→code edge is COMPLETE through Tier-3** — #201/#202/#203 + ADR-90; the coverage
  drift-guard (check #25) means *every* `ALL_CHECKS` member is now coverage-mapped or exempt.

So the ADR-88/89 file-oriented + computed-edge **dependency-management arc is largely complete.**
**Do not re-open it as the keystone.** What remains *in that family* is a single open ticket:
**#218** — safe-removal **M2** (path/dotted-module string refs) + **M3** (declared-prose reverse-deps),
the deferred phases of #195 (the only open `code-edge` group item, **witnessed P9: code-edge = #218**).

### 3-B — The new center of gravity: dissemination + the undecided frontiers

With the hub's governance + dependency machinery built and hardened, the open strategic question is
**where to invest next.** The strongest candidates (the next session must *choose + sequence*, not
build-all):

1. **Cross-repo dissemination / onboarding (VISION's "Disseminator" role).** **#131** (6-layer
   install runbook) + **#215** (onboard + verify in a new repo). Pre-handoff capture flagged the
   **#131↔#215 merge-or-split as an explicit start-of-next-session decision**, and the
   **two-lifelines generalization probe**: the first onboarding pilot (ai-council) doubles as the
   first real test of whether the *two lifelines* (Workflow + Coherence) transfer to a
   structurally-different repo-shape (CLI / provider-heavy / fewer governance docs). This is the
   natural "the hub is built — does it travel?" successor.
2. **The conformance dashboard (#171 → #169).** Build `ecosystem/conformance.md` (ADR-86, the
   ADR-80 committed-generated zone) as the surface ADR-85 R2's **ungated-doc staleness signal**
   (#169) lands in. #169 `depends-on` #171.
3. **Finish the code-edge family (#218).** Safe-removal M2/M3 — completes the dependency spine but
   is the *continuation* of a built arc, not a new frontier.
4. **ADR-85 R1 — harden the BACKLOG leg to a hard gate.** #170 (traceability-spine ADR, the
   issue-ID↔commit anchor) → #168 (promote the advisory BACKLOG leg). #168 `depends-on` #170.

### 3-C — Task-graph (pointer, not narration — read BACKLOG for current text)

Parallel-safety is **derived** from the #156 durable task-graph (no shared `serialize-group` + no
`depends-on` path). **Witnessed serialize-groups (P9):**
`code-edge` = **#218** · `coherence` = **#180/#181/#182** · `handoff` = **#1/#26/#159/#161/#162/#164** ·
`audit-py` = **#153/#7/#36/#95/#139/#166/#190/#210** · `playbook` = **#146/#77/#67/#18/#217/#219/#213/#214/#39** ·
`architecture` = **#165/#35** · `claude-md` = **#112/#157** · `claude-md-template` = **#17/#109** ·
`environment` = **#71** · `pre-commit-config` = **#15/#132** · `settings-json` = **#116/#117**.
Hard precedences (`depends-on`): **#112→#23**, **#168→#170**, **#169→#171**.

---

## §4 — Open decisions the next session must make

1. **#131↔#215 onboarding merge-or-split (the explicit start-of-session decision).** Captured
   pre-handoff: reconcile the two onboarding items — #131 (the 6-layer install *runbook*) and #215
   (onboard *+ verify it conformed*, tied to the unbuilt #171 conformance.md). **Decide: merge into
   one item, or keep split with a clean scope boundary** (and record the reason). This gates the
   dissemination keystone below.
2. **The new keystone — where to invest now that the dependency spine is built (§3-B).** Cross-repo
   dissemination + the two-lifelines **generalization probe** (#131/#215, ai-council pilot), the
   **conformance dashboard** (#171→#169), or **finishing #218** (code-edge M2/M3)? The first tests
   whether the methodology *travels*; the second builds the R2 staleness surface; the third
   completes a built arc. **Pick one as the center of gravity; sequence the rest.**
3. **#210 — promote the journal-wrap no-ff class to a standing rule.** 3 instances of one class
   (direct-to-`main` JOURNAL/transcript wraps) are now individually dispositioned — a recurring
   structural gap, not one-offs. **Decide the shape:** a path-scoped EXEMPT in `no_ff_merges`
   (JOURNAL/transcript-only diffs pass; any other path still WARNs) vs branch-then-merge the wrap
   (eliminate the class). Must NOT weaken core-invariant #5 for real code.
4. **#212 — `docs/handoffs/**` retention/rollup policy (likely ADR-class).** The immutable handoff
   bundles are the corpus's single largest growth vector (~416 bundles, unbounded by design).
   **Decide:** a compaction/rollup mechanism + retention window (the ADR-49/65 section-history
   pattern), or an explicit "keep all; unbounded accepted" with a recorded reason.
5. **ADR-85 R1 — harden the BACKLOG leg (#170 → #168).** The BACKLOG leg is still advisory. The
   blocker is the **traceability-spine ADR** (#170 — the un-gameable issue-ID↔commit anchor) that
   #168 `depends-on`. **Decide whether to design #170 now** (it unblocks the hard gate).
6. **#211 — closure-script hub↔plugin parity (corpus-graph audit obs A).** `propose/review_closures.py`
   are tested-canonical but their `plugins/tier1-lifecycle/scripts/` carriers (which actually run)
   have diverged with no parity test. **Decide:** a twin-parity test modulo the known `_host_root`
   delta (extend the #206 pattern) vs single-sourcing the carriers.
7. **ADR status-header legibility (the ADR-82 P1 class, §1) — and #162 vocab.** Decide the
   convention for ratified-ADR status (frozen-header-stays vs sanctioned amend-path #112 vs
   README-label-only) and apply it uniformly; **#162** (architect = Layer-1 *actor* vs handoff
   *mode*) is the adjacent still-open disambiguation. The `doctrine_enforcement_coherence` check
   (#166) would mechanize the "live enforcement ahead of Proposed doctrine" signal.
8. **#77 legs 1/2/4 (PLAYBOOK consolidation).** Operator ruled keep-open 2026-06-26 — leg 3
   (ESSENTIALS currency) advanced; legs 1/2/4 (extract PLAYBOOK §18; reconcile the 3 divergent
   lesson→rule statements; strip Section-history) are genuinely undone. Decide whether to schedule
   that focused arc — **do NOT trim on murky evidence** (the disposition stays live).

---

## §5 — Key facts from THIS window (so they aren't re-litigated)

1. **The dependency-management spine is BUILT — do not re-open it as the keystone.** #195 (computed
   code↔code, check #24) + #201/#202/#203 + ADR-90 (declared doc→code through Tier-3, check #25)
   closed the prior bundle's frontier. The *only* open code-edge item is **#218** (safe-removal
   M2/M3), and that is the *continuation* of a built arc, not a new direction.
2. **#195 shipped M1 ONLY (computed code→code).** M2 (path/dotted-module string refs) + M3
   (declared-prose reverse-deps) are explicitly **deferred to #218** — surfaced at close, not
   silently dropped. Do not assume safe-removal covers path/string/prose referrers.
3. **#77 is NOT closed (operator ruled keep-open).** Leg 3 (ESSENTIALS currency) advanced via the
   two-lifelines work; legs 1/2/4 (PLAYBOOK) remain. The `warn-77-voided-closure` disposition is
   **live by design** — closing #77 would orphan it and repeat the voided 2026-06-09 misattribution.
4. **ADR-90 is the resolver-allows-N record.** A doc→code rule enforced in N code organs declares
   its expected count in `ecosystem/doc-code-edge.yaml` and resolves at EXACTLY that count
   (duplicate-guard intact). Future multi-organ rules just add a `multi_site:` + `coverage_scope` row.
5. **#203's drift-guard covers the `ALL_CHECKS` surface, NOT every organ.** Enforcement organs
   *outside* `ALL_CHECKS` (the seal Stop-hook, the `backlog-id-on-close` commit-msg hook, the
   `block-ff-push` pre-push hook, the standalone pre-commit validators) are **not** auto-guarded —
   that heterogeneous remainder stays curated. The claim is "code↔doc sealed across the full
   `ALL_CHECKS` behavioral surface," never "every conceivable organ."
6. **`audit.py checks` crashes mid-listing on a bare cp1252 Windows console** (a `→` in a check
   docstring). Run with `PYTHONUTF8=1` or in git-bash. And **`ship-gate` can false-RED on
   `handoff_probes` under PowerShell** (grep/sed/ls not on PATH) — **verify the gate in git-bash.**
   Incidental, not a gate failure, but it bites anyone reading the registry / gate on bare PowerShell.
7. **The window is pushed + clean.** Unlike several recent windows that ended "push pending," HEAD
   `0e2af58` is in sync with `origin/main` at generation. This bundle's own commit + `/ship` merge
   will move HEAD ahead until pushed (P3).

---

## §6 — Pointers (read the source, never a copy)

- **Plan / task-graph:** `BACKLOG.md` (story-map; 7 themes / 21 stories / 85 tasks; **witnessed P9
  groups in §3-C**). Schema machine-checked by `scripts/validate_backlog.py`.
- **The new-frontier candidates:** dissemination — **#131/#215** (Cross-repo universalization theme);
  conformance dashboard — **#171/#169** (Canonical-file integrity theme); code-edge tail — **#218**.
- **Dependency-management doctrine (the built spine):**
  `docs/decisions/ADR-88-file-oriented-dependency-management.md` (declared edge) +
  `docs/decisions/ADR-89-computed-code-dependency-edges.md` (computed edge) +
  **`docs/decisions/ADR-90-*.md`** (resolver-allows-N, this window).
- **The builds that landed:** `scripts/safe_remove.py` + check **#24 `safe_removal`** (#195);
  `scripts/validate_doc_code_edge.py` (multi-site resolver) + check **#25 `doc_code_coverage_drift`**
  (#201/#202/#203); `ecosystem/doc-code-edge.yaml` (`multi_site` / `exempt` / `coverage_scope`).
- **Equilibrium contract (architect↔CC):** `docs/decisions/ADR-87-*` + `protocols/HANDOFF_BOOT.md`
  (the resident Architect Operating Contract — loop + gate-map + architect-mode posture + the
  canonical "two lifelines" division).
- **Handoff process (current):** `protocols/HANDOFF_PROCESS.md` **v5.3** (§5 5-probe manifest, §13
  architect mode + the always-generated `SUPPLEMENT.md`); `#164` is the unbuilt v5 generator.
- **Organ map / inventory:** `ARCHITECTURE.md` Ch2 (organ map) + §Validators (the `scripts/`
  inventory; **25 checks**, **884 collected**).
- **This window's commits:** `git log --oneline --first-parent 9b4230c..HEAD` (the #195 safe-removal
  gate, the #201/#202/#203 + ADR-90 completeness chain, the Wave-1 #205–#209 disposition, #204 close,
  #216, the #77 two-lifelines section, SEAL-1a, the corpus cleanup, pre-handoff hygiene).
- **Prior context:** `docs/handoffs/2026-06-25-dev-knowledge-architect/` (the bundle whose
  disposition queue + keystone this window executed) and `docs/audits/2026-06-25-*` (Audit-A/B,
  now disposed) + `docs/audits/2026-06-26-corpus-graph-justify-or-retire.md` (obs A → #211).
