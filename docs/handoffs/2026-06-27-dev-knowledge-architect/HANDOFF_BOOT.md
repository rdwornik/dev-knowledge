# Handoff boot — session header + paste-pointer (architect mode)
<!-- scope: meta -->

| Field | Value |
|---|---|
| **Slug** | `2026-06-27-dev-knowledge-architect` |
| **Mode** | **architect** (v5.3 §13 — planning / way-of-working scope) |
| **Purpose** | **The dependency-management spine is BUILT — decide the next strategic frontier.** The window since the 2026-06-25 architect handoff was a heavy **execution + audit-disposition** window that **drained that bundle's whole queue and closed its keystone.** The prior keystone — *"wire the PROVEN Pyright reverse-dep oracle into a gate (the computed/discovery edge frontier, GAP-1 / #195)"* — **landed**: **#195 the code↔code safe-removal gate is BUILT** (`scripts/safe_remove.py`, live `audit.py` check #24 `safe_removal`, demonstrated catch — blocks removing a `scripts/` module while a live external code referrer survives, names it). The **doc→code declared-edge family completed through Tier-3** (#201 resolver-allows-N + **ADR-90**, #202 four Tier-3 declarations, #203 coverage drift-guard = live check #25 `doc_code_coverage_drift` — *every* `ALL_CHECKS` member is now coverage-guarded; `doc_code_edge` grew **5→12 edges**). The 2026-06-25 **Audit-A/B were disposed** (GAP-1 cycle fix; GAP-2 twin-parity test #206; GAP-4 earned-by-value probe discriminator #207; GAP-6/7 data-driven doc_claims + doc_rot live-budget #208 — all closed as **Wave-1 #205–#209**). Also landed: **#204** closed (CONTRIBUTING nightly-outcome reconciled to ADR-84 divert-and-close), **#216** (PLAYBOOK §2 effort → architect-owned), **#77 advanced** (the canonical *"two lifelines"* section + architect↔CC consolidation — NOT closed, legs 1/2/4 remain), **SEAL-1a #213** (PLAYBOOK rule/history condensation, zero rules lost), and a **corpus cleanup** (retired `migrate_links.py`, archived the flat v3/v4 templates, filed #211/#212). So the next architect's center of gravity **shifts off the dependency-management spine** (now built + hardened) onto **(1) the explicit start-of-session decision — the #131↔#215 onboarding merge-or-split** (captured pre-handoff), and **(2) the new keystone call: where to invest now — cross-repo dissemination / the onboarding pilot + two-lifelines generalization probe (#215), the conformance dashboard (#171→#169), or finishing the code-edge family (#218 safe-removal M2/M3).** This is a **decision + sequencing** session, not itself a build. |
| **Generated at** | Bundle cut at HEAD `0e2af58`, **working tree clean**, `main` **in sync with `origin/main`** (the pre-handoff window is pushed). This bundle's own commit + the later `/ship` `--no-ff` merge move HEAD and push `main` **ahead of `origin` until pushed**. **Re-derive HEAD / sync at read-time** (`PROBES.md` P3) — do not trust this line. |

> **Generated COLD (cleared session).** This bundle was generated by **CC at a fresh `/clear`ed session start** — there is **no outgoing architect chat in the loop at generation**. The window since the 2026-06-25 handoff was reconstructed from JOURNAL + git + live state. `SUPPLEMENT.md` is therefore **committed EMPTY** — the **defined cold-handoff disposition** (`HANDOFF_PROCESS.md` §13: *empty ANSWERS = committed N/A, not a defect, not folded*), **not** a missing deliverable. **If the operator has an outgoing chat that did this window's strategic work, they MAY fill `SUPPLEMENT.md` from it** (paste its QUESTIONS → answers below the divider → tell CC `supplement filled`); otherwise the incoming **§13(d) operator-context beat fires FULL** (nothing to narrow against). The CC-derived repo-side "why" lives in `RESIDUAL.md`; the supplement is the **off-repo** complement the residual structurally cannot carry. Facts are **four-tag** scoped in `RESIDUAL.md`: recent-window facts reconstructed from JOURNAL/git are **`recall`/`inferred`**; everything CC re-derived live this generation is **`witnessed`** — re-derive the load-bearing ones via `PROBES.md`.

> **Operator runbook is not copied here.** The stable who-each-file-is-for / run-loop / rationale lives **once** in the canonical per-repo runbook **`docs/handoffs/README.md`** — read it for the walkthrough. This file carries only the **session header** (above) + the **paste-pointer** (below). v5 bundles carry **no per-bundle README** (the 2026-06-12 canonical-runbook collapse — `HANDOFF_PROCESS.md` §13).

## What the operator does (paste-pointer)

1. Open a fresh Claude.ai chat.
2. Paste **`PASTE_THIS.md`** (in this bundle directory) — the browser role file + residual + probes, assembled in order by `scripts/assemble_paste.py`. One paste; never hand-feed individual files to the file-less browser. (The supplement is EMPTY, so its ANSWERS are **not** folded into the paste — by design, cold handoff.)
3. The browser replies with its on-load acknowledgment line (it names the Layer-1 actor); a partial or missing paste is then visible.
4. **Architect mode** — the browser uses the **generative / decompositional** posture (`protocols/HANDOFF_BOOT.md` §"Architect mode"): orient first, ask the operator for off-repo context, drive decomposition, hold the whole-system view, surface design tensions — **not** the reactive-filter default.
5. **Orient before any mechanism** — hand the browser `PROBES.md` **P1** (orientation) first; it replies `run <command>`, CC reads live `VISION.md` / `ARCHITECTURE.md` Ch1 and substring-checks. The architect may not begin design until it holds both orienting lines. (P1's grep is a *tool* that confirms the frame — the **backlog navigates**, v5.3 §13c.)
6. **Then the operator-context beat (§13d) fires FULL.** The supplement is **EMPTY** (cold handoff), so the browser asks the **whole** question — *what off-repo context for this planning session: intent, priorities, findings not in the repo, changed decisions?* — it does **not** narrow to "anything changed since the supplement?" (there is no supplement content to narrow against).
7. Hand `RESIDUAL.md` — **drift-flags first** (the headline this time: `ship-gate` is **GREEN** and **clean** — the window added **zero** new WARN and carries **no `[stale]`** disposition; the only standing flags are the dispositioned #77 voided-closure + 3 journal-wrap no-ff) — then the **next-frontier decisions** (§4, led by the #131↔#215 onboarding reconcile + the new-keystone call) + the **shipped-this-window** map (§2) + run the rest of `PROBES.md` (P2–P9). Any probe FAIL blocks onboarding (the escalation ladder).

## Why a pointer, not a copy

The methodology, the browser role, and the operator runbook all live in the repo and are referenced by **pointer**; CC (which holds the repo) serves any part the file-less browser needs just-in-time. `PASTE_THIS.md` is the assembled boot source (generated by `scripts/assemble_paste.py`, never hand-edited); `docs/handoffs/README.md` is the operator runbook. If a pointer and its source disagree, the source wins — fix the pointer or regenerate `PASTE_THIS.md`.
