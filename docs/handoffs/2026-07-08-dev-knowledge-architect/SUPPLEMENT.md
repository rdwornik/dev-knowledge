# Architect strategic supplement — 2026-07-08-dev-knowledge-architect

Repo: .dev-knowledge · Mode: architect · Date: 2026-07-08

> **Operator — 3 steps:**
> 1. Copy the QUESTIONS below into the outgoing architect chat (the chat that did this
>    session's work).
> 2. Paste that chat's answers into the ANSWERS section at the bottom — combine multiple
>    chats if needed.
> 3. Tell CC `supplement filled` → CC commits this file on the handoff branch and folds
>    the answers into the next session's PASTE_THIS.
>
> **No outgoing chat to ask (cold / cleared handoff)?** Leave ANSWERS empty. The next
> session captures off-repo context live via the §13(d) operator-context beat. The empty
> file is still committed — a record that this session had no transmissible live "why"
> (this is the defined cold-handoff disposition, not a defect).

## QUESTIONS — paste these to the outgoing architect chat

1. **Strategic intent** — what should the next session achieve at the way-of-working
   level? (A design / methodology goal, not a task.)
2. **Tensions weighed** — which design trade-offs were weighed, where did you land, and
   why?
3. **Considered + rejected** — which options were rejected and why (so the next session
   does not relitigate them)?
4. **Open questions** — which design questions are unresolved or deliberately deferred?
5. **Decomposition rationale** — why this task-graph shape? What must the next session
   NOT redo or re-decide?
6. **Off-repo context** — intent, priorities, changed decisions, or findings not in the
   repo.
<!-- generator may append session-specific CC-observed addenda here as "A./B. ..." -->

===================== PASTE CHAT ANSWERS BELOW THIS LINE =====================
<!-- operator: paste answers here; combine multiple chats if needed; leave empty if there is no outgoing chat -->


# SUPPLEMENT ANSWERS — outgoing architect (Fable 5 browser chat), 2026-07-08

**Q1 — Strategic intent.** A **GROOMING + HARDENING session** (plan-first). The way-of-working goal: restore the prune half of the backlog metabolism — filing velocity has outrun kill velocity (81 → 124 open tasks in ~2 weeks; the quarterly groom lapsed 2026-07-01; the backlog grew by 9 DURING the closing session itself). Concretely, in order: **(a)** execute the leg-c ruling pass over the generated grooming worksheet (83 active / 33 aged / 7 new; per-story S-id proposals): every story ruled KEEP / KILL(removed-not-closed, #134 precedent) / MERGE / DEFER-with-peg, every aged task explicitly dispositioned — line by line, the operator's explicit ask; **(b)** institutionalize grooming as a cadence WITH TEETH — decide the mechanism (a fleet_health digest escalation line when the groom is overdue, a recurring peg item, or a `[load]` sub-line of #270), because a dateline in a footer demonstrably does not fire; **(c)** only AFTER the kills: selectively admit brainstorm Wave-1 (F1 discipline — the backlog should be SHORT) and triage the four fresh func SEEDs (ids 6 new-project-bootstrap · 7 ai-council-interface · 8 night-routines-suite · 9 dashboards-local-html); **(d)** sequence the Wave-1 consumer-side arcs (census fixes per repo) as night-2 candidates on frozen contracts. Secondary: rule #264 build-vs-park with the operator's local-HTML-in-VS-Code direction now on file (id 9). One line: **shrink, then grow — and leave behind a mechanism that keeps it shrunk.**

**Q2 — Tensions weighed.** (1) *Build-now vs groom-first*: every fresh theme (night suite, dashboards, council interface, bootstrap) pulls toward building; landed groom-first because ratification bandwidth is the binding resource (brainstorm F4) and 124 open items already exceed one operator's attention. Nothing new enters until the ruling pass completes. (2) *#269 conflict* (audit-index hook built early vs wait-for-shape): landed build-now — the 07-08 ruling was newer + explicit and `--check` is shape-agnostic; adjudicated APPROVED, reconciliation recorded. (3) *Story-id letters vs numbers*: numbers (`S<n>`) — letters recreate the just-retired Track-X collision class (#162) and 24 stories exhaust an alphabet. (4) *Night-suite ambition vs the #270 gate*: the gate holds absolutely — id 8 defines proposal CLASSES under intake-brief-1 §6 (judgment sleeps), never new autonomy; the Sonnet/Haiku subagent-army is a pattern to pilot inside those classes, not doctrine. (5) *Role hygiene*: the closing chat played functional+technical+developer simultaneously (pre-split legacy); the next cycle enforces the split — the architect chat does grooming/rulings only, elicitation goes to functional chats, builds to developer lanes. (6) *Fable economics*: Fable left subscription inclusion 2026-07-07 → the tactical window override is EXPIRED and the XL row's own pricing condition has FIRED — the routing question is live again (Q4).

**Q3 — Considered + rejected (do NOT relitigate).** Letters for story/epic ids (collision class) · accept-Track-X-as-durable for ai-council (re-pegged instead; 6/7 repos already story-map) · fixing census findings at night without rulings (judgment sleeps; census is read-only by design) · `ECOSYSTEM.md` at Dev root (memory error; actual mechanism = `ecosystem/REGISTRY.md` human + `ecosystem/<repo>/` machine — landed) · wholesale filing of the brainstorm's 34 candidates (F1; selective admission after kills only) · reverting the #269 early hook (offered, declined) · cloud Artifacts as the Tier-4 dashboard surface (operator counter-direction: LOCAL HTML in VS Code — id 9 explicitly counters intake-4 §S4) · closing #43 (kept open as the decomposition target, folded into id 6) · mass-renaming existing intake/audit files for the {func|tech} tags (going-forward only) · night re-testing of the fresh corp/ai-council deploys (theatre — enforcement was trip-proven same-day) · pre-filling SUPPLEMENT before the questions arrive (this session's process error; the flow is questions → fill).

**Q4 — Open questions (unresolved or deliberately deferred).** (1) The grooming-cadence mechanism — digest escalation vs recurring peg vs a #270 `[load]` sub-line: decide in-session, this is the session's second deliverable. (2) `S<n>` schema mechanics — numbering ratified; the validate_backlog schema extension is design-at-build. (3) #264 — build-vs-park against the local-HTML target; the operator's one-sentence usability verdict on the Arc-5 P6 dashboard is STILL outstanding and is the pilot's own ex-ante gate. (4) Bootstrap-vs-onboarding boundary — id 6 vs #131/#215/#280: one flow or two? resolve at triage. (5) XL/Fable tier — the amendment's pricing condition fired 07-07: keep XL under usage-credit economics or fall back to Opus; record the ruling (this closing arc itself is empirical input: multi-document adjudication quality high). (6) #285 PLAYBOOK genuine end-to-end re-read + stamp — its own arc. (7) CLAUDE.md §9 hook-roster drift (audit-index-freshness missing from the roster) — small, #269-adjacent, unfiled. (8) Operator words pending: corp-ops `tasks/` delete · ai-council/corp-monorepo worktree-orphan deletes · corp-ops remote yes/no. (9) The operator's functional smoke session — still pending; the natural topic is id 6's SEED→DRAFT elaboration (that IS functional-architect work, killing two birds). (10) H-theme residual: #288 filed detector-only; the prevention/policy half deliberately unfiled — revisit only if the detector ever fires.

**Q5 — Decomposition rationale (do not redo / re-decide).** Shape: **groom (shrink) → admit (SEEDs + selective brainstorm Wave-1) → sequence Wave-1 consumer arcs (night-2, frozen contracts) → build waves.** Why this shape: every downstream decision — what to build, what the fleet onboards, which SEEDs consume attention — depends on a backlog that reflects real intent; grooming first makes every later triage cheaper, and the census (trust HIGH, 22/23) plus the worksheet make it cheap NOW. Do NOT redo: the census (fresh) · the worksheet (generated — rule on it, never regenerate it) · the outgoing first-cut pre-triage (quick-close #122/#229 on operator words; merge #26→#164 and #161→#164-or-kill; #43→id6 already folded; kill-candidates #1/#133/#155; re-scope #164 to S/M; batch #106+#126+#155 into one posture session) · the intake relocation/conventions · the Wave-0 rulings · Arc 1–5 closures. Do NOT re-decide: everything in Q3. Wave-1 consumer arcs (ai-council story-map convergence + docs/intake taxonomy + Mermaid blocks; corp-sca §9+ruff-wire+push; corp-ops README+tasks/+remote; corp-monorepo #262 sites + classifier de-dup; demo-prep/life-architect methodology deploys — greenfield-skip already handles them) run AFTER grooming rulings, per-repo, ADR-41.

**Q6 — Off-repo context.** Fable 5 left subscription inclusion 2026-07-07 (usage-credit economics, ~2× Opus) — the window override is dead; route per ADR-70 + the XL amendment with its condition now fired (Q4.5). The operator's standing repeat-themes ALL have files now (intake ids 6–9 + #288) — future repetition is a pointer to the intake index, not re-elicitation; this was the operator's explicit frustration and it is structurally closed. Operator direction on dashboards: local HTML openable inside VS Code in-session, NOT cloud artifact pages. demo-prep is externally active (census caught its state changing between reads) — verify its live state before any demo-prep work. The grooming worksheet lives at `docs/audits/<date>-grooming-worksheet.md` (83/33/7 split, per-story S-id proposals). Operator's pending personal actions: the #264 usability sentence · the functional smoke session (id 6 topic) · the three one-word approvals. This fill was written by the outgoing architect in the closing browser session on 2026-07-08 and SUPERSEDES the premature fill emitted earlier in the same chat (before the questions arrived — process note for the incoming §13(d) beat: narrow to "anything changed since THIS fill?").
