---
intake-id: 4
status: SEED
origin: "Arc-5 pilot outcomes, 2026-07-06/07 (intake doc #2 AC2 follow-up channel)"
consumed-by:
---

# SEED batch — Arc-5 pilot follow-ups (one batched doc per the feed pattern)

Pre-intake candidates from the Arc-5 buy-vs-build pilots (`docs/audits/2026-07-06-arc5-buy-vs-build-verdicts.md`). A feed proposes; it never decomposes and never mutates the backlog. Each row is a candidate for a future functional/technical pass, not a commitment.

## S1 — /batch real-scale pilot (the n=2 gate named by verdict 2)

One real epic-sized /batch run UNDER REAL PERMISSIONS (the toy pilot ran permission-bypassed in a contained sandbox by design): ADR-97 grants at epic scale, our deny-first floor active, cockpit observability re-checked. Only after that evidence may any PLAYBOOK Ch8 hand-provisioning retirement be proposed. Caveat carried: /batch is officially undocumented (in-binary only) — schema drift risk; the changelog sentinel should watch for its docs debut.

## S2 — Outcomes WATCH (verdict 3)

Outcomes is Agent Platform-only today. Trigger: any changelog/docs line bringing outcome-grading to Claude Code surfaces. The pilot design is PRE-REGISTERED in the verdicts doc (rubric = epic done-contract verbatim · ground truth = the architect's recorded UAT walk · heterogeneous grader per the Codex doctrine, never the lane's own model) — do not re-derive it at pickup.

## S3 — Ultraplan attended availability check (verdict/P4 residual)

`/ultraplan` refused the headless environment on 2.1.202 ("isn't available in this environment") despite binary+docs presence. One attended interactive attempt by the operator (any repo with a GitHub remote, e.g. the hub) settles availability cheaply; if it works, the #213 draft-only comparison pilot is pre-scoped in the Arc-5 plan.

## S4 — #264 unparking recommendation (verdict 4; the architect rules)

RECOMMEND unparking #264 re-scoped: the Tier-4 human-facing visualization surface = Artifacts pages (P6 evidence: parity + dual-theme + zero-dep private page in one session); `mermaid_emit.py` wiring narrows to genuinely graph-shaped content embedded in Artifact pages, or retires entirely (safe-removal-gated, operator-gated) if the graph class also goes native-HTML. Data-classification note carried: publishing sends repo-derived content to claude.ai hosting (default-private) — per-surface operator call.
