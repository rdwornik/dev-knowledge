---
id: "[#702]"
title: "The repo declares zero `cron:` and zero `schedule:`, so the nightly layer `[S20]` exists to revive has nothing to revive"
status: open
priority: P1
size: M
theme: "[E7] Tooling & evaluation"
story: "[S20] Revive the nightly layer, load-gauge first (rent-rule discipline)"
generates: BACKLOG.md
---

- [#702] [P1][M] **The repo declares zero `cron:` and zero `schedule:`, so the nightly layer `[S20]` exists to revive has nothing to revive** - Named in the batch X roster section 4 as "nightly routines (B7, zero `schedule:`) -- after X1-3", carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93). LEDGER row B7 carries status **NOTHING** and the census finding behind it: zero `schedule:` in the repo, the only live schedule being `fleet_health` at 09:00, and "`[S20]` has nothing to revive". · **A whole story exists over an empty set.** `[S20]` is "Revive the nightly layer, load-gauge first", and it carries rows -- `[#271]` the nightly proposal loop, `[#419]` routines whose output nobody consumes, `[#348]` grooming as a standing routine, `[#123]` routine observability. Each is written as though a nightly layer exists and needs improving. None of them fires, because nothing schedules anything. The story's rows are improvements to a thing that is not running. · **"Nightly" is currently a word in prose, and that is the precise defect.** A routine declared with a cadence in a document and no schedule on a runner is the harness definition's INVENTORY case wearing a timetable -- it reads as a process and fires for no event. `[#657]` is the proof from the other side: the census found zero declarations while the integrator watched files appear and self-revert at 01:00, so the only thing actually running on a clock is a writer OUTSIDE the repo that the registry does not know. · **Sequenced after conductor E, by the roster's own words.** There is no runner to schedule against until lanes run on GitHub Actions; scheduling against the operator's laptop is the arrangement conductor E exists to end. So this row waits on that lane rather than racing it. · Done when: every routine declared to run nightly carries a real schedule on the runner rather than a prose cadence, and a check FAILS when a routine declares a nightly cadence that no schedule backs -- so the empty-set state this row records cannot recur silently · **Done-when authored by the filing lane, not carried**: the roster names this item in a section-4 prose list that carries no Done-when column · **Blocked on** the conductor E lane (`[#689]`) -- the roster's "after X1-3" · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, section 4), `to-browser/LEDGER-OPERATOR-EXPECTATIONS-2026-09-10.md` row B7 (status NOTHING, the census numbers), `docs/audits/2026-09-08-technical-process-trigger-census.md` (the census), `[#657]` (zero declarations while something writes at 01:00 -- the same fact from the other side), `[#689]` (conductor E, the runner this row waits for), `[#271]`, `[#419]`, `[#348]`, `[#123]` (the `[S20]` rows written over the empty set), `[#270]` (the load gauge the story sequences first) · kill-candidates: none -- `[#271]` owns ONE routine's constraints and presumes a scheduler exists to run it; `[#419]` owns whether a routine's output is consumed, which is a question about routines that run · **Archived annotations:** `tasks/archive/702.md`
