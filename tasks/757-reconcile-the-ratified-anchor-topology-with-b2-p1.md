---
id: "[#757]"
title: "Reconcile the ratified anchor topology with B2, P-1 and the `DEFINITION_OF_DONE` wording"
status: open
priority: P1
size: S
theme: "[E5] Canonical-file integrity"
story: "[S14] Keep the day-to-day docs right-sized and current"
generates: BACKLOG.md
---

- [#757] [P1][S] **Reconcile the ratified anchor topology with B2, P-1 and the `DEFINITION_OF_DONE` wording** — the operator ratified, 2026-09-14, that **a JOURNAL entry carried INSIDE each merge commit replaces one-anchor-per-batch**, recorded as `STANDING_RULINGS.md` **B8**. The ratification is live; three surfaces still state the shape it replaces, and a seat reading them will revert it. · **A · `STANDING_RULINGS.md` B2 "JOURNAL-rides-the-branch" prescribes a shape that is UNAVAILABLE for a lane merge.** B2 says to put the entry "on the **work branch**, last, naming the work commits". For an integrator merging a handed-back lane, the work branch is the LANE's branch — and **P-1 (`STANDING_RULINGS.md:1789`) says `JOURNAL.md` is the integrator's surface and a lane records its work in its artifact**, so the integrator may not author there and the lane may not author the entry. B2 and P-1 are each correct and jointly leave the lane-merge case with no sanctioned shape; B8 supplies one. B2 needs an explicit carve-out naming the lane-merge case, not a rewrite — its reasoning about journal-only branches being structurally unanchorable is what makes B8 correct · **B · The reason B8 is simpler to verify, which is the ratification's stated ground:** the entry and the SHA it anchors arrive in the same commit, so `is_anchored(merge)` is decidable from the merge alone and cannot be left pending on a later arc. The shape it replaces could leave a spine entry unanchored between two arcs — **which is the gap actually repaired by hand on 2026-09-14** (`e6acb23e`, discharged at `999fdd54`) · **C · What must NOT be lost in the reconciliation.** B6's append-only discharge is untouched and still binds. The entry must name a SHA in `introduced(merge)` — in practice the **lane tip**, never the merge's own hash, which cannot exist when the entry is authored (the defect that bit `e6acb23e`). And the mechanical enabler belongs in the wording: `git merge --no-commit --no-ff <tip>` → stage the entry → `git commit`, because a merge the merge machinery completes fires `pre-merge-commit`, which is not installed, while a merge finished by `git commit` fires `pre-commit`, which is — measured 2 hooks vs 31 on 2026-09-14 · **D · `protocols/DEFINITION_OF_DONE.md`'s journal leg** requires a wrap entry referencing >= 1 commit SHA produced this session; under B8 the entry is per-merge rather than per-session, so the leg's wording should say which unit it means before a seat reads "per session" as "one entry is enough for a six-merge batch" · Done when: `STANDING_RULINGS.md` B2 carries an explicit lane-merge carve-out pointing at B8; B8 states the two-step merge as the mechanical requirement and names the lane-tip rule; `protocols/DEFINITION_OF_DONE.md`'s journal leg states the per-merge unit; and no live surface still prescribes one-anchor-per-batch · refs `protocols/STANDING_RULINGS.md` B2 / B6 / B8 / P-1, `protocols/DEFINITION_OF_DONE.md`, `scripts/journal_anchor.py`, `scripts/block_unanchored_push.py`, `JOURNAL.md` 2026-09-14 (d)(e)(f) · kill-candidates: none — this row exists because a ratified change outran the documents that state the old rule; it retires no open id · source: operator ratification 2026-09-14, batch Y integration
