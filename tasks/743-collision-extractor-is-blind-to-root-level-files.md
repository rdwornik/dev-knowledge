---
id: "[#743]"
title: "The step-0 collision extractor requires a slash, so two lanes may both declare a root-level file and pass"
status: closed
priority: P1
size: S
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#743] [P1][S] **The step-0 collision extractor requires a slash, so two lanes may both declare a root-level file and pass** - Operator ratification 2026-09-13 item 3 (`to-browser/RATIFICATION-2026-09-13.md`), which held `[#675]` open as NO-GO and ruled its three verified false-pass holes into rows. This is the second. · **The defect, resolved in the code:** `scripts/seat_refusals.py:304-306` defines `_CONTRACT_PATH_RE` with a leading-delimiter alternation followed by a directory group of the form (dot-optional name segment, then a slash) quantified `+`, then a filename and a 1-6 character extension — the directory group is `+`-quantified, so **at least one `/` is structurally required for a path to be seen at all**. `_WRITE_ROOTS` immediately below then filters to eleven directory prefixes, every one of them slash-terminated. **A tracked file at the repo root can never be extracted and can never collide.** · **The invisible set is not obscure — it is this repo's most-contended surface.** `CLAUDE.md`, `ARCHITECTURE.md`, `JOURNAL.md`, `BACKLOG.md`, `AGENTS.md`, `README.md`, `LESSONS.md`, `.pre-commit-config.yaml`, `pyproject.toml`, `uv.lock`, `.methodology.yaml`. Two lanes may both declare `ARCHITECTURE.md` in their Done-contract and both pass step 0. · **It is live RIGHT NOW, which is why this is P1 rather than a note.** The same operator ratification (item 7) GO'd cuts to `ARCHITECTURE.md` (root) and `protocols/PLAYBOOK.md` (not root). A lane executing the ARCHITECTURE cut and any other lane touching `ARCHITECTURE.md` would be dispatched concurrently with no refusal — and the whole point of a STEP-0 refusal, per `[#675]`'s target 4, is that it is "evaluated on the frozen contract set before the first worktree exists, never as a warning afterwards, because run later the collision it exists to prevent has already been paid for and every remaining option is a teardown". · **The comment above the regex is the thing to preserve while fixing it:** it says the shape is deliberately the same one `file_purpose_graph._REL_PATH_RE` uses, "because two organs disagreeing about what counts as a path is a class of defect this repo already carries". So the fix is NOT to fork the regex — **either both organs learn root-level files, or the divergence is recorded as deliberate with the reason.** Check `_REL_PATH_RE` for the same blindness before changing either: if the graph shares it, this row has found a second and larger instance. · Done when: a Done-contract declaring a root-level tracked file (`ARCHITECTURE.md`, `.pre-commit-config.yaml`, `pyproject.toml` and the rest) is extracted by the collision checker and two lanes declaring the same one are REFUSED at step 0; **one test FAILS before the fix** — a RED-first witness per ADR-108 §B feeding two contracts that both declare a root-level file and asserting a refusal, still RED if the slash requirement is reintroduced; `_WRITE_ROOTS` admits the root without admitting the transport paths, absolute operator paths and prose nouns its comment exists to exclude (a bare `*.md` admission that lets `LANE-x-000-other.md` through is a regression, not a fix, and a test asserts that too); and **`file_purpose_graph._REL_PATH_RE` is checked for the same blindness in the same pass** — if it shares it, that is recorded here and either fixed with it or split with a stated reason · **Done-when authored by the integrator seat** from the ratification's item 3 and the code read at `scripts/seat_refusals.py:298-312` · refs `scripts/seat_refusals.py` (lines 300-312: `_DONE_CONTRACT_RE`, `_CONTRACT_PATH_RE`, `_WRITE_ROOTS`), `tests/test_seat_refusals.py`, `scripts/file_purpose_graph.py` (`_REL_PATH_RE` — the organ the comment says this shape is deliberately shared with), `[#675]` (target 4 — the parent row, held OPEN by the same ratification), `[#742]`, `[#744]` (the other two holes from the same review), `to-browser/CLOSURE-VERDICTS-2026-09-13.md` (the review finding), `to-browser/RATIFICATION-2026-09-13.md` (item 3), ADR-108 (§B, RED-first), ADR-110 (the lane apparatus step 0 guards), ADR-111 · kill-candidates: none -- `[#675]` owns the six-target programme and stays open as this row's parent; no open row owns the contract path grammar. `[#743]` and `[#742]` are siblings, not duplicates: one is a dispatch-time extractor blind to a path shape, the other a verdict reader degrading an error into a PASS · source: operator ratification 2026-09-13 item 3, filed by the integrator seat at batch X3 close · **CLOSED 2026-09-16** — evidence ed93586f
