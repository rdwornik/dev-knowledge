---
id: "[#633]"
title: "/boot-session gains HISTORY DELTA and EQUILIBRIUM MAP — two GENERATED sections"
status: open
priority: P2
size: M
theme: "[E1] Handoff continuity"
story: "[S1] Match the handoff payload to the work mode"
generates: BACKLOG.md
---

- [#633] [P2][M] **/boot-session gains HISTORY DELTA and EQUILIBRIUM MAP — two GENERATED sections** — **Operator seed 2026-09-01, the first `/boot-session` extension; reconcile with intake #66 and `[#611]`.** **(1) HISTORY DELTA** — this bundle's numbers against the PREVIOUS bundle's, with a **direction per metric**; `docs/handoffs/` is an immutable time series and the input already exists as two committed `FUNNEL_HEALTH.md` files from `funnel_lifecycle.measure` (FM-2). **(2) EQUILIBRIUM MAP** — per dimension (execution, funnel, contracts, routing, review, eval, observability, graph, memory/distiller, planning, doctrine) **who currently drives**: **REPO** mechanism / **BROWSER** judgment / **OPERATOR** decision, derived from the organ inventory plus the decision tree. Metric set: operator process questions (**target 0**), budget-decided vs escalated, architect rulings per batch, OPERATOR ASKS re-asked, validator-caught vs human-caught premise errors, review coverage, orphans/rot, off-laptop lane share, credits per lane per model. **The seat reads the map to know where it is still needed; the DIRECTION is the equilibrium's own health check** — a map that never moves BROWSER→REPO is not maturing, and one that moves OPERATOR→REPO without a ruling is drifting. **A WORKED EXAMPLE ALREADY EXISTS and is this row's input, not a duplicate of it:** `docs/audits/2026-09-01-technical-v7-history-delta-equilibrium-map.md` hand-derives both from live state, so the row builds a GENERATOR against a concrete target rather than a description — and its own sharpest output is already actionable (`routing = OPERATOR`: a routing table exists in every contract, `Start-DispatchLane` defaults to opus and ignores it, and `[#631]` fired three times in one window). **Two of the nine metrics are NOT yet derivable and the row must not fake them:** credits per lane per model is UNDERIVABLE until `[#615]` lands the attribution trailer, and the first HISTORY DELTA is n=2 because `FUNNEL_HEALTH.md` is itself a v7-era artifact. · Done when: `/boot-session` emits both sections from live state with no hand-authored numbers; HISTORY DELTA selects the previous bundle by the same git-add-date predicate `audit.py::_select_active_bundle` uses, never lexical order; the EQUILIBRIUM MAP's driver per dimension derives from a checkable surface rather than an assertion; and a metric with no live source renders **ABSENT with its reason** instead of a proxy · refs `docs/audits/2026-09-01-technical-v7-history-delta-equilibrium-map.md`, `docs/audits/2026-09-02-technical-batch-f-close-packet.md` §8, `protocols/HANDOFF_PROCESS.md` §17, intake #66, `[#611]`, `[#615]` · kill-candidates: none — an operator-seeded batch-G row closing nothing; `[#611]` stays open on its own measured size bar · source: operator seed 2026-09-01 (batch-G)
