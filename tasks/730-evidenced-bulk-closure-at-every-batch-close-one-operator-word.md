---
id: "[#730]"
title: "Work lands on main and its row stays open, because nobody hands the operator closures in bulk -- the close packet lists what the merges witnessed"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S7] Wire up the lifecycle hooks the workflow relies on"
generates: BACKLOG.md
---

- [#730] [P1][M] **Work lands on main and its row stays open, because nobody hands the operator closures in bulk -- the close packet lists what the merges witnessed** - AX10-1 of `to-cc/AMEND-BATCH-X-ROSTER-010.md`, carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93) Part 10. · **The act: at every batch close, the close packet lists every row whose Done-when the batch's own merges WITNESSED** -- row id · Done-when verbatim · the merged SHA · the test or gate that proves it -- and asks the operator for ONE word to close them all. · **The load-bearing clause is the refusal, not the list: a row with NO witness is not listed.** That single constraint is what separates this from every closure mechanism already filed. `[#277]`, `[#487]` and `[#693]` are all repairs to the DETECTOR -- a machine inferring closures from its own signal, which is why `[#693]` exists at all (it proposes the whole queue). This row proposes nothing. It reports what the batch's merges already proved, and a row the merges did not touch cannot appear no matter how finished it looks. Closing stays the operator's act; what changes is that he is handed evidence instead of a queue. · **The defect is measurable on this repo right now, which is why the row is filed rather than argued.** AX10-1's own first-application list is batch W: `[#683]` (/override removed), `[#638]` (proof layer), `[#278]` (test selection), `[#688]` (harness definition) and `[#684]` (the guard, if merged). Checked against `main` on 2026-09-11: four of the five have their work merged on the first-parent spine -- `[#683]` at `0bb1d5ce`, `[#638]` at `69a0266c`, `[#278]` at `e4929b09`, `[#688]` at `365ae7d1` -- and **all five rows are still `status: open`**. `[#684]` is the honest exception: its lane branch is unmerged, so under this row's own rule it would NOT be listed. The mechanism's first run is already specified by live evidence. · Done when: every batch close emits an evidenced-closure list in its close packet carrying, per row, the id, the Done-when verbatim, the merged SHA and the proving test or gate; a row with no witness is refused from the list rather than listed with a weaker claim; the operator closes the whole list with one word; and the batch W list above is the first instance, with `[#684]` included only if its lane has merged by then · **Done-when authored by the filing lane** from AX10-1, with the no-witness refusal kept as an acceptance clause because it is the clause that makes the list trustworthy · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, Part 10 AX10-1), `to-cc/AMEND-BATCH-X-ROSTER-010.md` AX10-1, `[#731]` (the other half -- relocation and the closure budget), `[#693]` (the detector proposing the whole queue), `[#277]` and `[#487]` (detector repair and its consumption arc), `[#552]` (window-close disposition -- audits and terminal ADRs, a different object), `[#589]` (the view the backlog is read through) · kill-candidates: none -- `[#277]`, `[#487]` and `[#693]` all own the DETECTOR and its signal, and repairing a proposer does not produce an evidence list from merges; `[#552]` closes audits and terminal ADRs rather than task rows. Read all four beside this row at grooming: if the detector is repaired to the point that it can cite a merge SHA and a proving gate per proposal, this row folds into it · source: `to-cc/AMEND-BATCH-X-ROSTER-010.md` AX10-1, filed by lane `lane-x-000-batch-x-roster-lands`
