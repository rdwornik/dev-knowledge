---
id: "[#760]"
title: "X3 — `ARCHITECTURE.md` to <= 15 KB behind the step-D render, starting from the measured eleven chapters"
status: open
priority: P1
size: L
theme: "[E5] Canonical-file integrity"
story: "[S14] Keep the day-to-day docs right-sized and current"
generates: BACKLOG.md
---

- [#760] [P1][L] **X3 — `ARCHITECTURE.md` to <= 15 KB behind the step-D render, starting from the measured eleven chapters** — **this is a wave X3 row, filed by operator ruling 2026-09-14 when the target LEFT batch Y**, whose lane proved it could not be met there. It is NOT a restatement of `[#755]` clause A; it is where that clause's measurement goes so an X3 lane starts from a number instead of a premise · **A · The render premise was REFUTED three ways, and the row starts from that rather than re-testing it.** `scripts/file_purpose_graph.py` exposes only `why` and `stats` — **there is no emit/render subcommand**, so nothing can render Ch2 from the graph; **no script in the repo WRITES `ARCHITECTURE.md`** (`audit.py`, `codemap/check.py`, `canonical_docs.py`, `canonical_freshness_gate.py` all only READ it); and `[#664]`'s own body sequences the work — *"step D (the `ARCHITECTURE.md` Ch2 render) plus the organ map are X3"*. So the render must be BUILT, through `[#667]`/`[#664]` step D, not routed around · **B · The measurement, 100,845 B across eleven chapters** (lane `lane-y-755-docs-cut-finish`, 2026-09-14): Organ map 25,251 B (11,157 B of it 37 table rows) · Validators and enforcement 24,927 B (**only 1,476 B in tables — ~23 KB is PROSE**) · Governing ADRs 11,920 B · Verification mesh 10,574 B · Distribution 6,870 B · Key conventions 6,124 B · Automation axes 5,918 B · Layer boundaries 2,970 B · Purpose 2,099 B · Authority 1,518 B · Codemap 906 B. **A table render alone cannot reach 15 KB when the bulk is prose** — that is the arithmetic this row exists to confront · **C · Two constraints an X3 lane must not trip.** `ecosystem/organ-index.md` states in its OWN header that it does not carry failure posture and that "Ch2 carries it by hand and stays the source for it" — **so Ch2 is not redundant with the generated index and cannot simply become a pointer to it**. And the 246-line review prologue `[#667]` expects to relocate into a JOURNAL entry **is already gone** (the header is now a ~25-line "How to read this doc"), so that byte budget cannot be spent twice · Done when: `ARCHITECTURE.md` is <= 15 KB with its Ch2 table rendered from the spine graph behind a freshness hook, delivered through `[#667]`/`[#664]` step D; Ch2's failure posture survives the cut with a named home; and the eleven-chapter measurement is re-taken at landing so the reduction is evidenced rather than asserted · refs `[#755]` clause A (this row's source measurement), `[#667]`, `[#664]` step D, `docs/audits/2026-09-14-technical-lane-y-755-docs-cut-finish.md`, `ecosystem/organ-index.md`, `scripts/file_purpose_graph.py` · kill-candidates: none — this row receives a target that left batch Y by ruling; `[#755]` keeps its other five residues and does not close here · source: operator ruling 2026-09-14, batch Y close
