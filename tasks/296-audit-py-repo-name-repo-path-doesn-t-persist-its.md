---
id: "[#296]"
title: "`audit.py repo <name> --repo-path` prints a report path that isn't there"
status: closed
priority: P3
size: S
theme: "[E2] Enforced governance"
story: "[S4] Extend structural validation to more governance artifacts"
serialize-group: audit-py
generates: BACKLOG.md
---

- [#296] [P3][S] `audit.py repo <name> --repo-path` prints a report path that isn't there (ai-council pilot G6) — the command exits 0 and prints `Report: …docs/audits/<date>-<name>-audit.md`, while `--help` claims "Same state.yaml / history / report writes as `run`". **Live repro 2026-08-06 CONFIRMS a misleading locator, not a lost report** — that path does NOT exist in the working tree and `git status` stays clean, but the report is real: it lands as a commit on `automation/fleet-audit` (ADR-80 replication), beside the `ecosystem/ai-council/history/` daily. So the printed path misleads any caller who looks where it points, and the row's original guess (a suppressed write or swallowed exception) is refuted. Row stays OPEN, re-aimed at the locator rather than at a missing write. · Done when: the command writes the report where it says, or prints where it actually lands, with a test · refs scripts/audit.py, #215 · serialize-group: audit-py · DEFER — the 45-day icebox sweep of 2026-08-27 (C4 §3, ruling X1's final step, night-harvest governance session). Peg: **no row-level dependency is claimed** — this is an ATTENTION decision, not a blocked-by. Un-defers when an arc claims the row or the operator re-prioritises it. Recorded explicitly so this row is never mistaken for one waiting on another row, which is the failure the same session's re-peg step was fixing. · **CLOSED 2026-08-28** ([#296], K4 DO-IT — "a refuted premise reduced to a one-line fix should be landed, not carried"). The row took the **second** branch of its own Done-when ("or prints where it actually lands"), because the first would be wrong: the report is not misplaced, it is deliberately isolated. `_commit_routine_outputs` records the durable scope onto `automation/fleet-audit` via `commit-tree` plumbing and then RESTORES the working tree (ADR-84 writer isolation), so the written path is gone by the time the command returns — writing it into the tree instead would break the invariant the isolation exists to hold. **Changes:** `_commit_routine_outputs` now returns `bool` (True = on the branch, freshly committed or already there under an identical tree; False = every fail-soft early exit), because "where it landed" and "whether it landed" are the same question and a caller that cannot ask it prints a path to nothing; new `report_locator()` renders the one honest sentence, repo-relative, naming the branch AND the exact `git show automation/fleet-audit:<path>` invocation; both call sites (`cmd_repo`, `cmd_run`) use it. The `--help` text that claimed *"Same state.yaml / history / report writes as `run`"* without mentioning the restore is corrected at the same time — it was the other half of the misleading locator. **Six tests**, including the end-to-end pin on a real git repo that asserts BOTH halves at once (the file is absent from the worktree AND present in `ls-tree` of the branch), so a future "fix" cannot satisfy one by breaking the other, plus the not-recorded branch which must never name a branch path with nothing at it
