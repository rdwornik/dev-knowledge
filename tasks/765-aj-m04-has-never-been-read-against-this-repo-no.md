---
id: "[#765]"
title: "AJ M04 has never been read against this repo -- no SHIPPED / OWNED / UNOWNED matrix exists, so its practices are neither adopted nor refused"
status: open
priority: P2
size: M
theme: "[E7] Tooling & evaluation"
story: "[S19] Decide the undecided artifact/tool models"
generates: BACKLOG.md
---

- [#765] [P2][M] **AJ M04 has never been read against this repo -- no SHIPPED / OWNED / UNOWNED matrix exists, so its practices are neither adopted nor refused** -- the M04 corpus landed on the operator's disk on 2026-09-14 (seven lessons, `AJ_M04L01..L07_transkrypcja.pdf` plus `AJ_M04_transkrypcje.md.zip` and `AJ_M04_ebook.pdf`, at `C:\Users\1028120\Documents\Priv\Architekt Jutra\`) and **a case-insensitive sweep of `docs/intake/`, `docs/audits/`, `tasks/`, `BACKLOG.md` and `protocols/` for `m04` returns two hits, both unrelated** -- a 2026-07-27 spike naming its own "module 4", and a wall-clock string `61m04s` in the N3 instrumentation audit. Zero surfaces in this repo name the module. · **Why that is a finding rather than a backlog gap.** ADR-111 rules that every finding is triaged into exactly one of OWNED / DISCHARGED / CANDIDATE / REJECTED, and the AJ corpus is the one input stream this repo reads from outside itself. M01-M02 were dispositioned by intake #70 and M03 by the 2026-09-10 night bundle; M04 sits in neither state and in no third one. An unread module is not REJECTED -- nobody refused it -- and it is not OWNED, because no row names it. It is outside the funnel entirely, which is the shape `decision_coverage` exists to refuse one layer up. · **The two adjacent rows do NOT cover it and this row does not restate them.** `[#695]` owns intake #70, whose scope is M01-M02 and whose defect is an ACCEPTED decision with zero implementing rows -- a disposition defect about material already read. `[#696]` owns the M03 leg-1a RE-RUN, whose defect is that evidence already produced cannot be re-opened -- an evidence-quality defect about material already read. Both are about the READING that happened; this row is about the reading that has not. · **First pass, deliberately, and the scope says so.** The deliverable is a matrix in which every M04 item lands in exactly one of SHIPPED (with the commit that landed it), OWNED (with the row id that owns it) or UNOWNED (which becomes a row) -- not an adoption, not an ADR, and not a second pass over M01-M03. A claim resolved to neither a commit nor a row id is a claim rather than evidence and stays UNOWNED. · **The corpus is local-only and that is why the lane is, too.** `[#660]` records the same constraint for the tools-and-evals table -- *"the source corpus sits on the operator's disk rather than in any repo, which is the reason the lane keeps being deferrable"* -- so the matrix must carry its own source locators for anyone who cannot open the corpus. · Done when: every item the M04 corpus teaches is recorded in one tracked repo artifact under a SHIPPED / OWNED / UNOWNED verdict that is exhaustive and mutually exclusive, each SHIPPED verdict resolving to a commit and each OWNED verdict to an open row id, and no UNOWNED item is left without a filed row · refs `docs/audits/2026-09-10-technical-night-aj-m03/MISSION-PROMPT.md` (the corpus path and the M03 precedent for reading it), `docs/audits/2026-09-10-technical-night-aj-m03/MATRIX.md` (the matrix shape this pass narrows), `[#695]` (intake #70 -- M01-M02, disposition defect), `[#696]` (M03 leg-1a -- evidence defect), `[#660]` (the local-only corpus constraint), `[#722]` (agy as READER ONLY, which is how the corpus may be read), `[#627]` (agy's inert admission, the standing caveat on that route), ADR-111 (the funnel this module sits outside of) · kill-candidates: none -- `[#695]` and `[#696]` each own a defect in a DIFFERENT module's already-completed reading and would survive this row's closure unchanged; `[#660]` owns a tool-by-tool table across the whole corpus rather than one module's practice set, and folding this into it would lose the module boundary that makes the matrix resolvable · source: batch Z lane z-10, frozen contract `LANE-z-10-aj-m04-first-pass.md`
