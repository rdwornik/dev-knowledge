---
id: "[#729]"
title: "FPG-1's queries are shell scripts the model must remember to run -- expose them as MCP tools so they sit in the tool list beside Grep"
status: open
priority: P2
size: M
theme: "[E7] Tooling & evaluation"
story: "[S18] Cut session friction with better tooling"
generates: BACKLOG.md
---

- [#729] [P2][M] **FPG-1's queries are shell scripts the model must remember to run -- expose them as MCP tools so they sit in the tool list beside Grep** - AX9-3 of `to-cc/AMEND-BATCH-X-ROSTER-009.md`, X2, carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93) Part 9. · **This is the REACHABILITY leg of AX9's thesis** -- `[#727]` refuses the wrong tool, `[#728]` makes the right one discoverable, and this row puts it in the same place the wrong one lives. The asymmetry is the whole argument: `Grep` is a first-class tool in the model's list, while `graph_queries.py why` is a shell invocation that has to be recalled, composed and interpreted. The organ loses that contest on ergonomics alone, every time, regardless of which is the better answer. · **A minimal stdio MCP server, and MINIMAL is the constraint that matters.** It exposes `why`, `process-list`, `stats` and the test selector. **No new query logic** -- AX9-3 says so explicitly, and the server CALLS THE EXISTING SCRIPTS. A server that reimplemented the queries would be a second implementation of the graph, which is exactly what APR-1 refuses one layer down with "no second graph". · **Library-first is named in the amendment** and points at the MCP protocol itself: the server is the protocol's minimal stdio shape rather than a bespoke integration, which is what makes the next clause possible. · **The multi-provider payoff is the reason to prefer a server over a set of local skills:** Codex and agy can reach the SAME server. Under `[#722]`'s interim routing, read-only work goes to agy -- and agy running raw greps over a repo whose structure is already computed is the same waste one provider further out. One server serves every admitted reader. · Done when: a minimal stdio MCP server exposes FPG-1's `why`, `process-list` and `stats` plus the impacted-test selector as tools, with NO query logic of its own -- it calls the existing scripts, proven by a test that the server's output matches the script's for the same input; the tools are reachable from CC and from at least one non-Claude admitted provider; and adding a query to the scripts does not require hand-editing the server · **Done-when authored by the filing lane** from AX9-3's clauses, with the no-second-implementation property made testable · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, Part 9 AX9-3), `to-cc/AMEND-BATCH-X-ROSTER-009.md` AX9-3, `scripts/graph_queries.py` (`why`, `process-list`, `stats`), `scripts/impacted_tests.py` (the selector), `[#727]` (the refusal leg), `[#728]` (the discovery leg), `[#722]` (interim agy routing -- the non-Claude reader that reaches the same server), `[#715]` / `[#664]` (FPG-1 itself -- this row exposes it and does not extend it) · kill-candidates: none -- `[#664]` and `[#715]` own the GRAPH and this row owns its reachability, adding no query and no store; `[#728]` makes the same organs discoverable as skills, which is a different surface from a tool the model can call · source: `to-cc/AMEND-BATCH-X-ROSTER-009.md` AX9-3, filed by lane `lane-x-000-batch-x-roster-lands`
