---
id: "[#754]"
title: "The two backlog row ceilings measure different corpora, and the source-side one has decayed back into the percentile its own amendment abolished"
status: open
priority: P2
size: S
theme: "[E5] Canonical-file integrity"
story: "[S14] Keep the day-to-day docs right-sized and current"
generates: BACKLOG.md
---

- [#754] [P2][S] **The two backlog row ceilings measure different corpora, and the source-side one has decayed back into the percentile its own amendment abolished** - batch Y lane 3, frozen at `docs/audits/2026-09-14-technical-batch-y-launch-contracts/LANE-y-754-backlog-to-bar.md`; the lane's ruling and its evidence are at `docs/audits/2026-09-14-technical-lane-y-754-backlog-to-bar-evidence.md`. · **The pair, stated so it cannot be conflated again.** `gen_task_tree._VIEW_ROW_BYTE_CEILING = 400` counts **bytes** on a `BACKLOG.md` **view** line - the one-line projection `- [#id] [P][size] title · tasks/<file>.md` - and is an anti-re-inflation tripwire: it fires when a row is rendered back at body length. `validate_doc_rot._BACKLOG_ROW_CEILING = 1320` counts **chars** on the **reassembled source body** (`backlog_source.canonical_text`, the `tasks/` bodies). Disjoint corpora, disjoint jobs, disjoint units. Only the first name says which corpus it measures, and that asymmetry is what invites the conflation. · **The lane REFUSED the disjunction it was handed.** Its contract reasoned that because the two measure different corpora, one must be retired or re-based; different corpora is a reason to keep both, and retiring either drops a live refusal. `_VIEW_ROW_BYTE_CEILING` is CONFIRMED untouched - growth-proof, never fired, longest live view row 301 B against 400, and it guards a regression measured at 279,814 B. · **What has actually decayed is the 1320's ROLE.** `[#532]`, 2026-08-16, declared it *"a stated contract"* rather than a rolling percentile, on the stated ground that *"a percentile ALWAYS has members by construction, so no percentile turns a ranker into a detector"* - 10 of 197 rows over it, 5%. The r5p arm-2 lane measured 70 of 224, 31%, p50 1259, and called it *"a line the corpus is sitting on"* while ruling the value *"reported against, never changed"*. Measured 2026-09-14 on 321 rows: **147 over, 46%, p50 = 1302 chars - the median has now CROSSED the ceiling.** A ceiling the median exceeds is a percentile again, which is the exact state its own amendment abolished. · **The value is NOT the fix and must not move.** Raising it is forbidden by this lane's own contract - *"the over-ceiling row COUNT falling rather than the ceiling rising"* - and by the r5p ruling; lowering it is arithmetically empty while 147 rows sit over it. What is owed is a re-declaration that means something - a ceiling with few members, or the honest retirement of the word *ceiling* in favour of *reference line*, which is what the arm already IS since r5p collapsed it to one corpus Finding carrying count, p50/p75/p90 and trend. That is a curated-baseline act and a V-2 escalation class, so the lane recorded it here rather than performing it. · **Carried finding, routed rather than duplicated.** `BACKLOG.md` has been a one-line VIEW since `[#589]` landed `128f5093` on 2026-08-26 and carries no row bodies, so `archive_row_body.py` - which relocates body clauses - cannot move the view by one byte; the lane proved it by measuring the view across 61 relocations. The 72,000 B view bar is `[#589]`'s own Done-when and was 19,281 B over at this lane's base; closing that gap is a mass-closure act on row COUNT, not a narration drain, which is `[#731]`'s enforced closure budget and `[#730]`'s evidenced closure list. · Done when: `_BACKLOG_ROW_CEILING` carries a re-declared role recorded in its own module comment - either a re-based value whose over-population is a minority of the corpus, or the word `ceiling` replaced by `reference line` in the constant name and in the arm-2 Finding text - with the choice ruled by the architect rather than by a lane; `_VIEW_ROW_BYTE_CEILING` and `_BACKLOG_ROW_CEILING` each state their corpus and their unit at their point of declaration; and no surface any longer reports the two as one contract · refs `docs/audits/2026-09-14-technical-lane-y-754-backlog-to-bar-evidence.md` (this lane's ruling and evidence), `docs/audits/2026-09-05-technical-r5p-lane1-docrot-arm2.md` (the standing arm-2 ruling: 1320 reported against, never changed), `docs/audits/2026-08-15-verification-night3-warn-ledger.md` S2 (the `[#532]` split that declared 1320 a contract), `[#589]` (owns the 72,000 B view bar and the projection), `[#731]` (owns the relocation trigger and the enforced closure budget), `[#612]` (owns `scripts/archive_row_body.py`) · kill-candidates: none - `[#589]` owns the view bar and `[#731]` the closure budget, and neither of them owns the source-side ceiling's declared role, which is the only thing this row carries; if a grooming pass folds the ceiling re-declaration into `[#589]` this row closes as absorbed rather than as done · source: lane `lane-y-754-backlog-to-bar`, batch Y wave 1
