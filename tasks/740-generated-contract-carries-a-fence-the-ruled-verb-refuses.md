---
id: "[#740]"
title: "The generator emits a `Dispatch-Lane` fence and the ruled verb admits only a `claude` fence, so `dispatch -DryRun` refuses every conforming generated contract — a SYMPTOM of the two-owner launch path (AX25-1)"
status: open
priority: P1
size: S
theme: "[E7] Tooling & evaluation"
story: "[S18] Cut session friction with better tooling"
generates: BACKLOG.md
---

- [#740] [P1][S] **The generator emits a `Dispatch-Lane` fence and the ruled verb admits only a `claude` fence, so `dispatch -DryRun` refuses every conforming generated contract — a SYMPTOM of the two-owner launch path (AX25-1)** - Filed 2026-09-12 on operator instruction after the batch X2 freeze ran the check rather than trusting it; **re-scoped the same day by AX25-1**, which names this a symptom rather than the defect. · **Witnessed on all three frozen contracts, verbatim:** `dispatch LANE-x-727-fail-closed.md -DryRun` returns *"Refusing: the contract's `## Dispatch` block must invoke 'claude', not 'Dispatch-Lane' -- this script never runs an arbitrary command from a contract file"* (`Invoke-Dispatch.ps1:285`). The contracts are not malformed: `gen_lane_contract.py check` reports **OK** on all three, and the refusing fence is the one the generator itself wrote for `--shape local`. **A contract cannot simultaneously satisfy the generator's gate and the verb's parser.** · **THE DEFECT IS NOT THIS MISMATCH — AX25-1 states it:** *"the lane-launch path has two owners and no conformance test between them. Every patch so far fixed one symptom of that seam. Rows `[#716]` `[#717]` `[#718]` are symptoms, not the defect."* This row is the fourth symptom in that list, and it is kept as a **witness record** rather than as a rival fix: the contract generator lives in the hub, the launch verb lives outside it, and nothing asserts they agree, so each patch lands on whichever symptom was last observed. · **THE MECHANISM THAT CLOSES THIS ROW IS AX25-2, AND IT LIVES ELSEWHERE — `[#675]`'s lane, clause 1, no separate lane.** That test generates a contract with `gen_lane_contract.py`, runs the ruled verb's DryRun against it, and asserts it resolves — **fence, contract location, model and base in ONE assertion**. It is RED today precisely because of this row's evidence. This row therefore proposes **no fix of its own** and does not pre-empt which side moves; the verb's `:285` refusal is a deliberate "never run an arbitrary command from a contract file" safety property, so widening it may be the wrong leg and that call belongs with the conformance test. · **`[#718]` DID NOT CLOSE THIS, and the distinction survives the re-scope.** `[#718]`'s Done-when is the **path** key, verbatim: *"the generator writes lane contracts to the location the verb reads, both resolved from ONE key rather than from two independently-correct literals, and a test asserts the written path equals the path the verb resolves"*. Its witness, `tests/test_gen_lane_contract.py::test_emit_writes_the_contract_where_the_dispatch_line_it_prints_will_be_read`, asserts **where the file lands** and says nothing about **what the fence inside it says**. `[#718]` is correctly closed on its own terms (merged `eccb5814`, fix `63e1d8de`); the fence mismatch was **never in its scope**. The two present identically — "dispatch refuses the contract the generator made" — and are told apart only by which line refuses: `:385` path, `:285` fence. · **The ruled STEP 0 check is currently unrunnable via the ruled verb.** `SEAT-BOOT-dispatcher.md` §2 instructs `dispatch <LANE-*.md> -DryRun` *"once per generated contract, and nothing after"* (AMEND-BATCH-V-002 §1, which exists because batch V froze six contracts the live verb refused). A seat following the boot literally hits a refusal on **every** conforming contract; the X2 freeze took its receipts with `Dispatch-Lane … -DryRun`, the fallback Ch8 §4 names. **A step-0 gate pointed at its own generator's output.** · **A detector certifies this seam and returns OK, which is how it survived four symptoms.** `audit.py`'s `dispatch_verb_agreement` passes with *"both point-of-use sites name the verb Ch8's dispatch table rules, and neither carries a rival literal launch form"* — it checks that the surfaces **name** the ruled verb, never that the named verb **accepts** the generated artifact. Recorded as evidence for AX25-1's "no conformance test"; **extending that check is NOT asked for here** — AX25-4 bounds work on the retiring PowerShell path to AX25-2's test plus whatever conductor E's runner needs. · Done when: **AX25-2's generator↔verb conformance test is GREEN** — the same assertion that closes this row's evidence, delivered in `[#675]`'s lane as its clause 1, not duplicated here; and this row's specific witness is covered by it, i.e. a contract emitted by `gen_lane_contract.py emit` passes the ruled verb's `-DryRun` without the `:285` fence refusal · refs `scripts/gen_lane_contract.py` (the `--shape local` fence it emits), `Invoke-Dispatch.ps1` (`:285` fence refusal; `:385` the path refusal `[#718]`/`[#509]` own), `to-cc/AMEND-BATCH-X-ROSTER-025.md` (AX25-1 root cause, AX25-2 the mechanism, AX25-4 the scope bound), `protocols/PLAYBOOK.md` Ch8 §4, `scripts/seat_refusals.py` (`dryrun-step0`), `docs/audits/2026-09-12-technical-batch-x2-manifest.md` §4 (the three live refusals and the fallback receipts), `scripts/audit.py` (`dispatch_verb_agreement` — evidence, not a target), `[#675]` (carries AX25-2, the mechanism that closes this row), `[#741]` (AX25-3 — moving launch ownership to the hub floor), `[#718]` (the PATH key on this same seam — closed, deliberately not re-opened), `[#509]`, `[#592]`, `[#617]` (the prompt distiller AX25-5 names) · kill-candidates: none — `[#718]` is CLOSED and owns the PATH key only, so there is no open row to supersede; `[#509]` owns the reader's prompts-dir resolution and is silent on fence grammar; `[#675]` carries the MECHANISM and this row carries the WITNESS, so neither substitutes for the other · source: dispatcher seat, batch X2 freeze, 2026-09-12; surfaced by running `dispatch -DryRun` over all three frozen contracts per the boot's step 0, recorded in `to-browser/SESSION-dispatcher.md` §9, filed on operator instruction and re-scoped the same day by `to-cc/AMEND-BATCH-X-ROSTER-025.md` AX25-1
