---
id: "[#707]"
title: "W-4 closed under a declared organ-index bypass with four repairs owed -- the committed index is one row stale against its own generator"
status: open
priority: P1
size: S
theme: "[E5] Canonical-file integrity"
story: "[S13] Keep canonical files accurate"
generates: BACKLOG.md
---

- [#707] [P1][S] **W-4 closed under a declared organ-index bypass with four repairs owed -- the committed index is one row stale against its own generator** - AX6-5 of `to-cc/AMEND-BATCH-X-ROSTER-006.md`, carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93) Part 4, and the third of the three rows the operator's 2026-09-11 instruction named. · **AX6-5 says in its own words that this is filed as a row and not performed** -- "updated at batch close as a row" -- and the acts it names are the INTEGRATOR's at batch close, from the primary checkout. This lane is TEXT-ONLY and regenerated no index; doing the work here would have been the defect, not the diligence. · **The residue is measured, not predicted.** W-4 completed at `5e17ecd7` with a DECLARED `organ-index-freshness` bypass, so the committed index genuinely disagrees with what its generator renders: **64 committed against 63 rendered**. A stale generated index is worse than none, because every reader treats it as computed truth -- and this one is stale by exactly the amount that makes it look fine. · **Four repairs, and they are coupled in one direction**: the WARN delta must be re-derived AFTER the regeneration, not before, or it is re-derived against the stale index and the whole exercise records the wrong number. (1) Regenerate the organ index, 64 to 63. (2) Fix `CLAUDE.md:138`. (3) Re-derive the ship-gate WARN delta against the regenerated index. (4) Supersede `graph_queries.py` `ORPHAN_DISPOSITIONS`' 2026-09-07 "keep `/override` as retirement notice" line per AW6-1. · **Repair (4) is a disposition that outlived its subject.** `/override` was RETIRED by the ADR-85 amendment section A2 and discharges no gate; a disposition telling the census to keep it as a retirement notice preserves a row for an organ that no longer does anything, which is the orphan class the census exists to report rather than to protect. · **A declared bypass is legal and this row is what makes it honest.** The bypass was declared, which is the rule satisfied; the debt it creates is discharged by a row, not by the declaration. · Done when: the organ index is regenerated and committed (64 to 63); `CLAUDE.md:138` is corrected; the ship-gate WARN delta is re-derived AGAINST the regenerated index rather than the stale one; and `ORPHAN_DISPOSITIONS`' 2026-09-07 `/override` line is superseded per AW6-1 -- all four as the integrator's act at batch close · **Done-when carried from AX6-5's four named acts**, with the ordering constraint on the WARN delta made explicit by the filing lane · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, Part 4 AX6-5), `to-cc/AMEND-BATCH-X-ROSTER-006.md` AX6-5, `ecosystem/organ-index.md`, `scripts/graph_queries.py` (`ORPHAN_DISPOSITIONS`), `CLAUDE.md`, `[#683]` (the v1.5.0 floor ships `/override`, a command that discharges no gate -- the same retired organ from the deploy side), `[#657]` (the orphan census), `[#639]` (retire the verification organs that fire but never report) · kill-candidates: none -- `[#683]` owns `/override` shipping in the FLOOR to consumers and is silent on the census disposition that keeps it listed here; no open row names the organ-index staleness at `5e17ecd7` · **Archived annotations:** `tasks/archive/707.md`
