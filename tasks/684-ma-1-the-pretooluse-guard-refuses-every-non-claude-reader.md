---
id: "[#684]"
title: "MA-1 — the deployed `PreToolUse` guard turns a missing environment variable into total refusal for any reader that is not Claude Code"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S7] Wire up the lifecycle hooks the workflow relies on"
generates: BACKLOG.md
---

- [#684] [P1][M] **MA-1 — the deployed `PreToolUse` guard turns a missing environment variable into total refusal for any reader that is not Claude Code** - Finding 3 of the 2026-09-09 night mission (`docs/audits/2026-09-10-technical-night-aj-m03/REVIEW.md:83`, pointer :105 `.claude/settings.json:21`), dispositioned FILE by `to-cc/DECLARE-REVIEW-CONSUMPTION-2026-09-10.md` row 3. · **The mechanism, stated precisely.** The hook command at `.claude/settings.json:21` is `"command": "python \"$CLAUDE_PROJECT_DIR/scripts/fleet_health.py\" --prompts-guard"`. `$CLAUDE_PROJECT_DIR` is set by Claude Code and by nothing else. Any other reader ordered against this tree — a non-Claude CLI on a review or derive leg — expands it to empty, the path resolves to garbage, the guard errors, and a `PreToolUse` guard that errors REFUSES. The result is not a degraded read but a total one: the reader is blocked before it does anything. · **Why this is the expensive half of REVIEW's R1-2.** The night mission's cursor-agent leg (`REVIEW.md:462`) names it as the first of two questions and the more expensive one — *"Do we fix `.claude/settings.json:21` before any non-Claude reader is ordered again?"* — because every future multi-reader leg pays this cost until it is fixed, and the failure looks like the reader being broken rather than the harness refusing it. · **Two edits, both narrow:** the hook resolves its own repo root rather than trusting an inherited variable (fallback to the repo root it can compute), and the matcher is narrowed so the guard applies to the tool classes it was written for instead of every call. Neither weakens what the guard actually guards — memory of the class is that a guard which refuses tools is landed unarmed and armed as a separate act. Batch W lane L2. · **THE POSTURE INVERTED 2026-09-11 — batch X lane W-2′ (`LANE-w-684-pretooluse-guard-root-prime.md`), and every clause below is carried VERBATIM per AX12-1 so that none of it stays merely SAID.** Batch W's W-2 lane landed the two edits this row names and then, across three Codex passes, argued its way to a FAIL-OPEN guard: a missing script, a missing interpreter and a crash all exited 0 and PERMITTED. The fresh review returned HIGH:2, both fail-open, which is why W-2 was CARRIED rather than closed. AX15-1 reverses that on the merits. · **floor: MUST** (AX4-1) — and it is one declaration over a PAIR, not two over two halves: the `PreToolUse` prompts-guard hook block in `.claude/settings.json` and `scripts/fleet_health.py` are ONE floor component. This lane declares the floor and lands the COUPLING CHECK that catches a split; the carrier act that decides which repos the hook itself reaches is the parity registry's, and AX4-1 assigns it by name to the X1-1 `decision_coverage` lane — a row this lane did not file and does not edit. · **`AX4-1` — verbatim:** *"Floor declaration is mandatory. Every row and every lane contract carries `floor: MUST | hub-only`; a NEW organ, hook, gate, doc or command without it is refused at commit (validated against `ecosystem/parity-surfaces.yaml`; `fleet_parity` is the check). Default is MUST — hub-only requires a one-line reason. Applies retroactively to batch W's outputs and every batch X row: W-7 selector, W-2 guard, `decision_coverage`, routing, STANDING_RULINGS, the docs cut, conductor E — each dispositioned MUST or hub-only before X1 dispatches. Owner: `decision_coverage` lane (X1-1) gains this clause; the parity registry is the carrier."* · **`AX9-4` — verbatim:** *"Exists-before-build clause on the decision engine (X1-1) and the floor declaration (AX4-1): a contract that CREATES an organ, script, hook or doc must quote a `process-list` / organ-index result showing no existing organ answers the need; a NEW organ without that quote is refused at the contract gate. This is the RED-first form of 'check the process map before you build'."* · **`AX15-1` — verbatim:** *"Guard design (technical ruling): the PreToolUse prompts-guard FAILS CLOSED for the matched class (filesystem-touching tools — the narrowed matcher stays) whenever it cannot evaluate: script missing, interpreter missing, crash, any rc other than 0. The refusal text names the cause and the fix (the operator's rule: a deviation raises an exception that teaches). A SessionStart check verifies interpreter + guard script once and reports loudly, so per-call refusals are the exception. Rationale: a guard that permits when it cannot run is declared enforcement without enforcement."* · **`AX15-2` — verbatim:** *"Floor coupling: the hook and `scripts/fleet_health.py` (its guard entry point) ship together as one floor component (AX4-1); a consumer with the hook and without the script is a deploy defect caught by `fleet_parity`, not a silent permit."* · **`AX15-3` — verbatim:** *"Lane W-2′ in the first set, slot 6 (replaces AX14-1's slot-6 interim): continues on the W-2 branch; Done-when = `[#684]`'s own + RED-first trip-tests for both failure modes (script removed → refused with message; interpreter broken → refused with message) + the M7 smoke still passes (a non-Claude CLI call is not refused) + a fresh Codex review with no unresolved HIGH. On merge it unblocks AX8-1 (read-only work to agy) and X1-4 routing."* · **What the inversion does NOT touch, stated so a later reader does not widen it:** the MATCHER stays narrowed (AX15-1 says so in its own parenthesis), so `ToolSearch` / `EnterWorktree` / `ExitWorktree` / `SendMessage` remain ungated and the break-glass survives a refusing guard; and `PROMPTS_UNSET` / `PROMPTS_NO_USER_SCOPE` remain PASSING verdicts, because AX15-1 enumerates inability-to-EVALUATE (script, interpreter, crash, rc), not the verdict enum — a guard that ran and had nothing to compare has evaluated. · Done when: one non-Claude CLI smoke passes under the guard, a RED-first test fails when the fallback is removed; **and, per AX15-3 carried into this row:** the guard FAILS CLOSED on script-missing, interpreter-missing and crash, each refusal naming cause AND fix; two RED-first trip-tests, one per failure mode, proven to fail against the fail-open guard before the fix makes them green; a SessionStart check that verifies interpreter + guard script once and reports loudly; the `fleet_parity` coupling that refuses a repo carrying the hook without the script; the M7 smoke still passing (a non-Claude CLI call is NOT refused); and a fresh Codex review with no unresolved HIGH · refs `docs/audits/2026-09-10-technical-night-aj-m03/REVIEW.md` (:83 finding, :105 pointer, :462 the R1-2 ruling), `.claude/settings.json:21`, `scripts/fleet_health.py` (`--prompts-guard`), ADR-108 §B (RED-first), `[#676]` (the sibling provider-CLI invocation-shape row) · kill-candidates: none — `[#676]` owns whether a provider CLI's non-interactive shape is recorded, not whether our own guard refuses it; no open row names `settings.json:21` · source: `to-cc/DECLARE-REVIEW-CONSUMPTION-2026-09-10.md` row 3, filed 2026-09-10 under AMEND-SESSION-PLAN-003 §1
