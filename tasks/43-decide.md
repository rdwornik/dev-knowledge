---
id: "[#43]"
title: "Decide +"
status: deferred
priority: P3
size: L
theme: "[E6] Cross-repo universalization"
story: "[S17] Make new-repo scaffolding correct-by-default"
generates: BACKLOG.md
---

- [#43] [P3][L] Decide + (if yes) author a one-step new-repo scaffold (ADR + templates/new-repo-skeleton/, no scripts) · **2026-07-08: scaffold scope folded into `docs/intake/2026-07-08-func-new-project-bootstrap.md` (intake-id 6) — superseded-by that intake, now the requirements spine for this work; this task persists as the decomposition target the technical triage will decompose** · Done when: decision recorded + scaffold authored if approved · refs durability-audit J6, docs/intake/2026-07-08-func-new-project-bootstrap.md · **HELD 2026-08-27** (K2 conditional, night-harvest adjudication; C4 census row 1) — closure was conditioned on re-anchoring intake #6 first, *and only if that re-anchoring is a pure pointer fix*. It is not. Measured this session: intake #6 (`docs/intake/2026-07-08-func-new-project-bootstrap.md`) is **`status: SEED` with an empty `consumed-by:`**, still *'awaiting functional-architect elaboration, then technical triage'* 50 days on — and a corpus-wide search finds **`tasks/43-decide.md` is its ONLY citer**. Closing this row would therefore leave intake #6 with zero carriers, which is precisely the orphaning the counter-case predicted. Re-anchoring needs either a NEW row (a birth, with kill-candidates and budget) or the architect elaboration the intake has been waiting for — neither is a pointer fix. HOLD. · DEFER — the 45-day icebox sweep of 2026-08-27 (C4 §3, X1). Peg: **the functional-architect elaboration of intake #6**, which has been `status: SEED` with an empty `consumed-by:` since 2026-07-08 and for which this row is the ONLY citer in the corpus. A real trigger, not the generic sweep peg: until #6 is elaborated there is nothing here to decompose.
