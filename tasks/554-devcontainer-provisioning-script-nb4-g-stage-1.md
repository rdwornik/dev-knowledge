---
id: "[#554]"
title: "Devcontainer + provisioning script (NB4-G stage 1)"
status: open
priority: P2
size: M
theme: "[E7] Tooling & evaluation"
story: "[S18] Cut session friction with better tooling"
generates: BACKLOG.md
---

- [#554] [P2][M] **Devcontainer + provisioning script (NB4-G stage 1)** — the reproducible container plus provisioning script that lets a lane run OFF this machine, so batch width stops being bounded by one workstation. **CONCURRENCY DOCTRINE AMENDED 2026-09-01 (operator ruling): OPTIMUM 6 lanes, CEILING 12.** This row previously said *"one workstation's measured admission ceiling of 6 concurrent lanes"* and that number was **one window's observation, not a rule** — it was never re-measured, and it was being cited as a hard bound. The ruled shape separates the two: **6 is where the box is comfortable, 12 is where it refuses**, and the sentinel warns **above 12 only, never below** — a warning at 7 would turn an optimum into a ceiling by the back door, which is exactly the error being corrected. Live witness at the ruling: **nine lane worktrees ran concurrently** (six batch-F lanes plus three from other seats) and were left running deliberately rather than throttled. **The 6-vs-12 question is now a measured curve, not a number**: per-lane wall-clock rides every batch packet from here, so the next window answers it with data instead of re-observing it. Four legs, each a measured failure mode: a pinned-`uv` assert, `git fetch --unshallow` (the gates read history; on a shallow clone they are vacuous), deterministic `pre-commit install` for all three hook types, and an env gate that refuses to start on a half-provisioned environment. The substrate is operator-decided and not this row's question. · Done when: one lane runs green (`audit.py health` **and** `pytest -m 'not slow'`) on the Codespaces free tier, and the *identical* script is runnable via `devcontainer up` on a VPS · refs docs/intake/2026-08-17-tech-off-machine-agent-substrate.md, #541, #453, #528 · kill-candidates: none — no row owns off-machine provisioning; [#541] owns the substrate DECISION and [#453] the cloud-container PREFLIGHT for an already-provisioned session, a two-of-four-leg overlap whose second lander discharges by pointing at the first · source: intake #39 `docs/intake/2026-08-17-tech-off-machine-agent-substrate.md`, which carries the four-leg rationale and the substrate pricing in full
