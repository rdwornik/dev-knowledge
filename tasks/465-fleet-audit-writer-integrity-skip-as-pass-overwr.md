---
id: "[#465]"
title: "fleet-audit writer integrity — LEG 1 DONE 2026-08-01; legs 2–4 remain"
status: open
priority: P2
size: M
theme: "[E7] Tooling & evaluation"
story: "[S20] Revive the nightly layer, load-gauge first (rent-rule discipline)"
serialize-group: audit-py
generates: BACKLOG.md
---

- [#465] [P2][M] **fleet-audit writer integrity — LEG 1 DONE 2026-08-01; legs 2–4 remain** — from the dailies + superseded same-day git blobs: **(1) DONE 2026-08-01** — skips emitted `pass`, inflating pass counts. **16 sites retagged** in `audit.py` to the enum's own honest tokens (`n/a` = ran but not applicable, `unavailable` = could not run), including exactly the 9 hub-only checks this row's evidence named. RED-first: a **structural** invariant test (`tests/test_skip_is_not_pass.py`) sweeps every `ALL_CHECKS` member against a planted skip (a bare non-hub tree) and fails on any `pass` carrying skip-shaped evidence — so a NEW check that emits pass-with-skip is caught the day it lands, not at the next digest read; it found 14 live offenders including one (`fleet_parity`) a manual enumeration had missed. **Two couplings the fix exposed, both real:** `enforcement_coverage._demonstrate_hub_scoped` CLASSIFIED organs by observing the buggy `pass`+"hub-only" signal and was retargeted to `n/a` in lockstep (the row's "corrupts EVERY consumer" made concrete); and a blanket retag wrongly flattened `reconciled_versions`, whose single Finding carries a CONDITIONAL evidence expression — matched edges are a genuine `pass`, only zero-edges is a skip — now split so status follows the branch. Gate verdicts are unchanged by design: `n/a`/`unavailable` are non-blocking, so the fix corrects the TALLY, never a verdict (pinned by a test that a skip is never promoted to `warn`/`fail`). 14 test assertions that encoded the old behaviour updated. **Legs 2–4 REMAIN OPEN**; **(2)** a date's digest/history is OVERWRITTEN by later same-day runs (two digests each dropped 14 WARNs); **(3)** the hub intermittently resolved as not-the-hub (9 hub-only checks skipped-as-PASS, 6 of the last 8 runs); **(4)** `handoff_tag_canonicity` self-disabled. Legs 2–4 UNBLOCKED: [#460] closed keeping the lane, so the writer stays and is fixable now. Evidence + 51-commit gap: L6 pack in refs. · Done when: a skip is never emitted `pass` (with a test), same-day runs append/version or last-run-wins is accepted on record, the flap has a root cause + regression, and tag-canonicity is fixed or retired · refs scripts/audit.py, docs/audits/2026-08-01-technical-night-batch-l6-460-decision-pack.md, #460 · kill-candidates: none — [#460] rules the lane; this is writer correctness · serialize-group: audit-py
