---
id: "[#930]"
title: "Wave 3 lane A (lane-loop-declaration) -- the one file declares every moment the loop needs"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#930] [P1][M] **Wave 3 lane A (lane-loop-declaration) -- the one file declares every moment the loop needs** - filed by LANE-W3-A per `to-cc/DECLARE-WAVE3-CONNECT-2026-09-21.md` hard precondition 2 (every wave row is filed before any other wave-3 lane fires); frozen contract `LANE-W3-A-loop-declaration.md`, branch `worktree-lane-loop-declaration`, a lane of the loop `[#929]` evaluated. Value: Four other lanes build against what you declare tonight. If the declaration is right, they wire; if it is wrong, they wire to the wrong place. This lane is the contract between the organs. · Done when: carried verbatim from the frozen contract — 1. **Rows first.** File one task row per wave-3 lane (A-F) with each lane's Done-contract verbatim, in one commit, before any other change. 2. **`pre-launch` moment** (R-W3-1): organs `worktree_occupancy`, `seat_refusals` (live integrator), `routing_agreement`. Moved out of `lane-start`, which keeps only in-lane organs. 3. **`lane-end` fires once:** the moment carries a declared precondition — a HANDBACK line present in the lane's session file — and the transport report is ordered so that its failure is recorded but does not stop the moment's remaining organs. 4. **`batch-close` moment**, run by the integrator after the last merge: `lane_digest`. 5. **`merge` moment:** the review row uses a `{merge}` placeholder (R-W3-6); a `go_reader` organ is declared at `scripts/go_reader.py` (built by W3-C) and `optional: true` until then. 6. **The 36 organs with no caller** (`uv run --locked python scripts/graph_queries.py moments`): each gets exactly one recorded fate — a moment, a `manual_until` date with a one-line reason, or `retire_candidate: true`. None is dropped from the roster. 7. **`dodo.py`** substitutes `{merge}` and evaluates the lane-end precondition. 8. **Tests (common rules 1-4):** every row you add or change is read from the file and executed as written; `pre-launch` refuses on an occupied fixture slug; `lane-end` with no HANDBACK line writes a SKIPPED receipt and runs no organ; a failing report does not stop the digest; the `{merge}` placeholder resolves; the 36-fates list parses and covers every organ the census names. 9. **Codex terra review** recorded; **handback** per common rules. · refs `to-cc/WAVE3-COMMON-2026-09-21.md`, `to-cc/DECLARE-WAVE3-CONNECT-2026-09-21.md`, `[#929]` · kill-candidates: none -- each wave-3 lane wires an organ that already exists; no open row is absorbed
