---
id: "[#962]"
title: "Wave 4b lane 6 (lane-fleet-health-split) -- sessions read fleet health; a guarded background producer makes it"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
implements: "ADR-120"
generates: BACKLOG.md
---

- [#962] [P1][M] **Wave 4b lane 6 (lane-fleet-health-split) -- sessions read fleet health; a guarded background producer makes it** - added to wave 4b by operator order 2026-09-22T22:21+02:00 (`to-browser/SESSION-postwave-chain-2026-09-22.md`), fired after the first MERGED among lanes 2-5 per that order; frozen contract `LANE-W4B-6-fleet-health-split.md`, branch `worktree-lane-fleet-health-split`. Not part of the original wave-4b 0-5 set -- filed by this lane itself per the wave's rule 4 (rows cite ADR-120 as in-repo provenance). Value (architect ruling, delegated by the operator 2026-09-22): fleet health is the harness's measure of its own purpose -- whether every project is still built the same way. Its SessionStart hook produced that state inside the session (full cross-repo audit, commits, restore), which caused the timeouts, the process that survives a kill, and the silent deletion of an untracked audit draft measured live in `docs/audits/2026-09-22-technical-lane-hooks-rearm-live-measurement.md` §2.1 (`fleet_health.py` hung past its 60s budget on a cold cache; its detached `audit.py run` grandchild survived a process-tree kill; on completion `audit.py::_commit_routine_outputs`'s working-tree restore deleted a lane's own untracked in-progress `docs/audits/` file) -- the same root cause as the 2026-09-17 emergency hook disable. Rule from this lane on: a start hook reads, an event hook triggers background work, heavy work runs in an isolated checkout. · implements: ADR-120 · Done when: 1. **Row first** (this row), citing ADR-120 and the live-measurement audit as provenance. 2. **Reader:** the SessionStart entry prints the latest published fleet-health digest and its age in under one second, spawns nothing, writes nothing. 3. **Trigger:** when the digest is older than a threshold held in version-controlled config, the same hook takes an exclusive claim and starts one detached producer, with the claim/receipt/reaper pattern of `scripts/lane_end_guard.py`; a second session in the window does nothing. 4. **Producer:** runs the fleet audit in its own clean checkout (a temporary clone or worktree outside every session's working tree), publishes as today, removes its checkout; its whole process tree ends with it. 5. **Safety net:** the publishing step (`audit.py::_commit_routine_outputs`) refuses, with a receipt and a non-zero exit, when the working tree it would restore has untracked or modified files in its durable scope beyond this run's own dated outputs; it never deletes a file it did not create. 6. **Re-armed** on a live test like lane-hooks-rearm's: duration, exit, processes before and after. 7. **Tests, red first:** reader under 1s with no child process; claim prevents a second producer; an untracked `docs/audits/` draft survives a producer run; the publishing step refuses a dirty tree. 8. **Codex terra review** with consumer cited; **self-check and purity**; **handback** · refs `LANE-W4B-6-fleet-health-split.md`, `to-cc/WAVE4B-COMMON-2026-09-22.md`, `to-browser/SESSION-postwave-chain-2026-09-22.md` (the operator order adding this lane), `docs/audits/2026-09-22-technical-lane-hooks-rearm-live-measurement.md` §2.1 (this row's own provenance -- the destructive-restore finding), `scripts/lane_end_guard.py` (the claim/receipt/reaper pattern reused), `scripts/fleet_health.py`, `scripts/audit.py` (`_commit_routine_outputs` and what it calls), `.claude/settings.json` (the `fleet_health.py` SessionStart entry), `[#956]` (the sibling lane whose live-test surfaced this defect) · kill-candidates: none -- `[#808]` is the general hook-timeout-bound row (owner of the bypass-rate class across every hook), not this lane's specific reader/trigger/producer split or the `_commit_routine_outputs` destructive-restore fix; no open row covers either
