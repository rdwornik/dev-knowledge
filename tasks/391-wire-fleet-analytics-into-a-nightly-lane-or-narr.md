---
id: "[#391]"
title: "Wire fleet_analytics into a nightly lane, or narrow #384 to a manual reporter"
status: closed
priority: P3
size: S
theme: "[E9] Fleet Desired-State System (North Star)"
story: "[S26] Mine our own history before predicting anything"
serialize-group: audit-py
generates: BACKLOG.md
---

- [#391] [P3][S] **Wire fleet_analytics into a nightly lane, or narrow #384 to a manual reporter** — #384 claims "runs as a nightly lane" but nothing invokes `scripts/fleet_analytics.py` (manual CLI only; terra 2026-07-22). Either wire it into the Tier-2 nightly surface (fail-soft), or narrow #384 to a manual reporter + file scheduling separately. If nightly, the [S20] load-gauge discipline applies. · Done when: `scripts/fleet_analytics.py` fires on a schedule under a `· routine:` block that `routine_consumers` passes, fail-soft, with `[S20]`'s load-gauge discipline applied · refs docs/audits/2026-07-22-verification-night-batch-integration-386-384.md §4, #384, #270 · kill-candidates: none — completes #384's nightly-lane claim · serialize-group: audit-py · **CLOSED 2026-08-28 — ARCHITECT-RULED (K4 DECIDE).** The row's own disjunction, quoted verbatim: *"Either wire it into the Tier-2 nightly surface (fail-soft), or narrow #384 to a manual reporter + file scheduling separately."* **Both options measured before choosing, as the verdict required.** **Option A (wire it nightly)** — needs a scheduling trigger, a `· routine:` block that `routine_consumers` passes, a fail-soft wrapper, `[S20]` load-gauge discipline and a test; and it has two problems beyond size. First, `routine_consumers` reads **backlog rows only**, so the routine declaration would have to live in a row — a mortal carrier, which is precisely the defect `[#348]` closed on this same day by re-anchoring its declaration into a living doc. Second, there **is no live Tier-2 nightly surface** to wire into: the night became the lane/batch protocol, and intake **#60 / ruling X8** is defining that protocol right now, so Option A would invent a rival scheduling surface beside an in-flight one. Size: **M**, with a live collision. **Option B (narrow to a manual reporter)** — `[#384]` is already CLOSED; a repo-wide grep for a live surface claiming a nightly cadence returns exactly ONE non-generated, non-archival hit, `ARCHITECTURE.md:440`, which already read *"manual CLI"*. Size: **S**, essentially already true. **Executed: Option B, the smaller.** The ARCHITECTURE cell is repointed from *"nightly wiring open #391"* — a claim this very closure would falsify — to **manual is the ruled end state, not a gap**, so a reader can no longer mistake a decision for an omission. The "file scheduling separately" half is NOT re-filed as a row: unattended cadence now belongs to intake #60's night protocol, which is its correct home, and adding a scheduling row beside it would recreate the rival-surface problem Option A was rejected for. **The row's premise is also recorded as stale:** *"#384 claims 'runs as a nightly lane'"* was true when filed and is not true now — `[#384]` is closed and out of the tree, and its nightly claim survives only in gitignored `logs/PROPOSALS-*.md` and immutable dated artifacts, neither of which is a live surface
