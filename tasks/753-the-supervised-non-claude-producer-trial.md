---
id: "[#753]"
title: "The supervised non-Claude producer trial — ten bounded tasks, stdout-only producers, admission recorded in the registry"
status: open
priority: P2
size: M
theme: "[E7] Tooling & evaluation"
story: "[S19] Decide the undecided artifact/tool models"
generates: BACKLOG.md
---

- [#753] [P2][M] **The supervised non-Claude producer trial — ten bounded tasks, stdout-only producers, admission recorded in the registry** — batch Y lane 6, frozen at `docs/audits/2026-09-14-technical-batch-y-launch-contracts/LANE-y-753-non-claude-trial.md`. **This row is filed AT FREEZE rather than by the lane, and the reason is a gate.** `graph-task-coverage` ([#664] clause 2) refuses a staged file that no OPEN row claims, and the lane contract could not be claimed by any existing row: `[#578]` is the earned mitigated rerun, scoped to the 14-item pack plus `C1-N3` with a role-reminder preamble; `[#568]` is provider config as code via junctions; `[#492]` is deferred and owns the Grok *review-lane* question. None of the three covers a supervised producer trial, and citing one to satisfy a gate would be a fake claim. The lane AMENDS this row rather than filing it. · **What the trial is.** Ten bounded tasks with **Grok 4.6** and **Copilot Enterprise** as **stdout-only producers** — they emit text and write nothing — plus `agy` on three read-only scans. CC writes every artifact itself and only after verifying the locators the producer cited, which is the control that makes an unverified citation a caught defect rather than a landed one. Each result passes the lane's RED-first tests **and** a review by a model that did not produce it. · **Why supervised and why daytime.** The contract is frozen at `shape: interactive`, not `local`: it reports after each task and must never run unattended, and a `--bg` session can neither ask a question nor report between tasks. Dispatching it `--bg` would contradict the lane's own frozen text, so it is deliberately not in batch Y's fired set and is launched by the operator when present. It also runs in the PRIMARY checkout, which the integrator holds for the batch, so it takes an author-chosen `feat/` branch and confirms no merge is in flight before starting. · **Sequencing is not optional.** This lane is SECOND on `ecosystem/provider-registry.yaml`. Step-0 `file-collision` refused batch Y on that file, claimed by this lane and by `[#751]`, and the collision is substantive rather than a checker artifact: the registry carries **no machine-readable rate field at all** — top-level keys are `providers` / `roles` / `models`, and cost appears only as prose — so `[#751]` must AUTHOR the rates. `[#751]` owns the file and goes first; this lane does not open it until `[#751]` has landed on `main`, and verifies that before its own first write. · **What it is worth.** The first real token saving, and the first evidence that the role table is more than a table. · Done when: ten bounded tasks are run one at a time with the producer emitting to stdout only, every cited locator verified by CC before any artifact is written, each result through RED-first tests and a review by a model that did not produce it; `agy` is run on three read-only scans; the operator receives a report after EACH task rather than a batched summary; and an admission verdict is recorded in `ecosystem/provider-registry.yaml` per task with failures NAMED rather than summarised, admission being >= 8/10 green on first review · refs `docs/audits/2026-09-14-technical-batch-y-launch-contracts/LANE-y-753-non-claude-trial.md` (the frozen contract), `docs/audits/2026-09-14-technical-batch-y-manifest.md` (the batch manifest and its serialisation groups), `[#751]` (owns `ecosystem/provider-registry.yaml` for batch Y and must land first), `[#578]`, `[#568]`, `[#492]` (the three rows checked and rejected as claims) · kill-candidates: none — `[#578]` owns the admission rerun of the existing pack and stays open on its own measurement; `[#568]` owns config-as-code and is unrelated to admission verdicts; neither folds into this and this folds into neither
