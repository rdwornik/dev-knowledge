---
id: "[#628]"
title: "DC-2 re-cut — dissolving `ESSENTIALS.md` is a FLEET-COUPLED release act, not a doc lane"
status: open
priority: P1
size: L
theme: "[E5] Canonical-file integrity"
story: "[S14] Keep the day-to-day docs right-sized and current"
generates: BACKLOG.md
---

- [#628] [P1][L] **DC-2 re-cut — dissolving `ESSENTIALS.md` is a FLEET-COUPLED release act, not a doc lane** — **Operator ruling 2026-09-01 (ruling 1), splitting DC-23.** DC-3 ran alone; this row is what DC-2 becomes. The batch-E lane REFUSED Act One correctly and its refusal is the specification: the A3 stale-doctrine census enumerates **ten breaking consumers of `ESSENTIALS.md` and NOT ONE is inside the frozen write-scope** — seven live instruction lines in `protocols/SESSION_SETUP.md` (three browser scale profiles each opening "upload ESSENTIALS.md"), `templates/CLAUDE-md-template.md:19` (the onboarding template every new repo is cut from), `templates/claude-regions/session-start-protocol.md:11` (a HUB region deploy-carried to every ADR-104 member), `protocols/HANDOFF_PROCESS.md:224` + `.claude/commands/handoff-verify.md:95` (the v6 exact-line-quote proof class, and HANDOFF_PROCESS is a registered spec), `ecosystem/disposition-register.yaml:131,134` (an ADR-88 entry whose recorded justification IS the freshness gate that dissolution makes vacuous), `protocols/STANDING_RULINGS.md:2617,2633` (RULING-W's Evidence locator), `protocols/DEFINITION_OF_DONE.md:195` and `.claude/commands/changelog-review.md:23`, plus five set memberships in `scripts/canonical_docs.py` and the `.claude/workflows/conformance-hub.js:133` agreement asserted by seam S11. **THE SECOND BLOCKER IS INSIDE THE FOOTPRINT AND IS WHY THIS IS A RELEASE ACT:** Act One's relocation target `templates/child-methodology-floor.md.tmpl` is hash-guarded THREE ways — the template bytes, the `.sha256` sidecar, and `anchors.floor_sha256` in **three live manifests** (v1.4.0, v1.3.1 and v1.2.0 are all pinned by live consumers) — and `deploy/release_lint.py` C5 asserts the three-way equality. **A floor edit is a release act by construction**, so this arc sequences WITH the v1.5.0 release rather than ahead of it · Done when: a frozen contract exists whose write-scope covers all ten consumers plus the floor sidecar and every pinned manifest; the ADR-88 register entry's justification is re-based or the entry re-pointed; `canonical_docs.py`'s five memberships move together with `canonical_freshness_gate.py:49`'s consumer-standalone fallback list; and the three tolerant readers (`canonical_freshness_gate:124`, `validate_doc_structure:319`, `validate_doc_rot:397`, all verified to skip on absence) are confirmed still tolerant after the move; **and the TWO SITES `DECLARE-RECOVERY-2026-09-09` § 2 names are reached by ANCHOR TEXT, never by the line numbers it quotes** — `protocols/AI_COUNCIL_PROCESS.md` § "Section history", the 2026-09-04 re-stamp entry, and `protocols/PLAYBOOK.md` Ch2 § "Authority hierarchy" item 1 — each re-worded or removed in the SAME act as the deletion, because on the 2026-09-09 re-measurement neither one ROUTES a session (the first records the repoint as already landed, the second says in so many words that the file "routes nobody"), so both are DANGLING REFERENCES the moment the file goes rather than re-points owed before it · **A FOOTPRINT DEFECT the batch-E lane found and this arc inherits:** the frozen write-scope named five region templates and OMITTED `session-start-protocol.md`, whose body is byte-identical to `CLAUDE.md`'s rendered region — so its own done-item "every hub-region edit lands in BOTH places" was unsatisfiable for that one region. Any re-cut fixes that or repeats it · refs `docs/audits/2026-08-31-census-stale-doctrine-references.md` (A3 §3B, the consumer enumeration), `docs/audits/2026-08-31-technical-batche-launch-contracts/LANE-b-2-essentials-and-claude-md.md` (the refused contract), `deploy/release_lint.py` C5, **its INPUT ARTIFACT at `docs/audits/2026-09-01-technical-act-one-preserved.md`** (the refused Act-One diff and its guard-tracing research, moved out of branch `worktree-lane-b-3-claude-md-genre` before that branch was deleted — a branch is a teardown target, not storage), `docs/audits/2026-09-01-technical-dc3-split.md` §2 for the hunk-by-hunk act assignment, `[#614]`, `docs/audits/2026-09-05-technical-v150-tag-checklist.md` (the five-gate tag checklist whose gate 2 IS this row's closure) · kill-candidates: none — `[#614]`'s DC-23 is the lane that refused this act and split; no open row owns the ESSENTIALS dissolution · **DE-BLESSED 2026-09-01 (operator, integrator micro-act) — so this arc's remaining job is the MECHANICAL DISSOLUTION ONLY.** `protocols/ESSENTIALS.md` is `status: superseded` and carries a top banner saying it is not a boot read; `CLAUDE.md` no longer sends any session to it (the three pointers reworded, the two `ESSENTIALS summarizes PLAYBOOK` consistency clauses replaced by the still-true hub-region↔template byte-match rule, and the session-start verify-line's `ESSENTIALS ↔ PLAYBOOK alignment` item dropped). Four of those five sites are HUB REGIONS, so `templates/claude-regions/{first-read,critical-rules-consistency,antipatterns-universal,session-start-protocol}.md` moved in lockstep — a region body that diverges from its template breaks deploy parity. **Nothing was deleted and the floor template was NOT touched**: the fleet coupling this row exists for is untouched, and the file's body is unedited apart from one retired reference fixed in place (`Mission anchor: VISION.md` → `README.md`, per DC-1's registry state). What remains for this arc is the deletion itself, its ten consumers, the floor sha regeneration and `release_lint` C5  · **DRIFT FOUND BY THE 2026-09-01 END-TO-END RE-READ, filed not fixed** (the de-bless ruling held the body untouched): the *Writing a Prompt* section still says **"Model is CC's pick (default Opus 4.8, the floor)"** — stale twice over against **R-MODELS** (dispatched lanes are sonnet; the model is the CONTRACT's, not CC's pick) and against the live model names. **The dissolution must not carry that sentence forward**: whatever absorbs this file's prompt guidance re-derives the model rule from R-MODELS and the contract routing table, per `[#631]`'s ruled mechanism · **AMENDED 2026-09-09 — the disposition is RULED and the count is MEASURED** (operator ruling 2026-09-09, relayed by the batch V integrator; executed by lane `lane-v-000-window-rulings`). **DISPOSITION: DELETE AFTER RE-POINT**, in that order — every consumer route re-pointed first, the file deleted second, as ONE fleet-coupled release act carrying the floor sha regeneration and `release_lint` C5. It is step D of the recovery plan (intake `#89`, executed by `[#667]`), and **this row owns the act**; no second row was filed for it. **THE COUNT: the operator's `52` was REVIEW carry 4's figure and YIELDS to the measurement**, and the `ten` this row's body carries above is the A3 census's count of BREAKING consumers — a different and narrower class, kept as the census said it, not as a rival total. Re-measured against the tree at `8869eb23`, each class named so the number is checkable rather than quoted: **563** tracked files contain the string `ESSENTIALS` (every class); **81** survive dropping the immutable and append-only classes (`docs/audits/`, `docs/handoffs/`, `JOURNAL.md`, `LESSONS*.md`, `logs/`, anything under an `archive/`); and **38 files carrying 95 mention lines** survive dropping the record classes as well (`docs/decisions/`, `docs/intake/`, `tasks/`, `BACKLOG.md`, `ecosystem/.dev-knowledge/history/`). **That 38 / 95 is the operational consumer set this row's write-scope must cover, and it is the number that now stands.** Two measured corrections to the enumeration above, filed rather than fixed here: `scripts/canonical_docs.py` carries **two constants** (`ESSENTIALS`, `ESSENTIALS_PATH`) and **four** set memberships (`CANONICAL_OPTIONAL` plus three path tuples), not five; and `tests/fixtures/repo-with-structural-checks/protocols/ESSENTIALS.md` is a **293-byte fixture stub**, not a consumer — a sweep by filename would take it, so the write-scope says explicitly whether it goes · source: operator ruling 1, 2026-09-01
