---
id: "[#698]"
title: "The SEAT-BOOT-integrator render emits a GO line per merge where the canon it implements grants ONE GO for the batch"
status: open
priority: P2
size: S
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#698] [P2][S] **The SEAT-BOOT-integrator render emits a GO line per merge where the canon it implements grants ONE GO for the batch** - First of the three rows the batch X roster section 4 bundles as "SEAT-BOOT render per-merge-GO defect + three-grammar collision + amendment-vs-control-surface ruling (AW5-4)", carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93). **Why three rows and not one is recorded in this lane's end-of-lane artifact and in the intake**: the three touch different organs, need different Done-whens, and one of them is a ruling rather than a fix. · **The defect is a generator contradicting its own canon**, and AW5-1 states it in as many words: "GO granularity: one GO for the batch. `/lane-integrate` section 0 governs; the SEAT-BOOT-integrator render's per-merge GO line contradicts its canon -- a generator defect, filed at batch close." · **Why a render defect here is worse than a wrong sentence.** The GO is the operator's authorization act. A render that asks for one per merge either trains the operator to grant N authorizations where the canon grants one -- inflating the operator-interrupt count the whole batch protocol exists to reduce -- or trains the seat to ignore the line it renders, which is worse, because then the rendered GO stops meaning anything and the real authorization moves back into memory. · Done when: the integrator seat render emits exactly one GO per batch, agreeing with `/lane-integrate` section 0; and one test FAILS on a render carrying a per-merge GO line -- a RED-first witness per ADR-108 section B, proven to bite before the fix makes it green · **Done-when authored by the filing lane, not carried**: the roster names this item in a section-4 prose list that carries no Done-when column · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, section 4), `to-cc/AMEND-BATCH-W-005.md` AW5-1 (the ruling and the defect), `.claude/commands/lane-integrate.md` (section 0, the canon), `templates/handoff/seats/`, `[#685]` (the GO leaves no artifact -- the adjacent half, about the GO's PERSISTENCE rather than its COUNT), `[#699]` and `[#700]` (the other two rows of this cluster), ADR-108 · kill-candidates: none -- `[#685]` owns whether a GO leaves a readable artifact and is silent on how many GOs a batch takes; no open row names GO granularity · **Archived annotations:** `tasks/archive/698.md`
