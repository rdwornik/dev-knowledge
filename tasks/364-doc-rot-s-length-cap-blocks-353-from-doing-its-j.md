---
id: "[#364]"
title: "`doc_rot`'s length cap blocks [#353] from doing its job"
status: retired
priority: P3
size: S
theme: "[E8] ARC-5 execution"
story: "[S22] Discharge the silent-rule census findings"
serialize-group: audit-py
generates: BACKLOG.md
---

- [#364] [P3][S] **`doc_rot`'s length cap blocks [#353] from doing its job** — [#353] exists to accumulate "n=N recovered-not-prevented" incident evidence, and at 1189 chars it has ~11 left under the 1200 cap. The 2026-07-19 entry (n=7) already had to be compressed to a pointer. **At the current rate it hits the cap within about two more incidents**, after which the mechanism forces either an uninformative evidence line or a disposition on a ticket that is working correctly — a gate degrading the record it is meant to protect. Options to record, **no ruling attached**: (a) a linked evidence file (e.g. an `incident-evidence`-class audit artifact, already in the ADR-101 enum) with the ticket carrying only a pointer; or (b) a cap exemption for evidence-accumulating tickets, declared per-ticket so it cannot become a blanket escape. · Done when: [#353] can accrue further incidents without tripping `doc_rot`, or the constraint is recorded as accepted-with-reason · refs #353, scripts/validate_doc_rot.py, ADR-101 audit-class enum · kill-candidates: none — a mechanism-blocks-ticket defect; #353 is the victim, not the owner · serialize-group: audit-py · **OBSOLETED 2026-08-15** (morning adjudication D4.9(a) / D6.2, resolving the briefing's Y-2 collision): the row's premise is discharged by this same boot-act's step 2. It was written as PREVENTION — *"at 1189 chars it has ~11 left under the 1200 cap… at the current rate it hits the cap within about two more incidents"* — and asked that `[#353]` be able to accrue a further incident without tripping `doc_rot`. Step 2 recalibrated `_BACKLOG_GROSS_CHARS` 1200 → 1320 (p90 of the post-conversion distribution), and `[#353]` now measures 1266 chars against a 1320 threshold: it trips nothing and has 54 chars of headroom. Both night-2 lanes independently measured `[#353]` at exactly 1266 (NB2-A's §1.5 register draft and NB2-G's §5.1 ambiguity fork), which is what let the collision be seen before either act landed. ADR-65 close-class is **obsoleted** — the cap this row exists to work around no longer binds its subject. Frontmatter carries `retired`, the nearest terminal value in `_TERMINAL_STATUSES` (`closed`/`retired`/`superseded`), which has no `obsoleted` member. Consequence for the wave: `[#364]` is REMOVED from conversion lane W2-b, which becomes 3 ids — no draft is applied, and NB2-G's §5.1 draft is moot rather than wrong
