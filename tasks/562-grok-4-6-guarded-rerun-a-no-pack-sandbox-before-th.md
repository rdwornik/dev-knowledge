---
id: "[#562]"
title: "Grok 4.6 guarded rerun — a no-pack sandbox before the A/B is re-run"
status: closed
priority: P3
size: S
theme: "[E7] Tooling & evaluation"
story: "[S19] Decide the undecided artifact/tool models"
generates: BACKLOG.md
---

- [#562] [P3][S] **Grok 4.6 guarded rerun — a no-pack sandbox before the A/B is re-run** — **Grok 4.6 is NOT ADMITTED this run** (architect, 2026-08-20), and the failure is clean rather than instrumental: **both lanes failed `C1-N1`** — the incumbent emitted `VIOLATION`, the candidate emitted `COMPLIANT` — which is a hard **G1** failure for the candidate whatever the other thirteen items scored, and `C1-N1` is one of the twelve items **no contamination touches**. What DOES need repair before a rerun is the instrument: **two of the candidate's fourteen items (`C1-R4`, `C1-N2`) are contaminated by the pack file itself** — `docs/audits/2026-08-19-technical-c1-seeded-defect-pack.md` sits in the tree at the head under test and carries the literal ground truth for every item, and the candidate read it. A rerun over a tree that still contains its own answer key measures nothing. **Blocked-on is narrower than `[#491]`'s, and this row says so:** the `[#491]`-class instrument rulings are **NOT required** here — Grok runs against the direct xAI API with an explicit `model` field and a passing P0 version probe, so the client-substitution question that gates the Gemini lane does not arise. The guard is the only precondition. Admission stays `G1 AND G2 AND G3` per `protocols/STANDING_RULINGS.md` Q9; effort tier medium per Q8. · Done when: the candidate's working tree cannot read the pack — proven by a probe run in which a deliberate read of the pack path FAILS, rather than by assertion — and the 14-item A/B is re-run under that guard with **all 14 items uncontaminated**, recorded in a `docs/audits/` artifact that reports the three gates and asserts no verdict · refs docs/audits/2026-08-20-technical-grok-ab-results-2.md, docs/audits/2026-08-19-technical-c1-seeded-defect-pack.md, protocols/STANDING_RULINGS.md Q7/Q8/Q9, #492, #491 · kill-candidates: none — `[#492]` owns the Grok review-lane acceptance question and stays open; this row owns the instrument defect that run exposed, and neither subsumes the other · RULED 2026-08-20 (R3): G1 is comparative-with-floor and at incumbent 0/2 the floor plus one control item carry the gate; verbatim -> `docs/audits/2026-08-22-technical-annotation-and-rulings-ledger.md` §2 · **CLOSED 2026-08-23 — both Done-when clauses met on evidence, and the row closes REFUSED rather than admitted.** Clause 1 (the tree cannot read the pack, proven by a probe rather than asserted) was met by legs 1–2 — 11/11 probe vectors at the head under test, a deliberate read of the pack path FAILS. Clause 2 (14 items uncontaminated) was met by leg 3, `docs/audits/2026-08-23-technical-lane-562-local-admission.md`: 240 commands across the two candidate lanes, **zero delivered-output contamination**, and neither lane ever reached for the pack — every Layer A refusal was a syntax refusal. **ADMISSION VERDICT (architect, 2026-08-23): `gemini-3.7-flash` REFUSED (G1 floor FAIL; G2/G3 PASS and P 12/14 are proximity, not admission) and `grok-4.6` REFUSED (G1 FAIL, G2 FAIL)** — verbatim -> `docs/audits/2026-08-22-technical-annotation-and-rulings-ledger.md` §5.1. **ONE mitigated rerun slot is earned, not an admission:** the `C1-N3` control PASSED on both candidates, so one role-reminder sentence flips the refusal outcome, and R3's own promptable-failure clause buys a single rerun with that preamble BAKED into every item; admission stays refused until that rerun clears the floor. **OWED:** the incumbent's own refusal promptability on the same items (Anthropic path, when billing allows) — the carried `P_i`/`Φ_i` are a stale unguarded-head baseline. Neither successor dies with this row: both are routed through this window's wave-close funnel and its next-window queue, per ledger §5.2.
