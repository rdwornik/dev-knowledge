---
id: "[#782]"
title: "The quality-requirements register: non-functional requirements as a registered, enforced, rendered artifact"
status: superseded
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#782] [P1][M] **The quality-requirements register: non-functional requirements as a registered, enforced, rendered artifact** - the technical requirements this repo runs on (no full suite on the workstation, a memory floor, attached Codespaces, teardown that actually kills the process tree, cost recorded without being asked) live in prose, in pastes, and in a session's memory. **An instruction is a request; a mechanism is a guarantee** -- and every one of these was written as an instruction to a session, which is the operator's own rule #1 violated. The register is `ecosystem/quality-requirements.yaml`: one entry per requirement carrying `id`, `statement`, the `organ` that enforces it, the `metric`, and the `trip_test` proving the enforcement REFUSES. Small and bounded by design -- a register, not more backlog rows -- rendered into ONE ARCHITECTURE section so the whole set reads in one screen, and declared floor MUST so every consumer repo inherits it. **The discipline that keeps it honest:** an entry is `status: measured` only when its organ exists and its trip-test fails when the requirement is violated; everything else is `status: candidate` with NO organ field, because a register that names enforcement it cannot prove is worse than no register. - Done when: `ecosystem/quality-requirements.yaml` is read by a gate rather than merely present; a test asserts every `measured` entry resolves to an organ that exists and a trip-test that REFUSES (a trip-test that passes unconditionally is itself a refusal); one generated, freshness-gated ARCHITECTURE section renders the set; and the floor carries it MUST · refs `docs/audits/2026-09-15-technical-batch-z-manifest.md` (the batch, and the four availability findings seeded into the register), `docs/audits/2026-09-15-technical-batch-z-launch-contracts/LANE-z-0-quality-requirements.md` (the frozen contract), `[#667]` (the render path AN2-1 cites), ADR-108 SecB (RED-first witnesses) · kill-candidates: none -- no open row owns non-functional requirements as a registered artifact; `[#719]` schemas the organ index and this row registers the requirements those organs enforce, which are different objects · source: batch Z, lane `lane-z-0-quality-requirements`. **Filed by the dispatcher at freeze, and this is a known duplication risk the integrator must resolve:** lane 0 is instructed to file or amend its own row as its first commit and cannot see this branch, so it may file a second row for the same work. If it did, AMEND that row into this one rather than keeping both · **CLOSED 2026-09-15** — evidence 2830d3d6 - **SUPERSEDED by `[#765]`, not completed.** This row and `[#765]` are the same work, filed independently: this one by the dispatcher at freeze, `[#765]` by lane z-0 as its contractual first commit, neither able to see the other. The status is `superseded` and not `closed` deliberately - **the work is NOT done.** `[#765]` remains OPEN because the organ lane z-0 landed does not yet satisfy its Done-when: the register's `trip_test` is verified as a FILE that exists and never as a TEST that exists, so a measured entry citing a non-existent test resolves clean. This row's own body directs that the lane's row be amended into THIS one; **the direction is inverted deliberately, and the reason is cost, not preference** - every artifact lane z-0 produced cites `[#765]`, so keeping `[#765]` live costs nothing while the stated direction would rewrite a landed commit trail's citations. `[#765]` carries the STRICTER Done-when of the two, requiring the trip-test to pass only because the organ refuses, proven by neutering the organ. **No acceptance criterion is lost by absorbing this row into it.**
