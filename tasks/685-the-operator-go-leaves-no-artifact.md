---
id: "[#685]"
title: "The operator GO leaves no artifact, so the authorization a lane runs under cannot be read back from the tree"
status: open
priority: P2
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#685] [P2][M] **The operator GO leaves no artifact, so the authorization a lane runs under cannot be read back from the tree** - Finding 6 of the 2026-09-09 night mission (`docs/audits/2026-09-10-technical-night-aj-m03/REVIEW.md:158`, pointer :178 `docs/handoffs/*/`), dispositioned FILE by `to-cc/DECLARE-REVIEW-CONSUMPTION-2026-09-10.md` row 6. · **The GO is the load-bearing authorization in the whole batch protocol and it is spoken, not written.** `boot_frontier.py` proposes; `/boot-session` states in terms that the proposal "requires an explicit operator GO before any lane dispatches (AUT-R1 Phase 4: adjudication stays a human act)". That GO then arrives as chat, a lane fires, and nothing in the tree records that it was given, by whom, or over which proposal. An auditor reading the repo afterwards sees dispatched lanes and no authorization — the same shape as a lane that dispatched itself. · **This is the human-gate half of finding 9's "no gate has ever been proven to refuse".** Finding 9 folds to `[#664]`'s trip-tests because that clause covers the mechanical gates; the GO is the one gate that is purely human, so it folds nowhere and needs its own carrier. · **The fix makes the GO a file rather than a ceremony:** a batch-manifest line or a `RATIFICATION-<date>.md` on the transport, which `/lane-boot` READS before it will provision. That turns an advisory convention into a refusal, which is the only form that survives a seat under time pressure. Its absence must fail the dispatch, not warn it. Batch X. · Done when: dispatch refuses without it, one test fails when the check is removed · refs `docs/audits/2026-09-10-technical-night-aj-m03/REVIEW.md` (:158 finding, :178 pointer), `.claude/commands/lane-boot.md` §2, `scripts/boot_frontier.py`, `protocols/OPERATOR-INTERFACE.md` §1 (the `RATIFICATION-<date>.md` grammar row), `protocols/PLAYBOOK.md` Ch8 (the batch protocol), `[#664]` · kill-candidates: none — `[#664]` owns the commit-tier graph queries and `[#669]` the mechanical transitions; neither names the human authorization act, which by AUT-R1 stays outside the machine · source: `to-cc/DECLARE-REVIEW-CONSUMPTION-2026-09-10.md` row 6, filed 2026-09-10 under AMEND-SESSION-PLAN-003 §1
