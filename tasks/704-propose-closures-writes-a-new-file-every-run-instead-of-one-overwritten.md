---
id: "[#704]"
title: "`propose_closures` writes a new proposals file every run, so four sessions of asking produced 154 swept files where one current file was wanted"
status: open
priority: P2
size: S
theme: "[E7] Tooling & evaluation"
story: "[S18] Cut session friction with better tooling"
generates: BACKLOG.md
---

- [#704] [P2][S] **`propose_closures` writes a new proposals file every run, so four sessions of asking produced 154 swept files where one current file was wanted** - AX1-4 of `to-cc/AMEND-BATCH-X-ROSTER-001.md`, which adds this row to the roster's section 4 and pairs it with X2-1 (`[#693]`). Carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93). · **LEDGER D5 is titled "`logs/PROPOSALS-*` litter -- 4 sessions of asking"** and records the state precisely: DONE (half). 154 files were swept into `logs/2026-09/` on 2026-09-09, and the SOURCE fix -- `propose_closures` writes one file, overwritten -- was believed to ride `[#664]` as a `writes` edge, with the ledger itself flagging the doubt: "verify it is actually in `[#664]`'s clauses; if not it is unowned". It is not, and AX1-4 files it rather than leaving the verification owed. · **Sweeping is not fixing, and the distinction is the row.** A sweep moves the accumulated files; the next run writes another one. Four sessions of the operator asking for the same thing is the measure of a fix that never reached the producer. · **This row is in TENSION with `[#626]`, and the tension is real rather than a duplicate.** `[#626]` owns `logs/` retention and its Done-when has the two callers resolve a BUCKETED path (`logs/YYYY-MM/PROPOSALS-*.md`) and the prefix exemptions removed -- a design that presumes a DATED SERIES of proposals files. This row's design is ONE file overwritten, which has no series to bucket and no exemption to retire. `[#626]` already names the collision from its own side: intake #66 places `logs/proposals/` under its retention, and it records that a genuinely append-only stream "carries no date in its name at all", so the mechanism as built would not reach it. **Whichever lands first constrains the other, and they must not be built independently.** · Done when: `propose_closures` writes ONE file, overwritten per run, rather than a new file per run; the readers that glob the old series -- `propose_closures.resolve_window` / `find_last_proposals_head` and `review_closures.latest_proposals` -- resolve the single file, with a test per reader; a second run leaves the file COUNT unchanged; and the decision is reconciled with `[#626]` in writing rather than by whichever lane commits last · **Done-when extends AX1-4's own sentence** ("`propose_closures` writes ONE file overwritten per run") with the reader and reconciliation clauses the tension above makes necessary · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, Part 2 AX1-4 and Part 5), `to-browser/LEDGER-OPERATOR-EXPECTATIONS-2026-09-10.md` row D5, `scripts/propose_closures.py`, `scripts/review_closures.py`, `scripts/logs_retention.py`, `[#626]` (the retention row this is in tension with -- read them together), `[#693]` (X2-1, the detector row AX1-4 pairs this with), `[#277]`, `[#487]`, `[#664]` (the row the ledger suspected owned this and does not) · kill-candidates: none -- `[#626]` is named above as in-tension rather than absorbing, and its Done-when presumes the dated series this row deletes; `[#664]` was checked for a `writes` edge covering this and does not carry one, which is why AX1-4 files it · source: `to-cc/AMEND-BATCH-X-ROSTER-001.md` AX1-4, filed 2026-09-11 by lane `lane-x-000-batch-x-roster-lands`
