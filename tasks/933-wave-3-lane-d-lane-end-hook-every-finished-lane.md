---
id: "[#933]"
title: "Wave 3 lane D (lane-end-hook) -- every finished lane reports itself, once, through a real event"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#933] [P1][M] **Wave 3 lane D (lane-end-hook) -- every finished lane reports itself, once, through a real event** - filed by LANE-W3-A per `to-cc/DECLARE-WAVE3-CONNECT-2026-09-21.md` hard precondition 2 (every wave row is filed before any other wave-3 lane fires); frozen contract `LANE-W3-D-lane-end-hook.md`, branch `worktree-lane-end-hook`, a lane of the loop `[#929]` evaluated. Value: Today a lane reports because its contract tells it to, and the operator is the one who notices when it does not. After this lane the Claude Code Stop event does it: the moment a lane writes its HANDBACK line, the next turn end runs the lane-end moment exactly once, and the report lands on the transport verified by read-back. This is the first event-driven link in the loop that no human and no prompt has to remember. · Done when: carried verbatim from the frozen contract — 1. **One new Stop entry** in `.claude/settings.json` runs a guard that exits in well under a second when the session's HANDBACK line is absent, and otherwise runs `doit moment:lane-end` once — a receipt marks the moment done so later turn ends skip it. The existing Stop entries are untouched; the bounded_hook wrapper stays unwired (operator ruling 2026-09-17 item 1). 2. **It can never block the session:** any failure is a receipt with a non-zero exit, never a prevented continuation. 3. **It fits the budget:** the guard path and the full moment are timed; the timings are recorded against the declared 15-second limit shared with the other Stop entries. 4. **Proof in a real background lane:** from this lane's own transcript, show one `stop_hook_summary` where the guard skipped (before the HANDBACK line) and exactly one lane-end moment receipt after it. 5. **Tests (common rules 1-4):** guard skips with no HANDBACK line; runs once with it; a second turn end after completion does nothing; an unmounted transport records a refusal and does not block. 6. **Codex terra review** recorded; **handback** per common rules — your own lane-end is the first live use of your work, so write the HANDBACK line, then end one more turn, then check. · refs `to-cc/WAVE3-COMMON-2026-09-21.md`, `to-cc/DECLARE-WAVE3-CONNECT-2026-09-21.md`, `[#929]` · kill-candidates: none -- each wave-3 lane wires an organ that already exists; no open row is absorbed
