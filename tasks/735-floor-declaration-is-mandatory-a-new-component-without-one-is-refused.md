---
id: "[#735]"
title: "Floor declaration is mandatory — a new organ, hook, gate, doc or command carrying no `floor: MUST | hub-only` is refused at commit"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
implements: "AMEND-BATCH-X-ROSTER-004, AMEND-BATCH-X-ROSTER-008, AMEND-BATCH-X-ROSTER-009, AMEND-BATCH-X-ROSTER-010"
generates: BACKLOG.md
---

- [#735] [P1][M] **Floor declaration is mandatory — a new organ, hook, gate, doc or command carrying no `floor: MUST | hub-only` is refused at commit** - `AX4-1` of `to-cc/AMEND-BATCH-X-ROSTER-004.md`, carried into every batch X lane contract and filed here by X1-1's lane, which the clause itself names as the owner. Carried verbatim: *"**AX4-1 · Floor declaration is mandatory.** Every row and every lane contract carries `floor: MUST | hub-only`; a NEW organ, hook, gate, doc or command without it is refused at commit (validated against `ecosystem/parity-surfaces.yaml`; `fleet_parity` is the check). Default is MUST — hub-only requires a one-line reason. Applies retroactively to batch W's outputs and every batch X row: W-7 selector, W-2 guard, `decision_coverage`, routing, STANDING_RULINGS, the docs cut, conductor E — each dispositioned MUST or hub-only before X1 dispatches. Owner: `decision_coverage` lane (X1-1) gains this clause; the parity registry is the carrier."* · **AX8-4 folds in, verbatim:** *"**AX8-4 · Templates are floor components** (the new-project starter): covered by X-C's census (`templates/`), declared MUST under AX4-1, rendered from the same source as the docs after the cut."* — so `templates/` is inside the declarable population rather than beside it, and its tier is already ruled MUST before this row's mechanism exists. · **AX10-3 folds in, verbatim:** *"**AX10-3 · Floor: MUST.** Backlog management is identical in every consumer repo."* — one worked disposition, carried so the mechanism lands with at least one declaration it did not invent. · **AX9-4 folds in, verbatim:** *"**AX9-4 · Exists-before-build clause on the decision engine (X1-1) and the floor declaration (AX4-1):** a contract that CREATES an organ, script, hook or doc must quote a `process-list` / organ-index result showing no existing organ answers the need; a NEW organ without that quote is refused at the contract gate. This is the RED-first form of 'check the process map before you build'."* — AX9-4 binds the CONTRACT GATE, not this row's own mechanism, and the two are deliberately separate refusals: AX4-1 asks *which repos must carry this component*, AX9-4 asks *whether the component should exist at all*. A single check answering both would pass a needed component with no tier and fail a well-tiered duplicate. · **The retroactive clause has already lapsed, and that is recorded rather than quietly dropped.** AX4-1 requires each of its seven named subjects to be "dispositioned MUST or hub-only **before X1 dispatches**". X1 dispatched 2026-09-11 with no disposition filed for any of the seven, because the registry the clause names as the carrier has no `floor:` key and `fleet_parity` has no floor leg — the mechanism did not exist to be used. The condition is therefore not satisfiable retroactively by this row; what survives it is the LIST, which this row carries forward as its own backfill set. · **Why `fleet_parity` is the right check and a new organ is not.** `ecosystem/parity-surfaces.yaml` already carries a per-surface `tier:` map keyed by role (`MUST | SHOULD | LOCAL | IGNORE | INVERSE | TOMBSTONE`) and `fleet_parity` already reads it per repo; a `floor:` declaration is that same question asked at COMPONENT BIRTH rather than at fleet-sweep time, so the answer belongs in the row that already owns the surface. A second registry would let one component hold two floors. · implements: AMEND-BATCH-X-ROSTER-004, AMEND-BATCH-X-ROSTER-008, AMEND-BATCH-X-ROSTER-009, AMEND-BATCH-X-ROSTER-010 · Done when: a NEW organ, hook, gate, doc, command or template that lands with no `floor: MUST | hub-only` declaration is REFUSED at commit tier, with the refusal naming the component and the two admissible values; `hub-only` requires a non-empty one-line reason and a declaration carrying none is refused the same way; the declaration is validated against `ecosystem/parity-surfaces.yaml` with `fleet_parity` as the check, so one component can never hold two floors; AX4-1's seven retroactive subjects — W-7 selector, W-2 guard, `decision_coverage`, routing, STANDING_RULINGS, the docs cut, conductor E — each carry a landed disposition; and one RED-first test FAILS before the refusal exists by landing an undeclared component, per ADR-108 §B · **Floor of this row's own mechanism: MUST** — the declaration discipline is not hub machinery, it is what tells a consumer which components it is owed · refs `to-cc/AMEND-BATCH-X-ROSTER-004.md` AX4-1, `to-cc/AMEND-BATCH-X-ROSTER-008.md` AX8-4, `to-cc/AMEND-BATCH-X-ROSTER-009.md` AX9-4, `to-cc/AMEND-BATCH-X-ROSTER-010.md` AX10-3, `docs/audits/2026-09-11-technical-batch-x-manifest.md` §3 (the clause-to-lane carriage table naming X1-1 as this clause's carrier), `ecosystem/parity-surfaces.yaml` (the carrier), `scripts/fleet_parity.py` (the check), `ecosystem/organ-index.md` and `scripts/generate_organ_index.py` (the component roster a declaration must cover), `[#692]` (the lane that filed this row), `[#736]` (AX13-1 — `lifecycle:` is the same per-component declaration discipline on the time axis, and the two keys should be designed together), `[#719]` (organ-index entries answer to no schema — the schema this key would extend), ADR-103 (parity ownership axis), ADR-102 (parity gate rev axis), ADR-108 §B · kill-candidates: none — `[#719]` owns whether organ-index ENTRIES validate against a catalog model and names no floor tier, `[#664]` owns the graph and the three commit-tier queries and names no parity surface, and `[#337]`'s fleet-parity gate measures EXISTING surfaces against declared tiers rather than refusing an undeclared new one · source: `to-cc/AMEND-BATCH-X-ROSTER-004.md` AX4-1, filed 2026-09-11 by lane `lane-x-692-decision-coverage` under AX12-1
