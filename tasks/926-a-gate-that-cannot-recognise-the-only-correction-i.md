---
id: "[#926]"
title: "A gate that cannot recognise the only correction its own invariants permit -- journal_day_letters and substrate_declaration refuse forever by construction"
status: open
priority: P1
size: M
theme: "[E7] Tooling & evaluation"
story: "[S18] Cut session friction with better tooling"
generates: BACKLOG.md
---

- [#926] [P1][M] **A gate that cannot recognise the only correction its own invariants permit -- journal_day_letters and substrate_declaration refuse forever by construction** - Two ship-tier checks hard-fail on artifacts that the repo's own invariants forbid editing, so no truthful act clears them and every handoff cut refuses (2026-09-19). (1) `audit.py::check_journal_day_letters` counts `### YYYY-MM-DD (<letter>)` headings in `JOURNAL.md`, which is append-only: correction by addition is the ONLY sanctioned correction, and the check cannot see it (2026-09-19 (aq) appended the correction for the duplicate 2026-09-18 (f); the check still FAILs; `[#913]`). (2) `check_substrate_declaration` leg 8 (`substrate-heartbeat-dead`) refuses `LANE-ac-694-cloud-census.md`, a dated immutable contract for a leg that has already run (it never dispatched to cloud at all -- `[#898]`); refusal cannot change any outcome. Its in-file `**Substrate deviation:**` line would mean editing that frozen contract, which the adapter's own docstring says "would falsify the record", and the heartbeat receipt says nothing about a past run and expires in 36 h. SEARCHED, 2026-09-19, for a sanctioned per-check waiver for a ship-gate FAIL: NONE exists -- `ecosystem/disposition-register.yaml` applies to `warn` only (`audit.py::cmd_ship_gate`: fails are collected before the register is consulted), `.methodology.yaml` `sanctioned_divergences` is read only by parity/conformance, and every floor/grandfather (`_JOURNAL_DAY_LETTER_FLOOR`, `LEG_ARM_DATES`) is check code. These are SCOPING DEFECTS IN THE CHECKS, not debt · Done when: each check recognises the correction its invariant permits -- the day-letter check honours an appended correction entry naming the collided pair (or an equivalent recorded exception), and the substrate check stops refusing a contract whose leg has already run -- each with a RED-first test pinning that the correction clears it AND that a new, uncorrected instance still fails; decided as a gate change by ruling, not as a quick fix · refs `scripts/audit.py` (`check_journal_day_letters`, `cmd_ship_gate`), `scripts/audit_checks/check_substrate_declaration.py`, `scripts/validate_substrate.py`, `[#913]`, `[#898]`, `[#921]`
