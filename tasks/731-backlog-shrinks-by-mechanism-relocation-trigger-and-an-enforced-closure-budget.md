---
id: "[#731]"
title: "The backlog only grows -- narration is relocated by hand and nothing stops a batch filing more rows than it closes"
status: open
priority: P1
size: M
theme: "[E5] Canonical-file integrity"
story: "[S14] Keep the day-to-day docs right-sized and current"
generates: BACKLOG.md
---

- [#731] [P1][M] **The backlog only grows -- narration is relocated by hand and nothing stops a batch filing more rows than it closes** - AX10-2 and AX10-3 of `to-cc/AMEND-BATCH-X-ROSTER-010.md`, carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93) Part 10. · **Two clauses the X2 backlog lane gains**, the lane being `[#589]` plus `scripts/archive_row_body.py`: (1) **narration RELOCATES to `tasks/archive/` on a TRIGGER** -- at each batch close -- rather than by hand, and (2) **the closure budget "close before filing" becomes ENFORCED**: a batch that files more rows than it closes reports the OVERDRAFT in its close packet. · **Filed as its own row rather than edited into `[#589]`, deliberately.** `[#589]` predates this lane and this lane does not edit rows it did not file -- the same rule under which X1-1/2/4/5/6's `NEW:` clauses went unapplied. The alternative was to record the clauses as owed, which is what happened to AX8-4 and AX9-4 and left them unreachable. A row is reachable; a note in a close packet is not. **`[#589]` is this row's fold target and should absorb it at grooming.** · **The overdraft clause is the sharper of the two, because it is the one that binds THIS window.** AX11-4 states plainly that this window files more rows than it closes -- and this lane is the largest single contributor, having filed 33 rows and closed none. An enforced budget would have made that visible at the moment it happened instead of at the next audit. Note the ordering it implies with `[#730]`: an evidenced-closure list is what makes the budget payable rather than merely reported, so the two are one mechanism seen from the filing and the closing end. · **AX10-3 · FLOOR: MUST.** Backlog management is identical in every consumer repo. This clause is carried HERE rather than on the floor-declaration row because that row does not exist in this repo -- it is AX4-1, inside the uncarried AMEND-004, which AX8-4 and AX9-4 also point at. Three amendments now attach to one absent row; that is recorded in the intake's Part 13 and is owed. · Done when: narration relocation to `tasks/archive/` fires on a batch-close trigger rather than by hand, using `archive_row_body.py` rather than a second mechanism; a batch that files more rows than it closes reports the overdraft in its close packet, as a number and not as prose; the two clauses are recorded against the X2 backlog lane rather than living only here; and backlog management is declared a floor MUST once a floor-declaration row exists to carry it · **Done-when authored by the filing lane** from AX10-2 and AX10-3 · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, Part 10 AX10-2/AX10-3), `to-cc/AMEND-BATCH-X-ROSTER-010.md`, `[#589]` (**the fold target** -- the X2 backlog lane and the view byte bar), `scripts/archive_row_body.py` (the relocation mechanism that already exists), `[#730]` (evidenced bulk closure -- what makes the budget payable), `[#664]` and `[#687]` (which already call `archive_row_body.py`) · kill-candidates: none -- but this row is filed EXPECTING to be folded: `[#589]` owns the X2 backlog lane and should absorb both clauses at grooming, at which point this row closes as absorbed rather than as done · source: `to-cc/AMEND-BATCH-X-ROSTER-010.md` AX10-2 and AX10-3, filed by lane `lane-x-000-batch-x-roster-lands`
