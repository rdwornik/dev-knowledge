---
id: "[#929]"
title: "The spine is the whole loop -- stages 13-16 (RUN, REVIEW, GATES, MERGE) exist only as organs a human strings together; map them and run the candidate mechanisms live before anything is built"
status: open
priority: P1
size: L
theme: "[E7] Tooling & evaluation"
story: "[S18] Cut session friction with better tooling"
generates: BACKLOG.md
---

- [#929] [P1][L] **The spine is the whole loop -- stages 13-16 (RUN, REVIEW, GATES, MERGE) exist only as organs a human strings together; map them and run the candidate mechanisms live before anything is built** - `to-cc/DECLARE-LOOP-EVAL-2026-09-20.md` R1 (ARCHITECT): `ecosystem/harness.yaml`, run by `scripts/dodo.py` on pydoit, keeps stages 1-12 and gains 13 RUN (an agent call behind an adapter -- dispatch, the Claude Agent SDK, `codex exec`), 14 REVIEW (a second provider), 15 GATES (audit / ship-gate / ruff / impacted tests) and 16 MERGE (the integrator protocol, the operator's GO as the condition). Today 13-16 are separate organs and the operator is the event loop between them. R2: no stage is built until the MAPPING and LIVE-PROBES records below are merged and read by the seat. R3: nothing is implemented without a row -- this is that row, and it owns the EVALUATION only; the stage-7 fix stays `[#922]`. · Done when: (1) the MAPPING record is committed under `docs/audits/`, covering every organ in `scripts/`, `.claude/` (commands, skills, hooks, settings), `.pre-commit-config.yaml`, `.github/workflows/` and the procedures in PLAYBOOK Ch8, each row naming its loop stage (1-16 or none), its current trigger, its verdict (KEEP / BECOMES-STAGE-n / MERGE-INTO-x / RETIRE-CANDIDATE) and the organ output that grounds it, with `GRAPH-GAP` where the graph cannot answer; (2) for each of stages 13-16 the record names the existing organ(s) that would fill it and the concrete missing piece, or states MISSING; (3) the LIVE-PROBES record is committed under `docs/audits/` and gives, for each of B1-B4 (Claude Agent SDK; `codex exec` as a subprocess; pydoit resume; a `--bg` spawn from inside a session), the exact command or script inline, its verbatim load-bearing output, and a PASS / FAIL / PARTIAL verdict against the probe's question; (4) a Codex pass over the MAPPING is recorded with an accept/reject reason per finding; (5) a draft ADR (Status: Proposed) "the spine is the whole loop" exists under `docs/decisions/`, naming the ADRs it amends, its implementing task, and a 30-day metric -- operator touches per unit of work, and interrupted runs resumed without a lost step; (6) DECLARE-LOOP-EVAL-2026-09-20's rulings are landed in their named homes, each citing the DECLARE; (7) `to-browser/DIGEST-loop-eval-2026-09-20.md` and `to-browser/SESSION-lane-loop-eval.md` exist, their landing is confirmed before delivery is claimed, and the DIGEST puts "now" first and answers in plain language what exists per stage 13-16, what is missing, what each probe proved and what to build first · refs `to-cc/DECLARE-LOOP-EVAL-2026-09-20.md`, `to-cc/AMEND-LOOP-EVAL-LAUNCH-2026-09-20.md`, `ecosystem/harness.yaml`, `scripts/dodo.py`, `[#922]` · kill-candidates: none -- `[#669]` / `[#689]` own the conductor (a state machine over rows, GitHub Actions as runner) and `[#922]` owns the stage-7 argv fix; this row owns the in-process spine's stages 13-16 evaluation and duplicates none of them
