---
id: "[#664]"
title: "Wire FPG-1 as the delivery spine — three commit-tier queries, and organs become views"
status: open
priority: P1
size: L
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#664] [P1][L] **Wire FPG-1 as the delivery spine — three commit-tier queries, and organs become views** — DECLARE-SPINE-2026-09-09 §3. FPG-1 exists and nothing triggers it: `scripts/file_purpose_graph.py` builds 1922 nodes / 12,664 edges / 12 edge kinds (ADR-118:34) and is wired into no gate, no check and no hook, while twelve organs recompute edges privately — thirteen answers to "what cites what", none reconciled. So "list all processes" is answered by re-reading the repo. **The registry is a QUERY over the graph, not a file**: the 2026-09-09 paste extending `organ-registry.yaml` to 216 rows is RETRACTED by the DECLARE and is explicitly not this row's work — a hand-maintained copy is INBOX-037's defect reproduced at the spine. Nodes: script · hook · command · skill · task · intake · ADR · file. Edges: `triggers` · `implements` · `cites` · `reads` · `writes` · `imports` — **corpus-structure edges ONLY** (DECLARE-REVIEWS §A.1, as narrowed on main by `ff103444`); state gates stay gates. Built on every commit from the source files and **persisted** as a queryable artifact (sqlite — stdlib, no new dependency), with organs becoming queries against it rather than parsers of their own. **Acceptance = three queries, each a REFUSAL at commit tier:** `orphan_census` (a script node with no inbound `triggers` edge FAILs), `task_coverage` (a file node changed in the commit with no inbound `implements` edge from an OPEN task FAILs — "nic bez taska" as a mechanism, at OPEN and not only at CLOSE), and `process_list` (the traversal that answers "all processes"; `ARCHITECTURE.md` Ch2 is RENDERED from it behind a freshness hook, and prose naming a process the graph lacks becomes a `dangling_reference`). **The fixture and the predicate are not the same cardinality, and that is named rather than rounded:** the census counts **32** orphans — 20 script-class, 3 hooks, 9 commands/skills — so `orphan_census` as §3 words it reaches the **20**, and the remaining 12 need either a widened node class or their own queries; whichever is chosen is recorded, not silently dropped. Intake `#86` folds into this row, its trigger being the commit-time rebuild. `propose_closures` writing one file per run and `archive_row_body` gaining its trigger ride are `writes` / `triggers` edges of this arc, **not separate rows**. **Anti-patterns, each disqualifying:** no YAML or markdown copy of the graph; no LLM inside a query; no organ parsing what the graph holds; a skill's mandate becomes a hook, never a graph row. MODE plan · decision budget 2 · terra pre-merge · merged last in its batch. · **CARRIED INTO THIS ROW by lane `lane-x-664-delivery-spine` (AX12-1 — nothing stays SAID), quoted with its AX id:** "**AX4-1 · Floor declaration is mandatory.** Every row and every lane contract carries `floor: MUST | hub-only`; a NEW organ, hook, gate, doc or command without it is refused at commit (validated against `ecosystem/parity-surfaces.yaml`; `fleet_parity` is the check). Default is MUST — hub-only requires a one-line reason." — this row's four `graph-*` pre-commit hooks are its components and are **hub-only**, one-line reason: *the graph is this repo's corpus and a consumer repo has its own; ADR-118 §5 ships it fleet-wide only at W-G4*. · "**AX3-7 · Waves after this amendment:** X1 = decision_coverage · conductor E (ships in floor) · merge cost · routing · spine `[#664]` · docs cut. X2 = prompt distiller · logs · `[#589]` byte bar + archive · `[#687]` keys · `[#669]` conductor · codespace row. X3 = `[#664]` step D + organ map · `[#667]` render · telemetry + GO artifact · M03 re-run." — **this row is deliberately SPLIT across waves**: X1 carries the spine, and **step D (the `ARCHITECTURE.md` Ch2 render) plus the organ map are X3**, so a lane in X1 that does not render Ch2 is conforming, not short. · **Status after lane `x-664` (X1-5):** clause 1 is DISCHARGED and re-measured on the merged tree — three refusals live at commit tier with a trip-test each, the store persisted in stdlib sqlite, `orphan_census` 0, `task_coverage` 0 FAIL, the 32-vs-20 gap recorded and the dead "12 → 0" bar re-measured at N-before 18 / N-after 18 / migrated 0. **What keeps the row OPEN:** the eighteen private edge computations still owe their W-G3 migration (ADR-118 §5, one organ per lane), and step D is X3. Evidence: `docs/audits/2026-09-11-technical-lane-x-664-delivery-spine.md`. · Done when: the corpus-structure edge computations are re-measured under §A.1's five-kind class (citation, generation, template, test, script call-site) and driven to 0 with every organ holding one reading FPG-1 instead — **the blanket "12 → 0" bar is DEAD**, withdrawn by `ff103444` because 12 counted state gates the class excludes, so the migration lane re-measures rather than inheriting an integer; the three queries refuse at commit tier with a trip-test each; `orphan_census` reaches 0 against its stated node class after dispositions; and `task_coverage` reports 0 FAIL on the merged tree · refs docs/decisions/ADR-118-one-graph-organs-are-views.md, docs/intake/2026-08-22-tech-document-dependency-graph-organ.md (intake #40), docs/intake/2026-09-07-tech-orphan-census-organ.md (intake #86), docs/audits/2026-09-08-technical-process-trigger-census.md, scripts/file_purpose_graph.py, [#618], [#640] · source: DECLARE-SPINE-2026-09-09 §3, filed by the batch V integrator
