---
id: "[#694]"
title: "Four telemetry modules are inventory rather than harness -- each gets a trigger or is deleted, and the roster allows no third state"
status: open
priority: P1
size: M
theme: "[E7] Tooling & evaluation"
story: "[S18] Cut session friction with better tooling"
generates: BACKLOG.md
---

- [#694] [P1][M] **Four telemetry modules are inventory rather than harness -- each gets a trigger or is deleted, and the roster allows no third state** - Lane X2-6 of the batch X roster, carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93) section 3. LEDGER F6 records the population as four modules unwired -- `scripts/cost_usage_telemetry.py`, `scripts/gen_trend_dashboard.py`, `scripts/window_metrics.py`, `scripts/failed_set.py` -- with `[#576]` reading a different store. · **The row is a disposition, not a build**, and that is the whole point of the operator's harness definition carried at intake 92: a capability with no trigger is not harness, it is inventory. `cost_usage_telemetry` is named there by name as witnessed INVENTORY. So each module either gains a call site on a real trigger -- the review tally and the lane receipt are the two the roster's own wave names -- or it is deleted. A module left in place with a docstring explaining what it would measure is the failure state this row exists to end. · **Do not take the wiring question as settled by a name grep.** Each of the four is MENTIONED by `scripts/graph_queries.py` and some by `scripts/assemble_paste.py`, `scripts/gen_task_tree.py`, `scripts/offload_admission.py`, `scripts/validate_doc_rot.py` and `scripts/gen_lane_contract.py` -- and a name appearing in an organ list proves the organ is ENUMERATED, not that it is CALLED. The row's first act is to resolve, per module, whether any of those mentions is a call site; the LEDGER's "unwired" is the claim under test, not the finding. · **The Done-when carried below is the roster's and covers two halves.** The telemetry half is this row. The GO-file half -- "dispatch refuses without the GO file, trip-tested" -- is `[#685]`, which is already open and is NOT re-filed here; the roster pairs them in one lane and they are two rows. · **AX9-5 ADDS A METRIC CLAUSE TO THIS ROW**, carried at intake 93 Part 9 and folded in here rather than filed separately: the telemetry records **raw-search calls versus organ calls per session, and organs never called in 30 days**, with the log-review routine (`[#709]`) flagging regressions. That number is the one the operator reads to see whether the model USES the harness or rebuilds it -- it is also the measurement that tells whether `[#727]`, `[#728]` and `[#729]` actually worked, so it wants a baseline taken BEFORE they land. · Done when: modules wired or deleted; dispatch refuses without the GO file, trip-tested · **plus AX9-5's metric clause**: raw-search calls vs organ calls per session, and organs never called in 30 days, both emitted by the surviving telemetry · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, section 3 lane X2-6 -- the Done-when above is the roster's own words), `scripts/cost_usage_telemetry.py`, `scripts/gen_trend_dashboard.py`, `scripts/window_metrics.py`, `scripts/failed_set.py`, `[#685]` (the GO-artifact half of the same lane), `[#576]` (reads a different store), `[#529]`, `[#565]`, `[#575]`, `[#705]` (the visualization row that renders from whatever survives here), `docs/intake/2026-09-10-tech-harness-is-process.md` (intake 92 -- the definition that makes "inventory" the verdict it is), `[#727]` / `[#728]` / `[#729]` (what AX9-5's metric measures), `[#709]` (the routine that flags its regressions) · kill-candidates: none -- `[#576]` owns a telemetry READ PATH over a different store and would not notice these four being deleted; `[#529]` owns emission from the gate mesh, not the disposition of unwired modules · source: `to-cc/DECLARE-BATCH-X-ROSTER-2026-09-11.md` section 3 lane X2-6 (LEDGER F6 the population), filed 2026-09-11 by lane `lane-x-000-batch-x-roster-lands`
