---
id: "[#271]"
title: "Nightly proposal loop"
status: open
priority: P3
size: L
theme: "[E7] Tooling & evaluation"
story: "[S20] Revive the nightly layer, load-gauge first (rent-rule discipline)"
generates: BACKLOG.md
---

- [#271] [P3][L] Nightly proposal loop — revive the Tier-2 draft ONLY under intake brief #1 §6 constraints (carried ex-ante, not renegotiable at build): night EXECUTES pre-authorized deterministic contracts only (full serial test suite + timing-trend report; mechanically-verifiable hygiene; rot-report once built) and PROPOSES the rest (refactor candidates flagged never deleted; ADR drafts status Proposed; feature ideas → `docs/intake/` as `status: SEED` — no separate proposals/ folder); morning-triage teeth: CAP ~5 proposals/night, untriaged items auto-expire in 7 days; no autonomous semantic refactoring at night · Done when: the loop runs nightly under a `· routine:` block that `routine_consumers` passes with `[#270]`'s load-gauge live; each §6 constraint — the ~5 proposals/night cap, the 7-day auto-expire, no autonomous semantic refactoring at night, and proposals landing in `docs/intake/` as `status: SEED` — is enforced by a check or a test rather than by convention; and a `docs/audits/<date>-technical-*` artifact records the first 2-week survival review with its measured accept-rate against the <20% kill threshold · refs docs/intake/archive/2026-07-06-functional-architect-nightly-loop.md §6, docs/audits/2026-07-05-draft-tier2-nightly-layer.md, ADR-98, #86 · **RE-CUT 2026-08-28** (K4). **Overtaken AS A CHARTER, and the charter half is struck.** The row was written to revive a Tier-2 nightly *loop* under intake brief #1 §6 — a nightly cadence that PROPOSES work into a morning triage. What actually happened is that the night became the **lane/batch protocol** (PLAYBOOK Ch8; the 2026-08-26 night ran four cloud sessions and every one delivered), and that protocol is being written down as doctrine by **intake #60 / ruling X8**, not by this row. Carrying a rival charter beside a live one is how two protocols drift. The §6 constraint list (~5 proposals/night cap, 7-day auto-expire, no autonomous semantic refactoring, proposals landing as `docs/intake/` SEEDs) travels to intake #60's protocol, which is its correct home. **REMAINDER, now the whole row: exactly what `[#288]` needs from it — the survival-metric review.** `[#288]` carries `depends-on: #271` because its model-identity guard exists to keep evidence COMPARABLE across unattended runs, and its own body names the reason: *"the survival-metric review at #271 depends on comparing like-for-like"*. So the surviving Done-when is the measurement, not the loop: a `docs/audits/<date>-technical-*` artifact records a survival review with a measured accept-rate against the **<20% kill threshold**, over whatever unattended cadence is actually running — which is now the batch protocol, not a nightly proposal loop. Re-size is owed (banded **L** against a charter; the remainder is plausibly **S**) and is deliberately left to the operator, since a band change moves the row in the ranking · un-deferred from the 2026-08-27 icebox sweep by this lane
