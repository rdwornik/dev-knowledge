---
id: "[#696]"
title: "M03 leg 1a ran to captured stdout with UNVERIFIED locators and one silent empty write, so the practices it produced rest on evidence nobody can re-open"
status: open
priority: P2
size: M
theme: "[E7] Tooling & evaluation"
story: "[S19] Decide the undecided artifact/tool models"
generates: BACKLOG.md
---

- [#696] [P2][M] **M03 leg 1a ran to captured stdout with UNVERIFIED locators and one silent empty write, so the practices it produced rest on evidence nobody can re-open** - Named in the batch X roster section 4 as "M03 leg-1a re-run (agy, stdout, tracked audit)", carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93). · **Three defects in the run, all recorded in the night log at the time rather than reconstructed.** (1) **stdout, not a file**: L01 came back through the streaming client, which emitted the table three times and was "deduped by hand on capture" -- a hand-deduplicated terminal capture is not a re-openable artifact. (2) **silent empty write**: `1b-ajcode.md` records that its first attempt "FAILED SILENTLY: printed DONE at exit 0 and wrote no file (same failure as 1a-L04)" -- so the same silent-success defect hit leg 1a, and an exit-0-with-no-output run is indistinguishable from a successful one. (3) **UNVERIFIED locators**: MATRIX.md marks the whole of section 1b "leg 1a, agy; all locators UNVERIFIED †", and the night log records why -- the chosen source carries no page numbers and no timestamps, so locators became line numbers in a split file the mission never asked for. That was recorded rather than silently swapped, which is correct practice and still leaves the evidence unresolvable. · **Why a re-run rather than a repair of the artifact.** This repo's rule is that a locator you have not opened is a claim rather than evidence; every M03 practice row sourced from leg 1a inherits that. The artifacts are immutable audits, so the repair is a new run whose output lands as a tracked file, not an edit to the existing one. · Done when: leg 1a is re-run with its output landing as a tracked file under `docs/audits/` rather than as captured stdout; the silent-success failure is fixed or trip-tested so a run cannot report DONE at exit 0 having written no file; and every locator the re-run emits resolves in the source it names, with the UNVERIFIED marker retired only for rows that actually resolved · **Done-when authored by the filing lane, not carried**: the roster names this item in a section-4 prose list that carries no Done-when column · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, section 4), `docs/audits/2026-09-10-technical-night-aj-m03/NIGHT-LOG.md` (the locator caveat and the L01 stdout capture), `docs/audits/2026-09-10-technical-night-aj-m03/MATRIX.md` (section 1b, the UNVERIFIED marking), `docs/audits/2026-09-10-technical-night-aj-m03/1b-ajcode.md` (the silent-failure record), `docs/audits/2026-09-10-technical-night-aj-m03/MISSION-PROMPT.md`, `[#695]` (intake #70, the other AJ evidence row), `[#627]` (agy's admission, which gates which model may do the re-run) · kill-candidates: none -- `[#627]` owns whether agy is ADMITTED for analysis at all and would block this re-run rather than perform it; no open row names leg 1a or the M03 matrix · source: `to-cc/DECLARE-BATCH-X-ROSTER-2026-09-11.md` section 4, filed 2026-09-11 by lane `lane-x-000-batch-x-roster-lands`
