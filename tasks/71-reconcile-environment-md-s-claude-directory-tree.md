---
id: "[#71]"
title: "Reconcile ENVIRONMENT.md's `~/.claude/` directory tree with live contents"
status: closed
priority: P3
size: S
theme: "[E5] Canonical-file integrity"
story: "[S13] Keep canonical files accurate"
serialize-group: environment
generates: BACKLOG.md
---

- [#71] [P3][S] Reconcile ENVIRONMENT.md's `~/.claude/` directory tree with live contents (commands = codex-review + session-summary only — boot/evolve archived 2026-06-05 Phase-C3, target re-scoped 2026-06-07 groom; skills = gotchas + verify; agents/hooks already match); **+ folded from #119:** ENVIRONMENT.md's "No Codex CLI / Rejected" lines (243/255) contradict the live `/codex-review` command + `codex-review` skill (Codex installed 0.141.0) — reconcile in the same pass · Done when: the ENVIRONMENT `~/.claude/` tree matches `ls ~/.claude/{commands,skills}` and the Codex/Rejected lines are reconciled to live state · refs G6 process-hardening sweep · serialize-group: environment · DEFER — the 45-day icebox sweep of 2026-08-27 (C4 §3, ruling X1's final step, night-harvest governance session). Peg: **no row-level dependency is claimed** — this is an ATTENTION decision, not a blocked-by. Un-defers when an arc claims the row or the operator re-prioritises it. Recorded explicitly so this row is never mistaken for one waiting on another row, which is the failure the same session's re-peg step was fixing. · **CLOSED 2026-08-28** ([#71], K4 DO-IT). Both Done-when legs discharged against LIVE L0 state read on the operator's own machine — the 2026-08-23 LANE-L5 pass could not do this, having run in a cloud container where `~/.claude` is unreachable, and said so. **Leg 1, the tree:** `commands/` (codex-review.md, session-summary.md) and `skills/` (gotchas) MATCH the file as written — the row's named predicate was already true. Two blocks did NOT match and are now corrected: `hooks/` listed `claude-notify.ps1` inside it when that file lives at the `~/.claude` ROOT, and omitted `surface-closures.ps1` and the three retained `block-onedrive.SUPERSEDED*.ps1` predecessors; `memory/` listed seven files where live state has TWO (corrections.jsonl, sessions.jsonl). **Leg 2, the Codex contradiction:** LANE-L5 annotated the *Active decisions* half and left the *Rejected* half standing, so the file still carried "Codex CLI (no advantage over Haiku subagents)" as live doctrine one screen below its own reversal — now struck and annotated on the same pattern, record annotated rather than erased. **Finding handed on, not fixed:** the global CLAUDE.md Self-Evolution Protocol step 1 directs a read of `~/.claude/memory/learned-rules.md`, which does not exist, along with README.md / evolution-log.md / observations.jsonl / violations.jsonl. That is an L0 file outside this repo's write scope (Layer-2 invariant), so it is recorded in the tree block and triaged as an ADR-111 CANDIDATE in this lane's report rather than silently repaired
