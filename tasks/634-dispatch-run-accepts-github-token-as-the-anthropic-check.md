---
id: "[#634]"
title: "dispatch-run.sh accepts GITHUB_TOKEN as its Anthropic-token check — a fail-open admission gate on a paid substrate"
status: open
priority: P1
size: S
theme: "[E7] Tooling & evaluation"
story: "[S18] Cut session friction with better tooling"
generates: BACKLOG.md
---

- [#634] [P1][S] **dispatch-run.sh accepts GITHUB_TOKEN as its Anthropic-token check — a fail-open admission gate on a paid substrate** — **RE-SCOPED, operator ruling 2026-09-05 (OPTION 2 amended): the filed defect is DISCHARGED, the STANDING HOLD IS LIFTED, and the title names the ORIGINAL scope, not the residual.** `win-tooling@d6cbd92` (2026-09-01, merged `49cb75e`) had already split the credential families — `adm_tok_anthropic` (ANTHROPIC_API_KEY / ANTHROPIC_AUTH_TOKEN / CLAUDE_CODE_OAUTH_TOKEN) vs `adm_tok_github` (GH_TOKEN / GITHUB_TOKEN), reported by name, never by value — so a GitHub credential can no longer satisfy an Anthropic check. This row was ruled from AMENDMENT 2 of the 632 long-run witness, which PRE-DATES that fix: the premise was stale on arrival. **The residual is NOT “refuse when the Anthropic token is absent”** — that is fail-CLOSED on a measured-working path: in `lane-632-longrun-a-w95qprj5wq4cg67g` `CLAUDE_CODE_OAUTH_TOKEN` was UNSET and `claude auth status` said `loggedIn: false`, yet the session ran 338 events authenticated over `CLAUDE_CODE_MESSAGING_SOCKET`. Both local probes FALSE-NEGATIVE, which is why neither family gates. **Owner: `win-tooling`** (`config/dispatch-helpers/DispatchHelpers.psm1`, the emitted STEP 0 heredoc, plus `tests/test_dispatch_helpers_codespace.py`); `dispatch-run.sh` is a generated per-run artifact, gitignored at this repo's root, not editable here. · Done when: (a) the admission probe can SEE the socket path — a third probe beside the env scan and `claude auth status` separating “authenticated over CLAUDE_CODE_MESSAGING_SOCKET” from “not authenticated at all”, so a receipt stops false-negating a container that works; and (b) Done-clause 0 is the correctness gate for every dispatched lane — **a commit on the lane's `worktree-<slug>` branch, never the receipt** — a receipt reports TRANSPORT, and `Ok` / `RemoteExitCode` say nothing about whether the lane committed · refs `win-tooling@d6cbd92`, `docs/audits/2026-09-01-verification-codespace-longrun-proof.md`, `[#632]`, `[#618]` · kill-candidates: none — an operator-ruled P1 defect row closing nothing; `[#632]` stays open as the six-layer substrate plan · source: operator ruling 2026-09-05 item 2d, re-scoped by operator ruling 2026-09-05
