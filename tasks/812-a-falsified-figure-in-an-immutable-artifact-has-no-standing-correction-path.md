---
id: "[#812]"
title: "A figure later falsified inside an immutable artifact has no standing correction path -- the protected file keeps circulating the error"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#812] [P1][M] **A figure later falsified inside an immutable artifact has no standing correction path -- the protected file keeps circulating the error** - Operator order 2026-09-17, after lane ab-808's retraction. Immutability protects the record, not the error: audits, handoffs, JOURNAL entries, intakes and merge subjects cannot be edited, so a figure falsified after landing stays readable as fact, and nothing warns the reader or stops the next author from quoting it. **The instance (three corrections, each reproducing the defect it fixed):** lane ab-808 reported the prompts guard "timed out and PERMITTED 477 of 573 calls (83%)" and `deny_and_point` "182 of 182" -- transcript records counted as calls. The figure reached the browser intake `docs/intake/2026-09-16-tech-browser-seat-findings-off-the-transport.md` (lines 124-125, 607) and the JOURNAL entry justifying the commit-gate strip `6e9f0bb8` (entry 2026-09-17 (d)). The lane retracted it to "13% and 4%", and that correction propagated into the merge subject `06a2bccf`, JOURNAL entries (l) and (m), the close packet `docs/audits/2026-09-16-technical-batch-ab-close-packet.md` (lines 101, 115) and `[#883]` -- but 13% counted only sessions that left a record, the same bias smaller. The operator ruled the event-fired denominator across all sessions (X-3): **6% and 2%**. 83% became 13% became 6%, across a close packet, a JOURNAL entry, a merge subject, a row and an intake, and only AMENDMENT 1 of `docs/audits/2026-09-16-technical-lane-ab-808-guard-timeout.md` carries the ruled value. The stopgap on this branch is hand-written: a flush-left SUPERSEDED marker, inserted at the head of the three immutable files, and `[#883]` edited as a living row; the merge subject cannot be marked at all. Nothing makes the next falsification get the same treatment, and nothing finds the copies. · Done when: RED-first, (1) a registry of superseded claims exists -- one entry per falsified figure: the literal claim text or pattern, the ruled value, the authority (amendment or ruling) and the date -- and adding an entry is the ONE act a correction requires; (2) a check fails any tracked file in an immutable class that contains a registered superseded claim without a SUPERSEDED marker at its head pointing at the authority, and names every such file, so a correction reaches all copies rather than the ones its author remembered; (3) a living file (row, protocol, README) quoting a registered superseded claim fails the same check until it is edited; (4) the marker shape is one template, a pure insertion that immutability gates accept; (5) merge subjects and commit messages, which cannot be marked, are listed by the check as known-unmarkable copies, so the residue is stated rather than silent · refs `docs/audits/2026-09-16-technical-lane-ab-808-guard-timeout.md` AMENDMENT 1, `docs/audits/2026-09-16-technical-batch-ab-close-packet.md`, `JOURNAL.md` entries 2026-09-17 (d) (l) (m), `docs/intake/2026-09-16-tech-browser-seat-findings-off-the-transport.md`, `tasks/883-a-gate-ships-with-a-counter-or-is-removed.md` · kill-candidates: none -- `[#811]` pins a contract's bytes before a lane boots; `[#843]` verifies a premise clause against main at freeze; this row corrects a figure after it has landed in files nobody may edit · source: operator order 2026-09-17, id 812 reserved by push from lane ab-808's block
