---
id: "[#625]"
title: "The rule-adherence eval corpus — a FRESH corpus, because neither fold target can carry it"
status: open
priority: P1
size: M
theme: "[E7] Tooling & evaluation"
story: "[S18] Cut session friction with better tooling"
serialize-group: none
generates: BACKLOG.md
---

- [#625] [P1][M] **The rule-adherence eval corpus — a FRESH corpus, because neither fold target can carry it** — intake #35's PROPOSED ROW R3 is the arc's RANK-1 item: **three independent AUT lanes converge on it from directions that never touch**, and it is the missing REWARD FUNCTION four other items sit behind (the ruled sequence is *metric → measure → only then an optimizer*). R3's `kill-candidates` line said *fold, do not birth* into `[#491]`/`[#492]`. **OPERATOR RULING 2026-08-30: never fold into a deferred row; fold into `[#491]` only if it is open and carries the fresh-corpus scope — otherwise birth under Z-G1 from the banked ledger.** BOTH fallback conditions fired, verified live: `[#491]` is **`status: deferred`** (the ruling's premise recorded it as open — stale), **and** its subject is *"Gemini scanning lane — ruling R-G plus an acceptance contract"*, which carries **no corpus scope whatsoever**. So the fold had two independent reasons to be impossible, and only one of them was known when the fold instruction was written. `[#492]` is annotated *superseded for this purpose* rather than reopened — it is the grok reviewer-lane admission row and its subject is a reviewer, not a corpus. · Done when: a rule-adherence harness runs ≥10 cases derived from `~/.claude/rules/core-invariants.md`, and it DISCRIMINATES — a deliberately weakened instruction set FAILS while the intact set passes; the config is deleted if it does not discriminate, because a harness that passes everything measures nothing · **Tier S CONDITIONALLY**, per ADR-112's own graduation trigger: `npx promptfoo eval` run by hand, unwired, touching no gate and no `scripts/`, is Tier S — **the moment it is wired into a hook or asserts a threshold the tree enforces, it was Tier L from the start** · installability: **ZERO** `uv.lock` packages (it is npm, not Python, so the `==0.11.19` pin is not engaged); Node already present via the tracked root `package.json`. **Carried trap: PyPI `promptfoo` 0.1.4 is a DIFFERENT, near-empty package.** · refs docs/intake/2026-08-17-tech-agent-instruction-layers-and-distillation.md (intake #35 R3 + its 2026-08-30 amendment), docs/audits/2026-08-29-technical-autonomy-synthesis.md (§1.1, §1.4, §(d) RANK 1), docs/audits/2026-08-30-technical-autonomy-decision-tree.md, #492, #624 · kill-candidates: none — `[#491]` and `[#492]` are both DEFERRED and neither carries corpus scope, so killing either frees nothing this row needs; the birth is operator-ruled under Z-G1 against the banked ledger · serialize-group: none
