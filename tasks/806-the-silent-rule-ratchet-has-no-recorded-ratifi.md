---
id: "[#806]"
title: "The silent-rule ratchet has no recorded ratification path"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#806] [P1][M] **The silent-rule ratchet has no recorded ratification path** - Operator ruling 2026-09-16. **A gate that cannot accept a ruled exception forces one of two bad outcomes: a bypass, or a reword that satisfies the counter rather than the corpus.** The measured instance: the operator APPROVED raising `ecosystem/silent-rule-baseline.yaml` 447 -> 452 for batch-AA lane aa-14 ("asked to add register entries and added eleven, net five"). The integrator measured 452 exactly on the merged tree and `audit-health` REFUSED the commit: `scripts/silent_rule_detector.py::validate_transition` rejects every `new > old`, and `scripts/audit.py::check_silent_rule_ratchet` reads the previous value from main with NO input through which a ruling can reach it. The baseline file's own header says "raising is an operator ruling, not a commit", and no code path exists for that ruling. · **WHAT THE SEAT DID, AND WHY IT IS THE DEFECT RATHER THAN THE FIX.** No bypass was used (the right call). Instead five DESCRIPTIVE must/never occurrences in aa-14's register notes were reworded, meaning unchanged, so the live count landed at 447 and no raise was needed (merge `e0c82c21`, recorded in its sync body). That was honest, but the five phrases changed to satisfy a counter, not because the wording was wrong. Repeated, that becomes the metric teaching authors to avoid normative words, which is the opposite of what a silent-rule register measures. · **THE PRIOR LANDINGS WERE ACCIDENTS, NOT A MECHANISM.** R8's 441 -> 443 landed only because the detector id also moved v4 -> v5 (non-commensurable -> WARN), and 2026-09-06's 443 -> 447 only by cutting an anchor branch from a pre-raise ref. Neither path exists for the next commensurable raise. · Done when: (1) a raise of `baseline` PASSES `silent_rule_ratchet` when, and only when, the same commit records a ratification the check can read (e.g. a `ratified_by:` entry naming a ruling id or a frozen lane-contract clause, a before/after count, and the measured delta), and the check verifies the recorded delta equals the measured one; (2) a raise without that record still FAILS exactly as today; (3) RED-first witnesses for both, plus one where the recorded delta disagrees with the measured count and is refused; (4) the ratification record is append-only in the baseline file's provenance, so every accepted raise stays auditable; (5) no `--no-verify`, env-var or disposition-register route is added -- the ratification is data in the tree, reviewed with the commit that uses it · refs `scripts/silent_rule_detector.py` (`validate_transition`), `scripts/audit.py` (`check_silent_rule_ratchet`, `_target_baseline_state`), `ecosystem/silent-rule-baseline.yaml`, `[#805]` Done-when (3b) (the same gap stated as an integrator default; this row owns the gate), `[#436]` (the ratchet), JOURNAL 2026-09-16 (c) and (f) · kill-candidates: none -- `[#805]` stays open because a ratchet that accepts ratifications is one of its three defaults, not all of them; `[#436]` is the ratchet's own row and this amends it rather than replacing it · source: operator ruling 2026-09-16 on the batch-AA integration, merge `e0c82c21`
