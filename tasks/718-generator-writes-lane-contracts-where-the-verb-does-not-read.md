---
id: "[#718]"
title: "The generator writes lane contracts to `to-cc/` while `Dispatch-Lane` resolves against the prompts root -- six contracts written, six refused"
status: closed
priority: P1
size: S
theme: "[E7] Tooling & evaluation"
story: "[S18] Cut session friction with better tooling"
generates: BACKLOG.md
---

- [#718] [P1][S] **The generator writes lane contracts to `to-cc/` while `Dispatch-Lane` resolves against the prompts root -- six contracts written, six refused** - First of AX7-3's three measured dispatch defects, each filed as its own FILE row with a RED-first test. Carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93) Part 5. · **Measured at full batch scale rather than inferred.** Batch W wrote SIX lane contracts to `to-cc/` while `Dispatch-Lane` resolves against the prompts root, and all six were REFUSED -- a whole batch's worth of generated contracts landing where nothing reads them. · **Two literals where there should be one key.** The writer resolves one root and the reader resolves another, and both are individually CORRECT about the root they name. That is why this survived review: there is no wrong line to find, only two right lines that disagree. It is exactly the class APR-2 generalises, and this row is its first instance. · **The refusal is the good outcome here, and it should survive the fix.** Six refusals cost six dispatches; six contracts silently read from a stale location would have cost six lanes running against the wrong text. So the fix is to make the two locations ONE KEY, not to make the reader more forgiving. · **`[#509]` is adjacent**: it owns `Invoke-Dispatch` resolving `CLAUDE_PROMPTS_DIR`, which is the READER's half of this same seam, where this row owns writer and reader agreeing by construction. They should be read together and may merge once APR-2's root declaration exists. · **AX12-1 CARRIED INTO THIS ROW by lane `lane-x-716-dispatch-defects`, 2026-09-11 (the AW-2 / `[#680]` pattern -- a clause addressed to a row does not stay SAID).** **AX9-4 (exists-before-build) is DISCHARGED BY REUSE rather than by a quote excusing a build: this lane creates NO organ, script, hook or doc.** `graph_queries.py process-list --render` was run live on 2026-09-11 and returned `scripts/gen_lane_contract.py` (triggered by `.pre-commit-config.yaml`), `scripts/gen_handoff.py` (triggered by `scripts/assemble_paste.py`) and `scripts/worktree_seed.py` (no trigger; ON-DEMAND-BY-OPERATOR, invoked by /lane-boot) -- three existing processes that already answer all three needs, so every fix below lands INSIDE one of them. **AX4-1 (floor declaration) is reported, not filed:** no `tasks/*.md` row in the tree carries a `floor:` key today (measured: zero of the open rows), `ecosystem/parity-surfaces.yaml` carries no lane-contract or dispatch-defect surface, and AX4-1 names the `decision_coverage` lane (X1-1) as its owner and the parity registry as its carrier. Adding an unvalidated frontmatter key ahead of its carrier would put this row outside the ADR-66 schema `validate-backlog` gates, so this lane declares its floor in PROSE here -- **floor: MUST**, because all three defects sit in the deployed methodology corpus's own generator and every consumer that dispatches a lane inherits them -- and reports the unfiled key to X1-1 rather than editing a registry it does not own. **AX7-5 stands until this row's sibling `[#717]` is green:** `-Model` explicit at the dispatch act, cheapest-admitted = sonnet. · **ONE KEY, and the key already exists in the tree.** `gen_handoff.transport_root()` resolves `CLAUDE_PROMPTS_DIR` with the documented `~/Downloads` fallback and returns None on UNRESOLVED -- the reader's own resolution rule, already written, already tested. `gen_lane_contract._default_out_dir()` returned `Path.cwd()`, a second literal, and that is the whole split. The fix imports the existing resolver BY NAME rather than adding a third: the writer's default and the verb's read become the same function call. **The refusal survives the fix, as this row asks:** an UNRESOLVED prompts directory is a refusal at emit time, not a silent fall back to cwd -- falling back would restore the split invisibly, which is worse than the six refusals that surfaced it. Witness in `tests/test_gen_lane_contract.py`. · Done when: the generator writes lane contracts to the location the verb reads, both resolved from ONE key rather than from two independently-correct literals, and a test asserts the written path equals the path the verb resolves -- a RED-first witness that FAILS against today's split before the fix makes it green · **Done-when carried from AX7-3's clause** ("the generator writes lane contracts to the location the verb reads -- one key") · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, Part 5 AX7-3), `to-cc/AMEND-BATCH-X-ROSTER-007.md` AX7-3, `to-cc/QUESTION-path-registry-2026-09-11.md` (fix 1 of the three), `scripts/gen_lane_contract.py`, `[#509]` (`Invoke-Dispatch` resolves `CLAUDE_PROMPTS_DIR` -- the reader's half), `[#715]` (the path row these three ship first for, and where the general form lives), `[#716]` and `[#717]` (the other two defects), `[#592]` (the dispatch-drift organ over every literal command) · kill-candidates: none -- `[#509]` is named above as the READER's half of the same seam and stays open on its own merits; `[#592]` owns detecting drift between documented and live commands rather than the writer/reader split · source: `to-cc/AMEND-BATCH-X-ROSTER-007.md` AX7-3, id assigned by the operator instruction of 2026-09-11, filed by lane `lane-x-000-batch-x-roster-lands` · CLOSED 2026-09-12 — operator closure word, `to-cc/AMEND-BATCH-X-ROSTER-024.md` AX24-5. Evidence per `[#730]`. **Done-when:** *"the generator writes lane contracts to the location the verb reads, both resolved from ONE key rather than from two independently-correct literals, and a test asserts the written path equals the path the verb resolves — a RED-first witness that FAILS against today's split before the fix makes it green"*. **Merged:** `eccb5814` (lane-5, fix commit `63e1d8de`). **Witness:** `tests/test_gen_lane_contract.py::test_emit_writes_the_contract_where_the_dispatch_line_it_prints_will_be_read` is the one-key assertion; `test_the_generators_default_out_dir_IS_the_root_the_verb_reads` and `test_an_UNRESOLVED_prompts_directory_is_a_REFUSAL_and_not_a_silent_fall_back_to_cwd` close the two ways the split could return.
