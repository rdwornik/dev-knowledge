---
id: "[#690]"
title: "The three SUPERSEDED `block-onedrive` hook copies under `~/.claude/hooks/` — removed, and the removal recorded in-repo"
status: open
priority: P2
size: S
theme: "[E6] Cross-repo universalization"
story: "[S16] Manage the methodology as a living thing (essence lifecycle: transfer · sync · PRUNE)"
generates: BACKLOG.md
---

- [#690] [P2][S] **The three SUPERSEDED `block-onedrive` hook copies under `~/.claude/hooks/` — removed, and the removal recorded in-repo** - Batch W item W-5, reduced to its unambiguous residue by `to-cc/AMEND-BATCH-W-001.md` AW-3 and executed as a **primary-session act**, not a worktree lane, because its subject is outside the repo and is operator global config (`to-cc/BATCH-2026-09-10-W-CONTRACTS.md` §W-5). · **This row exists because the deletion leaves no other trace.** The three files live in no git tree, so `main` can never witness their absence directly; AW-3's fix is that the row IS the in-repo record, which is why W-5 files one and why the record is bytes rather than prose. · **Core-invariant #6 was satisfied by an explicit naming, not by implication.** `~/.claude/` edits are exception-with-ruling and never unilateral, and W-5's contract makes the form of the ruling checkable: *"`GO delete` must name them explicitly — a GO for 'the deletions' does not reach L0 by implication."* The dispatcher REFUSED a first `GO delete` that did not name the paths, and executed only after the operator named all three and separately confirmed the live guard stays. · **Removed, with bytes, witnessed by a listing on both sides:** `block-onedrive.SUPERSEDED.ps1` (1,297 B) · `block-onedrive.SUPERSEDED-2026-07-07-allowlist-v1.ps1` (3,322 B) · `block-onedrive.SUPERSEDED-2026-07-08-emptygrant-v2.ps1` (5,432 B). **M6 bytes removed: 10,051.** · **The live `block-onedrive.ps1` was NOT touched** — present after, 9,016 B, byte count unchanged from before; the exclusion-zone guard stays armed. `surface-closures.ps1` (1,434 B) untouched. SUPERSEDED copies remaining in the directory: **0**. · **The guard proved itself mid-act, which is worth recording:** the first deletion command was REFUSED by the live `block-onedrive.ps1` because the command TEXT mentioned the exclusion zone and used a construct outside its read-only allowlist. The guard fired on a command that was not touching the zone at all — the hooks directory resolves to `C:\Users\1028120\.claude\hooks`, which is not in the zone — so the refusal was conservative rather than correct, and the act completed once the command was rewritten without the token. · **Two clauses were dropped by AW-3 and are NOT part of this row**, recorded so they are not silently re-added: `scripts/setup-fleet-scheduler.ps1` is **HELD, not deleted** — open `[#493]` carries it in its `refs` and deleting it destroys evidence for a diagnosis nobody has performed; and the husk-dir clause was already discharged by the 2026-09-10 (e) window close. · Done when: each of the three named paths absent from `~/.claude/hooks/`, witnessed by a listing before and after · this row on `main` recording the three paths and their bytes · ship-gate WARN count not higher · M6 bytes reported · refs `to-cc/BATCH-2026-09-10-W-CONTRACTS.md` §W-5, `to-cc/AMEND-BATCH-W-001.md` AW-3 (the reduction) and AW-2 (file-your-own-row), `docs/audits/2026-09-10-technical-batch-w-manifest.md`, `[#493]` (holds `setup-fleet-scheduler.ps1`), core-invariant #6 · kill-candidates: none — no open row names these three SUPERSEDED copies; `[#493]` names the scheduler script this row deliberately does not touch · source: batch W, W-5, executed 2026-09-11 by the primary session on the operator's `GO delete` naming all three paths
