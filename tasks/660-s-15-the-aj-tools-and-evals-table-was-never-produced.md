---
id: "[#660]"
title: "S-15 — the AJ tools-and-evals table is the one deliverable of that arc still owed"
status: open
priority: P3
size: M
theme: "[E7] Tooling & evaluation"
story: "[S19] Decide the undecided artifact/tool models"
generates: BACKLOG.md
---

- [#660] [P3][M] **S-15 — the AJ tools-and-evals table is the one deliverable of that arc still owed** — the outgoing browser seat's own accounting marks it NEVER WRITTEN and calls that the finding. The corpus was catalogued at claim level, but the tool-by-tool table — for each named tool: do we have it, do we use it, where, and does it cut tokens — was never produced, and the lane that would have produced it was never dispatched because local memory was critical at the time. Intake `#88` exists as a DRAFT and is marked local-only because the source corpus sits on the operator's disk rather than in any repo, which is the reason the lane keeps being deferrable · Done when: the table exists as a repo artifact with one row per named tool and a have/use/where/token-effect verdict in each, and every verdict cites the corpus location it was read from · refs REVIEW-followup §1 *Toola*, the AJ tools-and-evals intake (`#88`), `[#642]` · source: REVIEW-followup §1, filed by batch V lane V-4
