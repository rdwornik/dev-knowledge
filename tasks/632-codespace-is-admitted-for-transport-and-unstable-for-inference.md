---
id: "[#632]"
title: "Codespace is ADMITTED for transport and UNSTABLE for inference — the six-layer plan, the runner hardening, and the two defects they depend on"
status: open
priority: P1
size: L
theme: "[E7] Tooling & evaluation"
story: "[S18] Cut session friction with better tooling"
generates: BACKLOG.md
---

- [#632] [P1][L] **Codespace is ADMITTED for transport and UNSTABLE for inference — the six-layer plan, the runner hardening, and the two defects they depend on** — **Operator ruling 2026-09-01, after three committing-lane attempts produced ZERO committed work.** The honest status is two facts, not one: **ADMITTED for transport + gates** (3-of-3 on 2026-08-31 — L2 `ADMISSION-PROBE-OK`, `FETCH_EXIT=0`/`PUSH_EXIT=0`, `audit.py health` exit 0 **in-container**), and **INFERENCE PATH UNSTABLE** (one green run, one 75-minute hang). **THE SIX LAYERS, each measured and each its own fix:** (1) **auth** — "Not logged in" in 47 ms, closed by the F4 workspace-trust leg; (2) **shell matrix** — `claude` lives at `~/.local/bin` which reaches PATH only through the LOGIN profile, so a non-login `bash <path>` reported it absent while it was installed (`win-tooling ab6f087`); (3) **provision race** — a codespace reports Available while `postCreateCommand` runs, and persisting PATH is one of the stamped legs, so the runner now WAITS on `~/.dev-knowledge-provision-stamp`, bounded (`win-tooling 2101ee0`); (4) **guard conflation** — exit 91 said "claude is not installed" for three different worlds and sent two dispatches to the wrong fix, now one message per state with `provisioned`/`waited_s` in the receipt; (5) **create-vs-rebuild** — rebuild does not re-apply `devcontainer.json`, create does, so every container is CREATED; (6) **INFERENCE HANG, OPEN** — attempt 3 ran 1h15m with **3 seconds of CPU**, `wchan=ep_poll`, no children, no file written, receipt 0 bytes, after successfully fetching remote-settings 9s in. A bounded probe on a fresh codespace then returned **both** models green (`sonnet` api 2036 ms, `claude-opus-5` api 1871 ms, `is_error:false`), so the hang is **transient rather than structural** — and the probe was one trivial turn, which proves the auth/egress/model path and proves nothing about a long agentic run. · Done when: the runner writes a **real streaming log** (`--output-format stream-json` to a file, so a watcher has a subject — today `-p` emits one blob at the end and `~/.claude/sessions/` is empty, so there is literally nothing to tail); a **HARD INFERENCE TIMEOUT** bounds the call so a hang costs minutes and never 75; `--detach` lands so dispatch never blocks a shell; the **pre-create ambiguity probe** refuses with nothing created and deletes its own creation on a post-create refusal; and the **13 pre-existing cp/scp passthrough-ordering failures** in `tests/test_dispatch_helpers_codespace.py` are fixed FIRST, because the probe's own tests die in that same plumbing (every attempt stalls at *"no receipt came back"* — the fixture writes its receipt on `$a[2] -like 'remote:*'`, which the passthrough ordering moves) · **Sequence is load-bearing:** the 13 → the probe → the runner hardening. Reversing it was tried on 2026-09-01 and took the file from 13 failures to 22 without converging · **Next batch's first codespace lane runs WITH the timeout** — that is the operator's condition, not an option · refs `docs/audits/2026-08-31-verification-codespace-admission-probe.md`, `docs/audits/2026-09-01-technical-batch-f-manifest.md` (amendment 3, the attempt-3 receipt), `win-tooling` `ab6f087` + `2101ee0`, `[#554]`, `[#631]` · kill-candidates: none — `[#554]` owns the devcontainer/provisioning STAGE 1 and is amended rather than closed; this row owns what stage 1 did not reach, which is everything above the transport · source: operator ruling 2026-09-01, after attempt 3
