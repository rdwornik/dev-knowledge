---
id: "[#733]"
title: "Ratification #5: the operator never types a path and the browser never composes a boot line -- the SEAT-BOOT renders go to the transport"
status: open
priority: P1
size: S
theme: "[E1] Handoff continuity"
story: "[S1] Match the handoff payload to the work mode"
generates: BACKLOG.md
---

- [#733] [P1][S] **Ratification #5: the operator never types a path and the browser never composes a boot line -- the SEAT-BOOT renders go to the transport** - AX11-2 of `to-cc/AMEND-BATCH-X-ROSTER-011.md`, carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93) Part 11. · **The act: CC copies the CURRENT bundle's `SEAT-BOOT-*.md` renders into the transport's `to-browser/`, replacing the stale batch-V copies sitting there now.** A new seat is then booted by pasting that file's content, and the browser names only the FILE. · **Two prohibitions, and they are the row rather than decoration:** the operator never types a path, and the browser never composes a boot line. Both are ratification #5, and both are about the same failure -- a boot line assembled by whoever is holding the conversation is a boot line nobody validated. Moving the render to a known location converts composing into naming, and naming is checkable. · **STALE COPIES ARE THE LIVE HARM, not the absent mechanism.** `to-browser/` currently holds batch-V renders. A stale boot file does not fail -- it boots a seat into the wrong window, and the seat cannot tell, which is exactly the class `[#682]` names: a correct paste delivered to the wrong seat is indistinguishable from a correct act. Replacement, not addition, is therefore part of the Done-when. · **Not a duplicate of `[#698]`, and the distinction matters at grooming.** `[#698]` is a CARDINALITY defect inside the SEAT-BOOT-integrator render -- it emits a GO line per merge where the canon it implements grants ONE GO for the batch. This row does not touch what the render says; it is about WHERE the render lands and which copy is current. Same artifact, two independent failures, and fixing either leaves the other. · Done when: CC copies the active bundle's `SEAT-BOOT-*.md` renders to the transport's `to-browser/` as part of the handoff act rather than on request; the batch-V copies are REPLACED rather than accumulated beside the new ones; a seat is booted by pasting the named file's content, with the browser naming only the filename and composing no boot line; and the operator types no path at any point in the sequence · **Done-when authored by the filing lane** from AX11-2 · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, Part 11 AX11-2), `to-cc/AMEND-BATCH-X-ROSTER-011.md` AX11-2, `to-browser/RATIFICATION-2026-09-10.md` (ratification #5, the source), `[#698]` (the render's GO-per-merge cardinality defect -- same artifact, different failure), `[#682]` (a paste delivered to the wrong seat is indistinguishable from a correct act -- the harm a stale copy causes), `[#680]` (the handoff generator emitting files no open row claims) · kill-candidates: none -- `[#698]` owns the render's CONTENT and this row owns its DELIVERY and currency; neither fix produces the other, and both should be read together at grooming · source: `to-cc/AMEND-BATCH-X-ROSTER-011.md` AX11-2, filed by lane `lane-x-000-batch-x-roster-lands`
