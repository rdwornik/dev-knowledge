---
id: "[#705]"
title: "Nothing renders the task flow, so \"how is the batch going\" is answered by reading rows"
status: open
priority: P2
size: M
theme: "[E7] Tooling & evaluation"
story: "[S18] Cut session friction with better tooling"
generates: BACKLOG.md
---

- [#705] [P2][M] **Nothing renders the task flow, so "how is the batch going" is answered by reading rows** - AX6-3 of `to-cc/AMEND-BATCH-X-ROSTER-006.md`, carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93) Part 4, and one of the three rows the operator's 2026-09-11 instruction named when it added AMEND-006 to this lane's source set. · **The four charts AX6-3 names are all flow measures, not inventory measures**: open/closed per week, lanes per batch, merge minutes, cost per lane. Each answers a question the operator currently answers by reading a backlog of several hundred rows, which is the friction the row exists to remove -- and each is a number the batch protocol already produces and then discards. · **Library-first is named IN the amendment, so it is an acceptance clause rather than a habit.** AX6-3 says Maister's dashboard plugin is studied for SHAPE and not copied. The library-first order binds: stdlib, then an established dependency, then a stabilized project pattern, then an industry pattern -- hand-rolling only on a measured divergence recorded so it is not relitigated. · **LOCATOR CORRECTION, resolved by this lane rather than carried forward as written.** AX6-3 names "the existing chaotic `dashboard/conformance.html`". There is no `dashboard/` directory in this repo; the file is **`ecosystem/conformance.html`**. The amendment's instruction is unambiguous and only its path was wrong, so the row carries the corrected locator -- a locator not opened is a claim rather than evidence, and this one was opened. · **MIGRATE-not-delete governs the old surface**: intake #42's rule means `ecosystem/conformance.html` is folded into the new surface or deleted, not left beside it. Two dashboards is the outcome this clause exists to prevent, because the stale one keeps answering questions. · **DEPENDENCY, and it is currently dangling.** AX6-3 says the charts render "from the same telemetry the tally feeds (AX5-1)" -- and AX5-1 lives in `AMEND-BATCH-X-ROSTER-005`, which this intake does NOT carry (Part 6). The telemetry source named here is therefore `[#694]`, the roster's own trigger-or-remove row, which IS carried and IS filed: there is no telemetry to chart until the four modules are wired or deleted. The AX5-1 reference stays owed. · Done when: the four flow charts AX6-3 names render from the telemetry surface that survives `[#694]`; the charting choice passes a library-first check recorded against this repo, or carries a one-line reason for its absence; and `ecosystem/conformance.html` is folded in or deleted under intake #42's MIGRATE-not-delete rule rather than left beside the new surface · **Done-when authored by the filing lane** from AX6-3's clauses; AMEND-006 states them as a row description rather than a Done-when · **Blocked on** `[#694]` · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, Part 4 AX6-3 and Part 6's dangling-reference note), `to-cc/AMEND-BATCH-X-ROSTER-006.md` AX6-3, `ecosystem/conformance.html` (the corrected locator), `[#694]` (the telemetry this renders from), `[#685]`, `[#576]`, `[#581]` (backlog vitals -- three flow instruments), `[#322]` (the fleet dashboard) · kill-candidates: none -- `[#581]` owns three backlog FLOW INSTRUMENTS as committed numbers rather than a rendered surface, and `[#322]` owns a FLEET dashboard across repos rather than this repo's task flow; both should be read beside this row at grooming · **Archived annotations:** `tasks/archive/705.md`
