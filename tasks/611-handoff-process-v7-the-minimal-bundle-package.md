---
id: "[#611]"
title: "HANDOFF_PROCESS v7: the minimal-bundle package"
status: open
priority: P2
size: M
theme: "[E1] Handoff continuity"
story: "[S1] Match the handoff payload to the work mode"
generates: BACKLOG.md
---

- [#611] [P2][M] **HANDOFF_PROCESS v7: the minimal-bundle package** (operator-ruled birth 2026-08-28, funded by this lane's closure sweep; the v7 RESERVATION recorded in the v6.3.0 section history becomes a PLAN here rather than a loose reservation) — v6.3.0 retired the resident role file from the per-seat paste by replacing the inlined `protocols/HANDOFF_BOOT.md` with a 3-line role PIN (version + sha256 + standing refusal line), on the census R1 mechanism. v7 finishes the package: the **remaining census deltas** land, the paste hits a measured size target on a real cut, and the spec bumps as one act with every version-bearing surface reconciled by the Amendment-3 method (grep-and-classify LIVE-NORMATIVE / STRUCTURAL-LEGAL / HISTORICAL-IMMUTABLE — never a trusted enumerated list). · Done when: the remaining census deltas land — **b4 probes-pin** (the paste ships version-pinned VARIABLE rows only) plus any D-item still open after the v6.3.0 act; an assembled paste from a **real cut** measures **<=20KB at >=70% window-specific** content; and `HANDOFF_PROCESS` is at **v7.0.0** with every version-bearing surface reconciled and proven by a re-run of `reconciled_versions`, `silent_rule_ratchet` and `verify_handoff_probes` · refs docs/intake/2026-08-27-tech-night-batch-protocol.md (I-NIGHT), the census R1 artifact, protocols/HANDOFF_PROCESS.md section history (the v7 reservation), #511, #599
