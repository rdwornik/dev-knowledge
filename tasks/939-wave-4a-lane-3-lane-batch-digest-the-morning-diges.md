---
id: "[#939]"
title: "Wave 4a lane 3 (lane-batch-digest) -- the morning digest names the work, written by the machine"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#939] [P1][M] **Wave 4a lane 3 (lane-batch-digest) -- the morning digest names the work, written by the machine** - filed by LANE-W4-1 per `to-cc/DECLARE-WAVE4A-2026-09-22.md` hard precondition 3 (rows for lanes 1-5 exist before lanes 2-5 fire; R-W4-3); frozen contract `LANE-W4-3-batch-digest.md`, branch `worktree-lane-batch-digest`, a wave-4a lane closing the loop `[#929]` evaluated. Value: The operator reads one digest, not merges. Last night the batch-close moment exited 0 with an empty digest ("Did: no commits recorded"), so the integrator wrote it by hand — the loop's second stop. The cause: the digest takes its "Did" from `git log main..HEAD` after the merge, when that range is empty by construction. The lanes already report themselves at lane-end, from inside their worktrees, where the commits are visible. · Done when: carried verbatim from the frozen contract -- 1. **The lane-end report carries the lane's work:** `transport_report.py` writes commit subjects, changed files and the lane's verdict into `LANE-END-<lane>.md`, computed in the worktree at lane-end (where `main..HEAD` is right). 2. **The digest reads reports, not git (R-W4-3):** `lane_digest.py` composes the batch digest from every `LANE-END-<lane>.md` for the batch plus the integrator's receipt: per lane, what it did, merge sha, verdict, cost if recorded, what it left open; a lane with no report is named as missing, not silently dropped. 3. **Plain language:** no identifiers the operator does not use; the batch's one-sentence answer first. 4. **Live result:** run `doit -f scripts/dodo.py moment:batch-close` with `HARNESS_BATCH=WAVE3` against the WAVE3 reports on the transport (read-only) and paste the digest it produces into your handback; it must name the six wave-3 lanes. 5. **Tests (common rules):** synthetic reports -> a digest naming every lane and every open item; a missing report is named; a report with no commits says so; transport isolation proven. 6. **Codex terra review** with its consumer cited; **pre-handback self-check**; **handback**. · refs `LANE-W4-3-batch-digest.md`, `to-cc/WAVE4-COMMON-2026-09-22.md`, `to-cc/DECLARE-WAVE4A-2026-09-22.md` · kill-candidates: none -- no open row covers this lane's work
