---
id: "[#789]"
title: "The seat boot rendered its declared model split into an HTML COMMENT while the launch line was a bare `claude` — batch Z ran 100% Opus under a header claiming otherwise"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#789] [P1][M] **The seat boot rendered its declared model split into an HTML COMMENT while the launch line was a bare `claude` — batch Z ran 100% Opus under a header claiming otherwise** — filed from `DIGEST-2026-09-15-unrecorded-findings.md` §5. **`SEAT_MODELS` had carried the integrator split since 2026-09-11.** It rendered into an **HTML comment** in the boot paste, while the boot's actual launch line was a bare `claude` with no `--model`. So the declaration was present, readable, dated, and reached nothing. **Batch Z ran 100% Opus under a header claiming a split.** · **THIS IS DECLARED ENFORCEMENT WITHOUT ENFORCEMENT, in the mechanism this seat repeatedly cited as done** — the most expensive shape a gap can take, because the declaration's existence is what stops anyone checking. It is the same class as a stale generated index: the artifact still renders, so nobody notices it stopped meaning anything. · **`[#717]` DOES NOT COVER THIS.** `[#717]` closed the **lane-contract generator's** launch line, where `--model` was omitted so a sonnet contract dispatched at opus. **The SEAT boot's line was never in its scope**, and the two surfaces were never reconciled — so closing `[#717]` created the appearance of coverage across a surface it never touched. Read the two rows together before either is called done. · **The fix is structural, not a template edit.** A value that renders into a comment is a value the renderer had no slot for; adding the flag to the template fixes this instance and leaves the class. What is wanted is that a declared model which does not reach an executable launch line is a **REFUSAL at render time** — the generator cannot emit a boot whose declaration it did not honour. · **Coupling:** lane aa-12 (ENFORCED ROUTING) owns ordered-model-equals-ran-model at the RECEIPT, and `[#787]` owns the routing UNIT. This row owns the **seat-boot render surface**. All three are needed: a correct unit, declared on a surface that emits it, verified against what ran. · Done when: the seat boot's declared model reaches an executable launch line or the render REFUSES; a boot whose declaration does not reach the line is witnessed being refused by a test that failed before the fix; `[#717]`'s scope boundary is recorded in both rows so the gap cannot be re-created by closing one; and a batch's receipts can show the model split that its headers claim · refs `scripts/gen_seat_boot.py`, `protocols/PLAYBOOK.md` Ch8, `DIGEST-2026-09-15-unrecorded-findings.md` §5, `[#717]`, `[#787]`, `[#788]` · kill-candidates: none — `[#717]` is a different surface and is closed on it; closing this row does not discharge it, and the two must be read together · source: operator direction, dispatcher seat, 2026-09-15
