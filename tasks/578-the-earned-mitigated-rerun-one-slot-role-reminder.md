---
id: "[#578]"
title: "The earned mitigated rerun — one slot, role-reminder preamble baked in"
status: open
priority: P3
size: S
theme: "[E7] Tooling & evaluation"
story: "[S19] Decide the undecided artifact/tool models"
generates: BACKLOG.md
---

- [#578] [P3][S] **The earned mitigated rerun — one slot, role-reminder preamble baked in** — **This row exists because a closed row cannot carry an obligation.** The 2026-08-23 admission verdict (architect) **REFUSED both candidates** — `gemini-3.7-flash` on the G1 floor, `grok-4.6` on G1 and G2 — and closed `[#562]`; but the same verdict measured a mitigation and earned exactly **ONE** rerun on R3's own promptable-failure clause. The measurement is the whole basis: the `C1-N3` control is `C1-N1`'s prompt bytes **plus one sentence of role reminder**, it **PASSED on both candidates** where `C1-N1` failed on both, and the single variable was the reminder. So the reminder belongs in the **dispatch text**, baked into every item as a standing preamble — and **a mitigation is not a gate discharge**: admission stays REFUSED until this rerun clears the floor. **The second half of this row is the OWED measurement, and it is the more valuable one:** whether the **incumbent's** own refusal failure is promptable is still unmeasured — the 2026-08-23 run could not make it (Anthropic path HTTP 400, credit balance) — and until it exists, **no ruling touching the fan-out pin should be made from this evidence**. The carried `P_i`/`Φ_i`/`R_i` are a **stale baseline**: unguarded head `d2dc2fa6`, effort tier high, 2026-08-20. **Blocked-on, and this is a real gate not a note:** the instrument question filed as intake 41 (`docs/intake/2026-08-23-tech-nopack-guard-refusal-surface.md`) should be triaged first — a guard whose refusal surface still reaches a scored item makes the rerun measure the instrument, and the slot is one, not many. Effort tier medium per Q8; admission arithmetic `G1 AND G2 AND G3` per Q9, unchanged. · Done when: all 14 pack items plus `C1-N3` are re-run on both candidates with the role-reminder preamble present in every item's dispatch text, the incumbent's `C1-N3` is either measured on the Anthropic path or its absence is recorded as transport evidence rather than as a judgement, per-lane refusal counts are reported so the guard's contribution is a number, the three gates are computed, and the artifact asserts **no verdict** · refs docs/audits/2026-08-23-technical-lane-562-local-admission.md, docs/audits/2026-08-22-technical-annotation-and-rulings-ledger.md §5, docs/audits/2026-08-23-technical-wave-close-funnel.md, docs/intake/2026-08-23-tech-nopack-guard-refusal-surface.md, protocols/STANDING_RULINGS.md Q7/Q8/Q9, #492, #491 · kill-candidates: none — `[#562]` closed on 2026-08-23 and a closed row carries nothing; `[#492]` owns the Grok **review-lane** acceptance question rather than the admission rerun, and `[#491]` is deferred on an unrelated post-ratchet policy
