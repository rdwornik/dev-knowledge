---
id: "[#793]"
title: "The model a contract declares is the model that RUNS — resolved at freeze, verified off the transcript, refused on the receipt, with the default by lane kind in the generator"
status: open
priority: P1
size: M
theme: "[E7] Tooling & evaluation"
story: "[S19] Decide the undecided artifact/tool models"
serialize-group: architecture
generates: BACKLOG.md
---

- [#793] [P1][M] **The model a contract declares is the model that RUNS — resolved at freeze, verified off the transcript, refused on the receipt, with the default by lane kind in the generator** - batch aa lane 12, frozen at `LANE-aa-12-enforced-routing.md` in the operator's prompts directory. **The defect is witnessed and it is the acceptance target: a lane ordered `opusplan` RAN `sonnet-5`.** Every cost instruction this repo gives is PROSE, and until this row lands every routing ruling here is a suggestion a generator, a launcher or a seat can silently ignore — and one already did. · **CLAUSE 1, LEG RESOLVE — the new half.** A declared tier the background launcher cannot honour is refused AT FREEZE rather than passed through to evaporate at dispatch. `scripts/gen_lane_contract.py::routing_refusals` resolves the triple (model, kind, shape) and `LaneSpec.validated()` raises on it, so every path that produces a contract goes through the refusal rather than only the CLI. **The recorded resolution for `opusplan`: it resolves to ATTENDED SHAPES ONLY.** It is a split tier that is Opus only in plan mode, and every unattended dispatch carries `--permission-mode bypassPermissions`, which never enters one — so it is refused on `local`, `cloud` and `codespace` and kept on `interactive`, where AX22-3 routes the integrator seat. The MEASUREMENT is on `local` (`lane-x-689`, 84 of 84 assistant messages on `claude-sonnet-5`); the extension to the other two unattended shapes is by MECHANISM and is named as such in the code rather than smuggled. The inert-tier table is READ from `scripts/dispatch_surface.py::BACKGROUND_INERT_MODELS`, never restated — a second copy of a launch vocabulary is the defect the dispatch register's section V was ruled on. · **CLAUSE 1, LEGS VERIFY AND REFUSE — already landed by `[#752]`, and the hole they left is closed here.** `scripts/routing_agreement.py::model_reading` reads the ran model off the lane's own transcript and `scripts/merge_receipt.py`'s fourth incompleteness leg refuses a receipt whose `ran_model` differs from its `ordered_model`, naming both. The ORDERED side was still circular: `--ordered <tier>` took the seat's word for what had been asked, so a receipt could agree with itself from that side instead. `scripts/merge_receipt.py::ordered_model_from_contract` reads it off the FROZEN CONTRACT's routing row — written by the architect at freeze, before any session exists — while the transcript is written by the CLI at run time. **Neither surface can be produced from the other, which is what makes a divergence visible at all**, and the docstring says which side reads which and why they cannot collapse. · **CLAUSE 2 — the default by lane kind, IN THE GENERATOR.** A default that lives in a boot paste is a default a seat can not-read, so it lives where contracts are MADE. The kind is a DECLARED closed enum `{text | code | review}` with no default and no inference: an inferred kind that is wrong produces a refusal the author cannot act on, because they declared nothing to correct. `text` (text-only, deletion, read-only digest) never starts on Opus; `review` is refused on every shape this generator emits, because `ecosystem/routing-table.yaml` is the AUTHORITY for role -> CLI (Z-G3 A2) and routes `reviewer` to `codex` — read from that file, not restated, and `provider-registry.yaml`'s `roles:` RANKING is deliberately not consulted. `code` is free on either half of the plan/implement split and the emitted contract says in words that the split is **two sessions with a file between them**: the prompt cache is keyed per model, so one mid-session switch costs about 25 consecutive cheap turns to repay at this repo's measured context. · **THE TEXT-LANE REFUSAL SITS ON A LIVE RULE-VS-RULING CONFLICT AND REPORTS IT RATHER THAN RESOLVING IT.** `protocols/PLAYBOOK.md` Ch8 "Model + effort are stated at dispatch" makes `opus` the default for any arc touching `.dev-knowledge`, amended 2026-08-07 to key on CONTEXT LOAD rather than diff size; clause 2 narrows that for text-only lanes. A hub text-only lane is exactly where they disagree — escalation class (b). The refusal text names the conflict every time it fires, so the disagreement is visible at the point of use rather than settled silently in a table nobody re-reads. **This row does not adjudicate it; it needs an operator ruling.** · **CLAUSE 3 — a rate per provider, and the ordering fact that inverts the obvious plan.** `scripts/provider_bench.py` computes `comparable = my_usd and base_usd and not my_gap and not base_gap`, where `base_gap` is the BASELINE's own partial-pricing flag — so while the claude baseline was partial, `usd_comparable` read false on EVERY row of `logs/PROVIDER-VERDICTS.json` unconditionally and no other provider could be fixed first. The baseline was partial because `claude-haiku-4-5-20251001`, a bookkeeping side-call, was undeclared; `ecosystem/provider-registry.yaml` now carries it at USD 1.00/5.00 per million, keyed on the CLI-EMITTED string because `provider_registry.resolve_rate` looks the attested id up raw and the registry has no alias field. **A SECOND GAP SURFACED THAT THE CONTRACT DID NOT ANTICIPATE AND IT IS RECORDED RATHER THAN ESTIMATED:** the ledger never persisted the per-model token SPLIT, only the answering model's counts plus a prose reason naming the remainder's TOTAL, so the ten baseline rows of the 2026-09-15 sweep still cannot be completed — 959 tokens is anywhere between USD 0.000959 and USD 0.004795 at haiku rates, and that number is not invented here. `record()` now persists `model_usage` and `reprice_row` re-prices any row carrying it against today's card, so the NEXT card edit moves every derived figure, which is what the registry already claims of itself. A legacy row is returned untouched: re-pricing it from the answering model's counts alone would drop the remainder and clear a partial flag that is still true. · **The two rows that cannot be flipped by a YAML edit, stated rather than forced:** `ollama` is genuinely USD 0.00 and `ModelRates._a_rate_is_positive` REFUSES 0.0, so the one true zero cannot be expressed and needs a ruling; `copilot` meters in PREMIUM REQUESTS while `RateCard.unit` is `Literal["per_million_tokens"]`. And a correction carried so nobody files a false defect: pricing haiku does NOT reconcile this repo's USD 0.217038 with the vendor's USD 0.2915 — the side-call explains 1.3–6.4% of the USD 0.0745 gap and the residual is UNATTRIBUTED. · **RED-FIRST, WITNESSED BY A RECORDED RUN RATHER THAN A LANDED RED COMMIT, AND THE DIFFERENCE IS STRUCTURAL.** `graph-orphan-census` and `graph-task-coverage` both refuse a staged `scripts/*.py` with no wiring surface and no claiming open row, so a standalone RED commit cannot land in this repository; the RED run (21 failed, 5 passed) is captured verbatim in the packet before any implementation existed · Done when: a deliberately mis-declared contract is REFUSED end-to-end by the freeze gate with the reason, not by a unit test alone; a receipt whose ran-model differs from its ordered-model is REFUSED with both values named and the ordered side read off a surface the launcher did not write; the generator refuses an Opus declaration on a declared text-only lane at freeze; and the claude baseline is priced with the reason recorded for every provider whose `usd_comparable` still cannot flip · **NOT CLOSED BY THIS ROW:** the contract's fifth done-when — "the next batch's close packet reports cost split across at least two models" — is measured by the NEXT batch. This row delivers the enforcement plus the instrument and says plainly that it is not closing that half · refs `scripts/gen_lane_contract.py` (`KIND_ENUM`, `validate_kind`, `routing_refusals`, `background_inert_models`, `reviewer_cli`, `unattended_shapes`), `scripts/merge_receipt.py` (`ordered_model_from_contract`, the `--contract` flag on the `model` verb), `scripts/provider_bench.py` (`reprice_row`, `reprice`, the persisted `model_usage`), `scripts/dispatch_conformance.py` (the probe writer declares a kind), `tests/test_enforced_routing.py` (the RED-first witnesses), `ecosystem/provider-registry.yaml` (the haiku row), `ecosystem/routing-table.yaml` (READ as the role -> CLI authority, never written), `docs/audits/2026-09-15-technical-lane-aa-12-enforced-routing.md` (the end-of-lane packet with the recorded RED run), `protocols/PLAYBOOK.md` Ch8 (the ruled matrix clause 2 narrows), `[#752]` (authored the VERIFY and REFUSE legs this row extends), `[#785]` (authored the bench and the ledger this row re-prices), `[#751]` (authored the rate card), `[#717]`, `[#675]`, `[#664]` · kill-candidates: none — `[#752]` is CLOSED and owned the transcript reading only, so there is no open row to supersede; `[#785]` stays open on the unattended-capability question, which this row neither runs nor discharges; `[#574]` stays open on manifest emission, a different surface of the same generator · serialize-group: architecture
