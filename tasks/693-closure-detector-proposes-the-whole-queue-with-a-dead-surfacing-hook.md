---
id: "[#693]"
title: "The closure detector proposes the whole queue, its surfacing hook has been dead since the month-bucket move, and the run cap errors -- three defects that together make a closure proposal mean nothing"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S4] Extend structural validation to more governance artifacts"
generates: BACKLOG.md
---

- [#693] [P1][M] **The closure detector proposes the whole queue, its surfacing hook has been dead since the month-bucket move, and the run cap errors -- three defects that together make a closure proposal mean nothing** - Lane X2-1 of the batch X roster, carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93) section 3, and the roster states the finding as F1: **275 proposals = the whole queue**. A detector that proposes everything discriminates nothing, and a proposal set the operator cannot triage is the same as no proposal set -- which is how four sessions of asking produced a queue nobody convened on (LEDGER D6). · **Three defects, and they are independent of each other**, which is why the roster's Done-when says RED-first EACH rather than RED-first once. (1) The detector does not discriminate: proposals equal the queue. (2) `surface-closures.ps1` has been dead since the month-bucket relocation moved the files it globs -- the surfacing half of the loop, so even a good proposal reaches no one. (3) The 99-runs-a-day cap fires as an error rather than as a bound. · **`[#277]` is adjacent and does NOT absorb this row**, and the boundary is worth stating because both rows name `propose_closures.py`. `[#277]` owns SIGNAL QUALITY -- two documented STRONG false positives and the bare-file-churn WEAK heuristic -- and its Done-when is a STRONG:WEAK ratio better than 49:0. A better ratio does imply some discrimination, so defect (1) overlaps `[#277]` leg (b). Neither defect (2) nor defect (3) is reachable from that Done-when at all: a ratio says nothing about whether the surfacing hook runs or whether the cap raises. The two rows should be read together and may merge at grooming; filing one row here would have dropped two of three defects. · Done when: detector discriminates (proposals ≠ queue), surfacing hook alive, cap error gone, RED-first each · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, section 3 lane X2-1 -- the Done-when above is the roster's own words), `scripts/propose_closures.py`, `scripts/review_closures.py`, `[#277]` (signal quality; overlaps defect 1 only), `[#487]` (the consumption arc), `[#454]` (the closure-ids negation defect), `[#626]` (the retention exemption over the same file series), `[#704]` (the one-file-per-run source fix AX1-4 pairs with this row) · kill-candidates: none -- `[#277]` is named above as adjacent-not-absorbing and stays open on its own merits; no open row names the surfacing hook or the run cap · source: `to-cc/DECLARE-BATCH-X-ROSTER-2026-09-11.md` section 3 lane X2-1, filed 2026-09-11 by lane `lane-x-000-batch-x-roster-lands`
