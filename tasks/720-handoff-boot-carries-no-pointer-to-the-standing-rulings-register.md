---
id: "[#720]"
title: "The browser role file carries no pointer to the standing-rulings register, so a booting seat never learns a ruling landed after its ROLE PIN"
status: open
priority: P3
size: S
theme: "[E1] Handoff continuity"
story: "[S1] Match the handoff payload to the work mode"
generates: BACKLOG.md
---

- [#720] [P3][S] **The browser role file carries no pointer to the standing-rulings register, so a booting seat never learns a ruling landed after its ROLE PIN** - Section C of `to-cc/DECLARE-WINDOW-RULES-2026-09-11.md`, the 2026-09-10/11 window's close acts. Filed as a row rather than executed by lane `lane-x-000-window-rules-land`, which landed the rest of that declare as `protocols/STANDING_RULINGS.md` section AH, **because the role file is outside that window's write-scope** — section C says so in its own words and the lane's frozen contract repeats it as a do-not-touch. · **What is owed is one thin line**, in the operator's own wording: *"Standing rulings since v7.1: `protocols/STANDING_RULINGS.md` §<this section>"*, where `<this section>` is **AH** (landed 2026-09-11). · **The target is `protocols/HANDOFF_BOOT.md`, and the reading is recorded here so it is not re-litigated.** Section C says "the HANDOFF_BOOT template", which admits two files, and the deciding clause is its own parenthetical *"(ROLE PIN changes)"*: the ROLE PIN is computed over `protocols/HANDOFF_BOOT.md` and nothing else (`scripts/assemble_paste.py::_role_pin` — role file name, live `handoff-process` version, `sha256`), so a pointer that changes the PIN is a pointer in that file. `templates/handoff/v5/HANDOFF_BOOT.md.tmpl` is the rejected reading: it renders a bundle's per-cut session header (slug · chat title · mode · destination), it is declared ANSWER-FREE by its own header comment, and editing it changes no PIN. · **Why a pointer and not the rulings themselves.** The role file is INSTALLED ONCE as the browser project's knowledge and is deliberately thin (`protocols/OPERATOR-INTERFACE.md` §5, v6.3.0): every assembled paste opens with the three-line PIN instead of re-transmitting ~17 KB of role text on each handoff. Inlining a register that grows one section per window is exactly the cost that ruling removed; a pointer costs three lines and stays correct as AH grows. · **The act has a second half, and it is the operator's rather than the repo's:** editing the role file changes its `sha256`, so the ROLE PIN changes, so the installed project-knowledge file must be replaced before the next boot. A committed line that no installed copy carries has changed nothing for the seat it exists to inform. · Done when: `protocols/HANDOFF_BOOT.md` carries the standing-rulings pointer naming `protocols/STANDING_RULINGS.md` and section AH, the next bundle's assembled paste emits the ROLE PIN computed over that edited file, and the operator has replaced the installed project-knowledge file with it · refs `to-cc/DECLARE-WINDOW-RULES-2026-09-11.md` section C, `protocols/STANDING_RULINGS.md` section AH, `protocols/HANDOFF_BOOT.md`, `protocols/OPERATOR-INTERFACE.md` §5, `scripts/assemble_paste.py` (`_role_pin`), `protocols/HANDOFF_PROCESS.md` §4, `[#611]` · kill-candidates: none — `[#611]` owns the handoff-process spec family and `[#680]` owns generated bundle files that no row claims; neither names the role file's content, and no open row asks the role file to reference a register · **Archived annotations:** `tasks/archive/720.md`
