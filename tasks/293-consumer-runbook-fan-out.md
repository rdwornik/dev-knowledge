---
id: "[#293]"
title: "Consumer runbook fan-out"
status: open
priority: P3
size: S
theme: "[E1] Handoff continuity"
story: "[S2] Finish the v5 handoff machinery deferred at the #149 flip"
generates: BACKLOG.md
---

- [#293] [P3][S] Consumer runbook fan-out — seed each consumer repo's `docs/handoffs/README.md` from the hub canonical source via `python scripts/seed_runbook.py --target-root <repo>` (the DEFERRED half of #164 leg b — the hub-side seeder + hermetic test shipped with #164; operator ruling deferred the fan-out because the don't-touch-consumers guardrail bars a hub write into a consumer tree and the ai-council pilot is live in parallel). Executes per-repo in the Wave-1/Wave-2 onboarding arcs (ADR-41 — consumer-side, queue-only here), same shape as [#282]. · Done when: each onboarded consumer carries the seeded runbook (per-repo tracked, n≥1 recorded) · refs scripts/seed_runbook.py, docs/handoffs/README.md, #164, #282, ADR-41 · UN-DEFERRED 2026-08-09 (ARC-2): peg "Wave-1 onboarding" met 2026-07-07; work NOT done (0 of 6 consumers seeded), so un-defer not close · **DENOMINATOR RULED at phase-1 integration (R6): 8, not 6** — ADR-104's declared non-hub members; governance outranks row prose, so read "0 of 6" above as **0 of 8**. Lane Q merged `dce393ec` and advanced this row BY ZERO by design (contract line-3 STOP: the Done-when needs consumer-repo writes nothing authorised). **Seeding stays OPERATOR-GATED, not executed** · see the phase-1 integration packet · **LANE k 2026-08-16: operator word given, seeding executed 7 of 8 and REVERTED on the ADR-60 conflict.** Per-repo worktree/branch → PR (RULING-W shape; never the live checkout) opened corp-monorepo#54, corp-ops#1, corp-sca-time-automation#1, demo-prep#1, life-architect#1, terminal-setup#1, win-tooling#1 (none merged); post-execution self-audit surfaced BACKLOG `[#303]` (open, this row's own named kill-candidate prerequisite, unresolved before dispatch) plus ADR-60 plus the 2026-07-08 census-amendment ruling, all of which forbid a child repo carrying a local `docs/handoffs/` — the exact target this row's own prose names. Architect ruling: ADR-60 wins over a task row's stated path. All 7 PRs closed (never merged), all 7 branches deleted, all 7 consumer repos verified back to clean state; seeded content preserved verbatim in the lane-k packet appendix (nothing lost, only the destination was wrong). ai-council's own `validate-docs-registry` pre-commit hook independently refused the identical write for the identical reason (STOP-and-report, untouched throughout). **Row now BLOCKED-ON-RULING:** correct consumer-side home for hub runbooks per ADR-60 — derive candidate homes from ADR-60's own text and consumer governance, quote sources, propose in the packet — never invent a path. Candidates already named in existing governance (not decided here): `[#303]`'s own (a) seed `docs/intake/` guidance instead, or (b) skip + emit a documented status — see the lane-k packet's adjudication section. `[#303]` must land first; it is this row's own prerequisite. See the lane-k packet.
