---
id: "[#606]"
title: "The win-tooling first-slice instantiation arc, run in the RULING-W shape"
status: open
priority: P2
size: L
theme: "[E6] Cross-repo universalization"
story: "[S15] Converge every child repo on the universal baseline"
serialize-group: architecture
depends-on: "#604"
generates: BACKLOG.md
---

- [#606] [P2][L] **The win-tooling first-slice instantiation arc, run in the RULING-W shape** — win-tooling is in the fleet declaration and outside the fleet's enforcement in one move: its parity role renders every MUST row skipped, and it has zero enforcing organs. The first slice takes it from unonboarded to the floor plus the pre-commit set plus a session gate plus `/ship` in **one** act, because the carriers already exist and are proven at n=1 — the missing half of wave 3 is path-portability and the canonical-doc seed, not orchestration. **Shape is not negotiable:** ADR-41 and RULING-W admit exactly one way for the hub to write into a consumer, consumer worktree or branch then report, and mechanism-before-act means the authorizing amendment lands before the write, never retroactively. **Read this row against its own freshness ceiling:** every hub-side claim about win-tooling rests on a baseline three weeks stale at filing, with 51 further baseline commits existing locally only, so the arc **re-measures first** and treats the hub's picture as a plan rather than a fact. **A precondition this row cannot satisfy itself:** the deployable target declares a `source_tag` that does not resolve on the remote and preflight hard-aborts without it — the tag is an operator act (ADR-91: CC does not tag), tracked at intake `#56` open question 1, and deliberately not a backlog row. · Done when: `audit repo win-tooling` reports the floor present, the pre-commit set armed, the parity role flipped from pre-deploy to consumer, and a fresh baseline landed in `ecosystem/win-tooling/history/`; the whole arc ran as a consumer worktree or branch then report; and the `workspace_settings` finding is recorded as hand-fixed or as accepted debt, since the `editor-config` carrier is declaration-only and cannot close it · refs docs/audits/2026-08-26-technical-w3prep-recon.md (instantiation steps 1-5, gap map), docs/intake/2026-08-26-tech-wave3-wintooling.md (intake #56), protocols/ESSENTIALS.md (RULING-W), ADR-41, ADR-91, ecosystem/parity-surfaces.yaml, #604 · source: intake #56, recon row W3-6 · depends-on: #604 · kill-candidates: none — `[#463]` is the nearest neighbour and is closed BY RULING (register T-16 accepts its four items as consumer debt the hub may surface but not close), so the debt is live while the row is not and there is no open row to kill · serialize-group: architecture
