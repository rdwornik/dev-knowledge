---
id: "[#723]"
title: "Nothing reads the logs, so a model that silently stops delivering costs a whole night before anyone notices -- a log-review routine that files its own anomalies"
status: open
priority: P1
size: M
theme: "[E7] Tooling & evaluation"
story: "[S20] Revive the nightly layer, load-gauge first (rent-rule discipline)"
generates: BACKLOG.md
---

- [#723] [P1][M] **Nothing reads the logs, so a model that silently stops delivering costs a whole night before anyone notices -- a log-review routine that files its own anomalies** - AX8-2 of `to-cc/AMEND-BATCH-X-ROSTER-008.md`, carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93) Part 8. · **Self-healing is the point, and the filing half is what makes it so.** The routine does not just report -- it READS logs and telemetry, flags anomalies, writes ONE digest and **files each anomaly as a row**. A reviewer that only reports produces another artifact nobody consumes, which is the `[#419]` failure mode; a reviewer that files puts its findings into the one queue the operator already works. · **The four anomaly classes AX8-2 names are each a failure this window actually had:** a model that does not deliver (failed or empty legs per provider -- M03 leg 1a, `[#696]`); a stuck or zero-work lane (the codespace detached lanes, `[#697]`); a cost spike (the opus-instead-of-sonnet dispatch defect, `[#717]`); and a gate firing on empty (the empty-selection hardening, `[#703]`). Every one was found by a human noticing, days later. · **RUNS AS A LANE AT EACH BATCH CLOSE UNTIL CONDUCTOR E IS LIVE**, dispatched by the integrator; scheduling arrives with E. That is the operator's correction of 2026-09-11 and it supersedes AX8-2's own "daily; every 3 days until E is live" cadence, which presumed a scheduler that does not exist -- the repo declares zero `cron:` and zero `schedule:` (`[#702]`). A batch close is a real trigger that exists today; a daily cadence is not. · **On the cheapest admitted model**, which under `[#722]`'s interim rule and AX7-5 means agy as reader with sonnet as the cheapest admitted Claude -- and the reader-only bound applies here too: the anomaly VERDICT is a conclusion, so it stays on Claude. · Done when: a log-review routine runs as a lane at each batch close, dispatched by the integrator, on the cheapest admitted model; it reads logs and telemetry and flags the four named anomaly classes; it writes ONE digest and FILES each anomaly as a row rather than only reporting it; and it gains a real schedule only when conductor E provides a runner · **Done-when authored by the filing lane** from AX8-2, with the batch-close trigger replacing the daily cadence per the operator's correction of 2026-09-11 · **Blocked on** `[#694]` (C2 telemetry) and, for the scheduled form only, `[#702]` and conductor E · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, Part 8 AX8-2 and Part 10's AX3-4 note), `to-cc/AMEND-BATCH-X-ROSTER-008.md` AX8-2, `[#694]` (the telemetry it reads), `[#702]` (the nightly layer it joins once a runner exists), `[#693]` and `[#704]` (the closure/proposals surface AX3-4 folds this with -- that fold is recorded as owed since AMEND-003 is uncarried), `[#419]` (routines whose output nobody consumes -- what the filing half avoids), `[#271]` · kill-candidates: none -- `[#271]` owns the nightly PROPOSAL loop over the backlog and this row owns a review of LOGS and TELEMETRY; `[#419]` owns whether a routine's output is consumed rather than adding one · **Archived annotations:** `tasks/archive/723.md`
