---
id: "[#927]"
title: "User-level disableAllHooks: true has disarmed block-onedrive.ps1 since 2026-09-17 -- the P0 OneDrive zone rests on rules alone, uncommitted and outliving its emergency"
status: open
priority: P1
size: S
theme: "[E7] Tooling & evaluation"
story: "[S18] Cut session friction with better tooling"
generates: BACKLOG.md
---

- [#927] [P1][S] **User-level disableAllHooks: true has disarmed block-onedrive.ps1 since 2026-09-17 -- the P0 OneDrive zone rests on rules alone, uncommitted and outliving its emergency** - `~/.claude/settings.json` carries `"disableAllHooks": true` and no `block-onedrive.ps1` PreToolUse block, so core-invariant #1's T2 write/delete guard is not running in ANY session on this machine. DELIBERATE, and TEMPORARY by its own record: set 2026-09-17 (~11:54, file mtime) by operator emergency order during the suspended-hook wedge (`9af353f7` "The user-level ~/.claude/settings.json was disabled FIRST, outside git"; merged `bc8ddda5`); the side-file `~/.claude/settings.hooks-DISABLED-2026-09-17.json` says "COLLATERAL, TEMPORARY: block-onedrive.ps1 ... is disarmed with the rest; the permissions.deny Read rule on the OneDrive path still binds" and "RE-ENABLE only under .dev-knowledge [#863] ... with [#865] ruling on block-onedrive". Three things make it a P1 finding rather than a tracked state: (1) the change was never committed -- `~/.claude` is a git repo whose `settings.json` last commit is `e464416` (2026-08-07) and which shows it modified; (2) the repo copy was corrected the same day (`a42dd0c6`, repo `disableAllHooks: false`, "was the panic, not the measurement") but the user level was not, and 2026-09-18 added a Notification hook there that is inert under the flag; (3) `[#865]` (P2), the row asked to rule on block-onedrive, still says the user-level guard was "left untouched" -- stale -- and `[#863]`'s Done-when does not name removing the user-level flag. Filed, NOT fixed, by operator order 2026-09-19: re-enabling changes every session on the machine at once and is not a night act · Done when: the operator rules on the user-level flag and on block-onedrive (keep disarmed until `[#863]`'s watchdog, bound it under `[#808]`, or replace with `permissions.deny`); the ruled state is committed in `~/.claude`; `[#865]`'s body is corrected or it is closed into this row; and a check or SessionStart line reports whether the P0 guard is armed, so its absence is never again discovered by reading settings by hand · refs `~/.claude/settings.json`, `~/.claude/settings.hooks-DISABLED-2026-09-17.json`, `~/.claude/rules/core-invariants.md` #1, `[#863]`, `[#865]`, `[#808]`, `[#289]`, `ARCHITECTURE.md` Ch5 zone register
