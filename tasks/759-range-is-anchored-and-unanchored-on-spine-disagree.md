---
id: "[#759]"
title: "`range_is_anchored` and `unanchored_on_spine` disagree on scope, and the 2026-09-14 instance fix does not close the class"
status: open
priority: P2
size: S
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#759] [P2][S] **`range_is_anchored` and `unanchored_on_spine` disagree on scope, and the 2026-09-14 instance fix does not close the class** — the two ADR-85 organs ask different questions, and only the weaker one runs at push time. `scripts/block_unanchored_push.py:130` asks `range_is_anchored` — *does the pushed RANGE carry at least one anchor* — while `audit.check_journal_spine_anchor` asks `unanchored_on_spine` — *is EVERY spine entry anchored*. **A range containing one anchored merge and one unanchored merge is range-anchored and pushes cleanly, leaving a spine entry permanently unanchored.** · **A · This is not hypothetical: it is how `e6acb23e` reached `origin`.** Its range carried entry (c), which correctly anchored `8a41c650`, so a gate that **fails CLOSED** had nothing to refuse. The instance was discharged at `999fdd54`, and **the predicates still disagree, so the next one-commit arc reopens it** · **B · B8's per-merge entry is a real mitigation and it is a CONVENTION, not a mechanism** — it holds exactly as long as the seat holding it remembers. That is why this row is not closed by the ratification · **C · A second, independent disagreement in the same pair: the two organs read `JOURNAL.md` at DIFFERENT REVISIONS.** `block_unanchored_push` calls `journal_text(repo, tip)`; `audit.check_journal_spine_anchor` calls `journal_text(repo)` with no rev — **the committing TREE**, so an entry staged in the very commit being gated already counts. This caused a misattributed pass on 2026-09-14 that was reported to two peer seats as evidence of the ADR-110 exemption being live and had to be retracted: the commit passed because its own staged entry was visible to the reader, not because any exemption applied · **D · What this row must NOT do:** make the push gate strictly per-entry without measuring the blast radius first. `range_is_anchored` is permissive by design so a batch is not forced into one arc per merge, and B8 now supplies per-merge entries anyway — so the tightening may be cheap today and was not when the predicate was written. Measure before tightening · Done when: the two predicates' scopes are reconciled deliberately — either the push gate asserts the per-entry property, or the divergence is recorded as intentional with the residual named — AND the differing JOURNAL revisions are made explicit at both call sites so neither reading is mistaken for the other · refs `scripts/block_unanchored_push.py:130`, `scripts/journal_anchor.py` (`range_is_anchored`, `unanchored_on_spine`, `journal_text`), `scripts/audit.py` `check_journal_spine_anchor`, ADR-85 amendment 2026-08-03 §A5, `JOURNAL.md` 2026-09-14 (d) · kill-candidates: none — this row records a predicate-scope class whose instance was repaired on 2026-09-14; it retires no open id · source: batch-Y lane `lane-y-750-merge-receipts` finding, credited by the integrator 2026-09-14
