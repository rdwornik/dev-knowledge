---
id: "[#473]"
title: "Handoff suffix siblings — the base slug must reach the ACTIVE bundle, and no bundle may seal pointing at another"
status: closed
priority: P1
size: M
theme: "[E1] Handoff continuity"
story: "[S2] Finish the v5 handoff machinery deferred at the #149 flip"
generates: BACKLOG.md
---

- [#473] [P1][M] **Handoff suffix siblings — CLOSED 2026-08-02 (`0f5738be`)** — **2nd occurrence of the class.** A multi-handoff day produces `<slug>`, `<slug>-2`, … siblings, which is NORMAL operation, not an error. Two independent defects turned that normal shape into a verifier that reports a confident verdict about the wrong bundle. **(A) The active-bundle rule was advisory, not behavior:** the rule exists (`audit.py::_select_active_bundle`, built for the #372 `-arc5` false-green) but `verify_handoff_probes` never applied it, so the operator naming the BASE slug from muscle memory verified the STALE sibling — the #372 "green about the wrong file" class recurring through a different door. **(B) The generator sealed bundles that name a different directory:** `gen_handoff.generate` built its render tokens from the REQUESTED slug while `_resolve_bundle_dir` may DIVERT the write to a `-<n>` sibling under `--allow-suffix`, so a diverted bundle sealed with every internal self-reference — the `HANDOFF_BOOT` Slug field, the PROBES P0c/P3/P8 locators, `PASTE_THIS`'s embedded `/handoff-verify` command — pointing at the sibling. **(B′) The defect is invisible to the structural validator and already sealed into immutable artifacts:** `verify_handoff_probes` proves a locator BINDS, and the mis-pointed path binds fine — the sibling exists and carries same-named files. Live proof, 2026-08-02: `2026-08-01-dev-knowledge-architect-2` self-references its un-suffixed sibling 7 times and verified 14/14 GREEN, correct only because both bundles coincidentally shared the three checked values. **Binding is not identity.** · Done when: any member of a suffix family resolves to the active member (`--exact` for deliberate archaeology, reporting the supersession), generation derives every internal self-reference from the FINAL directory name and refuses to seal a bundle whose internal slug ≠ its own directory, already-sealed defective bundles verify correctly via check-time locator rebase with the identity defect reported, and the operator's existing boot prompt naming the BASE slug verbatim returns the ACTIVE bundle's verdict with zero errors and zero manual correction · refs #372 (the selection rule this makes behavior), scripts/verify_handoff_probes.py, scripts/gen_handoff.py, docs/handoffs/README.md, protocols/HANDOFF_PROCESS.md §5 · kill-candidates: none — [#372] shipped the selection rule and is closed; no open row owns generator seal-identity or locator rebasing
