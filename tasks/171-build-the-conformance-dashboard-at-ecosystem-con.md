---
id: "[#171]"
title: "Build the conformance dashboard at `ecosystem/conformance.md`"
status: open
priority: P3
size: M
theme: "[E2] Enforced governance"
story: "[S5] Catch spec/dependent drift mechanically, not by memory"
generates: BACKLOG.md
---

- [#171] [P3][M] Build the conformance dashboard at `ecosystem/conformance.md` (ADR-86 / ADR-85 R2) — a read-only validator generates it and commits its own output (ADR-80 committed-generated-zone writer policy); this is the deterministic surface #169's ungated-doc staleness signal lands in, and ARCHITECTURE Ch2 points to it. · Done when: `ecosystem/conformance.md` is generated + committed by a read-only validator (Layer-2-safe) and ARCHITECTURE Ch2 carries the pointer · refs docs/decisions/ADR-86-conformance-dashboard-location.md, ADR-80, ARCHITECTURE.md Ch2, #169 · UN-DEFERRED 2026-08-09 (ARC-2): peg "post-Wave-1 n=2 consumers" met 2026-07-07; un-blocks the [#169]/[#322] chain · **RE-CUT 2026-08-27** (K3, night-harvest adjudication; C4 census row 6) — split, nothing closed outright. **SHIPPED HALF, verified live this session and hereby settled:** both stated Done-when legs are discharged — `ecosystem/conformance.md` exists (31,122 B, committed) and is generated by the read-only `scripts/gen_dashboard.py` (Layer-2-safe, ADR-80 committed-generated zone), and ARCHITECTURE Ch2 carries the pointer (`Conformance dashboard -> ecosystem/conformance.md`). **UNSHIPPED REMAINDER, and the row is now narrowed to exactly this:** the dashboard does NOT yet carry `[#169]`'s ungated-doc staleness signal — `grep -ci 'ungated|staleness'` over `ecosystem/conformance.md` returns **0**, and its six sections (release notes / backlog / intake gate / ADR ledger / telemetry / gate health) contain no such surface. Narrowed Done-when: a deterministic staleness signal for the four ADR-85-ungated docs is RENDERED in `ecosystem/conformance.md` by the same read-only generator. **Why this is the re-cut and not a close:** `docs/audits/2026-08-21-fresh-eyes-cloud-r3-conformance.md` F1 found leg-2 scope *unresolvable from the row* across three incompatible readings, so any bare close would be ambiguous about what was closed; and `[#169]` is deferred-pegged here, so a close would strand it. This re-cut resolves the ambiguity by naming the remainder in measurable terms and keeps `[#169]`'s peg live and meaningful. **OWED:** the `[P3][M]` band was set against the whole scope; the remainder is plausibly `S`. Re-sizing is deliberately NOT taken here — a band change moves the row in the ranking, and that is an operator call, not an executor's.
