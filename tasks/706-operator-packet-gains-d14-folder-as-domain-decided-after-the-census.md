---
id: "[#706]"
title: "The operator packet gains D14, folder-as-domain -- and it cannot be decided before the domain census returns"
status: open
priority: P2
size: S
theme: "[E7] Tooling & evaluation"
story: "[S19] Decide the undecided artifact/tool models"
generates: BACKLOG.md
---

- [#706] [P2][S] **The operator packet gains D14, folder-as-domain -- and it cannot be decided before the domain census returns** - AX6-4 of `to-cc/AMEND-BATCH-X-ROSTER-006.md`, carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93) Part 4, and the second of the three rows the operator's 2026-09-11 instruction named. · **The operator's model is that every folder is a domain with an owning process**, with process management on top -- AMEND-006's header records it, along with the fear driving it: that many implemented processes are orphans nobody remembers. D14 is where that model becomes a decision instead of a view. · **Two questions, and they are not the same question.** (1) Rename `docs/` to `process/`? (2) One owning process per folder? The second is the structural commitment and the first is a consequence of it -- a rename that gets made without the commitment is churn, and the commitment can be made without any rename at all. · **The sequencing is the row's substance, not decoration.** AX6-4 says D14 is decided AFTER lane X-C's census, with its per-folder verdicts as the input, and records that the seat recommends no rename before the census. A folder-level rename touches every locator in the corpus; deciding it against an unmeasured guess about what lives in each folder is how a rename becomes a week of broken references. The census exists precisely to supply the per-folder verdicts -- LIVE / UNTRIGGERED / UNREAD / GARBAGE-CANDIDATE -- that the decision needs. · **Why this is a ROW and not a packet edit done here.** The row tracks an OPERATOR decision to the point of ruling; it does not make it, and it does not close when the packet is written. A decision item that has been drafted but not ruled is exactly the class `decision_coverage` is built to keep visible. · Done when: D14 is in the operator decision packet with both questions stated separately, with lane X-C's per-folder verdicts named as its input and the seat's recommendation -- no rename before the census -- carried with it; and the row closes on the operator's RULING, not on the packet being written · **Done-when authored by the filing lane** from AX6-4's clauses; AMEND-006 states them as a packet item rather than a Done-when · **Blocked on** lane X-C's census artifact, `docs/audits/2026-09-11-technical-domain-census.md` · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, Part 4 AX6-4 and Part 5's AX6-1 note), `to-cc/AMEND-BATCH-X-ROSTER-006.md` AX6-4 (the packet item) and AX6-1 (the census lane that supplies its input), `protocols/OPERATOR-INTERFACE.md`, `[#692]` (`decision_coverage` -- what keeps an unruled decision visible), `[#526]` (the root-hygiene audit: which root files must be root), `[#420]` (whether a top-level `docs/archive` still makes sense) · kill-candidates: none -- `[#526]` owns which files belong at the ROOT rather than what owns a folder, and `[#420]` owns one directory's fate rather than the folder-as-domain model; both are inputs to D14 rather than substitutes for it · **Archived annotations:** `tasks/archive/706.md`
