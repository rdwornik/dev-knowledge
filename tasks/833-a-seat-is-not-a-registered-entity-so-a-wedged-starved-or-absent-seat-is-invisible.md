---
id: "[#833]"
title: "A seat is not a registered entity, so a wedged, starved or absent seat is invisible until a human asks"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#833] [P1][M] **A seat is not a registered entity, so a wedged, starved or absent seat is invisible until a human asks** - Operator order 2026-09-16, plan 2026-09-16 row 5 (`to-browser/PLAN-2026-09-16-execution-order.md`): ~17 agent-hours lost this week -- 6 h wedged integrator, 10.7 h starved subagents, and a half-day with NO integrator because the browser did not boot one. **Wedged / absent / starved are three values of ONE field**, and today no organ holds that field: `seat_refusals.py` refuses a seat's acts but never records the seat, `resource_lifecycle.py` counts `claude` processes without knowing whose they are, `lane_cost.py` keys an attended seat by session id only to price it after the fact, and `claude agents` reported a session "busy" after its PID was dead (`[#803]`) -- a control-plane self-report is not evidence of the state it reports. · Done when: (1) **one registry, one field** -- a seat record (session id, role in {dispatcher, integrator, lane, browser}, batch, `state` in {live, wedged, absent, starved}) persisted in ONE append-only store, where `state` is WRITTEN ONLY BY AN EVENT (a hook / heartbeat / receipt with a timestamp), never by a model's prose, and a test proves a model-authored write is refused; (2) **refusal** -- dispatching a lane into a batch with no `live` integrator seat REFUSES, naming the missing role, with a RED-first trip-test plus a passing-path test so it cannot pass by refusing everything; (3) **self-surfacing** -- a seat whose last event is older than a stated threshold reads as `wedged`/`starved` in the SessionStart surface without anyone asking, the threshold a constant with a `THRESHOLD_PROVENANCE`-style reason; (4) **library-first, named** -- the end-of-lane artifact states which of `seat_refusals.py`, `resource_lifecycle.py`'s census, `preflight_contract.py`, `claude agents` / the session store and the heartbeat receipt ledger became the registry, and why a new store was or was not needed; (5) no enforcement rests on allow/ask rules (under `bypassPermissions` only deny rules and PreToolUse exit 2 refuse), and no new commit-tier gate -- this is a dispatch-time refusal · **SYMPTOM ROWS, found by search -- each OVERLAPS, none is superseded, none is closed here:** `[#648]` overlaps -- its dispatcher-liveness half; NOT covered: the locator-staleness half, and "refuse from a wired GATE stage" (this row refuses at dispatch time, not at a gate tier) · `[#805]` overlaps -- the integrator that sat 10.6 h waiting becomes a surfaced `starved` seat; NOT covered: all four of its Done-when legs (the three recorded defaults, their detectors, the AA dry-run, the ratchet-raise authorisation, the checklist item) · `[#808]` overlaps -- a session wedged by a hung hook reads as `wedged` at the next SessionStart; NOT covered: the bounded hook, the fail-OPEN bypass record, the per-hook bound check and posture statement · `[#682]` overlaps -- this registry is the "live seat roster" its addressee vocabulary must be drawn from; NOT covered: the OPERATOR-INTERFACE contract, machine-placed addressees and its RED-first test · adjacent, not counted: `[#769]` (a bound on a commit-tier gate), `[#803]` (the stale `claude agents` self-report is this row's motivation, not its Done-when) · refs `scripts/seat_refusals.py`, `scripts/resource_lifecycle.py`, `scripts/lane_cost.py`, `scripts/preflight_contract.py`, `scripts/lane_boot.py`, `scripts/fleet_health.py`, `docs/audits/2026-09-16-technical-batch-ab-manifest-amendment-1.md` (ids, and the ab-808 <-> ab-833 `settings.json` sequencing) · kill-candidates: none -- the four symptom rows above each keep an uncovered leg, named · source: operator order 2026-09-16, lane ab-833
