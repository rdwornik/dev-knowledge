---
id: "[#617]"
title: "FILE DISTILLATION — the output half, and the only worsening series"
status: open
priority: P2
size: M
theme: "[E5] Canonical-file integrity"
story: "[S14] Keep the day-to-day docs right-sized and current"
generates: BACKLOG.md
---

- [#617] [P2][M] **FILE DISTILLATION — the output half, and the only worsening series** — intake #23's P1 named repomix as the *input*-side distiller; that half is **rejected on measurement** (`[#467]`: 0% on markdown, the paste re-renders LARGER). The **output** half — the bytes the corpus makes a seat read before it can work — has never had a carrier, and now has a number: the dashboard reports **`paste / boot bytes  WORSENING  +11232 bytes`** over a 12-sample window — **the only worsening panel**; the board's other live series read IMPROVING. Its store is the `protocols/HANDOFF_BOOT.md` blob size per revision — the artifact every session reads first. `[#467]`'s closure names the re-entry route — *"a new filing citing the pain"* — and this is it. · Done when: the worsening is attributed to NAMED growth sites, not to the corpus at large, a distillation act lands on at least one, and `gen_trend_dashboard.py --write` reports the paste/boot panel no longer WORSENING over the same window · refs docs/audits/2026-08-29-technical-nb2-n-packet.md §0, docs/intake/2026-08-01-func-distillation-and-library-first.md, #449, #467 · kill-candidates: none — `[#511]` is adjacent but scoped to the NON-MECHANIZED handoff authoring load, and `[#607]` owns the PLAYBOOK census's mechanical half, not the diet
