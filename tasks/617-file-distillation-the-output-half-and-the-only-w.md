---
id: "[#617]"
title: "FILE DISTILLATION — the output half, and the only worsening series"
status: open
priority: P2
size: M
theme: "[E5] Canonical-file integrity"
story: "[S14] Keep the day-to-day docs right-sized and current"
generates: BACKLOG.md
---

- [#617] [P2][M] **FILE DISTILLATION — the output half, and the only worsening series** — intake #23's P1 named repomix as the *input* distiller, **rejected on measurement** (`[#467]`: 0% on markdown, paste re-renders LARGER). The **output** half — bytes a seat reads before it can work — had no carrier, now has a number: **`paste / boot bytes  WORSENING  +11232 bytes`**, the dashboard's **only** worsening panel. Store: `protocols/HANDOFF_BOOT.md` bytes per revision. **An eval loop is a PRECONDITION, not a companion**: LLM-authored artifacts without one contribute ~**zero percentage points** (SkillsBench). One leg of the **`.CLAUDE` GOVERNANCE MODEL** (intake #62, as amended): its admission gate and this precondition are one clause from two sides. **ORDERING:** that gate CONSUMES THE METRIC (north-star rank 1) — a gate built before its scoring harness has nothing to read. · Done when: the worsening is attributed to NAMED growth sites, a distillation act lands on one, and `gen_trend_dashboard.py --write` reports the paste/boot panel no longer WORSENING over a 12-sample window · refs docs/audits/2026-08-29-technical-nb2-n-packet.md §0, intake #23, intake #62, #449, #467 · kill-candidates: none — `[#511]` is the NON-MECHANIZED authoring load, `[#607]` the PLAYBOOK census's mechanical half, not the diet
