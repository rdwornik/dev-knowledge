---
id: "[#555]"
title: "Closing campaign batch 1 + kill-candidates instrument"
status: open
priority: P1
size: M
theme: "[E7] Tooling & evaluation"
story: "[S20] Revive the nightly layer, load-gauge first (rent-rule discipline)"
generates: BACKLOG.md
---

- [#555] [P1][M] **Closing campaign batch 1 + kill-candidates instrument** — the detector-fed batch whose output is CLOSED OR KILLED ROWS rather than new findings, plus the instrument that makes the kill half real. This is the row that changes the window class: the operator’s standing acceptance test for the next window is visible net closure, not another audit. The denominator and the classes are already measured, so batch 1 does not re-derive them — NB4-E censused the open set into NOW-CLOSABLE 2 · CLOSABLE-AFTER-WAVE-2 **0** (its most load-bearing finding: nothing is merely waiting on wave 2) · NEEDS-ACT 4 · NEEDS-RULING 25 · LIVE 165, and drafted six batches across windows 3–8. **The kill half is the part with no organ today.** `backlog-filing-backpressure` already forces a `kill-candidates:` assertion at BIRTH and `preflight_backlog_ids` checks each named row is open, but nothing ever REVISITS those assertions, so a row that was born naming its rivals is never actually weighed against them — filing backpressure without a collection step. The instrument is the collection step: read the standing `kill-candidates` assertions across the open set and turn them into proposals a batch can execute. Pair it with the `[#277]` STRONG:WEAK ratio, which NB4-E already reads live, so the campaign reports a quality figure and not just a count. **Denominator discipline is part of the deliverable, not a footnote:** measured live at this window’s close the OPEN set is 191 rows (7 P1 / 96 P2 / 88 P3), which `BACKLOG.md` renders as **215** bullets once the 24 `deferred` rows are included — two different true numbers on two different bases, and a campaign quoting the wrong one will claim progress it did not make. A **third** figure disagrees with both: the `SessionStart` load gauge printed `8 P1 / 92 P2 / 96 P3` = 196 at this window’s boot, which matches neither basis, so the campaign's first act is to name ONE predicate and make the gauge use it. This window alone birthed 19 rows — 17 in batch 7a plus [#554] and this row — which is exactly why "net-negative" must be measured against a live denominator rather than a remembered one. · Done when: the first batch closes **net-negative** — closures strictly greater than births — measured against the live denominator at that batch’s close, with the before/after figures both re-derived rather than carried · refs docs/audits/2026-08-16-census-nb4-closing-campaign.md, scripts/check_backlog_filing.py, scripts/propose_closures.py, #277, #552, #534, #348, #270 · kill-candidates: none — the campaign IS the kill mechanism, so a row proposing it cannot be dispatched by it; [#552] owns the disposition/archival routine for AUDITS and [#348] owns grooming CADENCE, neither of which closes a backlog row · source: architect authorization at the 2026-08-17 window close (priority-order-v2 slot 5)
