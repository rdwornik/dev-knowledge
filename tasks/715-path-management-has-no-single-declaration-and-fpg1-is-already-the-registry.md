---
id: "[#715]"
title: "Path management has no single declaration -- three governed-path mismatches in one day, and the registry the operator is describing already exists as FPG-1"
status: open
priority: P1
size: L
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#715] [P1][L] **Path management has no single declaration -- three governed-path mismatches in one day, and the registry the operator is describing already exists as FPG-1** - AX7-3 of `to-cc/AMEND-BATCH-X-ROSTER-007.md` files this row into X2 and **not conditional**, superseding AX5-3's conditional filing. Its intake body is `QUESTION-path-registry-2026-09-11` **as amended by** `AMEND-PATH-REGISTRY-001`, both carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93) Parts 6 and 7, **and the amendment governs where they differ**. · **READ APR-1 BEFORE READING THE QUESTION, because the amendment withdraws the QUESTION's recommendation.** The QUESTION recommends Option A, "registry file + one resolver per language + literal-path gate". APR-1 rules that the registry the operator is describing **already exists -- it is FPG-1**: path, purpose, trigger, edges, query. Option A is withdrawn AS A NEW BUILD and becomes "FPG-1 completed". **No second graph.** This row is not a new registry and must not be built as one. · **WHAT IS ALREADY `[#664]`, and is therefore deliberately NOT in this row's Done-when.** APR-1's completion legs are `[#664]`'s own clauses: a `writes` edge / artifact column is `[#664]` step D, and FPG-1 wired as the spine so it is CALLED rather than re-read is `[#664]` clauses 1-3. Restating them here would duplicate a live row, so this row CITES them and scopes its Done-when to what `[#664]` does not carry. The one completion leg not plainly inside `[#664]` -- last-touched read from git AT QUERY TIME and never stored twice -- is named below, because it is a constraint on how that column is built rather than a second store. · **WHAT IS GENUINELY NEW IS APR-2: ROOTS ARE CONFIGURATION.** The three measured defects are about ROOT locations rather than file purpose, which is exactly why FPG-1 does not already answer them: contracts written to `to-cc/` while `Dispatch-Lane` resolves against the prompts root; the launch line rendered without `-Model`; `worktree.baseRef` unset. One declaration -- transport root, worktree base, prompts dir, outputs -- read by FPG-1's Python loaders AND by the PowerShell verbs, with a lint refusing a governed root written inline. Library-first is named in the amendment: `platformdirs` for OS-standard directories, git for history, nothing hand-rolled beyond the declaration itself. · **APR-4 FOLDS THREE FILED-BUT-NEVER-DECIDED CANDIDATES IN AS CLAUSES**, and its instruction is that they get no rows of their own: ARCHITECT-INBOX-2026-09-05-009's (c) derived-copies registry becomes an FPG-1 EDGE KIND, (j) freshness registry becomes a QUERY over last-touched, and the per-consumer registry becomes the same graph per repo root joined by the floor manifest. Three registries that would each have been a build become three uses of one graph. · **THIS ROW LANDS OPEN, AND THAT IS APR-5 RATHER THAN INDECISION.** The QUESTION and the amendment are the intake BODY; the decision is made by the decision engine (AX7-1) with the thesis table -- the browser proposes and does not rule. The QUESTION's own flip condition and sensitivity are carried with it: A flips to B if fewer than ~10 governed roots exist and all are already behind environment variables; answering Q3 NO (no renames ever) and Q4 NO makes C win on cost, and Q3 is LIVE because D14 (`[#706]`) sits on the operator's packet. · **Format sub-question, carried unanswered:** YAML is the house convention (`provider-registry.yaml`, `parity-surfaces.yaml`), PowerShell reads JSON natively and YAML only with a module -- so the QUESTION routes it to CC to check whether win-tooling already carries a YAML reader. That check is part of this row, not a prerequisite to it. · Done when: the ROOT declaration of APR-2 exists once and is read by both FPG-1's Python loaders and the PowerShell verbs, with the YAML-versus-JSON sub-question resolved by checking whether win-tooling already carries a YAML reader; a lint REFUSES a governed root written as an inline literal, with a RED-first test; APR-4's three candidates are discharged as CLAUSES of this row -- derived-copies as an FPG-1 edge kind, freshness as a query over last-touched, per-consumer as the same graph per repo root -- with no separate registry built for any of them; last-touched is read from git at query time and never stored twice; and the response measure is met at ZERO path mismatches per batch, against today's baseline of three in one day · **Done-when authored by the filing lane** from APR-1..APR-5 and the QUESTION's response measure, **deliberately scoped to EXCLUDE `[#664]`'s clauses** so the two rows do not duplicate · **disposition: OPEN -- decided via the decision engine (AX7-1) per APR-5; the browser proposes, it does not rule** · **Blocked on** `[#664]` for the FPG-1 completion legs, and on `[#718]` / `[#717]` / `[#716]`, which APR-3 ships FIRST · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, Part 5 AX7-3 and Parts 6-7, the carried intake body), `to-cc/QUESTION-path-registry-2026-09-11.md` (the proposal; Option A WITHDRAWN as a new build), `to-cc/AMEND-PATH-REGISTRY-001.md` (APR-1..APR-5, which governs), `scripts/file_purpose_graph.py` (FPG-1 -- the registry that already exists), `[#664]` (the spine clauses APR-1 points at -- read together, do NOT duplicate), `[#716]` `[#717]` `[#718]` (APR-3's three fixes, which ship first), `[#706]` (D14, whose rename answer is the QUESTION's Q3), `[#692]` (`decision_coverage`, the engine half that will rule this) · kill-candidates: none -- `[#664]` owns the FPG-1 completion legs and this row is written to EXCLUDE them rather than compete, which is why its Done-when is roots and the lint; the three inbox-009 candidates APR-4 folds in were filed as inbox candidates and never became rows, so there is nothing to kill · source: `to-cc/AMEND-BATCH-X-ROSTER-007.md` AX7-3 with `to-cc/QUESTION-path-registry-2026-09-11.md` and `to-cc/AMEND-PATH-REGISTRY-001.md`, id assigned by the operator instruction of 2026-09-11, filed by lane `lane-x-000-batch-x-roster-lands`
