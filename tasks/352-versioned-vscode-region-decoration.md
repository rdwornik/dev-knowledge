---
id: "[#352]"
title: "Versioned `.vscode` region decoration"
status: closed
priority: P3
size: S
theme: "[E6] Cross-repo universalization"
story: "[S15] Converge every child repo on the universal baseline"
serialize-group: settings-json
generates: BACKLOG.md
---

- [#352] [P3][S] Versioned `.vscode` region decoration (RULING-S human-facing half) — editor-side background decoration (grey = owner=hub, navy = owner=repo; dark theme) of the #312 methodology-boundary marker regions via a versioned `.vscode` config. P4a, shelf-life 2026-08-13 (revisit/kill if not advanced). · Done when: versioned `.vscode` region-decoration config (grey/navy) covers the owner=hub/repo regions of ≥1 governed file AND carries no hand-maintained ownership state · AMENDED (architect ruling, visible-boundary lane): the original generator clause is satisfied BY ELIMINATION, not built — the shipped config holds two regexes keyed on the #312 marker vocabulary and no per-region state, so region add/remove/reclassify needs no config change and it cannot rot; generating a static two-regex file is ceremony. #329 UNAFFECTED — its folder-level ownership genuinely needs generation from parity-surfaces.yaml; do not conflate. · refs #329, #312, RULING-S, docs/audits/2026-07-11-technical-fleet-boundary-marker-design.md · kill-candidates: none — pairing with #329 (folder-level vs in-file; neither subsumes the other) · serialize-group: settings-json · **CLOSED 2026-08-15** (morning adjudication D2.1, ratifying the night-2 census as this seat's P10 discharge): **already-shipped** under ADR-65. Both Done-when clauses hold against the live tree. Clause 1 — `.vscode/settings.json`'s `//boundary` block carries two `highlight.regexes` keyed on the #312 marker vocabulary (`owner=hub` → grey `rgba(140,140,140,0.16)`, `owner=repo` → navy `rgba(38,79,140,0.30)`), dark-theme tuned, `filterFileRegex` scoped to `CLAUDE.md` and `.claude/**.md`; the file is tracked and landed at `897577b7`, whose subject reads "flip colours to the [#352] spec". Clause 2 — no hand-maintained ownership state: the config holds two regexes and no per-region state, so region add/remove/reclassify needs no config change (the row's own AMENDED clause already adjudicated this). Third signal: the row's declared shelf-life 2026-08-13 (revisit/kill-if-not-advanced) has passed, and the tree says the work is done, so the reading is close, not kill-unbuilt. Evidence locators: `docs/audits/2026-08-14-census-night2-census.md` §2, `docs/audits/2026-08-15-technical-night2-consolidated-briefing.md` D2.1. Serialize-group flag weighed: `settings-json`, 11th of 18 — contention, not dependency
