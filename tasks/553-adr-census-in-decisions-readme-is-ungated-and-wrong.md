---
id: "[#553]"
title: "`docs/decisions/README.md`'s ADR census is hand-maintained, ungated, and currently wrong in two places"
status: open
priority: P3
size: S
theme: "[E5] Canonical-file integrity"
story: "[S14] Keep the day-to-day docs right-sized and current"
generates: BACKLOG.md
---

- [#553] [P3][S] **`docs/decisions/README.md`'s ADR census is hand-maintained, ungated, and currently wrong in two places** — the "Measured at declaration" paragraph states the census across *"the 86 ADR files in this folder and `archive/`"* with **Accepted (81)**. Measured live 2026-08-17: **85 live + 2 archived = 87 files, Accepted 82** — the sentence was touched at ADR-112's ratification without incrementing, so ADR-111 (ratified 2026-08-10) is uncounted. **n=2:** first reported by `docs/audits/2026-08-16-census-nb6-archive-sweep.md` §2.2, still true a day later, and no row owns it. **A second claim in the same block is false independently:** it records as a "measured defect" that `ADR-61-git-worktree-parallel-sessions.md` *"carries no parsable status line at all"* — that file carries `status: Accepted 2026-05-28` in YAML frontmatter, which is parsable; the true statement is that it is the corpus's only THIRD-format status line, so a single-format parser returns a false `None` on it. This is the silent-rot class the repo already gates elsewhere — `audit-index-freshness` regen-and-diffs `docs/audits/README.md` and `claude-rosters-freshness` the CLAUDE.md fragments — while this index has no generator and no freshness hook. **A number swap is the WRONG fix, and the reason is the row's point:** the sentence is a *dated* measurement, so overwriting its figures would make it claim that the 2026-08-12 measurement produced today's numbers, falsifying its own provenance. The honest options are (a) generate the census figures from disk so they cannot rot, or (b) append a dated re-measurement and leave the original intact. · Done when: the ADR census figures are either machine-generated with a regen-and-diff gate on the `audit-index-freshness` model, or carry a dated re-measurement matching a live count; and the ADR-61 claim states the three-format parser hazard instead of asserting an absent status line · refs docs/decisions/README.md, docs/audits/2026-08-16-census-nb6-archive-sweep.md §2.2, docs/decisions/ADR-61-git-worktree-parallel-sessions.md, scripts/gen_audit_index.py, #242, #269, #552 · kill-candidates: none — [#242] owns header-versus-README STATUS coherence for a single ADR and [#269] owns the audits index; nothing owns this index's aggregate figures or its missing generator · source: docs/audits/2026-08-17-technical-batch-7a-lane-b-contract.md step 1 (ADR currency sweep), confirming and extending docs/audits/2026-08-16-census-nb6-archive-sweep.md §2.2
