---
id: "[#577]"
title: "Adopt `AGENTS.md` as the portable instruction layer — the bounded execution lane"
status: open
priority: P2
size: M
theme: "[E6] Cross-repo universalization"
story: "[S16] Manage the methodology as a living thing (essence lifecycle: transfer · sync · PRUNE)"
serialize-group: claude-md
generates: BACKLOG.md
---

- [#577] [P2][M] **Adopt `AGENTS.md` as the portable instruction layer — the bounded execution lane** — **RULED ADMITTED (architect, 2026-08-22); this row is the execution, not the decision.** Do not relitigate the admission and do not re-cost the alternatives — the ruling settled both grounds it could have failed on. **The size objection is dead, measured not assumed:** the proposed shape is **15,439 B = 47.1% of the Codex 32 KiB `project_doc_max_bytes` cap, with 16.9 KiB of headroom**, so R2 §2.5's refusal conditional does not fire; and **the doctrine reading is ruled** — ADR-53 forbids *two content-carrying files*, not the filename, so a one-line pointer preserves the substance ADR-53 protects. **Bounds, all four binding:** (1) `AGENTS.md` is **≤120 lines** and is **portability-not-quality** per the recorded caveat — it carries build/test commands, boundaries and PR rules, never the Claude-runtime specifics; (2) `CLAUDE.md` keeps the Claude-runtime-specific remainder with the pointer relation the narrow reading permits; (3) the `~/.codex` precedence collision is resolved **by scope, recorded in the file header** — repo `AGENTS.md` governs in-repo work, the L0 reviewer pin governs the reviewer role; (4) the **guard is stated in BYTES, not lines** — this repo averages ~117 B/line, so a line ceiling does not bound what the cap measures. **Two traps the R1 packet found and this lane must not re-discover:** copying `CLAUDE.md` wholesale lands at **43.50 KiB, 11.50 KiB OVER the cap, and Codex truncates silently**; and the precedence chain has an **unpriced third layer** — `codex/AGENTS.md` sits at an intermediate directory *inside this repo*, so a cwd at or below `codex/` yields `role → doctrine → role` and the role wins by position rather than by intent. **`CLAUDE.md` §10's first anti-pattern bullet is now false doctrine** ("Narrating or managing AGENTS.md — AGENTS.md is retired") and is inverted or deleted by this lane, not left to rot. **Gemini's `.gemini/settings.json` is explicitly NOT admitted** — it would be a new top-level directory that `validate_hermetization.py` Rule A blocks absent an ADR-101 §1 amendment, and ADR-53 records the active toolset as Claude Code + Codex only; a third provider is a cost with no present consumer. · Done when: a root `AGENTS.md` ≤120 lines exists carrying the portable layer, `CLAUDE.md` carries only the Claude-runtime remainder plus the permitted pointer, the combined global+root payload is asserted **in bytes** against the 32 KiB cap by a test, the scope resolution for the `~/.codex` collision is stated in the `AGENTS.md` header, and `CLAUDE.md` §10's retired-AGENTS.md anti-pattern is corrected in the same commit · refs docs/audits/2026-08-22-technical-intake-r1-decision-packet.md, docs/intake/2026-08-17-tech-agent-instruction-layers-and-distillation.md R1, ADR-53, ADR-101, docs/audits/2026-08-22-technical-cloud-wave-close-funnel.md line A2 · kill-candidates: none — intake #35's R1 is the DECISION and is now ruled, so it carries no execution; no open row owns the portable-instruction surface, and `[#427]`'s claude-md group owns region templates, not the instruction layer · serialize-group: claude-md
