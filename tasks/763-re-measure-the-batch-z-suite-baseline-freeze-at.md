---
id: "[#763]"
title: "Re-measure the batch-Z suite baseline freeze at batch close — a cause whose lane merged must have left the set"
status: open
priority: P1
size: S
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#763] [P1][S] **Re-measure the batch-Z suite baseline freeze at batch close — a cause whose lane merged must have left the set** — `logs/SUITE-BASELINE-FREEZE.md` freezes 51 conductor failures at `b5270d63`, grouped into 11 causes, by operator ruling 2026-09-15 (*freeze, do not triage to green tonight*). · **This row IS the expiry.** A freeze with no re-measurement is not a baseline, it is a new normal — the thing the ruling explicitly refuses. At batch Z close the suite is re-measured on the conductor and compared **member by member** against the frozen roster. · **The predicate, stated so it cannot be skimmed past:** for every cause whose lane merged during batch Z, that cause **must have left the set**. C8 (BACKLOG over bar) has lane 7; C5 (fail-open guards) has lanes 2 and 3; C2 (audit health / telemetry) has lane 1. **If the number has not fallen, that is a row, not a new normal** — one row per cause that should have cleared and did not, each naming its lane and its merge SHA. · **Membership is by test node id, never by count.** A file with 3 frozen members that fails 4 has a regression the count alone hides; the freeze file lists all 51 ids for exactly this reason. · **The re-measurement must report its own resolved worker count.** `addopts = "-n auto"` resolved to 4 workers on `ubuntu-latest`; a different runner re-partitions the suite and can flip cross-worker-sensitive members (cause C9 is that class), so a comparison across differing worker counts is not like-for-like and must be declared as such rather than quietly compared. · **Recorded discrepancy to settle here:** the freeze was ordered with two causes named as known — a missing optional dependency (~17) and module-shadowing (~5) — and **neither exists in the conductor measurement** (0 matches for `ModuleNotFoundError`/`ImportError`/`No module named`). If those figures came from a local Windows run they are a different baseline on a different substrate; this row settles whether a local baseline is also wanted, as a separate file, or whether the figures were mistaken · Done when: the suite is re-measured on the conductor at batch Z close, the per-cause table in `logs/SUITE-BASELINE-FREEZE.md` carries a second column with the new count and the merged lane for each cause, every cause that should have cleared and did not has its own filed row, and the ~17/~5 discrepancy has a recorded verdict · refs `logs/SUITE-BASELINE-FREEZE.md` (the frozen set), conductor run `34901604346` (the measurement), `[#744]` (the false-pass class this freeze must not become), `[#746]` (lane 15's devcontainer root cause, same batch) · kill-candidates: none — no open row carries suite-baseline discipline; `[#744]` is adjacent (it owns a forced exit-0, not a frozen verdict set) · source: integrator seat, batch Z night, operator ruling 2026-09-15
