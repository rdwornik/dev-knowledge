---
id: "[#755]"
title: "Finish the docs cut — `ARCHITECTURE.md` to ≤ 15 KB behind the X3 render, and the four ESSENTIALS residues the dissolution could not reach"
status: open
priority: P1
size: M
theme: "[E5] Canonical-file integrity"
story: "[S14] Keep the day-to-day docs right-sized and current"
generates: BACKLOG.md
---

- [#755] [P1][M] **Finish the docs cut — `ARCHITECTURE.md` to ≤ 15 KB behind the X3 render, and the four ESSENTIALS residues the dissolution could not reach** — the residual of batch-Y lane `lane-y-755-docs-cut-finish`, which DELETED `protocols/ESSENTIALS.md` (16,461 B) and closed `[#628]` on the file's absence. **What this row carries is what that lane deliberately did NOT do, each with a measured reason rather than an omission.** · **A · `ARCHITECTURE.md` ≤ 15 KB is BLOCKED, not skipped, and the premise is REFUTED three ways.** `scripts/file_purpose_graph.py` exposes only `why` and `stats` — there is no emit/render subcommand, so nothing can render Ch2 from the graph; no script in the repo WRITES `ARCHITECTURE.md` (`scripts/audit.py`, `scripts/codemap/check.py`, `scripts/canonical_docs.py` and `scripts/canonical_freshness_gate.py` all only READ it); and `[#664]`'s own body sequences the work elsewhere in its own words — *"step D (the `ARCHITECTURE.md` Ch2 render) plus the organ map are X3, so a lane in X1 that does not render Ch2 is conforming, not short"*. `[#667]` is `depends-on: #664`, and `[#664]` stays open precisely because step D is X3. **Measured at 100,845 B across eleven chapters so the X3 lane starts from measurement:** Organ map 25,251 B (11,157 B of it 37 table rows) · Validators and enforcement 24,927 B (only 1,476 B in tables — ~23 KB is PROSE) · Governing ADRs 11,920 B · Verification mesh 10,574 B · Distribution 6,870 B · Key conventions 6,124 B · Automation axes 5,918 B · Layer boundaries 2,970 B · Purpose 2,099 B · Authority 1,518 B · Codemap 906 B. **Two constraints that lane must not trip:** `ecosystem/organ-index.md` states in its OWN header that it does not carry failure posture and that "Ch2 carries it by hand and stays the source for it", so Ch2 is NOT redundant with the generated index and cannot simply become a pointer to it; and the 246-line review prologue `[#667]` expects to relocate into a JOURNAL entry is **already gone** — the header is now a ~25-line "How to read this doc" — so that byte budget cannot be spent twice. A table render alone cannot reach 15 KB when the bulk is prose · **B · `templates/child-methodology-floor.md.tmpl:41` still names ESSENTIALS in prose, and removing it IS a release act.** The floor is hash-guarded three ways — the template bytes, the `.sha256` sidecar, and `anchors.floor_sha256` in three live manifests — and `deploy/release_lint.py` C5 asserts the three-way equality. `[#628]` says this sequences WITH the v1.5.0 release; the deletion did not force it because the line is stale prose, not a broken route · **C · The six `deploy/manifest-v*.yaml` `doc_shapes:` rows for `protocols/ESSENTIALS.md` describe a file that no longer exists.** They are INERT — `release_lint` C7 compares only entries with a truthy spine and those with `freshness_gated: true`, and v1.5.0's row is `spine: []` + `freshness_gated: false` — and `scripts/canonical_docs.py:255-268` records the precedent that a shipped manifest's `doc_shapes` is answered by a **version bump, not a retro-edit**. So this is a v1.6.0 act, not a cleanup · **D · `protocols/AI_COUNCIL_PROCESS.md` § "Section history" still names ESSENTIALS in its 2026-09-04 re-stamp entry.** That file's LIVE citations were already re-pointed to PLAYBOOK; what remains is a dated history entry recording the re-point, which routes nothing and stays true. It is a member of `audit._FRESHNESS_FILES`, so editing it forces a `last_reviewed` re-stamp — and a stamp means "re-read end-to-end and confirmed accurate", which its 424 lines did not receive. Left rather than stamped falsely · **E · `protocols/STANDING_RULINGS.md` T-29's premise has CHANGED and the ruling is untouched.** T-29 dispositions the stale ESSENTIALS citations at `protocols/PLAYBOOK.md:649` and `protocols/AI_COUNCIL_PROCESS.md:325,413` as *accepted-by-relocation*, on the stated reasoning that "the target still exists under another name". After the deletion the target does not exist at all, so the reasoning no longer holds even though the disposition may still be right. A closed ruling is not reopened by a lane; re-adjudicate or re-word it deliberately · **F · `protocols/PLAYBOOK.md` owes a stamp this lane could not honestly give, and a TRIPWIRE is RED because of it.** The dissolution edited PLAYBOOK (Ch2 "Authority hierarchy" item 1, the living-doc registry rows, the per-repo table, the `last_reviewed` roster, the casing example), so content landed on 2026-09-14 after its prose `> Last updated:` stamp of 2026-09-08. `tests/test_canonical_docs.py::test_the_live_playbook_doctrine_row_shows_its_reconciled_spec_and_a_derived_date` asserts `CLASS_UNGATED_FRESH` as a deliberate tripwire whose own comment says a flip back to STALE "is a real event that should be seen, not absorbed". Moving the prose date without a genuine end-to-end re-read of ~6,100 lines would absorb it and would be the false-stamp failure that test exists to catch. **The stamp is owed by the next pass that genuinely re-reads PLAYBOOK**, not by the lane that tripped it · Done when: `ARCHITECTURE.md` is ≤ 15 KB with its Ch2 table rendered from the spine graph behind a freshness hook (through `[#667]`/`[#664]` step D, not around them); the floor template's ESSENTIALS mention is removed in the same act as a floor sha regeneration with `deploy/release_lint.py` C5 agreeing; the manifest `doc_shapes` rows are retired at a version bump; `protocols/AI_COUNCIL_PROCESS.md`'s history entry is re-worded in a pass that genuinely re-reads the file and re-stamps it; T-29 is re-adjudicated against its changed premise; and `protocols/PLAYBOOK.md` carries a stamp from a pass that genuinely re-read it, turning the tripwire green on evidence rather than on a date edit · refs `docs/audits/2026-09-14-technical-lane-y-755-docs-cut-finish.md` (this row's evidence packet — the measurement, the proposed diffs and the attribution of the four pre-existing suite failures), `docs/audits/2026-09-14-technical-batch-y-manifest.md`, `docs/audits/2026-09-14-technical-batch-y-launch-contracts/LANE-y-755-docs-cut-finish.md`, `[#628]`, `[#667]`, `[#664]`, `deploy/release_lint.py`, `templates/child-methodology-floor.md.tmpl`, `protocols/STANDING_RULINGS.md` · kill-candidates: none — `[#628]` closes on the deletion this row is the residual of, and `[#667]` owns the docs rewrite this row hands back to rather than duplicating · source: batch-Y lane `lane-y-755-docs-cut-finish`, 2026-09-14
