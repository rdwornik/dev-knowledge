---
id: "[#277]"
row: tasks/277-propose-closures-signal-repair.md
record: row-body-archival
schema: 1
events: 1
pointer: "**Archived annotations:** `tasks/archive/277.md`"
---

# Archived row annotations — [#277]

Dated-amendment narration relocated **verbatim** out of `tasks/277-propose-closures-signal-repair.md` by
`scripts/archive_row_body.py` under `[#612]`. Nothing was trimmed, summarised or
rewritten; the row keeps the path-qualified pointer

> **Archived annotations:** `tasks/archive/277.md`

and this file keeps the narration. Ruling **B1** (`protocols/STANDING_RULINGS.md`,
"trim-vs-disposition") records that dropping the dated-amendment narration is the
drain that works on this class — relocation is that drain performed without the loss.

**Proof, re-derived not asserted.** `uv run --locked python
scripts/archive_row_body.py verify` recomputes every digest below and re-splices
these clauses back into the live row, asserting the result hashes to the recorded
pre-relocation digest. See that module's docstring for the four legs and for the
one honest limit (a row edited after relocation reports UNPROVEN, never a false pass).

Latest event: **2842 bytes → 1357 bytes** in the row body.

## Event 1 — relocated 2026-08-29

- pre-relocation row body: `afd5a03d581a4a8843c92ba507864c0c05cd1665047621c099de232c7bbd04f8` (2842 bytes)
- post-relocation row body: `7093be926fa771a6ea460462e3f8e72ffa856add94d51136b95047d3aff63c4a` (1357 bytes)
- pointer already present before this event: false
- clauses relocated: 2

### Clause 1.1 — sha256 `d0d550ed9f81695820666564cef0faf2791af04d77e6115fbd02e613153a247e` (1472 bytes)

````text
**RE-CUT 2026-08-28** (K4). **Leg (a) CLOSED on a live run, not on the row's assertion.** `propose_closures.py` was run this session over the full history: `2 strong, 202 weak over 4229 commit(s)`, and a grep of the emitted `logs/PROPOSALS-2026-08-28.md` for the two documented STRONG false positives returns **0 occurrences of `[#5]` or `[#77]`**. The STRONG set is two items (`#430`, `#554`), both carrying real `closes [#N]` evidence commits. So the two permanent-suppression targets no longer surface. **One sub-clause of leg (a) is NOT met and travels to the remainder rather than being waved through:** *"pinned by a test seeding each"* — there is no such test, and it belongs with (b)'s rework anyway, since a suppression with no test is a behaviour that can silently regress. **REMAINDER: legs (b) + (c).** (b) rework the WEAK heuristic away from bare file-churn — the live number makes the case sharper than the row's own 49:0 framing did: **202 WEAK** against 2 STRONG, because any task naming `audit.py` / `PLAYBOOK.md` / `BACKLOG.md` is flagged every arc by construction; candidates unchanged (require a closes-adjacent id reference, scope WEAK to non-churn files, or drop WEAK and keep STRONG-with-FP-suppression). (c) the hub↔plugin closure-script parity test, absorbed from #211. Done-when narrows to: the WEAK ratio measured again after the rework and recorded in the closing commit, with tests including the FP-suppression pins carried over from (a)
````

### Clause 1.2 — sha256 `827e0ceb570c20ee5d1aecd2962d7aca4b85abf5aa549f19629695b3b258e7a0` (57 bytes)

````text
un-deferred from the 2026-08-27 icebox sweep by this lane
````
