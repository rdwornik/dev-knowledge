---
id: "[#610]"
row: tasks/610-the-night-batch-protocol-named-with-its-two-verb.md
record: row-body-archival
schema: 1
events: 1
pointer: "**Archived annotations:** `tasks/archive/610.md`"
---

# Archived row annotations — [#610]

Dated-amendment narration relocated **verbatim** out of `tasks/610-the-night-batch-protocol-named-with-its-two-verb.md` by
`scripts/archive_row_body.py` under `[#612]`. Nothing was trimmed, summarised or
rewritten; the row keeps the path-qualified pointer

> **Archived annotations:** `tasks/archive/610.md`

and this file keeps the narration. Ruling **B1** (`protocols/STANDING_RULINGS.md`,
"trim-vs-disposition") records that dropping the dated-amendment narration is the
drain that works on this class — relocation is that drain performed without the loss.

**Proof, re-derived not asserted.** `uv run --locked python
scripts/archive_row_body.py verify` recomputes every digest below and re-splices
these clauses back into the live row, asserting the result hashes to the recorded
pre-relocation digest. See that module's docstring for all five legs (A-E) and for
the honest limits — a row edited after relocation reports UNPROVEN rather than a
false pass, and deletion is made detectable by the declared counts, not impossible.

Latest event: **5003 bytes → 2496 bytes** in the row body.

## Event 1 — relocated 2026-09-14

- pre-relocation row body: `8d3b7c46adbf60839b7d5bb1a2de0a953eb950d01566c95bbf5d7efcd3538ab2` (5003 bytes)
- post-relocation row body: `8d8dd1f0da350d65c5644b2be9a0644e60c3e4d20c769c63dff738587c38d821` (2496 bytes)
- pointer already present before this event: false
- clauses relocated: 1

### Clause 1.1 — sha256 `3fa25809b3e51899679b395b2e8a782a30b7b8304a29f98104eddcc8386d5d03` (2555 bytes)

````text
**AMENDED 2026-08-29 (batch lane `pn-filings`, filing pass) — LEG 3 IS DISCHARGED and five other legs were already LANDED; the row is deliberately NOT closed here, closure is `/review-closures`' call.** Verified live on the operator host rather than relayed: `~/.dispatch-helpers/Modules/DispatchHelpers/DispatchHelpers.psm1` aliases `Harvest-Cloud` → `Save-CloudSessionReport` and `Dispatch-After` → `Start-DispatchAfter`, exports both plus `Get-DispatchAfterVerb` and `Get-CloudSessionEventText` in one `Export-ModuleMember` block (cite the block, never a count), carries `.EXAMPLE` usage lines for each, records the API surface `GET /v1/code/sessions/{cse_id}/events?sort_order=asc[&cursor=<next_cursor>]`, and states above the alias block that neither verb is a fourth substrate — `Dispatch-After` is a SCHEDULE around the three ruled `Dispatch` verbs, which is exactly the *form-of, not rival* clause leg 3 asks for. **Per-leg verdict against `protocols/PLAYBOOK.md` "The night batch — the batch protocol run unattended, in five phases":** legs 1, 2, 4, 5 and 6 are all LANDED there (phases 1–5 each carry *Inputs. / Outputs. / Refusal conditions.*; "The harvest-manifest shape — specified so two seats produce the same sections" with the 2026-08-27 manifest named as its reference instance; "Byte-identical, or state the deviation" citing C1 and C3; "Report selection — and the Stop-hook-noise trap"; Phase 5's "The ledger is a REQUIRED output of the protocol, not an optional one"). **Genuinely open, and none of it is in the Done-when:** that section's own "Honest limits" paragraph still calls the two verbs *"the other half of row `[#610]`"* — now false, and a `protocols/**` write outside this lane's scope, so it is OWED; and no gate reads any of the protocol, so it stays a recorded practice at n=1. **A stale absence claim, recorded here because audits are immutable:** `docs/audits/2026-08-28-technical-batch-2-manifest.md` D-B1, `docs/audits/2026-08-28-technical-batch2-launch-contracts/NB2-LANE-B-610-dispatch-verbs.md`, `docs/audits/2026-08-29-verification-night-mission-close-packet.md` D-B1 and `JOURNAL.md` each state `next_cursor` *"appears nowhere in the module"*. True at the 2026-08-28 freeze, stale now: the module carries eight occurrences and its own comment scopes the claim — *"`next_cursor` appears nowhere in this module **before this function**"* — because lane B then wrote the loop that finding dispatched. Superseded by their own consequence rather than mis-scoped; none of the four may be edited.
````
