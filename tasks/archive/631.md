---
id: "[#631]"
row: tasks/631-a-freeze-cannot-bind-a-rule-that-postdates-it.md
record: row-body-archival
schema: 1
events: 1
pointer: "**Archived annotations:** `tasks/archive/631.md`"
---

# Archived row annotations — [#631]

Dated-amendment narration relocated **verbatim** out of `tasks/631-a-freeze-cannot-bind-a-rule-that-postdates-it.md` by
`scripts/archive_row_body.py` under `[#612]`. Nothing was trimmed, summarised or
rewritten; the row keeps the path-qualified pointer

> **Archived annotations:** `tasks/archive/631.md`

and this file keeps the narration. Ruling **B1** (`protocols/STANDING_RULINGS.md`,
"trim-vs-disposition") records that dropping the dated-amendment narration is the
drain that works on this class — relocation is that drain performed without the loss.

**Proof, re-derived not asserted.** `uv run --locked python
scripts/archive_row_body.py verify` recomputes every digest below and re-splices
these clauses back into the live row, asserting the result hashes to the recorded
pre-relocation digest. See that module's docstring for all five legs (A-E) and for
the honest limits — a row edited after relocation reports UNPROVEN rather than a
false pass, and deletion is made detectable by the declared counts, not impossible.

Latest event: **3687 bytes → 2488 bytes** in the row body.

## Event 1 — relocated 2026-09-14

- pre-relocation row body: `652d766bf70625ea1de7eee5577b7f88b2ec6fb9e1191c7f27e92026334aac19` (3687 bytes)
- post-relocation row body: `c3309cca7b1e38a13a514f2898e12b7ec9dc3ca336463d3a099a0f3ef651decc` (2488 bytes)
- pointer already present before this event: false
- clauses relocated: 2

### Clause 1.1 — sha256 `7ebe10aff2bacb22c53150f517dff3307b7b5042db54913e5fdd4075041c03ae` (1190 bytes)

````text
**RULED MECHANISM (operator, ADR-108 §A, 2026-09-01) — THE CONTRACT'S ROUTING TABLE IS THE SINGLE SOURCE OF MODEL TRUTH.** Three surfaces default to `opus` independently today and none checks another, which is how five batch-E lanes ran opus with every committed surface reporting otherwise. The ruled shape, three legs: **(i)** `DispatchHelpers` **drops its `[string]$Model = 'opus'` default (line 96) and READS the contract** — a default that silently outranks a frozen declaration is the defect, not a convenience; **(ii)** `gen_lane_contract` **asserts contract-model == dispatch-model**, exactly as it already does for the dispatch VERB, whose refusal text is the precedent in its own words — *"two forms free to disagree is the class this generator removes"*, closed for the verb and left open for the model; **(iii)** the ledger's model column **reads the `.jsonl` turn witness, never the declaration** — and `[#615]` is what would make that witness a repo surface rather than a per-machine one, since `~/.claude/projects/` is outside the repo and is not a governance surface. Evidence and the per-lane witness: `docs/audits/2026-09-01-verification-model-routing-witness.md`
````

### Clause 1.2 — sha256 `53250228cc7a2aad17c99eb0da75c953ddf756f8c2c0ef6fcfc1016e77791a7b` (53 bytes)

````text
source: operator ruling 2026-09-01, at the batch-F GO
````
