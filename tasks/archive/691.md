---
id: "[#691]"
row: tasks/691-provider-routing-is-an-unordered-role-set-with-no-admission-gate.md
record: row-body-archival
schema: 1
events: 1
pointer: "**Archived annotations:** `tasks/archive/691.md`"
---

# Archived row annotations — [#691]

Dated-amendment narration relocated **verbatim** out of `tasks/691-provider-routing-is-an-unordered-role-set-with-no-admission-gate.md` by
`scripts/archive_row_body.py` under `[#612]`. Nothing was trimmed, summarised or
rewritten; the row keeps the path-qualified pointer

> **Archived annotations:** `tasks/archive/691.md`

and this file keeps the narration. Ruling **B1** (`protocols/STANDING_RULINGS.md`,
"trim-vs-disposition") records that dropping the dated-amendment narration is the
drain that works on this class — relocation is that drain performed without the loss.

**Proof, re-derived not asserted.** `uv run --locked python
scripts/archive_row_body.py verify` recomputes every digest below and re-splices
these clauses back into the live row, asserting the result hashes to the recorded
pre-relocation digest. See that module's docstring for all five legs (A-E) and for
the honest limits — a row edited after relocation reports UNPROVEN rather than a
false pass, and deletion is made detectable by the declared counts, not impossible.

Latest event: **10515 bytes → 8662 bytes** in the row body.

## Event 1 — relocated 2026-09-14

- pre-relocation row body: `9a7b3da16f5ae813f6d3f09d24d653e94badc0a5feb570ec8533c85e13a14955` (10515 bytes)
- post-relocation row body: `5350774a715c8ebd082a5f4d1108861e3e3e38b277ac876777ac0646eba7384f` (8662 bytes)
- pointer already present before this event: false
- clauses relocated: 2

### Clause 1.1 — sha256 `26e309b5456a74aecac2845990d0bce13cbeb25c0e83617b6e177b6eb60a3346` (115 bytes)

````text
source: `to-cc/AMEND-BATCH-W-004.md` AW4-1 row 2, filed 2026-09-11 by lane `lane-w-000-three-decisions-become-rows`
````

### Clause 1.2 — sha256 `fb4e0c969eb57dfe704a6dc67e6d8536fcc89f91bd2511ca1df5c8d3ccafc029` (1782 bytes)

````text
**HALF A MERGED 2026-09-12 at `7a380153`; the row stays OPEN for Half B** (AX23-2). **Two HIGH findings from the integration Codex review ride this row into Half B** — reviewer `gpt-5.6-terra`, effort low, `TALLY HIGH:2 MED:0 LOW:0`, run at the lane tip `dc0eb7de`. Both were VERIFIED against the code rather than taken on the reviewer's word, and both are LATENT today because `scripts/provider_router.py` has **zero production callers** — only its own CLI and tests. (1) **`provider_router.py` routes to NON-ADMITTED providers for every role but `implement`.** `ADMISSION_GATED_ROLES` is a frozenset of one, so the admission gate fires only on `implement` unless an entry opts in with `requires_admission`. Confirmed by running it: `route('review', '.dev-knowledge')` returns `openai`/`gpt-5.6-terra` first and `route('read', '.dev-knowledge')` returns `antigravity`, neither admitted. The code states the scoping deliberately in its own refusal text, so this is a DECLARED bound rather than a bug — but it is the same shape AX24-2 refuses elsewhere (enforcement declared without enforcement), and Half B is where non-Claude providers actually get routed, so the bound has to be ruled there rather than inherited silently. (2) **The AX22-2 self-review check compares across two namespaces.** `_refuse_self_review` compares `reviewed_by` to `response_model or request_model` as raw strings, so a PROVIDER-valued `reviewed_by` never equals a MODEL-valued producer: `reviewed_by='openai'` against `response_model='gpt-5.6-terra'` passes, though `openai` is precisely the provider that produced it. The registry already carries the provider→model mapping the check would need. Half B is when a non-Claude provider first reviews anything, which is when this stops being latent.
````
