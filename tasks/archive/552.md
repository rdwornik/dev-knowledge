---
id: "[#552]"
row: tasks/552-window-close-disposition-and-archival-routine.md
record: row-body-archival
schema: 1
events: 1
pointer: "**Archived annotations:** `tasks/archive/552.md`"
---

# Archived row annotations — [#552]

Dated-amendment narration relocated **verbatim** out of `tasks/552-window-close-disposition-and-archival-routine.md` by
`scripts/archive_row_body.py` under `[#612]`. Nothing was trimmed, summarised or
rewritten; the row keeps the path-qualified pointer

> **Archived annotations:** `tasks/archive/552.md`

and this file keeps the narration. Ruling **B1** (`protocols/STANDING_RULINGS.md`,
"trim-vs-disposition") records that dropping the dated-amendment narration is the
drain that works on this class — relocation is that drain performed without the loss.

**Proof, re-derived not asserted.** `uv run --locked python
scripts/archive_row_body.py verify` recomputes every digest below and re-splices
these clauses back into the live row, asserting the result hashes to the recorded
pre-relocation digest. See that module's docstring for all five legs (A-E) and for
the honest limits — a row edited after relocation reports UNPROVEN rather than a
false pass, and deletion is made detectable by the declared counts, not impossible.

Latest event: **5036 bytes → 4136 bytes** in the row body.

## Event 1 — relocated 2026-09-14

- pre-relocation row body: `f783c02b4472936bd334f85915c10f517503271ee00df3dc06b1a199f23c122e` (5036 bytes)
- post-relocation row body: `528445b83b852e7ed7e55e4f5c8bab883687222266fec3edc1894cf5b102b8ab` (4136 bytes)
- pointer already present before this event: false
- clauses relocated: 1

### Clause 1.1 — sha256 `635211a3be2d007d25db63e6b4a2dde19ea75077f7610ae6b06f49cb28240694` (948 bytes)

````text
**FIRST PARTIAL DISCHARGE 2026-09-05 (R5/B3+B4):** `docs/audits/2026-09-05-technical-r5-funnel-disposition-ledger.md` dispositions the 13 `funnel_coverage` artifacts that survived the B1 manifest-link hotfix, plus itself. This row CITES that ledger deliberately and not decoratively: `consumer_at_landing`'s pool is `tasks/`, `docs/decisions/`, `docs/intake/`, `protocols/` — `docs/audits/` is outside it, so a ledger cannot consume ITSELF for that organ the way it can for `funnel_coverage`, and without a pool-side citation the disposition mechanism adds one unconsumed artifact every time it is used. That is the same recursion the ledger's self-discharge rule closes on the other organ, closed here on this one, by the route B1 named: the owning row cites its own end-of-lane artifact. Legs (a) and (b) are still unbuilt; this discharges only the "one window closes with every new audit dispositioned" clause of the Done-when, for one window.
````
