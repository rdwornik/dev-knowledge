---
id: "[#937]"
title: "Wave 4a lane 1 (lane-known-reds) -- one base run per batch, so a merge takes minutes instead of an hour"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#937] [P1][M] **Wave 4a lane 1 (lane-known-reds) -- one base run per batch, so a merge takes minutes instead of an hour** - filed by LANE-W4-1 per `to-cc/DECLARE-WAVE4A-2026-09-22.md` hard precondition 3 (rows for lanes 1-5 exist before lanes 2-5 fire; R-W4-2, R-W4-9); frozen contract `LANE-W4-1-known-reds.md`, branch `worktree-lane-known-reds`, a wave-4a lane closing the loop `[#929]` evaluated. Value: In wave 3 every merge cost the integrator about an hour, most of it running main's tests twice per lane to tell old reds from new. Main has ~50 reds that nobody owns. After this lane the integrator runs the base once per batch and each lane is compared in minutes. Measure: median integrator time per lane after you land, target 15 minutes (DECLARE G1). · Done when: carried verbatim from the frozen contract -- 1. **Rows first.** File one row per wave-4a lane (1-5) with each lane's Done-contract verbatim, in one commit, before any other change. Close nothing (R-W4-9). 2. **Registry mode.** `test_pairing.py` gains a batch-level registry: `record-base` runs the selected tests on a given commit once and writes the red set with its skip count; `compare` runs the merged tree and classifies against the registry (pre-existing / lane's / newly red / flake on rerun). The registry is a machine-written per-batch artifact; put it where L1 put the receipts (`RECON-NIGHT-2026-09-20.md` §2 shape (a)), citing that choice. 3. **Skip-count guard.** A comparison whose skip counts differ from the registry's is refused as unattributable and says so; the integrator found this blind spot on 2026-09-20. 4. **All-digit sha.** The preflight extractor accepts a short sha made only of digits; row #936's defect is closed by a test that uses one. 5. **Same isolation as before:** a clone, not a worktree, for the base run; reason stated in the module. 6. **Tests (common rules):** registry written once and reused; a red in both is pre-existing; a red only in the merged tree is the lane's; skip mismatch is refused; the all-digit sha passes; synthetic repositories only, `-n 2`. 7. **Codex terra review** with its consumer cited; **pre-handback self-check**; **handback**. · refs `LANE-W4-1-known-reds.md`, `docs/audits/2026-09-22-codex-lane-known-reds.md` (this lane's terra review), `to-cc/WAVE4-COMMON-2026-09-22.md`, `to-cc/DECLARE-WAVE4A-2026-09-22.md` · kill-candidates: none -- no open row covers this lane's work
