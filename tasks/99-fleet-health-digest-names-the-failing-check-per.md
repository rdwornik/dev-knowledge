---
id: "[#99]"
title: "FLEET-HEALTH digest names the failing check per red repo"
status: closed
priority: P3
size: S
theme: "[E2] Enforced governance"
story: "[S4] Extend structural validation to more governance artifacts"
generates: BACKLOG.md
---

- [#99] [P3][S] FLEET-HEALTH digest names the failing check per red repo — today the digest shows `corp-monorepo !! 1 fail` with no check name; the operator needs a second command (`audit.py repo <name>`) to learn WHAT is red · Done when: a red repo's digest line carries the failing check name(s) · refs ADR-76, #85, ADR-36 · DEFER — the 45-day icebox sweep of 2026-08-27 (C4 §3, ruling X1's final step, night-harvest governance session). Peg: **no row-level dependency is claimed** — this is an ATTENTION decision, not a blocked-by. Un-defers when an arc claims the row or the operator re-prioritises it. Recorded explicitly so this row is never mistaken for one waiting on another row, which is the failure the same session's re-peg step was fixing. · **CLOSED 2026-08-28** ([#99], K4 DO-IT — the census framed this as "DO IT, or kill it; only you can say if it still annoys", and the operator ruled DO-IT). Done-when met: a red repo's digest line now carries the failing check name(s). `_load_state_yaml` gained `_check_status_pairs`, which pairs each finding's `status:` with the `check_name:` above it — order-based, matching how the state writer emits findings, and robust to the WRAPPED multi-line `evidence:` values live state files actually contain (pinned by a test). `failing_check_names` renders them into a new **Failing checks** column, capped at 3 with an honest `(+N more)` tail — the cap never drops a failure silently. `repo_summary`'s 4-tuple arity is deliberately UNCHANGED (it is consumed positionally by `build_digest` and by the tests), so the names travel as a separate read rather than as a ripple. **Live proof against the real `ecosystem/` states, not a fixture:** `ai-council !! 2 | adr_status_grammar, substrate_declaration` · `corp-ops !! 1 | substrate_declaration` · `win-tooling !! 2 | dot_prefix_discipline, canonical_freshness` — the operator no longer needs `audit.py repo <name>` to learn WHAT is red, which was the row's whole complaint. Six tests added (pairing across wrapped evidence, failures-only, empty-when-green, cap-counts-the-overflow, unnameable-status-attributed-not-dropped, and legacy states with no `checks` key still rendering); `tests/test_fleet_health.py` 136 passed
