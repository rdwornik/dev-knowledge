---
id: "[#786]"
title: "Give the PLAYBOOK order-conventions heading a gate"
status: open
priority: P2
size: S
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#786] [P2][S] **Give the PLAYBOOK order-conventions heading a gate** - `protocols/PLAYBOOK.md:1399` "### Order conventions" declares `LESSONS.md`, `JOURNAL.md` and `logs/TOKEN-LOG.md` newest-first prepend / append-only, and until this row nothing refused a violation: the rule was carried by that heading, by ADR-29, by CLAUDE.md §5 rule 1, and by each file's own header line *"Never edit old entries. Never delete."* - four prose carriers and zero organs. It was breached anyway, by seats that had read the prose. Measured with `git log --numstat -- LESSONS.md`: `eb08075c` 2026-05-16 (155+/157-, "reorder pre-convention tail"), `99a104ed` 2026-05-14 (20+/20-, "rewrite 9 same-day LESSONS to canonical schema"), `febafcac` 2026-04-28 (7+/25-, "standardize entry format"). `scripts/hooks/block_immutable_edits.py` does not reach them - its zone is `docs/decisions/transcripts/**` and its own docstring states it matches the Edit/Write TOOLS only, so a shell write is uncaught - and no commit-time organ existed at all. Landed as `scripts/validate_prepend_order.py` + `tests/test_validate_prepend_order.py`, wired as the `prepend-order` pre-commit hook: with `tail(x)` = the text from the first dated entry heading onward, the predicate is `tail(staged).endswith(tail(HEAD))`, which refuses the append-only half (an edited/deleted entry breaks the suffix) and the newest-first half (a bottom-append or mid-insert breaks it too, with nothing deleted) in one byte-exact test, while leaving the header block above the first dated heading free so a `Last updated:` bump stays legal. The ADR-29 amend. 2026-07-17 archival carve-out is honoured only in its sanctioned byte-identical shape · Done when: the three residuals the gate's own docstring names are ruled rather than inherited - (a) the `Immutable (dated)` class at the SAME heading (ADRs, transcripts, handoffs, audits) is either given a commit-time organ or recorded as deliberately covered by `block_immutable_edits.py` + `validate_hermetization` with the shell-write hole named, (b) `GUARDED` is ruled to be a roster rather than shape detection, or a registration step is added to whatever creates a new dated log, and (c) the merge-time hole (`MERGE_HEAD` present => skip; a `--no-ff` merge runs zero pre-commit hooks) is either closed at the integrator seat's checklist or accepted in writing · refs `protocols/PLAYBOOK.md:1399`, `scripts/validate_prepend_order.py`, `tests/test_validate_prepend_order.py`, `.pre-commit-config.yaml` (`prepend-order`), ADR-29 + its 2026-07-17 amendment, CLAUDE.md §5 rule 1, `scripts/hooks/block_immutable_edits.py` · source: batch AA lane aa-4, measured on main `5b880b03`
