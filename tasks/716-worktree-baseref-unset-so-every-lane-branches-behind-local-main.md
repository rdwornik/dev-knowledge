---
id: "[#716]"
title: "`worktree.baseRef` is unset, so every lane branches from `origin/main` and starts behind local `main` -- the mandatory step-0 sync exists to paper over it"
status: closed
priority: P1
size: S
theme: "[E7] Tooling & evaluation"
story: "[S18] Cut session friction with better tooling"
generates: BACKLOG.md
---

- [#716] [P1][S] **`worktree.baseRef` is unset, so every lane branches from `origin/main` and starts behind local `main` -- the mandatory step-0 sync exists to paper over it** - Third of AX7-3's three measured dispatch defects, each filed as its own FILE row with a RED-first test. Carried at `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93) Part 5. · **SETTING NAME VERIFIED BY THIS LANE, because AX7-3 asked for exactly that** ("CC verifies the setting name") rather than asking for it to be taken on trust. The setting is `worktree.baseRef`. It is **unset** in this repo's `.claude/settings.json` and in the operator's user settings, both checked. Its documented values are `fresh` -- the DEFAULT, which branches from `origin/<default-branch>` -- and `head`, which branches from the current local HEAD. AX7-3's diagnosis holds: unset means `fresh` means `origin/main`. · **ONE PRECISION THE FIX NEEDS, and it is why this row says more than "set it".** AX7-3 asks that baseRef "points at local `main`", and the available value is `head` -- the DISPATCHING SESSION's current local HEAD, not literally `main` HEAD. The two coincide only when the dispatcher is itself on `main`. A dispatcher sitting on a feature branch would, with `head`, seed every lane from THAT branch instead, which is a different defect wearing the fix's clothes. So the Done-when asserts the PROPERTY -- a lane's base equals `main` HEAD at dispatch -- and lets the setting be whatever makes the property true. · **The cost is measured on this lane itself, which is as close to a reproduction as a row gets.** This lane branched behind and synced TWICE: once at step 0 as its contract mandates, and again mid-run when batch W's `a3374b70` landed. Each sync is a merge commit that exists only because the base was wrong. · **THE STEP-0 SYNC STAYS MANDATORY UNTIL THIS LANDS**, and AX7-3 says so in as many words. That is the standing consequence this row carries: no lane contract may drop its step-0 sync on the grounds that baseRef is being fixed -- it drops only once the test below is green. The reason is not ceremony: a generator run against a base that lags `main` silently DROPS rows that exist on `main`, and the dropped row looks like a clean regeneration. · **AX12-1 CARRIED INTO THIS ROW by lane `lane-x-716-dispatch-defects`, 2026-09-11 (the AW-2 / `[#680]` pattern -- a clause addressed to a row does not stay SAID).** **AX9-4 (exists-before-build) is DISCHARGED BY REUSE rather than by a quote excusing a build: this lane creates NO organ, script, hook or doc.** `graph_queries.py process-list --render` was run live on 2026-09-11 and returned `scripts/gen_lane_contract.py` (triggered by `.pre-commit-config.yaml`), `scripts/gen_handoff.py` (triggered by `scripts/assemble_paste.py`) and `scripts/worktree_seed.py` (no trigger; ON-DEMAND-BY-OPERATOR, invoked by /lane-boot) -- three existing processes that already answer all three needs, so every fix below lands INSIDE one of them. **AX4-1 (floor declaration) is reported, not filed:** no `tasks/*.md` row in the tree carries a `floor:` key today (measured: zero of the open rows), `ecosystem/parity-surfaces.yaml` carries no lane-contract or dispatch-defect surface, and AX4-1 names the `decision_coverage` lane (X1-1) as its owner and the parity registry as its carrier. Adding an unvalidated frontmatter key ahead of its carrier would put this row outside the ADR-66 schema `validate-backlog` gates, so this lane declares its floor in PROSE here -- **floor: MUST**, because all three defects sit in the deployed methodology corpus's own generator and every consumer that dispatches a lane inherits them -- and reports the unfiled key to X1-1 rather than editing a registry it does not own. **AX7-5 stands until this row's sibling `[#717]` is green:** `-Model` explicit at the dispatch act, cheapest-admitted = sonnet. · **This lane's own base is the reproduction, measured rather than argued.** At step 0 on 2026-09-11 the worktree branched at `78d99d55` while local `main` stood at `0be08b3c` -- SEVEN commits behind, fast-forwarded by the mandatory sync. `worktree.baseRef` was re-verified unset in BOTH `.claude/settings.json` (no `worktree` key at all) and the operator's `~/.claude/settings.json` (keys read: no `worktree`), so the documented default `fresh` = `origin/<default-branch>` holds and the gap is exactly local-main-ahead-of-origin. **The step-0 sync's retirement surface is narrower than the Done-when assumes, and this lane reports that rather than inventing one:** the sync region is hand-authored into each frozen contract under `docs/audits/2026-09-11-technical-batch-x-launch-contracts/` (immutable) and appears in NEITHER `templates/prompt-template.md` v1.15, NOR `.claude/commands/lane-boot.md`, NOR `protocols/PLAYBOOK.md`, NOR `scripts/gen_lane_contract.py::render_contract` -- grepped for `fetch origin` / `sync before anything else` across all four. This lane therefore makes the retirement MECHANICAL instead of editorial: the generator gains the step-0 region CONDITIONED ON the very predicate the test asserts, so a contract emitted while the base property is unheld carries the sync and one emitted while it holds does not -- retired by the same change, and self-restoring if the setting is ever unset again. The predicate lands in `scripts/worktree_seed.py`, the existing worktree-provisioning organ, with its witness in `tests/test_worktree_seed.py`. · Done when: a lane's base equals `main` HEAD at dispatch, asserted by a TEST rather than by the setting's value -- so a dispatcher on a non-`main` branch cannot satisfy it accidentally; the RED-first witness FAILS against today's unset configuration before the fix makes it green; and the mandatory step-0 sync is retired from the lane-contract template only in the same change that makes that test green · **Done-when authored by the filing lane** from AX7-3's clause, with the `head`-versus-`main`-HEAD precision added from the verification AX7-3 asked for · refs `docs/intake/2026-09-11-tech-batch-x-roster.md` (intake 93, Part 5 AX7-3), `to-cc/AMEND-BATCH-X-ROSTER-007.md` AX7-3, `to-cc/QUESTION-path-registry-2026-09-11.md` (fix 3 of the three), `.claude/settings.json` (checked: no `worktree.baseRef` key), `[#715]` (the path row these three ship first for), `[#716]`'s siblings `[#717]` and `[#718]`, `[#429]` (worktree provisioning is not portable across the fleet) · kill-candidates: none -- `[#429]` owns whether worktree provisioning works across MACHINES and is silent on which ref a lane branches from; no open row names `baseRef` · source: `to-cc/AMEND-BATCH-X-ROSTER-007.md` AX7-3, id assigned by the operator instruction of 2026-09-11, filed by lane `lane-x-000-batch-x-roster-lands` · CLOSED 2026-09-12 — operator closure word, `to-cc/AMEND-BATCH-X-ROSTER-024.md` AX24-5. Evidence per `[#730]`. **Done-when:** *"a lane's base equals `main` HEAD at dispatch, asserted by a TEST rather than by the setting's value — so a dispatcher on a non-`main` branch cannot satisfy it accidentally; the RED-first witness FAILS against today's unset configuration before the fix makes it green; and the mandatory step-0 sync is retired from the lane-contract template only in the same change that makes that test green"*. **Merged:** `eccb5814` (lane-5, fix commit `acef1832`). **Witness:** `tests/test_worktree_seed.py::test_A_LANES_BASE_EQUALS_MAIN_HEAD_AT_DISPATCH` — named for the clause it asserts — with `test_head_off_main_is_reported_as_the_ACCIDENTAL_satisfaction_the_row_warns_about` covering the accidental-satisfaction half the Done-when singles out, and `tests/test_gen_lane_contract.py::test_a_contract_OMITS_the_step_0_sync_region_once_the_property_holds` the same-change retirement of step-0 sync.
