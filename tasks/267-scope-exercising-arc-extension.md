---
id: "[#267]"
title: "Scope-exercising arc extension"
status: open
priority: P2
size: S
theme: "[E2] Enforced governance"
story: "[S8] Make hub enforcement reach consumers, not just the hub (enforcement-transfer mesh)"
generates: BACKLOG.md
---

- [#267] [P2][S] Scope-exercising arc extension (REFINEMENT, root-ratified 2026-07-06 — not a closure gate; armed-as-enforcing NOT adopted as doctrine) — the ai-council FULL-COVERAGE measurement left `hub-toc-hooks` + `floor-hash-verify-hook` ARMED-BUT-SKIPPED: wired + consulted, but the arc's single-file commit never matches their `files:` scopes, so firing stays unwitnessed. Extend the arc with an in-scope edit to witness both FIRED (a floor edit must FAIL the hash gate — that block IS the firing), and encode the scope-conditional expectation in their manifest `engages:` entries. **Block 5: half-b (scope condition + observer FIRED tests) DONE; half-a (live re-measurement) DEFERRED — design decision, feasibility proven; measurement-4 memo.** · **Mechanism RATIFIED 2026-08-28 (architect, option (b); register `protocols/STANDING_RULINGS.md` Y-1): (iii) instruct-the-child for the n=1 attended witness — ALONE.** Part (ii) (discover-from-config at fleet scale) is **CUT from this row**, not deferred: it was coupled to P6, and P6 is unowned (`[#221]` closed at `8aab4356` with no successor). Deferring onto a peg that does not exist is the **dead-peg class** the 2026-08-27 ledger measured when it had to re-peg **nine** rows with spent or dead pegs (`docs/audits/2026-08-27-technical-night-harvest-consumption-ledger.md` §D item 3). The fleet-scale half is recorded as an **ADR-111 §1(c) CANDIDATE** — no peg, no owner, by design — routed at the next intake pass (Y-2). · Done when: **one attended consumer measurement shows both components FIRED under a scope-matching edit**, via (iii). The second leg is **already met and was verified on disk 2026-08-28, not assumed**: both `engages:` entries carry their scope condition, each citing this row — `deploy/manifest-v1.4.0.yaml` `hub-toc-hooks` (*"file-scoped: FIRED iff an edit matches the consumer's toc-freshness `files:` pattern, else ARMED-BUT-SKIPPED"*) and `floor-hash-verify-hook` (*"FIRED (and FAILING) iff a floor edit is staged"*) · refs deploy/lived_sandbox/arc.py (ARC_PROMPT), deploy/manifest-v1.2.0.yaml, docs/audits/2026-07-06-ai-council-measurement-3.md, #238 · **RULED 2026-08-28 (architect), closing the HOLD this row carried for one day.** The K4 pass declined to ratify the two-part LEAN because part (ii) was coupled to an unowned P6, and returned the fork rather than deciding it; the architect took **option (b)** — cut (ii), keep (iii). Full HOLD text, with the contradiction as first argued, condensed to git per ADR-49/65 (`git log -p -- tasks/267-scope-exercising-arc-extension.md`); the ruling and its generalisable dead-peg rule are at `protocols/STANDING_RULINGS.md` **Y**. One tension the HOLD raised is left standing deliberately, because the ruling does not settle it: this row's header calls itself *"REFINEMENT … not a closure gate"* while the K4 table treated it as a decision row — not irreconcilable, but it should not be read as both · un-deferred from the 2026-08-27 icebox sweep by this lane so the decision is visible rather than iceboxed
