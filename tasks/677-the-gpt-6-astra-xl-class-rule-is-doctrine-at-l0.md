---
id: "[#677]"
title: "The `gpt-6-astra` XL-class rule is doctrine at L0 with no gate, and exactly ONE codex call site pins a model"
status: open
priority: P2
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#677] [P2][M] **The `gpt-6-astra` XL-class rule is doctrine at L0 with no gate, and exactly ONE codex call site pins a model** - **A one-word edit to an un-versioned, un-carried file silently re-routes every inheriting caller, and nothing in any repo notices.** Measured 2026-09-09: `~/.codex/config.toml` carried `model = "gpt-6-astra"` as the GLOBAL default - the same string `[#676]` measured dying at HTTP 400 (`'gpt-6-astra' model requires a newer version of Codex`) on every caller that inherited it. Pinned to `gpt-5.6-terra` this session, and `~/.claude/ROUTING.md` now carries the XL-class line (ruling-class / adversarial only, explicit `-c model=` opt-in, never default / lane / review / fan-out) at `dot-claude@ac5f918`. **The census is the finding.** Exactly one EXECUTING caller pins the model: `~/.claude/bin/codex-review.ps1:218-220` (`$reviewModel = 'gpt-5.6-terra'` feeding `-c model=$reviewModel`), covering both the code and doc lanes. **Every other invocation shape on disk INHERITS whatever `config.toml` holds** - `protocols/PLAYBOOK.md:5785` (ad-hoc `codex exec` for one-off reads outside a diff), `protocols/PLAYBOOK.md:5795` (the sanctioned interim producer-lane design prompt), `docs/decisions/ADR-85-session-lifecycle-enforcement.md:107` (`codex exec -s read-only`, brief only), and three `~/.claude/skills/gotchas/gotchas.md` lines (`:14` verify, `:333` fix, `:769` fix). **`:333` is the shape that reads as pinned and is not:** it prescribes `codex exec --sandbox read-only -c model_reasoning_effort=high` - a `-c` flag pinning EFFORT while the MODEL still defaults, so a reader scanning for `-c` sees a pin that does not exist. `ecosystem/provider-registry.yaml:79` carries `version_command: ["codex", "--version"]` and no model at all; that absence is `[#676]`'s and is cited here, not re-filed. **The un-governed surface is named rather than assumed:** `~/.codex/config.toml` is on no manifest - `deploy/` carries `global-instructions-codex.md` to `~/.codex/AGENTS.md`, which is the ROLE layer, not the MODEL layer - so the file that decides which model nine documented shapes resolve to is the one file no carrier, checker or test reads. · Done when: a check asserts **(a)** the Codex default model is not an XL-class id, reading the class list from a data surface rather than a hardcoded string, and **(b)** every executing codex call site either pins `-c model=` / `-m` or is explicitly registered as default-inheriting, so the inheriting set is declared rather than discovered; and **one test FAILS when the default is set to an XL id** - a RED-first witness per ADR-108 section B, proven to bite before the check makes it green · refs `~/.claude/ROUTING.md` (the XL-class line, `dot-claude@ac5f918`), `~/.codex/config.toml`, `~/.claude/bin/codex-review.ps1:218-220`, `ecosystem/provider-registry.yaml`, `protocols/PLAYBOOK.md:5785,5795`, `[#676]`, ADR-108 (section B, RED-first) · kill-candidates: none - `[#676]` owns the per-provider INVOCATION SHAPE (whether the call works at all) and this row owns the MODEL that shape resolves to; the two fail differently and this one survives `[#676]` closing · source: operator act 2026-09-09, from pinning the Codex default off `gpt-6-astra` and grepping every codex caller in-repo and in `~/.claude`
