---
id: "[#960]"
title: "Wave 4b lane 4 (lane-verify-in-lane) -- a lane tests itself in parallel; the integrator only confirms"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
implements: "ADR-120"
generates: BACKLOG.md
---

- [#960] [P1][M] **Wave 4b lane 4 (lane-verify-in-lane) -- a lane tests itself in parallel; the integrator only confirms** - filed by LANE-W4B-0 per `to-cc/WAVE4B-COMMON-2026-09-22.md` rule 1; frozen contract `LANE-W4B-4-verify-in-lane.md`, branch `worktree-lane-verify-in-lane`. Depends on lane 0 (`[#956]`); runs in parallel with lanes 2, 3, 5 after lane 0 merges. Value: median merge was 2h 21min because every merge ran ~93 test files serially at the integrator -- the selector reads the merge commit, which always contains regenerated prose. After this lane the selector reads the lane's own diff, the lane runs its tests in parallel with other lanes and records the result by tree, and the integrator reuses it when main has not moved. The merge queue is measured, not assumed. · implements: ADR-120 · Done when: 1. **Selection from the lane's diff:** a new mode of `impacted_tests.py` selects from `origin/main...<lane>`, excluding files the integrator regenerates at merge (list them from the regeneration step, cited); rows-only changes map to the funnel test (finding 3). The old mode is unchanged (common rule 8). 2. **Record by tree:** `test_pairing.py` gains a mode that runs the lane's selection on `origin/main` + lane, compares against the batch registry, and writes a record keyed by the tree sha and the `origin/main` sha it was run on; a second mode tells the integrator whether a record is reusable (same `origin/main`) or which subset must re-run. 3. **The two registry gaps:** a new conditional skip inside an existing file no longer makes the compare unattributable (finding 2); a new finding inside a test already red is reported, not masked (finding 8) -- or, if a test-level registry cannot see it, say so and name what can. 4. **Merge-queue measurement (no repository setting changes):** for the last ten pushes to `main`, compare the CI workflow's verdict with the local gate list's verdict on the same commit, using `gh`/the GitHub API read-only; record parity, disagreements with their causes, and CI duration. The recommendation -- adopt the native merge queue or not -- goes in the audit record with numbers. 5. **Acceptance tests of FR3** from the plan, red first. 6. **Codex terra review** with its consumer cited; **self-check and purity**; **handback** · refs `LANE-W4B-4-verify-in-lane.md`, `to-cc/WAVE4B-COMMON-2026-09-22.md`, `to-cc/PLAN-WAVE4B-SESSION-2026-09-22.md` (FR3 and "Library-first"), `to-browser/DIGEST-WAVE4-FINAL-2026-09-22.md` (G1 table, findings 2, 3, 8), `docs/audits/2026-09-22-technical-lane-verify-in-lane-merge-queue-measurement.md` (this row's own merge-queue measurement, consumed here), `docs/audits/2026-09-23-codex-lane-verify-in-lane-960-terra.md` (this row's own Codex terra review, consumed here), `[#955]` (the epic this row implements) · kill-candidates: none -- no open row covers lane-side verification
