---
id: "[#689]"
title: "Conductor E is decided and unbuilt — GitHub Actions as the runner, state stays in tasks/, required checks as gates"
status: open
priority: P1
size: L
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#689] [P1][L] **Conductor E is decided and unbuilt — GitHub Actions as the runner, state stays in tasks/, required checks as gates** - Option E of `to-cc/DECLARE-CONDUCTOR-DECISION-2026-09-09.md`, decided on that file's §3 matrix (weighted 58, against A 52, B 55, C 38, D 46) and filed here under `to-cc/AMEND-BATCH-W-004.md` AW4-1 row 1. · **Why the row exists.** The decision was ruled and then scheduled nowhere — the exact failure the operator's standing rule of 2026-09-10 names: *"a decided item must never be left unscheduled; the system — not the browser's memory — guarantees it"* (`to-browser/RATIFICATION-2026-09-10 (1).md`, Standing rule). It is the golden rule applied to a decision rather than to code: nothing is implemented without a task (`to-browser/RATIFICATION-2026-09-10.md` row 4, adopted as `DECLARE-NO-TASK-NO-BUILD-2026-09-09`). A decision that is safe only in a seat's memory is not safe, and the DECLARE itself is not a schedule. · **What E is, from §4, so the row is readable without the transport.** State stays in the repo: `tasks/<row>.md` gains `phase:`, `validate_backlog` knows the enum, ADR-38 untouched. Runner is GitHub Actions: `.github/workflows/conductor.yml`, `on: push · pull_request · schedule · workflow_dispatch`, reading `tasks/`, evaluating the phase gate, performing the phase action — scheduling, durability, retries, logs and UI are GitHub's, and the only custom content is the phase table, which is custom in every option because it is the process. Gates are required checks on `main`: pytest, ruff, seal, terra review, with Codex review a job rather than a paste. Lane executor is `claude-code-action`, reading the task file as its contract and opening the PR; local `claude` is unchanged for when the operator wants to code himself. View is `gh` + Projects, LEDGER rendered rather than written. **Fallback is B** — same task files, same graph, same phase table, run locally by a hook — reversible precisely because state never left the repo. · **§5 is what E deletes, and that is both its value and its risk surface:** dispatcher and integrator as seats, the four dispatch verbs in win-tooling, worktree discipline and the `[#441]` test, SEAT-BOOT's dispatcher/integrator halves, most of PLAYBOOK Ch8, and every "paste this to the integrator" — while KEEPING `tasks/`, BACKLOG as render, the pre-commit hooks, the audit organs and the graph. It retires live organs, so it does not start before its preconditions hold. · **§7's three facts are verified before build, not assumed:** the operator's GitHub plan and required checks on a private repo · Actions minutes quota · `/install-github-app` succeeding on the hub repo. **§8 is the build:** 1. `conductor.yml` + ruleset. 2. `phase:` field + validator. 3. SessionStart hook. · **Sequencing: batch X, after lane 0.** Lane 0 is `[#692]` (`decision_coverage`), which `to-cc/AMEND-SESSION-PLAN-009.md` A9-1 places explicitly *"(before conductor E)"* — this row is one of the decisions A9-1's query must be able to see, so building the query after its first subject would leave the query unwitnessed. **Blocked on the operator's GitHub Pro**, ratified as D2: *"D2 · GitHub Pro — YES (~$4/month, account rdwornik): ... Purpose: required checks as the merge gate for conductor E (GitHub Actions)."* The ellipsis is the lane contract's own and stands for the operator's consent sentence, recorded in Polish and elided here under this lane's Done-contract clause 6 (docs in English); read it from the cited file. **The citation is corrected, and the correction matters.** D2 lives ONLY in `to-browser/RATIFICATION-2026-09-10 (1).md` (1,097 B) — a Drive-style duplicate. The 4,332 B `to-browser/RATIFICATION-2026-09-10.md` carries no D-numbering at all and lists GitHub Pro under **NOT RATIFIED**, so the two same-named transport files disagree on this row's blocker; the duplicate is the later by arrival (mtime 2026-09-11 00:27 against 2026-09-10 12:02). Read the ratification before building the gate leg; do not infer it from this row. · **LANE RECORD, `lane-x-689-conductor-e`, 2026-09-11 — the carried clauses and the three §7 verdicts, written into this row rather than left SAID (AX12-1).** **`AX4-1` verbatim:** *"Floor declaration is mandatory. Every row and every lane contract carries `floor: MUST | hub-only`; a NEW organ, hook, gate, doc or command without it is refused at commit (validated against `ecosystem/parity-surfaces.yaml`; `fleet_parity` is the check). Default is MUST — hub-only requires a one-line reason. Applies retroactively to batch W's outputs and every batch X row: W-7 selector, W-2 guard, `decision_coverage`, routing, STANDING_RULINGS, the docs cut, conductor E — each dispositioned MUST or hub-only before X1 dispatches. Owner: `decision_coverage` lane (X1-1) gains this clause; the parity registry is the carrier."* — **disposition for conductor E: `floor: MUST`**, for both new surfaces (the Actions workflow and the required-check ruleset declaration), which is AX3-2's demand stated in AX4-1's vocabulary. The parity-registry ROW that carries this machine-side is owed by X1-1 and is NOT written by this lane: AX4-1 names `decision_coverage` as the clause's owner, and AX12-1 bars a lane from editing a surface another row owns. **`AX9-4` verbatim:** *"Exists-before-build clause on the decision engine (X1-1) and the floor declaration (AX4-1): a contract that CREATES an organ, script, hook or doc must quote a `process-list` / organ-index result showing no existing organ answers the need; a NEW organ without that quote is refused at the contract gate. This is the RED-first form of 'check the process map before you build'."* — **the quote, live at `0be08b3c`:** `uv run --locked python scripts/graph_queries.py process-list --repo-root .` reports `process-list: 158 processes, 119 triggered, 39 not` and `process-list: OK`; the `--render` walk grepped case-insensitively for `github.?action|workflow|conductor|phase|required.check` returns exactly ONE row — ``- `scripts/fleet_analytics.py` -- triggered by `.github/workflows/report-only-wall.yml` `` — and that workflow's own header declares itself *"REPORT-ONLY FOREVER, not a gate awaiting promotion"*. So no existing process reads a phase field, evaluates a phase gate, or gates a merge: the need is unanswered and the three build legs are new organs, not duplicates. The instruments this lane does NOT re-build, having found them: `scripts/window_metrics.py` is the COMPUTED-not-CLAIMED metric pattern (including its NOT-COMPUTED-with-a-reason discipline) and is reused rather than re-derived. **`AX3-2` verbatim:** *"Conductor E ships to consumers. X1-2's Done-when adds: the Actions workflow and its required-check ruleset are floor components in the deploy manifest, so corp-monorepo (and every consumer) gets them with the floor; the operator sees runs in the repo's Actions tab and checks on every merge."* — landed as a `conductor` carrier plus two components in `deploy/manifest-v1.5.0.yaml`, DECLARATION-ONLY (`implemented: false`), on the v1.5.0 `editor-config` precedent. Declaration-only is not a hedge, it is the operator's own sequencing: `to-browser/RATIFICATION-2026-09-11.md` rules *"No harness deployment to corp-monorepo until batch X's waves are done"*, so a carrier that WROTE into a consumer today would execute a deployment he has deferred. **`AX12-2` verbatim:** *"One role per substrate (PLAYBOOK Ch8 Q1–Q4 still decides; ratification #7 bounds the workstation): CC cloud = read-only and text-only work at any hour · Codespace, attached, never detached = gate-dependent committing work in the daytime · local = committing work overnight only · GitHub Actions replaces the overnight local role once X1-2 lands. Detached Codespace lanes stay barred until the retire-or-fix row decides."* — carried; and its `local = committing work overnight only` leg is ALREADY SUPERSEDED by the operator at ~16:45 on 2026-09-11 (`to-browser/RATIFICATION-2026-09-11.md`: *"Continuous work, no night gating — supersedes ratification #7's timing"*), which is why this lane runs local in the middle of the day without a deviation. The Actions leg of AX12-2 is what the workflow this row builds makes true. **§7's three preconditions — READ live at `0be08b3c`, one verdict each, never inferred.** **P1 · GitHub plan + required checks: PASS.** `gh api user` returns `plan: pro` (`private_repos: 9999`), and `gh api repos/rdwornik/dev-knowledge/rulesets` returns `[]` at HTTP 200 — not the 403 that `to-cc/AMEND-CONDUCTOR-DECISION-001.md` §1 measured on Free. D2 is READ, not inferred, and **its locator in this row is stale**: the 1,097 B duplicate this row cites as `to-browser/RATIFICATION-2026-09-10 (1).md` no longer exists under that name — the same 1,097 B file is now `to-browser/RATIFICATION-2026-09-10-architect.md`, and it carries D2 unchanged: *"D2 · GitHub Pro — YES (~$4/month, account rdwornik) … Purpose: required checks as the merge gate for conductor E (GitHub Actions)"*. Same bytes, same D-numbering, renamed; the 4,332 B `to-browser/RATIFICATION-2026-09-10.md` still lists GitHub Pro under NOT RATIFIED, exactly as this row says. A locator resolution, not an amendment. **P2 · Actions minutes quota: PASS.** The `users/{u}/settings/billing/actions` endpoint the amendment used is HTTP 410 (moved); `users/rdwornik/settings/billing/usage` reports September 2026 = **1,192 Actions minutes**, all on `dev-knowledge`, `netAmount` 0 — against Pro's 3,000/month that is 1,808 of headroom. AMEND-CONDUCTOR-DECISION-001 §3's FIFTH number (minutes per week against the quota) is therefore computable and is instrumented with the other four, even though this row's Done-when carries only four. **P3 · `/install-github-app` on the hub repo: NOT MET, and it gates NO leg of this row's Done-contract.** `gh api repos/rdwornik/dev-knowledge/actions/secrets` returns an EMPTY secret list and `actions/workflows` lists only `report-only-wall.yml` plus the Codespaces prebuild, so the app has never been installed here — which is what AMEND-CONDUCTOR-DECISION-001 §1 already recorded as *"not yet reported"*, so no premise is refuted. What it gates is §4's LANE-EXECUTOR leg (`claude-code-action` reading a task file and opening the PR). §8's build is *"1. `conductor.yml` + ruleset. 2. `phase:` field + validator. 3. SessionStart hook"* — none of the three names the app, and all three are built here with zero dependency on it. It is a live operator act (an interactive OAuth flow no lane can perform), carried to the end packet as this lane's single operator ask rather than PAUSEd against a leg it does not block. **One fact neither §7 nor the amendment anticipated, reported because it changes the gate argument: the repo is PUBLIC.** `gh api repos/rdwornik/dev-knowledge` returns `private: false, visibility: public`. Every artefact this row rests on says private — AMEND-CONDUCTOR-DECISION-001 §2 declines the public-repo option *"no — private, personal"*, `.github/workflows/report-only-wall.yml` states *"the repo is private on the Free tier, so required checks are unavailable"*, and `ecosystem/fleet-shape-spec.yaml`'s `.github` sanction comment reads *"Report-only (private repo, Free tier), so no gate lives there"*. Three surfaces assert a visibility the API contradicts. It is reported, not acted on: visibility is the operator's, and Pro makes P1 PASS either way · phase: build · Done when: the four numbers of `to-cc/DECLARE-CONDUCTOR-DECISION-2026-09-09.md` §6 move, carried verbatim from that section (the source hard-wraps; the wrap is not content) — "1. Phase transitions without operator action ÷ all transitions — today ~0%, target > 80% (Actions logs). 2. Operator hours per feature landed in corp-monorepo — today ∞, target: a number. 3. Operator pastes per week — today dozens, target < 5. 4. Organs deleted from the §5 list — target ≥ 10. If 1 and 3 do not fall, revert to B. No state migration needed." · **This lane's build footprint, named here so every file it adds carries an `implements` edge from this row (the `graph-task-coverage` gate refuses a staged file no OPEN row claims):** `.github/workflows/conductor.yml` (the runner), `scripts/conductor.py` (the phase table, the gate and the §6 numbers — the table is NOT in the workflow, because a gate written in workflow shell is a gate no local test can exercise), `deploy/conductor-required-checks.ruleset.json` (the required-check ruleset, a pure Rulesets-API payload that arrives `enforcement: disabled`), `tests/test_conductor.py` (the RED-first witnesses), plus the `· phase:` clause and its enum in `scripts/validate_backlog.py` and the manifest declaration in `deploy/manifest-v1.5.0.yaml` · refs `to-cc/DECLARE-CONDUCTOR-DECISION-2026-09-09.md` (§3 matrix, §4 decision, §5 deletions, §6 the Done-when numbers, §7 preconditions, §8 build), `docs/audits/2026-09-10-technical-batch-w-launch-contracts/LANE-w-000-three-decisions-become-rows.md` (the in-repo frozen carriage of AW4-1 row 1, byte-identical to the transport contract), `docs/audits/2026-09-10-technical-batch-w-manifest.md` (AMEND-BATCH-W-004's declared carrier, §"AMENDED AFTER DISPATCH"), `to-browser/RATIFICATION-2026-09-10 (1).md` (D2), `docs/intake/2026-09-10-tech-review-consumption.md` (intake #91, the FILE line), `[#669]`, `[#664]`, `[#692]` · kill-candidates: none — `[#669]` owns the four delivery-loop transitions and computes state from spine edges, which is E's phase table only once E exists to fire it; `[#664]` owns the graph E would query; neither names a runner, a gate surface, or the `phase:` field, and neither can close by building `conductor.yml` · source: `to-cc/AMEND-BATCH-W-004.md` AW4-1 row 1, filed 2026-09-11 by lane `lane-w-000-three-decisions-become-rows`
