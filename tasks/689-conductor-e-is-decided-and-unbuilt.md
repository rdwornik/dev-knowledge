---
id: "[#689]"
title: "Conductor E is decided and unbuilt — GitHub Actions as the runner, state stays in tasks/, required checks as gates"
status: open
priority: P1
size: L
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#689] [P1][L] **Conductor E is decided and unbuilt — GitHub Actions as the runner, state stays in tasks/, required checks as gates** - Option E of `to-cc/DECLARE-CONDUCTOR-DECISION-2026-09-09.md`, decided on that file's §3 matrix (weighted 58, against A 52, B 55, C 38, D 46) and filed here under `to-cc/AMEND-BATCH-W-004.md` AW4-1 row 1. · **Why the row exists.** The decision was ruled and then scheduled nowhere — the exact failure the operator's standing rule of 2026-09-10 names: *"a decided item must never be left unscheduled; the system — not the browser's memory — guarantees it"* (`to-browser/RATIFICATION-2026-09-10 (1).md`, Standing rule). It is the golden rule applied to a decision rather than to code: nothing is implemented without a task (`to-browser/RATIFICATION-2026-09-10.md` row 4, adopted as `DECLARE-NO-TASK-NO-BUILD-2026-09-09`). A decision that is safe only in a seat's memory is not safe, and the DECLARE itself is not a schedule. · **What E is, from §4, so the row is readable without the transport.** State stays in the repo: `tasks/<row>.md` gains `phase:`, `validate_backlog` knows the enum, ADR-38 untouched. Runner is GitHub Actions: `.github/workflows/conductor.yml`, `on: push · pull_request · schedule · workflow_dispatch`, reading `tasks/`, evaluating the phase gate, performing the phase action — scheduling, durability, retries, logs and UI are GitHub's, and the only custom content is the phase table, which is custom in every option because it is the process. Gates are required checks on `main`: pytest, ruff, seal, terra review, with Codex review a job rather than a paste. Lane executor is `claude-code-action`, reading the task file as its contract and opening the PR; local `claude` is unchanged for when the operator wants to code himself. View is `gh` + Projects, LEDGER rendered rather than written. **Fallback is B** — same task files, same graph, same phase table, run locally by a hook — reversible precisely because state never left the repo. · **§5 is what E deletes, and that is both its value and its risk surface:** dispatcher and integrator as seats, the four dispatch verbs in win-tooling, worktree discipline and the `[#441]` test, SEAT-BOOT's dispatcher/integrator halves, most of PLAYBOOK Ch8, and every "paste this to the integrator" — while KEEPING `tasks/`, BACKLOG as render, the pre-commit hooks, the audit organs and the graph. It retires live organs, so it does not start before its preconditions hold. · **§7's three facts are verified before build, not assumed:** the operator's GitHub plan and required checks on a private repo · Actions minutes quota · `/install-github-app` succeeding on the hub repo. **§8 is the build:** 1. `conductor.yml` + ruleset. 2. `phase:` field + validator. 3. SessionStart hook. · **Sequencing: batch X, after lane 0.** Lane 0 is `[#692]` (`decision_coverage`), which `to-cc/AMEND-SESSION-PLAN-009.md` A9-1 places explicitly *"(before conductor E)"* — this row is one of the decisions A9-1's query must be able to see, so building the query after its first subject would leave the query unwitnessed. **Blocked on the operator's GitHub Pro**, ratified as D2: *"D2 · GitHub Pro — YES (~$4/month, account rdwornik): ... Purpose: required checks as the merge gate for conductor E (GitHub Actions)."* The ellipsis is the lane contract's own and stands for the operator's consent sentence, recorded in Polish and elided here under this lane's Done-contract clause 6 (docs in English); read it from the cited file. **The citation is corrected, and the correction matters.** D2 lives ONLY in `to-browser/RATIFICATION-2026-09-10 (1).md` (1,097 B) — a Drive-style duplicate. The 4,332 B `to-browser/RATIFICATION-2026-09-10.md` carries no D-numbering at all and lists GitHub Pro under **NOT RATIFIED**, so the two same-named transport files disagree on this row's blocker; the duplicate is the later by arrival (mtime 2026-09-11 00:27 against 2026-09-10 12:02). Read the ratification before building the gate leg; do not infer it from this row. · Done when: the four numbers of `to-cc/DECLARE-CONDUCTOR-DECISION-2026-09-09.md` §6 move, carried verbatim from that section (the source hard-wraps; the wrap is not content) — "1. Phase transitions without operator action ÷ all transitions — today ~0%, target > 80% (Actions logs). 2. Operator hours per feature landed in corp-monorepo — today ∞, target: a number. 3. Operator pastes per week — today dozens, target < 5. 4. Organs deleted from the §5 list — target ≥ 10. If 1 and 3 do not fall, revert to B. No state migration needed." · refs `to-cc/DECLARE-CONDUCTOR-DECISION-2026-09-09.md` (§3 matrix, §4 decision, §5 deletions, §6 the Done-when numbers, §7 preconditions, §8 build), `docs/audits/2026-09-10-technical-batch-w-launch-contracts/LANE-w-000-three-decisions-become-rows.md` (the in-repo frozen carriage of AW4-1 row 1, byte-identical to the transport contract), `docs/audits/2026-09-10-technical-batch-w-manifest.md` (AMEND-BATCH-W-004's declared carrier, §"AMENDED AFTER DISPATCH"), `to-browser/RATIFICATION-2026-09-10 (1).md` (D2), `docs/intake/2026-09-10-tech-review-consumption.md` (intake #91, the FILE line), `[#669]`, `[#664]`, `[#692]` · kill-candidates: none — `[#669]` owns the four delivery-loop transitions and computes state from spine edges, which is E's phase table only once E exists to fire it; `[#664]` owns the graph E would query; neither names a runner, a gate surface, or the `phase:` field, and neither can close by building `conductor.yml` · source: `to-cc/AMEND-BATCH-W-004.md` AW4-1 row 1, filed 2026-09-11 by lane `lane-w-000-three-decisions-become-rows`
