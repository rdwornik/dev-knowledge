---
id: "[#691]"
title: "Provider routing is an unordered role set with no admission gate and no licence field — a role needs an ORDERED fallback list only admitted providers may enter"
status: open
priority: P2
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#691] [P2][M] **Provider routing is an unordered role set with no admission gate and no licence field — a role needs an ORDERED fallback list only admitted providers may enter** - `to-cc/AMEND-BATCH-W-004.md` AW4-1 row 2, filed under that AMEND's own instruction that a decision is safe only once it is a row. · **What the registry holds today, measured rather than assumed.** `ecosystem/provider-registry.yaml` carries `roles:` as a per-MODEL list (`roles: [reviewer]`, `roles: [provenance]`, `roles: []` on two rows whose candidacy was refused) — a membership set attached to models, read in the model direction. There is no role-keyed entry, so nothing states an ORDER, nothing states a fallback, and a caller asking "who answers for this role, and who answers when the first is down" has no surface to read. The file's own header states the two collections it has, `providers:` and `models:`; a role entry is a third thing, not a field rename. · **Three properties are being asked for, and each one is absent for a different reason.** (a) **ORDER** — today's `roles:` is a set, and a set cannot express a fallback chain. (b) **ADMISSION** — nothing today couples registry membership to having passed a bar; `roles: []` records a refusal by convention, not by refusal. (c) **LICENCE** — the registry has **no licence or terms field at all** (grepped: zero hits for licence/license/terms), so "whose licence permits the use" is a new datum to record per provider before it can be a filter, not a predicate over data already there. Filing that as one row rather than three is deliberate: all three are legs of one role entry and splitting them multiplies a coupled edit to a single YAML shape. · **The admission bar is intake #75's and it is measurement, not argument** — `docs/intake/2026-09-06-tech-copilot-offload-role-and-account-map.md:52`, verbatim: *"**Admission is by seeded-defect measurement**, not by argument: the role is admitted if it finds seeded defects at a rate that justifies its cost, measured, not asserted."* That intake is `status: DRAFT` and in force nowhere — its own filing note says so — which is why this row's dependency is on its RATIFICATION and not on its existence. · **Locator disclosure — the dependency's citation does not resolve as written, and the substance is weaker than the citation claims.** AW4-1 row 2 attributes *"ratify the bar, not Copilot"* to an **operator packet D8**. Searched across the whole transport and the repo: that phrase appears verbatim **nowhere** except inside the AMEND and this lane's own contract, and no 2026-09-10 packet carries a D8 — every live D8 in the corpus is the unrelated intake-id-collision ruling of `DECLARE-SITTING-2026-09-06`. The substance resolves to two paths, and **both label it a browser-seat RECOMMENDATION rather than an operator act**: `to-cc/SUPPLEMENT-ANSWERS-2026-09-10.md:118` — *"`#75` (offload admission — the recommendation is to ratify the BAR, which Copilot failed; ratifying the bar is not ratifying Copilot)"* — and `to-browser/RATIFICATION-2026-09-10.md`, which lists `#75` under **"Intakes awaiting ratification"**, i.e. explicitly NOT ratified. So the blocker is real and OPEN; only its attribution to the operator was wrong. Nothing is filed here as ratified that is not. · **Sequencing: batch X.** No lane-0 ordering is claimed by the source. · Done when: carried verbatim from `to-cc/AMEND-BATCH-W-004.md` AW4-1 row 2 — "`provider-registry.yaml` gains a role entry with an ORDERED fallback list; only providers that passed intake #75's seeded-defect admission bar, and whose licence permits the use, may appear on it; the review tally records which model actually answered. RED-first test: listing a non-admitted provider fails." · refs `to-cc/AMEND-BATCH-W-004.md` AW4-1 row 2, `docs/audits/2026-09-10-technical-batch-w-launch-contracts/LANE-w-000-three-decisions-become-rows.md:67` (the in-repo frozen carriage, byte-identical to the transport contract), `docs/audits/2026-09-10-technical-batch-w-manifest.md` (AMEND-BATCH-W-004's declared carrier), `ecosystem/provider-registry.yaml` (`roles:` at :289, :306, :314, :319, :328, and the two refused rows at :347, :362), `scripts/provider_registry.py`, `scripts/check_provider_registry.py`, `tests/test_provider_registry.py`, `docs/intake/2026-09-06-tech-copilot-offload-role-and-account-map.md` (intake #75, the bar at :52, `status: DRAFT`), `to-cc/SUPPLEMENT-ANSWERS-2026-09-10.md:118`, `to-browser/RATIFICATION-2026-09-10.md` ("Intakes awaiting ratification"), `docs/intake/2026-09-10-tech-review-consumption.md` (intake #91, the FILE line), `[#676]`, `[#627]`, `[#649]` · kill-candidates: none — `[#676]` owns each provider row's non-interactive INVOCATION SHAPE and its commit-tier check, which is how you CALL a provider and not who may hold a role; `[#627]` owns agy's single `analysis` admission and names no ordering, no fallback and no licence; `[#649]` owns the reviewer tally carrying three numbers where it carries one, which this row consumes rather than replaces · source: `to-cc/AMEND-BATCH-W-004.md` AW4-1 row 2, filed 2026-09-11 by lane `lane-w-000-three-decisions-become-rows`
