---
id: "[#491]"
title: "Gemini scanning lane — ruling R-G plus an acceptance contract"
status: open
priority: P3
size: S
theme: "[E7] Tooling & evaluation"
story: "[S19] Decide the undecided artifact/tool models"
generates: BACKLOG.md
---

- [#491] [P3][S] **Gemini scanning lane — ruling R-G plus an acceptance contract** — lane is the subscribed Gemini CLI; Antigravity stays EXCLUDED pending the identity question. Acceptance is real work, never a synthetic probe: the lane runs [#487]'s ranked sheet OR the fleet dependency scan, read-only, and is spot-verified on the load-bearing rows. **Retrieval-not-classification is baked into every Gemini contract** — not a style note but a measured incident: a fan-out leg fabricated an ADR count, so a lane that classifies against a doctrine clause is outside its competence by ruling, not by preference. · Done when: `protocols/STANDING_RULINGS.md` carries the R-G ruling in a section naming `[#491]`, and one acceptance run over real work — `[#487]`'s ranked sheet or the fleet dependency scan, read-only — is recorded in a `docs/audits/` artifact naming the load-bearing rows that were spot-verified and the outcome of that verification · refs #487, ADR-105 (routine consumer declaration), protocols/PLAYBOOK.md fan-out rules · kill-candidates: none — no open row owns a non-Anthropic scanning lane · **A/B EVIDENCE 2026-08-20 — annotation, not a re-ruling:** the Gemini 3.7 Flash A/B (`docs/audits/2026-08-20-technical-gemini-ab-results.md`) is **evidence for the identity ruling** and was run **classed evidence-only** — it is NOT the acceptance run this row's Done-when requires and it discharges no clause here. **Gemini 3.7 Flash is NOT ADMITTED this run:** G1 fail, plus G2 fail once `STANDING_RULINGS` Q7 rules Φ trajectory-inclusive — the results file computes G2 PASS on the response-only reading and states at §9.2/§8.3 that the trajectory reading inverts the comparative leg to `1 <= 0`. The clean rerun follows this row's ruling, at effort tier **medium** per Q8.
