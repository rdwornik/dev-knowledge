---
id: "[#665]"
title: "Step A — the chapter map of PLAYBOOK and ARCHITECTURE, without which step B's sitting cannot happen"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#665] [P1][M] **Step A — the chapter map of PLAYBOOK and ARCHITECTURE, without which step B's sitting cannot happen** — step A of the recovery plan (intake `#89` § 4). `protocols/PLAYBOOK.md` is ~500 KB and `ARCHITECTURE.md` opens on a 246-line review prologue; the operator's keep/cut sitting (step B) is a reading job neither he nor the browser seat can do at that size, so it has not happened. **The deliverable is a map, not a rewrite:** per chapter of both files — purpose · live, dead or duplicated · the modules that implement it, named · a keep / merge / cut recommendation. **Substrate is Gemini (`agy`), read-only, because 500 KB is a reader's job and not the browser's** — and the map is an LLM's claim about the corpus until CC resolves it, so **every locator the map cites is verified against the live tree before it is trusted** (REVIEW carry 2). The measured hazards on that substrate are known and are the lane's to handle, not to rediscover: `agy` logs a workspace model that the run ignores, and an unknown model id is silently substituted rather than refused — so the run records the model that actually answered · Done when: one map file covers every chapter of `protocols/PLAYBOOK.md` and `ARCHITECTURE.md` with the four fields per chapter; every locator the map cites has been resolved by CC against the live tree, with the unresolvable ones marked as unresolved rather than dropped; the model that actually answered is recorded, not the one requested; and step B has ONE file to rule from · refs `docs/intake/2026-09-09-tech-recovery-plan.md` (intake `#89`) § 4 step A, `protocols/PLAYBOOK.md`, `ARCHITECTURE.md`, `[#666]`, `[#664]` · kill-candidates: none — no open row owns a chapter map of either file; `[#666]` is the sitting this map feeds and is deliberately separate, because one is a reading act and the other a decision · **Archived annotations:** `tasks/archive/665.md`
