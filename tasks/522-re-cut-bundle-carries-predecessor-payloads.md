---
id: "[#522]"
title: "A re-cut handoff sibling carries its predecessor's payloads — the thinner-refill hole"
status: open
priority: P2
size: M
theme: "[E2] Enforced governance"
story: "[S7] Wire up the lifecycle hooks the workflow relies on"
serialize-group: handoff
generates: BACKLOG.md
---

- [#522] [P2][M] **A re-cut handoff sibling carries its predecessor's payloads — the thinner-refill hole** — BORN at the batch-4 GO (2026-08-11); the ARC-6 carry-forward defect, which until now was **attached as evidence to `[#310]` without being in any row's contract**. `gen_handoff.py --allow-suffix` (`_resolve_bundle_dir`) writes a fresh `-2`/`-3` sibling with **FILL-IN regions re-rendered EMPTY**; the flag exists to prevent a worse failure, since silent suffixing once *"wrote into"* an existing bundle *"reproduced with real data loss"*. On 2026-08-10 a `-2` sibling nearly shipped four unfilled regions minus its predecessor's residual and was **refused by `residual_completeness`** (`86078062`) — the gate working. **But the gate catches only the EMPTY case:** `validate_residual_completeness.py:25-33` states its own limit — *"It asserts only placeholder-replaced, never value-present. A by-reference fill that names no verdict, count, or sha PASSES"* — so a region refilled **thinner** passes. Nothing names a re-cut's predecessor sibling or carries its payloads. **Ownership already adjudicated, do not re-search:** `[#422]` was checked and REJECTED as owner (its subject is cold framing prose surviving a FILLED flip — a missing detector after a fold, a different mechanism), and `[#511]`, `[#399]`, `[#298]`, `[#404]`, `[#353]`, `[#365]`, `[#366]` were each read and none owns it (JOURNAL 2026-08-10 (c)). `[#310]` keeps the evidence and does NOT gain this scope — its own Done-when is the cold-annotation surface, which does not cover this. · Done when: (a) `gen_handoff.py --allow-suffix` **refuses unless a predecessor is named** (e.g. `--carry-from <predecessor-bundle>`), so the carry is asserted at the generator where the predecessor is known rather than inferred later from byte counts; (b) a test proves the refusal — `--allow-suffix` without the predecessor argument exits non-zero, and with it exits 0; (c) a test proves the carry — a re-cut sibling's FILL-IN regions are non-empty and reference the predecessor's payloads; (d) `validate_residual_completeness.py`'s stated limit is either closed for the re-cut path or re-annotated to record that the generator now covers it · refs `scripts/gen_handoff.py` (`_resolve_bundle_dir`), `scripts/validate_residual_completeness.py:25-33`, `86078062`, [#310] (evidence home, not owner), [#422] (rejected as owner), JOURNAL 2026-08-10 (c), `docs/audits/2026-08-10-technical-batch-4-prep-evidence.md` Sheet E · kill-candidates: none — Sheet E records that `[#422]` and seven further rows were each read and rejected as owner, and `[#310]`'s Done-when does not cover the carry-forward defect · serialize-group: handoff
