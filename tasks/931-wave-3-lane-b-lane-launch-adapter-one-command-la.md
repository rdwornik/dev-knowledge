---
id: "[#931]"
title: "Wave 3 lane B (lane-launch-adapter) -- one command launches a lane, refuses a collision, never kills a run"
status: open
priority: P1
size: M
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
generates: BACKLOG.md
---

- [#931] [P1][M] **Wave 3 lane B (lane-launch-adapter) -- one command launches a lane, refuses a collision, never kills a run** - filed by LANE-W3-A per `to-cc/DECLARE-WAVE3-CONNECT-2026-09-21.md` hard precondition 2 (every wave row is filed before any other wave-3 lane fires); frozen contract `LANE-W3-B-launch-adapter.md`, branch `worktree-lane-launch-adapter`, a lane of the loop `[#929]` evaluated. Value: Yesterday two agents entered one worktree because the launcher checks nothing, every browser seat relearned the launch syntax because the launcher cannot describe itself, and the one cap-aware launcher we built would kill a lane mid-task. After this lane: one command, self-describing, collision-proof, and incapable of stopping a run. · Done when: carried verbatim from the frozen contract — 1. **`scripts/dispatch.py launch <contract>`** reads the contract's Dispatch block, runs `doit moment:pre-launch` for its slug, and on a pass spawns the provider — `claude --bg` by default, `codex exec` when the contract names it — writing a launch receipt and a job-to-lane record. On a pre-launch refusal it spawns nothing and exits non-zero with the reason. 2. **Typed domain objects:** a `LaunchRequest` and a `LaunchResult` dataclass; the result carries provider, model requested, model reported (or "not attestable" for Codex), job id, worktree. 3. **`govern` is a monitor.** It records spend against the cap field and never stops, pauses or kills a lane, under any condition, including unreadable usage. 4. **`dispatch.py --help`** describes every subcommand; the shim's help does the same. 5. **The shim** becomes a thin wrapper over `launch` with `-DryRun`, keeps `-Run` working, needs no cap argument, and is **not deployed** by this lane. 6. **Tests (common rules 1-4):** an occupied slug is refused and nothing spawns (spawn mocked at the process boundary); a free slug spawns exactly once; running `launch` twice on one contract refuses the second; `govern` over a synthetic over-cap transcript records and does not stop; the Codex path returns "not attestable" for the served model. 7. **Codex sol adversarial pass** on the `launch` interface, plus the terra review; both recorded. 8. **Handback** per common rules. · refs `to-cc/WAVE3-COMMON-2026-09-21.md`, `to-cc/DECLARE-WAVE3-CONNECT-2026-09-21.md`, `[#929]` · kill-candidates: none -- each wave-3 lane wires an organ that already exists; no open row is absorbed
