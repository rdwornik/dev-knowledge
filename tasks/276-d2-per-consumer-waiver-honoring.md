---
id: "[#276]"
title: "D2 per-consumer waiver-honoring"
status: closed
priority: P2
size: M
theme: "[E6] Cross-repo universalization"
story: "[S16] Manage the methodology as a living thing (essence lifecycle: transfer · sync · PRUNE)"
generates: BACKLOG.md
---

- [#276] [P2][M] D2 per-consumer waiver-honoring — a `.methodology.yaml` divergence-allowlist the deploy tool actually READS, on BOTH legs. PRUNE leg: once corp-monorepo records 1.2.0, a future remove-leg run (v1.3.x+) re-enters the sweep and REFUSEs on corp's consumer-owned ruff (URL-matches the ruff-gate tombstone). ADD/converge leg (FOLD 2026-07-11): `--execute` re-appends `codemap-freshness`, which BOTH live consumers legitimately exclude — re-adding it breaks them. The P4 `.methodology.yaml` allowlist is Informant-side only; neither deploy leg consults it. Build the allowlist consumed by `detect_prune` AND the add-loop (pairs with #245's last-deployed-bytes oracle); + standardize the waiver-recording convention — all sanctioned divergences declared in `.methodology.yaml`, an inline comment a courtesy duplicate only. · Done when: a consumer-declared divergence for a component causes BOTH the prune sweep to SKIP it (no REFUSE-abort) AND the add/converge leg to NOT re-append it (no re-break) on a previously-deployed consumer, with tests · refs ADR-96 amendment 2026-07-06, deploy/carrier_precommit.py, deploy/tool.py, .methodology.yaml, #245, #244, #221 · **UN-DEFERRED 2026-09-01 (architect's batch-F cut, lane L4) — on the row's OWN peg, which named exactly two triggers: *"un-defers when an arc claims the row or the operator re-prioritises it"*. BOTH fired.** An arc claims it (batch-F L4), and the basis recorded for the re-prioritisation is the operator's standing mandate that **monorepo deploy is the objective** — this row gates BOTH consumer instantiations, so it sits ON the path to that GO rather than beside it. It was deferred by the 45-day icebox sweep of 2026-08-27 (C4 §3, ruling X1's final step) as an ATTENTION decision with **no row-level dependency claimed**, which is why un-deferring needs no unblocking act — nothing was ever waiting. **The contradiction this resolves was live and measured:** ruling 3 (`docs/audits/2026-09-01-technical-ruff-gate-divergence-classification.md`) treated this row as a hard blocker on two instantiations while its own frontmatter said `deferred` — a row cannot be both the thing nothing waits on and the thing everything waits on, and `[#624]` (*nothing watches a BLOCKER's status*) is the organ that would have caught it. Build site measured: `deploy/carrier_precommit.py:777` `_classify_prune`, which returns `PRESENT_MODIFIED` (REFUSE) with no waiver input; zero occurrences of `waiver` or `methodology.yaml` anywhere in `deploy/*.py`
