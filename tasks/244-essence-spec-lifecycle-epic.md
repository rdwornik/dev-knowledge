---
id: "[#244]"
title: "Essence-spec lifecycle epic"
status: open
priority: P2
size: L
theme: "[E6] Cross-repo universalization"
story: "[S16] Manage the methodology as a living thing (essence lifecycle: transfer · sync · PRUNE)"
generates: BACKLOG.md
---

- [#244] [P2][L] Essence-spec lifecycle epic — P1 SHIPPED (feat/essence-spec-p1: manifest-v1.1.0 grew `anchors:`/`components:`/`doc_shapes:` behavior-preserving — golden-diff plan-identical, carriers deep-equal; `deploy/release_lint.py` reconciles all 5 version anchors to source_tag, C1–C7, 27 tests; lint is MANUAL this phase, wired into neither ALL_CHECKS nor preflight). Remaining phases, each gated — **P2/P3/P4 all SHIPPED 2026-07-04:** P2 PRUNE (ADR-96 remove leg; n=1 ruff-gate pruned+verified-ABSENT on ai-council v1.2.0, locally-modified REFUSED; D3=2-state active→removed; FU-1 #245/FU-2 #246) · P3 ROSTER (n=1 hub; FU #248/#249) · P4 SYNC SURFACING (Informant Tier-3 + fleet_health drift line; n=1 seb inject->DRIFT->rejected proven; HELD 1.2.0; FU #250) · P5 hub self-prune · P6 fleet (both UNOWNED — #221 closed at 8aab4356; no successor). · Done when: a tombstoned component's artifacts are demonstrably REMOVED from a consumer and verified ABSENT (leg-e mirrored), the roster regenerates without it, and per-repo drift surfaces in fleet_health · refs deploy/release_lint.py, deploy/manifest-v1.1.0.yaml, ADR-91, ADR-92, ADR-93, ADR-96, #221, #238 · **RE-CUT 2026-08-27** (K3, night-harvest adjudication; C4 census row 17) — split-and-close-the-shipped-half, nothing closed outright. **SHIPPED HALF, settled:** P1 (manifest-v1.1.0 anchors/components/doc_shapes, `deploy/release_lint.py`, C1-C7, 27 tests), P2 PRUNE, P3 ROSTER and P4 SYNC SURFACING are all SHIPPED (P2/P3/P4 on 2026-07-04), each with its own n=1 verification and its follow-ups already filed as `[#245]` `[#246]` `[#248]` `[#249]` `[#250]`. **UNSHIPPED REMAINDER, and the row is now narrowed to exactly this:** **P5 hub self-prune** and **P6 fleet** — both UNOWNED, because `[#221]` closed at `8aab4356` with no successor. Narrowed Done-when: a tombstoned component's artifacts are demonstrably REMOVED from the hub itself (P5) and then across the fleet (P6), verified ABSENT, with the roster regenerating without the component and per-repo drift surfacing in `fleet_health`. **Why this is the re-cut and not a close:** the census's own counter-case — an L-sized row with real remaining scope, where a bare kill converts two shipped-and-verified phases plus two unowned ones into two unfiled rows and loses the P1-P4 evidence chain. The epic keeps carrying its history; only its forward scope narrows. **OWED:** the `[P2][L]` band was set against six phases and now covers two. Re-sizing deliberately NOT taken — same reason as `[#171]`.
