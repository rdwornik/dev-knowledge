---
id: "[#955]"
title: "Wave 4b -- one merge path, handback as an organ, verification in the lanes (FR1-FR5)"
status: open
priority: P1
size: L
theme: "[E2] Enforced governance"
story: "[S3] Turn advisory guards into enforced gates"
implements: "ADR-120"
generates: BACKLOG.md
---

- [#955] [P1][L] **Wave 4b -- one merge path, handback as an organ, verification in the lanes (FR1-FR5)** - the epic row for wave 4b, filed by `LANE-W4B-0-hooks-rearm` per `to-cc/PLAN-WAVE4B-SESSION-2026-09-22.md` hard precondition 2 ("Row `implements: ADR-120` filed, carrying FR1-FR5") and `to-cc/WAVE4B-COMMON-2026-09-22.md` rule 1 ("Rows first, one commit"). Carries FR1-FR5 only -- FR6 (plan lint) is scoped to its own lane row `[#961]` and is not carried here, per the frozen contract `LANE-W4B-0-hooks-rearm.md` Done-contract item 1. Six lane rows implement this epic: `[#956]` (lane 0, hooks-rearm, serial-first), `[#957]` (lane 1, merge-path, FR1/FR5), `[#958]` (lane 2, handback-organ, FR2), `[#959]` (lane 3, gate-verdicts, FR4), `[#960]` (lane 4, verify-in-lane, FR3), `[#961]` (lane 5, plan-lint, FR6). Goal of the wave: the loop we test is the loop we operate, and a lane proves itself before it asks to be merged; success is measured at batch close by merge-ledger rows == merges, integrator refusals for organ-checked causes == 0, median pickup-to-push <= 30 min, and the connection walk passing `merge/gates` on main. · implements: ADR-120 · Done when: 1. **FR1 One merge path.** A production merge is `doit moment:merge` for the lane, executed in a dedicated integration worktree on a temporary branch; `main` is fast-forwarded only after the moment is green and pushed in the same step. No prose two-step exists any more. 2. **FR2 Handback organ.** A lane hands back by calling one organ that (a) runs the full self-check -- ship-gate hard-fails, ratchet, review-record consumer, branch purity (`origin/main..HEAD` contains only this lane's commits), transport-write check; (b) writes the LANE-END report, the session file at the canonical name, and the HANDBACK/STATE lines; (c) refuses with a receipt when any check fails. Nothing inter-seat is hand-written. 3. **FR3 Lane-side verification.** The lane runs impacted tests selected from its own diff on `origin/main` + lane and records the result keyed by tree sha; the integrator accepts that record when `origin/main` is unchanged, else re-runs only the impacted set. 4. **FR4 Structured verdicts.** `gates.py` and the census emit JSON per organ with all finding names; consumers (connection test, digest, integrator) read the structure, never output tails. 5. **FR5 Batch close is complete.** The batch-close digest has commits, merge sha and verdict for every lane, from organ-written data only · refs `to-cc/PLAN-WAVE4B-SESSION-2026-09-22.md` (FR1-FR6, acceptance tests, SMART goals B1-B7), `to-cc/WAVE4B-COMMON-2026-09-22.md`, `to-cc/DECLARE-WAVE4B-DIRECTION-2026-09-22.md` (root causes RC1-RC6), `docs/decisions/ADR-120-the-spine-is-the-whole-loop.md` (D7 build order this wave executes), `[#929]` (ADR-120's carrier row), `[#956]`, `[#957]`, `[#958]`, `[#959]`, `[#960]`, `[#961]` (the six lane rows implementing this epic) · kill-candidates: none -- no open row covers wave 4b's build; `[#929]` is the evaluation this wave executes, not the execution itself
