---
id: "[#610]"
title: "The night-batch protocol, named — with its two missing verbs"
status: open
priority: P2
size: M
theme: "[E7] Tooling & evaluation"
story: "[S20] Revive the nightly layer, load-gauge first (rent-rule discipline)"
generates: BACKLOG.md
---

- [#610] [P2][M] **The night-batch protocol, named — with its two missing verbs** (intake #60 / I-NIGHT, ruling X8; D6 birth funded by this lane's closure sweep) — the 2026-08-26 night ran four unattended cloud sessions and every one delivered, read-only, nothing lost. **None of it is written down as a protocol**: it exists as operator habit plus the residue in one night's files, and the cost is already visible in that same night — the harvest verb has no name (performed by hand against `GET /v1/code/sessions/{id}/events`, paginated by `next_cursor`, mechanics **rediscovered** rather than looked up); the dispatch-and-return verb has no name (`Dispatch` exists, its *deferred* form does not); and the manifest shape that made the harvest auditable was invented that night and will be reinvented the next. Ruling X8 makes the protocol doctrine: dispatch → sentinel → harvest → manifest → morning adjudication. **This row is where `[#271]`'s charter half went** when that row was re-cut 2026-08-28 — the intake brief #1 §6 constraints (~5 proposals/night cap, 7-day auto-expire, no autonomous semantic refactoring at night, proposals landing as `docs/intake/` SEEDs) travel here as the protocol's own constraints rather than living in a rival charter. · Done when: PLAYBOOK carries a named night-batch protocol section enumerating all five phases with each phase's inputs, outputs and refusal conditions; the manifest shape is specified precisely enough that two different seats produce the same sections, with the 2026-08-27 landed manifest validating against it as the reference instance; `Dispatch-After` and `Harvest-Cloud` exist as named win-tooling verbs each with a usage line and its API surface recorded, `Dispatch-After` documented as a **form of** the ruled `Dispatch` verb rather than a rival to it; the protocol states the byte-identical landing rule and the record-the-deviation rule, citing the two C-reports that opened with prose as the worked example; the report-selection rule names the Stop-hook-noise trap explicitly; and a ledger is a **required** output of the protocol, not an optional one · refs docs/intake/2026-08-27-tech-night-batch-protocol.md (intake #60, acceptance criteria 1-6 verbatim), docs/audits/2026-08-27-technical-night-harvest-manifest.md (the reference instance), docs/audits/2026-08-27-technical-night-harvest-consumption-ledger.md, protocols/STANDING_RULINGS.md X8, #271, #288
