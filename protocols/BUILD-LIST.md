# BUILD LIST

> The one flat build list of BUILD MODE (`protocols/BUILD-MODE.md` rule 1). No ids, no ADRs, no
> intakes. Row fields are fixed: `subject` · `prior-art` · `delta` · `removes` · `size` · `done`.
> `delta` is one verb from the closed list ARM · WIRE · REWRITE · MOVE · DELETE · BUILD.
> Rows filled by batch B1 (inventory only) — method, evidence and caveats:
> `docs/audits/2026-09-18-census-build-mode-b1-inventory.md`.

## Decisions (one line each, dated — rule 6)

- 2026-09-18 — BUILD MODE declared; expiry 2026-11-18; scope frozen, new items go to the END of this list.
- 2026-09-18 — B1 inventory landed: 25 rows, 0 BUILD. Rows whose delta ARMs a hook now at `stages: [manual]` would reverse the 2026-09-17 emergency order ([#863]); each needs the operator's word before B2 arms it.

## Ratchet — five numbers, weekly (a rise in ANY = that week failed, next batch is deletion only)

| week | date | ref | mechanisms | doc bytes | organs | uncalled organs | open rows |
|---|---|---|---|---|---|---|---|
| 0 | 2026-09-18 | `1a3910b0` | 171 | 43,228,069 | 66 | 36 (PROXY) | 386 |

Commands (run at the ref; WITNESSED 2026-09-18):
- mechanisms — `uv run --locked python scripts/graph_queries.py process-list --render` → sum of the `### <class> (T triggered of N)` totals (script 159 + command 10 + skill 2)
- doc bytes — `git ls-tree -r -l <ref>` filtered to `*.md`, size column summed
- organs — headline line of `ecosystem/organ-index.md` (`**N organs across M classes.**`)
- uncalled organs — PROXY: count of `- \`…\` -- no trigger` rows in the process-list render above. No organ records invocations over a 30-day window yet; B2 (`build-b2-brakes`) replaces the proxy with a counter
- open rows — `uv run --locked python scripts/gen_task_tree.py --rank`: `N open task(s) ranked` (334) + `N deferred row(s) excluded` (52)

## Rows

| subject | prior-art | delta | removes | size | done |
|---|---|---|---|---|---|
| backlog | BACKLOG.md generated by `gen_task_tree.py` from `tasks/` (code, fires) ; `validate-backlog` + `backlog-id-on-close` hooks (config, `stages: [manual]` — conductor only) | ARM | the `stages: [manual]` override on `backlog-id-on-close` and its "moved to conductor" comment | S | no |
| ADR | `docs/decisions/` + README registry (prose) ; `decision-coverage` + `amendment_coherence` checks (code, conductor-only since 2026-09-17) | WIRE | CLAUDE.md §4 prose restating "every accepted decision is implemented" once `decision-coverage` refuses locally | S | no |
| intake | `docs/intake/` ADR-98 spine (prose) ; generated Contents block ; `intake-index-freshness` hook (config, `stages: [manual]`) | WIRE | the Contents-block staleness caveat in `docs/intake/README.md` | S | no |
| docs | PLAYBOOK.md, ARCHITECTURE.md (prose) ; `check-against-spec` skill (prose, no trigger) ; `coherence-nudge` (code, non-blocking, always exits 0) ; doc_rot / doc_claims / doc_structure checks (code) | REWRITE | PLAYBOOK paragraphs describing manual doc-review steps already mechanised by check-against-spec | M | no |
| DDD/SDD/TDD | CLAUDE.md §4 ADR-108 §B (prose) ; PLAYBOOK Ch5 testing rules (prose) ; `check-against-spec` skill (prose) ; `impacted-tests-guard` (code, `stages: [manual]`) | REWRITE | CLAUDE.md §4 TDD/SDD paragraphs and PLAYBOOK Ch5 prose, folded into skills | M | no |
| folder and file naming | PLAYBOOK Ch3 naming / folder / capitalisation / root hygiene (prose) ; CLAUDE.md §4 (prose) ; dot_prefix_discipline, canonical_md_visibility checks (code) ; `validate_hermetization` (code, `stages: [manual]`) | REWRITE | PLAYBOOK Ch3 naming-rule prose, once carried as validator error text | M | no |
| Python script standards | `pyproject.toml` + `uv.lock` (ADR-106, config) ; `tach.toml` (config) ; ruff (config, `stages: [manual]`) ; library-first prose PLAYBOOK Ch3 | REWRITE | PLAYBOOK Ch3 library-first / script-standard prose, once encoded as ruff config + review skill | M | no |
| ARCHITECTURE | ARCHITECTURE.md 110,357 B vs ≤15 KB target (prose, `canonical_freshness` gated) ; codemap generated (`codemap-freshness`, `stages: [manual]`) | DELETE | narrative sections duplicating the generated codemap / organ-index / decision ledger, or describing mechanisms that do not fire (~95 KB) | L | no |
| BACKLOG | BACKLOG.md via `gen_task_tree.py --emit-source` (code, fires) ; `validate-backlog` + `row-archive-proof` (config, `stages: [manual]`) | WIRE | the "moved to conductor" override on `validate-backlog` | M | no |
| LESSONS | LESSONS.md append-only (prose) ; `prepend-order` hook covers it (code, `stages: [manual]`) | ARM | the `stages: [manual]` override on `prepend-order` for LESSONS.md | S | no |
| JOURNAL | JOURNAL.md newest-first (prose) ; `block-unanchored-push` (code, LIVE pre-push, fail-closed) ; `prepend-order` (code, `stages: [manual]`) ; Stop backpressure (code, advisory) | ARM | the `stages: [manual]` override on `prepend-order` for JOURNAL.md | S | no |
| pre-action distillation | `gen_lane_contract.py` (code) ; `/lane-boot` + `lane_boot.py` (code, operator-invoked) ; `/preflight` (command) ; `lane-contract-check` (code, `stages: [manual]`) ; `ecosystem/routing-table.yaml` (config) ; PLAYBOOK Ch8 dispatch prose | ARM | the `stages: [manual]` override on `lane-contract-check`; PLAYBOOK Ch8 contract-authoring prose once the distiller (B2) emits contracts | M | no |
| constraints | CLAUDE.md rules (prose) ; `.claude/rules/git-discipline.md` (rules with `verify:` lines, live) ; PLAYBOOK Ch2–4 (prose) | REWRITE | PLAYBOOK Ch2–4 prose duplicating rules already codified in git-discipline.md's `verify:` lines | M | no |
| dependency | `ecosystem/dependency-baseline.yaml`, `ecosystem/doc-code-edge.yaml` (config) ; FPG-1 graph `file_purpose_graph.py` / `graph_store.py` (code, fires on query) ; `graph-rebuild` (code, `stages: [manual]`) | WIRE | the `stages: [manual]` override on `graph-rebuild` | M | no |
| efficiency and performance | `resource_lifecycle.py` (code, SessionStart, live) ; `context_reclamation.py` (code, no caller) ; `ecosystem/quality-requirements.yaml` (config, `quality-requirements-freshness` manual) | WIRE | PLAYBOOK threshold / reclamation prose once `context_reclamation.py` has a caller | M | no |
| input/output | HANDOFF_PROCESS v7 (prose) ; `gen_handoff.py`, `gen_seat_boot.py` (code, fire) ; `/handoff` (command) ; `templates/` (prose) | ARM | sections of `templates/prompt-template.md` superseded by `gen_seat_boot.py` output | M | no |
| handoff | HANDOFF_PROCESS v7 (prose) ; `/handoff`, `/handoff-verify`, `/boot-session` (commands) ; `gen_handoff.py` (code) ; `check-seal-identity` (code, `stages: [manual]`) | WIRE | HANDOFF_PROCESS v7 manual-verification prose once `/handoff-verify` is a hard gate; the manual override on `check-seal-identity` | L | no |
| logs | `logs/TOKEN-LOG.md` (prose, append-only) ; JOURNAL.md ; `prepend-order` covers TOKEN-LOG (code, `stages: [manual]`) | ARM | the `stages: [manual]` override on `prepend-order` for `logs/TOKEN-LOG.md` | S | no |
| evaluation | 55 registered checks, `audit.py checks` (code) ; conductor `commit-gate` runs the full hook registry REPORT-ONLY (config header) | WIRE | the REPORT-ONLY comment in `.pre-commit-config.yaml` once conductor enforcement is armed | M | no |
| GitHub and Actions | `.github/workflows/conductor.yml` + `scripts/conductor.py` (code) ; `deploy/conductor-required-checks.ruleset.json` (config, arrives `enforcement: disabled`) | ARM | the "enforcement: disabled" caveat once the ruleset is applied | M | no |
| dispatch | `ecosystem/substrate-registry.yaml` (config) ; `/lane-boot`, `gen_lane_contract.py` (code) ; PLAYBOOK Ch8 (prose) ; `dispatch-conformance` (code, `stages: [manual]`) ; launch verb in win-tooling (`Assert-ClaudeCommand` refuses any head token but `claude`) | WIRE | the `stages: [manual]` override on `dispatch-conformance` | S | no |
| integrator | `/ship` (plugin), `/lane-integrate` (command) ; `actions_verdict.py` (code, imported by `merge_receipt.py`) ; `merge_receipt.py` (code) ; git-discipline MERGE IS ATOMIC (rule) | WIRE | PLAYBOOK Ch8 prose describing manual merge-minute recording, superseded by `merge_receipt.py` | M | no |
| merge | `merge_receipt.py` (code; imports `actions_verdict.py`, `dispatch_surface.py`, `routing_agreement.py`) ; `/ship`, `/lane-integrate` (commands) ; `block-ff-push` (code, LIVE pre-push) | WIRE | the MERGE IS ATOMIC prose duplicate in git-discipline.md once the receipt is the enforced record | S | no |
| local / cloud / Codespace substrates | `ecosystem/substrate-registry.yaml`, `provider-registry.yaml`, `routing-table.yaml` (config) ; `.devcontainer/` (devcontainer.json, Dockerfile, provision.sh) ; `provider-registry-agreement` (code, `stages: [manual]`) | ARM | the `stages: [manual]` override on `provider-registry-agreement` | M | no |
| load | `substrate-registry.yaml` concurrency data (config) ; PLAYBOOK Ch8 concurrency-ceiling prose ; `resource_lifecycle.py` (code, live) ; `context_reclamation.py` (code, no caller) | WIRE | PLAYBOOK Ch8 concurrency-ceiling prose once enforced at `/lane-boot` | L | no |
