# BUILD LIST

> The one flat build list of BUILD MODE (`protocols/BUILD-MODE.md` rule 1). No ids, no ADRs, no
> intakes. Row fields are fixed: `subject` · `context-cost` · `prior-art` · `delta` · `removes` · `size` · `done`.
> `context-cost` (added 2026-09-18, B2 order) — KB of prose a seat must read to do the row correctly
> TODAY, on top of the 43 KB boot base every seat pays (CLAUDE.md 24.6 + AGENTS.md 5.8 +
> BUILD-MODE 3.8 + this file 9.1), and whether landing the row REDUCES that number. PRIMARY sort
> key for B2, ahead of cost of inaction. Bytes are file sizes at `4377e729` (1 KB = 1,000 B); the
> read set per row is a judgement of which prose the row's prior-art obliges a seat to read.
> `delta` is one verb from the closed list ARM · WIRE · REWRITE · MOVE · DELETE · BUILD.
> Rows filled by batch B1 (inventory only) — method, evidence and caveats:
> `docs/audits/2026-09-18-census-build-mode-b1-inventory.md`.

## Decisions (one line each, dated — rule 6)

- 2026-09-18 — BUILD MODE declared; expiry 2026-11-18; scope frozen, new items go to the END of this list.
- 2026-09-18 — B1 inventory landed: 25 rows, 0 BUILD. Rows whose delta ARMs a hook now at `stages: [manual]` would reverse the 2026-09-17 emergency order ([#863]); each needs the operator's word before B2 arms it.
- 2026-09-18 — B2 ordered. `context-cost` added to every row as the PRIMARY B2 sort key. B2 order by context-cost, reducing rows first: docs 653 · efficiency 265 · ARCHITECTURE 262 · pre-action distillation 236 · integrator 214 · load 212 · handoff 200 · input/output 188.
- 2026-09-18 — The B1 order carried neither `context-cost` nor the three-repo calibration leg: the only B1 order on the transport (`DECLARE-BUILD-MODE-2026-09-18.md`) names neither, and nothing after it does. They did not run; B2 step 0 and step 2 run them.
- 2026-09-18 — Operator's word given for B2 lane 4 to ARM the COMMIT layer (reversing [#863] for that layer only), each hook with a counter and an expiry; a hook that caught nothing in its one-week window is REMOVED, not tuned. Push and CI arm later. The agent tool-loop stays empty except path protection.
- 2026-09-18 — B2 gate re-worded by the operator: mechanisms and doc bytes must NOT RISE against week zero (171 / 43,228,069) and AT LEAST ONE MUST FALL. Supersedes BUILD-MODE.md's "net growth NEGATIVE" wording for B2.

## Ratchet — five numbers, weekly (a rise in ANY = that week failed, next batch is deletion only)

| week | date | ref | mechanisms | doc bytes | organs | uncalled organs | open rows |
|---|---|---|---|---|---|---|---|
| 0 | 2026-09-18 | `1a3910b0` | 171 | 43,228,069 | 66 | 36 (PROXY) | 386 |
| B2 close | 2026-09-19 | `6fc296d8` | 171 | 43,211,457 | 65 | 90 of 159 (census; not comparable to the PROXY) | 390 (+4: [#908]-[#911], operator-ordered filings) — ROSE |

B2 close read set (step-0 method, 1 KB = 1,000 B, bytes at `6fc296d8`): boot base 43.4 → 48.1 KB (this file 9.1 → 13.7); median row read set, base-INCLUSIVE, 137.0 → 138.3 KB over 26 rows (step 0's "186 KB" added the base twice); 2 of 26 rows ≤ the 60 KB target. ARCHITECTURE 110.4 → 22.7 KB (the quality register was restored after the 15.4 KB cut) moved only the rows that read it.

Commands (run at the ref; WITNESSED 2026-09-18):
- mechanisms — `uv run --locked python scripts/graph_queries.py process-list --render` → sum of the `### <class> (T triggered of N)` totals (script 159 + command 10 + skill 2)
- doc bytes — `git ls-tree -r -l <ref>` filtered to `*.md`, size column summed
- organs — headline line of `ecosystem/organ-index.md` (`**N organs across M classes.**`)
- uncalled organs — `uv run --locked python scripts/organ_usage_metric.py census` → the `states over 30 days, of M observable: CALLED a / REACHABLE-BUT-UNOBSERVED b / UNREACHABLE c` line (B2 lane 2, replacing week 0's no-trigger PROXY; the two are not comparable). THREE STATES, never a binary (operator ruling 2026-09-19, night wave 2 L4): a transcript cannot see a hook, CI or import firing, and reachable is not called, so every two-state reading was wrong (36, 90, 13). Record all three. Which state this column ratchets on is OPEN for the operator; measured 2026-09-19 over the primary: 70 / 76 / 13 of 159, FPG-1 store reported stale
- open rows — `uv run --locked python scripts/gen_task_tree.py --rank`: `N open task(s) ranked` (334) + `N deferred row(s) excluded` (52)

## Rows

| subject | context-cost | prior-art | delta | removes | size | done |
|---|---|---|---|---|---|---|
| backlog | 181 KB (BACKLOG.md 80 + hook config 58) · reduces: no | BACKLOG.md generated by `gen_task_tree.py` from `tasks/` (code, fires) ; `validate-backlog` + `backlog-id-on-close` hooks (config, `stages: [manual]` — conductor only) | ARM | the `stages: [manual]` override on `backlog-id-on-close` and its "moved to conductor" comment | S | no |
| ADR | 210 KB (decisions README 99 + hook config 58 + PLAYBOOK Ch12 10) · reduces: no | `docs/decisions/` + README registry (prose) ; `decision-coverage` + `amendment_coherence` checks (code, conductor-only since 2026-09-17) | WIRE | CLAUDE.md §4 prose restating "every accepted decision is implemented" once `decision-coverage` refuses locally | S | no |
| intake | 131 KB (intake README 30 + hook config 58) · reduces: no | `docs/intake/` ADR-98 spine (prose) ; generated Contents block ; `intake-index-freshness` hook (config, `stages: [manual]`) | WIRE | the Contents-block staleness caveat in `docs/intake/README.md` | S | no |
| docs | 653 KB (PLAYBOOK 500 + ARCHITECTURE 110) · reduces: YES — every row citing PLAYBOOK or ARCHITECTURE shrinks; size set by what B2 lane 1 deletes | PLAYBOOK.md, ARCHITECTURE.md (prose) ; `check-against-spec` skill (prose, no trigger) ; `coherence-nudge` (code, non-blocking, always exits 0) ; doc_rot / doc_claims / doc_structure checks (code) | REWRITE | PLAYBOOK paragraphs describing manual doc-review steps already mechanised by check-against-spec | M | no |
| DDD/SDD/TDD | 65 KB (PLAYBOOK Ch5 17 + check-against-spec skill 5) · reduces: YES — its CLAUDE.md §4 bullets leave the 43 KB base every row pays | CLAUDE.md §4 ADR-108 §B (prose) ; PLAYBOOK Ch5 testing rules (prose) ; `check-against-spec` skill (prose) ; `impacted-tests-guard` (code, `stages: [manual]`) | REWRITE | CLAUDE.md §4 TDD/SDD paragraphs and PLAYBOOK Ch5 prose, folded into skills | M | no |
| folder and file naming | 58 KB (PLAYBOOK Ch3 15) · reduces: YES — Ch3 naming prose, unmeasured | PLAYBOOK Ch3 naming / folder / capitalisation / root hygiene (prose) ; CLAUDE.md §4 (prose) ; dot_prefix_discipline, canonical_md_visibility checks (code) ; `validate_hermetization` (code, `stages: [manual]`) | REWRITE | PLAYBOOK Ch3 naming-rule prose, once carried as validator error text | M | no |
| Python script standards | 78 KB (PLAYBOOK Ch3 15 + pyproject 20) · reduces: YES — Ch3 library-first prose, unmeasured | `pyproject.toml` + `uv.lock` (ADR-106, config) ; `tach.toml` (config) ; ruff (config, `stages: [manual]`) ; library-first prose PLAYBOOK Ch3 | REWRITE | PLAYBOOK Ch3 library-first / script-standard prose, once encoded as ruff config + review skill | M | no |
| ARCHITECTURE | 262 KB (ARCHITECTURE 110 + decisions README 99 + organ-index 10) · reduces: YES, ~95 KB — and ARCHITECTURE is the mandatory pre-read of every structural change (CLAUDE.md §3), so the fall lands on every structural row | ARCHITECTURE.md 110,357 B vs ≤15 KB target (prose, `canonical_freshness` gated) ; codemap generated (`codemap-freshness`, `stages: [manual]`) | DELETE | narrative sections duplicating the generated codemap / organ-index / decision ledger, or describing mechanisms that do not fire (~95 KB) | L | no |
| BACKLOG | 181 KB (BACKLOG.md 80 + hook config 58) · reduces: no | BACKLOG.md via `gen_task_tree.py --emit-source` (code, fires) ; `validate-backlog` + `row-archive-proof` (config, `stages: [manual]`) | WIRE | the "moved to conductor" override on `validate-backlog` | M | no |
| LESSONS | 101 KB (hook config 58) · reduces: no | LESSONS.md append-only (prose) ; `prepend-order` hook covers it (code, `stages: [manual]`) | ARM | the `stages: [manual]` override on `prepend-order` for LESSONS.md | S | no |
| JOURNAL | 101 KB (hook config 58) · reduces: no | JOURNAL.md newest-first (prose) ; `block-unanchored-push` (code, LIVE pre-push, fail-closed) ; `prepend-order` (code, `stages: [manual]`) ; Stop backpressure (code, advisory) | ARM | the `stages: [manual]` override on `prepend-order` for JOURNAL.md | S | no |
| pre-action distillation | 236 KB (PLAYBOOK Ch8 165 + prompt-template 24 + routing table 4) · reduces: YES — a lane reads its generated contract (a few KB) instead of Ch8; the largest per-row payoff on the list | `gen_lane_contract.py` (code) ; `/lane-boot` + `lane_boot.py` (code, operator-invoked) ; `/preflight` (command) ; `lane-contract-check` (code, `stages: [manual]`) ; `ecosystem/routing-table.yaml` (config) ; PLAYBOOK Ch8 dispatch prose | ARM | the `stages: [manual]` override on `lane-contract-check`; PLAYBOOK Ch8 contract-authoring prose once the distiller (B2) emits contracts | M | no |
| constraints | 96 KB (PLAYBOOK Ch2–4 47 + git-discipline 6) · reduces: YES — duplicated rule prose | CLAUDE.md rules (prose) ; `.claude/rules/git-discipline.md` (rules with `verify:` lines, live) ; PLAYBOOK Ch2–4 (prose) | REWRITE | PLAYBOOK Ch2–4 prose duplicating rules already codified in git-discipline.md's `verify:` lines | M | no |
| dependency | 128 KB (hook config 58 + doc-code-edge 27) · reduces: indirect — an armed graph answers "what depends on X" by query instead of reading; removes no bytes | `ecosystem/dependency-baseline.yaml`, `ecosystem/doc-code-edge.yaml` (config) ; FPG-1 graph `file_purpose_graph.py` / `graph_store.py` (code, fires on query) ; `graph-rebuild` (code, `stages: [manual]`) | WIRE | the `stages: [manual]` override on `graph-rebuild` | M | no |
| efficiency and performance | 265 KB (PLAYBOOK Ch8 165 + quality-requirements 57) · reduces: YES, small — threshold prose | `resource_lifecycle.py` (code, SessionStart, live) ; `context_reclamation.py` (code, no caller) ; `ecosystem/quality-requirements.yaml` (config, `quality-requirements-freshness` manual) | WIRE | PLAYBOOK threshold / reclamation prose once `context_reclamation.py` has a caller | M | no |
| input/output | 188 KB (HANDOFF_PROCESS 121 + prompt-template 24) · reduces: YES — superseded template sections | HANDOFF_PROCESS v7 (prose) ; `gen_handoff.py`, `gen_seat_boot.py` (code, fire) ; `/handoff` (command) ; `templates/` (prose) | ARM | sections of `templates/prompt-template.md` superseded by `gen_seat_boot.py` output | M | no |
| handoff | 200 KB (HANDOFF_PROCESS 121 + HANDOFF_BOOT 18 + handoffs README 18) · reduces: YES — manual-verification prose | HANDOFF_PROCESS v7 (prose) ; `/handoff`, `/handoff-verify`, `/boot-session` (commands) ; `gen_handoff.py` (code) ; `check-seal-identity` (code, `stages: [manual]`) | WIRE | HANDOFF_PROCESS v7 manual-verification prose once `/handoff-verify` is a hard gate; the manual override on `check-seal-identity` | L | no |
| logs | 101 KB (hook config 58) · reduces: no | `logs/TOKEN-LOG.md` (prose, append-only) ; JOURNAL.md ; `prepend-order` covers TOKEN-LOG (code, `stages: [manual]`) | ARM | the `stages: [manual]` override on `prepend-order` for `logs/TOKEN-LOG.md` | S | no |
| evaluation | 143 KB (hook config 58 + ARCHITECTURE validators chapter 25 + conductor.yml 17) · reduces: no | 55 registered checks, `audit.py checks` (code) ; conductor `commit-gate` runs the full hook registry REPORT-ONLY (config header) | WIRE | the REPORT-ONLY comment in `.pre-commit-config.yaml` once conductor enforcement is armed | M | no |
| GitHub and Actions | 67 KB (conductor.yml 17 + ARCHITECTURE automation axes 6 + ruleset 1) · reduces: no | `.github/workflows/conductor.yml` + `scripts/conductor.py` (code) ; `deploy/conductor-required-checks.ruleset.json` (config, arrives `enforcement: disabled`) | ARM | the "enforcement: disabled" caveat once the ruleset is applied | M | no |
| dispatch | 212 KB (PLAYBOOK Ch8 165 + substrate registry 4) · reduces: no | `ecosystem/substrate-registry.yaml` (config) ; `/lane-boot`, `gen_lane_contract.py` (code) ; PLAYBOOK Ch8 (prose) ; `dispatch-conformance` (code, `stages: [manual]`) ; launch verb in win-tooling (`Assert-ClaudeCommand` refuses any head token but `claude`) | WIRE | the `stages: [manual]` override on `dispatch-conformance` | S | no |
| integrator | 214 KB (PLAYBOOK Ch8 165 + git-discipline 6) · reduces: YES — Ch8 merge-recording prose | `/ship` (plugin), `/lane-integrate` (command) ; `actions_verdict.py` (code, imported by `merge_receipt.py`) ; `merge_receipt.py` (code) ; git-discipline MERGE IS ATOMIC (rule) | WIRE | PLAYBOOK Ch8 prose describing manual merge-minute recording, superseded by `merge_receipt.py` | M | no |
| merge | 49 KB (git-discipline 6) · reduces: YES, small | `merge_receipt.py` (code; imports `actions_verdict.py`, `dispatch_surface.py`, `routing_agreement.py`) ; `/ship`, `/lane-integrate` (commands) ; `block-ff-push` (code, LIVE pre-push) | WIRE | the MERGE IS ATOMIC prose duplicate in git-discipline.md once the receipt is the enforced record | S | no |
| local / cloud / Codespace substrates | 118 KB (provider-registry 47 + ENVIRONMENT 20 + two registries 8) · reduces: no | `ecosystem/substrate-registry.yaml`, `provider-registry.yaml`, `routing-table.yaml` (config) ; `.devcontainer/` (devcontainer.json, Dockerfile, provision.sh) ; `provider-registry-agreement` (code, `stages: [manual]`) | ARM | the `stages: [manual]` override on `provider-registry-agreement` | M | no |
| load | 212 KB (PLAYBOOK Ch8 165 + substrate registry 4) · reduces: YES — Ch8 concurrency-ceiling prose | `substrate-registry.yaml` concurrency data (config) ; PLAYBOOK Ch8 concurrency-ceiling prose ; `resource_lifecycle.py` (code, live) ; `context_reclamation.py` (code, no caller) | WIRE | PLAYBOOK Ch8 concurrency-ceiling prose once enforced at `/lane-boot` | L | no |
| model: ORDERED vs RAN | 49 KB (git-discipline 6; `merge_receipt.py` docstrings) · reduces: no | `merge_receipt.py` `ordered_model` / `ran_model` + `routing_agreement.compare_order` ([#752], code, fires on a lane receipt only) ; `ecosystem/routing-table.yaml` (config) | WIRE | the operator's hand check at integration: the primary / orchestrating session was ordered Sonnet and RAN OPUS twice (B1 and B2, 2026-09-18) and no receipt covers that seat | S | no |
