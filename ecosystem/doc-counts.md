# Generated doc-counts — decoupled count claims (#222)

Machine-derived count claims moved out of `ARCHITECTURE.md` so a count bump no longer trips
`canonical_freshness` A2 into forcing a `last_reviewed` re-stamp with no genuine re-read
(the 2026-07-01 forced-false-stamp failure). This file is a committed-generated fragment: it
is **not** in `audit.py` `_FRESHNESS_FILES` and carries **no** `last_reviewed` frontmatter, so
editing it never advances a freshness-gated file's commit date.

Regenerate: `python scripts/gen_doc_counts.py --write` (runs `pytest --collect-only`).
Verified against repo state by the `doc_claims` audit check (WARN-tier; ship-gate is the teeth).

<!-- COUNTS:START -->
<!-- generated by gen_doc_counts.py; do not edit by hand -->
- audit: **29 registered checks** (`python scripts/audit.py checks`)
- pre-commit gates (15) (`.pre-commit-config.yaml`)
- tests: **1592 collected** (`pytest --collect-only`)
<!-- COUNTS:END -->
