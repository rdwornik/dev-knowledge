{
  "disableAllHooks": false,
  "//hooks-RESTORED-AND-DISABLED-INDIVIDUALLY-2026-09-17": {
    "why": "Operator order 2026-09-17 (handoff cut), correcting the indiscriminate disableAllHooks of bc8ddda5/9af353f7: lane ab-808 measured which hooks wedge (docs/audits/2026-09-16-technical-lane-ab-808-guard-timeout.md, AMENDMENT 1, week to 2026-09-17, event-fired denominator). The three parity-REQUIRED hooks were not among the nine over the bar: session_end_backpressure 8% (58/725), deny_and_point 2% (182/9,382), arm_hooks not in the measured table. They are restored below and run; the nine are off individually. disableAllHooks is set FALSE here explicitly because the user-level ~/.claude/settings.json still sets it true and would otherwise suppress this file.",
    "restored": {
      "arm_hooks.py": "SessionStart, timeout 60 -- parity settings-precommit-arm; not in ab-808's measured table (unmeasured, not 'within')",
      "session_end_backpressure.py": "Stop, timeout 15 -- parity settings-stop-backpressure; 8% within bar",
      "deny_and_point.py": "PreToolUse Bash|PowerShell|Grep, timeout 10 -- parity settings-deny-and-point; 2% within bar. SUPERSEDES the 2026-09-15 //deny-and-point-DISABLED condition by operator order on the measurement: that condition (a bounded time that fails open with an in-band LOUD record) is NOT met -- the harness timeout bounds it and records hook_cancelled in the transcript only, and the bounded_hook.py wrapper is held unwired by operator ruling 2026-09-17 item 1."
    },
    "disabled_individually": [
      {
        "hook": "surface_triage.ps1",
        "event": "SessionStart",
        "wired": "repo",
        "bypassed": 252,
        "runs": 262,
        "rate": "96%",
        "owner": "[#808]",
        "reenable_when": "fixed, then `scripts/hooks/bounded_hook.py reinstate --id <hook>`, and a full 168 h window at or under the 10% bar on >= 20 runs (bar: operator ruling 2026-09-17 item 2)"
      },
      {
        "hook": "surface-closures.ps1",
        "event": "SessionStart",
        "wired": "user-level ~/.claude (hooks {} there; not wired)",
        "bypassed": 145,
        "runs": 252,
        "rate": "58%",
        "owner": "[#808] (advisory, out-of-repo)",
        "reenable_when": "fixed, then `scripts/hooks/bounded_hook.py reinstate --id <hook>`, and a full 168 h window at or under the 10% bar on >= 20 runs (bar: operator ruling 2026-09-17 item 2)"
      },
      {
        "hook": "fleet_health.py",
        "event": "SessionStart",
        "wired": "repo",
        "bypassed": 107,
        "runs": 263,
        "rate": "41%",
        "owner": "[#808]",
        "reenable_when": "fixed, then `scripts/hooks/bounded_hook.py reinstate --id <hook>`, and a full 168 h window at or under the 10% bar on >= 20 runs (bar: operator ruling 2026-09-17 item 2)"
      },
      {
        "hook": "codespace_regime.py session-start",
        "event": "SessionStart",
        "wired": "repo",
        "bypassed": 15,
        "runs": 45,
        "rate": "33%",
        "owner": "[#808]",
        "reenable_when": "fixed, then `scripts/hooks/bounded_hook.py reinstate --id <hook>`, and a full 168 h window at or under the 10% bar on >= 20 runs (bar: operator ruling 2026-09-17 item 2)"
      },
      {
        "hook": "resource_lifecycle.py session-start",
        "event": "SessionStart",
        "wired": "repo",
        "bypassed": 16,
        "runs": 63,
        "rate": "25%",
        "owner": "[#808]",
        "reenable_when": "fixed, then `scripts/hooks/bounded_hook.py reinstate --id <hook>`, and a full 168 h window at or under the 10% bar on >= 20 runs (bar: operator ruling 2026-09-17 item 2)"
      },
      {
        "hook": "billing_leak_sentinel.ps1",
        "event": "SessionStart",
        "wired": "repo",
        "bypassed": 45,
        "runs": 205,
        "rate": "22%",
        "owner": "[#863]",
        "reenable_when": "fixed, then `scripts/hooks/bounded_hook.py reinstate --id <hook>`, and a full 168 h window at or under the 10% bar on >= 20 runs (bar: operator ruling 2026-09-17 item 2)"
      },
      {
        "hook": "conductor.py session-start",
        "event": "SessionStart",
        "wired": "repo",
        "bypassed": 34,
        "runs": 199,
        "rate": "17%",
        "owner": "[#808]",
        "reenable_when": "fixed, then `scripts/hooks/bounded_hook.py reinstate --id <hook>`, and a full 168 h window at or under the 10% bar on >= 20 runs (bar: operator ruling 2026-09-17 item 2)"
      },
      {
        "hook": "propose_closures.py",
        "event": "Stop",
        "wired": "plugin tier1-lifecycle Stop hook, loaded IN PLACE from plugins/tier1-lifecycle/hooks/hooks.json in the primary checkout (debug log, session f2d6cb3c) -- the same source every repo on this machine loads",
        "bypassed": 112,
        "runs": 737,
        "rate": "15%",
        "owner": "KNOWN-UNGOVERNABLE, operator ruling 2026-09-17 -- LEFT RUNNING deliberately. Every lever is machine-wide or fleet-wide (it loads in place from plugins/tier1-lifecycle/hooks/hooks.json, which every repo on this box reads), or reds parity settings-plugin-tier1 (MUST). No per-repo option exists, so disabling it costs more than it saves. Recorded here so the next seat does not rediscover the dead end; revisit only if Claude Code gains a per-hook off switch outside the tracked settings file.",
        "reenable_when": "fixed, then `scripts/hooks/bounded_hook.py reinstate --id <hook>`, and a full 168 h window at or under the 10% bar on >= 20 runs (bar: operator ruling 2026-09-17 item 2)",
        "state": "RUNNING (not disabled)"
      },
      {
        "hook": "changelog_sentinel.py",
        "event": "SessionStart",
        "wired": "repo",
        "bypassed": 27,
        "runs": 263,
        "rate": "10.3%",
        "owner": "[#808]",
        "reenable_when": "fixed, then `scripts/hooks/bounded_hook.py reinstate --id <hook>`, and a full 168 h window at or under the 10% bar on >= 20 runs (bar: operator ruling 2026-09-17 item 2)"
      }
    ],
    "still_off_not_restored_by_this_act": "ADR-77 transcript guard (block_immutable_edits.py), prompts guard (fleet_health.py --prompts-guard, 6% within), logs_retention.py (6% within): not in the order's restore set; their return stays [#863]'s act.",
    "record_gap": "[#888]: the repo has no DEGRADED state; this per-hook list is a comment-key register, not a gate-read record.",
    "known_ungovernable": "propose_closures.py -- see its entry: over the bypass bar, measured 15%, and deliberately left running because no per-repo lever exists."
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "uv run --locked python \"$CLAUDE_PROJECT_DIR/scripts/arm_hooks.py\"",
            "timeout": 60
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "uv run --locked python \"$CLAUDE_PROJECT_DIR/scripts/session_end_backpressure.py\"",
            "timeout": 15
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|PowerShell|Grep",
        "hooks": [
          {
            "type": "command",
            "command": "G=\"$CLAUDE_PROJECT_DIR/scripts/hooks/deny_and_point.py\"; if [ -f \"$G\" ]; then python \"$G\"; else exit 0; fi",
            "timeout": 10
          }
        ]
      }
    ]
  },
  "//hooks-ALL-DISABLED-2026-09-17": "SUPERSEDED 2026-09-17 by //hooks-RESTORED-AND-DISABLED-INDIVIDUALLY-2026-09-17 (disableAllHooks false; three required hooks restored; nine measured-broken hooks off individually). Original text recoverable verbatim at 4a3f32bd:.claude/settings.json.",
  "extraKnownMarketplaces": {
    "dev-knowledge-methodology": {
      "source": {
        "source": "directory",
        "path": "C:\\Users\\1028120\\Documents\\Dev\\.dev-knowledge"
      }
    }
  },
  "//": "Project-scoped Claude Code hooks for .dev-knowledge. Merges with (does not replace) the user-level ~/.claude/settings.json hooks. uv migration (ADR-106, [#432]): python hook commands run `uv run --locked` so they execute in the declared environment (committed uv.lock); the ONE deliberate exception is the ADR-77 PreToolUse guard below, which stays on the system interpreter — it is stdlib-only and fail-closed, and a stale lockfile must never be able to block every Edit/Write call in a session. PreToolUse: the E-29/013-A prompts-dir scope guard (scripts/fleet_health.py --prompts-guard, matcher narrowed off \"*\" to the filesystem-touching tool classes by [#684]) -- ARMED 2026-09-07 in the same act as the daemon restart, never before: while the two CLAUDE_PROMPTS_DIR scopes disagree it refuses EVERY tool call with no in-session escape (measured 2026-09-06). Rationale and the two-legged design are in that module's wiring comment. [#684] 2026-09-11 -- the command now RESOLVES ITS OWN ROOT: `${CLAUDE_PROJECT_DIR:-.}` prefers the variable and falls back to cwd (hook commands run through a POSIX shell whose cwd is the project dir, measured), and `[ -f \"$g\" ] || exit 0` made a guard that cannot be LOADED pass instead of refuse -- REVERSED 2026-09-11 by AX15-1, see below. A SECOND `[ -f ]` attempt at cwd was added 2026-09-11 by the fresh Codex review of this branch (HIGH-1): `:-` defaults only on unset-or-empty, so a variable set to the WRONG root skipped the fallback and fell through to fail-open without consulting a guard that was in cwd all along (measured: old rc=0, new rc=7). THE POSTURE INVERTED 2026-09-11 (batch X lane W-2', ruling AX15-1): the command now FAILS CLOSED on every inability to evaluate -- script missing, interpreter missing, crash, any rc other than 0 -- because a guard that permits when it cannot run is declared enforcement without enforcement. Only rc=0 whose ENTIRE stdout is the guard's own PROMPTS-GUARD-EVALUATED-OK marker passes -- one trailing CR tolerated, stripped with shell builtins so an emptied PATH cannot break it, because a Python build that emits CRLF must never be able to refuse every tool call. Containment was the first form and round 2 of the same review broke it: stdout reading 'not-PROMPTS-GUARD-EVALUATED-OK-proof' satisfied it. The accidental shims never emit the token at all, so this closes the deliberate case, and it closes it for free: the 2026-09-11 fresh Codex review (HIGH-1) found the last inferred permit here -- an exit of 0 on its own is byte-identical to what a shim, a wrapper or the wrong 'python' first on PATH produces, so the hook was reading the ABSENCE of an error as proof of a check it never got. The permit is now proven, not assumed. ROUND 3 closed the mirror hole on the other verdict: rc=2 went straight to a bare exit 2, so a python shim exiting 2 -- or this file dying of SystemExit(2) above prompts_guard() -- refused the seat with an EMPTY stderr, which is MA-1's presentation exactly and was the last route by which this hook could stop a session without saying why. A refusal now must carry PROMPTS-GUARD-EVALUATED-REFUSE, a deliberately DIFFERENT token, and anything else exiting 2 gets the teaching message instead. What the impostor printed is quoted back in that message, so the reader sees the evidence rather than a verdict. NOT CLOSED, and not closable here: a shim that prints the exact marker and exits 0 still permits. The hook's only channel to the guard is an interpreter the PATH chooses, so any token the guard can emit a substitute can emit too -- there is no secret to share with a prover the attacker selects. It is also moot: whoever controls that python controls the guard's answer outright. What exists instead is DETECTION -- the SessionStart preflight prints the absolute path of the interpreter it resolved through the hook's own PATH, so a shadowed python is visible at boot. fleet_health.py's GUARD_EVALUATED_MARKER is the one home for that token and a wiring test reads it from there to assert this command still tests for it, so the two halves cannot drift apart silently. Each refusal names its CAUSE and its FIX on stderr, which is what makes this different from the MA-1 failure it superficially resembles: MA-1 refused SILENTLY and presented as the reader being broken. Two things pay for the inversion and neither may be removed without re-opening it: the MATCHER stays narrowed (the break-glass family is ungated, so a refusing guard still leaves an in-session way out), and scripts/fleet_health.py's SessionStart leg preflights interpreter + guard script once at boot, so a per-call refusal is the exception rather than the first news. AX15-2 couples the two halves: this hook block and scripts/fleet_health.py are ONE floor component, and a repo carrying the hook without the script is a fleet_parity MUST-absent (surface settings-prompts-guard-coupling), never a silent permit. CLAUDE_PROJECT_DIR is set by Claude Code and by nothing else, so the old bare form turned any other reader honouring this file (cursor-agent, measured over two paid runs) into total refusal of every tool call, presenting as the reader being broken. Nothing the guard guards is weakened -- where the script resolves it runs with full force and a mismatch still exits 2. See docs/audits/2026-09-10-technical-night-aj-m03/REVIEW.md:83 and tests/test_prompts_guard_hook_wiring.py. The MATCHER also narrowed in the same act, off \"*\" to Read|Write|Edit|MultiEdit|NotebookEdit|Glob|Grep|Bash|PowerShell|Monitor|LSP|ReadMcpResourceTool|mcp__.* -- the classes a stale CLAUDE_PROMPTS_DIR actually makes lie. The MCP and secondary filesystem routes (Monitor, LSP, ReadMcpResourceTool, mcp__.*) were added 2026-09-11 by the fresh Codex review of this branch (HIGH-2), which is right that an MCP filesystem tool could reach a stale scope without passing the guard. Its other two names are REFUSED on the merits: EnterWorktree/ExitWorktree are the break-glass family, and gating the DESTINATION of the ToolSearch escape route defeats the route as surely as gating the route itself. The refusal is NOT narrowed: a mismatch still stops the seat at its first file-touching call, which is the first thing any session does. What the narrowing buys is the break-glass: matcher \"*\" gated ToolSearch too, and ToolSearch is the only route to the deferred ExitWorktree / SendMessage tools, so the 2026-09-06 wedge could not be undone from inside the session at all (recovery took an external shell). Then the ADR-77 immutability guard (scripts/hooks/block_immutable_edits.py) blocks in-place edits of existing transcripts under docs/decisions/transcripts/** (fail-closed in-zone, fail-open out-of-zone; follows the global block-onedrive template) — refs #105. Then the [#727] deny-and-point guard (scripts/hooks/deny_and_point.py, matcher \"Bash|PowerShell|Grep\") refuses a raw search over a GOVERNED question — a pattern that resolves to a process FPG-1 already holds — and the exception text names the organ to run instead (file_purpose_graph.py why · graph_queries.py process-list · impacted_tests.py select). Fail-OPEN everywhere, which is the opposite posture to the ADR-77 guard above and deliberately so: over-blocking a search wedges every session, which is the failure mode [#727] names in its own text, so an unparseable command, an absent/stale store or any internal error ALLOWS. It stays on the system interpreter for the same reason as the ADR-77 guard (stdlib-only read path; a stale lockfile must never gate every Bash call), and the command is wrapped in an existence test so that a missing $CLAUDE_PROJECT_DIR cannot make python exit 2 — which a PreToolUse hook reads as DENY, the [#684] defect W-2' is fixing on line 21. A declared '# raw-needed: <reason>' on the command line is the one sanctioned escape. SessionStart: Tier-2 fleet-health + triage surfacing + billing-leak sentinel + changelog version sentinel (#113 — LOCAL, fail-soft: nudges /changelog-review when an installed tool version exceeds its last_reviewed in ecosystem/tool-versions.yaml; no network) + RF-2 hub self-arm (`scripts/arm_hooks.py` — idempotent, fail-soft `pre-commit install` of the 3 hook types; makes the hub apply configured->armed->proven to ITSELF, closing the configured-not-armed window a fresh clone or a deleted hook opens; quiet no-op when already armed; core.hooksPath-tolerant; Fable architecture review 2026-07-04 §4; asserted by audit.py check_hooks_armed). Tier-1 closure loop runs via the enabled tier1-lifecycle plugin (Stop->propose) + the global ~/.claude surface hook (#73 5c convergence).",
  "//deny-and-point-DISABLED": "SUPERSEDED 2026-09-17 by //hooks-RESTORED-AND-DISABLED-INDIVIDUALLY-2026-09-17 (restored on the ab-808 measurement, 2%; its re-enable condition is recorded there as NOT met). Original text recoverable verbatim at 4a3f32bd:.claude/settings.json.",
  "//hooks-EMERGENCY-DISABLED-2026-09-17": "ALL PreToolUse hooks are DISABLED 2026-09-17 by operator emergency order -- the prompts-dir guard (python scripts/fleet_health.py --prompts-guard) and the ADR-77 transcript guard (python scripts/hooks/block_immutable_edits.py) -- and the SessionStart billing-leak sentinel (powershell scripts/billing_leak_sentinel.ps1) with them. WHY: they hang with no bounded execution time and wedged SEVEN sessions for 12-18 hours on 2026-09-16/17, every one stopped at 'PreToolUse hook' or 'SessionStart hooks 9/10'. MEASURED 2026-09-17: 20 orphaned hook interpreters (.venv python.exe running fleet_health.py --prompts-guard, one running deny_and_point.py) aged up to 47 h, each with its parent shell DEAD, 0 s CPU, no image path and its single thread in Wait/Suspended -- created suspended and never resumed, so the script never ran a line; the declared 'timeout' (10 s) bounded none of them. The one live session parked at SessionStart 9/10 (lane-ab-833, started 21:02:29) matches the billing_leak_sentinel.ps1 orphan spawned in its worktree at 21:04:05. The removed blocks are recoverable verbatim from this file at the parent commit. RE-ENABLING IS CONDITIONAL, same bar as //deny-and-point-DISABLED: a hook returns only with a bounded execution time that FAILS OPEN on timeout with a loud in-band record, and only once the suspended-at-creation cause is identified.",
  "permissions": {
    "allow": [
      "Workflow"
    ]
  },
  "autoMode": {
    "classifyAllShell": true
  },
  "//autoMode": "A2 hardening on the #86 cloud-night envelope (Arc 5, architect-ruled land-now 2026-07-06; source: docs/audits/2026-07-06-changelog-review.md A2). Native auto-mode safety nets: 2.1.178 pre-launch subagent classification + 2.1.183 destructive-git blocks are automatic in auto mode; classifyAllShell (2.1.193) additionally routes ALL Bash/PowerShell through the classifier, not just arbitrary-code-exec patterns. RECONCILIATION vs the deny-first floor (reconcile-or-reject, ruled): RECONCILED as a layered ADDITIVE net — the deny-first floor (PreToolUse guards, platform guards, deny-beats-allow) stays authoritative and is not weakened; these nets fire only in auto-mode contexts (the unattended nightly), can only BLOCK more (never grant), and are inert in the operator's interactive modes. Reinforces core-invariant #3; replaces nothing.",
  "//permissions": "Cloud-night safety envelope (platform-guards model; advances #86). Pre-allows the Workflow tool. A2 layer (2026-07-06): see //autoMode — native auto-mode nets harden the unattended path as a complementary layer. RUNTIME REALITY (re-probed 2026-06-04): the native Workflow launcher is NOT enabled in the cloud context (Claude Code on the web / Routines), so the canonical cloud execution path is SPEC-ORCHESTRATION -- the nightly Routine reads .claude/workflows/conformance-hub.js as the canonical SPECIFICATION and executes its three stages via read-only Explore agents (the script's exact stage prompts + schemas; Sonnet verifiers, Opus skeptic/digest). Each nightly run ATTEMPTS the native launcher first and falls back to spec-orchestration -- a free nightly re-probe; the digest reports which path ran, and execution swaps back to native automatically when the platform re-enables it. The Workflow allow is retained for local sessions (where the launcher works) and for that cloud re-enable. NO Write/Edit deny is committed by design: deny beats allow with no path carve-outs, so a blanket deny would break BOTH local daily work and the routine's own digest write. Cloud subagent containment is instead provided by platform guards -- Routines push only to claude/* branches, every run lands in a PR (no auto-merge), the conformance review is read-only by design (schema-bound agents that never write), and the Routine prompt runs a post-run `git status --porcelain` tripwire reported in the PR/digest.",
  "enabledPlugins": {
    "tier1-lifecycle@dev-knowledge-methodology": true
  },
  "//worktree": "[#716] A lane branches from the DISPATCHING checkout's HEAD, not from origin/main. Unset means 'fresh', which binds the base to a REMOTE ref the dispatcher does not control: with anything unpushed on local main, every lane starts behind, and a generator run against a lagging base silently DROPS rows that exist on main -- the dropped row looks like a clean regeneration. Measured on lane-x-716 itself: the lane sent to fix this branched SEVEN commits behind. The operator dispatches from the primary checkout ('from the target repo root'), which sits on main, so 'head' resolves to main HEAD. It is not a guarantee on its own -- a dispatcher on a feature branch would seed lanes from THAT branch -- which is why the PROPERTY is asserted by a test rather than by this value: scripts/worktree_seed.py::base_ref_verdict, witnessed by tests/test_worktree_seed.py::test_A_LANES_BASE_EQUALS_MAIN_HEAD_AT_DISPATCH. Key shape verified against the shipped Claude Code 2.1.268 binary: NESTED under 'worktree', enum exactly {fresh|head}; a flat 'worktree.baseRef' key is ignored in silence.",
  "worktree": {
    "baseRef": "head"
  }
}
