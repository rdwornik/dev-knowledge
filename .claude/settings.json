{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "python \"$CLAUDE_PROJECT_DIR/scripts/fleet_health.py\"",
            "timeout": 60
          }
        ]
      }
    ]
  },
  "extraKnownMarketplaces": {
    "dev-knowledge-methodology": {
      "source": {
        "source": "directory",
        "path": "C:\\Users\\1028120\\Documents\\Dev\\.dev-knowledge"
      }
    }
  },
  "//": "Project-scoped Claude Code hooks for .dev-knowledge. Merges with (does not replace) the user-level ~/.claude/settings.json hooks. Tier-1 closure loop now runs via the enabled tier1-lifecycle plugin (Stop->propose) + the global ~/.claude surface hook; this file keeps only the Tier-2 fleet-health SessionStart hook (#73 5c convergence).",
  "permissions": {
    "allow": [
      "Workflow"
    ]
  },
  "//permissions": "Cloud-night safety envelope (platform-guards model; advances #86). Pre-allows ONLY the Workflow tool so cloud Routines / Claude Code on the web can launch the in-repo conformance-hub workflow -- a headless/cloud session auto-denies the Workflow tool unless pre-allowed (phase0 2026-06-03 gate-4). Additive and harmless locally (the global ~/.claude skipAutoPermissionPrompt already runs workflows prompt-free). NO Write/Edit deny is committed by design: deny beats allow with no path carve-outs, so a blanket deny would break BOTH local daily work and the routine's own digest write. Cloud subagent containment is instead provided by platform guards -- Routines push only to claude/* branches, every run lands in a PR (no auto-merge), the workflow is read-only by design (schema-bound verifiers that never write), and the Routine prompt runs a post-run `git status --porcelain` tripwire reported in the digest.",
  "enabledPlugins": {
    "tier1-lifecycle@dev-knowledge-methodology": true
  }
}
