---
last_reviewed: 2026-09-18
reconciled_with: handoff-process@7.1.0
status: active
owner: Rob
---

# Architecture — `.dev-knowledge`
<!-- scope: meta -->

> Living document — the **navigation map** of the methodology system. It charts the
> layers, organs, and flows and **points** to the ADRs/protocols/generated surfaces that
> hold the doctrine or the live inventory; it never restates what a generated surface
> already carries (resident-copy drift is the failure class this repo exists to kill).
> Fix the map when reality moves; fix the *source* when the doctrine moves.
>
> **BUILD MODE cut (2026-09-18, B2 lane 1):** was 110,357 B; target ≤15 KB. Removed: the
> hand Organ map table (→ generated `ecosystem/organ-index.md`), the per-validator prose
> roster (→ script docstrings + `audit.py checks`), the Governing-ADR recap (→
> `docs/decisions/README.md`), and prose describing a pre-commit hook as firing at commit
> time — since 2026-09-17 only `audit-index-freshness`, `organ-index-freshness`
> (pre-commit) and `block-ff-push`, `block-unanchored-push` (pre-push) run locally; the
> rest of `.pre-commit-config.yaml` is `stages: [manual]`, run report-only by conductor
> job `commit-gate`. What survives is doctrine with no other home, or a one-line pointer.
>
> **How to read this doc.** Ch1 (layers) → Ch2 (organs, now a pointer) → Ch3 (automation)
> → Ch4 (distribution) → Ch5 (zones) → Ch6 (verification mesh). **Daily working mode is
> NOT here** — worktree lanes, dispatch, the ADR-110 batch protocol: `protocols/PLAYBOOK.md`
> Ch8 "Session boundaries".

## Purpose [CORE]

`.dev-knowledge` is the universal LLM-driven development guide and methodology
framework for all `Dev/` projects. It is **Layer 2** of the ADR-28 three-layer
ecosystem model — passive storage and governance authority, not an execution engine. It
holds protocols, ADRs, intake docs, handoffs, templates, and hub-local validators,
generators and gates; it prescribes conventions child repos must follow. **Layer 2 may
not drive a CHILD repo's state** (Ch1 invariant 2).

**A fleet count is meaningless without its surface** — three nest (machine-registered ⊂
human-registered ⊂ all git repos):

| Denominator | Authoritative surface | Answers |
|---|---|---|
| machine-registered | `ecosystem/index.yaml` — derived from `ecosystem/<repo>/state.yaml`; regen via `audit.py registry update`; never hand-edit | which repos the audit machinery walks |
| human-registered | `ecosystem/registry.md` — hand-maintained | which repos the operator tracks |
| all git repos | ADR-104 | the fleet's shape as ruled |

`onboarded` is `registry.md`'s Status column — a separate axis, not a fourth denominator.
Read a count off the surface that defines it; do not restate one here.

---

## Codemap [CORE]

The canonical *code*-structure artifact. Auto-generated by
`python -m scripts.codemap.cli generate . --source-root scripts --write`; **do not
hand-edit**. The *organ* map (what fires when) is Ch2, not here.

<!-- CODEMAP:START -->
Modules (source root: `scripts/`; layer from tach.toml, `-` = unassigned):

| module | layer | path | flags |
|---|---|---|---|
| codemap | - | scripts/codemap/ | orphan |
| toc | - | scripts/toc/ | orphan |

Dependencies (`from -> to`; `[cycle]` marks an edge on an import cycle):
- (none)
<!-- generated by codemap tool; do not edit by hand -->
<!-- CODEMAP:END -->

---

## Layer Boundaries & Invariants [CORE]

**Chapter 1 — Layers & authority.** A closed loop across three actors:

| Actor | Role |
|---|---|
| L1 — browser chat | architect — analysis & design |
| Operator (Rob) | consent gate — the operator pasting a prompt *is* consent |
| L3 — Claude Code | executor — does Layer 3 work |
| **L2 — `.dev-knowledge`** | **this repo** — passive storage & governance |

Loop: L1 → Operator (prompt/handoff) → L3 executor (ratified prompt) → L2 (handoff → git
commit) → L3 (read/pull, prescribes read-only) → L1 (reflection → new chat). The
architect proposes, the operator ratifies, the executor acts only inside ratified scope.
**Ratification bandwidth is the scarce resource** every context-compressing protocol
(handoff bundles, the floor, valves) exists to spend less of.

**Invariants** (→ ADR-28, ADR-36, ADR-39, ADR-41):
1. **Layer 2 never executes** — no script here orchestrates or drives state in another repo.
2. **Validators are read-only on siblings** — `scripts/` only reads/checks/reports; the
   cross-repo `audit.py run` writes only into `.dev-knowledge` (ADR-36, ADR-69).
3. **Prescriptive authority** — PLAYBOOK + ADRs bind child repos; no local override (ADR-31).
4. **Append-only files never edited** — `LESSONS.md`, `logs/TOKEN-LOG.md` (ADR-29, ADR-39);
   LESSONS.md may relocate a byte-identical older block into `LESSONS-legacy-<span>.md`.
5. **Dated artifacts are immutable** — ADRs, transcripts, handoffs, audits (Ch5).

---

## Authority and governance [CORE]

Per **ADR-31**, `.dev-knowledge` is the binding source of cross-repo prescriptions
(Authority model 1B). Operator = ratification authority; hub = prescription authority;
enforcement is out-of-band and read-only.

- **Ratification, not adjudication.** The operator rules on *constraints, priority,
  scope*, not correctness (ADR-63); the architect surfaces trade-offs.
- **Enforcement.** `scripts/audit.py`: self-audit `health` + cross-repo `run` (manual).
  Live check registry: `python scripts/audit.py checks`.
- **Tier system retired** 2026-05-23 — the ADR-38 A5 baseline applies to every repo.
  `ARCHITECTURE.md` is mandatory for every repo (ADR-51).
- **Baseline rule:** the audit runs green on first invocation (ADR-31).

→ Full doctrine: ADR-31, ADR-36, ADR-63. Codex reviewer config: `~/.codex/AGENTS.md`
(canonical source `deploy/global-instructions-codex.md`, ADR-54).

---

## Organ map

**Chapter 2 — Organ map.** The full behavioural inventory — what fires an organ, its
layer, trigger, failure posture, current status — is **generated, not hand-maintained
here**: `ecosystem/organ-index.md`, gated by `organ-index-freshness` (one of the four
locally-live hooks). The hand table this section used to carry (35 rows vs the index's 8
classes) went stale by omission and is deleted rather than resynced by hand again. Hook
roster: `CLAUDE.md` §9.

**Failure-posture legend** (no other home): fail-closed = blocks the action; propose-only
= writes a proposal, never mutates; fail-soft = logs/exits 0, never blocks; report-only =
records the outcome, judges nothing, has no gate to arm even in principle.

**Tier-1 closure loop** (still live): commit closes `[#id]` → `Stop: propose_closures.py`
writes `logs/PROPOSALS-*.md` → `SessionStart: surface-closures.ps1` prints
`[closures] N proposed` → `/review-closures` confirms → `BACKLOG.md` updated.
Detect-and-propose only; the human gate closes (ADR-70).

**Deploy subsystem** versions the methodology corpus (ADR-91) and delivers via carrier
modules behind a per-carrier verify-gate (ADR-92); the `floor` carrier additionally arms
the ADR-78 floor (ADR-93, hash-guarded). Operator-run; runbook PLAYBOOK §20.

**Desired-state class (ADR-109) — two parts live, one retired.** `ecosystem/schema/`
(typed contract) + `desired_state_loader.py` stay read-only, operator-invoked, no gate.
`desired_state_report.py` **retired 2026-09-12** ([#734], untriggered). Evidence + a
false-PASS finding (static oracle missed an `importlib`-by-name edge):
`docs/audits/2026-09-12-technical-lane-x-734-retire-stage-2-evidence.md`.

**One-graph class — PROPOSED, NOT RATIFIED (ADR-118, `Proposed`/DRAFT, 2026-09-07).** No
row here, nothing built as a view. Landed: `scripts/file_purpose_graph.py` (FPG-1)
answers `why <path>` over corpus-structure edges, wired into **no gate, no check, no
hook**. The graph/catalog/orphan-census layers ADR-118 proposes do not exist. State gates
(JOURNAL spine-anchor, `block_unanchored_push.py`) judge commit-time acts, not corpus
edges — stay gates.

**Machinery retired (C3 sweep 2026-06-05+):** `/boot`/`/evolve` archived;
`CHANGELOG.md`/`BACKLOG_ARCHIVE.md` deleted; root `README.md` deleted then recreated
(ADR-114); `VISION.md` relocated to `docs/archive/VISION.md`. Current state: `CLAUDE.md`
§5. ADR-68's local night-agent was never registered — superseded by the cloud Routine (Ch6).

---

## Validators and enforcement (executable, here) [CORE]

Layer 2 hosts hub-local validators, generators and gates — read-only **on siblings**, not
on its own tree (`audit.py` pushes to `origin`). **Roster not restated here** — run
`uv run --locked python scripts/audit.py checks`, or read each validator's docstring.

**Gate tiering ([#597]).** Every `ALL_CHECKS` member declares commit-tier or ship-tier,
nested (commit ⊂ ship). **As of 2026-09-17 `audit-health` (the pre-commit hook) is
itself `stages: [manual]`** (conductor `commit-gate` runs it report-only) — the
commit-tier legs do not block a local commit today; they still gate at `ship-gate`
(`/ship`) and SessionStart's `fleet_health` full sweep. Basis:
`docs/audits/2026-08-27-technical-lane-nb-tiering.md`.

- `scripts/fleet_parity.py` — fleet-wide parity checker; blocking member via
  `check_fleet_parity` ([#337]). CLI: `python scripts/fleet_parity.py --run-date YYYY-MM-DD`.
- `deploy/tool.py` + carriers — deploy orchestrator (`deploy/manifest-v*.yaml`
  `carriers:`); `deploy <repo> --target vX.Y.Z` plans, `--execute` applies per-carrier
  verify-gates. Runbook: PLAYBOOK §20.
- `deploy/floor_conformance.py` (ADR-93) — proves the floor loop end-to-end. Read-only;
  hub CI + `--consumer` real-clone mode.
- `scripts/reverse_dep_oracle.py` (ADR-89) — code→code reverse-dep query via headless
  Pyright; enforcement leg is `audit.py::check_safe_removal`.
- **Graph-level conformance** (`tests/test_legibility_graph_conformance.py`) proves the
  four edge-validator types (spec→dependent, doc→code, undeclared, code↔code) each fire
  through their real entry point; code↔code is skipif-guarded on a vendored Pyright.

---

## Quality requirements

Register: `ecosystem/quality-requirements.yaml`, gated by `quality-requirements-freshness`
(commit-tier, currently `stages: [manual]`/conductor) + `tests/test_quality_requirements.py`.
A **measured** row names its organ + a RED-first trip-test; a **candidate** row has
neither. Read the live register off the YAML, not a copy here.

---

## Automation axes

**Chapter 3.** Doctrine, not restated here — PLAYBOOK "Two-tier automation doctrine":
Axis 1 LLM-judgment vs deterministic (ADR-80); Axis 2 friction cadence — Tier1
always-on, Tier2 scheduled, Tier3 episodic (ADR-70/74).

**Channels.** Cloud Routine commits nothing to `main` — pushes `claude/conformance-*` →
PR → closed, never merged (ADR-84). Local writer `audit.py::_commit_routine_outputs`
writes to `automation/fleet-audit` via `commit-tree`; `main` untouched.

**Model routing:** S=Haiku, M=Sonnet, L=Opus (PLAYBOOK Appx B, ADR-70). **The routing
table, reviewer pin and Codex config live at L0** (`~/.claude/`, `~/.codex/`), out of
this repo by ruling (architect, 2026-08-22).

**Spec-orchestration:** the cloud Routine has no native `Workflow` launcher; it falls
back to reading `conformance-hub.js` as spec by hand (re-probed nightly).

**Adoption:** tool-vs-platform / pain-owned / subscription-fit, pre-registered kill
criteria, n=2 evidence gate (ADR-74 Footnote B).

---

## Distribution and transfer

**Chapter 4.** Five channels: user-layer `~/.claude` (fleet-wide) · `tier1-lifecycle`
plugin (repo-class) · pre-commit hook export (consumer-pull) · child floor
`.claude/CLAUDE-FLOOR.md` (ADR-78) · browser boot (`HANDOFF_BOOT.md`+`PASTE_THIS.md`,
ADR-82). `deploy/tool.py` orchestrates the first four (ADR-91/92). Agents author in-hub,
distribute via `~/.claude/agents/` or the plugin; children never author. Cloud clones are
hub-independent by design.

**Execution substrates** (not a channel): `.devcontainer/` (off-machine venue, ADR-101
amend. 2026-08-18) and `deploy/lived_sandbox/` (enforcement-in-effect observer, [#252]).

`methodology_surface` whitelist/blacklist: Ch5 Zone register table, below.

---

## Key conventions & zones

**Chapter 5.** File lifecycle classes and naming conventions: `CLAUDE.md` §4; `AGENTS.md`
"File rules that will bite you" — not restated here.

**Archive-inside-folder:** terminal-status artifacts relocate byte-identical into their
own folder's `archive/` (intake CONSUMED|REJECTED; ADRs Superseded/Deprecated). Manual
today — no validator yet (BACKLOG W3 seed 2).

**`tasks/` is the source of truth; `BACKLOG.md` is generated** (ADR-107 §7.2, [#439]) —
regenerate with `scripts/gen_task_tree.py --emit-source`. `--prune` is refused:
retirement drops a task's manifest node but its file stays as the allocation record
(retire-not-delete). Enforced by `audit.py::check_task_tree_coherence`.

**Zone register:** P0 exclusion (`OneDrive - Blue Yonder`) → `block-onedrive.ps1` (global
PreToolUse). Immutable paths (`docs/decisions/transcripts/**`) → `block_immutable_edits.py`
(hub PreToolUse; currently a no-op zone). `methodology_surface` scope → `audit.py
floor_integrity` + child `.sha256` hook + `/ship` gate. All three are session/PreToolUse
— unaffected by the 2026-09-17 pre-commit strip.

Output formatting for anything copied into browser chat: `CLAUDE.md` §4 / PLAYBOOK §8.

---

## Verification mesh and decision flow

**Chapter 6.** Engine loop (doctrine, not restated): Lessons → ADR → Convention →
Enforcement → Dissemination — PLAYBOOK "The verification mesh".

**Layers, current status:**
- In-session: `verify` skill (advisory: pytest+ruff+git).
- Pre-merge: `/codex-review` + `/ship` (ship-gate, the full `ALL_CHECKS` sweep).
- Post-merge, off-host: `.github/workflows/conductor.yml` job `report-only-wall.yml` —
  re-runs `pytest`/`audit.py health`/JOURNAL-anchor on every push to `main`,
  **report-only, never blocks**; a green badge means "recorded," never "passed" — verdict
  is in the job-summary table.
- Nightly, cloud: conformance Routine (claims-vs-docs digest).
- Nightly, local: `fleet_health.py`/`audit.py run` → `surface_triage.ps1` + human triage.

Cross-tier disagreement is expected, not a bug — PLAYBOOK "What each tier checks".

**Nightly outcome loop, current status:** digest producer (cloud Routine) **alive**;
triage/Issue producer **dead since 2026-07-09** (`82227f08`); consumer
`surface_triage.ps1` **alive but reading a fossil** (15 stale `nightly-triage` Issues).
Status: ARMED (stale input). Tracked as [#428].

**Deployed-version record:** `ecosystem/deployed-versions.yaml`, written only by the
deploy runbook's verify-gate; read by `fleet_health`.

**Requirements intake:** ADR-98 — `docs/intake/` → ratification → BACKLOG row.

**Decision flow:** Council debate → ADR → BACKLOG row → enforcement organ. ADR-68's
status and real replacement: Governing ADRs, below.

---

## Governing ADRs

Full ledger + editorial one-liners: `docs/decisions/README.md`. Most recent five:
`CLAUDE.md` §11 (machine-enumerated, regenerated on every ADR add/status change).
Not restated here — a curated recap of a generated ledger is the exact drift class
this rewrite removes.
