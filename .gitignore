# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environments
.venv/
venv/

# Environment
.env

# Claude
.claude/settings.local.json

# Session-end closure proposals (ADR-70 Tier-1; ephemeral, reviewed at next /boot —
# the durable record is the eventual `closes [#N]` commit + JOURNAL, ADR-65)
logs/PROPOSALS-*.md

# Fleet-health digest (ADR-70 Tier-2; daily cross-repo audit snapshot, auto-refreshed)
logs/FLEET-HEALTH.md

# OS
*.tmp
*.bak
