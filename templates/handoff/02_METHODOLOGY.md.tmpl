<!-- HANDOFF v4 bundle template — 02_METHODOLOGY. Generated at Phase 2. Budget ≤200 lines.
     Pulled FROM source (PLAYBOOK + ESSENTIALS) at handoff time — do NOT hand-maintain.
     Each {{PULL}} marker => condense the named section to operative rules, not a full copy.
     v4.4 (§E): emit the ===== FILE: 02_METHODOLOGY ===== separators below as the
     literal first/last lines of the generated file (this header comment is stripped). -->

===== FILE: 02_METHODOLOGY — start =====

# 02 · How we work

Methodology floor for `{{CONTEXT: repo}}`. The authoritative sources are
`protocols/PLAYBOOK.md` and `protocols/ESSENTIALS.md` in `.dev-knowledge`; this is a
working extract generated from them at handoff time. When in doubt, the live files
win.

## Model selection

{{PULL: ESSENTIALS#How Claude thinks}}
<!-- Extract the model/tier rule: Tier 1 (Sonnet) for ≤5 files / existing pattern /
     automated checks; Tier 2 (Opus) for new abstractions / cross-module / unfamiliar
     APIs / security. Trivial (<60s manual) → no AI. -->

## Prompt format

{{PULL: PLAYBOOK#Writing prompts for Claude Code}}
<!-- Extract the standard 8-section prompt structure + delivery format + pre-send
     checklist essentials, INCLUDING the "hooks/commands in play" pre-flight line
     (which hooks auto-fire vs which commands to invoke — see PLAYBOOK §"Usage
     protocol: which command / hook, when"). This subsumes the v3.4 "Prompt
     Generation Card". -->

## Hooks & enforcement

{{PULL: PLAYBOOK#LLMs advise; hooks/tests enforce}}
<!-- Extract: LLMs advise, hooks/tests enforce. pre-commit auto-fires
     normalize-dated-headers + codemap-freshness + validate-backlog + audit-health
     (audit.py health — FAIL blocks the commit) + backlog-id-on-close; ruff is NOT
     wired as a hook (run manually — BACKLOG #13). Executable rules live in ~/.claude
     with verify: lines, not in prose. -->

Codex is a separate code-review CLI (per ADR-54, configured globally at
`~/.codex/AGENTS.md`), not Claude Code. Acts as a second pair of eyes on 3+ file or
safety-critical changes — distinct system, used in addition to CC's own work.

## AI Council process

{{PULL: ESSENTIALS#Architect routing for technical proposals}}
<!-- Extract: when to convene Council (architecture/ADR-level decisions), how
     transcripts return to docs/decisions/transcripts/. Architecture decisions are
     NOT made unilaterally. -->

## Conventions that bite

{{PULL: PLAYBOOK#Repo conventions}}
<!-- Extract the operative conventions: Conventional Commits; branch naming
     (feat/fix/docs/chore off main); file naming (UPPERCASE living docs, ADR-NN,
     YYYY-MM-DD slugs); append-only files (LESSONS, JOURNAL newest-first); immutable
     ADRs/handoffs/audits; output the operator copies back is flat + code-fenced so
     the TUI doesn't paint box-drawing (CLAUDE §4 render-layer). -->

## Four-tag discipline canonicity

Four-tag discipline (witnessed/recall/inferred/unknown) is canonical per
HANDOFF_PROCESS v4.3 Amendment A. The §3.1 three-tag text in the spec body is
**superseded** — amendment-precedence applies. Bundle generation (Phase 2)
enforces four tags; spec consolidation at v5 will fold amendments into the body.
The definitions appear in `04_RECENT.md` of every generated bundle (so you don't
need to read the spec to apply the discipline). This is enforced **syntactically**
at lint time by `audit.py` check #9 — the lint verifies §3.1 has either all four
canonical tags enumerated OR an explicit pointer to Amendment A. **Check #9 does NOT
catch mis-labeled tags** (a `witnessed` claim that should have been `recall` passes
the lint). Semantic accuracy of tag application requires sage discipline + Phase-2
verification cross-checking.

## Bundle maintenance during session

If you discover drift between this bundle and repo state during your work — flag
to Rob, JOURNAL the discovery, and amend the bundle's `04_RECENT.md` Load-bearing
facts table via append (don't rewrite). The bundle is living until the next
handoff. This implements the "handoff is back-and-forth, not unilateral guess"
rule in PLAYBOOK methodology.

## Session lifecycle

{{PULL: ESSENTIALS#Starting a Session}}
<!-- Extract: /boot, git status clean, read recent handoff/JOURNAL, scope at start,
     stop-signs, clean tree at session end. -->

===== FILE: 02_METHODOLOGY — end =====
