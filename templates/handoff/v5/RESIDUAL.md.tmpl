<!--
  RESIDUAL.md.tmpl — v5 residual scaffold (HANDOFF_PROCESS §2/§6/§13). Rendered by
  scripts/gen_handoff.py. Region classes as in HANDOFF_BOOT.md.tmpl: {{TOKEN}} spans are
  re-substituted every render; FILL-IN:<name> comment regions are hand-authored and copied
  byte-for-byte from the on-disk file on re-render (RF-6 — the generator never narrates).

  ANSWER-FREE (RF-1 option b): §1 carries the drift-flag FRAMING ("re-derive; the teeth are
  in PROBES") but NOT the values — the ship-gate verdict, the dispositioned-WARN count, the
  [stale] status, the drifted #ids, the counts are ALL live probe answers (P4/P6/P7/P9) and
  live only in the JOURNAL generation-entry, never here.

  BYTE BUDGET — ~8,000 BYTES (v7.1). This is the ONE hand-authored term in the assembled paste,
  and after the v7.1 cut it is the LARGEST term in it. The budget is a SUBTRACTION, not a
  preference, and it is stated as a derivation rather than a frozen number so it cannot go stale
  the way a typed count does (CLAUDE.md §4):

      residual budget = PASTE_BYTE_CEILING - (boot session-header + PROBES.md) - overhead

  where the ceiling is `assemble_paste.PASTE_BYTE_CEILING`, the two template-controlled sections
  are what THIS bundle's own `PASTE_THIS.md` reports for them, and overhead is the role PIN + the
  END sentinel + section separators (~400 B). Recompute it for any bundle instead of trusting a
  number here -- `scripts/assemble_paste.py <bundle-dir>` prints the assembled total, and the
  per-section split is readable from `PASTE_THIS.md`'s `=== <label> ===` markers. Measured once,
  on the v7.1 cut (2026-09-07, architect mode): session-header 3,079 B + PROBES 8,707 B = 11,786 B,
  plus ~351 B of PIN and sentinel, leaving ~7,860 B. Round DOWN, not up: 8,000 is the ceiling to
  aim under, not a floor to fill.

  A residual over budget does not fail a gate today -- `assemble_paste` WARNs on the assembled
  total -- but it is the term that puts the paste over, and the seat pays for it on every turn of
  the window (inbox 030).

  WHERE THE BYTES GO WHEN IT IS OVER: re-narrated task-state (§6 — point at BACKLOG.md), and
  re-transmitted methodology (§3 — point at the protocol). Those are the two §2 prohibitions,
  and a residual over budget is usually one of them wearing a local name. Trim THOSE first,
  before trimming the un-committed "why", which is the only content here the repo does not
  already hold and the only reason this file exists.
-->
# Residual — {{SLUG}} — the part the repo does not already encode

<!-- scope: meta -->

> **What this is (§2).** CC's handoff is **only the residual** — the un-committed "why," the
> pointers, and the **drift-flags as the headline**. It does **not** re-transmit methodology
> (pointer + mechanical enforcement, §3) or re-narrate task-state (pointer to `BACKLOG.md`, §6).
> Mode: **{{MODE}}** (§13).
>
> **Four-tag discipline.** `witnessed` = CC re-derived it live this generation; `recall`/`inferred`
> = reconstructed from the JOURNAL/git window, may have moved (the load-bearing ones are re-checkable
> via `PROBES.md`); `unknown` = stated as such.
>
> **Supplement fill-state:** stated once, in this bundle's `HANDOFF_BOOT.md` session header
> ([#611] — not duplicated here).

---

## §1 — Drift-flags (THE HEADLINE — surfaced first, not buried)

Produced by the live read-only drift-checks (`audit.py ship-gate`, `validate_git_backlog`,
`validate_doc_claims`, `validate_backlog`). **Re-derive each at read-time — the teeth are in
`PROBES.md` (P4/P6/P7/P9), not in trusting these lines.** This bundle states **no** ship-gate
verdict, WARN count, `[stale]` status, drifted `#id`, or count — those are the probes' live answers.

{{STANDING_VS_NEW}}

<!-- FILL-IN:driftflags START (hand-authored — ONE judgment only: name which NEW flag above is a DECISION rather than a defect. The standing-vs-new attribution is GENERATED directly above; do not restate it. Do NOT state the ship-gate verdict, the WARN count, the [stale] status, or any drifted #id/sha — those are P7/P4's LIVE answer; naming a value here re-inverts the anti-bluff contract.) -->
_(fill: of the NEW-and-undispositioned organs listed above, name which is a DECISION rather than a defect, and why. Nothing else belongs here — the standing-vs-new split is generated. State no verdict/count/[stale]/sha value; P7/P4 re-derive those.)_
<!-- FILL-IN:driftflags END -->

---

## §2 — Shipped this window (the map — pointer, not re-narration)

<!-- FILL-IN:shipped START (hand-authored — a terse map of what shipped, by #id + ADR, pointing at BACKLOG/JOURNAL; NOT a detailed recap JOURNAL already encodes, §2/RF-6) -->
_(fill: terse shipped-this-window map — `#id`s + ADRs, one line each, pointing at `BACKLOG.md` / `JOURNAL.md`. Not a recap; the JOURNAL already encodes the detail.)_
<!-- FILL-IN:shipped END -->

---

## §4 — Next-frontier decisions (the design "why" that travels)

<!-- FILL-IN:frontier START (hand-authored — the open architecture questions + decision context the next session must resume rather than rediscover; the residual's core payload in architect mode) -->
_(fill: the open design questions / next-frontier decisions the next session should resume — the un-committed "why." This is the residual's core payload; the generator cannot author it.)_
<!-- FILL-IN:frontier END -->

---

## §6 — Task-state (pointer, not narration)

The **BACKLOG is the spec; items are tickets.** Task-state is: a pointer to `BACKLOG.md`, the live
in-progress branches (`git branch -v`), and any **drift-flag** `validate_git_backlog` raises (§1 /
`PROBES.md` P4). Re-narrating item text splits the truth and drifts — the pointer + the drift-flag is
the whole task-state.
