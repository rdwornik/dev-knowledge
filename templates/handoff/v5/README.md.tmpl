<!-- HANDOFF v5 — runbook source template (DEFERRED STUB).

     v5 bundles no longer carry a per-bundle README. The canonical operator runbook is a single
     per-repo file at `docs/handoffs/README.md` (generic across repos of the same handoff version).
     The session-specific header (slug · purpose · mode) lives in each bundle's `HANDOFF_BOOT.md`.

     This file is intentionally NOT the runbook content — keeping a second hand-synced copy here
     while the generator is deferred would reintroduce exactly the drift this redesign removed
     (two copies, no sync mechanism).

     TODO(#164): when per-repo seeding is built, extract the generic runbook source HERE and have
     the generator render/update each repo's `docs/handoffs/README.md` idempotently
     (write-if-absent-or-version-changed). Until then, the canonical content lives at
     `docs/handoffs/README.md` and is the single source of truth.
-->
