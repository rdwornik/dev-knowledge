<!-- HANDOFF v4 bundle template — README (operator-facing). Generated at Phase 2.
     Markers: {{CONTEXT: x}} substitute captured value; {{PULL}}/{{SYNTHESIZE}} n/a here.
     This file is NOT pasted into the new chat — it is the operator's runbook, and is
     therefore EXEMPT from the v4.4 §E ===== FILE ===== separators (those wrap 01–07,
     the files that are pasted). -->

# Handoff — {{CONTEXT: slug}}

**Generated by HANDOFF_PROCESS v4.4 (status: live)**
<!-- v4 bundles come from the FROZEN v4.4 template — its version is fixed, so the stamp is
     literal v4.4/live, NOT the {{VERSION}}/{{STATUS}} resolver (which now reads the v5
     canonical spec header). Pinned by fix/restore-v4-handoff-templates (2026-06-12) after
     the #149 flip archived these templates prematurely; retires with the templates when
     #164 (repo-parameterized v5 cross-repo generator) lands. -->


**Repo:** {{CONTEXT: repo}}  ·  **Type:** {{CONTEXT: type}}  ·  **Generated:** {{CONTEXT: date}}
**Repo state at generation:** branch `{{CONTEXT: branch}}`, HEAD `{{CONTEXT: head_sha}}`

This bundle onboards a **new chat** to continue work on `{{CONTEXT: repo}}`. It is a
teaching sequence, not a file dump: the new chat learns its role, our methodology,
the project, what just happened, and what to do now — then proves it understood
before touching anything.

Files `01`–`07` are the teaching sequence (paste them to the new chat). This
`README.md` is for you only — do not paste it.

## Paste sequence + escalation ladder

```
Step A: Paste 01_ROLE + 02_METHODOLOGY + 03_PROJECT + 04_RECENT + 05_NOW
        as ONE message into the new chat. KEEP the ===== FILE: NN — start/end =====
        separators around each file (they make the boundaries salient — v4.4 §E).
        Wait for acknowledgment.
Step B: Paste 06_QUESTIONS. Read the chat's answers — and CHECK each answer names
        the bundle file + section it draws from (v4.4 §C). An answer with no source
        reference fails, however well it reads; treat it as a comprehension FAIL.

IF comprehension FAILS:
  Tier 1 — Re-paste the specific file(s) the chat got wrong + "read again carefully".
  IF still fails:
  Tier 2 — Ask Claude Code to verify the specific facts against repo state;
           paste CC's findings to the new chat.
  IF still fails:
  Tier 3 — ABORT onboarding. Reactivate the sender chat, or do a manual context dump.

IF comprehension PASSES:
  Step C: Paste 07_ASK_BACK. Answer the chat's questions from memory or the sender chat.
  Step D: The chat begins work.
```

## Drift cross-check

{{CONTEXT: drift_notes}}
<!-- Phase 2 fills this from the cross-check of the sender's load-bearing claims
     against actual repo state. Placed here (before the bundle contents table) so
     the operator sees drift on first read, not buried at the end (v4.2 item C).
     - No drift: "**No drift detected** — all sender claims verified against repo
       state at Phase 2."
     - Drift found: summarize the mismatches here (claim → repo fact); the full
       per-claim table with verification commands lives in 04_RECENT.md.
     Graceful degradation (§9) also surfaces here when a source file was missing:
     e.g. "03 built without VISION.md — target repo lacks one." -->
{{CONTEXT: degradation_notes}}

## Bundle contents

| File | Purpose | Budget |
|---|---|---|
| `01_ROLE.md` | Who the new chat is; who Rob is | ≤100 |
| `02_METHODOLOGY.md` | How we work (PLAYBOOK pointers + key extracts) | ≤200 |
| `03_PROJECT.md` | Vision + sacred files | ≤150 |
| `04_RECENT.md` | Narrative of recent work | ≤250 |
| `05_NOW.md` | Top P1s + in-progress branches | ≤100 |
| `06_QUESTIONS.md` | Comprehension check (7 Q + fixed first-move Q8) | ≤80 |
| `07_ASK_BACK.md` | New chat's question slot (max 3) | ≤50 |
